{
  "name": "@pevo/contracts",
  "version": "0.1.0",
  "description": "Shared TypeScript type definitions for PEvO API, Hive schemas, and domain models",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
