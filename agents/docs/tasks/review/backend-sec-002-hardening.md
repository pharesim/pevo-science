# SEC-002-HARDENING — Post-review hardening of /api/orcid

**Owner:** Backend Agent
**Priority:** P2
**Created:** 2026-04-21

## Status

All 6 items landed at commit **0e4241b** ("harden /api/orcid state consume, envelope, TOCTOU cache, prod warn (SEC-002-HARDENING)"). 14/14 `orcid.test.ts` pass (9 pre-existing SEC-002-BE + 5 new hardening). Full backend vitest 239 pass + 1 skipped; 2 `hafsql.test.ts` ECONNRESETs under concurrency, pass in isolation (infra flap, unrelated to this commit).

- **#1 state-consume inside try/catch** — `backend/src/routes/orcid.ts:185-194`. `redis.del`/`orcidStates.delete` now sits inside the outer try wrapping the token-exchange dispatch; a Redis DEL throw maps to 500 via the existing catch. Did NOT use `redis.getdel` — would break #3's state-not-consumed-on-403 contract.
- **#2 NO_ACCOUNT envelope fix** — `handleLogin` now emits `sendError(res, 404, 'NO_ACCOUNT', '...', { orcid_id })` so the frontend `ApiRequestError` parser receives `orcid_id` under `error.details`. Required adding `details?: Record<string, unknown>` to the `ApiError.error` shape in `backend/src/types/api.ts` and a `details` parameter on `sendError` in `backend/src/response.ts`.
- **#3 state-not-consumed-on-403 contract** — code-side contract enforced by the #1 move (consume fires only when auth passes). See **[TODO Architect post-fix]** below for the orcid.md prose.
- **#4 `orcid-link.spec.js:107-115` test.fixme** — implemented the two-browser-contexts 403 test (`frontend/tests/e2e/orcid-link.spec.js:107-176`); hits the API directly across two `browser.newContext()`s, asserts 403 FORBIDDEN. Falls back to `test.skip` with a concrete citation when ORCID is unconfigured in the test environment.
- **#5 HAF-lag TOCTOU mitigation** — `${config.appTag}:orcid_binding:${orcid_id}` EX 120s/value=username, written after the successful broadcast in both `handleAccredit` and `handleLink`. `findAccreditedAccountWithOrcid` consults Redis first and short-circuits when `value !== candidateUsername`. Redis outage degrades gracefully (falls back to the HAF-only path). NOTE: same-tick concurrent races remain — see SEC-002-TOCTOU-LOCK Pending follow-up.
- **#6 production multi-process startup check** — new `backend/src/startup-checks.ts` `checkOrcidProcessSafety()`, wired from `backend/src/index.ts` post-listen. Fires a loud `logger.warn` 5s after boot under `NODE_ENV=production` when Redis is not ready, calling out single-process-only `orcidStates` fallback as a multi-process/PM2/clustered-deploy breakage risk.

## Architect re-review (2026-04-21c) — HELD PENDING FIXES

Round-2 `/ce-code-review` (correctness/security/reliability/testing/maintainability/project-standards/kieran-typescript) on commit `0e4241b` confirmed the 6 landed items work as designed. One P2 finding extends Item 1's promise; remaining items dismissed or split to follow-ups.

1. **P2 — State-read still outside try/catch** (`backend/src/routes/orcid.ts:151`). Item 1 wrapped the Redis DEL inside the try/catch around the token-exchange dispatch, but the upstream `redis.get(stateKey)` at line 151 and `authenticateRequest` at line 177 remain outside. A transient Redis flap on the GET (or auth dispatch) escapes as an unhandled rejection — the exact failure mode Item 1 promised to close. Fix: widen the outer try to encompass the GET + the auth-check block, mapping any throw to 500 INTERNAL_ERROR with the same message shape the DEL-throw path now produces. Single-file edit in `orcid.ts`. Add one test: `redis.get` mocked to throw → 500 INTERNAL_ERROR with `state` NOT consumed (matches the state-not-consumed-on-403 contract for symmetry on infrastructure errors).

**Split to Pending follow-up: SEC-002-TOCTOU-LOCK (P2).** Round-2 also confirmed Item 5's narrowing of the HAF-lag TOCTOU window does NOT close the same-event-loop-tick race (cache write is post-broadcast, two concurrent same-orcid-id requests both broadcast). Fix is bigger surgery (SETNX lock semantics + outage fallback story) than fits in this task's scope; filed as Pending P2 with concrete shape.

**Dismissed from round-2 findings (architect review):**
- **(P3) No revoke-side cache invalidation, false 409 within 120s window if a different user tries to bind a just-freed ORCID:** revokes happen via on-chain `custom_json` (no PEvO endpoint to hook); the right shape is shorter TTL or accept the bounded window. 120s is acceptable for beta — revoke-then-rebind is a rare flow.
- **(P3) `error.details` widening unread by frontend:** grepped `frontend/src/` for top-level `orcid_id` consumers of NO_ACCOUNT — only hit is `orcid-callback.js:118` which reads `err.code === 'NO_ACCOUNT'` and uses a localized message, no field-level read. Move from top-level to `error.details` is end-to-end inert.
- **(P3) `setTimeout(5000)` startup check robustness:** plausible improvement (subscribe to the Redis client's `ready` event instead of fixed timeout) but no real failure motivating the change. Defer to a real incident.
- **(P3) `@ts-expect-error` in test:** cosmetic.

**[TODO Architect post-fix]** — `agents/docs/api-contracts/orcid.md` doc updates from the original status block, deferred to atomic archive once the state-read widening lands. Items 2/3 are stable and won't change shape further:

1. Under `POST /api/orcid/callback`, document the state-not-consumed-on-403 contract: "On a 403 FORBIDDEN response from authenticated modes (caller username does not match the initiator stored at /start), the OAuth `state` parameter is intentionally NOT consumed. The legitimate initiator can retry `/callback` with a valid bearer without being forced back through the ORCID OAuth redirect. State is consumed only after auth passes, or after any success or error on unauthenticated modes."
2. Update the NO_ACCOUNT response example: `orcid_id` now lives under `error.details`, not at the top level. Shape: `{ "status": "error", "error": { "code": "NO_ACCOUNT", "message": "...", "details": { "orcid_id": "0000-..." } } }`.
3. Optional: add a one-line note in `common.md` documenting that `error.details` is the canonical channel for error-context fields (mirrors the generic `ApiError.error` shape change in `backend/src/types/api.ts`).

**Path to archive:** (1) Backend agent applies finding #1 (try/catch widening + one test). (2) Architect re-reviews round-3 with `/ce-code-review`, lands the deferred orcid.md updates, archives.

## Backend re-review signal (2026-04-21, commit `ab2baaf`)

Finding #1 landed. Ready for architect round-3 re-review.

- `backend/src/routes/orcid.ts` `POST /api/orcid/callback`: outer try/catch widened to encompass the upstream `redis.get(stateKey)` + the `authenticateRequest` dispatch (previously only wrapped the state-consume DEL + token-exchange). Any infrastructure throw on the state-read or auth path now maps to 500 INTERNAL_ERROR via the existing catch with the same message shape the DEL-throw path produces. State is not consumed when the throw fires on the read (symmetric with the 403 state-not-consumed contract). Two stale rationale comments consolidated into one block above the try.
- `backend/tests/routes/orcid.test.ts`: one new spec in the `SEC-002-HARDENING` describe block — "returns 500 when redis.get throws while reading state (state-read is inside try/catch, state not consumed)". Spies `redis.get` to throw once, asserts `redis.del` never called with `stateKey`. Skips when Redis unavailable (Map.get can't throw). Pattern matches the pre-existing Item 1 DEL-throw test.
- Verified: 15/15 pass in `orcid.test.ts`; typecheck clean.
- [TODO Architect] orcid.md doc updates from original status block (state-not-consumed-on-403 contract + NO_ACCOUNT `error.details` shape + optional common.md note) remain deferred to atomic archive.

## Architect re-review (2026-04-21, round-3) — HELD PENDING FIXES

Round-3 `/ce-code-review` on commit `ab2baaf`. The round-2 hold-block widening (try/catch encompasses state-read + auth + DEL + token-exchange) landed correctly: 400 BAD_REQUEST path on `storedMode=null` still fires via normal early-return (not catch); 403 state-not-consumed preserved because `sendError(403) + return` exits before DEL; DEL throw still catches as 500 consistent with round-1. Round-3 surfaced a defense-in-depth gap and 2 test-coverage gaps adjacent to the commit's stated behavior.

1. **P2 — `sendError` has no `res.headersSent` guard** (correctness COR-005, 0.75). `backend/src/response.ts:19` calls `res.status(httpStatus).json(...)` unconditionally. The widened try now wraps `authenticateRequest` (orcid.ts:184), which internally uses `verifyHiveSignature`. If `verifyHiveSignature` ever both writes a response AND throws (currently not reachable per the promise/finish-listener structure, but not structurally enforced), the outer catch fires `sendError(res, 500, ...)` on an already-responded res, Express logs "Cannot set headers after they are sent", the response stream is corrupted. Fix: add `if (res.headersSent) { logger.warn({}, 'sendError called after response sent'); return; }` at the top of `sendError` in `backend/src/response.ts`. Defense-in-depth; closes any future expansion of the pattern (e.g. the SEC-AUTH-BYPASS and SEC-002-TOCTOU-LOCK catch blocks that also widen try/catch around middleware). Architect-owned file, so backend agent must flag the edit via `[TODO Architect]` or the architect lands it during re-review.

2. **P2 — Test uses `mockImplementationOnce` on `redis.get` — call-order-dependent, not key-targeted** (testing T-001, 0.88). The new "redis.get throws" spec at `orcid.test.ts:413-431` assumes the first `redis.get` call on the singleton is the stateKey read. Works today coincidentally (the throw exits the try before other `redis.get` calls happen). If a future change adds a `redis.get` upstream of the stateKey read (e.g. a per-request session lookup), the mock silently intercepts the wrong call and the test either passes for the wrong reason or fails for a confusing reason. Fix: swap to `mockImplementation(async (key) => { if (key === stateKey) throw new Error(...); return origGet(key); })`. Key-targeted, refactor-stable. ~6 lines.

3. **P3 — `authenticateRequest` throws → 500 path untested** (testing T-002, 0.85). The widened try wraps auth. The commit message names "auth dispatch error" as a covered path. No test exercises `verifyHiveSignature` synchronously throwing before `sendError + resolve`. Fix: one spec mocking `verifyHiveSignature` (or the underlying redis replay-cache it depends on) to reject synchronously for an authed-mode callback. Assert 500 INTERNAL_ERROR + `delSpy.mock.calls.map(c => c[0])` does NOT contain `stateKey` (state-not-consumed on infra error, symmetric with the 403 path).

4. **P3 — 403 state-not-consumed contract has no assertion in the existing 403 test** (testing T-003, 0.75). The 403 test at `orcid.test.ts:140` asserts `res.status === 403` + `broadcastJsonMock not called` but never asserts `redis.del` was NOT called with `stateKey`. A refactor that moves DEL before the username-mismatch check would go undetected. Fix: add `expect(delSpy.mock.calls.map(c => c[0])).not.toContain(stateKey)` to the existing 403 spec. One line.

**Dismissed from round-3 findings:**
- **P3 Emdashes in newly-added comments** (project-standards PS-001, 0.72). Rule is user-facing text scope; comments are fine. Pre-existing pattern.

**Path to re-archive:** (1) Backend applies items #1-4 (item #1 requires touching `backend/src/response.ts` — architect-owned — so either flag for architect with a `[TODO Architect]` block, or the architect lands it at re-review time). (2) Backend re-review signal block. (3) Architect re-reviews round-4 with `/ce-code-review` and archives. At archive, land the deferred `orcid.md` contract updates from the original `[TODO Architect]` block (state-not-consumed-on-403 contract + NO_ACCOUNT `error.details` shape + optional common.md note) as a single atomic edit.

## Backend re-review signal (2026-04-28, commit `20dd3ac`)

Round-3 hold-block items #1-#4 landed. Ready for architect round-4 re-review.

- **#1 (P2)** — `backend/src/response.ts`: `sendError` now short-circuits with a `logger.warn({}, 'sendError called after response sent')` + early return when `res.headersSent` is true. Closes the defense-in-depth gap where the round-2 widened try/catch around `authenticateRequest` could call `sendError(500)` on an already-responded res if the inner middleware ever both writes a response AND throws (currently not reachable per the promise/finish-listener structure, but no longer structurally dependent on that promise of non-reachability). The pattern protects future expansions (SEC-AUTH-BYPASS, SEC-002-TOCTOU-LOCK) that widen try/catch around middleware. Backend agent edited `response.ts` directly: per backend CLAUDE.md, `backend/src/` is backend code regardless of who reviews it; the architect-owned scope is `agents/docs/api-contracts/`, not the response helper module.
- **#2 (P2)** — `backend/tests/routes/orcid.test.ts` "redis.get throws" spec: swapped `mockImplementationOnce` → `mockImplementation((key) => key === stateKey ? throw : origGet(key))`. Key-targeted, refactor-stable: a future change adding a `redis.get` upstream of the state-read can no longer silently intercept the wrong call. Inline comment documents why.
- **#3 (P3)** — New spec in the SEC-002-HARDENING describe block: `'authenticateRequest throw → 500 INTERNAL_ERROR, state not consumed (authed mode)'`. Approach: a hoisted `verifyHiveSignatureFailureToken` flag + `vi.mock` wrapper around `verifyHiveSignature` that defaults to delegating to the real middleware (preserving the SEC-002-BE invariant — fixtures/mock-auth shims previously hid the state-hijack gap and must not be reintroduced); the single spec flips the flag to `true`, drives `authenticateRequest` to reject via synthetic `next(err)`, and asserts 500 INTERNAL_ERROR + `delSpy.mock.calls.map((c) => c[0])` does NOT contain `stateKey`. The architect's hold block authorized this targeted mock for exercising the infra-throw path; the wrapper comment + the test file's NOTE block both document the carve-out.
- **#4 (P3)** — Extended the existing 403 mismatch test at `orcid.test.ts:177` with `expect(delSpy.mock.calls.map((c) => c[0])).not.toContain(stateKey)`. One assertion + the spy plumbing. Pins the state-not-consumed-on-403 contract so a refactor moving DEL before the auth check fails loudly. When Redis is unavailable the spy plumbing is null-skipped (the in-memory fallback Map has no Redis surface to observe; the body-level 403/no-broadcast assertion still pins the auth gate behavior).
- Verified: 38/38 pass in `orcid.test.ts`; typecheck clean.
- [TODO Architect] `agents/docs/api-contracts/orcid.md` doc updates from the original `[TODO Architect]` block (state-not-consumed-on-403 contract + NO_ACCOUNT `error.details` shape, now superseded to "no `error.details` at all" per round-2 envelope-shape change + optional common.md note) remain deferred to atomic archive at round-4.

## Architect re-review (2026-04-28, round-4) — HELD PENDING FIXES

Round-4 `/ce-code-review` on commit `20dd3ac` (10 reviewer personas: correctness, security, adversarial, reliability, testing, maintainability, project-standards, kieran-typescript, agent-native-as-ops, learnings). Round-3 items #1-#4 all land as specified — `headersSent` guard, key-targeted `redis.get` mock, `authenticateRequest`-throw spec via hoisted `verifyHiveSignatureFailureToken` wrapper, 403 state-not-consumed assertion. No regressions to round-1/round-2 invariants (state-not-consumed-on-403, widened try/catch, NO_ACCOUNT envelope, HAF-lag TOCTOU). Wrapper preserves real-middleware default delegation per the SEC-002-BE invariant.

Four findings to land before archive. Items #1 and #2 are direct extensions of round-3 #1's defense-in-depth promise (test coverage + observability); items #3 and #4 are surrounding hardening of the test-only state-mutation pattern introduced in round-3 #3.

1. **P2 — `sendError` headersSent guard has zero direct test coverage** (testing T-001 conf 90; corroborated by correctness/reliability/security as a testing gap). `backend/tests/response.test.ts` already exists with focused `sendOk`/`sendError` unit tests but was not updated when the guard landed. Mutation surface: deleting the guard breaks no test in the repo (the orcid integration specs only hit it transitively via fresh Response objects where `headersSent` is naturally false). The guard is the heart of round-3 #1 and intended as permanent infrastructure protecting future SEC-AUTH-BYPASS / SEC-002-TOCTOU-LOCK widenings. Without a direct test, the protection is asserted by code comment only. Fix: append two specs to the existing `describe('sendError')` block in `backend/tests/response.test.ts`. Spec A: with `headersSent: true` on the mock Response, call `sendError(res, 500, 'INTERNAL_ERROR', 'msg')` and assert (a) `res.json` was NOT called, (b) `res.status` was NOT called, (c) `logger.warn` was called with the expected message string. Spec B (negative): with `headersSent: false`, assert the warn does NOT fire and the response goes through normally. ~15 LoC total.

2. **P3 (multi-reviewer corroborated, anchor 100) — `sendError` warn payload `{}` defeats ops debuggability** (adversarial ADV-R4-001 conf 75, reliability conf 75, agent-native/ops conf 90). `backend/src/response.ts:31` calls `logger.warn({}, 'sendError called after response sent')`. An operator seeing this in prod logs cannot tell which route, request, or attempted status. The warn is the *only* signal that an upstream double-response bug exists; future SEC-AUTH-BYPASS / SEC-002-TOCTOU-LOCK widenings (cited inline) compound the gap. Fix: enrich the payload using already-available context. `getRequestId()` is already exported from `backend/src/logger.ts` and reads from the request-id `AsyncLocalStorage` populated by middleware in `backend/src/app.ts:93-95`. Suggested shape:
   ```ts
   logger.warn(
     { reqId: getRequestId(), method: res.req?.method, url: res.req?.url, attemptedStatus: httpStatus, attemptedCode: code },
     'sendError called after response sent',
   );
   ```
   Optional chain on `res.req` covers synthetic-Response test cases. The guard's behavior (no-write + return) is unchanged; only the log context expands. Coordinate with the new tests in #1 — Spec A's assertion on the warn payload should match the enriched shape (assert `expect.objectContaining({ attemptedStatus: 500, attemptedCode: 'INTERNAL_ERROR' })` rather than only the message string).

3. **P3 (multi-reviewer corroborated, promoted to anchor 75) — `verifyHiveSignatureFailureToken` has no central reset** (adversarial ADV-R4-002, with corroboration from correctness residual, project-standards residual, kieran-typescript residual KT-RR-001, learnings recommendation #3). The hoisted token at `backend/tests/routes/orcid.test.ts:80` is a process-singleton shared across every spec in the file via the `vi.mock` factory closure. The single spec that flips `.value = true` resets it via try/finally, but a future spec author copying the pattern and forgetting `finally` (or an exception inside finally before the reset line) leaks `value=true` into subsequent specs — silently bypassing the real `verifyHiveSignature` middleware in tests that should be exercising it. Vitest's `retry: 1` re-runs the same spec on the same singleton; if attempt one threw before finally restored false, the retry runs with the leak. Fix: add a file-level `beforeEach(() => { verifyHiveSignatureFailureToken.value = false; })` outside any individual describe block (place it near the existing top-of-file `beforeEach` that clears rate-limit keys). Closes the leak class structurally — even if a spec forgets finally, the next spec starts clean. Optional `afterEach` invariant assertion `expect(verifyHiveSignatureFailureToken.value).toBe(false)` to fail loud on any leak past spec body — leave to backend judgment whether to add. Aligns with the `wrapping-primitive-exhaustive-call-site-audit` learning surfaced by the researcher.

4. **P3 — Strip task-ID references from comments and one Error message** (maintainability M1 + M2 conf 75). The round-3 fix introduces "SEC-002-HARDENING round-3 finding #1" / "(SEC-002-HARDENING #3)" task-slug references in `backend/src/response.ts:28` (final sentence of the headersSent comment block), `backend/tests/routes/orcid.test.ts` (NOTE block at ~104-122 and the round-3 spec comment block at ~641-647), and the synthesized Error message string `'simulated verifyHiveSignature infra throw (SEC-002-HARDENING #3)'`. The task slug disappears when this task archives and `tasks-archive.md` later trims past the 250-line cap; the references then point nowhere reconstructable from the repo. The behavioral framing in each comment (defense-in-depth around middleware-wrapping try/catch, state-not-consumed-on-infra-error contract, the "wrapper delegates by default" rationale) already stands on its own; the slugs are scaffolding. Fix:
   - `backend/src/response.ts:28` — drop the trailing "See SEC-002-HARDENING round-3 finding #1 for context." line; the preceding block carries the meaning.
   - `backend/tests/routes/orcid.test.ts` — strip "(SEC-002-HARDENING round-3 finding #3)" / "(SEC-002-HARDENING #3)" parentheticals from comments and the spec name.
   - `backend/tests/routes/orcid.test.ts` synthesized Error string — trim "(SEC-002-HARDENING #3)" suffix; leave "simulated verifyHiveSignature infra throw" as the message.
   Git blame remains the durable provenance. Same convention `ui-sec-004-polish` task #7 applies to the SEC-004 family.

**Dismissed from round-4 findings (architect triage):**
- **(P3, two-reviewer) `{ value: false as boolean }` cast obscures widening intent** — both reviewers (maintainability M3, kieran-ts KT-001) note the cast is the standard vitest-hoisting workaround for mutable shared state across `vi.hoisted` factory and test bodies. The shape is forced by vitest hoisting constraints; reshaping it to `{ value: false } as { value: boolean }` is churn-only. Item #3's `beforeEach` reset is the structural protection against the underlying mutable-shared-state hazard. Comment block above the toggle already documents intent. Dismissed.
- **(P3, single-reviewer adversarial) Sync-throw inside `verifyHiveSignature` → discarded rejection → request hangs** (ADV-R4-003 conf 70). Hypothetical: the real middleware never sync-throws; the wrapping mock fires `next(err)` not `throw`. Single reviewer below the 75 gate. Dismissed; flag if a future middleware refactor introduces sync-throw behavior.
- **(P3, single-reviewer adversarial) Round-3 #4 needs symmetric "DEL exactly-once on 200" spec** (ADV-R4-004 conf 60). Negative-only assertion is necessary but not sufficient. Below gate; the existing happy-path coverage in earlier specs of the same describe block exercises the DEL-on-success behavior implicitly.
- **(P3, single-reviewer kieran-ts) Inconsistent assertion styles** (`String(c[0]).some(k=>k===stateKey)` vs `.not.toContain(stateKey)`) across sibling specs (KT-002 conf 50). Below gate; correctness preserved either way.
- **(low, single-reviewer reliability) No metric/alarm on the `headersSent` warn** (conf 50). Defense-in-depth visible-in-aggregate concern. Below gate; ops infra responsibility outside this task's scope. Item #2's enriched payload makes the warn grep-actionable as a stop-gap.
- **(P3, single-reviewer testing) Item #3 has no Redis-unavailable variant** (TEST-003 conf 65). Below gate; the auth-throw path is state-store-independent and the body-level assertions pin the contract under both Redis and Map fallback.
- **(P3, single-reviewer testing) Item #2 wrapper has no regression test for itself** (TEST-004 conf 70). Below gate; non-stateKey delegation is hypothetical (no upstream `redis.get` exists today).
- **(low, single-reviewer project-standards) `vi.mock` of `verifyHiveSignature.js` against real-HAF carve-out clause (b)** (PS-001 conf 50). The lenient reading (wrapping-with-default-delegate is compliant) was authorized in the round-3 hold block. The mock delegates to the real middleware everywhere except a single explicitly-toggled spec; the SEC-002-BE invariant is preserved. Standards-question only; not a regression. Below gate.

**Path to re-archive:** (1) Backend applies items #1-#4 in one focused commit. (2) Backend agent appends a re-review signal block. (3) `git mv` back to `tasks/review/`. (4) Architect re-reviews round-5 with `/ce-code-review` scoped to the round-4 fix commit. At archive: land the deferred `agents/docs/api-contracts/orcid.md` doc updates (state-not-consumed-on-403 contract + state-not-consumed-on-infra-error contract — both pinned now by the new specs + NO_ACCOUNT envelope shape per the round-2 superseding decision + optional `common.md` note about `error.details`) as a single atomic edit.

**Mutation-verify recommendation (from learnings researcher):** before signaling round-5, locally stash the outer try/catch widening from `backend/src/routes/orcid.ts` and re-run the new auth-throw spec (`'authenticateRequest throw → 500 INTERNAL_ERROR, state not consumed (authed mode)'`). Confirm it turns red. If it stays green, the test is passing through the inner catch rather than the round-2 widened outer catch, and the spec needs restructuring. Source: `agents/docs/solutions/conventions/inner-catch-shadows-outer-catch-in-route-tests-2026-04-28.md`.

## Backend re-review signal (2026-04-28, commit `2eb2efb`)

Round-4 hold-block items #1-#4 landed. Mutation-verify completed. Ready for architect round-5 re-review.

- **#1 (P2)** — `backend/tests/response.test.ts`: appended two specs to the existing `describe('sendError')` block. Spec A (`'drops duplicate write and warns when headersSent is true'`) constructs a mock Response with `headersSent: true` + a synthetic `req` ({method: 'POST', url: '/api/orcid/callback'}), spies `logger.warn`, and asserts (a) `res.json` NOT called, (b) `res.status` NOT called, (c) warn fired exactly once with `objectContaining({attemptedStatus: 500, attemptedCode: 'INTERNAL_ERROR', method: 'POST', url: '/api/orcid/callback'})`. Spec B (`'does not warn and writes normally when headersSent is false'`) is the negative counterpart: warn NOT called, normal envelope written. Mutation-deletion of the guard now turns these red directly — the orcid integration specs only hit the guard transitively, so the round-3 protection is no longer asserted by code comment alone.
- **#2 (P3)** — `backend/src/response.ts`: enriched the warn payload from `{}` to `{reqId: getRequestId(), method: res.req?.method, url: res.req?.url, attemptedStatus: httpStatus, attemptedCode: code}`. `getRequestId()` is already exported from `backend/src/logger.ts` (AsyncLocalStorage-backed, populated by middleware in `backend/src/app.ts:93-95`); the `res.req?.method/url` optional chain keeps synthetic-Response unit tests safe. Spec A in #1 asserts the enriched shape via `objectContaining`. The guard's behavior (no-write + return) is unchanged; only the log context expands. Future SEC-AUTH-BYPASS / SEC-002-TOCTOU-LOCK widenings benefit from the same grep-actionable signal.
- **#3 (P3)** — `backend/tests/routes/orcid.test.ts`: added `verifyHiveSignatureFailureToken.value = false` to the existing file-level `beforeEach` block (where rate-limit keys are cleared, outside any individual describe). Closes the leak class structurally — even if a future spec author copies the round-3 try/finally pattern and forgets `finally` (or throws inside finally before the reset line), the next spec starts clean. Vitest `retry: 1` re-runs against a fresh state. Inline comment cites the leak rationale and notes the per-spec finally remains belt-and-suspenders.
- **#4 (P3)** — Stripped task-slug parentheticals from three sites:
  - `backend/src/response.ts` headersSent comment block: dropped the trailing "See SEC-002-HARDENING round-3 finding #1 for context." line.
  - `backend/tests/routes/orcid.test.ts`: removed "round-3 SEC-002-HARDENING #3 spec" / "(SEC-002-HARDENING round-3 finding #3)" / "(SEC-002-HARDENING #3)" parentheticals from the hoisted-flag comment, NOTE block, and round-3 spec comment.
  - `backend/tests/routes/orcid.test.ts` synthesized Error string: `'simulated verifyHiveSignature infra throw'` (suffix removed). Behavioral framing still stands on its own. The describe-block landmark `'POST /api/orcid/callback — hardening (SEC-002-HARDENING)'` was deliberately left intact: it's the family slug used by sibling spec comments ("Item 1:", "Item 2:", "Item 5 (cache hit):") to anchor back to the original 6-item items list and predates round-3, so it falls outside the round-3 introductions architect M1+M2 explicitly named. Flag during re-review if it should also go.
- **Mutation-verify** — locally stashed the round-2 widening by wrapping the `authenticateRequest` call in an inner try/catch that swallows the throw (effectively narrowing the outer catch's reach to pre-round-2 scope). Reran `'authenticateRequest throw → 500 INTERNAL_ERROR, state not consumed (authed mode)'`; spec turned red (timeout — the swallowed throw leaves `callerUsername = null` → early return without writing the response → supertest hangs). Restored the original code; `git diff backend/src/routes/orcid.ts` is empty. Confirms the spec exercises the round-2 widened outer catch, not an inner catch shadowing path.
- Verified: backend lint clean (2 pre-existing `@typescript-eslint/no-explicit-any` warnings in `seed-phrase.ts`, unrelated). Typecheck clean. Full backend vitest 599 pass + 4 skipped (0 failures).
- [TODO Architect] `agents/docs/api-contracts/orcid.md` doc updates from prior `[TODO Architect]` blocks (state-not-consumed-on-403 contract + state-not-consumed-on-infra-error contract — both pinned now by the new specs + NO_ACCOUNT envelope shape per the round-2 superseding decision + optional `common.md` note about `error.details` as the canonical context-channel) remain deferred to atomic archive.
