## UI-ORCID-CALLBACK-FRESH-AUTH-MODE-DISPATCH (archived 2026-05-17) — round-3 clean (set_password empty `root_permlink` guard refinement + positive-case regression test) ✓

Add a `_handleFreshAuth` branch to the orcid-callback switch so ORCID-mediated consent-op proofs can dispatch. Originally surfaced as P3 latent (no live SPA caller today; consumer arrives with `ui-multi-author-consent-affordances`). Cache-shape decision: new `cacheConsentOpProof` / `getCachedConsentOpProof` / `clearCachedConsentOpProof` helpers in `frontend/src/lib/fresh-auth.js` (single-slot sessionStorage, strict-equality lookup against `(action, rootAuthor, rootPermlink)`); session-kind vs consent_op-kind kept first-class on the frontend mirroring the backend. Round-1 surfaced a P0 wire-contract gap (cache stored `{action: undefined, …}` because backend didn't echo the target triple) — resolved by archived `BACKEND-ORCID-FRESH-AUTH-CALLBACK-ECHOES-TARGET-TRIPLE`. Round-2 added defensive triple validation. Round-3 (commit `611b86c`) fixed a P1 latent defect in round-2's own guard shape: truthy check on `root_permlink` rejected the backend's contract-valid `set_password` echo (`root_permlink: ''` is intentional for account-level non-paper targets); refined to typeof-only for `root_permlink` while preserving typeof+truthy on `action` and `root_author`. Added one positive-case regression test asserting the `set_password` response shape caches verbatim (including empty permlink), navigates, and surfaces toast.

Round-3 architect `/ce-code-review` (7 personas: correctness on Opus; testing/maintainability/project-standards/julik-frontend-races/learnings-researcher on Sonnet; adversarial on Opus; `ce-agent-native-reviewer` skipped per project CLAUDE.md): zero findings cleared the confidence gate. Adversarial surfaced 2 P2 anchor-50 findings (symmetric empty-permlink admission for consent-op actions where a backend regression could cause a silent cache-miss loop; future consumer-side `null` vs `''` mismatch risk for `set_password` lookup) — both suppressed by the anchor gate as defense-in-depth concerns guarded by backend `/start` validation and the integration test pin. The E2E test deferred per acceptance §4 still waits for `ui-multi-author-consent-affordances` to land the consumer side.

**Cross-references:** `frontend/src/pages/orcid-callback.js` (`_handleFreshAuth` at the dispatch switch + defensive triple validation), `frontend/src/lib/fresh-auth.js` (consent-op cache helpers + NaN-finite TTL guards), `frontend/tests/unit/pages-orcid-callback.test.js` (55 tests including round-3's positive-case set_password regression), `agents/docs/solutions/conventions/wire-contract-shape-pinned-on-backend-not-stub-2026-05-16.md` (round-1 P0 root cause), `agents/docs/api-contracts/orcid.md` `fresh_auth` section. Full 3-round history survives in git: commits `c0a6e8e` → `f92de11` → `611b86c` + architect re-review signals. Skipped `/ce-compound` — wire-contract-shape-pinned convention already documented; no non-obvious new learning surfaced.

## UI-RETRY-UPGRADE-BACKEND-TEST-COVERAGE (archived 2026-05-17) — round-2 clean (R3 sibling parity assertions + file-level Keychain stub extraction) ✓

Add unit tests for `retryUpgradeBackend`'s catch branches in `frontend/tests/unit/pages-settings.test.js`. Round-1 (commit `70ccf2d`) enumerated H1/H2 dispatcher + R1-R11 retry branches and asserted state on `upgradePhase` / `upgradeError` / `upgradeWarnings` / warning-emission spies per branch; one branch documented as `// untestable without DI` (`loginFromResponse` is synchronous; no navigate-away interleave window between R5 and R6 guards). Round-1 architect held two P3 items: T1 (R3 empty-newSeedPhrase defensive-branch test missing `upgradePhase === 'error'` entry-invariant and `canRetryUpgrade === false` non-retryable-routing assertions present in sibling tests R7/R9/R10/R11; mutation-killing weaker than the pattern) + M1 (3 structurally identical `stubKeychainImportKey` helpers across describe blocks). Round-2 (commit `78e9ab3`) landed both: 2 missing assertions added with inline mutation-kill rationale; extracted file-level `stubKeychainImportKeySuccess()` removing 3 nested copies and renaming 9 call sites. 77 tests pass (refactor + assertion-only, no test-count change).

Round-2 architect `/ce-code-review` (5 personas: correctness on Opus; testing/maintainability/project-standards/learnings-researcher on Sonnet; adversarial/julik-frontend-races/security/reliability skipped — test-only diff below adversarial threshold, no async-UI / auth / contract change; `ce-agent-native-reviewer` skipped per project CLAUDE.md): zero findings cleared the confidence gate. Correctness info-noted 4 unrelated inline `vi.stubGlobal` blocks (lines 892/951/989/1024) using the same Keychain shape but outside M1's scope — not surfaced as a follow-up per YAGNI. PS-001 (top-of-file carve-out JSDoc header) pre-existing across 50+ tests; not bundled per round-1 architect dismissal posture.

**Cross-references:** `frontend/tests/unit/pages-settings.test.js` (file-level `stubKeychainImportKeySuccess()` at line ~144 + 9 renamed call sites + 2 R3 sibling-parity assertions), `frontend/src/pages/settings.js` (`retryUpgradeBackend` + `handleRetry` dispatcher), `canRetryUpgrade` getter + `NON_RETRYABLE_UPGRADE_ERROR_KEYS` list. Full 2-round history survives in git: commits `70ccf2d` → `78e9ab3`. Skipped `/ce-compound` — no non-obvious new convention surfaced; existing test-helper-closure-capture and mutation-kill-claims-must-match-assertion conventions in `agents/docs/solutions/conventions/` cover the patterns this task aligned with.

## UI-UPGRADE-FLOW-MOUNTED-GUARDS (archived 2026-05-17) — round-2 clean (integrated destroy() path test fidelity) ✓

Add `_mounted` guarding around `_completeUpgradeAfterBackend`'s Keychain import loop so navigate-away mid-upgrade short-circuits the loop, skips orphan state writes in the `finally` block, and clears sensitive state. Round-1 (commit `89c1dab`) landed the four production guards in `frontend/src/pages/settings.js` (`executeUpgrade` + `retryUpgradeBackend` re-check `_mounted` before invoking `_completeUpgradeAfterBackend`; per-iteration guard inside the Keychain loop; finally-block guard; `destroy()` wipe-then-teardown ordering so `_clearSensitiveUpgradeState()` runs before `_teardownTimers()` flips `_mounted`). Round-1 architect held one P2 item with 3-way cross-reviewer corroboration (correctness + testing + adversarial): the navigate-away test directly set `compRef._mounted = false` inside the `requestImportKey` stub, bypassing the production `destroy() → _clearSensitiveUpgradeState() → _teardownTimers()` chain entirely — the wipe-once-on-unmount invariant the production comments cite as the security guarantee had no end-to-end test against the integrated path. Round-2 (commit `1bc3c4b`) swapped the stub to call `compRef.destroy()` and folded all three AC-#4 invariants (loop short-circuit `importKeyCalls.length === 1`, sensitive state wiped exactly once with `_mounted === false`, no `upgradePhase='done'` write) into one integrated test. Companion `destroy() wipes sensitive upgrade state` test retained as idle-path coverage.

Round-2 architect `/ce-code-review` (6 personas: correctness on Opus; testing/maintainability/project-standards/julik-frontend-races/learnings-researcher on Sonnet; adversarial/security/reliability skipped — test-only diff below thresholds, production unchanged; `ce-agent-native-reviewer` skipped per project CLAUDE.md): zero findings cleared the confidence gate. Pre-existing adversarial-1 (auth singleton strand via `loginFromResponse` guard ordering trades disconnect-protection for the strand risk; see settings.js:797) and adversarial-3 (orphan 45s Keychain `Promise.race` timeout uses raw `setTimeout` not registered with `_pendingTimers`, extends WIF closure-capture lifetime past unmount by up to 45s) remain explicitly out-of-hold per round-1 architect triage.

**Cross-references:** `frontend/src/pages/settings.js` (the four `_mounted` reference points around `_completeUpgradeAfterBackend` + `destroy()` wipe-then-teardown ordering at ~line 512), `frontend/src/lib/timer-guard.js:64-68` (`_teardownTimers` flips `_mounted`), `frontend/tests/unit/pages-settings.test.js` (navigate-away integrated test + companion idle-path destroy() test). Full 2-round history survives in git: commits `89c1dab` → `1bc3c4b`. Skipped `/ce-compound` — synchronous-flag-before-await convention already documented; integrated-destroy-path test fidelity is an instance of an existing pattern, not a new one.

## BACKEND-IDEMPOTENCY-HAF-INTEGRATION-TEST (archived 2026-05-17) — round-2 clean (P0 bare-catch removed; 3rd helper covered; 1 finding dismissed) ✓

Real-HAF integration coverage for `findCustodyBroadcastByIdempotencyKey`, `findAccreditationBroadcastByIdempotencyKey`, and `findExistingAccreditation` — the load-bearing dedup helpers added by `BACKEND-BROADCAST-IDEMPOTENCY-CLUSTER-FOLLOWUP` and `BACKEND-ACCREDITATION-EXISTING-ACCREDITATION-GATE`. Round-1 (commit `42ac79c`) added `backend/tests/lib/idempotency-real-haf.test.ts` with pre-existing-fixture-discovery skip-gating (mirrors `consent-ops-real-haf.test.ts`) for two of three helpers; architect held 3 items: P0 bare `catch {}` blocks structurally inverted the file's purpose (schema-rename errors caught by the SQL but absorbed as `ctx.skip()` instead of failing the test); P1 `findExistingAccreditation` describe block missing (1 of 3 in-scope functions silently uncovered); P1 `result?.tx_id` after `expect(result).not.toBeNull()` doesn't narrow TS so a null regression produces "expected undefined to be <trxid>" instead of a clear null-vs-value signal. Round-2 (commit `05e6210`) landed all three: every bare-catch site removed (grep confirms zero matches in the file); new `describe('findExistingAccreditation — real HAF SQL shape', ...)` block with positive-hit-via-fixture-discovery + negative-miss + per-route-scoping arms; `result!.tx_id` / `result!.block_num` at the load-bearing positive-hit assertions in all three describe blocks. Test header acknowledges the third function; mock carve-out clause (c) satisfied (the test IS the real-path companion).

Round-2 architect `/ce-code-review` (6 personas: correctness on Opus; testing, maintainability, project-standards, learnings-researcher, kieran-typescript on Sonnet; security/adversarial/reliability/api-contract/performance skipped at scope — test-only commit, no production code change; ce-agent-native-reviewer skipped per project CLAUDE.md): zero findings cleared the confidence gate. testing T1 + correctness-1 cross-flagged the `findExistingAccreditation` per-route-scoping arm as vacuously green when no forged op exists on chain (the sibling `findAccreditationBroadcastByIdempotencyKey` arm has an unconditional `randomUUID()`-keyed miss call as an anchor; this arm doesn't) — dismissed per `feedback_dismiss_preemptive_test_hardening`: the function under test IS exercised unconditionally by the negative-miss arm one block above (catches schema regressions); the per-route scoping property is what's vacuous, not the function itself, and forged non-authority `accredit` ops realistically don't exist on chain (only admin signs accredit ops in production).

**Cross-references:** `backend/tests/lib/idempotency-real-haf.test.ts`, `backend/src/lib/idempotency.ts`, `agents/docs/solutions/conventions/test-mock-carve-out-clause-c-2026-05-04.md` (clause-C contract satisfied), `agents/docs/solutions/conventions/inner-catch-shadows-outer-catch-in-route-tests-2026-04-28.md` (round-1 P0 fix against this convention), `agents/docs/solutions/conventions/accreditation-state-read-latest-action-wins-2026-05-15.md` (the new describe block exercises this invariant). Full 2-round history survives in git: commits `42ac79c` → `05e6210` → `67e033f` (mv to review/) and the task file body at `agents/docs/tasks/review/backend-idempotency-haf-integration-test.md` (deleted at archive).

## UI-KEYCHAIN-WARNING-COPY-OR-RETRY-ACTION (archived 2026-05-17) — copy-only direction A, validated retroactively by backend-seed-phrase-keychain-compat ✓

Rewrite four partial-Keychain-import warning strings in `frontend/public/messages/*.json` (16 locales) to direct users to the Hive Keychain extension's import flow using the seed phrase as the recovery mechanism, replacing the prior "retry from settings later" instruction (no such affordance existed). Architect `/ce-brainstorm` 2026-05-16 selected direction A (copy-only) over direction B (settings affordance) and direction C (hybrid) on pre-launch / frequency-unknown grounds: the seed phrase IS the recovery mechanism by design, and the Keychain extension already supports the import workflow PEvO would otherwise duplicate.

Round-1 implementer commit `849481d` updated all 16 locale files to identical new English copy, added STUBS.md ledger entry, and updated 2 `settings.js` code-comments to reflect the new copy with no code-path change. Round-1 `/ce-code-review` (4 reviewer personas plus learnings-researcher) returned clean against all four ACs. Architect HELD the task on a deeper premise issue: PEvO's seed-derivation used a custom HMAC-SHA512 scheme incompatible with Hive Keychain's `PrivateKey.fromLogin`, so the copy promised a recovery flow that didn't exist at the time. Migration footprint nil (single dormant account `testaccount23652`); architect filed `backend-seed-phrase-keychain-compat` as the unblocker, which archived 2026-05-16 (commit `658963f`).

Round-2 architect verification (this archive, 2026-05-17) confirmed the upstream landed: both `backend/src/seed-phrase.ts:92` and `frontend/src/hive-keys.js:57` now call `PrivateKey.fromLogin(account, mnemonic, role)` directly. All 16 locale files carry the new copy uniformly (each contains exactly 4 occurrences of "Use your seed phrase to import"). No implementer changes were needed in round-2; the upstream derivation switch validated the copy retroactively. Round-2 review path was architect verification rather than full `/ce-code-review` re-fan-out, because the only post-hold question was whether the upstream blocker resolved — round-1 already cleared the diff against ACs.

**Cross-references:** `849481d` (implementer commit); `658963f` (architect archive of `backend-seed-phrase-keychain-compat`).

## UI-LOADDHIVE-ROTATION-SIGNUP-VERIFY (archived 2026-05-17) — first-review clean, convention swap ✓

Replace the bare `await import('@hiveio/dhive')` at `frontend/src/pages/signup-verify.js:380` with `await loadDhive()` from `frontend/src/hive-keys.js`, plus the import-list update. The convention established by `ui-seed-phrase-keychain-compat` rounds 4-5 (commits `f648303`, `2fe8a98`) is that every frontend dhive consumer routes through the cached `loadDhive` so the dynamic-import cache is shared across call sites. `settings.js` had been fully migrated; `signup-verify.js` was the residual call site. Runtime impact zero (browser module registry deduplicates the underlying fetch); the change is pure convention consistency.

Implementer commit `00cb9d2` added `loadDhive` to the existing import list from `hive-keys.js` and swapped the one call site inside `_checkUsername`. Architect `/ce-code-review` (5 reviewers — correctness, testing, maintainability, project-standards, learnings-researcher; `ce-agent-native-reviewer` skipped per PEvO CLAUDE.md) returned zero findings. Convention-audit confirmation: `grep -rn "await import.*@hiveio/dhive" frontend/src/` after the change returns only `frontend/src/hive-keys.js` (the canonical cache definition at line 27) and a comment reference at line 21 — task AC #2 holds. Pre-existing test-coverage gap surfaced by testing reviewer: `_checkUsername` has no unit tests, and the file's existing `vi.mock('../../src/hive-keys.js')` factory does not include `loadDhive` — a latent trap for whoever adds `_checkUsername` tests in future. Not introduced by this diff; documented as residual for awareness.

**Cross-references:** `00cb9d2` (implementer commit); `agents/docs/solutions/conventions/wrapping-primitive-exhaustive-call-site-audit-2026-04-22.md` (governing convention — the grep-backed audit prescribed there is exactly what AC #2 enforces).

## BACKEND-SEED-PHRASE-KEYCHAIN-COMPAT (archived 2026-05-16) — round-3 clean (atomic-cache collapse closes _wordlist! residue) ✓

Switch backend BIP39 → Hive key derivation from custom HMAC-SHA512 + `PrivateKey.fromSeed` to `PrivateKey.fromLogin(account, mnemonic, role)` — the same algorithm Hive Keychain's "Add Account by Master Password" flow uses. After this change, the 12-word seed phrase functions as a Hive master password; a user can paste it into Keychain's import field and the WIFs derived match the on-chain account's pubkeys. Migration footprint nil (single dormant account `testaccount23652` retirable per architect HAF query against `pevo.onboarding` + `pevotest.admin`). Mirrored by UI half `ui-seed-phrase-keychain-compat` archived same-day — both halves coordinated archive per task header.

Round 1 (commit `611d744`) swapped the derivation primitive in `backend/src/seed-phrase.ts`, added 7-spec parity test `backend/tests/seed-phrase.test.ts` (every role's WIF equals `PrivateKey.fromLogin(...).toString()` computed directly), dropped the old `crypto` HMAC import. Round-2 (commit `98b3b46`) addressed architect-held items: dhive pin `^1.3.6` → exact `1.3.6` (mirrors frontend exact pin; prevents algorithm split under hypothetical 1.3.x patch); `_bip39: any` + `Function` cast replaced with typed-namespace static import `import type * as Bip39 from '@scure/bip39' with { 'resolution-mode': 'import' }` (closes tsc bypass at three call sites); `derivePrivateKey` single-line passthrough inlined into `deriveKeysFromMnemonic`; load-bearing JSDoc consolidated; `HiveRole` type alias dropped. Round-3 (commit `3c51598`) closed kieran-typescript's residual `_wordlist!` non-null assertion by collapsing the two separate `T | null` module-level variables into a single atomic `_cache: { bip39: typeof Bip39; wordlist: string[] } | null` so tsc proves both fields non-null at the return site.

Round-3 architect `/ce-code-review` (6 personas: correctness Opus; testing/maintainability/project-standards/kieran-typescript/learnings-researcher Sonnet; `ce-agent-native-reviewer` skipped per PEvO CLAUDE.md; security/adversarial skipped per scope after the algorithm settled round-1): zero findings. learnings-researcher cited `agents/docs/solutions/conventions/ts-closure-denarrowing-nullable-property-hoist-2026-05-04.md` — the round-3 atomic-cache shape is the canonical resolution of the pattern that doc describes. `[TODO ARCHITECT]` from round-1 (algorithm description) landed at archive in root `CLAUDE.md` "Account Creation" paragraph (the implementer's signal block cited `ARCHITECTURE.md` sections that did not exist; the canonical algorithm-description anchor is root CLAUDE.md). Cross-references: `backend/src/seed-phrase.ts`, `backend/tests/seed-phrase.test.ts`, `backend/package.json` (exact dhive pin), `agents/docs/solutions/conventions/ts-closure-denarrowing-nullable-property-hoist-2026-05-04.md`. Full 3-round history survives in git: commits `611d744` → `98b3b46` → `3c51598` plus architect hold commits (`0867486`, `96bcaf9`) and task file body at `agents/docs/tasks/review/backend-seed-phrase-keychain-compat.md` (deleted at archive).

## UI-SEED-PHRASE-KEYCHAIN-COMPAT (archived 2026-05-16) — round-5 clean (rotation uniformity + roundtrip-comment anchor) ✓

Mirror backend `PrivateKey.fromLogin` derivation in `frontend/src/hive-keys.js` so the algorithm broadcast at signup matches the algorithm backend uses for recovery and Keychain uses for import. Paired with `backend-seed-phrase-keychain-compat`; both halves archive together per task header Coordination section.

Round 1 (commit `6660694`) rewrote `deriveHiveKeys` to async + WIF-returning shape; updated `deriveAllKeys`, `deriveHivePublicKeys`, and 3 `settings.js` callsites (`_signUpgradeProof`, `_performUpgradeKeyRotation`, `_performKeychainImport`); replaced `PrivateKey.fromSeed(hex)` with `PrivateKey.fromString(wif)`; dropped `mnemonicToSeedSync` re-export and `@noble/hashes` HMAC imports; regenerated inline snapshots and added parity test in `frontend/tests/unit/hive-keys.test.js`. Round 4 (commit `f648303`) addressed architect-held items: test-boundary fix `deriveCallCount >= 3 → >= 4` so the synthetic throw landed inside `_performKeychainImport` pre-loop (the catch path the test name claimed) rather than `_signUpgradeProof`; single-source-of-truth in `deriveAllKeys` (now delegates to `deriveHiveKeys` so `PrivateKey.fromLogin` appears at one module site only); exported `loadDhive` from `hive-keys.js` and routed `_signUpgradeProof` through it instead of `await import('@hiveio/dhive')` direct. Round 5 (commit `2fe8a98`) addressed two architect-held items: `_performUpgradeKeyRotation` (sibling missed in round 4) now reuses `loadDhive()`; inline comment at `deriveAllKeys`' `PrivateKey.fromString(wifs[role])` call anchoring why the `fromLogin → toString → fromString` round-trip is the deliberate cost of single-source-of-truth — a future "optimize the apparent redundancy" refactor could otherwise re-inline `fromLogin` and silently re-introduce algorithm-split risk (parity test pins WIF output, not call shape).

Round-5 architect `/ce-code-review` (6 personas: correctness Opus; testing/maintainability/project-standards/julik-frontend-races/learnings-researcher Sonnet; `ce-agent-native-reviewer` skipped per PEvO CLAUDE.md; adversarial skipped per scope): zero findings. testing-ui-r5 surfaced two pre-existing observations filed as follow-up tasks: `frontend/src/pages/signup-verify.js:380` direct dhive import predates the `loadDhive` convention (filed as `ui-loaddhive-rotation-signup-verify`); `retryUpgradeBackend()` has zero test coverage on 5 catch branches and dispatcher (filed as `ui-retry-upgrade-backend-test-coverage`). Cross-references: `frontend/src/hive-keys.js`, `frontend/src/pages/settings.js`, `frontend/tests/unit/hive-keys.test.js`, `frontend/tests/unit/pages-settings.test.js`. Unblocks `ui-keychain-warning-copy-or-retry-action` (the new copy "Use your seed phrase to import the account from the Keychain extension" is now accurate); that file moves from `tasks/blocked/` back to `tasks/review/` at archive. Full 4-round history (rounds 1, 4, 5 plus architect hold commits `c57250f`, `e699d62`) survives in git: commits `6660694` → `f648303` → `2fe8a98` and the task file body at `agents/docs/tasks/review/ui-seed-phrase-keychain-compat.md` (deleted at archive).

## BACKEND-PAPERS-FILTER-ACCREDITATION (archived 2026-05-16) — round-2 clean (escape hatch verified; counter-pattern landed out-of-band on main) ✓

Accreditation hard-gate invariant — removed `accredited_only=false` opt-out across papers list / comments list / search (lanes 1-3) and added SQL accreditation gate to `/api/reviews/:author/:permlink` single-doc (lane 4 — the only lane closing a real existing leak). All four lanes landed across commits `986f07e` (reviews) → `b55d691` (papers) → `a0845dc` (comments) → `82f8850` (search) → `9633049` (lane-1 follow-up bridge-paper canary update). Round-1 architect resolution reframed the gate from "privacy/curatorial" to "ontological" (PEvO objects are author-vouched; unaccredited author's review-shaped Hive comment isn't a PEvO review; 404 reflects boundary, not hidden state). Round-2 architect re-review (`/ce-code-review` on `986f07e..9633049` with 11 personas) held 1 item: (P2 kieran-typescript) `accredCte.nextIdx + 2` flat-offset arithmetic in `reviews.ts` diverges from `paramIdx++` canonical counter pattern; bind-insertion-safe by intent only. Round-2 hold's escape hatch: "verify current working-tree state first; if `paramIdx++` is already in place on main, mark this item done." Architect intake verified `reviews.ts:70-75` on current main uses the canonical `paramIdx++` counter idiom — flat-offset form cited at `986f07e` was superseded out-of-band. Per escape hatch, /ce-code-review skipped and proceeded straight to archive.

**Cross-references:** `agents/docs/solutions/conventions/defense-in-depth-canary-must-pin-each-layer-2026-05-07.md` (the canonical `paramIdx++` pattern); `agents/docs/ARCHITECTURE.md` "Accredited-Only Data Policy" (ontological framing landed round-1); `agents/docs/api-contracts/common.md` + `papers.md` (accredited-only summary harmonized; `accredited_only` rows removed); `backend/src/routes/reviews.ts:70-75` (counter pattern verified on main); `backend/src/routes/{papers,comments,search}.ts` (lane gate hardening). Filed follow-up: `backend-search-partial-degradation-allsettled.md`. Out-of-scope CTE fix at `893f43d` made lane-2 canaries live on main. Full 2-round history survives in git: commits `986f07e..9633049` + `893f43d` and the task file body at `agents/docs/tasks/review/backend-papers-filter-accreditation.md` (deleted at archive).

## BACKEND-PAPER-DETAIL-CID-VALIDATE-ON-EMIT (archived 2026-05-16) — round-2 clean (zero primary findings; 3 anchor-50 suppressions, 1 testing-gap demotion) ✓

Output-side IPFS-CID shape validation at every API response-emit boundary. Round-1 added `isValidIpfsCid` + `validatedCid` + `CidValidationContext` in `backend/src/lib/ipfs-validation.ts`, wrapped 3 emit sites in `routes/papers.ts`, 16 unit canaries + 2 integration canaries in `continuation-author-gate.test.ts`. Round-1 architect re-review held 3 items: (P1) missed fourth emit site at `helpers.ts:toPaperSummary` feeding `/api/profile/:username/papers`; (P1) dead branch in `validatedCid` typeof guard (param typed `string|null|undefined` made the typeof check unreachable); (P2) extract named `CidValidationContext` interface. Round-2 (commit `ba649fe` + test-fixture refresh `f0d17ed`) landed all three: (1) wraps `toPaperSummary` ipfs_cid in `validatedCid({author, permlink})` + 2 canaries on `/api/profile/:username/papers` in new file `profile-papers-cid-validate.test.ts` (carve-out clauses a/b/c documented); (2) chose widen-to-unknown (matches sibling `isValidIpfsCid`'s `unknown` signature; removes need for `as` casts at future TS-loose call sites) + 2 unit canaries pinning the now-live typeof branch (number → '42' prefix; object → '[object Object]' prefix); (3) extracted exported `interface CidValidationContext { author: string; permlink: string }`. Grep audit per `wrapping-primitive-exhaustive-call-site-audit-2026-04-22.md` confirmed 4 emit sites in `papers.ts` + 1 in `helpers.ts`, all `validatedCid`-wrapped; no fifth unwrapped site.

Round-2 architect re-review (`/ce-code-review` on `ba649fe`+`f0d17ed` with 7 personas: correctness+security Opus; testing/maintainability/project-standards/ce-learnings-researcher/kieran-typescript Sonnet; adversarial/reliability/api-contract/performance skipped at architect judgment; ce-agent-native-reviewer skipped per PEvO CLAUDE.md): zero primary findings. 3 anchor-50 suppressions (task-slug comment, CidValidationContext naming, redundant `as unknown` cast — all preemptive). 1 testing-gap demotion: mutation-kill comment at `ipfs-validation.test.ts:175` describes "silent coerce" but deletion actually throws TypeError before the warn fires — mutation IS killed, only the explanatory comment is inaccurate; dismissed per `feedback_dismiss_preemptive_test_hardening`. Cross-refs: `backend/src/lib/ipfs-validation.ts`, `backend/src/helpers.ts:319-333`, `backend/src/routes/papers.ts:662,1036,1043,2179`. Full 2-round history survives in git: commits `7a5ccc1`/`a264ec3` → `ba649fe`/`f0d17ed` and the task file body at `agents/docs/tasks/review/backend-paper-detail-cid-validate-on-emit.md` (deleted at archive).

## BACKEND-LANGUAGE-FILTER-LENGTH-CAP (archived 2026-05-16) — round-2 clean (zero findings) ✓

Close the unbounded-string vector on `?language=` cache-key parameter (16-char cap, `SEARCH_LANGUAGE_MAX_LEN`). Round-1 (commit `0a10584`) added the constant + 5-branch `parseLanguageFilter` (undefined / non-string-shape / empty-as-absent / over-cap / pass-through) + 8 helper-direct unit tests + 2 route-level tests + helper docblock rewrite naming the cluster-3 review pass as the gate that closed the length deferral. Round-1 architect re-review (`/ce-code-review` with 8 personas) held 1 item: `INVALID_LANGUAGE_LENGTH_MESSAGE` hardcoded `'16'` literal independent of `SEARCH_LANGUAGE_MAX_LEN` — SSoT gap flagged cross-reviewer by maintainability + kieran-typescript at anchor 100. Round-2 (commit `3aff1ba`) templated the message via the constant; tests left as-is per architect's explicit "implementer's choice — skip it unless trivial" guidance on optional regex-from-constant tightening.

Round-2 architect re-review (`/ce-code-review` on `3aff1ba` with 6 personas: correctness Opus; testing/maintainability/project-standards/ce-learnings-researcher/kieran-typescript Sonnet; ce-agent-native-reviewer skipped per PEvO CLAUDE.md): all 6 returned zero findings. ce-learnings-researcher surfaced two reinforcing convention docs (`req-query-as-string-cast-silent-coerce-2026-05-16.md` + `enumerated-exemption-lists-are-drift-vectors-2026-04-28.md`). Cross-refs: `backend/src/types/search-filters.ts:216-227`, `backend/tests/lib/search-filters.test.ts`, `backend/tests/routes/search.test.ts`. Full 2-round history survives in git: commits `0a10584` → `3aff1ba` and the task file body at `agents/docs/tasks/review/backend-language-filter-length-cap.md` (deleted at archive).

## BACKEND-ORCID-POST-BROADCAST-SEVERITY-CLASSIFICATION (archived 2026-05-16) — round-2 clean (orphan SHA corrected, missing convention queued for /ce-compound) ✓

Landed on main at `4a0fc7e` (the round-2 signal block cites worktree-subagent SHA `0305eb9f` which is NOT an ancestor of main — orphan detected by the architect at re-review intake per `agents/docs/solutions/conventions/worktree-fanout-orphan-detection-2026-04-29.md`; equivalent diff replayed on main as `4a0fc7e`). Round-2 added `AppPoolNotInitialisedError extends Error` sentinel to `backend/src/lib/broadcast-error.ts`, replaced the bare `Error` throw at `routes/orcid.ts` `updateAccountOrcid`'s pre-pool guard with the sentinel, extended `classifyPostBroadcastSeverity`'s `'permanent'` union to include it, amended the helper docblock to honestly acknowledge the `'transient'` branch IS reachable (correcting the prior "dead code on the orcid call path" claim), and added a unit-test pin for `classifier(sentinel) → 'permanent'`. Plus slug-citation cleanup at 7 sites per the slug-staleness convention; `grep -rn 'BACKEND-ORCID-POST-BROADCAST-SEVERITY-CLASSIFICATION' backend/src/ backend/tests/` confirmed zero hits. Result: a pre-pool throw on the orcid post-broadcast cascade now emits 502 `POST_BROADCAST_OPERATOR_REQUIRED` with "please contact support" copy instead of 502 `POST_BROADCAST_FAILED` with the misleading "will reconcile automatically" copy.

Architect `/ce-code-review` (7 reviewers — correctness on opus; reliability, kieran-typescript, testing, maintainability, project-standards, learnings-researcher on sonnet; `ce-agent-native-reviewer` + adversarial + security + api-contract skipped per scope). Reliability + maintainability clean. Five findings triaged: (1) signal-block orphan SHA P1/100 — corrected in this archive entry; (2) `AppPoolNotInitialisedError` missing `Object.setPrototypeOf(this, new.target.prototype)` for cross-realm `instanceof` reliability P2/75 — dismissed (theoretical-only today; not exercised by vitest config or any worker_thread import); (3) integration matrix at `orcid.test.ts:2817-2838` covers 4 error classes but not the new sentinel P3/100 cross-reviewer corroborated — dismissed per `feedback_dismiss_preemptive_test_hardening` (structurally-unusual mutation surface; classifier and route plumbing covered separately); (4) cascade-fn null-pool spec uses `rejects.toThrow('App pool not initialised')` message-substring assertion that passes for both pre- and post-fix throw shapes P3/90 — dismissed (same convention; revert-to-bare-Error mutation is structurally unusual since round-2 just made the opposite change); (5) missing convention slug `BACKEND-CASCADE-FNS-RETHROW-PERMANENT-ERRORS` cited in production docblocks — `/ce-compound` invoked post-archive to file it. Two findings suppressed at anchor 50 (handleAccredit/handleLink wrap comments don't enumerate cascade fns; sentinel class location semantic-home debate); one carried-forward residual (`Error.cause` unwrap in classifier — deferred per round-1 architect-attention note; current call order keeps the gap latent).

## BACKEND-SETTINGS-EMAIL-REAUTH-FRESH-AUTH (archived 2026-05-16) — closes JWT-only takeover at /api/settings/email; gate live but mint-path follow-up filed ✓

Landed at commit `b27bcdf`. `POST /api/settings/email` change-email branch now requires a `fresh_auth_proof` body field on the JWT path; Keychain (Hive-signature) requests remain body-proof-free at the middleware; Add-flow no-row branch is JWT-unreachable by construction. Per-state mechanism matrix (A: password; B: password OR orcid; C: orcid; D: matches preserved factors) enforced via `mechanismAccepted` predicate that reads `password_hash !== null` / `orcid !== null` directly — predicate is state-machine-agnostic and tracks any post-upgrade column mutations. The proof binds to `(change_email, <username>, '')` via the new `changeEmailFreshAuthTarget` helper in `fresh-auth.ts`; collision-free against consent-op targets at the hash layer (consent ops require non-empty `root_permlink` per orcid.ts validation). Wrong-mechanism returns 401 FRESH_AUTH_REQUIRED with `details.reason: 'wrong_mechanism'`; the status-code split for `username_mismatch`/`target_mismatch` already mirrors custody.ts (403). Integration test at `tests/routes/settings-email-fresh-auth.test.ts` pins 13 cases including state A/B/B-orcid/C happy-paths, missing-proof 401, cross-user 403, cross-target 403, wrong-mechanism 401, expired 401, single-use replay 401, Keychain path 200-no-proof, Add-flow no-row INSERT 200.

Architect review via `/ce-code-review` (10 personas — correctness, security, adversarial on Opus; testing, maintainability, project-standards, kieran-typescript, api-contract, reliability, learnings-researcher on Sonnet; `ce-agent-native-reviewer` skipped per project CLAUDE.md) surfaced findings co-triaged with the sibling set-password review (commit `9818e32`, held back to pending/). Email-reauth-specific outcomes: (1) **mint-path absent** — `orcid.ts /start` and `custody.ts /fresh-auth` both 400-reject `action: 'change_email'`, leaving the gate closed but mint closed too; SPA cannot reach happy path until the widening lands (P0 follow-up filed as `backend-change-email-mint-path-and-followups.md`); (2) `kind_mismatch` falls through to 401 on this route while custody.ts maps to 403 (bundled into follow-up); (3) state-B orcid happy-path test asserts only 200 without DB-write check (bundled); (4) concurrent change-email + SMTP fail destroys first valid 24h verify link via blind rollback (snapshot-and-restore bundled); (5) Add-flow no-row branch defense-in-depth — `if (req.hiveAuthMethod === 'jwt') return 401` guard added at branch entry to make the invariant local rather than implicit across JWT-mint sites (bundled); (6) task-slug prefix on the block comment above POST /email will rot at archive (bundled). Dismissed at triage: proof-burned-before-SMTP-success (same UX/security tradeoff as the set-password argon2 case — closed-default on transient failure is correct); `result.mechanism` runtime guard (preemptive hardening on a stable discriminated union); state-D dedicated test (predicate is shared with A/B/C, defensible per implementer signal).

Architect-pass doc edits landed in the same archive commit: ARCHITECTURE.md § 6.4 row "Change email" updated from "TBD — re-auth model unverified" to document the per-state matrix + JWT/Keychain path split, citing this commit and the originating audit task. `api-contracts/settings.md` POST /email section updated with the path-dependent body shape, the per-state mechanism dispatch contract, and the FRESH_AUTH_REQUIRED 401/403 split with `details.reason` enumeration. `api-contracts/orcid.md` widened to document `change_email` as a non-broadcast action with `root_author`/`root_permlink` synthesized server-side (mint path documented as pending until the follow-up lands). POST /set-password contract in settings.md updated in parallel (state-C-only, mechanism MUST be `'orcid'`). The held-back set-password task (returned to pending/ with 10-item HELD-PENDING-FIXES block) will bring code into line with the documented 401/403 split (currently set-password collapses all reasons to 401 — finding #2 of triage).

Full triage record + 21-finding ranked list survives in the architect session. Cross-references: `backend-settings-email-reauth-audit` (archived same day, originating audit); `backend-change-email-mint-path-and-followups` (P0 follow-up filed in pending/); `backend-settings-set-password-fresh-auth` (held back to pending/ — same fresh-auth primitive, sibling review cycle).

## BACKEND-SETTINGS-EMAIL-REAUTH-AUDIT (archived 2026-05-16) — analytical audit, gap-found verdict confirmed by follow-up implementation ✓

Audit task surfaced by the architect's account-state-machine brainstorm (`ARCHITECTURE.md § 6` rebuild). § 6.4's row for `POST /api/settings/email` was marked "TBD — re-auth model unverified" pending verification of the route's actual re-auth posture against § 6.5 invariant #1 (no JWT-only critical actions). Backend agent audited the handler at `settings.ts:96-198` and the `/email/verify/:token` partner at `settings.ts:199`, then walked the JWT-to-takeover chain: stolen JWT → change email to attacker address → `/auth/reset-request` → `/auth/reset` → password under attacker control → `/custody/fresh-auth` → `/custody/broadcast` as victim. Verdict: **GAP FOUND**. Per-state analysis: states A and B fully exploitable via the reset chain; state C cannot be taken over in isolation (§ 6.3 forbids `/reset` from C) but the same JWT compounded with the set-password gap (separate task, also being closed) chains to the same takeover. Both gaps had to close together. Per-state availability prescribed: A = password proof; B = password OR orcid proof; C = orcid proof; D = matches preserved factors; Keychain path = no body proof (already fresh-bound at middleware). Follow-up filed as `backend-settings-email-reauth-fresh-auth.md` (now also archived).

Architect close-out: § 6.4's TBD removed; the per-state matrix the audit prescribed is now documented inline in the row, matching the implementation that landed in `b27bcdf`. No code review on this task itself (analytical doc only — no implementer diff); the audit's analytical correctness was independently confirmed by reviewing the follow-up implementation against ARCHITECTURE.md § 6.1/§ 6.4/§ 6.5 (10-persona `/ce-code-review` fan-out, results in the email-reauth archive entry above). Audit acceptance criterion (a) satisfied. No `/ce-compound` warranted — the audit reasoning is the kind of work § 6 was set up to make systematic; the convention is the section, not a separate solutions entry.

## BACKEND-ORCID-FRESH-AUTH-CALLBACK-ECHOES-TARGET-TRIPLE (archived 2026-05-16) — clean review (architect-pass doc edit + UI unblock) ✓

Landed at commit `b6e193a`: `/api/orcid/callback` `handleFreshAuth` `sendOk` envelope widened from 4 to 7 fields (adds `action`, `root_author`, `root_permlink` echoed from the Redis state's `fresh_auth_target` passed into `issueFreshAuthToken`). Without the echo the frontend `cacheConsentOpProof` helper writes three undefined fields and the strict-equality lookup is a permanent no-op the moment a real consumer wires up. Dispatch-site defensive 400 at `orcid.ts:540-547` fires before `handleFreshAuth(:548)` so `target` is guaranteed non-null at the echo site (inline comment documents the invariant). Integration test pin extends the fresh_auth happy-path describe block at `orcid.test.ts:3050+` with three independent `expect().toBe()` assertions on the new fields against `startAuthed('fresh_auth', ...)` helper defaults; no new mock surface introduced. Archive-pass doc edits land in the same commit: response shape rows added at `api-contracts/orcid.md:204-211` with explanatory paragraph on the per-op target binding; narrative at line 56 rewritten to distinguish "SPA submits" vs "backend echoes"; `action` enumeration corrected at lines 24, 49 to the 3-value union (`author_accept | author_resign | set_password`) that /start actually accepts.

Architect `/ce-code-review` (9 reviewers — correctness, security, adversarial on opus; testing, maintainability, project-standards, api-contract, kieran-typescript, learnings-researcher on sonnet; `ce-agent-native-reviewer` skipped per PEvO CLAUDE.md). Correctness, security, testing, maintainability, project-standards all clean. Five findings triaged: (1) `sendOk(data: unknown)` lacks compile-time wire-contract enforcement — P1 pre-existing structural debt, dismissed per wire-contract-shape-pinned-on-backend-not-stub which the project has explicitly adopted as discipline strategy; (2) `api-contracts/orcid.md` documented `action` as 2-value but /start accepts 3 (`set_password` missing) — P2, folded into the archive-pass doc edit above; (3) test pin assertion values match `startAuthed` defaults so a hardcoded-literal mutation at the echo site would pass — P3, dismissed per `feedback_dismiss_preemptive_test_hardening` despite 3-reviewer cross-corroboration; (4) `[TODO Architect]` block line reference off-by-one — P3, dismissed (about-to-be-archived file); (5) SPA `_handleFreshAuth` blindly trusts echo without `typeof`/truthiness validation — P3, folded into `ui-orcid-callback-fresh-auth-mode-dispatch.md` (unblocked and moved to `tasks/pending/` as part of this archive) as an architect-review item asking the UI implementer to add the guard before calling `cacheConsentOpProof`. No `/ce-compound` warranted — wire-contract-pin convention already covers the canonical lesson.

## BACKEND-APP-SSR-REAL-PATH-COMPANION (archived 2026-05-16) — round-2 clean (clause-c paperwork corrected) ✓

Filed 2026-05-15 as carve-out clause-(c) follow-up for the mocked `app-ssr-discipline-canon.test.ts`: a real-HAF SSR smoke test for `/en/paper/:author/:permlink` JSON-LD `about` wiring against the existing all-lowercase corpus. Round-1 (commit `b71e3c6`) landed `backend/tests/routes/app-ssr-discipline-real-path.test.ts` (128 LOC, equality assertion on `paperDisciplineField(raw_chain_discipline)`) plus a clause-c paragraph in the mocked sibling's header. Round-1 architect hold (1 P1 + 4 P2/P3 dismissed) flagged that the "helper call replaced with raw `pevoMeta.discipline`" claim was false on the all-lowercase corpus — `paperDisciplineField` is `trim + toLowerCase`, so raw === canon for all real data, and the load-bearing equality assertion cannot kill that mutation. Round-2 (commit `225bbac`) dropped the false claim from the 3 architect-enumerated sites (real-path header, mocked-sibling clause-c paragraph, task-body bullet) plus a 4th inline assertion comment that carried the same overstatement (disclosed in signal block). Round-2 `/ce-code-review` (5 reviewers; `ce-agent-native-reviewer` skipped per project CLAUDE.md) verified all three wiring-bypass kills via trace (import-drop, branch short-circuit, catch-all bypass) and confirmed the mocked sibling's `'  Computer Science  '` seed kills both raw-bypass and transform-regression mutations. One P2 maintainability finding (inline comment duplicates the file header enumeration) dismissed at triage per `feedback_dismiss_preemptive_test_hardening`. Convention `mutation-kill-claims-must-match-assertion-and-corpus-2026-05-15.md` (created same day as the round-1 hold) captures the corpus-conditional-kill discipline this task surfaced.

## ARCHITECT-CUSTODY-SESSION-AUTH-CONTRACT-DOC (archived 2026-05-16) — architect-self-task, doc-only ✓

Added `### POST /api/custody/session-auth` section to `agents/docs/api-contracts/custody.md`, mirroring the `/api/custody/fresh-auth` shape: body `{ password }`, response `{ fresh_auth_proof, expires_at: ISO-8601, mechanism: 'password' }`, 10/min rate limit, 401 (incl. no-password-mechanism oracle defense)/403 (upgraded)/400 (missing password)/500/503. Source-of-truth verified against `backend/src/routes/custody.ts:813` and `backend/src/lib/fresh-auth.ts:369` (`issueSessionFreshAuthToken`); endpoint itself landed earlier in `backend-custody-session-auth-password-mint`. Cross-link to orcid.md `mode=session_auth` included in the new section's intro. No `/ce-code-review` invoked: architect-self-task with no implementer diff. Surfaced by `/ce-code-review` of `backend-expires-at-iso-conformance` (archived 2026-05-16) as api-contract AC-02 P3 conf 75. Test comment at `backend/tests/routes/custody-session-auth.test.ts:240` citing `custody.md:108` (fresh-auth's `expires_at`) remains accurate post-insert since the new section appends after line 108.

## BACKEND-SEARCH-Q-LIKEGUARD-AND-LENGTH-CAP (archived 2026-05-16) — round-2 clean (1 P2 assertion-strength hold item landed) ✓

200-char length cap + LIKE-metacharacter escape (`\`, `%`, `_` → `\\`, `\%`, `\_`) with `ESCAPE '\\'` clause on every ILIKE site for `/api/search?q=`. Round-1 (`869fea4`) landed the validator helper + 5 real-HAF specs + 3 mocked-pool SQL contract specs; the lone P2 round-1 hold tightened the papers-search ESCAPE-count assertion. Round-2 fix at `aea0396` (1-line + comment expansion at `backend/tests/routes/disciplines-canon-mocked.test.ts:881`): `expect(occurrences).toBeGreaterThanOrEqual(2)` → `expect(occurrences).toBe(3)` so a regression dropping the ORDER BY CASE `ESCAPE` clause (while leaving textMatch sites clean) surfaces as a test failure rather than passing silently. Inline comment expanded to name the regression class.

Architect `/ce-code-review` (5 reviewers — correctness opus; testing, maintainability, project-standards, ce-learnings-researcher sonnet; `ce-agent-native-reviewer` skipped per PEvO CLAUDE.md). All clean. Correctness verified the count of 3 is exact for the papers-search default-`relevance` sort path: textMatch `c.title` ESCAPE + textMatch `c.body` ESCAPE + ORDER BY CASE relevance ESCAPE = 3. Learnings-researcher confirmed the change is the direct repair `agents/docs/solutions/conventions/tests-must-fail-on-mutation-of-code-under-test-2026-04-22.md` prescribed. Residual risks (informational): default-sort dependency, capture-filter precision (relies on `'type' AS type,` aliasing), hardcoded count structurally coupled to SQL (self-mitigating via comment enumeration). Out-of-scope testing gap: `sort=date` branch emits 2 ESCAPE clauses (no ORDER BY CASE) and is not pinned — acceptable for this round-2 scope. No `/ce-compound` warranted.

## UI-E2E-SEARCH-RESULTS (archived 2026-05-16) — round-4 clean (P3 docblock-anchor hold item landed; orphan re-land recovery) ✓

E2E spec coverage for `frontend/src/pages/search.js` (6 tests: keyword query, empty-result, discipline-filter, URL-pagination, result-card navigation, error-path). Round-4 fix at commit `7630fb2` (re-land — the original round-4 commit `693df43` was orphaned by `git reset --hard b07d90f` which also stripped `2fb89f8`/`6fcd853`; all 3 since re-applied) addressed the round-3 architect hold P3 item: rewrote the file-header carve-out comment from ordinal anchors ("tests 4 and 6 mock /api/search", "(test 4) requires...", "tests 1, 2, 3, 5") to behavioral anchors ("the URL-pagination test and the search-error test mock /api/search", "the pagination test requires...", "live-route tests here (keyword query, empty-result state, discipline-filter combination, result-card navigation)") per `agents/docs/solutions/conventions/docblock-anchor-stable-symbols-not-line-numbers-2026-05-15.md`. 18 LOC comment-only edit + 13-line task-signal-block append documenting the re-land context.

Architect `/ce-code-review` (5 reviewers — correctness opus; testing, maintainability, project-standards, ce-learnings-researcher sonnet; `ce-agent-native-reviewer` skipped per PEvO CLAUDE.md). All clean. Correctness verified each rephrased reference is factually correct against the current test bodies (URL-pagination test at line 172, search-error test at line 261, four live-route tests enumerated match exactly the non-mock tests). Maintainability verified all 4 ordinal references replaced; no remaining ordinal references in body comments. Learnings-researcher confirmed the convention applies cleanly. `/ce-compound` candidate flagged but not invoked: the `git reset --hard` incident that caused the orphan (3 commits stripped in one command) — root CLAUDE.md "Shared-index race discipline" item #2 already covers it.

Four-round cycle (2026-04-28 → 2026-05-16) compounded P3 hardening: structurally-sound 6-test coverage of the search page through behavioral-anchor docblocks, mutation-resistant test discriminators, and explicit carve-out clause (a/b/c) documentation.

## UI-E2E-EDIT-PAPER-FLOW (archived 2026-05-16) — round-5 clean (round-4 hold landed: 1 P2 maintainability item) ✓

E2E spec for `frontend/src/pages/edit.js` covering original-author in-place edit, continuation edit by another accredited user, accepted-claimer continuation, no-changes guard, non-head full-body branch, and authorization rejection (7 tests total). Round-5 fix at commit `a7bb201` (48 lines, spec-only) addressed the round-4 architect hold item: extracted the 1.2KB academic-prose body string that was duplicated verbatim between `buildPaperFixture.body` and `NEW_BODY` in test 1 to a module-scope `ORIG_BODY_PARAGRAPHS` constant. Fixture body now derives as `'## Abstract\n\n' + abstract + '\n\n---\n\n' + ORIG_BODY_PARAGRAPHS`; `NEW_BODY` derives as `ORIG_BODY_PARAGRAPHS + ' We also note an open question around drift detection.'`. The additive relationship (NEW_BODY a near-superset of the fixture body) is now structurally enforced; the diff-broadcast branch invariant survives by code shape rather than parallel prose maintenance.

Architect /ce-code-review (5 reviewers — correctness on opus; testing, maintainability, project-standards, learnings-researcher on sonnet; ce-agent-native-reviewer + adversarial + julik-frontend-races skipped per PEvO CLAUDE.md and per-diff scope). Correctness verified byte-identity preserved (extracted constant is character-for-character identical to the replaced inline strings; node repro: `_originalBody.length=1237`, `newPostBody.length=1339`, `diffText.length=187`, branch DIFF, `body.startsWith('@@')` holds — identical to round-3 numbers). Test count unchanged at 7.

Triaged: 1 project-standards finding (P2 conf 100, "task file remained in pending/ at commit a7bb201; moved to review/ 44 seconds later in be3703e") dismissed — 44s gap is effectively atomic for batched-move purposes; memory `feedback_task_mv_to_review_after_each_round` covers the move HAPPENING (which it did), not atomicity with the fix commit; the sibling search round-3 review accepted the same pattern with a 2.4-min gap. Carried-forward advisory from round-3 (below-gate, not blocking): `ORIG_BODY_PARAGRAPHS` docblock cites `edit.js:1063` by line number per `docblock-anchor-stable-symbols-not-line-numbers-2026-05-15.md`.

`/ce-compound` candidates flagged at round-4 — diff-match-patch fixture-sizing pattern already captured at `mutation-kill-claims-must-match-assertion-and-corpus-2026-05-15.md` (commit `3917935` extended the doc with the size-threshold subtype on 2026-05-16); `percent_hbd: 0` rewards-policy pinning newly captured at `rewards-policy-pin-percent-hbd-not-allow-curation-2026-05-16.md` (commit `6fcd853`). The five-round cycle (2026-04-28 → 2026-05-16) compounded 4 conventions: alpine-value-property-not-attribute-trap (round-1), mutation-kill-claims size-threshold subtype (round-3), rewards-policy percent_hbd pinning (round-3), and the structural-duplication-via-module-scope refactor pattern (round-4, archived clean here).

## UI-AUTH-LOGINFROMRESPONSE-HELPER-ADOPTION (archived 2026-05-16) — round-4 clean (3 round-3 hold items landed; 1 false positive dismissed) ✓

Round-4 fix at commit `2f4596c` (5 files, 61 lines) closed all 3 round-3 architect hold items: (2b P2) `_pollingGeneration` passed from the 2 live one-shot call sites (`settings.js:507` ORCID-return init + `orcid-callback.js:279` `_handleAccredit`), with the dead `gen === undefined` admit branch removed from `auth.js:202` — guard simplified to `if (gen !== this._pollingGeneration) return;`; (1 P2) `_mounted` guard added to `retryUpgradeBackend` at `settings.js:940` mirroring `executeUpgrade`'s round-2 pattern (post-await guard for the 20s post-503 cleanup window); (2a P2) `loginFromResponse` docblock at `auth.js:97-126` rewritten to document the actual contract — callers whose response shape omits `is_accredited`/`accreditation` (canonical examples `sign-in-modal.js#handleEmailLogin`, `signup.js#_resolveExistingAccount`) MUST pass explicit `false`/`null` overrides via spread, with the cross-user accreditation-leak failure mode named. 23/23 auth.test.js pass; 3 test sites at lines 252/315/327 updated to pass `store._pollingGeneration` so the gen guard admits and the post-await `!username || !isConnected` second guard remains the invariant exercised.

Architect /ce-code-review (7 reviewers — correctness + adversarial on opus; testing, maintainability, project-standards, julik-frontend-races, learnings-researcher on sonnet; ce-agent-native-reviewer skipped per PEvO CLAUDE.md). Correctness verified all 3 items landed at named sites and the `gen === undefined` branch is genuinely dead (3 test no-arg sites at lines 284/291/302 exit on earlier guards or throw before reaching the simplified gen guard). Adversarial confirmed the cross-user storage-event login race is closed by the gen guard (post-await `!username || !isConnected` second guard CANNOT catch the new-user race; gen guard is load-bearing). Julik-frontend-races confirmed `_mounted` wiring via `createTimerGuard()` spread + `_teardownTimers()` in `destroy()`.

Triaged: 1 maintainability finding (P2 conf 85, "swallows network errors test at auth.test.js:295 claimed vacuous under simplified gen guard") dismissed as false positive — control-flow read against `auth.js:189-214` shows the `await fetchAccreditationStatus()` is inside the try block BEFORE the gen guard; when the mock rejects, control jumps to `catch` and `console.warn` fires, so `warnSpy.toHaveBeenCalled()` correctly passes. Three other reviewers (correctness, testing, adversarial) independently reached the correct conclusion.

Learnings-researcher cross-referenced active conventions: `helper-contract-flip-untouched-adopter-audit-2026-05-16.md` (round-2 origin doc; round-3 docblock rewrite respects the residual-site disposition list pattern), `synchronous-flag-before-await-idempotency-guard-2026-05-16.md` (retryUpgradeBackend post-await guard is the teardown-during-init shape, correct scope), `docblock-anchor-stable-symbols-not-line-numbers-2026-05-15.md`, `wrapping-primitive-exhaustive-call-site-audit-2026-04-22.md`, `object-shape-fix-every-reset-site-2026-04-21.md`. All respected; no new compound candidates surfaced.

## BACKEND-EXPIRES-AT-ISO-CONFORMANCE (archived 2026-05-16) — first-review clean (P0 deploy-blocker unblocker for `ui-non-consent-broadcast-fresh-auth-wiring`) ✓

P0 deploy-blocker fix at commit `afecd6e`. Backend was emitting `expires_at` as epoch SECONDS (number) from `backend/src/lib/fresh-auth.ts:290`; contract docs (`agents/docs/api-contracts/custody.md:108`, `agents/docs/api-contracts/orcid.md:208,239`) document ISO-8601 string. Frontend's `new Date(expiresAt).getTime()` silently misinterpreted the numeric epoch-seconds as milliseconds → resolved to 1970 → SPA fresh-auth cache 100% non-functional → every light-account broadcast triggered a full ORCID OAuth round-trip. Fix: emit `new Date(memExpiresAtMs).toISOString()` from both `issueFreshAuthToken` and `issueSessionFreshAuthToken`; type-change `IssuedFreshAuth.expires_at` from `number` to `string` (TS would have caught any numeric-arithmetic consumer; none surfaced — all 4 downstream call sites pass through to `sendOk` verbatim). 4 test assertion blocks updated across 3 files (`custody-consent-ops.test.ts`, `custody-session-auth.test.ts`, `orcid.test.ts:3047 + :3155`) now assert `typeof === 'string'`, `Number.isFinite(Date.parse(...))`, and bounded-future TTL window.

Architect `/ce-code-review` (9 reviewers — correctness on opus; testing, maintainability, project-standards, learnings-researcher, api-contract, security, reliability, kieran-typescript on sonnet; ce-agent-native-reviewer skipped per PEvO CLAUDE.md): clean review on the substantive fix. Correctness verified storage tiers preserved (`memStore` still ms-precision via `memExpiresAtMs`; Redis SETEX still uses `FRESH_AUTH_TTL_SECONDS` integer), audit of all `expires_at` emitters spot-checked across `auth.ts:278/834/1258`, `custody.ts:1124`, `orcid.ts:695`, `signup-verify.ts:531,749` (all already `.toISOString()` — no other regression sites), and `Date.parse(numeric)` returns NaN on Node 20 so `Number.isFinite` mutation-kills both numeric and string-of-epoch-seconds regressions. Security: ms-precision side channel dismissed (anchor 25, `expires_at = Date.now()+300_000` is predictable from attacker's own clock + RTT, no per-mint nonce); DoS via mass-cache-bust resolved by fix (cache restored, mint rate drops ~10× per session).

Triaged findings: api-contract AC-01 P3 conf 75 (contract examples used `"2026-05-06T12:05:00Z"` without `.000Z` milliseconds while `toISOString()` always emits them) fixed in place during archive — `custody.md:108` and `orcid.md:208,239` now show `.000Z`. api-contract AC-02 P3 conf 75 (`POST /api/custody/session-auth` has no entry in `custody.md` — tests cite custody.md:108 which is the `/api/custody/fresh-auth` section) filed as architect-self-task `architect-custody-session-auth-contract-doc` in `tasks/review/`. Cross-reviewer ×3 finding (testing T-01 + reliability R1 + kieran-typescript KT-2 → conf 100, P3) on `301_000` vs `302_000` upper-bound inconsistency at `custody-consent-ops.test.ts:270` (vs `+302_000` in 3 sibling blocks) dismissed at triage — mutation-kill identical at either value, 1s vs 2s slack difference is cosmetic, slow-CI flake risk negligible. Pre-existing-bug residual surfaced by api-contract: `frontend/src/pages/orcid-callback.js:205-224` switch handles `signup`/`login`/`accredit`/`link`/`session_auth` but NOT `fresh_auth` → falls to `default` with error. Verified at architect intake: no SPA caller uses `mode: 'fresh_auth'` today (grep clean), so the gap is LATENT — will become live when ORCID-only consent ops ship (per `ui-multi-author-consent-affordances` in `blocked/`). Filed as `ui-orcid-callback-fresh-auth-mode-dispatch` in `tasks/pending/`.

Learnings-researcher: ×4 reviewer cross-corroboration on the wire-contract type-mismatch class (correctness+security+adversarial+api-contract all hit it in the parent UI review) matches the documented PEvO mandatory-capture signal per `wrapping-primitive-exhaustive-call-site-audit-2026-04-22.md` Case 3 precedent. No existing entry covers the `new Date(epoch-seconds)` → 1970 silent-misinterpretation failure mode or the test-stub-shape-divergence-masks-production-bug class. `/ce-compound` invocation triggered at archive checkpoint.

## BACKEND-CUSTODY-UPGRADE-LIMITER-SKIP-FAILED (archived 2026-05-16) — first-review clean (P0 deploy-blocker unblocker for `ui-custody-upgrade-seed-phrase-derive-flow`) ✓

P0 deploy-blocker unblocker. Acceptance #1+#2 (the substantive `skipFailedRequests: true` on `upgradeLimiter` at `backend/src/routes/custody.ts:50` + the `rateLimit` primitive option in `backend/src/middleware/rateLimit.ts`) landed in flight via commit `9210dd2` (round-2 of sibling task `backend-custody-upgrade-seed-phrase-reauth`). Closing-the-acceptance commit `f99d201` added the missing acceptance #3 canary test in `backend/tests/routes/custody-upgrade.test.ts` (16th test, "Hive getAccounts throws then recovers: 503 refunds limiter slot so the retry succeeds") + acceptance #4 sibling-limiter audit in the task signal block. Cascade closed: a transient Hive RPC blip during seed-phrase upgrade no longer burns the user's only retry slot for the hour with chain rotated and backend keys stale.

Architect `/ce-code-review` (7 reviewers — correctness on opus; testing, maintainability, project-standards, learnings-researcher, kieran-typescript, reliability on sonnet; ce-agent-native-reviewer skipped per PEvO CLAUDE.md): clean. Correctness verified the canary mutation-kill is load-bearing (`beforeEach` `clearRateLimitKeys(['custody-upgrade'])` neutralises cross-test ordering hazard with the preceding 503 test; `STATE_A_USER` row is reseeded with `upgraded_at=NULL`; `mockRejectedValueOnce` + `mockResolvedValue` ordering correct; removing `skipFailedRequests: true` would flip the retry assertion from 200 to 429). Architect-intake verified the audit grep claim by hand: `grep -rn "rateLimit(" backend/src/ --include="*.ts" | grep -v rateLimit.ts | wc -l` = 35 (matches signal); only `upgradeLimiter` opts in to `skipFailedRequests`. Reliability flagged the audit's HIGH-tagged sibling `accreditationRequestLimiter` (`backend/src/routes/accreditation.ts:25`) as a confirmed live 5xx-slot-burn cascade via the SMTP `sendMail` → `sendError(res, 500, ...)` path; filed as separate follow-up task `backend-accreditation-limiter-skip-failed` (architect call: not blocking this archive — audit was the explicit deliverable, and the recommendation matches implementer's own HIGH classification).

Triaged findings: kieran-typescript P1 conf 75 (canary asserts only `status===200`, miss `body.data.custody==='self'`) dismissed per dismiss-preemptive-test-hardening (theoretical mock-exhaustion mutation, existing `expect(retryRes.status).toBe(200)` is sufficient mutation-kill against the documented `skipFailedRequests` removal mutation, inline comment names the rationale). Reliability P2 conf 75 (symmetric 4xx-consumes-slot canary missing) dismissed for the same reason (preemptive pin against theoretical mutation; existing 401 canaries already pass through the limiter live). Below-gate residuals recorded: correctness RR1 (Redis skipFailed path defers consume via fire-and-forget IIFE — not applicable here, first request is 503 so no consume queued); maintainability MNT-R1+R2 (comment block duplicates task-file prose; signal-block acceptance numbering out of sequence #3,#2,#4 — both conf 40, cosmetic). Learnings-researcher cross-referenced `wrapping-primitive-exhaustive-call-site-audit-2026-04-22.md` (the audit-by-grep convention this signal block honors) and `defense-in-depth-canary-must-pin-each-layer-2026-05-07.md` (no gap — the upstream gate IS the canary's load-bearing target).

## UI-E2E-COAUTHOR-ACCREDITED-PREFILL (archived 2026-05-16) — round-3 clean (3 P1/P2 hold items landed; 0 findings) ✓

E2E spec `frontend/tests/e2e/coauthor-accredited-prefill.spec.js`. Round-2 hold raised 3 items: (1) P1 — delete single-call wrapper `pickTwoAccreditedResearchersWithOrcid`, inline `pickAccreditedResearchers` with the identical predicate at 3 call sites; (2) P1 — hoist `assertAccreditationsListContract` from per-test (3×) into `test.beforeAll` to make the HAF probe run once per spec run; (3) P2 — raise `waitForAccreditedDirectoryLoaded` timeout from 10s to 20s to absorb cold-HAF tail latency on CI (window-function CTE over `custom_json` since genesis on cold cache + scheduler contention + Alpine reactivity tick). Round-3 commit `01bd104` (61 lines, test-file only) landed all three: wrapper inlined at 3 sites with identical predicate, `beforeAll` hook placed at line 127 (between `ACCREDITED_BADGE_TEXT` constant and first `test(...)`), timeout raised on line 94 with extended rationale comment.

Architect `/ce-code-review` (5 reviewers, tight prompts): 0 primary findings. Project-standards P1 conf 100 (task-file mv split from fix-commit) dismissed at triage — same pattern as `ui-applyaccreditedprefill-reactivity-verify`: subsequent commit `be3703e ui(tasks): mv 3 round-N hold tasks to review/ after fix-landing` did the mv (batched for 3 tasks); file IS in review/ at architect-scan time; signal arrived just split across two commits. Process-discipline observation: prefer the canonical Edit→git add→git mv→commit shape per root CLAUDE.md "Shared-index race discipline" item 3. Correctness flagged 2 below-gate residual risks: Playwright `beforeAll` runs once-per-worker not once-per-invocation (matters only if `fullyParallel: true` is ever enabled at the test level), and the inlined `pickAccreditedResearchers` call sites can drift if a future edit changes one site's predicate without updating the others — both speculative. Maintainability flagged a single residual (predicate-rationale comment lives inside first test body, not at file scope) conf 50 below the anchor gate.

## UI-APPLYACCREDITEDPREFILL-REACTIVITY-VERIFY (archived 2026-05-16) — round-3 clean (1 P2 line-anchor drop landed; 0 findings) ✓

Round-2 hold (P2 conf 75) flagged the `(alpinejs/src/index.js:40-42)` parenthetical in `frontend/src/lib/accredited-directory.js`'s `applyAccreditedPrefill` docblock as drift-prone under the caret-range Alpine dep. Round-3 commit `0a11d31` (6 lines) dropped the parenthetical, preserving the symbol-anchored claim ("Alpine 3's reactivity engine is @vue/reactivity's `reactive()`, lazily deep-proxying nested objects on access including objects pushed into reactive arrays after init, `for...of` traversal yielding proxied rows"). Per `solutions/conventions/docblock-anchor-stable-symbols-not-line-numbers-2026-05-15.md`. Architect `/ce-code-review` (5 reviewers, tight prompts after 50% context cap): 4 clean. Project-standards flagged P1 conf 90 that the fix-commit didn't include the `tasks/pending/` → `tasks/review/` mv inline with the content edit; dismissed at triage — UI agent landed the mv in subsequent commit `be3703e ui(tasks): mv 3 round-N hold tasks to review/ after fix-landing`, file IS in review/ at architect-scan time, signal arrived (just split across two commits). Process observation: prefer the canonical Edit→git add→git mv→commit shape per root CLAUDE.md "Shared-index race discipline" item 3.

## UI-SEARCH-DOSEARCH-INFLIGHT-GUARD (archived 2026-05-16) — round-2 clean (2 P2 hold items landed; 0 findings) ✓

Round-1 implementation added an AbortController-based in-flight supersession guard to `doSearch` in `frontend/src/pages/search.js`. Round-1 hold raised 2 P2 items: (1) `frontend/src/api.js` `request()` helper dropped any caller-supplied `{signal}` because `init.signal` was being overridden by an unconditional `AbortSignal.timeout(...)` — regression for `researchers.js` and any future signal-passing adopter; (2) supersession-guard `catch` block had a third clause `(err && (err.name === 'AbortError' || err.code === 'ABORT_ERR'))` that was unreachable under the current control flow because every abort path goes through `_searchController.abort()` which synchronously sets both `signal.aborted = true` and reassigns `_searchController`. Round-2 commit `e01cd79` (20 lines across 2 files): item 1 composed signal via `AbortSignal.any([init.signal, AbortSignal.timeout(DEFAULT_TIMEOUT_MS)])` when caller passes signal (falls back to bare timeout otherwise), placed AFTER `...init` spread; item 2 took option (a) and removed the third clause, replacing the rationale comment.

Architect `/ce-code-review` on round-2 (6 reviewers — correctness on opus; testing/maintainability/project-standards/learnings/julik-frontend-races on sonnet; ce-agent-native skipped per PEvO CLAUDE.md; adversarial not triggered for <50 LOC non-auth diff; reliability skipped — julik covers the abort domain): 0 primary findings. Testing T-1 (conf 75, low) flagged the composed-signal branch in `api.test.js` as untested; demoted per Stage 5 step 6c (testing-only contributor, P2/P3 advisory). Maintainability M-1 (conf 72) flagged the rationale comment as slightly inaccurate (abort + reassignment are 3 lines apart with a `new AbortController()` between, not "synchronously" adjacent); below anchor-75 gate, suppressed. Julik explicitly confirmed both round-1 findings CLOSED. Learnings researcher pointed to 5 adjacent conventions (fetch-abort-controller-bounds-headers-only, pg-abortcontroller-budget, walker-early-exit-discipline, synchronous-flag-before-await, promise-race-loser-rejection) — all advisory; none describe a violated invariant in this diff.

## UI-CANRETRYUPGRADE-DISCRIMINATOR-KEY-REFACTOR (archived 2026-05-16) — round-2 clean (2 hold items landed; 2 findings demoted/suppressed) ✓

Round-7 follow-up from `ui-keychain-api-misuse` that bundled two cross-reviewer-converged findings on locale-text fragility of `canRetryUpgrade`. The refactor replaced translated-string comparisons with a typed `upgradeErrorKey` discriminator + `NON_RETRYABLE_KEYS` registry, addressing the i18n-fragility class once the locale-store change can no longer flip retry-ability silently. Round-1 implementation `14d65b7` landed the discriminator field, all 4 catch-branch writes, the audit at all 4 reset sites, and 16-locale i18n parity. Round-1 hold raised 2 items: (1) P1 cross-reviewer-converged (reliability + adversarial, conf 100) — the `TimeoutError/AbortError` catch shortcut at `settings.js:786` fired unconditionally regardless of `broadcastLanded`, so a future pre-broadcast Timeout/Abort (dhive hung connection, or a budgeted broadcast timeout added later) would route to non-retryable copy when retry is genuinely safe; (2) P2 maintainability — `NON_RETRYABLE_KEYS` array literal was allocated fresh on every reactive getter read inside the getter body, and the field-level comment pointed at a name only visible from inside the getter. Round-2 commit `0f9255c` (34 lines in `settings.js` only) gated the shortcut on `broadcastLanded` (semantically correct by construction rather than by assumption about which AbortSignal is in scope) and hoisted `NON_RETRYABLE_UPGRADE_ERROR_KEYS` to module top above `Alpine.data`, with the field comment updated to direct future devs to the module-top name.

Architect `/ce-code-review` on round-2 commit `0f9255c` (7 reviewers — correctness + adversarial on opus; testing/maintainability/project-standards/reliability/julik-frontend-races/learnings-researcher on sonnet; ce-agent-native skipped per PEvO CLAUDE.md): 0 primary findings after merge-pipeline gating. Testing reviewer raised a P2/testing-gap finding (no test for the new broadcastLanded=false pre-broadcast fall-through path); demoted per Stage 5 step 6c (P2 + advisory + all contributors testing/maintainability) AND per PEvO memory `feedback_dismiss_preemptive_test_hardening`. Adversarial reviewer raised a conf-50/medium finding (dhive sendOperations rejection after on-chain inclusion leaves `broadcastLanded=false`, so Try Again would re-broadcast with old-seed keys after the chain has rotated); suppressed at the anchor-75 confidence gate, and the reviewer marked it pre-existing — the split-response-vs-inclusion edge case is real but was not introduced or worsened by round-2. Recorded as residual risk for future consideration if a backend signal arrives indicating it materializes in practice. Reliability reviewer explicitly confirmed round-1 finding fully closed.

Learnings researcher noted the `broadcastLanded`-gate pattern (gate a catch shortcut on whether the irreversible async operation already landed) is a non-obvious convention worth `/ce-compound` capture. Architect declined per "err on the side of skipping" bias; pattern is documented in this task file (now in archive) and the gate's inline comment block at `settings.js:820-827` carries the rationale at the code site. Reset-site audit per `solutions/conventions/object-shape-fix-every-reset-site-2026-04-21.md` was followed during round-1 implementation per the task spec Step 6.

## UI-BRIDGE-REGISTER-LOCK-HELD-UX (archived 2026-05-16) — round-2 clean (2 P2 hold items landed; 0 findings) ✓

Task added a friendly LOCK_HELD-specific catch branch in `bridge.js handleRegister()` after the backend `backend-bridge-write-haf-lag-and-retry-amplification` round-2 rename distinguished "registration in progress, retry shortly" (`LOCK_HELD`) from "preprint already registered, see existing post" (`DUPLICATE`). Round-1 implementation `812e7cc` added the `err.code === 'LOCK_HELD'` branch with a transient-retry i18n key. Round-1 hold raised 2 P2 items: (1) `err && err.code === 'LOCK_HELD'` had a redundant null-guard inconsistent with sibling sites (`bridge.js:291` lookup catch and `settings.js` DUPLICATE branch both omit the prefix); (2) `console.warn('[bridge register]', err)` fired unconditionally before the LOCK_HELD branch — log spam on routine Redis-lock contention (35s TTL), against the project's logging-minimal bias and `frontend-error-sanitization-2026-04-21.md`'s warn-on-unexpected-only convention. Round-2 commit `1da2750` (12 lines in `bridge.js`) dropped the `err &&` prefix and reordered the catch so the LOCK_HELD branch returns BEFORE the warn. 39/39 unit tests in `pages-bridge.test.js` pass.

Architect `/ce-code-review` on round-2 commit `1da2750` (6 reviewers — correctness on opus; testing/maintainability/project-standards/learnings/julik-frontend-races on sonnet; ce-agent-native skipped per PEvO CLAUDE.md; adversarial not triggered for the 12-LOC fix; kieran-typescript not triggered for `.js`): 0 findings. Learnings researcher confirmed `frontend-error-sanitization-2026-04-21.md` is still satisfied and noted backend-side adjacent considerations (assertNever exhaustiveness on the lock discriminator, cache predicate audit) that are out of scope for this UI commit. No `/ce-compound`: both findings are direct applications of existing solutions docs, no new non-obvious learning.

## BE-ACCOUNT-CREATION-TOKENS-DROP (archived 2026-05-16) — round-4 clean (1 P3 comment-edit hold item landed; 0 findings) ✓

Task asked backend to eliminate the `account_creation_tokens` DB table and the reconcile path that kept it in sync with the on-chain `pending_claimed_accounts` counter after broadcast timeouts. The design pivot came from backend's premise challenge: the table is a reservation mutex (each row fungible, `used_for` written but never read), `FOR UPDATE SKIP LOCKED` defends against DB-row-allocation contention not chain double-spend, and chain consensus already serializes `create_claimed_account` ops — so the entire DI-1/DI-2/reconcile complexity surface was wrapping a chain primitive that didn't need wrapping (PEvO principle #1). Implementer commit `72978a0` dropped the table, removed the reservation/release dance, added a 10s Redis cache (`${appTag}:hive:pending_claimed_accounts:${hiveOnboardAccount}`) with invalidation on every successful `claim_account` or `create_claimed_account` broadcast, and translated consensus-rejection chain errors to the same retriable error shape as the pre-broadcast capacity check. Round-1 hold (2 P3 items): cache-del failure log promoted from `debug` → `logger.warn` (operator anchor when stale cache blocks signups for ≤10s) and tightened the consensus-rejection regex from two loose alternatives to a strict alternation `/assertion failed: pending_claimed_accounts|no available account creation/i` with a `logger.warn` of the original error before throw to preserve diagnostic context (landed `3736932`). Round-2 hold (4 P3 items): mirror positive-case test for the second regex arm, `err: expect.any(Error)` pin on the cache-del warn matcher, dual-call-site failure-mode rewrite of the catch-block comment, dot-namespaced event-slug rename (`pending_claim_cache_invalidate_failed` → `account_creation.cache.invalidate_failed`; `create_claimed_account_consensus_rejected` → `account_creation.broadcast.consensus_rejected`) to align with sibling modules (landed `621e9de`). Round-3 hold (1 P3 item): replaced a forward-reference phrase in the dual-call-site comment ("trips the consensus-rejection regex below and gets translated to") with a behavior-anchored phrase ("surfaces as a chain rejection, translated to") — the regex lives in a separate function `createClaimedAccount` ~150 lines later, so a future refactor of that catch site could have silently invalidated the comment (landed `371e78c`).

Architect `/ce-code-review` on round-4 commit `371e78c` (5 reviewers — correctness on opus; testing/maintainability/project-standards on sonnet; ce-learnings-researcher on sonnet; ce-agent-native skipped per PEvO CLAUDE.md; conditional personas not triggered for the comment-only diff): 0 findings. Learnings researcher confirmed the round-4 fix is the canonical resolution shape prescribed by two recent solutions docs (`task-slug-citations-in-comments-go-stale-on-archive-2026-05-15.md` and `docblock-anchor-stable-symbols-not-line-numbers-2026-05-15.md`) — both conventions explicitly cover the spatial / forward-reference rot pattern the original "below" phrasing exhibited. No `/ce-compound`: the convention is already documented, no new non-obvious learning to capture. Carve-out criterion (c) follow-up task `backend-account-creation-logger-spy-real-path-companion.md` remains in `tasks/pending/` per round-2's filing (depends on this task's round-3 slug-rename landing; not blocking archive). The two pre-existing `[TODO Architect]` items at the bottom of the task file (api-contracts language sweep + predecessor task supersession) were verified stale at archive time — `auth.md` no longer contains the legacy "claim token" phrasing and the predecessor `backend-claim-account-chain-reconcile.md` was already deleted in commit `935ccc2`.

## UI-MOUNT-EDITORS-DESTROYED-GUARD (archived 2026-05-16) — round-2 clean (2 hold items landed; 0 findings) ✓

Closed a `_mountEditors()` teardown-during-init race in both `frontend/src/pages/publish.js` and `frontend/src/pages/edit.js`. Round-1 (commit `12c610e`) added the post-await destroyed-guard (`if (!this._mounted) return;` after `await import('../editor.js')` resolves). Round-2 scope was broadened to also cover the mount-during-mount race (Retry click landing between the `_loadInFlight` mutex clear and the editor import resolution → second `_mountEditors` schedules, both call `createEditor` on same $refs, first instance pair leaks): commit `55e21cb` added the synchronous-prefix idempotency guard (`if (this._editorsInitialized) return; this._editorsInitialized = true;` before the `await`) with a `_editorsInitialized = false;` reset on both the post-await destroyed-guard early-return path AND `destroy()`, plus 2 unit test cases per page (concurrent-call idempotency + destroy-clears-flag). Round-2 architect re-review of `12c610e + 55e21cb` found the round-1 destroyed-during-mount tests were structurally vacuous: their `expect(mockCreateEditor).not.toHaveBeenCalled()` assertion stayed green against the "remove the mounted-guard block" mutation because production's null-ref guards (`if (abstractEl)`, `if (bodyEl)`) suppress `createEditor` independently. Round-2 hold raised 2 P2 items to add `expect(comp._editorsInitialized).toBe(false)` to each destroyed-during-mount test. Commit `4ad456d` landed both items mirror-parity.

Architect round-2 re-review of `4ad456d` dispatched 6 personas (correctness Opus; testing/maintainability/project-standards/julik-frontend-races/learnings-researcher Sonnet; `ce-agent-native-reviewer` skipped per project CLAUDE.md). Zero confidence-gate-passing findings. Correctness verified the mutation-kill trace structurally: `destroy()` flips `_mounted=false` then sets `_editorsInitialized=false`; at `_mountEditors` entry both are false; synchronous prefix sets `_editorsInitialized=true`; mounted-guard is the only path that resets it; null-ref guards do NOT reset. Removing the mounted-guard leaves `_editorsInitialized=true` and the assertion fails. Clean kill for both `pages-edit.test.js` and `pages-publish.test.js`. Learnings-researcher confirmed both round-2-cited conventions (`mutation-kill-claims-must-match-assertion-and-corpus-2026-05-15.md`, `defense-in-depth-canary-must-pin-each-layer-2026-05-07.md`) are satisfied. Julik flagged a P3 testing-gap (synchronous-prefix mutation uncovered by this test) — verified incorrect: the sibling idempotency test in the same describe block already kills the prefix mutation via `expect(mockCreateEditor.mock.calls.length).toBe(2)`. Dismissed at triage. JFR-R2-RR-001 (`_editorsInitialized` undeclared in Alpine data initializer) carried preemptive-hardening dismissal from round-2 hold per `feedback_dismiss_preemptive_test_hardening`.

`/ce-compound` capture queued for the synchronous-flag-before-await idempotency idiom (set the guard flag BEFORE the first `await` boundary, reset only on post-await early-return paths) — non-obvious learning that no existing `agents/docs/solutions/` entry captures; adjacent to but distinct from the per-layer canary doc.

## UI-AUTHOR-INPUT-ACCREDITED-PREFILL (archived 2026-05-16) — round-2 clean (3 hold items landed; 0 findings) ✓

Prefilled and locked ORCID inputs on the publish/edit pages' authors-list when the author's `hive` field resolved to a currently-accredited account; surfaced an `(accredited)` indicator on the row. Round-1 (commits `d1d8935`, `ae7e853`, `820a710`, `eb1416b`) introduced `frontend/src/lib/accredited-directory.js` (cached-singleton + in-flight coalescing + normalization) and integrated it into `publish.js` / `edit.js` via shared `applyHiveChangePrefill` + `applyAccreditedPrefill` helpers. Round-1 architect hold listed 9 items spanning correctness (clear-ORCID on accredited→non-accredited transition, reapplication-loop preserving user-typed), testing (page-integration coverage at both pages, concurrent-rejection coalescing, undefined-orcid-in-directory), maintainability (dedupe `_loadAccreditedDirectory`, drop speculative `accreditedCoAuthor`), and project-standards (carve-out justification, idiom unification). All 9 landed.

Round-2 architect hold listed 3 items: (1) `pages-edit.test.js` missing the stay-accredited transition test that `pages-publish.test.js` had; (2) speculative `filterAccreditedByPrefix` export with zero consumers; (3) undocumented + untested no-negative-cache retry semantic in `loadAccreditedDirectory`'s catch. Round-2 (commits `4a28f79`, `b0bc48c`) landed all 3: added the alice→bob stay-accredited test to pages-edit, deleted `filterAccreditedByPrefix` + its 4 unit tests + named import, and added both an inline catch-block comment documenting the intentional `cache` non-assignment + a regression test asserting `fetchAccreditations` is called twice across rejection→success.

Architect round-2 re-review dispatched 7 personas (correctness Opus; testing/maintainability/project-standards/julik-frontend-races/previous-comments/learnings-researcher Sonnet; `ce-agent-native-reviewer` skipped per PEvO CLAUDE.md). Zero P0-P3 findings. `previous-comments` verified all 3 round-2 items as fixed. Testing flagged two preemptive-hardening residuals (warnSpy assertion, `isNewCoAuthorAccredited` post-transition) — both auto-dismissed per project bias (`feedback_dismiss_preemptive_test_hardening`). Julik flagged the comment's "re-enter this IIFE" phrasing as imprecise — documentation only, no functional concern. Learnings-researcher cross-cited two reinforcing conventions (`per-request-memo-catch-block-negative-cache-contract-2026-05-06` and `caching-wrapper-discriminated-union-poisoning-2026-05-11`); diff is conformant to both.

Cross-references retained: `agents/docs/tasks/blocked/ui-multi-author-consent-affordances.md` (adjacent task), `tasks/pending/ui-applyaccreditedprefill-reactivity-verify.md` (round-2 split investigation), `tasks/pending/architect-orcid-typed-vs-accredited-supersession-spec.md` (round-2 architect-spawn for the typed-vs-accredited supersession question).

## UI-E2E-HAF-FIXTURE-GAPS (archived 2026-05-16) — first-review clean (1 P2 + 1 P3 dismissed at triage; 0 holds) ✓

Converted 4 full-suite Playwright E2E specs from hard fixture-shortage assertions (`expect(target).toBeTruthy()` / `throw new Error(...)`) to `test.skip(condition, reason)` with named-fixture reasons. Sparse-HAF runs (test-mode `pevo_app_test` with 0–1 papers, ≤6 accreditations, no reviews from non-`pboulet` authors) previously reported `failed`; now report `skipped` with a clear reason naming the missing fixture shape. Implementer (commit `2649e37`) picked Path 2 (skip-with-reason) from the task's 3 remediation paths — lighter than seeding testnet, satisfies the "must NOT silently pass" rule. Specs touched: `paper-detail.spec.js` (reviews-AND-votes conjunction), `papers-browse.spec.js` (filter-narrows-to-non-empty + consensus-search), `researchers.spec.js` (non-empty accreditations + accredited-researcher-with-paper), `vote-comment.spec.js` (4 conversions — voter + non-self target for upvote, commenter + non-self-reviews target for comment-reply).

Architect first review dispatched 5 personas (correctness Opus; testing/maintainability/project-standards/learnings-researcher Sonnet; `ce-agent-native-reviewer` skipped per PEvO CLAUDE.md). Zero correctness defects: all 8 skip conversions verified to halt execution correctly via Playwright's `TestSkipError`; downstream property accesses unreachable. Project-standards clean. Maintainability flagged one P2 (mid-test `test.skip` after side-effectful awaits in vote-comment.spec.js lines 75/123/144 — structurally forced by the discovery-loop pattern, no actionable fix). Learnings flagged a silent-green hazard at `papers-browse.spec.js:114`: the defensive `(searchBody.data || []).find(...)` fallback conflates HAF-data-absent with API-envelope regression — backend shape change would silently skip instead of failing. User dismissed both at triage (the silent-skip would surface as "consensus search-card flow never exercised" on next investigation; the mid-test cognitive-cost finding is structurally inherent to the test shape).

## UI-EDIT-NARROW-GATING-DROP-ACCREDITED-FALLBACK (archived 2026-05-16) — round-2 clean (4 hold items landed; 0 findings) ✓

Narrowed `/edit/:author/:permlink` access from "any accredited user" to "original author + named co-authors + accepted authorship-claimers". Round-1 (commit `00d3f97`) shipped the `isAuthorized` getter narrowing + template collapse + 32 i18n string updates + 4 unit-test cases. Architect round-1 hold listed 4 items (P1 16-locale `howToEditClaim` trailing-clause trim, P2 dead-getter deletion at `edit.js:417-420`, P3 `isAuthorized` 4-case `describe` block in `pages-edit.test.js`, P3 dead-key cleanup `edit.notAuthorized` + `edit.accreditationRequired` across 16 locales + STUBS.md trim). Round-2 (commit `e9adf2b`) landed all 4 items in a single commit via a Python script driving the i18n trims.

Architect round-2 re-review dispatched 5 personas (correctness Opus; testing/maintainability/project-standards/ce-learnings-researcher Sonnet; `ce-agent-native-reviewer` skipped per PEvO CLAUDE.md): zero findings. Mutation-kill validity for the new `isAuthorized` unit tests confirmed by both correctness and testing reviewers (restoring `|| this.isAccredited` to the getter makes the "accredited non-author with no claim" assertion flip from `false` to `true` via the Alpine store mock's `isAccredited: true` default). Learnings-researcher surfaced cross-surface parity audit (publish/review/edit) as a potential follow-up direction; deferred — narrow-gating's "Out of scope" carved that out, and `tasks/review/ui-gating-coherence-publish-review-edit.md` was archived 2026-05-15 via alt-shape clause.

## ARCHITECT-ORCID-TYPED-VS-ACCREDITED-SUPERSESSION-SPEC (archived 2026-05-16) — spec landed, two downstream tasks filed ✓

Architect-self-task: defines the read-time supersession rule for ORCID values on `pevo.authors[]`. Rule: chain-stored `authors[i].orcid` is the publisher's frozen claim at broadcast time; the **accreditation-attested ORCID** (from `active_accreditations` per `authors[i].hive`) supersedes the chain value for **canonical display** when the hive account is currently accredited AND the accreditation carries a non-empty `orcid`. Four supersession cases enumerated (no hive, hive-not-accredited, hive-accredited-no-orcid, hive-accredited-with-orcid). Discrepancy is surfaced via two additive API fields, not by mutating chain history or introducing a new on-chain `verified_orcid` field (explicitly out of scope per the parent dialog).

Landed in: `agents/docs/hive-schemas.md` § 1.1 (supersession rule + canonical SQL pattern using the existing `activeAccreditationsCteBody` CTE with LEFT JOIN + `jsonb_array_elements WITH ORDINALITY`); `agents/docs/api-contracts/papers.md` (PaperSummary + PaperDetail `authors[]` extended with `orcid_verified` and `orcid_discrepancy`); `agents/docs/reputation-algorithm.md` § "ORCID-keyed Aggregations" (clarifies no algo change today — reputation aggregates by hive account; `authorshipClaimsCteBody`'s ORCID auto-accept is a slot-matching trigger, not an aggregation; future ORCID-keyed queries MUST honor the supersession via LEFT JOIN onto `active_accreditations`).

Two downstream implementation tasks filed: `tasks/pending/backend-papers-canonical-orcid-resolution.md` (backend emits the two new fields on PaperSummary + PaperDetail via the canonical SQL pattern; tests cover all four supersession cases) and `tasks/pending/ui-paper-detail-orcid-discrepancy-indicator.md` (frontend uses `orcid_verified ?? orcid` for canonical display and renders a discrepancy affordance when `orcid_discrepancy === true`; blocked on the backend task). Source: user-architect dialog on 2026-05-16 during round-2 triage of `ui-author-input-accredited-prefill` finding #15 — user clarified that authors can type an ORCID for co-authors without a hive account, but once that co-author is on hive, their authed ORCID supersedes the typed one if different. Skipped `/ce-doc-review` and `/ce-compound` — the rule is short, distilled directly from a recent user dialog, and the canonical SQL pattern leans entirely on a documented existing CTE (`activeAccreditationsCteBody`) and a documented existing convention (`accreditation-state-read-latest-action-wins-2026-05-15.md`); no non-obvious learning specific to this task.

## BE-CANONICAL-WALKER-CANARY-LAYER-MUTATION-KILL (archived 2026-05-16) — round-2 clean (2 P3 findings dismissed) ✓

Round-2 hold-fixes commit `6f3d356` landed all 6 items from the architect's prior hold: (1) `afterEach(() => vi.restoreAllMocks())` guard at describe level with corrected pool-restore comment (clarifies `beforeEach` is the next-test isolation primitive); (2) pino-spy-level-filter cross-reference comments at the `sql_filter_or_missing` debug site in `papers.ts` AND at the SQL canary's `vi.spyOn(logger, 'debug')` in the test file (both cite `pino-spy-level-filter-ordering-trap-2026-05-07.md`); (3) BRITTLENESS WARNING block above the inline `/'type'/.test(sql)` discriminator in `installTypeSpoofStartResponder` (states SQL-parametrization sweep would break regex match → false alarm, instructs future readers not to "fix" by relaxing); (4) NEW 5th canary `'pins SQL IS NOT NULL guard on initial probe ...'` using the same faithful-mock dispatch pattern as the SQL canary, pinning the `c.json_metadata -> $3 -> 'continues' IS NOT NULL` predicate (Mutation E → canary FAILS RED); (5) closed by construction (item 4's canary pins the IS NOT NULL invariant that `cont_columns_invalid` warn-level depended on); (6) level-discipline comment reshaped from list-form to rule-form keyed on `CanonicalRootBailReason` type alias as SSOT for which reasons exist. Mutation-kill matrix re-attested 5 canaries × 6 mutations: each canary FAIL RED on its targeted mutation, PASS on the orthogonal four. tsc + lint clean; vitest 20/20 (was 19; +1 from item 4).

