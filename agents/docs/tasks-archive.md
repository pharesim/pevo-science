## UI-AUTHOR-INPUT-ACCREDITED-PREFILL (archived 2026-05-16) — round-2 clean (3 hold items landed; 0 findings) ✓

Prefilled and locked ORCID inputs on the publish/edit pages' authors-list when the author's `hive` field resolved to a currently-accredited account; surfaced an `(accredited)` indicator on the row. Round-1 (commits `d1d8935`, `ae7e853`, `820a710`, `eb1416b`) introduced `frontend/src/lib/accredited-directory.js` (cached-singleton + in-flight coalescing + normalization) and integrated it into `publish.js` / `edit.js` via shared `applyHiveChangePrefill` + `applyAccreditedPrefill` helpers. Round-1 architect hold listed 9 items spanning correctness (clear-ORCID on accredited→non-accredited transition, reapplication-loop preserving user-typed), testing (page-integration coverage at both pages, concurrent-rejection coalescing, undefined-orcid-in-directory), maintainability (dedupe `_loadAccreditedDirectory`, drop speculative `accreditedCoAuthor`), and project-standards (carve-out justification, idiom unification). All 9 landed.

Round-2 architect hold listed 3 items: (1) `pages-edit.test.js` missing the stay-accredited transition test that `pages-publish.test.js` had; (2) speculative `filterAccreditedByPrefix` export with zero consumers; (3) undocumented + untested no-negative-cache retry semantic in `loadAccreditedDirectory`'s catch. Round-2 (commits `4a28f79`, `b0bc48c`) landed all 3: added the alice→bob stay-accredited test to pages-edit, deleted `filterAccreditedByPrefix` + its 4 unit tests + named import, and added both an inline catch-block comment documenting the intentional `cache` non-assignment + a regression test asserting `fetchAccreditations` is called twice across rejection→success.

Architect round-2 re-review dispatched 7 personas (correctness Opus; testing/maintainability/project-standards/julik-frontend-races/previous-comments/learnings-researcher Sonnet; `ce-agent-native-reviewer` skipped per PEvO CLAUDE.md). Zero P0-P3 findings. `previous-comments` verified all 3 round-2 items as fixed. Testing flagged two preemptive-hardening residuals (warnSpy assertion, `isNewCoAuthorAccredited` post-transition) — both auto-dismissed per project bias (`feedback_dismiss_preemptive_test_hardening`). Julik flagged the comment's "re-enter this IIFE" phrasing as imprecise — documentation only, no functional concern. Learnings-researcher cross-cited two reinforcing conventions (`per-request-memo-catch-block-negative-cache-contract-2026-05-06` and `caching-wrapper-discriminated-union-poisoning-2026-05-11`); diff is conformant to both.

Cross-references retained: `agents/docs/tasks/blocked/ui-multi-author-consent-affordances.md` (adjacent task), `tasks/pending/ui-applyaccreditedprefill-reactivity-verify.md` (round-2 split investigation), `tasks/pending/architect-orcid-typed-vs-accredited-supersession-spec.md` (round-2 architect-spawn for the typed-vs-accredited supersession question).

## UI-E2E-HAF-FIXTURE-GAPS (archived 2026-05-16) — first-review clean (1 P2 + 1 P3 dismissed at triage; 0 holds) ✓

Converted 4 full-suite Playwright E2E specs from hard fixture-shortage assertions (`expect(target).toBeTruthy()` / `throw new Error(...)`) to `test.skip(condition, reason)` with named-fixture reasons. Sparse-HAF runs (test-mode `pevo_app_test` with 0–1 papers, ≤6 accreditations, no reviews from non-`pboulet` authors) previously reported `failed`; now report `skipped` with a clear reason naming the missing fixture shape. Implementer (commit `2649e37`) picked Path 2 (skip-with-reason) from the task's 3 remediation paths — lighter than seeding testnet, satisfies the "must NOT silently pass" rule. Specs touched: `paper-detail.spec.js` (reviews-AND-votes conjunction), `papers-browse.spec.js` (filter-narrows-to-non-empty + consensus-search), `researchers.spec.js` (non-empty accreditations + accredited-researcher-with-paper), `vote-comment.spec.js` (4 conversions — voter + non-self target for upvote, commenter + non-self-reviews target for comment-reply).

Architect first review dispatched 5 personas (correctness Opus; testing/maintainability/project-standards/learnings-researcher Sonnet; `ce-agent-native-reviewer` skipped per PEvO CLAUDE.md). Zero correctness defects: all 8 skip conversions verified to halt execution correctly via Playwright's `TestSkipError`; downstream property accesses unreachable. Project-standards clean. Maintainability flagged one P2 (mid-test `test.skip` after side-effectful awaits in vote-comment.spec.js lines 75/123/144 — structurally forced by the discovery-loop pattern, no actionable fix). Learnings flagged a silent-green hazard at `papers-browse.spec.js:114`: the defensive `(searchBody.data || []).find(...)` fallback conflates HAF-data-absent with API-envelope regression — backend shape change would silently skip instead of failing. User dismissed both at triage (the silent-skip would surface as "consensus search-card flow never exercised" on next investigation; the mid-test cognitive-cost finding is structurally inherent to the test shape).

## UI-EDIT-NARROW-GATING-DROP-ACCREDITED-FALLBACK (archived 2026-05-16) — round-2 clean (4 hold items landed; 0 findings) ✓

Narrowed `/edit/:author/:permlink` access from "any accredited user" to "original author + named co-authors + accepted authorship-claimers". Round-1 (commit `00d3f97`) shipped the `isAuthorized` getter narrowing + template collapse + 32 i18n string updates + 4 unit-test cases. Architect round-1 hold listed 4 items (P1 16-locale `howToEditClaim` trailing-clause trim, P2 dead-getter deletion at `edit.js:417-420`, P3 `isAuthorized` 4-case `describe` block in `pages-edit.test.js`, P3 dead-key cleanup `edit.notAuthorized` + `edit.accreditationRequired` across 16 locales + STUBS.md trim). Round-2 (commit `e9adf2b`) landed all 4 items in a single commit via a Python script driving the i18n trims.

Architect round-2 re-review dispatched 5 personas (correctness Opus; testing/maintainability/project-standards/ce-learnings-researcher Sonnet; `ce-agent-native-reviewer` skipped per PEvO CLAUDE.md): zero findings. Mutation-kill validity for the new `isAuthorized` unit tests confirmed by both correctness and testing reviewers (restoring `|| this.isAccredited` to the getter makes the "accredited non-author with no claim" assertion flip from `false` to `true` via the Alpine store mock's `isAccredited: true` default). Learnings-researcher surfaced cross-surface parity audit (publish/review/edit) as a potential follow-up direction; deferred — narrow-gating's "Out of scope" carved that out, and `tasks/review/ui-gating-coherence-publish-review-edit.md` was archived 2026-05-15 via alt-shape clause.

## ARCHITECT-ORCID-TYPED-VS-ACCREDITED-SUPERSESSION-SPEC (archived 2026-05-16) — spec landed, two downstream tasks filed ✓

Architect-self-task: defines the read-time supersession rule for ORCID values on `pevo.authors[]`. Rule: chain-stored `authors[i].orcid` is the publisher's frozen claim at broadcast time; the **accreditation-attested ORCID** (from `active_accreditations` per `authors[i].hive`) supersedes the chain value for **canonical display** when the hive account is currently accredited AND the accreditation carries a non-empty `orcid`. Four supersession cases enumerated (no hive, hive-not-accredited, hive-accredited-no-orcid, hive-accredited-with-orcid). Discrepancy is surfaced via two additive API fields, not by mutating chain history or introducing a new on-chain `verified_orcid` field (explicitly out of scope per the parent dialog).

Landed in: `agents/docs/hive-schemas.md` § 1.1 (supersession rule + canonical SQL pattern using the existing `activeAccreditationsCteBody` CTE with LEFT JOIN + `jsonb_array_elements WITH ORDINALITY`); `agents/docs/api-contracts/papers.md` (PaperSummary + PaperDetail `authors[]` extended with `orcid_verified` and `orcid_discrepancy`); `agents/docs/reputation-algorithm.md` § "ORCID-keyed Aggregations" (clarifies no algo change today — reputation aggregates by hive account; `authorshipClaimsCteBody`'s ORCID auto-accept is a slot-matching trigger, not an aggregation; future ORCID-keyed queries MUST honor the supersession via LEFT JOIN onto `active_accreditations`).

Two downstream implementation tasks filed: `tasks/pending/backend-papers-canonical-orcid-resolution.md` (backend emits the two new fields on PaperSummary + PaperDetail via the canonical SQL pattern; tests cover all four supersession cases) and `tasks/pending/ui-paper-detail-orcid-discrepancy-indicator.md` (frontend uses `orcid_verified ?? orcid` for canonical display and renders a discrepancy affordance when `orcid_discrepancy === true`; blocked on the backend task). Source: user-architect dialog on 2026-05-16 during round-2 triage of `ui-author-input-accredited-prefill` finding #15 — user clarified that authors can type an ORCID for co-authors without a hive account, but once that co-author is on hive, their authed ORCID supersedes the typed one if different. Skipped `/ce-doc-review` and `/ce-compound` — the rule is short, distilled directly from a recent user dialog, and the canonical SQL pattern leans entirely on a documented existing CTE (`activeAccreditationsCteBody`) and a documented existing convention (`accreditation-state-read-latest-action-wins-2026-05-15.md`); no non-obvious learning specific to this task.

## BE-CANONICAL-WALKER-CANARY-LAYER-MUTATION-KILL (archived 2026-05-16) — round-2 clean (2 P3 findings dismissed) ✓

Round-2 hold-fixes commit `6f3d356` landed all 6 items from the architect's prior hold: (1) `afterEach(() => vi.restoreAllMocks())` guard at describe level with corrected pool-restore comment (clarifies `beforeEach` is the next-test isolation primitive); (2) pino-spy-level-filter cross-reference comments at the `sql_filter_or_missing` debug site in `papers.ts` AND at the SQL canary's `vi.spyOn(logger, 'debug')` in the test file (both cite `pino-spy-level-filter-ordering-trap-2026-05-07.md`); (3) BRITTLENESS WARNING block above the inline `/'type'/.test(sql)` discriminator in `installTypeSpoofStartResponder` (states SQL-parametrization sweep would break regex match → false alarm, instructs future readers not to "fix" by relaxing); (4) NEW 5th canary `'pins SQL IS NOT NULL guard on initial probe ...'` using the same faithful-mock dispatch pattern as the SQL canary, pinning the `c.json_metadata -> $3 -> 'continues' IS NOT NULL` predicate (Mutation E → canary FAILS RED); (5) closed by construction (item 4's canary pins the IS NOT NULL invariant that `cont_columns_invalid` warn-level depended on); (6) level-discipline comment reshaped from list-form to rule-form keyed on `CanonicalRootBailReason` type alias as SSOT for which reasons exist. Mutation-kill matrix re-attested 5 canaries × 6 mutations: each canary FAIL RED on its targeted mutation, PASS on the orthogonal four. tsc + lint clean; vitest 20/20 (was 19; +1 from item 4).

Architect `/ce-code-review` (9 reviewers — correctness + security + adversarial on Opus; testing + maintainability + project-standards + reliability + kieran-typescript + learnings on Sonnet; `ce-agent-native-reviewer` skipped per PEvO CLAUDE.md): 2 findings surviving anchor-75 gate, both P3, both dismissed at user triage. (a) Adversarial paired-mutation cascade where future agent downgrades `cont_columns_invalid` warn→debug AND drops IS NOT NULL predicate → IS-NOT-NULL canary's negative assertion `warnSpy.toHaveLength(0)` holds vacuously — multi-step regression dismissed per `feedback_dismiss_preemptive_test_hardening` (theoretical-only failure mode). (b) Reliability finding that `.env.example` is silent on the `LOG_LEVEL=debug` opt-in for `sql_filter_or_missing` production observability — operator-side gap dismissed per `feedback_pevo_logging_minimal` (default-dismiss logging-related findings). 12 below-gate findings (adversarial ADV-2/3/4/5, maintainability RR-1/2/3, kieran KT-R2-01/02/03/04, reliability REL-2/3/4) all suppressed by the confidence gate. Learnings-researcher HONORED all 9 surfaced conventions (pino-spy-level-filter, tests-must-fail-on-mutation, defense-in-depth-canary-must-pin-each-layer, mock-carve-out clause c, mutation-kill-claims-must-match-assertion-and-corpus, event-label-granularity-tier, vi-spyon-mockimplementation, symmetric-walker-convention, per-request-memo-catch-block).

**Cross-references:** `backend/src/routes/papers.ts:~1295-1410` (findCanonicalRoot level-discipline + sql_filter_or_missing pino-spy cross-ref + 4 START-bail event sites), `backend/tests/routes/canonical-root-walker.test.ts:~80 afterEach + ~127-160 installTypeSpoofStartResponder + ~430-1311 5 layer-pinning canaries`. Sibling tasks `backend-canonical-walker-cycle-detection` and `backend-haf-walker-wall-clock-budget` both held round-1 on 2026-05-16 with cross-task event-naming convergence in the cycle task's round-2 (hopIndex symmetry across cycle/unauthorized_hop/depth_exceeded events). Skipped `/ce-compound` — all relevant conventions were already documented in `agents/docs/solutions/conventions/`; no non-obvious learning surfaced by this round.

## UI-ORCID-CALLBACK-POST-BROADCAST-FAILED-HANDLER (archived 2026-05-16) — round-2 clean ✓

Implementer commit `64476ce` lands the 4 round-1 hold items: (1) second branch in `_verify()` catch matching `err.code === 'POST_BROADCAST_OPERATOR_REQUIRED' && err?.details?.outcome === 'confirmed'` immediately after the existing POST_BROADCAST_FAILED branch (same `errorAction = 'settings'`, distinct copy via new key `orcid.postBroadcastOperatorRequired` with operator-contact framing rather than "give it a moment to sync"); (2) new i18n key in en.json + 15 non-English locale stubs + STUBS.md sweep heading `### Added 2026-05-15 (UI-ORCID-CALLBACK-POST-BROADCAST-FAILED-HANDLER)`; (3) 4 mirror unit tests in `frontend/tests/unit/pages-orcid-callback.test.js` (happy path + 3 fallthrough mutation guards: missing outcome / wrong outcome / no details); (4) errorAction enumeration comment at line 69 extended to include both POST_BROADCAST codes with their `(outcome:'confirmed')` discriminators. 46 unit tests green.

Architect `/ce-code-review` (7 reviewers — correctness on Opus; testing, maintainability, project-standards, learnings-researcher, api-contract, previous-comments on Sonnet; `ce-agent-native-reviewer` skipped per PEvO CLAUDE.md; adversarial/security not selected for ~30 LoC SPA branch + 80 LoC tests with no auth surface): all 4 hold items VERIFIED-FIXED by the previous-comments reviewer. Zero P0-P2 findings. One P3 conf 50 single-reviewer maintainability nit on the errorAction comment line length (suppressed by anchor gate). Learnings-researcher surfaced 5 Known Patterns (chain-write-timeout-ambiguous-outcome, test-marker-stub-vacuous-or-fallback, frontend-error-sanitization, mutation-kill-claims-must-match-assertion-and-corpus, inner-catch-shadows-outer-catch); each spot-checked clean against the diff. API-contract reviewer noted that `backend/src/routes/signup-verify.ts` and `backend/src/routes/accreditation.ts` emit POST_BROADCAST codes with no corresponding SPA branches in `signup-verify.js` / `accreditation-verify.js` (cross-resource `common.md:74` MUST gap), but explicitly out-of-scope for this ORCID-callback-scoped task.

**Cross-references:** `agents/docs/api-contracts/orcid.md` (POST_BROADCAST_FAILED + POST_BROADCAST_OPERATOR_REQUIRED bullets), `agents/docs/api-contracts/common.md:74` (cross-resource client-MUST for both codes), `backend/src/routes/orcid.ts:871-886` (accredit emit site) + `:1035-1039` (link emit site), `backend/src/lib/broadcast-error.ts:497-500` (PostBroadcastWriteError envelope shaper). Skipped `/ce-compound` — the contract is already canonical and the implementation pattern follows the existing POST_BROADCAST_FAILED branch exactly; no non-obvious learning specific to this task.

## UI-ORCID-CALLBACK-SETTINGS-CTA-LABEL (archived 2026-05-16) — first-review clean ✓

Implementer commit `67f6370` replaces `$t('common.tryAgain')` with `$t('common.verifyInSettings')` on the `errorAction === 'settings'` template branch in `frontend/src/pages/orcid-callback.js:45`. The "Try Again" label was misleading on two of the three error codes routing to `errorAction = 'settings'`: BROADCAST_TIMEOUT ("verify in Settings before retrying") and POST_BROADCAST_FAILED/OPERATOR_REQUIRED with `outcome: 'confirmed'` ("Your ORCID is linked. Verify in Settings"). The `recover` / `''` / `null` template branches keep `common.tryAgain` (those are genuine retry affordances). New i18n key in en.json + 15 non-English locale stubs + STUBS.md sweep heading `### Added 2026-05-15 (UI-ORCID-CALLBACK-SETTINGS-CTA-LABEL)`. Per task spec acceptance §4 ("No test changes are strictly required — the template change is a copy edit, not a behavior change"), no unit-test changes were required.

Architect `/ce-code-review` (5 reviewers — correctness on Opus; testing, maintainability, project-standards, learnings-researcher on Sonnet; `ce-agent-native-reviewer` skipped per PEvO CLAUDE.md; api-contract / security / adversarial / previous-comments not selected — no contract change, no auth surface, single-line label swap, first review): zero findings across all reviewers. Learnings-researcher surfaced `alpine-factory-exposure-vs-template-mutation-coverage-2026-04-28.md` as the relevant convention — template-expression changes have mutation sensitivity zero against unit tests asserting only factory shape / state. Task spec already documents this trade-off ("copy edit, not behavior change"); e2e is the implicit probabilistic backstop. No conscious-acceptance gap.

**Cross-references:** `agents/docs/solutions/conventions/alpine-factory-exposure-vs-template-mutation-coverage-2026-04-28.md` (the template-mutation-coverage convention this task's acceptance §4 implicitly invokes). Skipped `/ce-compound` — the convention this task aligns with is already documented; the single-line template label fix doesn't surface a new cross-cutting learning.

## UI-COAUTHOR-CONTINUATION-PUBLISHING (archived 2026-05-16) — round-3 clean ✓

Implementer commit `37109a3` lands the 4 round-3 hold items: (1) **REMOVAL** of the speculative `showMetadataIncompleteWarning` feature entirely (architect-authorized "OR remove" path) — the predicate fired only for legitimate first-time co-author publish (false positive) and could not fire for the malformed-metadata case it was supposed to cover; architect's heuristic candidate also false-positives on case (a); defensive UI without a working predicate is worse than no UI. Removed: getter, `reloadPage()` method, banner template, three locale keys (`edit.metadataIncomplete`, `edit.metadataIncompleteHint`, `common.refresh`) across all 16 locale files, and the STUBS.md sweep section. (2) Test coverage on final getter — collapsed by item-1 removal; no pre-existing tests referenced the removed symbols. (3) Parameterized `isSubmitting` step-table test added in `frontend/tests/unit/pages-edit.test.js` mirroring `pages-publish.test.js:143-153` and `pages-review.test.js:121-130` — 7 rows covering `idle/success/error → false`, `diffing/uploading/broadcasting → true`, `unknown-future-step → false` (pins positive-set inclusion semantics; dropping any step from `STEP_IN_PROGRESS` now fails the corresponding row). (4) handleSubmit local-capture comment at `edit.js` narrowed from over-claim ("closes the latent class entirely") to honest chain-routing scope only, naming `this.paper` fields still live-read after awaits and warning future engineers adding a paper-refresh hook. 1056 unit tests green.

Architect `/ce-code-review` (7 reviewers — correctness on Opus; testing, maintainability, project-standards, learnings-researcher, previous-comments, julik-frontend-races on Sonnet; `ce-agent-native-reviewer` skipped per PEvO CLAUDE.md): all 4 hold items VERIFIED-FIXED by previous-comments. Zero P0-P2 findings. Two P3 residuals — comment-as-forward-contract (no future enforcement) + pre-existing latent class on other `this.paper` live-reads — both dismissed during architect triage as acknowledged-and-documented limitations. Julik-frontend-races confirmed the removal is total (zero orphan references to `showMetadataIncompleteWarning` / `reloadPage` / removed locale keys in any async path, `$watch` handler, or setTimeout), no new post-await live-reads introduced, and `_mounted` guard discipline preserved. Learnings-researcher cited 5 conventions (inverted-predicate-collapse-encode-invariant-structurally, mutation-kill-claims-must-match-assertion-and-corpus, marker-stub-vacuous-or-fallback, task-slug-citations-in-comments-go-stale, frontend-error-sanitization); the narrowed comment was spot-checked against task-slug-citations and found clean (anchors on behavioral invariants and code symbols, no task-slug reference).

**Cross-references:** `agents/docs/solutions/architecture-patterns/pevo-inverted-predicate-collapse-encode-invariant-structurally-2026-05-05.md` (the canonical pattern this round-3 removal exemplifies — un-encodable invariants get removed, not patched). Skipped `/ce-compound` — the supporting convention already exists; this task's round-3 work is its application, not a new cross-cutting learning.

## UI-EDIT-LOADPAPERDATA-CONCURRENT-RETRY-GUARD (archived 2026-05-16) — round-1 clean (6 findings; 2 routed to sibling task, 4 dismissed)

Guard `loadPaperData()` in `frontend/src/pages/edit.js` against re-entrant invocation from rapid Retry-button clicks. Two concurrent fetches raced; the slower-resolving overwrote `_originalBody` / `this.paper`, corrupting the diff base for the next native edit. Implementer commit `c0de45b` adds a non-reactive `_loadInFlight` boolean mutex with early-return guard and `finally`-block clear (clears on all reachable exits including stale-key/unmounted early-returns inside try, and `Promise.allSettled` rejection); 2 unit tests pin (a) rapid concurrent invocation → 1 fetch + consistent state and (b) flag-resets-after-rejection enables retry.

Review (2026-05-16) dispatched 6 reviewers — correctness (Opus); testing, maintainability, project-standards, julik-frontend-races, learnings-researcher (Sonnet). `ce-agent-native-reviewer` skipped per PEvO CLAUDE.md. correctness / testing / project-standards returned clean. Findings: (P2 julik) `_loadInFlight` clears in `finally` before `$nextTick(_mountEditors)` fires; concurrent mount-during-mount can leak editor instances because the simple boolean guard does not reach the dynamic-import-resolution window — routed: folded into in-flight sibling `ui-mount-editors-destroyed-guard` task (re-hold to add `_editorsInitialized` idempotency-guard scope). (P3 julik) `_mountEditors` missing `if (!this._mounted) return;` guard — already in flight as the same sibling task. (P2 maintainability) Dual-state `_loadInFlight` + `loadingPaper` could use one-line clarifying comment at declaration — dismissed (the existing 8-line in-function block already pins the rationale; PEvO "Default to writing no comments" stance). (P3) Concurrent-retry test does not exercise the editor-leak window — dismissed; covered when the routed-to-sibling fix lands and adds its own test. (P3) 8-line comment block in `loadPaperData` partially restates mechanism — dismissed, load-bearing trap prevention. (P3) Latent route-change-then-retry interleave — dismissed, router recreates the component on route change so not reachable today.

Source: sibling JFR-R3-001 finding on `ui-coauthor-continuation-publishing` (commit `26c3b6b`). Learnings researcher cited 5 adjacent solutions docs (`vitest-retry-fire-and-forget-side-effect-poisoning`, `vitest-fake-timers-module-private-state-isolation`, `promise-race-loser-rejection-handled`, `per-request-memo-catch-block-negative-cache-contract`, `object-shape-fix-every-reset-site`); none flagged a rule violation. Skip `/ce-compound` — no non-obvious cross-cutting learning specific to this task.

**Cross-references:** `agents/docs/tasks/review/ui-mount-editors-destroyed-guard.md` (in-flight sibling that received the routed P2 editor-leak-window scope-broadening); `c0de45b` (implementer commit).

## BE-WOT-VOUCH-ROUTE-COVERAGE (archived 2026-05-16) — round-2 clean (4 hold items addressed)

Add route-level test coverage for `/api/wot/vouch` failure branches (`backend/src/routes/wot.ts`). The chain_error branch `logger.error` and the pre-existing timeout branch had zero coverage prior — `tests-must-fail-on-mutation-of-code-under-test-2026-04-22` violation. Round-1 landed 4 arms (happy + timeout + chain_error + skipped) in `backend/tests/routes/wot-vouch-broadcast-outcomes.test.ts` via authenticated supertest + mocked `broadcastWotAccreditation` against real `verifyHiveSignature` middleware (stronger than required); architect hold flagged 1 P1 fixture-shape bug + 3 P2/P3 polish items.

Round-2 (commit `5722d60`) landed all four hold items: `VOUCH_STATUS_FIXTURE`'s phantom `is_accredited_via_wot` replaced with `eligible: false` + `satisfies VouchStatus` annotation + typed mocks (catches future drift at compile time); 403 unaccredited-voucher gate test added (asserts status + `not.toHaveBeenCalled` on the broadcast mock); docblock invokes the carve-out catch-all clause explicitly for the `getVouchStatus`/`getAccreditedSet` intra-app helper mocks; `chain_error` substring tightened to arm-unique `broadcast for bob failed` (matches `wot.ts:103`). Round-2 review fan-out (correctness / testing / maintainability+project-standards / kieran-typescript) returned zero findings.

**Architect-attention notes (deferred — NOT findings against this commit):** ORCID 504 vs WoT 200-with-`accreditation_outcome:'timeout'` design divergence (verify intentional vs accidental 2xx collapse); per-attempt audit event possibly missing on `/vouch` ok arm per `broadcast-per-attempt-vs-error-event-roles-2026-05-13.md` convention.

**Cross-references:** `backend-wot-broadcast-timeout-handling.md` (already archived — task that introduced the `chain_error` branch); `tests-must-fail-on-mutation-of-code-under-test-2026-04-22.md` (convention this task closes for both timeout and chain_error arms).

## BE-LOG-SHAPE-CONVERGENCE-SIBLING-FILES (archived 2026-05-15) — round-3 clean (zero findings) ✓

Migrate non-`auth.ts` route emissions onto the canonical structured-log shape established by BE-AUTH-STRUCTURED-LOG-CONVENTION-CONVERGE. The shape: `logger.<level>({ event: '<file>.<endpoint>.<sub_event>', route: '<file>.<endpoint>', email_hash?, emailKnown?, err? }, '<message>')`. File-level prefixes substitute for `auth.`: `accreditation.<endpoint>.*`, `custody.<endpoint>.*`, `signup_verify.<endpoint>.*`, `settings.<endpoint>.*`, `orcid.<endpoint>.*`. CNPD compliance preserved via `email_hash`/`safeHashEmailForLogs`; `burnSentinel` emissions stay under `auth.burn_sentinel.*` per the "prefix tags the file the emission lives in" rule even when called cross-file. Round-2 architect inline-fixed convention-doc title scope at commit `c21d856` (narrowed from "auth-route emissions" to encompass non-auth routes).

**Arc by round:**
- Round 1 (commit `54532c2`): 41 emissions migrated onto canonical shape across `accreditation.ts`, `custody.ts`, `signup-verify.ts`, `settings.ts`, `orcid.ts`. 20 spy assertions added (rename-only) in 3 updated test files. Held on (1) signup-verify.ts: 4 new outer-catch `*.failed` events with zero spy coverage (`signup_verify.{verify,resume_signup,confirm,link}.failed`); (2) settings.ts: 7 new event emissions with zero spy coverage and no logger-spy infrastructure in `settings.test.ts`; (3) sibling-event coverage gaps in orcid.ts (7), custody.ts (4), accreditation.ts (4) — total 15 events outside the rename-only assertions.
- Round 2 (4 worker fan-out commits cherry-picked: `bac0615`, `ade1d20`, `21eb8b7`, `8200b85`): 26 new spy assertions across 5 test files, every hold-block event covered with exact-match `event` literals + canonical-shape `route`+structural assertions. Settings.test.ts gained fresh logger-spy infrastructure (findEvent helper, nodemailer import, file-header carve-out docblock with clauses a/b/c). The `accreditation.cleanup.failed` driver used invasive Map.prototype[Symbol.iterator] global patch + `vi.resetModules`-induced setInterval re-registration — accepted at user triage as documented invasive trade-off (fails loud not silent; future `__test_seams.cleanupExpiredTokens` refactor would let the spec spy directly). Held on (1) static `upgradeLimiter` usernames in 2 custody upgrade specs collide with per-account 1/hr limiter across vitest retries — outlier vs sibling specs that already used timestamp-suffixed usernames per round-2 signal block.
- Round 3 (commit `4c82a15`): 4-line mechanical fix per architect's specified shape — switched `lightupgnullhsh` and `lightupgouterct` to `<base>_${Date.now() % 100000}`, matching established sibling pattern at `accreditation.test.ts:1276,1320`.

**Round-3 architect re-review (`/ce-code-review` scoped to commit `4c82a15`):** 4 personas right-sized for the 4-line cosmetic-flake fix — correctness (Opus); testing, project-standards, kieran-typescript (Sonnet); skipped maintainability + ce-learnings-researcher (no new abstraction; pattern documented in round-2 signal); ce-agent-native-reviewer per project CLAUDE.md. **All 4 reviewers returned zero findings.** Type-flow correct (`Date.now()` → `number` → template-literal `string`); `upgradeUser` const flows consistently to `bearerFor` + `bearerPost` + spy assertion at all reference sites; `upgradeLimiter` confirmed at `backend/src/routes/custody.ts:41` as `keyFn: byAccount` with `max=1/hr`; sibling Date.now()-suffix precedent verified at `accreditation.test.ts:1276,1320`. Subject-prefix style + Co-Authored-By trailer + narrow staging (1 file) all conform. Residual notes (Date.now() rollover collision under sub-millisecond retries; underscore-separator style divergence) explicitly preemptive-hardening territory per `feedback_dismiss_preemptive_test_hardening`.

**Cross-references:** `solutions/conventions/auth-structured-log-shape-2026-04-29.md` (the convention doc; round-2 architect inline-broadened the title to encompass non-auth routes via commit `c21d856`); `solutions/conventions/wrapping-primitive-exhaustive-call-site-audit-2026-04-22.md` (applied during round-1 migration); `solutions/conventions/tests-must-fail-on-mutation-of-code-under-test-2026-04-22.md` (applied during round-2 spy-assertion design); BE-AUTH-STRUCTURED-LOG-CONVENTION-CONVERGE (the auth-side proof at commit `153605c`); BE-LOG-PII-EMAIL-HASH (Group 7 sibling, archived same day; CNPD `email_hash` migration that this task preserves). Full 3-round history (round signal blocks, hold blocks, mutation-kill attestations, `/ce-code-review` triage per round, worker fan-out cherry-pick provenance) survives in git: commits `54532c2` → `bac0615`/`ade1d20`/`21eb8b7`/`8200b85` → `4c82a15` and the task file body at `agents/docs/tasks/review/backend-log-shape-convergence-sibling-files.md` (deleted at archive).

## BE-LOG-PII-EMAIL-HASH (archived 2026-05-15) — round-3 clean (2 findings dismissed at triage) ✓

CNPD-aligned migration of plaintext `email:` log fields to truncated SHA-256 `email_hash:` across `accreditation.ts /verify`, `auth.ts /signup` SMTP-not-configured branch, and `signup-verify.ts /confirm` + `/link` broadcast catch paths. Helper `hashEmailForLogs(email: string): string` (strict, sha256+lowercase+trim+12-hex truncation) plus `safeHashEmailForLogs(email: string | null | undefined): string | undefined` companion for nullable-email columns (ORCID-only signups insert `accounts.email = NULL`). Closes the round-1 hazard where the strict helper threw `null.trim()` TypeError on ORCID-only `/confirm`+`/link` broadcast catch paths and turned a recoverable log+200 flow into 500. Round-2 hold-fix #8 from BE-REPUTATION-SSOT later flipped the broadcast-failure semantic from "log+200" to "log+502 BROADCAST_FAILED" — load-bearing privacy invariant remained the absence of any top-level `email` key in log payloads.

**Arc by round:**
- Round 1 (commit `04e95e8`): introduce `hashEmailForLogs` + migrate 4 plaintext sites. Held on (1) `null.trim()` TypeError P1 on ORCID-only `/confirm`+`/link`, (2) zero call-site mutation coverage on the migrated emissions, (3) two vacuous tests in `lib/log-pii.test.ts` (substring-walk loop iterating non-hex chars; `not.toContain` against non-hex-character substrings).
- Round 2 (commits `e4b0c17` + signal-only `a0e89f8`): added `safeHashEmailForLogs` companion with widened pg query result generics on `signup-verify.ts:243,383`; 4 spy assertions covering `email_hash` shape + `not.toHaveProperty('email')` negative; replaced vacuous tests with value-pinned `expect(hashEmailForLogs('alice@example.com')).toBe('ff8d9819fc0e')` (verified independently via `crypto.createHash('sha256').update('alice@example.com').digest('hex').slice(0, 12)`); `/link` spec uses real `verifyHiveSignature` with deterministic test key. Held on (1) `safeHashEmailForLogs` API contract mismatch (`null` vs `undefined` between helper return / call-site coercion / test assertion), (2) stale comment block at signup-verify.test.ts:376-381 contradicting committed assertions (claimed `email_hash: null` and `200 + JWT` while actual was `undefined` and `502 BROADCAST_FAILED`), (3) `signRequestBound` helper duplicated across 2 test files, (4) auth.test.ts file-header missing carve-out clause (a) acknowledgment for the new `logger.error` spy.
- Round 3 (commits `5e9e548` + cross-task `bc7674e`): aligned helper return type to `string | undefined`; rewrote stale comment block; extracted `signRequestBound` to `backend/tests/support/sign-request.ts` (per-file binding wrapper pattern); auth.test.ts file-header carve-out clauses (a)+(b)+(c) acknowledged. Held on (1) extraction was incomplete — 4 sibling test files still carried verbatim inline copies, (2) extracted helper duplicated `buildCanonicalAuthMessage` SSoT instead of importing it (latent `?? {}` vs `|| {}` body-coalesce drift). Worker-fanout commit `8841a12` landed both: `sign-request.ts` now imports `buildCanonicalAuthMessage` from `backend/src/lib/authMessage.ts` (the same template `verifyHiveSignature.ts:151` consumes server-side); 4 sibling files (`bridge.test.ts`, `claims.test.ts`, `bridge-haf-lag-locks.test.ts`, `signup-verify-postbroadcast-severity.test.ts`) migrated to per-file binding wrappers.

**Implementer-flagged worker deviation:** 3 of 4 migrated files (`bridge`, `claims`, `bridge-haf-lag-locks`) used top-level `await import('../support/sign-request.js')` instead of static import because their `vi.mock('../../src/config.js', factory)` closures reference module-level `const TEST_BRIDGE_KEY` / `TEST_ADMIN_KEY` not wrapped in `vi.hoisted()`. Static import pulls `config.js` into eager evaluation; the `vi.mock` factory fires before the const initializers run; tests crash with `ReferenceError: Cannot access 'TEST_BRIDGE_KEY' before initialization`. Implementer sidestepped via dynamic import placed adjacent to the existing dynamic `config` import; inline TDZ-rationale comment in each affected file. `signup-verify-postbroadcast-severity.test.ts` has no config mock so it kept the static import. Implementer asked the architect whether `vi.hoisted()` wrapping should be the canonical fix.

**Round-3 architect re-review (`/ce-code-review` scoped to commit `8841a12`):** 6 personas — correctness (Opus); testing, maintainability, project-standards, ce-learnings-researcher, kieran-typescript (Sonnet); `ce-agent-native-reviewer` skipped per PEvO root CLAUDE.md. Correctness + project-standards clean. Surfaced and dismissed at user triage: (P3 cross-corroborated, anchor 100 after testing+kieran-typescript promotion + maintainability residual + learnings convention-drift flag) `vi.hoisted()` canonical fix for the TDZ deviation — dismissed because the two TDZ shapes are technically distinct: the existing `solutions/conventions/route-level-error-class-coverage-after-helper-extraction-2026-04-29.md` convention prescribes `vi.hoisted+await import` only for cases where the support module's output is needed *inside* a `vi.mock` factory closure body. In this case `signRequestBound` is only called from `it()` blocks (never inside a factory), so dynamic-import-at-module-scope is correct; the inline comments document the constraint adequately. (P3 solo from maintainability) per-file binding wrapper missing the explanatory "Per-file binding" comment that auth.test.ts established as the round-2 anchor — dismissed as comment-stacking on top of the already-present TDZ-rationale comment; the wrapper body `signRequestBoundShared(TEST_BRIDGE_KEY, ...)` is self-evident.

**Learnings-researcher gap surfaced:** No existing convention doc disambiguates the two TDZ shapes (TDZ-from-static-import-pulling-config-into-eager-graph vs TDZ-from-factory-closure-referencing-uninitialized-const) and prescribes the canonical remedy for each. Future `/ce-compound` candidate to capture the distinction.

**Cross-references:** `solutions/conventions/route-level-error-class-coverage-after-helper-extraction-2026-04-29.md` (the `vi.hoisted+await import` convention for kit-import-into-factory pattern); `solutions/conventions/tests-must-fail-on-mutation-of-code-under-test-2026-04-22.md` (round-1 hold #3 vacuous-test rationale); `solutions/conventions/test-mock-carve-out-clause-c-2026-05-04.md` (round-3 file-header carve-out acknowledgment); BE-LOG-SHAPE-CONVERGENCE-SIBLING-FILES (Group 7 sibling, archived same day); `backend/src/lib/log-pii.ts` (the helper module; signature `string → string` strict + `string|null|undefined → string|undefined` safe); `backend/tests/support/sign-request.ts` (the SSoT-delegating signing helper). Full 3-round history (round signal blocks, hold blocks, mutation-kill attestations, `/ce-code-review` triage per round) survives in git: commits `04e95e8` → `e4b0c17`/`a0e89f8` → `5e9e548`/`bc7674e` → `8841a12` and the task file body at `agents/docs/tasks/review/backend-log-pii-email-hash.md` (deleted at archive).

## FE-KEYCHAIN-API-MISUSE (archived 2026-05-15) — round-7 closed; 2 spinoff findings filed as new task ✓

Fixed Hive Keychain API misuse in the custody-upgrade flow (`requestAddAccountAuthority` → `requestImportKey`) and hardened the surrounding partial-application recovery UX over 7 architect rounds. Original landing `c4e27f1` swapped the broken Keychain API call to the correct one with WIF derivation via `dhive.PrivateKey.fromSeed(...).toString()`. Every subsequent round closed a distinct failure-mode class on the same flow.

**Arc by round:**
- Round 1 (`1f36b7a`): broadened Keychain import from posting-only to posting + active + memo (excluded owner — owner keys do not belong in browser extensions). E2E stub + unit regression assert all 3 imports + 3 distinct WIFs + owner excluded.
- Round 2 (carry, no separate landing commit; fold into round 3): reorder + best-effort import surfaced as P1 — mid-loop Keychain deny on call 2/3 wedged the user (broadcast landed, mnemonic wiped in catch, backend cleanup skipped). Held pending the round-3 helper split.
- Round 3 (`2343aea`): split `_performUpgradeKeyRotation` (broadcast only) from `_performKeychainImport` (best-effort 3-key loop). Reorder to (b) broadcast → (c) backend cleanup → (d) keychain import (best-effort). Per-role deny becomes localized `upgrade.keychainImportWarning.<role>` push to `upgradeWarnings`, never throws, never wipes mnemonic on the import branch. 6 new mid-loop / all-deny / ordering specs.
- Round 4 (`0a6b176`): closed the round-3 P1 pre-loop-throw injection point. `try { _performKeychainImport(...) } catch { warn } finally { wipe; phase='done' }` so a throw from `await import('@hiveio/dhive')`, `mnemonicToSeedSync`, `deriveHiveKeys`, or `PrivateKey.fromSeed` can't escape both helper and `executeUpgrade`. New `upgrade.keychainImportFailed` i18n key + 15-locale stubs.
- Round 5 (`8c6b352`): closed the round-4 wedge surface via 3 round-4-hold items — `Promise.race(requestImportKey, setTimeout(45s))` so a never-settling Keychain callback doesn't wedge the loop; `if (upgradePhase !== 'enter-old') return` concurrent-invocation phase guard; `AbortSignal.timeout(20_000)` on the backend-cleanup fetch with TimeoutError-specific catch routing to `upgrade.backendTimeout`. Fixture-update sweep on 12 sites for the new phase-guard precondition.
- Round 6 (`1b0c3ed`): reverted the round-5 no-wipe-on-timeout decision (mnemonic wiped on timeout same as generic error — the partial-apply state is already known to the user from the new-seed/confirm-new phases, and DOM-reachable mnemonic is pure XSS surface). Rewrote the phase-guard comment to drop the wrong "Alpine batched microtask" framing for the actual invariant (phase assignment is synchronous before the first await). `confirmNewSeed`→`proceedToOldSeed` rename swept across 13 sites. `clearTimeout(timerId)` in `Promise.race` `try/finally` so per-role timers don't orphan for ~135s on successful upgrade.
- Round 7 (`aba3dc3`): rewrote the `upgrade.backendTimeout` copy to drop the structurally-wrong "verify Keychain has posting/active/memo keys" sentence (`_performKeychainImport` never runs on the timeout branch — catch at `:746` returns before the call site at `:782`). Hid the "Try Again" button on the timeout sub-case via new `canRetryUpgrade` getter because `resetUpgrade()→startUpgrade()` generates a NEW mnemonic and re-broadcasting `account_update` with old seed-derived keys is rejected by the chain (prior rotation already landed). Collapsed STUBS.md two-section duplicate and normalized heading to `### Added <YYYY-MM-DD> (<task-slug>)`.

**Round-7 architect re-review (`/ce-code-review` against `aba3dc3`):** 7 personas — correctness, adversarial (Opus); testing, maintainability, project-standards, learnings, reliability (Sonnet). `ce-agent-native-reviewer` skipped per project `CLAUDE.md`; security skipped (no new auth surface); julik-frontend-races skipped (no new async/race surface). Zero findings from correctness, testing, project-standards, learnings — the round-6 hold items #1-4 all landed cleanly. Round-7 surfaced 2 cross-reviewer-converged findings: (P1, reliability + adversarial, anchor 100) `canRetryUpgrade` suppresses Try Again ONLY on `TimeoutError`/`AbortError`; other post-broadcast fetch failures (TypeError network-drop, backend 5xx, 429, 409 ALREADY_UPGRADED, `body.error` from `!res.ok`) fall through to `upgrade.failed` and show the same chain-rejection dead-end button; (P2, maintainability + adversarial + correctness, anchor 100) the getter compares against a translated string `$t('upgrade.backendTimeout')` not a discriminator key — fragile under locale switch (masked today because all 16 locales carry identical English text) and under any future non-retryable sub-case. Both findings cohere into a single discriminator-key refactor; bundled into a new task at `agents/docs/tasks/pending/ui-canretryupgrade-discriminator-key-refactor.md` (P1).

**Dismissed at round-7:** adv-3 browser-crash mid-`upgraded_at`-commit (single reviewer, below gate; structurally orthogonal recovery requires server-side state reconciliation); REL-02 latent `upgradeLimiter max:1/hr` blocker of a hypothetical future retry affordance (single reviewer, below gate); testing residuals on template-binding gap (E2E-territory per project convention) and untested null/empty `upgradeError` (preemptive hardening). Round-2/4/5/6 dismissals stand.

**Filed separately during this task's lifetime:** `agents/docs/tasks/blocked/ui-keychain-warning-copy-or-retry-action.md` (P3, [BLOCKED by Architect] — needs `/ce-brainstorm` pass; "retry from settings later" copy promises an affordance that doesn't exist).

**Cross-references:** `agents/docs/solutions/conventions/chain-write-timeout-ambiguous-outcome-2026-04-22.md` (sibling principle: `retriable: true` is meaningless when state is single-use — the doctrinal basis for hiding Try Again on the post-rotation dead-end); `correlated-options-discriminated-union-2026-04-28.md` (the canonical pattern the round-7 follow-up task instantiates); `object-shape-fix-every-reset-site-2026-04-21.md` (every `upgradeError` reset site must also reset `upgradeErrorKey` once the discriminator lands). Full 7-round history (round signal blocks, hold blocks, `/ce-code-review` triage per round) survives in git: commits `c4e27f1` → `1f36b7a` → `2343aea` → `0a6b176` → `8c6b352` → `1b0c3ed` → `aba3dc3` and the task file body at `agents/docs/tasks/review/ui-keychain-api-misuse.md` (deleted at archive).

## BE-BRIDGE-CUSTODY-BROADCAST-DISCRIMINATION (archived 2026-05-15) — round-5 clean ✓

Migrated `bridge.ts` `/register` + `/update` and `custody.ts` `/broadcast` from a single 500 `BROADCAST_FAILED` catch to the 504 `BROADCAST_TIMEOUT` / 502 `BROADCAST_FAILED` / 500 `INTERNAL_ERROR` discrimination already in place on the 7 routes migrated by BE-ORCID-BROADCAST-ABORT-TIMEOUT round-1. Closes AC-009 (same code returning two HTTP statuses across the route family). Removed `err.message` / `jse_shortmsg` interpolation from response bodies (static strings per branch; full error to operator log only). Normalized 7 `(err as Error).message` casts in `bridge.ts` to pass full error objects to pino. Originally surfaced as MAINT-002 + KT-R2-2 + KT-R2-3 in the round-2 review of BE-ORCID-BROADCAST-ABORT-TIMEOUT.

**Arc by round:**
- Round 1 (commit `27cc588`): 3 call sites migrated via the new `handleBroadcastError` helper.
- Round 2 (commit `706db7c`, 6 hold items): extracted `MockBroadcastTimeoutError` + `makeDhiveLikeError` to `backend/tests/support/broadcast-mocks.ts` with top-of-`describe` structural-identity assertion (mutation-kills future substitution-chain regressions); custody outer-catch INTERNAL_ERROR spec; VError-shape leak surface; always-log custody attempts via the new `logBroadcastAttempt` helper; outer-scope `op_types`/`op_count`; narrowed `LogContext` interface.
- Round 3 (commit `e08bf72`, 9 hold items): removed `attempt_n: 1` from the helper (constant would silence retry-amplification dashboards keyed on the field); dropped 5 dead fields from `LogContext`; per-field unique sentinels in `makeDhiveLikeError` so each leak path is independently asserted; outer-catch event no-emit check; load-bearing comment on `LogContext`'s deliberate `cause` omission; `DhiveLikeError.cause` made optional; `return sendError(...)` symmetry; underscore→dot event-name fix in test comments; 2 outer-catch specs (`pool.query` throw, `PrivateKey.fromString` throw) using save/restore-in-`finally` because sibling middleware also calls `appQueryMock` (consumes `mockImplementationOnce`).
- Round 4 (commit `6da1024`, 4 hold items): rewrote `attempt_n` rationale at three `custody.ts` sites to drop the now-archived `backend-broadcast-idempotency-cluster-followup.md` reference and reframe on behavioral invariants (the idempotency layer landed but added no per-attempt counter); `:263` line-ref → function-name anchor; underscore-form `custody_broadcast_failure` → dot-form in production source; hoisted `DEFAULT_APP_QUERY_IMPL` as module-scope `const` for unconditional restore (closes the `if (savedImpl)` silent-skip path that would leak under a future `mockReset` switch).
- Round 5 (commit `3b7994f`, 1 hold item): rewrote the `makeLogBroadcastAttempt` factory docblock at `backend/src/lib/broadcast-error.ts:649-664` to apply the round-4 framing to the sibling site implementer surfaced in the round-4 signal.

**Round-5 architect re-review (`/ce-code-review` scoped to commit `3b7994f`):** 6 personas — correctness (Opus); testing, maintainability, project-standards, kieran-typescript, ce-learnings-researcher (Sonnet); `ce-agent-native-reviewer` skipped per PEvO root CLAUDE.md. Zero structured findings across all reviewers. Correctness verified the 6 factual claims in the new docblock against actual code (`embedIdempotencyKey` at `lib/idempotency.ts:91`; `lookupCustodyBroadcastIdempotency` at `lib/idempotency.ts:571`; "dedup gate against HAF" matches the HAF-pool cache wrap; signature and `extra: Record<string, unknown>` typing accommodate future thread-through). Testing confirmed the strengthened invariant ("factory does NOT accept `attempt_n` as a parameter") is already pinned by 3 existing assertions at `backend/tests/lib/broadcast-error.test.ts:1111-1115`, `:1122-1141`, and `backend/tests/routes/custody.test.ts:479`. Maintainability confirmed no drift between the broadcast-error.ts site and the three round-4 custody.ts sibling sites; no rot-prone anchors. Kieran-typescript confirmed type-shape claims match the live signatures.

**Architect followups landed at archive:** (A1, round-5) convention doc `task-slug-citations-in-comments-go-stale-on-archive-2026-05-15.md` already existed from a prior session — verified by learnings researcher. (A2, round-5) `agents/docs/solutions/conventions/broadcast-per-attempt-vs-error-event-roles-2026-05-13.md` Related-section stale `backend-broadcast-idempotency-cluster-followup.md` reference rewritten per the new convention's "Good — durable cross-reference" example. (A3-carryforward, round-3 carry) folded "use distinct per-field markers when fields may leak independently" into `pino-spy-serializer-ordering-trap-2026-05-06.md` Test-fixture-discipline section; added "Save/restore vs `mockImplementationOnce` when the mock is shared with sibling middleware/helpers" subsection to `vi-spyon-mockimplementation-bypasses-function-under-test-2026-05-12.md`. (A2-carryforward, round-2 carry) `timeout_ms` qualifier `(present; these routes always use the timer-fire path)` added to the 504 entries in `bridge.md` (POST /register, POST /update) and `custody.md` (POST /broadcast).

**Cross-references:** `agents/docs/solutions/conventions/task-slug-citations-in-comments-go-stale-on-archive-2026-05-15.md` (the new convention this archive instantiates); `chain-write-timeout-ambiguous-outcome-2026-04-22.md` (504/502 discrimination pattern); `vi-spyon-mockimplementation-bypasses-function-under-test-2026-05-12.md` (round-3's `mockImplementationOnce` trap); `pino-spy-serializer-ordering-trap-2026-05-06.md` (round-3's per-field marker scheme). Full 5-round history (round signal blocks, hold blocks, mutation-kill attestations, `/ce-code-review` triage per round) survives in git: commits `27cc588` → `706db7c` → `e08bf72` → `6da1024` → `3b7994f` and the task file body at `agents/docs/tasks/review/backend-bridge-custody-broadcast-discrimination.md` (deleted at archive).

## FE-UPGRADE-CLOSURE-WIPE (archived 2026-05-15) — round-2 clean ✓

Narrowed the window where closure-captured key material (`oldSeed`, `oldKeys`, `newSeed`, `newKeys`, `newPubKeys`, `ownerKey`) is reachable during the custody-upgrade flow. Defense-in-depth follow-up to FE-UPGRADE-CREDENTIAL-WIPE. Original landing `d6978a6` (merge `35c3c3b`) extracted `_performUpgradeKeyRotation` as a narrower-scoped helper so seed-derivation and `account_update` broadcast live in a frame popped before `_clearSensitiveUpgradeState` runs.

Two architect rounds:
- Round-1 hold (`df1ed60`): 4 existing closure-wipe tests passed against a no-op `_performUpgradeKeyRotation = async () => {}` stub — ordering-only assertion was trivially true around an empty function. Item #1: instrumented `mnemonicToSeedSync` to push `'mnemonicToSeed:call'` and required the first call lie between `perform:enter` and `perform:exit` (filter `i > enterIdx && i < exitIdx` ignores legitimate post-helper calls from `_performKeychainImport`); no-op helper now produces `firstSeedCallInHelper === -1` and fails. Item #2: the existing "error path" test stubbed `fetch` which runs AFTER the helper returns, so the ordering assertion was trivially true; added a new test that stubs `Client.broadcast.sendOperations` to reject INSIDE the helper, used a try/finally wrapper around `origPerform` so `perform:exit` fires on rejection, asserts catch wipes and frame pops before wipe. Renamed the old test to `(error path: post-helper fetch failure)`. Item #3: locked the round-3 helper-split invariant — calling `_performUpgradeKeyRotation` directly with Keychain uninstalled must still call `sendOperations`.
- Round-2 architect re-review (`/ce-code-review` against `df1ed60`, 7 personas — correctness, adversarial at opus; testing, maintainability, project-standards, julik-frontend-races, ce-learnings-researcher at sonnet; `ce-agent-native-reviewer` skipped per PEvO CLAUDE.md). 4 findings surfaced + all dismissed at triage: (i) P1 adversarial — Item #2's wipe-event recorder pushes `'wipe'` BEFORE calling `origWipe()`, so a future `_clearSensitiveUpgradeState = () => {}` mutation would pass the ordering assertion despite the wipe being a no-op (dismissed as preemptive — failure mode requires a cross-helper future regression); (ii) P1 adversarial — Item #1's `mnemonicToSeed:call` filter is a presence-check not a narrow-scope check, a future `this._cachedKeys = deriveHiveKeys(...)` escape inside the helper would still satisfy it (dismissed as preemptive); (iii) P2 maintainability — Item #2 missing explicit `wipeIdx >= 0` guard that the happy-path test carries (dismissed — `-1 > N` already fails the ordering check, no mutation-kill gap); (iv) P3 project-standards — Item #2 re-review signal-block heading missing trailing colon per `agents/ui/CLAUDE.md` (dismissed — cosmetic, other ui-* tasks share the omission). Adversarial's claim that `settings.js:730` TimeoutError/AbortError branch skips wipe is documented intentional design (mnemonic preserved on backend-cleanup timeout per ui-keychain-api-misuse round-4); false positive. 6/6 FE-UPGRADE-CLOSURE-WIPE specs pass; pages-settings 51/51; full frontend unit suite 1028/1028.

## FE-SAVESESSION-API-MISUSE-SWEEP (archived 2026-05-15) — round-2 clean ✓

Converted three remaining `_saveSession(6 args)` call sites (`signup-verify.js:412`, `signup-verify.js:457`, `settings.js:636`) to no-arg `_saveSession()` form with explicit pre-save state assignments, completing the FE-ORCID-CALLBACK-FIXES sweep.

Two architect rounds:
- Round-1 (`748e1ac`): 3-site conversion landed clean. Two test gaps held: (1) full-state specs asserted on `mockAuthStore.<field>` *after* the await returned (final-state), so a refactor moving any pre-save assignment to AFTER `_saveSession()` would pass the assertion while re-introducing the "stale prior expiresAt persisted, user logged out on reload" regression class; (2) `settings.js executeUpgrade` lacked coverage for the explicit `expires_at: null` branch of `if (result.data?.expires_at)` — a refactor to `'expires_at' in result.data` would treat explicit null as truthy and silently log users out on next reload.
- Round-2 fix (`d80fc2d`): replaced `_saveSession: vi.fn()` with snapshot-capturing stub `vi.fn(function () { savedSnapshot = { ...mockAuthStore }; })` across 3 specs in `pages-signup-verify.test.js` (submitCreateAccount, handleLinkAccount) and `pages-settings.test.js` (executeUpgrade); migrated assertions to `savedSnapshot.<field>` (state-at-call-time); added `expect(savedSnapshot).toBeDefined()` sanity check. New `preserves existing expiresAt when backend response has expires_at: null explicit` spec asserts `mockAuthStore.expiresAt === originalExpiry` (non-null sentinel from `seedUpgradeState` — contrastive, not vacuous).
- Round-2 architect re-review (`/ce-code-review` against `d80fc2d`): 5 personas — correctness (opus); testing, maintainability, project-standards, ce-learnings-researcher (sonnet); `ce-agent-native-reviewer` skipped per PEvO root `CLAUDE.md`. Zero structured findings across all 4 reviewer personas. Correctness verified seeded baselines (`null` for signup-verify, `'2099-01-01T00:00:00.000Z'` for settings) flip the snapshot assertion under a reordering mutation; testing verified the explicit-null spec correctly exercises the falsy branch of `if (data.token && data.expires_at)` at `frontend/src/auth.js:104`. Learnings researcher's prior-conventions concerns (stub-never-fired, vacuous-vs-reset, reactive-proxy spread) all either already covered by `toBeDefined()` + `toHaveBeenCalledWith()`, already correct given the non-null sentinel, or theoretical-only (`mockAuthStore` is a plain JS object). 78/78 affected specs pass; full frontend unit suite 1026/1026. No `/ce-compound` warranted — no non-obvious learnings beyond existing `tests-must-fail-on-mutation-of-code-under-test-2026-04-22.md` and `vi-spyon-mockimplementation-bypasses-function-under-test-2026-05-12.md` solutions docs.

## FE-UPGRADE-CREDENTIAL-WIPE (archived 2026-05-15) — round-4 clean ✓

Closed the `executeUpgrade()` credential-wipe gap via `_clearSensitiveUpgradeState()` helper that zeros `oldSeedPhrase`/`newSeedPhrase`/`newSeedWords`/`confirmInputs`/`upgradePassword` on both success and error paths (original landing `dfece3e`).

Four architect rounds:
- Round-1 hold (`fd116e4`): `executeUpgrade()` catch-block sanitization — replaced `this.upgradeError = err.message` with `console.warn('[custody upgrade]', err); this.upgradeError = this.$t('upgrade.failed')`. New `upgrade.failed` i18n key + 15 locale stubs.
- Round-2 hold (`9af76fd` + `9e8ca0f` collateral): sibling-catch sanitize in `startUpgrade()` via `upgrade.generationFailed`. Introduced sentinel `$t = (key) => 't:' + key` stub + `.toMatch(/^t:/)` matcher as the OR-fallback regression-class guard. `warnSpy.mock.calls.find(c => c[0] === '[custody upgrade]')` prefix filter replacing brittle `[0]` index.
- Round-3 hold (`c65bc87`): the `.toMatch(/^t:/)` matcher under the sentinel stub was vacuous (truthy short-circuit catches no OR-fallback mutation). Reverted sentinel stub to `(key) => key` matching ~20 sibling test files; reverted 19 sibling assertions from `'t:<key>'` to `'<key>'`; deleted the vacuous matcher. Added focused empty-`$t` regression test (`does not leak key-material when $t returns empty for upgrade.failed`) that stubs `comp.$t = (key) => (key === 'upgrade.failed' ? '' : key)` to ACTUALLY exercise the OR-fallback path.
- Round-4 architect re-review (`/ce-code-review` against `c65bc87`, 5 personas — correctness at opus; testing, maintainability, project-standards, learnings at sonnet; `ce-agent-native-reviewer` skipped per PEvO CLAUDE.md). Zero findings. Correctness + testing both traced production code at `frontend/src/pages/settings.js` and confirmed the new test kills the `$t('upgrade.failed') || err.message` OR-fallback mutation (regressed code falls through to `err.message` containing leak strings → `not.toContain` fails). Existing leak-guard retains `.toBe('upgrade.failed')` exact-value pin; the two tests cover distinct mutation classes (direct bypass vs OR-fallback). Convention drift between this test file and ~20 sibling test files closed.

Sibling P3 follow-ups (`frontend-settings-error-message-sanitize-sweep.md`, `docs-locale-stub-convention.md`) filed and closed under their own tasks. P2 closure-wipe split into `FE-UPGRADE-CLOSURE-WIPE` (separate archive).

## BACKEND-AUTH-STRUCTURED-LOG-CONVENTION-CONVERGE (archived 2026-05-15) — round-2 clean (items 1-2 landed) ✓

Round-2 hold-fix at commit `93d34ec` (with task-mv at `7d3d1a7`) addressed both architect items: (1) P2 — 3 missing spy specs added to `backend/tests/routes/auth-log-shape.test.ts` covering `auth.resend_verification.smtp_send_failed` (auth.ts:665, warn), `auth.reset_request.smtp_send_failed` (auth.ts:957, warn), and `auth.resend_verification.failed` (auth.ts:712, outer-catch). Each new spec follows the established patterns: nodemailer.createTransport spy for SMTP-failure paths; per-test pool.query patch that throws on first call only with try/finally restore for outer-catch; assertion shape `event` exact-match + route + emailKnown:known + err: instanceof Error + `res.status === 200` (SMTP status-code oracle invariant) + `sendMailSpy.toHaveBeenCalledTimes(1)` (prevents short-circuit-noop regressions). Each spec mutation-killed by typo of the event literal. (2) P3 — findEvent helper signature widened from `ReturnType<typeof vi.fn>` to the minimal structural `{ mock: { calls: unknown[][] } }` so it accepts both `MockInstance<LogFn>` and `Mock<[], void>` without the maximally-suppressive `as never` cast; all 7 cast sites dropped, 3 new round-2 specs use the uncast pattern. Total spy coverage on the file: 10/18 emissions pinned + 2 in recover.test.ts = 12 total. The 8-event delta is 5 file-level startup + 2 burn_sentinel + 1 already-covered-elsewhere — all explicitly out of scope per round-1 hold-block "pragmatic scope."

Companion follow-up commit `bc7674e` migrated the /link log-shape spec in `signup-verify.test.ts` (originally added by LOG-SHAPE-CONVERGENCE-SIBLING-FILES round-2 worker fan-out) to use the shared `signRequestBound` helper from `backend/tests/support/sign-request.ts` (extracted in BE-LOG-PII-EMAIL-HASH round 2). Cross-task fixup; clean delegation, same signing protocol.

Architect /ce-code-review (6 reviewers — correctness on opus; testing, maintainability, project-standards, reliability, kieran-typescript on sonnet; ce-agent-native-reviewer skipped per PEvO CLAUDE.md): 1 P2 finding (open-coded pool.query patch-once pattern in 6+ outer-catch specs — strong helper-extract candidate for `backend/tests/support/`) dismissed at user triage on the "not a regression of this round; pattern was established in round 1; cross-cutting cleanup belongs in its own task" rationale. Below-gate items recorded: verify_token length 32 hex vs production 64 hex divergence (correctness, P3 conf 50); outer-catch patch couples to "first pool.query is the SELECT" assumption (correctness, P3 conf 50); emailKnown:'unknown' inverse case structurally untriggerable (testing, P3 conf 25); nodemailer.createTransport spy cross-file duplicated with recover.test.ts (maintainability, P3); round-2 specs grouped at file bottom rather than with sibling event-class specs (maintainability, P3, fan-out artifact). Reliability 4 × P3 (Date.now() emails at module load; config.smtpHost mutation precedes try block; pool.query patch lifecycle; resend/reset rate-limit serialized within file) all dismissed.

## BACKEND-ERROR-HANDLER-INCLUDE-ERR-NAME-IN-LOG-PROJECTION (archived 2026-05-15) — round-2 clean (Path B + test rewrite both landed) ✓

Round-2 hold-fix at commit `620428c` (with task-mv at `8f4f3b0`) addressed both architect items: (1) P1 — Path A → Path B switch in `backend/src/middleware/errorHandler.ts`. The plain-object projection `{err: {name, message, stack}}` from round 1 was silently defeated at HEAD by the project's Layer-A wrapper (`redactErrInArg` → `safeRedactErr` → `redactErrSerializer` in `backend/src/logger.ts`, landed by BACKEND-BRIDGE-KEY-STARTUP-VALIDATION-AND-PINO-REDACT after f715b07): the wrapper treats plain objects as ErrorLike, projects `type: 'Object'` from `constructor.name`, and drops the explicit `name` field (not in SAFE_BASELINE_FIELDS). Path B (`logger.error({ err }, 'Unhandled error')`) lets the serializer project `type: err.constructor.name` = `'TestError'` / `'TypeError'` / `'BridgeKeyParseError'` etc. — the class identity operator dashboards key on. (2) P3 — test rewrite at `backend/tests/middleware/errorHandler.test.ts`: replaced `mockImplementation(() => undefined as unknown as void)` with `vi.spyOn(logger, 'error')` plus `logger.level='silent'` beforeEach/afterEach so the wrapper actually runs end-to-end and the assertion verifies post-serializer shape; dropped the `mock.calls[0] as [...]` cast in favor of `expect.objectContaining({err: expect.objectContaining({type: 'TestError', message: 'test message'})})`.

The `[skip-zone-audit]` flag on `620428c` was justified by stale-base reconcile: the worker branched from `36a8040` where the task file lived in `tasks/review/`, predating the architect's `dc86b68` mv-to-pending + round-2-hold-block append; the worker's review→pending mv reconciles to parent's HEAD rather than asserting architect direction.

Architect /ce-code-review (6 reviewers — correctness on opus; testing, maintainability, project-standards, reliability, kieran-typescript on sonnet; ce-agent-native-reviewer skipped per PEvO CLAUDE.md): clean review. Items 1+2 landed correctly with mutation-kill verified (Path A revert produces `type: 'Object'` red, Path B → green). Wrapper trace verified end-to-end through Layer A + Layer B + makeLevelWrapper. Carve-out clauses (a)/(b)/(c) all pass; clause (c) sibling-coverage at `wot.test.ts:18-19`, `bridge.test.ts:504+`, `signup-verify.test.ts:666+`, `accreditation.test.ts:375-415` all hit errorHandler via real `createApp()` at `src/app.ts:519`. Companion convention doc `agents/docs/solutions/conventions/pino-err-slot-plain-object-projection-loss-2026-05-15.md` (architect commit `644c670`) captured the trap.

Below-gate items recorded for transparency: comment block in errorHandler.ts:19-20 cites `SAFE_BASELINE_FIELDS` by name and the negation logic is the most drift-prone sentence if the allowlist ever expands (maintainability MAINT-1, P3 conf 50); non-Error throwables passed to `next(err)` produce degraded log shape with no type/stack (reliability, P3 — typed signature is the primary defense; codebase grep shows consistent Error usage); `originalLevel` captured at describe-scope rather than per-test could pin to a polluted level if cross-test pollution ever occurs (reliability, P3 — vitest default isolation prevents this today).

## BACKEND-ACCOUNT-CREATION-LOGGER-SPY-REAL-PATH-COMPANION (archived 2026-05-15) — DISMISSED with documented criteria, clause (c) closed ✓

Backend resolution at commit `a7c67ee` (with architect-zone compound doc at `169547e`): both warn-log risk classes audited against carve-out clause (c) and dismissed per the "follow-up task is filed" → "reasoned dismissal documented" branch. Deliverable was `agents/docs/solutions/conventions/real-path-companion-dismissal-criteria-2026-05-11.md` capturing the dismissal criteria for future logger-spy clause (c) audits — generalized so subsequent reviewers don't re-derive the analysis. The mocked tests in `backend/tests/account-creation.test.ts` mutation-kill the field-shape, log-level, slug-literal, and ordering invariants; real-path companions for the two specific warn-logs (`account_creation.cache.invalidate_failed`, `account_creation.broadcast.consensus_rejected`) add only marginal coverage (ioredis subclass-shape drift for cache-del; non-deterministic chain-state for consensus rejection) that doesn't justify the infrastructure cost.

Architect /ce-code-review (5 reviewers — correctness on opus; testing, maintainability, project-standards, learnings-researcher on sonnet; ce-agent-native-reviewer skipped per PEvO CLAUDE.md; no conditionals fired for docs-only diff): 4 P2 findings surfaced. 3 fixed in-place at commit `a13f364`: (a) dismissal doc line 93 — reworded "warn-BEFORE-throw ordering via spy call order" to "ordering pinned by spy-was-called assertion" since the test asserts the warn and throw separately rather than via `mock.invocationCallOrder`; (b) dismissal doc lines 111-112 — replaced stale `pending/` task path and soon-to-be-stale `review/` parent task path with `tasks-archive.md` anchor + commit `3736932` reference for durability across the 250-line trim cliff; (c) `test-mock-carve-out-clause-c-2026-05-04.md` Related section — added bidirectional back-link to the dismissal-criteria doc so future reviewers landing on the def doc see the dismissal half. 1 P2 (asymmetric warn-log assertion coverage across regex arms) dismissed at user triage because source-structural placement (warn outside any arm-specific branch) makes the mutation class genuinely rare.

Below-gate items recorded for transparency: test-file header doesn't back-reference the dismissal doc (M3, P3 conf 50); Examples section pins to assertion content that may drift if tests are reshuffled (M4, P3); task-file `## Backend resolution` heading non-standard vs tasks/README.md documented set (ps-1, P3); commit attribution architect-vs-backend tension on solutions/-zone /ce-compound output (ps-3, P3 conf 25). Convention reinforced: a reasoned dismissal cross-referenced from a /ce-compound doc closes clause (c) just as a real-path companion does; the criteria distinguish marginal-coverage dismissal from genuine-gap deferral.

## BE-P3-CLEANUP-SWEEP (archived 2026-05-15) — round-2 clean (M-01 anchor-to-symbol fix verified) ✓

Round-1 (commit `04fddee`, 2026-04-22) bundled 5 P3 hygiene items: past-tense rewrite of the "semaphore (when landed)" conditional in `routes/auth.ts` SENTINEL_ARGON2_HASH_PROMISE comment; soften `.env.example` UV_THREADPOOL_SIZE comment to drop the `16/4=4` arithmetic attacker-reconnaissance disclosure; collapse `if (!process.env.VITEST) { ... }` startup-assertion wrapper into a bare block by injecting `UV_THREADPOOL_SIZE=16` via `vitest.config.ts` `env:` (removes the env-injection bypass class); fix `orcid.test.ts` "10 chars" comment to "10 bytes → 20 hex chars"; add `afterAll` cleanup to `recover.test.ts BE-SIGNUP-INSTITUTIONAL-GATE-ORDERING` 422-warmup block — implementer corrected architect's suggested `username LIKE 'signup_422_%'` to `email LIKE 'signup_422_%'` because signup inserts set email-only (username later, on verify), consistent with the sibling SMTP describe-block cleanup.

Round-1 architect re-review (2026-05-11) held one item M-01: `lib/argon2-semaphore.ts:495-496` docblock cross-referenced the just-removed `routes/auth.ts:153-160` VITEST gate (rotted documentation introduced by the same cleanup pass). Round-2 (commit `ff3ed4f`, 2026-05-11) anchored the cross-reference to the stable symbol `SENTINEL_ARGON2_HASH_PROMISE` instead — dropped the line numbers entirely, kept the standalone VITEST guard at `:499` untouched per architect note.

Round-2 architect `/ce-code-review` (5 reviewers: correctness on Opus; testing, maintainability, project-standards, learnings-researcher on Sonnet; `ce-agent-native-reviewer` skipped per project CLAUDE.md; no conditionals fired for the 4-line docblock-prose diff): 1 P3 finding dismissed at triage — project-standards conf 75 surfaced missing `## Backend re-review signal (2026-05-11)` block (file renamed pending→review with similarity-100% no-content-change). Dismissed because root CLAUDE.md rule #8 ("the move itself is the re-review signal") governs over agents/backend/CLAUDE.md's separate doc-expectation, and the commit message at `ff3ed4f` already self-documents the M-01 fix shape (anchored to symbol, line-numbers dropped, standalone guard untouched). Round-1 dismissed-at-triage items recorded for transparency: negative-path test for the UV_THREADPOOL_SIZE startup assertion (every test file fails on a vitest.config.ts regression is genuinely strong protection); derivation-chain question from `verify-resource-knob-math-before-load-bearing-security-margins-2026-04-22.md` (fail-loud assertion is stronger than inline math, convention's "Right" example pre-dates the assertion); past-tense "semaphore landed" claim grep-verified (40 `runWithArgon2Slot` references across `backend/src/`). Learnings-researcher flagged a candidate `/ce-compound` entry — "docblock cross-references should anchor to stable symbol names, not line numbers" — deferred to next opportunity (memory `feedback_no_handwritten_learnings` forbids hand-writing).

## BACKEND-LOAD-ACTIVE-AUTHORS-DEAD-CODE-REMOVAL (archived 2026-05-15) — clean (1 P3 dismissed) ✓

Implementer commit `baaa487` deleted the dead `loadActiveAuthors / getActiveAuthors / startActiveAuthorsCache` chain in `backend/src/reputation.ts` (and the startup wiring at `backend/src/index.ts`) plus the unused `REPUTATION_CACHE_TTL` constant. Net `-57 +1`. The chain populated a Redis cache (`active_pevo_authors`) that no production caller read — `computeReputationBatch` defines its own `active_authors` CTE inline at `reputation.ts:345` (byte-shape-identical UNION; same `validPevoPaperWhere(..., source: 'all')` calls). Implementer signal block included a grep-verified call-site audit: 7 hits total, all 100% inside the chain itself (5 in reputation.ts, 2 in index.ts, 0 in tests/). Per the `chain-primitive-proxy-prefer-deletion-2026-04-28.md` convention.

Architect `/ce-code-review` (5 reviewers — correctness at opus; testing, maintainability, project-standards, learnings at sonnet; `ce-agent-native-reviewer` skipped per PEvO CLAUDE.md): 1 P3 finding (conf 80, correctness) — line-number drift in `backend/tests/routes/bridge-paper-author-gate.test.ts:111` and `:382-385` where the canary header comments cite `reputation.ts:374` / `:494` but post-deletion the inline `active_authors` and `user_papers` CTEs live at `:345` / `:452`. Test still functions correctly (assertions use substring matching, not line-pinning); only the comment-discovery surface is degraded. Dismissed at triage — same rot class as `BACKEND-BRIDGE-KEY-CLAIMS-ROUTE-MIGRATION` round-2 (accepted as future-rot risks; current state correct), and PEvO memory `feedback_dismiss_preemptive_test_hardening` directly applies. Stronger systemic fix (convert comment from line-number citations to function/CTE-name citations) deferred — orthogonal hygiene task if it recurs across the codebase.

Cross-reviewer disagreement: maintainability separately checked the same canary header and said "no stale `loadActiveAuthors` entry exists; it was already absent before this commit" — true on the literal question (no `loadActiveAuthors` mention to strip) but missed the surviving line-number citations drifting. Architect sided with correctness on the literal-read miss. ARCHITECTURE.md grep at archive time returned no rows referencing the deleted cache key, so no architect-side doc cleanup needed (consistent with maintainability's separate check). One pre-existing Redis-key-prefix violation surfaced as project-standards residual: `'active_pevo_authors'` lacked the mandated `${config.appTag}:` prefix; eliminated by this deletion, kept as awareness in case the pattern recurs elsewhere.

Convention reinforced: `chain-primitive-proxy-prefer-deletion-2026-04-28.md` — speculative cache scaffolding with zero load-bearing reader is deleted, not preserved. This is the Redis-tier analogue of the originating `account_creation_tokens` case.

## BACKEND-FLUSH-AND-EXIT-DOCBLOCK-COMPLETENESS (archived 2026-05-15) — clean ✓

Implementer commit `740023b` replaced the enumerated "Used by..." sentence in `backend/src/lib/flush-and-exit.ts:15-18` with the architect-preferred category-agnostic invariant (acceptance shape (b)): "Used at every boot-fatal and runtime-fatal exit site in index.ts. Any new path that needs to exit the process with a flushed fatal log MUST go through this helper rather than a bare process.exit(1)." Closes the same docblock-completeness rot class round-6 of `BACKEND-BRIDGE-KEY-STARTUP-VALIDATION-AND-PINO-REDACT` already closed once (parent fixed a "boot-fatal only" misdescription; the round-6 rewrite then still missed the `initAppDb().catch(...)` fourth site at `backend/src/index.ts:151` because the enumeration named only three caller categories). The category-agnostic phrasing removes the enumeration entirely and inherits future call sites (e.g., the pending `backend-flush-and-exit-auth-converge.md` migration of `routes/auth.ts:175-193`) without further docblock edits.

Architect `/ce-code-review` (5 reviewers — correctness at opus; testing, maintainability, project-standards, learnings-researcher at sonnet; `ce-agent-native-reviewer` skipped per PEvO CLAUDE.md; no cross-cutting or stack-specific conditionals fired for the 6-line comment-prose diff): zero findings across all reviewers. Learnings-researcher confirmed alignment with two pre-existing institutional records: `boot-fatal-flush-watchdog-pattern-2026-05-11.md` (canonical record for the helper; explicitly lists `initAppDb().catch` as a covered call site) and `enumerated-exemption-lists-are-drift-vectors-2026-04-28.md` (argues exactly the case that enumerated caller/exemption lists are drift vectors; structural invariants are the correct shape). The round-6 edit aligns the in-code docblock with the structural-invariant form the institution already chose for the companion convention doc.

## BACKEND-MASK-EMAIL-HELPER-EXTRACT-AND-FIX (archived 2026-05-15) — clean (3 ce-code-review findings: 1 architect-fixed inline, 2 dismissed) ✓

Implementer commits `7bb546f` + `82a6cfb` extracted `maskEmail` to `backend/src/lib/log-pii.ts`, removed 3 divergent inline private copies (accreditation.ts, auth.ts, settings.ts), and fixed 2 pre-existing bugs (a dead `length<=2 ? '${local[0]}***' : '${local[0]}***'` ternary in accreditation.ts that silently produced the short-local shape for ALL inputs; an unmasked-domain output in settings.ts). Unified canonical form: `<first>***<last>@***.<tld>` (long local, ≥3 chars) or `<first>***@***.<tld>` (short local, ≤2 chars). 69 lines of new specs in `backend/tests/lib/log-pii.test.ts` covering all documented forms + edge cases (missing-@, leading-@, trailing-@, empty, multi-@, no-dot domain). Companion test fixup `82a6cfb` updated `settings.test.ts` regex to match the unified format and bundled an unrelated disciplines-canon mock metadata fix surfaced by the same vitest run.

Architect `/ce-code-review` (6 reviewers — correctness on opus; testing/maintainability/project-standards/learnings/kieran-typescript on sonnet; ce-agent-native skipped per PEvO CLAUDE.md; testing rescoped after first attempt got confused by 89-commit diff range between 7bb546f^..82a6cfb): 3 P2/P3 findings. (1) P3 conf 100, correctness — `agents/docs/api-contracts/{settings,accreditation}.md` had stale pre-extract maskEmail examples (`j***h@example.com` / `j***h@mit.edu`) that the original sweep updated only in auth.md. Architect fixed both inline in commit `c21d856` along with the convention-doc broadening for the parallel sibling-files task. (2) P2 conf 75, single-reviewer maintainability — `maskEmail` housed in `log-pii.ts` (which previously held only opaque-hash log helpers) creates a discoverability mismatch with the file's "log-only" name. Dismissed: the file name "log-pii" plausibly covers all email/PII helpers, the function's JSDoc explicitly disambiguates intent inline (`Distinct from hashEmailForLogs (operator-log correlation, fully opaque)`), and the colocation benefit (3 route files already import log-pii.ts) outweighs the single-reviewer P2 risk at PEvO's small route surface. (3) P2 conf 90, project-standards — `82a6cfb` bundled two unrelated test fixes (settings regex from this task + disciplines-canon mock metadata for a different task). Dismissed with reaffirmation: past-tense, on main, commit body honest about the bundling and explains the single-test-run causal chain; rewriting history to split is destructive and disproportionate. Future drift-from-different-roots fixups should be split into separate commits.

## BACKEND-PINO-ERR-SIBLING-WATCH-LIST-AUDIT (archived 2026-05-15) — empty audit, no commit ✓

Filed at archive of `BACKEND-BRIDGE-KEY-STARTUP-VALIDATION-AND-PINO-REDACT` round-4 hold (NEW#3 carry-forward) to scan `backend/src/**/*.ts` for sibling-name shapes (`originalError`, `wrappedError`, `postErr`, `innerError`, `rootCause`, `nestedError`, `sourceError`, plus `cause`) on `{err, ...}` log-call sites that bypass both pino redact layers. Backend signal block (2026-05-11) reported zero fixable hits: the 6 watch-list names had zero matches; `postErr` hits were all catch-binding variables passed positionally to `new PostBroadcastWriteError()` constructors (never enter logger payloads); `cause` hits were either inside `logger.ts`/`broadcast-error.ts` (the redact policy code itself) or a string-enum discriminator at `orcid.ts:1316-1328` (no leak surface). Architect re-verified at HEAD on 2026-05-15: same grep, same zero-hit verdict. The audit closing empty was the expected outcome of the parent's 6-round arc — `broadcast-error.ts` was the only known violating site and was closed in rounds 3 and 4. This audit confirms no other sites exist on the watch list as of the verification commit. Cross-reference added to `agents/docs/solutions/conventions/pino-err-slot-sibling-bypass-redact-policy-2026-05-06.md` documenting the 2026-05-15 watch-list completion. No `/ce-code-review` invoked (no diff to review); no findings; no `/ce-compound` (the audit's empty result IS the convention-doc completion, no new learning warranted).

## BE-APP-SSR-DISCIPLINE-CANON (archived 2026-05-15) — clean (3 findings: 1 follow-up task filed, 2 dismissed) ✓

Task asked backend to close the last response-shaping bypass of `paperDisciplineField()` at `backend/src/app.ts:347`, where the SSR JSON-LD `about` field emitted raw on-chain `pevoMeta.discipline` while every `/api/papers*` surface ships canon-lowered values. External academic indexers (Google Scholar, Semantic Scholar, OpenAlex) fingerprinting disciplines by exact-string match saw two different values for the same paper depending on which surface they crawled. Implementer commit `b282017` chose Option B (omit `about` when `paperDisciplineField()` returns null — schema.org-cleaner than emitting empty string; the API-side coalesce-to-`''` exists to preserve `PaperSummary.discipline: string` typing, which doesn't apply to JSON-LD). Added `paperDisciplineField` import, replaced the one-line `if (pevoMeta.discipline) jsonLd.about = pevoMeta.discipline;` with the helper-routed canon-lower + omit-on-null, and landed a new 139-line mocked-pool test file `backend/tests/routes/app-ssr-discipline-canon.test.ts` exercising 4 cases (mixed-case + whitespace-padded → `'computer science'`; absent / whitespace-only / non-string → `about` omitted). Audit grep included in the implementer signal block confirms `app.ts:347` was the last bypass; `helpers.ts:118`, `papers.ts:391/591/983` already route through the helper; `search.ts:320` uses the security-guard `validateDisciplineFilter` (different concern); `bridge.ts:351` is an on-chain write context explicitly out of scope per task non-goals + cluster-review dismissal.

Architect `/ce-code-review` (5 reviewers: correctness on Opus; testing/maintainability/project-standards/learnings-researcher on Sonnet; `ce-agent-native-reviewer` skipped per project CLAUDE.md; cross-cutting + stack-specific conditionals all evaluated non-applicable for the 8-LoC string-normalization diff) surfaced 3 P2 findings: #1 anchor 100 (testing + project-standards cross-reviewer corroboration) — carve-out clause (c) gap on the test file header; no real-path companion exercising `/en/paper/:author/:permlink` integrated SSR emission, no follow-up task filed. Risk class is wiring mutation (import reverted, branch short-circuited, helper call bypassed) which the mocked test cannot distinguish from a correct no-op on the all-lowercase corpus. #2 anchor 75 (testing) — omit-field cases 2-4 don't pin `jsonLd['@type'] === 'ScholarlyArticle'` like case 1 does; theoretical regression net for a hypothetical future refactor splitting JSON-LD blocks. #3 anchor 75 (maintainability) — 4-line comment partly explains WHAT (line 1 "Route the schema.org `about` field through `paperDisciplineField`…") which the variable name and helper call already communicate; load-bearing WHY (Option-B-omit-vs-empty, SSR-vs-API parity) covered more fully in the test header + helper docstring. Triage: #1 → filed follow-up task `backend-app-ssr-real-path-companion.md` (P3) satisfying clause (c)'s filed-task alternative; #2 dismissed per `feedback_dismiss_preemptive_test_hardening` (today's behavior correct via deterministic regex first-match; failure mode is hypothetical future refactor); #3 dismissed (comment-at-call-site captures non-obvious Option-B decision for readers who don't chase docstring/test header; trim requires backend-agent edit and round-trip cost outweighs marginal policy-purity gain on a P2 cosmetic). Implementation itself is correct, behaviorally strictly tighter than parent (non-string truthy values like `{...}` or `42` previously leaked into JSON-LD raw, now correctly omitted via the helper's typeof guard), and closes the last bypass of the helper's "every site" docstring claim — the structural maintainability invariant now holds.

## BACKEND-GETPROFILESTATS-USER-REVIEWS-PARITY-GATE (archived 2026-05-15) — review clean (7 ce-code-review findings; 4 dismissed at triage, 3 architect-zone doc updates folded into the archive commit) ✓

Task asked backend to lift `validPevoPaperWhere(source:'all')` into 4 review-class SQL sibling sites — `profile.ts getProfileStats user_reviews` CTE, `search.ts:178` review-search parent gate, `papers.ts:2216 fetchEnrichmentFromHaf`, `reviews.ts:88` single-doc parent JOIN — for the display↔reputation parity invariant; at `getProfileStats` also lift the accreditation gate `(c.author IN active_accreditations OR c.author = $anonAccount)`. Round-3 hold #1 of parent `backend-review-validity-gate-and-display-reputation-parity` established the invariant on `fetchUserReviewsFromHaf`; round-4 closed that site and the security reviewer flagged the 3 sibling parity gaps + `getProfileStats` accred gap. Implementer commit `515eeb3` landed all 4 sites in a single combined commit with mutation-kill verified by stashing each src/ edit individually (5 expected red canaries fire; all 12 green after restore). 3 new SQL-shape canary files modeled on `profile-reviews-accred-gate.test.ts` + a new describe block on `reviews.test.ts`. Follow-up commit `a241941` added `vi.mock` for `config.hiveBridgeAccount` in `search.test.ts` (test corpus stores bridge papers under `pevotest.bridge` while dev `.env` falls back to admin); commit `bd52e72` appended the follow-up signal note to the task file.

Architect `/ce-code-review` (10 reviewers: correctness/security/adversarial on Opus; testing/maintainability/project-standards/learnings-researcher/performance/api-contract/kieran-typescript on Sonnet; `ce-agent-native-reviewer` skipped per project CLAUDE.md) surfaced 7 findings: 1× P1 adversarial #1 (`HIVE_BRIDGE_ACCOUNT` env-default fallback silently excludes bridge-paper reviews from the 4 newly-gated surfaces when prod `.env` doesn't explicitly set it — implementer flagged same risk in their own follow-up signal), 5× P2 (adversarial+correctness #2: `profile-stats-parity-gate` canary's native-paper substring assertion is tautological because `user_papers` CTE independently emits `= 'paper'`, so `source:'all'`→`'bridge'` mutations pass green; maintainability+kieran-typescript #3: `papers.ts:2239` literal `$3`/`$6` deviation from canonical `paramIdx++`; project-standards #4: `reviews.test.ts` mocked describe block puts carve-out clause (a)/(b)/(c) justification adjacent to the block instead of the file header; api-contract #5+#6: `reviews.md` NOT_FOUND description and `papers.md review_count` field + `?type=review` notes cite `validReviewWhere()` as sole source-of-truth without naming `validPevoPaperWhere` co-gate), 1× P3 adversarial #7 (`search.ts` review search intentionally omits the anon-proxy OR-arm vs the other 3 sites and `reputation.ts` — deliberate divergence by design but undocumented at the convention level). Triage: #1 dismissed (operator-config concern, single-instance deployment, user controls `.env`); #2 dismissed (whole-gate-drop caught via bridge_paper substring; source-axis mutation theoretical-only per `feedback_dismiss_preemptive_test_hardening`); #3 dismissed (local literal-`$N` style consistent with sibling queries in same `Promise.all`; task spec prohibited offset arithmetic only, not literal `$N`); #4 dismissed (justification text present and locally adjacent to the block). #5+#6+#7 folded into the archive commit: `reviews.md` NOT_FOUND extended to name `validPevoPaperWhere(source:'all')` as parent-paper gate; `papers.md review_count` field note (`:57`) and `?type=review` notes (`:524`) extended to attribute the two-gate composition with explicit note on search-reviews' deliberate anon-arm omission; `cross-surface-parity-audit-at-sibling-composition-sites-2026-05-14.md` got a new "Name the axis of parity, not just 'match the canonical sibling'" guidance step (#5) documenting `search.ts` deliberate divergence on author admission so future contributors don't "fix" it into a privacy regression.
