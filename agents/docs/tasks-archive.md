## BACKEND-ESLINT-RULE-BITMAPAND-FLOOR-GUARD (archived 2026-05-30) — round-4 clean ✓

ESLint rule `pevo/no-custom-id-block-num-floor` (in `backend/eslint.config.mjs`) guarding the BitmapAnd-toxic SQL shape (`custom_id = $appTag AND block_num >= $genesis` against `hafsql.operation_custom_json_view`, which blows the per-request walker budget → 503). Modelled on `noBridgePaperLiteralRule`; regex-over-string-literal, not a SQL parser. Rounds 1-4: built the rule + RuleTester suite; suppressed 16 pre-existing toxic sites with `eslint-disable` directives anchored on route/helper symbols; broadened `CUSTOM_ID_RE` from `\b\w+\.custom_id\b` to `\b(?:\w+\.)?custom_id\b` to match aliased AND bare `custom_id` (caught `wot.ts` `loadWotThreshold` as the unflagged 16th site); added a mid-token-split (`cj.custom_${suffix}`) valid case that mutation-kills the NUL-quasi-join drop; extracted shared `foldStringExpr`/`foldArrayJoin` walker delegated by both the floor rule and `no-bridge-paper-literal`, and added a `CallExpression` visitor closing the `.join(' AND ')`-assembled-literal evasion; refreshed stale `\b\w+\.custom_id\b` citations in the test docblock to the shipped form; round-4 reflowed a widowed docblock line (cosmetic). Round-4 `/ce-code-review` (maintainability + project-standards on Sonnet; `ce-agent-native-reviewer` skipped per PEvO): 0 findings — words byte-identical after whitespace normalization, anchors clean. OUTSTANDING [TODO Architect]: the 16 suppressed toxic-shape sites still await per-site audit/fix follow-up tasks (drop the inert `block_num >=` floor + remove the disable, each needing a real-HAF row-count/ordering re-verify) — not yet filed. Cross-refs: `convention-sweep-syntactic-form-misses-semantic-siblings-2026-05-21.md`, `convention-enforcing-fix-must-audit-its-own-new-code-2026-05-17.md`.

---

## BACKEND-CACHE-SINGLE-FLIGHT-COALESCING-SWR-COLD-PATH (archived 2026-05-30) — round-3 clean ✓

Extended single-flight coalescing (Gap A) + per-tier epoch-guard invalidation-during-flight protection (Gap B) from `QueryCache.getOrSet` to the three unguarded sibling paths in `backend/src/cache.ts`: `getOrSetSWR` cold-path, the `revalidate` background helper, and the `registerPeriodicRefresh.reload` closure. All four success-path `this.set` sites now capture `volatileEpoch`/`stableEpoch` synchronously before the sole `await fn()` and gate non-stable writes on both counters, stable writes on `stableEpoch` only. The cold-path shares `this.inflight` with `getOrSet` but namespaces its key as `${this.prefix}swr-cold:${key}` so an SWR caller can never coalesce onto a `getOrSet` promise that skips the stale-key write. Depended on the parent task's per-tier counter split (archived 2026-05-26). Rounds 1-3: round-2 added `clearVolatile()` tier-distinguishing companion specs that mutation-kill the wrong-counter (volatile-vs-stable) bug the `clear()`-only specs left alive, corrected the `revalidate` stale-key assertion to `toEqual({value:'v1'})` (since `invalidate(key)` does not delete the `swr:` key), namespaced the cold-path inflight key, and fixed docblock drift; round-3 (comment-only) replaced a stale `${config.appTag}:cache:<routeKey>` field-comment example with a docblock-deferring description and reworded the cold-path "impossible by construction" overclaim to an honest reserved-prefix-by-convention invariant. Round-3 `/ce-code-review` (correctness on Opus; maintainability + project-standards on Sonnet; `ce-agent-native-reviewer` skipped per PEvO): 0 findings — correctness verified all three rewritten comment claims (getOrSet vs cold-path namespacing, the reserved-prefix collision case, per-caller cast) are factually accurate against the code. Residual (documented, not held): the `swr-cold:` reserved-prefix invariant and the check-then-set TOCTOU window are convention/single-instance-bounded, not runtime-enforced. Cross-refs: `single-flight-coalescing-amplifies-cache-invalidation-race-2026-05-20.md`, `caching-wrapper-discriminated-union-poisoning-2026-05-11.md`.

---

## BACKEND-IPFS-APPTAG-FLIP-PENDING-UPLOAD-DRAIN (archived 2026-05-30) — WON'T-DO, premise rejected ✗

P3 task to guard the IPFS cleanup unpin decision (`cidReferencedInHaf`) against unpinning live `pevotest`-era files across an `APP_TAG` flip. Dismissed because its founding premise — "must not unpin live old-tag files post-flip" — no longer holds. Settled product decision: after an `APP_TAG` flip, production needn't serve or retain old-tag content (an old-tag corpus, if kept, lives on a separate instance pinned to the old tag). The `APP_TAGS_HISTORICAL` code-hardening path this task spawned was reverted (`backend-revert-apptag-historical-widening`, archived 2026-05-28, restoring `cidReferencedByAppTag` to its single-tag form); the `[TODO Architect]` `.env.example` + `ARCHITECTURE.md` flip-day drain runbook were deliberately never written so as not to bake in a rejected stance. The de-duplication extraction that incidentally rode along is preserved separately (`backend-ipfs-cid-containment-query-extraction`, archived 2026-05-30). No code remains attributable to this task. If a future decision reintroduces a need to serve/retain old-tag content during a transition window, re-scope fresh rather than restoring the reverted widening. Full task body and the architect PARKED-review note in git history.

---

## BACKEND-COMMENTS-HIDE-REPLIES-TO-UNACCREDITED-PARENTS (archived 2026-05-30) — round-4 re-review clean ✓

P2 surface-correctness fix in `GET /api/papers/:author/:permlink/comments`: the recursive CTE descended through non-accredited parents, surfacing accredited-authored replies whose parent had no visible context (reproducer: `re-joann2-tdeuxx` orphan under `pevo.science/pevo-original-whitepaper-...`). Round-1 added an EXISTS gate to the recursive arm (descend only when `ct.author` is in `active_accreditations`), mirrored the gate in the count query so `meta.total` matches `data.length`, added a base-arm safety comment, and a regression test. Round-1 hold (P1 positive-presence floor + P3 base-arm comment precision); round-2 commits (`4b4f669f` + `5994c01a`) landed item 2 reword + the worker's `status === 200` addition, and the floor item resolved with a documented deviation — no accredited non-orphan sibling exists on the reproducer paper (HAF query confirmed: 5 direct replies all unaccredited, the sole accredited comment IS the orphan being hidden), so `status === 200` + `.not.toContain(ORPHAN_REPLY_PERMLINK)` proves non-vacuous because reverting the descent-gate makes the orphan reappear (its own author IS accredited), flipping the response to non-empty. Round-3 hold (P3 stale `WITH RECURSIVE` docblock parenthetical reading "the failure is silent here — the caller catches the parse error and returns []" — the catch actually loud-fails via `throw new HafQueryError(...)`, never silent `200 []`). Round-4 (commit `861df2fe`, comment-only) reworded the parenthetical to reflect loud-fail semantics, anchored on the `HafQueryError` symbol + "the catch below" (no slug/SHA/line/round-N anchor; the accurate in-catch `200 []` counterfactual left untouched per the hold's NOTE). Round-4 `/ce-code-review` (5 personas — correctness on Opus; testing/maintainability/project-standards/learnings on Sonnet; `ce-agent-native-reviewer` skipped per PEvO): CLEAN. The reworded text matches the actual `throw new HafQueryError(...)` catch and the route's 503/500 mapping; `haf-outage-translation-canaries.test.ts` pins the loud-fail propagation behaviorally; the positional-anchor stable-named-container carve-out applies to "the catch below" (same function, named companion `HafQueryError`). Cross-refs: `positional-anchor-stable-named-container-carve-out-2026-05-20.md`, `convention-enforcing-fix-must-audit-its-own-new-code-2026-05-17.md`.

---

## BACKEND-IPFS-CID-CONTAINMENT-QUERY-EXTRACTION (archived 2026-05-30) — round-3 re-review clean ✓

P2 maintainability + drift-class closure: the tags-scoped CID-reference containment query was duplicated byte-for-byte across `cidIsKnown` (`routes/ipfs.ts`, the `GET /ipfs/:cid` gateway CID-known check) and `cidReferencedInHaf` (`ipfs-cleanup.ts`, the orphan-cleanup reference check). Drift cost is asymmetric and severe: a wrong predicate in cleanup irreversibly unpins live on-chain-referenced content via Kubo `pin/rm` (not refcounted). Extraction landed `cidReferencedByAppTag` in `lib/ipfs-shared.ts` (single helper, single docblock); both consumers delegate; folded-in cleanups (`toPinBackend` type-predicate replacing the inner `as PinBackend` cast, `unrecognizedPinBackendMessage` de-duplicated). Round-1 hold (3 items: stale `imageSrfGuardExpr` docblock still naming `cidIsKnown`/`cidReferencedInHaf`; drop the export on `unrecognizedPinBackendMessage`; throw on null `rowCount` in `cidReferencedByAppTag` for the irreversible-path guard). Round-2 (commit `c6d56685`) landed all three: docblock repointed at `cidReferencedByAppTag`, module-local message, helper now throws on null `rowCount` (cleanup's per-row try/catch logs+skips and keeps the row pinned + tracking row; gateway yields a transient 502, never wrong content) — new tests: helper-level rejects-throw, cleanup-side no-unpin/no-DELETE on null reference-check. Round-2 hold (1 item: same `imageSrfGuardExpr` docblock's alias-rationale parenthetical at the upper region still named the de-duplicated `cidIsKnown` / `cidReferencedInHaf` call sites despite the same docblock's lower "sole production interpolation site" claim — internal contradiction). Round-3 (commit `ac0b66d4`, single-line edit) repointed the parenthetical at `cidReferencedByAppTag` ("aliases `comments` as `c` in its containment scan") — symbol-anchored, no new rot. Round-3 `/ce-code-review` (5 personas): CLEAN. The new parenthetical matches the code at `cidReferencedByAppTag` (which aliases `comments` as `c`), no contradiction with the lower interpolation-site claim, the separate `cidReferencedByAppTag` invariants docblock untouched per hold's NOTE, and `ipfs-shared-cid-containment.test.ts:87` already pins the `c.json_metadata` alias claim. Cross-refs: `docblock-anchor-stable-symbols-not-line-numbers-2026-05-15.md`, `convention-enforcing-fix-must-audit-its-own-new-code-2026-05-17.md`.

---

## BACKEND-APPROVE-AUTHORSHIP-SIGNER-GATE (archived 2026-05-30) — round-3 re-review clean ✓

P1 reputation-integrity (live forgery path): the `approve_authorship` arm of `accepted_claims` (`reputation.ts`'s cycle) and `authorshipClaimsCteBody`'s `approvals` source (`hafsql.ts`'s read surface) matched the approve op only on broadcaster-controlled JSON fields with no signer check — per `hive-schemas.md` `approve_authorship` must be signed by post author or bridge account. An accredited attacker could self-broadcast `claim_authorship` naming a victim's paper + self-broadcast `approve_authorship` and the claim resolved to `accepted` → forged co-author reputation credit per cycle + accepted-co-author display. Round-1 (commit `595a8c6e`) gated both surfaces on `ap.approver IN (ap.paper_author, $bridge)` projected from `required_posting_auths ->> 0`; the revoke-override MAX subquery carries the same gate (a self-signed approve cannot out-rank a legitimate revoke); folded the `§ 2.10`/`§ 2.11` reanchor into `backend-anchor-rot-sweep`'s scope (Cluster C). Round-1 hold (P1 cycle-surface SQL-shape canary gap — a param insertion before `$18` or a predicate removal in `reputation.ts` alone would silently re-open the forgery while the read-surface test stayed green). Round-2 (commit `d90ae3b9`) added `reputation-approve-signer-gate-cycle-sql-shape.test.ts` driving `computeReputationBatch` to the real inline `accepted_claims` SQL emission (captured at the pool boundary, not stubbed), asserting `ap.approver IN (ap.paper_author, $18)` appears EXACTLY TWICE (the Explicitly-approved EXISTS arm + the revoke-override `MAX(approve_block)` subquery) — a predicate removal drops count to 1 → red, a bridge-param drift drops to 0 → red. Round-2 hold (P2 the new canary file's header introduced a fresh `§2.10` schema-section anchor — the exact rot class the sibling anchor-rot-sweep exists to eliminate). Round-3 (commit `028c3761`, single-prefix-drop) removed the `§2.10` qualifier, anchoring on the op-action string `approve_authorship`. Round-3 `/ce-code-review` (5 personas): CLEAN. The `§2.10` was the file's sole `§ N.N` occurrence; replacement uses only stable symbols (`computeReputationBatch`, `accepted_claims`, `authorshipClaimsCteBody`); no slug/SHA/line/round-N anchor introduced. Notification `claim_approved`/`claim_pending` arms intentionally left ungated (nuisance-spam only, no trust grant). Cross-refs: `backend-anchor-rot-sweep-2026-05-21` (Cluster C reanchored adjacent `§ N.M` citations in production source).

---

## BACKEND-ANCHOR-ROT-SWEEP-2026-05-21 (archived 2026-05-30) — Cluster-C round-3 re-review clean ✓

Two unrelated convention-rot clusters surfaced during the 2026-05-21 review batch: Cluster A (migration files 005/006/007 with task-slug leading-title prefixes) and Cluster B (sibling rot in `hafsql.test.ts`: line-number anchors, round-N hold-block citations, soft slug-shaped redirects). Clusters A+B landed in commit `47009e53` (rewrote migration leading titles onto behavioral form; swept `hafsql.test.ts` to zero hits per the acceptance grep); round-2 hold item axis-5 docblock contract correction (an inverted "admitted" → "excluded" claim for case-different co-author exclusion via `LOWER(TRIM(...))` + charset regex normalization) landed in commit `b580a3f4`. Round-2 architect addition surfaced Cluster C: the sibling `backend-approve-authorship-signer-gate` review's `§ 2.10`/`§ 2.11` schema-section citations across `authorshipClaimsCteBody` + `computeReputationBatch`'s `accepted_claims` + the new behavioral regression test — schema section numbers renumber on doc restructure exactly like line numbers do, so per CLAUDE.md "Comment anchors" they are rot-prone; the durable anchor is the op's `custom_json` action string. Round-3 (commit `9bea1e53`) reanchored every authorship signer-gate `§ N.M` citation onto the op `action` strings (`claim_authorship` / `approve_authorship` / `revoke_authorship`); preserved each behavioral statement (approve is valid only when signed by post author or bridge; revoke is signer-permissive — the claimer may self-revoke); left exactly 3 `§ 1.1` "Canonical SQL pattern" hits in `authorsWithSupersessionSelect` per the explicit escape-hatch in acceptance item 3 (coupled to the pending `architect-cumulative-union-doc-edits` doc rewrite — they cite a section TITLE not a bare number, and are added to the convention by default on next touch). Round-3 `/ce-code-review` (5 personas): CLEAN. Acceptance grep returns zero new `§ N.N` hits in the touched regions; the 3 retained hits are out of Cluster C's named scope; correctness verified each behavioral claim against the actual SQL (the `approve_authorship` gate `ap.approver IN (ap.paper_author, $bridgeIdx)` mirrored across `hafsql.ts` and `reputation.ts` `$18` × 2; revoke arm intentionally permissive matching the `revocations` CTE projection); cross-file mirror comments use the same op-action anchor style. Cross-refs: `convention-enforcing-fix-must-audit-its-own-new-code-2026-05-17.md`, `comment-sweep-expansion-must-audit-added-clause-behavioral-accuracy-2026-05-20.md`, `docblock-anchor-stable-symbols-not-line-numbers-2026-05-15.md`.

---

## BACKEND-AUTHOR-IDENTITY-MODEL (archived 2026-05-30) — round-5 re-review clean ✓

P1 structural gaps in the author-identity model rooted at the cumulative-union construction (`buildCumulativeAuthorsForChain`) and the shared supersession projection: (1) Hive-less co-authors structurally dropped on multi-link papers — the cumulative-union dedups on `hive` and skips entries without a normalizable Hive account, violating the "authors can't be dropped" invariant for `{name, hive: null}` co-authors that appear in some broadcaster's `authors[]`; (2) no name-supersession — accredited author's attested `researcher_name` should win over broadcaster-claimed name, mirroring ORCID supersession. Plus type-soundness gap: `PaperAuthor.name` was required but construction emitted entries with no name guarded by an unsound `as unknown as PaperAuthor` cast on hive-string only. Ratified model (from `/ce-brainstorm`): `name` mandatory; `hive` optional; no grandfathered posts (clean cutover); name-supersession is silent (no `name_discrepancy` field — name variation is benign noise unlike ORCID mismatch). R1-R4 landed (commits `fb576c0c` + `aae3009a`): composite-key Hive-less track in cumulative-union (orcid / name dedup, two tracks never auto-merge — that's the bridge-author-claim-attestation flow's responsibility), name-supersession in `applyAuthorSupersession` + `authorsWithSupersessionSelect`, mandatory-name with read-time fallback (attested → broadcaster → hive → orcid), sound `typeof a.name === 'string'` guard replacing the cast. Round-1 hold (3 items: R3 incomplete on `?version=N` + `metadata_restored` paths; SQL/JS BTRIM divergence on whitespace-only attested names; degenerate-entry SQL/JS shape divergence). Round-2 (commit `5517f8ca`) landed all three with BTRIM dropped on the JS side (charset-free exact-empty test matching the SQL arm) and a degenerate-drop WHERE on `authorsWithSupersessionSelect`. Round-2 hold (P0 RED test — round-2's SQL degenerate-drop broke the pre-existing `authorsWithSupersessionSelect SRF cascade-fail defense` test, uncaught by round-2 verification; sound `name` guard still unmet on two `papers.ts` branches; item-2/3 fixes unpinned). Round-3 (commit `8e0cbfc1`) moved the soundness filter into the shared `applyAuthorSupersession` helper (all 3 consumers — `toPaperSummary`, `?version=N`, `metadata_restored` — inherit without re-guarding), fixed the RED test, added real-Postgres loader-whitespace test + degenerate-drop canary, de-coupled the brittle full-WHERE regex. Round-3 hold (P2 direct unit test for the load-bearing name-soundness `.filter` was missing; P2 stale `PaperAuthor.orcid_verified` docstring in `domain.ts` describing non-object branch projecting `{orcid_verified, orcid_discrepancy}` for every output, now contradicted by the drop). Round-4 (commit `239a2ae0`) added 2 direct unit tests in `papers-canonical-orcid-resolution.test.ts` (`applyAuthorSupersession([{affiliation:'x'}, {name:'Alice', hive:'alice'}], new Map(), new Map())` → length 1; non-object + named-entry case → length 1; mutation-kill confirmed by commenting out the `.filter` line) and reworded the `orcid_verified` docstring anchored on `applyAuthorSupersession` (no new §/slug/SHA/line/round-N anchor introduced). Round-4 `/ce-code-review` (5 personas): CLEAN. Correctness traced both mutation-kill claims against the actual `.filter` at `author-supersession.ts`; pre-existing `§ 1.1`/`§ 2` anchors in `domain.ts` correctly left untouched (belong to anchor-rot-sweep scope); test fixtures use deliberately-degenerate entries (`{affiliation:'x'}`, `null`, `'alice'`) that exercise the drop path. Architect-owned doc edits (`hive-schemas.md § 1.1` name-supersession rule, `api-contracts/papers.md` mandatory-`name` + nameless-drop invariant, `ARCHITECTURE.md § 2` Hive-less display-only persistence note) remain deferred to `architect-cumulative-union-doc-edits` (blocked). Cross-refs: `tests-must-fail-on-mutation-of-code-under-test-2026-04-22.md`, `mutation-kill-claims-must-match-assertion-and-corpus-2026-05-15.md`, `comment-sweep-expansion-must-audit-added-clause-behavioral-accuracy-2026-05-20.md`.

---

## BACKEND-BRIDGE-IMPORTS-ENTRY-ENRICH (archived 2026-05-28) — round-1 re-review clean ✓

P2: the "My imports" surface (`GET /api/bridge/imports` → `BridgeImportListResult`) lacked three fields the UI needed — `title` (rows fell back to the raw identifier), `eta_seconds` (every pending row showed "ETA unknown"), and a resolved `author` for fresh broadcasts (only `existing_author` was set on a permlink-collision short-circuit, so "View paper" was suppressed for the common fresh-broadcast case). Masked during UI dev because `?demo=1` injected synthetic fields the real wire shape lacked. Implementation: R1 persists the register-time preprint `title` on the queue row (`null` only if metadata had not resolved at enqueue); R2 emits per-non-terminal `eta_seconds` reusing the 202-path `etaSecondsForPosition` derivation (position 1 → 0), `null` for terminal entries; R3 emits resolved `author` (the `HIVE_BRIDGE_ACCOUNT` post author once `completed`, equal to `existing_author` on collision, `null` while non-terminal) so the SPA builds the completed-post link without injecting the bridge account into frontend config. Migration 015: textbook-safe additive nullable column. Round-1 hold (two P2 testing-assertion gaps tied to the task's own acceptance) landed: the collision entry now asserts `title === TITLE` (collision-path serialization regression caught), and the pending entry's `eta_seconds` is pinned `=== 0` (replacing a `typeof`/`>= 0` pair), pinning both the position-1 `queue_position` correlated subquery and the formula, matching the 202 path. Round-2 `/ce-code-review` (testing + correctness): round-1 items confirmed sound. One disputed P2 (C1) DISMISSED — correctness flagged the `=== 0` pin as a theoretical cross-file flake via the global (un-username-filtered) `queue_position` subquery; testing judged it low/pre-existing. Dismissed as preemptive flake-hardening: the pin was architect-requested at round 1, the sibling 202-path assertion carries the identical global-rank coupling and has been stable, the implementer empirically validated it in a parallel batch, and the second-pending-row position-2 case was correctly NOT added for exactly this global-rank risk. Architect-owned `bridge.md` doc edits (202 example lists title/author/eta_seconds; terminal `failed` → null author) landed at round 1. Dismissed P3 advisories: persisted-vs-live bridge account (single-instance stable config), unbounded external-metadata title length (beta hardening), `in_progress` non-zero ETA (best-effort by design), missing `(state, id)` covering index (beta-scale fine). UI consumer wiring (View-paper link for all completed entries, per-row ETA, title labels) is the held `ui-bridge-import-queue-ux` task, gated on this.

---

## BACKEND-CUSTODY-LIMITER-CPU-AMPLIFICATION-MITIGATION (archived 2026-05-28) — round-4 re-review clean ✓

P2 DoS-amplification: `skipFailedRequests:true` (added for stolen-JWT protection) caps limiter successes, not request rate, so a JWT holder could spray malformed/wrong-proof requests at the nominal cap, each paying verifyHiveSignature ECDSA + (on `/upgrade`) argon2/Signature.recover/getAccounts with up to 30s Hive failover. Fix across every opt-in site: body-shape validation BEFORE the limiter so malformed sprays reject cheaply without consuming a slot or paying the upstream cost. Rounds 1-3 landed: body-validate-before-limiter on the 3 custody routes + 2 accreditation routes (zod hoist); the corrected 7-site audit (round-1 undercounted at 5 — missed `papers.ts retractLimiter`, closed with a pre-auth URL-shape `validateRetractParams`, and `bridge.ts registerLimiter`, already gated); a shared `lib/body-record.ts` (`assertBodyRecord` + discriminated-union `requireStringField`) collapsing six `as Record<string,unknown>` casts into one guarded site with module-private length caps; a fail-loud `rateLimitCount` test helper (throws on Redis-unreachable vs key-absent); `HIVE_PERMLINK_MAX_LEN`/`HIVE_PERMLINK_FORMAT_REGEX` SSOT in `lib/hive-permlink.ts`; the `/retract` validator hoisted ABOVE `verifyHiveSignature` (URL-keyed → cheap pre-auth reject, while the byAccount-keyed custody siblings stay auth-first). Option 2 (IP-keyed layer) deferred to the nginx layer (NAT-lockout risk > residual at beta scale); Option 3 (cost-aware bucket) deferred. Item 4 (drop `req.params as string` casts) WITHDRAWN — `@types/express` `ParamsDictionary` indexer is `string|string[]`, so the casts are required. Round-4 (commit `3fa489b8`) closed the final 5 items, the load-bearing one being no-trim credential parity: round-3's unconditional trim in `requireStringField` mutated the credential, so an edge-whitespace password passed `/login` (raw verify) but failed `/fresh-auth`+`/session-auth` (trimmed verify) — a lockout from consent broadcasts and critical-action re-auth. Fix: opt-in `trim` param defaulting to no-trim; the empty-check trims regardless (whitespace-only still rejected for every field) but the success arm returns raw for the 4 password reads (byte-exact argon2 input matching the hash/login paths) and trimmed for the 10 identifier/slug reads. Plus a real-path credential byte-exactness test (real verifyHiveSignature+argon2+Postgres: edge-whitespace accept across login==fresh-auth==session-auth + a trimmed-form 401 control), a `/retract` 400-before-401 ordering pin, and two comment fixes. Round-4 was a stale-base (~20-commit) fan-out worker; items 1+5 were re-applied by hand against main (commit verified an ancestor of HEAD, no orphan). Round-4 `/ce-code-review` (8 personas — correctness/security/adversarial on Opus; testing/maintainability/project-standards/kieran-typescript on Sonnet; learnings; ce-agent-native skipped per PEvO): CLEAN on substance — correctness+security+adversarial all returned zero findings, verifying no-trim is reject-narrowing only (no auth widening), byte-exact across login==fresh-auth==session-auth==hash, §6.5 invariant #1 upheld, the hand-reapplication landed with all 14 call-site trim flags correct. Dismissed nits: a P2 maintainability ask to add inline no-trim rationale at the 4 password sites (the rationale already lives in the `RequireStringFieldOptions.trim` JSDoc), a P3 comment-math typo, and a P3 missing `/fresh-auth` trimmed-password negative control (preemptive — the positive test already reds on a trim regression). Architect doc edits landed at round-3 (`papers.md` /retract VALIDATION_ERROR). Cross-refs: `validator-limiter-ordering-depends-on-key-class-2026-05-21.md`, `skip-failed-requests-jwt-required-credential-verify-carve-out-2026-05-17.md`, `helper-contract-flip-untouched-adopter-audit-2026-05-16.md`.

---

## BACKEND-NOTIFICATION-ACCREDITATION-UPDATE-AUTHORITY-GATE (archived 2026-05-28) — round-2 re-review clean ✓

P2 notification-integrity hygiene: the `accreditation_update` arm of `fetchNotificationsFromHaf` (`notification-queries.ts`) read accredit/revoke ops with no authority-signer gate, letting anyone self-broadcast a forged accredit/revoke naming a victim to push a spurious notification (nuisance spoof; no trust grant, since real accreditation is computed via the gated `active_accreditations`). Round-1 added `cj.required_posting_auths ?| $authorities::text[]` reusing the already-bound authorities param. Round-2 (commit `4af9e555`) closed two architect-held items: (1) replaced the backward param arithmetic `nextIdx - 1` with a forward expression anchored on the CTE start index (authorities is the 2nd of `activeAccreditationsCteBody`'s two params), so a future bound param shifts it in-step instead of silently mis-resolving; (2) added a SQL-shape canary that captures the query params, resolves the arm's `required_posting_auths ?| $N` placeholder, and asserts `params[N-1]` deep-equals `config.accreditationAuthorities`, killing the nextIdx-shift mis-binding class the prior text-only canary missed. Round-2 `/ce-code-review` (correctness+security on Opus; testing/maintainability/project-standards/kieran-typescript on Sonnet; learnings; `ce-agent-native-reviewer` skipped per PEvO): CLEAN. Verified the forward expr resolves to `$5` identically today; the binding canary pins placeholder position (a shifted gate resolves to the appTag string, which can't `toEqual` the authorities array, so the shared-`config` read is not a tautology for this kill). Vouch arm intentionally left ungated (peer web-of-trust). Cross-refs: `defense-in-depth-canary-must-pin-each-layer-2026-05-07.md`, `perf-floor-drop-removes-incidental-security-predicate-2026-05-25.md`.

---

## BACKEND-REREVIEW-TEST-COMMENT-ACCURACY-FIXES (archived 2026-05-28) — first review clean ✓

P3 test-comment accuracy, no production change (commit `0196398e`): reworded two overclaiming test comments. Fix 1 (`reputation-orcid-auto-accept-trim-canary.test.ts`): the docblock + sub-case (4) comment claimed the inline raw-`=` negative control catches a call-site revert — false, since sub-case (4) builds a hardcoded raw predicate inline and never references the production call site. Reworded so the helper-body BTRIM revert is attributed to sub-case (1) (the tab-padded auto-accept assertion built from `chainOrcidAutoAcceptMatchSql`), while sub-case (4) only documents the BTRIM-vs-raw semantic contrast. Fix 2 (`smtp-helper-exhaustive-call-sites.test.ts`): the docblock described the pre-tightening grep that matched comment text; after the regex tightened to match nodemailer import statements (`from`/`require('nodemailer')`), a bare `// nodemailer.createTransport` comment is no longer a hit. Reworded to describe import-matching; describe/it titles renamed to 'imports nodemailer'. First-review `/ce-code-review` (correctness/testing/maintainability/project-standards; `ce-agent-native-reviewer` skipped per PEvO): CLEAN — both rewords verified accurate (sub-case (1) is helper-built and reds on a BTRIM revert; the grep cannot match a bare comment) and consistent. Dismissed: a conf-50 nit that the Fix-1 docblock says 'assertion above' (referent unambiguous). The pre-existing `BE-AUTH-SMTP-STATUS-CODE-ORACLE round-1` slug on the smtp docblock is unchanged by this commit (acceptance 'no NEW rot' satisfied) and already tracked by the anchor-rot-sweep task. Cross-refs: `mutation-kill-claims-must-match-assertion-and-corpus-2026-05-15.md`, `convention-enforcing-fix-must-audit-its-own-new-code-2026-05-17.md`.

---

## BACKEND-REVERT-APPTAG-HISTORICAL-WIDENING (archived 2026-05-28) — first review, code clean ✓

P2 maintainability/correctness: the `APP_TAGS_HISTORICAL` widening (commit `28de10f8`) had OR-widened the shared CID-reference containment check (`cidReferencedByAppTag` in `lib/ipfs-shared.ts`) over `[appTag, ...config.appTagsHistorical]` to keep old-tag (`pevotest`) IPFS files pinned after an APP_TAG flip, on both the gateway (`cidIsKnown`) and cleanup (`cidReferencedInHaf`) paths. A product decision rejected the premise — old-tag content need not be served/retained by the production app post-flip (unpinning old-tag files is acceptable; an old-tag corpus may live on a separate instance, undecided) — so the widening defended a non-problem. This task removed it (commit `7d9b9790`): dropped `config.appTagsHistorical` + its `APP_TAGS_HISTORICAL` env read, restored `cidReferencedByAppTag` to single-tag form (one `c.tags @> $1` tags-scope, one `ipfs_cid` + one `supplementary_files` namespace containment, image-SRF guard), and reduced the containment test to single-tag shape + matching-row + null-`rowCount`-throw. Kept (NOT reverted): the predecessor extraction de-duplicating the query across both consumers, the null-`rowCount` throw from the extraction-hold task, and an orthogonal SRF call-site canary regex tightening from `28de10f8` (`jsonb_array_elements_text\([^)]`, excludes empty-paren docblock prose) — applied surgically rather than `git revert 28de10f8` precisely to preserve that fix. First-review `/ce-code-review` (8 personas + learnings — correctness/adversarial on Opus; testing/maintainability/project-standards/performance/kieran-typescript on Sonnet; `ce-agent-native-reviewer` skipped per PEvO): CLEAN. Adversarial diffed the restored SQL byte-for-byte against pre-widening `b88a261a` (identical modulo the kept null-throw + a cosmetic `const appTag`); all three current-tag match arms preserved, binds map exactly `$1..$4`, injection-safe; the SRF canary regex confirmed still present. `config.appTagsHistorical` removed with zero dangling refs (src/tests/`.env.example`), `tsc` clean. Learnings: aligns with `enumerated-exemption-lists-are-drift-vectors-2026-04-28.md` (restores structural single-symbol scope), no migration needed (backend-internal predicate, no API-shape surface). The lone cross-review finding (stale `imageSrfGuardExpr` docblock alias-example) belongs to the sibling extraction task and was held there round-2, not here. Old-tag retention / separate-instance design remains parked in `blocked/backend-ipfs-apptag-flip-pending-upload-drain`; re-addable later if a future flip needs a transition mechanism. Cross-refs: `enumerated-exemption-lists-are-drift-vectors-2026-04-28.md`, `pg-cross-join-lateral-where-guard-fires-after-srf-2026-05-16.md`.

---

## BACKEND-SETTINGS-EMAIL-DELETE-FRESH-AUTH-GATE (archived 2026-05-27) — round-2 re-review, code clean ✓

P2 security (destructive-action re-auth): `DELETE /api/settings/email` is the de-facto right-to-erasure path (deletes the `accounts` row + `notification_preferences` + `pending_recovery`, anonymizes `custody_audit_log`) but was mounted with `verifyHiveSignature` only, so a stolen/replayed JWT alone could erase an account — § 6.5 invariant #1. Architect + user decided to gate it (not exempt). Round-1 (commit `6dd1f8b5`) added the fresh-auth body-proof gate on the JWT path bound to a distinct `delete_account` action target (mirroring the change-email consume side): missing/expired → 401, cross-action/target/kind mismatch → 403, mechanism-not-registered → 401 `wrong_mechanism`; the Keychain path stays fresh-at-middleware; mint paths (`/custody/fresh-auth`, `/orcid/start mode=fresh_auth`) widened to issue `delete_account` proofs. Round-1 review found the security goal met and clean (a stolen JWT alone cannot erase; cross-action replay blocked by the length-prefixed `action` in `computeFreshAuthTargetHash`; single-use GETDEL consume); held 4 test/doc items. Round-2 (commit `73c5a78f`) closed them: state-A/C happy-path tests now seed + assert the companion-row cascade (prefs+staging deleted, audit anonymized), a state-B (password AND orcid) seed + two tests pin the `mechanismAccepted` OR branch (password leg + orcid leg), and two doc-comment corrections (the `FreshAuthVerifyFailureReason` docstring enumerates all three `wrong_mechanism` call sites; `delete_account` appended to the `orcid.ts`/`custody.ts` action-set comments). Round-2 `/ce-code-review` (7 personas + learnings — correctness/security on Opus; testing/maintainability/project-standards/kieran-typescript on Sonnet; ce-agent-native skipped per PEvO): CLEAN. Security confirmed the state seeds match § 6.1 (A/B/C) and the OR-branch coverage matches the § 6.4 delete-account contract; correctness confirmed the cascade assertions are non-vacuous and `expectCascadeErased` matches the handler's delete-vs-anonymize split. All residuals dismissed as preemptive/cosmetic (rejection-path cascade-survival assertions against a not-present mutation; docstring live-count; a missing `dbReachable` guard fenced by `describe.skipIf`; state-B shared-user run-order). Architect doc edits at archive: `settings.md` DELETE /email re-auth requirement + error shapes + the `NOT_FOUND`→`UNAUTHORIZED` doc-bug fix; `ARCHITECTURE.md` § 6.4 row updated from "gate not yet implemented" to the implementing commit. Unblocked `ui-account-delete-fresh-auth-proof-challenge` (blocked/ → pending/). Cross-refs: `anonymization-must-audit-onchain-derived-columns-2026-05-26.md`, `shared-verifier-primitive-canonical-status-mapping-2026-05-16.md`.

---

## BACKEND-AUTH-TOKEN-SESSION-BINDING (archived 2026-05-27) — round-3 re-review, code clean ✓

P1 security: `/api/auth/confirm` + `/api/auth/link` looked up pending signups by `auth_token` alone, so a token leaked via mailbox / referer / login-error-body / logs let anyone finalize the signup with attacker-controlled keys (the "token as credential" audit theme). Fix binds the `auth_token` to the browser session that initiated the signup: an httpOnly `pevo_signup_session` cookie (minted by `/verify`, `/resume-signup`, and the ORCID-direct `/signup` branch) whose SHA-256 is stored as `signup_binding_hash` (migration `011`); `/confirm` + `/link` reject a missing/mismatched cookie with the SAME 400 as an invalid token (no oracle); the login `PENDING_SIGNUP` 409 stopped returning `auth_token` (now `data: { email }` only); per-auth_token rate limits added. Rounds 1-2 built the mechanism (constant-time verify, no-oracle reject, the malformed-cookie 500-oracle closed, the `/link` JWT-replay bypass gated on `hiveAuthMethod==='signature'` per § 6.5 invariant #1). Round-3 (commit `b8b1287e`) closed the final 3 items: a `/link` JWT-replay-gate regression test (real `verifyHiveSignature`; a Bearer JWT for a stuck self-custody row falls through to the no-row 400, never the bypass/broadcast — reverting the gate flips it RED), comment-anchor rewording (task-item-number citations → behavioral anchors), and removal of the provably-dead sibling `verify_token`-invalidation sweep from both routes (migration 007's partial-unique orcid index makes a same-orcid sibling row impossible, so the sweep could never match — replaced by a comment anchored on that invariant). Round-3 `/ce-code-review` (7 personas + learnings — correctness/security/adversarial on Opus; testing/maintainability/project-standards/kieran-typescript on Sonnet; ce-agent-native skipped per PEvO): CLEAN. Correctness/security/adversarial independently traced the dead-sweep removal against migration 007 + every insert path (`auth.ts`, `orcid.ts`, `settings.ts`) and confirmed unreachable; the JWT-replay test is load-bearing; no new comment rot. Lone advisory residual (no test pins the migration-007 orcid-uniqueness invariant the removal now leans on) noted, non-blocking. Architect doc edits at archive: `auth.md` cookie + login-409 contract (already landed `a4b35903`), `common.md` PENDING_SIGNUP `data:{email}` sanctioned-exception note, `ARCHITECTURE.md` § 6.3 signup-finalization binding-as-auth-factor note. Cross-refs: `hive-signature-request-binding-shape-2026-04-21.md`, `auth-gate-revives-pre-existing-read-side-oracle-2026-05-17.md`.

---

## BACKEND-CONFIRM-CONCURRENT-ACTIVATION-LOCK (archived 2026-05-27) — round-2 re-review, code clean ✓

P3 (pre-existing, surfaced by the signup-binding review): two near-simultaneous `/confirm` (or `/link`) requests carrying the same valid `auth_token` could both pass the row lookup and double-fire the single-use `createClaimedAccount` chain broadcast. Fix (commit `099c1a20`) wraps lookup + activation in a `pg_advisory_xact_lock(hashtext('signup_activation:'||auth_token))` transaction; the second request re-reads under the lock, sees `verify_token` cleared, and takes the already-consumed reject, so the broadcast fires at most once (COMMIT precedes only the post-activation accreditation broadcast, never `createClaimedAccount`). Round-2 (commit `f3b3ebce`) landed all 6 hold items: repointed `/confirm`+`/link` log-shape tests to drive the inner-catch ROLLBACK, a `/link` accreditation-broadcast call-count assertion (`>=1` winner, `<= okCount`), `SET LOCAL lock_timeout='45000'` after BEGIN so a blocked waiter fails (pg 55P03 → 500) and frees its pool connection instead of pinning it for the holder's ~30s duration, and three comment corrections (int4 `hashtext` birthday-collision wording, best-effort-dedup framing, the intentional cross-broadcast connection-hold warning). Round-2 `/ce-code-review` (8 personas — correctness/adversarial on Opus; reliability/testing/maintainability/project-standards/kieran-typescript/learnings on Sonnet; `ce-agent-native-reviewer` skipped per PEvO): all 6 items verified, correctness + adversarial confirmed the double-fire is genuinely closed and the client releases on every exit path. Three residuals all routed to `backend-signup-activation-failure-recovery-and-pool-hold` (which reworks this activation path and supersedes the interim `lock_timeout`), none blocking: a slow holder (>45s under Hive-node degradation) makes a same-token waiter's `lock_timeout` fire and 500 a legitimate retry (no correctness break — the `verify_token` UPDATE is the backstop; the int4-collision docblock shares the edge), and the repointed ROLLBACK test injects on the no-op `SET LOCAL` statement so the real `lock_timeout`→500 path lacks direct coverage. The `/link <= okCount` broadcast bound was confirmed the tightest honest bound given best-effort dedup (dismissed). Cross-refs: `read-then-write-races-on-haf-backed-routes-2026-05-15.md`, `chain-write-timeout-ambiguous-outcome-2026-04-22.md`.

---

## BACKEND-RATELIMIT-AUTHTOKEN-KEY-LENGTH-BOUND (archived 2026-05-27) — clean ✓

P3 (pre-existing pattern surfaced by the signup-binding review): the per-auth_token rate limiter on `/confirm`+`/link` keyed on `tok:${req.body.auth_token}` after only a non-empty-string guard, so an attacker rotating arbitrarily long tokens (bounded only by the 1MB body limit) could inflate Redis / in-memory-fallback key length. Commit `0bdb8d92` caps the key by SHA-256-hashing the token (fixed 68-char `tok:${sha256hex}`); per-token accumulation preserved (distinct tokens → distinct buckets, same token → same bucket), hashing cost negligible on the hot path. `/ce-code-review` (12 personas, ce-agent-native skipped per PEvO; reviewed in one cluster pass with the session-binding round-2 + concurrent-activation commits against base `44b26476`): security CLEAN (no new oracle — the limiter hashes valid/invalid tokens identically and runs before body validation), performance CLEAN (fixed-length key fully bounds the cardinality vector), correctness confirmed no accumulation regression (a 4KB token still 429s at attempt 6), Redis prefix `${appTag}:rl:` intact. Lone residual (kieran-ts): no isolated unit test pinning `digest('hex')` vs base64 — tolerable, the integrated test catches an encoding regression indirectly.

## BACKEND-SIGNUP-BINDING-DEPLOY-WINDOW-ORCID-STUCK (archived 2026-05-27) — runbook-only, decision clean ✓

P3 deploy-window (surfaced by the signup-binding review): migration `011`'s nullable `signup_binding_hash` back-fills NULL on in-flight rows, and `verifyBinding` fails closed on NULL, so signups mid-flight at deploy time are stranded. Email-flow rows self-recover via `/resume-signup` (password re-verify re-mints the cookie+hash); ORCID-only rows (no password) cannot and see a generic `400` until they re-start ORCID signup or the 24h `expires_at` lapses. Backend took **Option 1 (operator runbook) only** and correctly **skipped Option 2** (a distinct NULL-binding-row message): the architect verified the oracle analysis — a distinct message is reachable only when the `verify_token` lookup found a row (token IS valid) with a NULL hash, so emitting it would split the response space and leak token-validity for the stranded subset, violating the no-oracle invariant. The `/ce-code-review` security + data-migrations personas confirmed NULL-binding rows fail closed on the GENERIC `400` (no distinct message). Archive landed the operator runbook into `ARCHITECTURE.md` § Schema Migrations (stranded-row predicate `verify_token IS NOT NULL AND signup_binding_hash IS NULL AND orcid IS NOT NULL AND password_hash IS NULL`, the inspect-SELECT, and the DELETE cleanup — never-activated pending rows, so deletion is non-destructive). Operationally gated on the `backend-auth-token-session-binding` prod deploy.

## BACKEND-REVIEW-VALIDITY-GATE-AND-DISPLAY-REPUTATION-PARITY (archived 2026-05-27) — code clean (round-7) ✓ + doc-sync landed

P2 review-filter rework: drop the `app LIKE 'pevotest/%'` authoring-client gate (an accredited scientist's review from any Hive client is a PEvO review per the trust layer), add a structural-validity gate (the `rating` object must be present with all four dimensions integers in `[1,5]`, matched via `~ '^[1-5]$'` regex over `->>` text — portable across HAF readers and avoids the `::numeric` cast-and-crash on string ratings in the reputation cycle), and route every review-class SQL site through one canonical `validReviewSql` helper so "shown on the page" ↔ "counted in reputation" cannot drift. Seven review rounds; the later rounds were all clause-(b)/(c) test-header accuracy on the `notifications-arm-sql-shape` SQL-shape canary (over-claim → under-claim → accurate citation of `auth.test.ts` as the real-`verifyHiveSignature` companion). Round-7 `/ce-code-review` confirmed the gate/parity code CLEAN; archive was gated only on architect doc-sync, landed here: `reputation-algorithm.md` was already synced by the `reputation-single-source-of-truth` archive (prefixed `${appTag}:` Redis keys, `active_accreditations` CTE); `ARCHITECTURE.md` § 3 Batch Computation corrected (stale unprefixed `reputation:batch:{username}` + "Keys are not namespaced by `APP_TAG`" → app-tag-prefixed keys + batch-is-SSoT / no head-block recompute); and `test-mock-carve-out-clause-c-2026-05-04.md` clause-(b) refreshed (the `MOCK_VERIFY_SIGNATURE` fixture is permitted for non-crypto-focused tests; only auth-focused / crypto-dependent tests must run real `verifyHiveSignature`). The real-signature successful-auth-to-HAF companion for `/api/notifications` remains a noted uncovered residual, not a blocker.

## UI-LOGIN-PENDING-SIGNUP-RESUME-REBIND (archived 2026-05-27) — round-2 re-review, code clean ✓

P1 SPA rebind of the PENDING_SIGNUP recovery flow after the backend login 409 stopped carrying `auth_token` (it was the `/confirm`+`/link` row-lookup credential and leaked via referer/logs; `/confirm`+`/link` now require the httpOnly `pevo_signup_session` binding cookie minted by password re-verify). Round-1 (commit `89e99a95`) rebound `login.js` / `sign-in-modal.js` / `signup-verify.js` / `api.js`: no `auth_token` in the URL, resume-marker landing, `credentials:'same-origin'` on the cookie-bearing calls. Round-2 (commit `8ed38622`) closed the 5 hold items: (1) `signup.js _resolveExistingAccount()` was a THIRD handler still reading the removed `auth_token` and dead-ending duplicate-email recovery at `?auth_token=undefined` — now bare `PENDING_SIGNUP` guard + `?resume=RESUME_MARKER` + optional email, never `auth_token`; (2) rewrote the masking `pages-signup.test.js` case to the `{email}`-only 409 (asserts `resume=1`, `not.toContain('auth_token')`); (3) added `linkExistingAccount credentials:'same-origin'` coverage in `api.test.js` (signRequest stubbed under the mock carve-out); (4) removed dead `this.email` state in `signup-verify.js`; (5) promoted the resume marker to exported `RESUME_MARKER='1'` shared by the consumer (`init()`) and all three producers. Round-2 `/ce-code-review` (11 personas — correctness/security/adversarial on Opus; testing/maintainability/project-standards/api-contract/reliability/kieran-typescript/julik-frontend-races/learnings on Sonnet; `ce-agent-native-reviewer` skipped per PEvO): all 5 items verified end-to-end, `auth_token` grep-clean (only comments + request bodies remain), §6.4/§6.5 account-state defense upheld (binding cookie is the password-re-verify second factor). One conf-100 test-DRY nit (test feeds literal `'1'` instead of importing `RESUME_MARKER`) dismissed at triage — it conflicts with the `dedup-shared-constant-defeats-test-value-pin` convention (the independent literal IS the correct value-pin; the input faithfully simulates the real URL wire value) and the drift failure mode is theoretical. P3 carve-out-header-formalization advisory also dismissed (testing persona verified carve-out clauses a/b/c are met). Gate-deferral note from the task: do NOT deploy `backend-auth-token-session-binding` to prod ahead of this SPA ship.

## BACKEND-CUMULATIVE-UNION-LISTING-SURFACES-PARITY (archived 2026-05-26) — round-4 re-review, code clean ✓

P2 extension of the cumulative-union invariant from the detail surface to listing + profile via the shared `resolveChainCumulativeAuthors` helper + per-root Redis chain cache. Round-4 (commits `62829a9c` + `49426e92`) closed the final hold item: the profile `GET /api/profile/:username/papers` handler now gates its cumulative takeover on `chainResult && chainResult.authors.length > 0` (verbatim mirror of the listing row-map), so a non-null-but-empty cumulative (a multi-link chain whose every author entry is unrenderable) falls back to the head-meta projection instead of serving `authors: []` — closing a cross-surface parity break against listing + detail. New deterministic canary `profile-papers-empty-cumulative-fallback.test.ts` pins both gate outcomes (mocks `getPool` + the sibling-route helper). `/ce-code-review` (correctness/testing/api-contract/maintainability/project-standards): code clean; correctness confirmed the empty-cumulative state is genuinely reachable (not fictional) and the gate mirrors listing exactly. One open finding is doc-only and architect-owned: `api-contracts/profiles.md` still says profile-papers does not reconstruct continuation-chain co-authors (stale since the takeover shipped). That fix folds into `architect-cumulative-union-doc-edits` (blocked), which unblocks once this task and `backend-author-identity-model` both archive.

## BACKEND-ORCID-TRIM-PARITY (archived 2026-05-26) — round-4 re-review, code clean ✓ (1 comment nit → follow-up)

P2 SQL/JS whitespace-parity closure on chain `orcid`. Round-4 (commit `b506bfd5`) extracted the chain-orcid auto-accept match predicate into a shared `chainOrcidAutoAcceptMatchSql` builder in `hafsql.ts`, routing both production arms (`reputation.ts` `accepted_claims` + `authorshipClaimsCteBody`) and the reputation-cycle canary through it, so a helper-body BTRIM revert reds the canary — satisfying the round-3→4 acceptance criterion the prior canary missed. `/ce-code-review` (correctness/testing/security/maintainability/project-standards): production fix correct, SQL byte-identical, the commutative `=` flip at `authorshipClaimsCteBody` inert, no injection (all helper args hardcoded literals; the authority-attested side stays raw). One conf-100 finding: the sub-case (4) negative-control comment overclaims (it documents the BTRIM-vs-raw delta but does NOT catch an inline call-site revert as the comment claims). Triaged to consolidated follow-up `backend-rereview-test-comment-accuracy-fixes` — comment reword only; code correct.

## BACKEND-SETTINGS-AUDIT-LOG-PRESERVATION-ON-ACCOUNT-DELETE (archived 2026-05-26) — round-2 re-review clean ✓

P1 data-integrity: `DELETE /api/settings/email` now anonymizes `custody_audit_log` (NULLs username + user_agent + session_id) instead of wiping it, closing the one-call audit-history-wipe vector. Round-2 (commit `412adb11`) landed all 7 round-1 hold items: reworded GDPR Art. 17 claim (retained `tx_id`/`block_num` are user-signed public-ledger refs, out of erasure scope), inverted-order comment fix, retention-sweep reference replacing "follow-up" (the `Retention: 24 months` SOT substring the sweep regex reads stays intact), `auditExtras` local-var anchor → `POST /api/custody/broadcast` route anchor, guarded ROLLBACK, plus test items pinning forensic-column survival and pin-by-id determinism. `/ce-code-review` (correctness/security/data-migrations/testing/project-standards): clean; security confirmed the GDPR claim is accurate and no re-auth-gate regression. ARCHITECTURE.md § 4 was corrected during round-1 review.

## BACKEND-INITAPPDB-SCHEMA-DRIFT-FIX (archived 2026-05-26) — round-2 re-review clean ✓ + § Migrations doc landed

P0 schema-drift closure: `initAppDb`'s startup DDL is gone, replaced by the `verifyAppDbMigrations` boot probe that fails loud (`BootFatalError`) on a stale schema. Round-2 (commit `315d734b`) landed both hold items: dropped the contradictory `node-pg-migrate` tooling + dependency (lockfile pruned to removals-only, `deploy.sh`'s raw-psql apply path confirmed independent, no orphaned `npm run migrate` invocations) and wrapped the `readdir` ENOENT path as `BootFatalError` with `{cause}` naming the resolved dir. `/ce-code-review` (correctness/data-migrations/reliability/project-standards): clean. The owed `[TODO Architect]` doc landed at this archive: ARCHITECTURE.md § 1 now has a "Schema Migrations" subsection (migrations authoritative, no DDL on startup, the boot probe, the deploy ordering).

## BACKEND-PENDING-RECOVERY-AGEING-PURGE (archived 2026-05-26) — round-2 re-review clean ✓

P3 data-minimization: TTL ageing-purge for the `pending_recovery` staging table (carries a plaintext new email + offline-crackable argon2id hash past the dispute window). Round-2 (commit `82877df9`) landed the 3 hold items: boot-time first tick (clears backlog at restart; fire-and-forget, errors swallowed, mirrors `startSignupCleanup`), dropped the `recovery.purge_started` info log, and a state-uniform deletion test seeding consumed + disputed rows (kills an `AND consumed_at IS NULL` predicate narrowing). `/ce-code-review` (correctness/reliability/testing/project-standards/maintainability): clean; reliability confirmed the boot tick runs after pool init and cannot disturb startup.

## BACKEND-SMTP-TRANSPORTER-HELPER-PROMOTE-AND-MIGRATE (archived 2026-05-26) — round-2 re-review, code clean ✓ (1 docblock nit → follow-up)

P1: every nodemailer transporter routes through one `lib/smtp.ts` helper pinning the 5-field timeout shape; a structural-lock test forbids direct `nodemailer.createTransport` elsewhere. Round-2 (commit `1dbbc69a`) landed both hold items: tightened the structural-lock grep from the literal substring to `(from|require\().*['"]nodemailer['"]` (closes 5 import-shape bypasses) and dropped the external-library line-number anchor for a behavioral one. `/ce-code-review` (correctness/testing/project-standards/maintainability): code clean (grep match set independently confirmed = only `lib/smtp.ts`). One conf-100 finding: the docblock still describes the pre-tightening literal-grep behavior (a false "bare comments are caught" guarantee). Triaged to consolidated follow-up `backend-rereview-test-comment-accuracy-fixes` — docblock reword only.

## BACKEND-PEVO-STRING-HELPER-ADOPTION-SWEEP (archived 2026-05-26) — round-3 re-review clean ✓

P3 migration of `|| null` / cast-and-coalesce pevo-metadata reads to the `pevoString` / `pevoStringArray` helper family. Round-3 (commit `9ea7aa82`, test-only) landed the 3 hold items: corrected the `language: ''` docblock parity framing (dropped a `||`/`??` category error and the false "behavioral upgrade" framing), relabeled a misleading `(real HAF)` describe to `(mocked-pool: 404 path)`, and replaced a `reviews.ts:30` line anchor with the `buildReviewDetail` symbol. `/ce-code-review` (correctness/testing/project-standards/maintainability): clean; correctness verified the parity claim against the real `pevoString` implementation. The clause-(c) header in `reviews.test.ts` was subsequently re-rewritten by the reviews-real-haf task to cite the now-landed companion, so no stale comment exists at HEAD.

## BACKEND-REVIEWS-REAL-HAF-INTEGRATION-COVERAGE (archived 2026-05-26) — round-2 re-review clean ✓

P3 real-HAF companion suite for `GET /api/reviews/:author/:permlink` (the clause-(c) follow-up filed during the pevoString sweep). Round-2 (commit `dec26ea5`, test-only) landed the 2 hold items: replaced a "per task acceptance #3" coordination-anchor comment with a behavioral rationale, and added `expect(typeof data.created).toBe('string')` pinning the `created` projection. `/ce-code-review` (correctness/testing/maintainability/project-standards/learnings): clean; correctness traced `c.created` → ISO-string serialization and confirmed the assertion is a real mutation-kill, structurally non-null on a 200 (not flaky).

## UI-BRIDGE-IMPORT-QUEUE-UX (archived 2026-05-26) — round-3 re-review clean ✓

P2 frontend half of the async bridge-import queue model (sibling `backend-bridge-import-queue`): make the submission UX honest about chain pacing. Scaffolded (`5eb66f81`) the `/my-imports` page + `myImports` i18n namespace (en + 15 stubs) + authenticated-dropdown nav link, then wired (`e53306fd`) against the live `agents/docs/api-contracts/bridge.md` contract: `bridge.js` reads `queue_position`/`eta_seconds` off the 202 and renders an inline queued state (position + ETA, link to `/my-imports`, no more synchronous success-redirect); the per-user in-flight cap (5) `RATE_LIMITED` 429 is discriminated from the per-IP limiter on `err.details.cap` presence and styled distinctly (amber remediation vs crimson transient); `my-imports.js` `loadEntries()` fetches via `fetchBridgeImports()`, `adaptEntry()` absorbs wire field-name differences (`created_at→submitted_at`, `error_message→failure_reason`, `existing_permlink→permlink`), failed entries re-POST `/register` behind a re-entrancy guard, and a shared `src/lib/format-eta.js` localizes `eta_seconds`. Three review rounds: round-1 (9 items — teardown guard/`_mounted` rechecks, one-retry-at-a-time lock, RATE_LIMITED+cap/LOCK_HELD/DUPLICATE retry discrimination, `error_code`-derived `retriable`, `loadEntries` in-flight guard, orphaned `bridge.stepSuccess` key removal, docblock re-anchor; commit `54ef2399`); round-2 (items 11-15 — `SERVICE_UNAVAILABLE` non-retriable, DUPLICATE on-chain-vs-already-queued discrimination, fail-open-default test pin, toast-after-await reorder, `?demo=1` retry short-circuit; commit `492959e7`); round-3 (items A+B — widened `adaptEntry` author to `wire.author ?? wire.existing_author ?? null` so the View-paper link renders for ALL completed entries, not just permlink-collisions, plus a post-reload teardown test; commit `f22937a2`, after `backend-bridge-imports-entry-enrich` made the backend emit `author`/`title`/`eta_seconds`). Round-3 review (`/ce-code-review`, 8 personas — correctness + adversarial on Opus; api-contract/testing/maintainability/julik-frontend-races/project-standards/learnings on Sonnet; `ce-agent-native-reviewer` skipped per PEvO): no P0/P1. Correctness mutation-verified the post-reload `_mounted` recheck (removing it fails exactly the new test); adversarial confirmed the test is non-vacuous and the widened author path stays suppressed for non-completed entries (backend nulls `author` when `state != completed`, template re-gates on `state === 'completed'`). Three findings dismissed at triage: a defensive `existing_author`-set/`existing_permlink`-null cross-pairing guard (api-contract P2 — the contract co-populates the pair; defends a fictional state) and two preemptive test-hardening nits (assert the template gate directly; strengthen the negative-toast assertion). Cross-refs: `frontend/src/pages/my-imports.js` (`adaptEntry`, `loadEntries`, `retryEntry`, `buildDemoEntries`), `frontend/src/pages/bridge.js`, `frontend/src/lib/format-eta.js`, `agents/docs/api-contracts/bridge.md` (`GET /api/bridge/imports`, `POST /api/bridge/register` 202), `backend-bridge-import-queue` + `backend-bridge-imports-entry-enrich` (the backend halves).

---

## BACKEND-CUSTODY-CRYPTO-DIRECT-TEST-COVERAGE (archived 2026-05-26) — round-3 review clean ✓

P0 (testing-tier): the AES-256-GCM + HKDF custody-key crypto module (`backend/src/custody-crypto.ts`, load-bearing for every light-account broadcast and recovery path) had zero direct tests. Added `backend/tests/lib/custody-crypto.test.ts` covering round-trip, tamper detection (ciphertext/tag/nonce/aad bit-flips), username HKDF-context separation, IV non-reuse, master-key length boundary, GCM tag presence, plus a canonical `crypto.hkdfSync` independent re-derivation (the strongest no-mock mutation-kill against HKDF-info-drop). Three rounds. Round-1 (4 items): anchor hygiene — stripped severity-label + triage-artifact citations and a verbatim task-prescription quote from test comments, replaced with behavioral rationale; and exported `HKDF_INFO_PREFIX`, `IV_LENGTH`, `AUTH_TAG_LENGTH` from the module as single sources of truth. Round-2 landed the constant-dedup. Round-3 (2 items): the dedup had made a constant-VALUE drift tautological (both production and test read the same export, so the round-trip stayed green while a prefix/length change silently broke stored-ciphertext interoperability). Restored independent value pins — `expect(HKDF_INFO_PREFIX).toBe('pevo:custody:')`, `expect(IV_LENGTH).toBe(12)`, `expect(AUTH_TAG_LENGTH).toBe(16)` — so a prefix change (which would render every already-stored custody ciphertext permanently undecryptable on recovery/broadcast) or an IV/tag length drift flips the suite RED. Round-3 review: `/ce-code-review` (8 reviewers, `ce-agent-native-reviewer` skipped per PEvO); adversarial *mechanically* verified all three pins flip RED on the real mutations, 5 reviewers clean. Sole finding — a P3 `above` relative-positional anchor in two value-pin comments — dismissed at triage as near-zero rot risk (same-`it()`-block, semantically descriptive reference). Cross-refs: `deriveKey`, `encryptKey`, `decryptKey`, `HKDF_INFO_PREFIX`, `IV_LENGTH`, `AUTH_TAG_LENGTH`; module `backend/src/custody-crypto.ts`. Related convention: `dedup-shared-constant-defeats-test-value-pin-2026-05-26`.

---

## BACKEND-IPFS-CIDISKNOWN-HAF-SCAN-SCOPE (archived 2026-05-26) — review clean ✓

P1 fix to the CID-reference check shared by `cidIsKnown` (`backend/src/routes/ipfs.ts`, the `GET /ipfs/:cid` gateway) and `cidReferencedInHaf` (`backend/src/ipfs-cleanup.ts`). Two co-located bugs in the same containment query: (1) the `@>` branches bound a literal `pevo` namespace key, but real PEvO metadata is namespaced under `config.appTag` (`pevotest`) — so the predicate matched zero real papers, 404ing a published paper's CID at the gateway once its 24h `pending_ipfs_uploads` row expired and unpinning live on-chain-referenced files on cleanup; (2) `hafsql.comments` has no GIN index on `metadata`, so the unscoped containment full-scanned ~67.6M rows per unknown-CID lookup. Fix binds `{[config.appTag]: …}` in both `@>` branches and wraps the predicate in `c.tags @> [appTag]` (the only index-assisted PEvO scope) — all CID-carrying PEvO content is appTag-tagged, so the scope drops no real reference, and the parenthesized OR-group ANDs the tags scope across all three branches. EXPLAIN on live HAF confirms a tags-GIN Bitmap Index Scan (~2905 est rows) replacing the seq-scan. A real-HAF `GET /ipfs/:cid` test pins the namespace fix against a published `metadata.<appTag>.ipfs_cid`. Review: `/ce-code-review` IPFS-cluster pass (11 personas, `ce-agent-native-reviewer` skipped per PEvO); param ordering + SQL precedence + monotonic-safety of the narrowing verified clean. All findings dismissed or routed to follow-ups: `backend-ipfs-cid-containment-query-extraction` (the cross-site query duplication) and `backend-ipfs-apptag-flip-pending-upload-drain` (the beta→prod flip data-loss path). Cross-refs: `cidIsKnown`, `cidReferencedInHaf`, `imageSrfGuardExpr`.

---

## BACKEND-IPFS-CLEANUP-BACKEND-DISPATCH (archived 2026-05-26) — round-1 hold FIXED, re-review clean ✓

P1: persist the originating pin backend per pending upload so orphan cleanup can release Pinata-origin pins. `runCleanup` (`backend/src/ipfs-cleanup.ts`) hardcoded `unpinFromKubo`, so a Pinata-origin pin reaching cleanup fired `pin/rm` at Kubo (benign "not pinned"), deleted the tracking row, and leaked the pin on Pinata forever. Migration 014 adds `pin_backend TEXT NOT NULL DEFAULT 'kubo'`; the `POST /ipfs/upload` INSERT records `result.backend`; `runCleanup` dispatches via the shared `unpinFromIpfs(cid, backend)`. Round-1 architect hold (1 item): the `pin_backend` domain was unenforced end-to-end (free-text column + `as PinBackend` cast + non-exhaustive `unpinFromIpfs` else-routes-to-Pinata), so an out-of-domain value would fire a Pinata DELETE for a CID Pinata never held and reap the row — the exact inverse of the leak being fixed. **Held item FIXED across all three defense layers:** migration 014 gains `CHECK (pin_backend IN ('kubo','pinata'))` (mirrors the bridge-queue `state` discriminator; DROP-then-ADD idempotent); `toPinBackend(value)` narrows at read time and throws on out-of-domain (caught by `runCleanup`'s per-row try/catch → logs + skips + row survives for an operator); `unpinFromIpfs` rewritten to an exhaustive switch with a `never`-typed default. An `s3` regression test asserts an unrecognized backend issues no unpin fetch and no row DELETE. Re-review: `/ce-code-review` IPFS-cluster pass (11 personas); migration verified safe — the `deploy.sh migrate` runner re-applies every `*.sql` unconditionally, so the CHECK lands even on a DB that ran the pre-CHECK 014 (`schema_migrations` is read only by the boot verifier, never by the runner). All findings dismissed or routed to the two follow-up tasks above. Cross-refs: `runCleanup`, `unpinFromIpfs`, `toPinBackend`, `PinBackend`, migration `014_pending_ipfs_uploads_pin_backend.sql`.

---

## BACKEND-PEVO-ADMIN-KEY-STARTUP-VALIDATION (archived 2026-05-26) — round-5 re-review clean ✓

P3 boot-time config validation so malformed deploy values fail boot loudly instead of surfacing as a runtime 504 (ambiguous-outcome misroute) or a silent-zero-rows author-pin mismatch. `validateConfig()` (`backend/src/startup-checks.ts`) runs `validatePostingKeyFormat` over the three config-sourced posting keys (admin/bridge/anon — optional-with-empty-fallback, skip-if-unset) and `validateAccountNameFormat` over the five Hive-account-name env vars (`HIVE_ADMIN_ACCOUNT`, `HIVE_BRIDGE_ACCOUNT`, `HIVE_ONBOARD_ACCOUNT`, `HIVE_ANON_ACCOUNT`, `BLOG_AUTHOR`). The canonical account-name pattern lives in `backend/src/lib/hive-account-name.ts` (`HIVE_ACCOUNT_NAME_REGEX`) — dot-separated segments, each `[a-z][a-z0-9-]*[a-z0-9]` of length 3-16, overall ≤16 — replacing an earlier permissive regex that accepted canonically-invalid names. Both helpers `.trim()`-guard whitespace-only input to a recognizable copy-paste-artifact message before dhive/regex sees it. Five hold rounds: r1 boot validator (admin/bridge); r2 anon-key + account-name coverage + blank-reject; r3 canonical regex + shared lib + `BLOG_AUTHOR` + whitespace guard + boundary tests; r4 removed a rot-prone docblock import-map + stale `:29` line anchor; r5 removed the surviving `round-2` qualifier, anchoring the docblock on the literal historical pattern. Round-5 re-review (`/ce-code-review` scoped to comment-only commit `9d6e78a9`, 3 personas — project-standards/maintainability/learnings on Sonnet; executable-behavior personas skipped per file-type-awareness; `ce-agent-native-reviewer` skipped per PEvO): all zero findings — round-N qualifier gone, replacement anchors on the literal regex (no new/sibling rot), all three governing comment-anchor conventions satisfied. Cross-refs: `backend/src/startup-checks.ts` (`validateConfig`, `validatePostingKeyFormat`, `validateAccountNameFormat`), `backend/src/lib/hive-account-name.ts` (`HIVE_ACCOUNT_NAME_REGEX`), `backend/tests/startup-checks.test.ts`.

---

## BACKEND-ANCHOR-ROT-SWEEP-STARTUP-CHECKS (archived 2026-05-26) — review clean ✓

P3 comment-anchor sweep on `backend/src/startup-checks.ts` + `backend/tests/startup-checks.test.ts` (commit `5c8b9b39`). Rewrote ~20 `round-N` / `hold #N` prefixes and `BACKEND-BRIDGE-KEY-*` task-slug citations to describe the behavioral invariant each annotates (validator semantics, `BootFatalError` throw shape, lazy-fallback cache contract), and reworded test `it`/`describe` titles to state what they assert rather than which review round added them. The implementer also folded in the pre-existing `.ts:NN` line-number anchors in the two files (a different rot class, swept since leaving them in a file being de-rotted would contradict the anchor conventions). Acceptance verified: `grep -nE "[Rr]ound-[0-9]|hold #|BACKEND-BRIDGE-KEY"` over both files = 0 hits; audit-own-replacement clean (no new slug / SHA / line-number / round-N introduced); comment-only — typecheck + lint clean, `startup-checks.test.ts` 42/42 at parity. Review (`/ce-code-review`, 5 personas scoped to `5c8b9b39` — correctness on Opus; testing/maintainability/project-standards/learnings on Sonnet; `ce-agent-native-reviewer` skipped per PEvO): correctness/testing/maintainability clean; the learnings pass confirmed the self-violation audit clean and the two `below` positional anchors pass the named-container carve-out. Project-standards flagged one P3 — the relative-time anchor `Pre-this-task` surviving in a rewritten comment block — dismissed at triage as pre-existing, trivial, and outside the strict round/hold/slug grep target. Cross-refs: `backend/src/startup-checks.ts` (`validateConfig`, `initBridgePostingKeyCache`, `getCachedBridgePostingKey`, `getRequiredBridgePostingKey`, `BootFatalError`, `BridgeKeyCacheUnpopulated`, `BridgeKeyLazyParseDivergence`); `backend/tests/startup-checks.test.ts`.

---

## UI-ACCOUNT-DELETE-CONSEQUENCES-AND-FRESH-AUTH (archived 2026-05-26) — re-review clean ✓

P2 rework of the account-deletion flow in `frontend/src/pages/settings.js` (`handleEmailDelete`): `DELETE /api/settings/email` erases the entire PEvO account row (`accounts`/`notification_preferences`/`pending_recovery`; `custody_audit_log` anonymized), not just the email column, so the old optimistic `emailStatus` patch left the user on a settings view bound to a dead account. Round-1 (commit `3f90107e`) landed the three independent requirements: (1) consequences copy rewritten (`deleteWarningLight`/`deleteWarningSelf` state that all PEvO-server account data is erased and cannot be undone; affordance/confirm relabeled to account-deletion framing); (2) new `deleteSeedPhraseContinuation` (light accounts) clarifying the on-chain Hive account survives and a seed-phrase holder can import into Keychain and continue self-custody (grounded in `ARCHITECTURE.md` § 6.3); (3) post-delete session teardown — `auth.disconnect()` + `notifications.stop()` + `accountDeleted` toast + `navigate('/')`, replacing the optimistic patch (same teardown mechanism `header.js` logout uses). Round-1 review held 3 cleanup items; round-2 (commit `f49d5ffc`) landed all three: greened the unit suite (76/76) by rewriting the happy-path test to positively assert the teardown contract (`disconnect` + `notifications.stop` + `accountDeleted` toast + `navigate('/')`) and dropping the accidentally-passing `preserves hasPassword` test (it survived only via the thrown-`disconnect` catch path before the mock existed); removed the orphaned `settings.emailDeleted` i18n key from all 16 locales (zero remaining refs, no STUBS entry); removed the dead pre-navigate state resets (`showDeleteConfirm`/`showChangeForm`/`emailMessage`/`emailError`), replaced with a behaviorally-anchored comment (`navigate('/')` destroys the component the same tick so local-field writes are unobserved). Re-review scoped to `f49d5ffc` (5 personas — correctness on Opus; testing/maintainability/project-standards/learnings on Sonnet; `ce-agent-native-reviewer` skipped per PEvO): zero actionable findings. Architect ran the suite directly (76/76 green) and confirmed the rewritten test is load-bearing (`vi.clearAllMocks()` in `beforeEach`; all four assertions map to real production calls via the component factory, so dropping any teardown call goes red). Dismissed at triage: a code-comment emdash flagged P3 by maintainability is explicitly exempt per root `CLAUDE.md` (comments are not user-facing text) and project-standards concurred; pre-existing round/hold/task-slug citations in `pages-settings.test.js` predate this diff and are out of scope. Deferred fresh-auth proof-challenge wiring (mint proof via the password/ORCID path and send with the DELETE) correctly split to its own blocked task pending `backend-settings-email-delete-fresh-auth-gate`. Cross-refs: `frontend/src/pages/settings.js` `handleEmailDelete`; `frontend/tests/unit/pages-settings.test.js`; `ARCHITECTURE.md` § 6.3 account-deletion note; `agents/docs/tasks/blocked/ui-account-delete-fresh-auth-proof-challenge.md` (the deferred follow-up).

---

## UI-AUTHOR-LIST-PREFILL-ON-REVISION (archived 2026-05-26) — re-review clean ✓

P2 write-path companion to the backend read-path cumulative-union (`backend-author-identity-model`): on the revise/continuation flow, `_prefillForm` (`frontend/src/pages/edit.js`) prefills the full prior author list from the API-resolved `authors[]` (falling back to raw `json_metadata` `pevo.authors` when the resolved set is absent/empty) so a revision defaults to "same authors as before" rather than an empty form that silently drops co-authors; each entry requires a non-empty `name` (R2). Original impl was held (2026-05-26, 3 findings) plus an escalated orcid-conflict the implementer correctly did NOT silently resolve; architect+user settled it as **normalize absent orcid to `''` on the data side (option a)**, superseding the prior `UI-PAPERS-ORCID-NULL-FALLBACK` null-preservation contract (the API's `orcid: null` is a read-side projection for accredited authors that the backend re-derives every read, so normalizing to `''` on re-broadcast is lossless and matches the primary-author path). Re-review scoped to held-fix commits `143bd090` + `aa54d945` (5 personas — correctness on Opus; testing/maintainability/project-standards/learnings on Sonnet; `ce-agent-native-reviewer` skipped per PEvO): **zero findings, all four held items landed clean.** #1 replaced the object-spread `{...a, name: a.name||a.hive||a.orcid||''}` with an explicit `{name, hive, orcid, affiliation}` projection + trim-aware fallback `String(a.name||'').trim() || a.hive || a.orcid || ''` + `orcid: a.orcid || ''` — strips read-time projection fields (`orcid_verified`/`orcid_discrepancy`) from the chain re-broadcast, restores `handleSubmit`'s `metaChanged` no-change comparison against the raw head claim, and closes a whitespace-only-name dead-end on read-only existing rows. #2 dropped the `(R3)` coordination anchor from the prefill test title. #3 added the empty-`p.authors` source-fallback unit test. #4 updated the `UI-PAPERS-ORCID-NULL-FALLBACK` block to assert `toBe('')` and de-anchored its comments off `papers.ts:417-434` / `edit.js:183` onto stable symbols (`buildCumulativeAuthorsForChain`, the `:value="ca.orcid || ''"` binding). No new line-number/SHA/slug rot introduced (convention-enforcing fix audited its own replacement). Soft items NOT held: a whitespace-only (`'   '`) name trim path is not directly unit-tested so `.trim()` isn't mutation-killed (preemptive hardening, dismissed per `feedback_dismiss_preemptive_test_hardening`); pre-existing anchor rot (`Round-2 item 2`, `edit.js:50`, E2E `edit.js:NNNN` clusters) lives in the same files but predates this diff and is out of held-fix scope. Split-off sibling `ui-dedup-author-hive-on-rebroadcast` archived separately. Cross-refs: `frontend/src/pages/edit.js` `_prefillForm`/`handleSubmit`; `frontend/tests/unit/pages-edit.test.js`; `frontend/tests/e2e/edit-paper.spec.js` accepted-claimer continuation (R2 broadcaster-name fill, `aa54d945`); `backend-author-identity-model` (read-path companion, source of the ratified model); `ARCHITECTURE.md` § 2 Multi-Author Trust Model; `agents/docs/solutions/conventions/hold-block-must-not-contradict-convention-docs-2026-04-22.md` (the escalate-don't-guess move).

---

## BACKEND-CLAIMER-ORCIDS-ACCREDITATION-AUTHORITY-GATE (archived 2026-05-26) — round-1 clean ✓

P1 reputation-integrity fix. The reputation cycle's co-author ORCID auto-accept arm (`accepted_claims` in `backend/src/reputation.ts` `computeReputationBatch`) sourced the claimer's attested ORCID from a hand-rolled `claimer_orcids` CTE that filtered only on `custom_id` + `action` + `block_num`, with no `required_posting_auths` authority gate. Exploit (verified reachable at the code level): an already-accredited attacker reads a victim paper's public on-chain ORCID `V`, self-broadcasts an `accredit` op signed with their OWN posting key (not an accreditation authority) setting their attested ORCID to `V`, then claims authorship at the matching `author_index` — pre-fix the self-attested `V` won `rn=1` and drove the auto-accept, accruing co-author reputation credit on a paper they did not author, every cycle. Fix (`969ac1ee`) replaced `claimer_orcids` with a JOIN to the authority-gated `active_accreditations` CTE (composed via `activeAccreditationsCteBody(20)`, whose `accred_ranked` applies `required_posting_auths ?| accreditationAuthorities`), the same gated source the read-surface `authorshipClaimsCteBody` ORCID arm joins — so a self-signed attestation never enters the trusted set, a revoke clears the prior attestation (strictly better than the old most-recent-op read), and the cycle and read surfaces agree on which attestations are valid. New params `$20 = config.appTag`, `$21 = config.accreditationAuthorities`. Behavioral regression (`backend/tests/routes/reputation-orcid-auto-accept-authority-gate.test.ts`, clause-(c) carve-out header) runs the production `activeAccreditationsCteBody` fragment against synthetic VALUES (HAF view redirected to a synthetic CTE): a self-signed accredit op does NOT auto-accept, an authority-signed one does; reverting the `?|` gate flips the attacker assertion red. `/ce-code-review` (9 personas: correctness/security/adversarial on Opus, testing/maintainability/project-standards/performance/kieran-typescript/learnings on Sonnet; ce-agent-native skipped per PEvO) found the fix correct — security independently re-ran the exhaustive call-site audit and confirmed no remaining ungated trust read of `accredit`/`revoke` ops; the `?|` operator is canonical (see `perf-floor-drop-removes-incidental-security-predicate`); the test was actually executed (passed, 150ms) with verified mutation-kill power. Reproducibility from public on-chain data preserved (authority set is `config.accreditationAuthorities`, not the singular `hiveAdminAccount` — see `project_admin_is_singular`). Architect updated `agents/docs/reputation-algorithm.md` to document the authority-gated `active_accreditations` source feeding ORCID auto-accept. The adversarial pass surfaced a PRE-EXISTING P1 in the sibling `approve_authorship` arm (signer-ungated: a self-claim + self-approve on a victim paper accrues co-author credit, same invariant class via a different arm) — filed as `backend-approve-authorship-signer-gate` (P1, pending), out of scope of this task. Cross-refs: `reputation.ts` `accepted_claims` ORCID arm; `hafsql.ts` `activeAccreditationsCteBody` / `accred_ranked`; `hive-schemas.md` accreditation-op signer contract; `wrapping-primitive-exhaustive-call-site-audit`.

---

## UI-DEDUP-AUTHOR-HIVE-ON-REBROADCAST (archived 2026-05-26) — round-2 clean ✓

P3 write-path cleanliness guard (split from the `ui-author-list-prefill-on-revision` review): before broadcasting a revision, `handleSubmit` (`frontend/src/pages/edit.js`) drops author entries whose normalized `hive` duplicates an earlier entry (keep first, preserve order); hive-less (`hive: null`) display-only credits never collapse against each other. Backend re-dedups on read, so this is on-chain `json_metadata.authors` cleanliness, not a correctness fix. Round-1 (`4c91729e`) landed the dedup + unit tests for both duplicate sources (carried-forward `authors[]` dupes, and a new co-author colliding with an existing one); `/ce-code-review` (6 personas) found the core correct and all acceptance criteria met, held 1 item. Round-2 (`a286faa8`) closed it: the key derived `(a.hive || '').trim().toLowerCase()`, which threw a `TypeError` on a non-string truthy `hive` (reachable via the broadcaster-controlled raw `json_metadata` author fallback that reads the head post's `pevo.authors` when the API's resolved `authors[]` is empty, unvalidated), aborting the whole revision broadcast — a denial-of-edit. Fix derives the key only when `hive` is a string (`typeof a.hive === 'string' ? a.hive.trim().toLowerCase() : ''`), else treats the entry as hive-less (preserved, never collapsed), restoring the pre-dedup passthrough instead of crashing. Added a load-bearing unit test (numeric `hive` alongside a string hive → broadcast succeeds with both preserved, numeric not collapsed; architect verified reverting the guard fails it with `(a.hive || "").trim is not a function`). Round-2 re-review (correctness / testing / project-standards, ce-agent-native skipped per PEvO): all clean — TypeError path fully closed (line 1019 is the only string-method `hive` call site; other `.hive` refs are equality/display/null-coalesce), no false collapse of multiple non-string hives, happy-path string dedup intact, no behavior change for empty/null, comment + test comment anchored on behavioral semantics (no slug/round/SHA/line citations). Three round-1 normalization edge cases architect-DISMISSED as corrupt-data-only and neutralized by the authoritative backend read-time dedup: mixed-case self-hive (protocol produces no mixed-case account names), Unicode-whitespace `hive` collapse, and inline-key vs `normalizeUsername` `@`-strip gap (the gap is under-dedup = pre-change status quo, harmless). Landed via a fan-out worktree whose harness base was ~141 commits stale and lacked `4c91729e`; the worker rebased onto current main before applying the guard, parent cherry-picked as `a286faa8` (so `4c91729e` is an ancestor — no orphan). Cross-refs: `frontend/src/pages/edit.js` `handleSubmit` dedup filter; `frontend/tests/unit/pages-edit.test.js` "drops duplicate-hive authors on re-broadcast" describe block; ARCHITECTURE.md § 2 Multi-Author Trust Model (additions allowed, removals only via `author_resign`); `feedback_dismiss_preemptive_test_hardening`; `feedback_dont_relitigate_settled_ssot` (chain SSoT, backend dedup authoritative).

---

## BACKEND-ANONYMOUS-REVIEW-HAPPY-PATH-TEST-COVERAGE (archived 2026-05-26) — round-2 clean ✓

P0 testing-tier (no production defect): close the zero-happy-path-coverage gap on `POST /api/reviews/anonymous`. Round-1 (`247159af`) added three specs to `backend/tests/routes/anonymousReview.test.ts` (valid-submission round-trip asserting proxy-account broadcast + attestation `custom_json` + `getAnonMapping`/`decryptMapping` byte-for-byte recovery; ciphertext byte-inequality across hex/utf8/latin1/base64 decodings; post-TTL expiry via a `__test_seams` seam) plus the `__test_seams` export on `backend/src/routes/anonymousReview.ts`; held 4 items. Round-2 (`a3e0640e`) landed all four: (1) an IV-distinctness spec — `__test_seams.encryptMapping('carol')` twice, `expect(env1.iv).not.toEqual(env2.iv)` — mutation-killing a `crypto.randomBytes(12)`→`Buffer.alloc(12)` nonce-reuse regression the byte-inequality spec misses, plus removal of two vacuous hex-only `not.toContain(reviewer)` lines; (2) docstring rewrite on the "admits a true third-party" spec anchored on the `not.toBe(403)` admission invariant (the file-level config mock now routes the request through the 200 success path, not the prior 500-misconfig path); (3) a `no-restricted-imports` rule in `backend/eslint.config.mjs` scoped to `src/**/*.ts` flagging the `__test_seams` named import from `**/routes/anonymousReview` (the seam re-exports gate-bypassing `storeAnonMapping`/`encryptMapping`); (4) removal of dead `ANON_TTL_DAYS` from the `__test_seams` block. Round-2 `/ce-code-review` (8 reviewers — correctness/security/adversarial on Opus; testing/maintainability/project-standards/kieran-typescript/learnings on Sonnet; ce-agent-native-reviewer skipped per PEvO): six clean. Security empirically confirmed the ESLint guard blocks static named + namespace + alias + deep-relative + no-extension import forms (the `.ts`-extension form is caught by `tsc` TS5097 under Node16 resolution); all `src/` files are `.ts` so `src/**/*.ts` leaves no production path unguarded. Two adversarial findings architect-DISMISSED at triage: (a) the IV-distinctness 2-sample `not.toEqual` would false-pass under a counter/incrementing nonce — preemptive per `feedback_dismiss_preemptive_test_hardening`, since `encryptMapping` uses `crypto.randomBytes` and the held `Buffer.alloc(12)` mutation IS correctly killed (a counter is not a present code path); (b) dynamic `import()` evades the static-import lint guard — the lint-rule approach was the explicitly-accepted round-1 scope (runtime-guard/module-split was named the not-taken option), `tsc` + single-instance posture cover the residual. Re-review intake note: round-2 landed cleanly at `a3e0640e` in HEAD (no orphan; the orphan-SHA caveat applies only to the sibling custody-crypto task). Cross-refs: `feedback_dismiss_preemptive_test_hardening`, `project_single_instance_only`, `test-seams-export-shape-as-const-2026-05-04.md`, `test-mock-carve-out-clause-c-2026-05-04.md`.

---

## BACKEND-CONTINUATION-CHAIN-TRUNCATION-NEGATIVE-CACHE-GUARD (archived 2026-05-26) — round-1 clean ✓

P2 availability/correctness. `resolveContinuationChain` (`backend/src/routes/papers.ts`, commit `6af88ff9`) now returns a discriminated `ChainResolution { chain, degraded }`; `degraded` starts true and flips false ONLY at the clean natural-end break (`rows.length === 0`), staying true on every other exit (pool-unavailable, pre/mid-loop wall-clock abort, empty/failed root head-authors fetch under HAF eventual consistency, swallowed inner-loop `statement_timeout` 57014, cycle detection, MAX_HOPS depth cap). `findCanonicalRoot` gates its membership-failed negative cache on `resolved !== null || !forwardDegraded` — positives always cache, a genuine clean miss still caches, but a degraded negative is skipped so a transient HAF blip can no longer lock a false `{root:null}` in for the 30-min TTL. `computeChainCumulativeFromHaf` returns null (skipping the `chain-authors:` cache write) on a degraded walk, subsuming the abort variant and closing the mid-walk non-abort truncation gap; the `chain.length === 1` short-circuit already covered root-only degraded walks. Live detail/version surfaces destructure `.chain` only and stay fail-soft (fewer versions on a partial walk, not negative-cached). Three canaries in `papers-canonical-root-walker.test.ts`: forward-walk SQL statement-timeout (non-abort) and empty-root-head-authors (eventual consistency) both leave the deep-chain leaf uncached; the clean negative still caches. Round-1 `/ce-code-review` (correctness + adversarial on Opus; performance, reliability, testing, maintainability, project-standards, kieran-typescript, learnings on Sonnet; `ce-agent-native-reviewer` skipped per PEvO): correctness and project-standards clean. The one substantive finding (adversarial conf 70 + performance conf 75, cross-reviewer corroborated) — the `degraded` flag conflates transient-recoverable causes (SQL timeout, empty head-authors) with attacker-deterministic ones (forward-walk cycle, MAX_HOPS depth cap), so skipping the negative cache reopens a per-request re-walk amplifier (canonical-root uses raw get/set, no single-flight) for the deterministic class — was architect-DISMISSED: caching deterministic-degraded negatives would reintroduce the false-negative this task fixed (the membership check cannot distinguish a legitimately-deep/cyclic chain, where the dropped leaf is real, from an attacker-built one), and the amplifier is bounded by `readLimiter` 120/min/IP + per-request wall-clock budget + MAX_HOPS on a single instance. Below-gate/dismissed: untested cycle/depth-cap/`computeChainCumulative` degraded paths (mechanically identical to the tested timeout path); pre-existing `"lines 826-827"` line-number anchor + `next.permlink as string` cast in `papers.ts` (noted for the existing `backend-anchor-rot-sweep`). Learnings confirm alignment with `caching-wrapper-discriminated-union-poisoning-2026-05-11`, `per-request-memo-catch-block-negative-cache-contract-2026-05-06`, `pg-abortcontroller-budget-bounded-by-statement-timeout-2026-05-16`. Cross-refs: `project_single_instance_only`; sibling `backend-canonical-root-walker-cumulative-aware` (round-2 abort re-check this extends).

---

## BACKEND-LISTING-PROFILE-ERROR-ISOLATION-ROUTE-TEST (archived 2026-05-26) — round-1 clean ✓

P2 test-coverage follow-up from `backend-cumulative-union-listing-surfaces-parity` round-2. Added `backend/tests/routes/papers-cumulative-route-error-isolation-mocked.test.ts` (commit `bbfc24ca`, test-only, 2 tests) that drives the REAL `GET /api/papers` and `GET /api/profile/:username/papers` handlers via supertest with a 2-row fixture in which exactly one row's `resolveChainCumulativeAuthors` is forced to throw — failure injected at `hafCache.getOrSet` for the erroring row's `chain-authors:<author>:<permlink>` key, all other keys (including the profile response-cache key and the sibling's pre-seeded warm entry) delegated to the real impl. Asserts response 200 (not 5xx), the erroring row falls back to head-meta `authors` with empty `accredited_authors` and NO sibling inheritance, and the sibling keeps its cumulative-union enrichment. Closes the gap the prior helper-boundary canary (`papers-cumulative-cross-surface-parity-mocked.test.ts`) could not catch: that test re-implemented the route's `Promise.all` + per-row `try/catch` inline, so a refactor relocating the catch out of the per-row map went unobserved; this file exercises the integrated route so the relocation surfaces (profile → 500; listing → 200-with-empty-page caught by the row-count assertion). Round-1 `/ce-code-review` (correctness on Opus; testing, project-standards, maintainability on Sonnet; `ce-agent-native-reviewer` skipped per PEvO): clean. The correctness reviewer ran a MUTATION experiment — relocated the per-row catch in both routes, confirmed both tests go RED, restored to a clean diff — proving the test is not false-green. Mock carve-out clauses (a)/(b)/(c) verified in the file header (both routes are unauthenticated reads behind `readLimiter`, no auth middleware mocked or bypassed). Dismissed: duplicated fixture/spy setup between the listing and profile blocks (test-code DRY, non-blocking; the complement itself is justified); optional vacuous-pass guard asserting `getOrSet` was called with the erroring key. Note: the commit message's "surfaces here as a 5xx" framing is literally true only for the profile surface (listing surfaces as 200-empty caught by the row-count assertion) — cosmetic, in the message not the code. Cross-refs: sibling `backend-cumulative-union-listing-surfaces-parity`; `test-mock-carve-out-clause-c-2026-05-04.md`.

---

## BACKEND-CACHE-KEYS-SCAN-AND-INVALIDATEPREFIX-RACE (archived 2026-05-26) — round-1 clean ✓

P3 reliability/perf split out of `backend-cache-single-flight-coalescing` round-3 review (both items pre-existing, not introduced by the per-tier work). Two fixes to `QueryCache` in `backend/src/cache.ts` (`58a2bec9`): (1) replaced the blocking `redis.keys(prefix+'*')` in `clear()` and `clearVolatile()` with a non-blocking SCAN cursor loop — `clearVolatile()` fires on every ~3s Hive block tick, so the O(N) blocking KEYS stalled the whole Redis server continuously; the SCAN pattern (already used by `invalidatePrefix`) was extracted into a private `scanAndDeleteKeys(redis, suffix, shouldDelete?)` helper (SCAN COUNT 200, batched DEL CHUNK 200, optional unprefixed-key predicate for `clearVolatile`'s stable-retention filter), and all three multi-round-delete methods route through it. (2) Closed the multi-round-SCAN invalidation-during-flight window: `clear()`/`clearVolatile()`/`invalidatePrefix()` now bump their tier's epoch(s) BOTH before AND after the sweep — the before-bump suppresses fetchers that started before the call (even if they resolve mid-loop after their key was deleted), the after-bump suppresses fetchers registered DURING the loop that resolve after it; `invalidate(key)` stays a single before-bump (genuinely single-shot, one `del`). `clearVolatile()` brackets only `volatileEpoch` (never `stableEpoch`, preserving the `stable: true` contract). New mocked-`getRedis()` test `cache-invalidation.test.ts` pins SCAN-not-KEYS (spy on `redis.keys` not-called), `clearVolatile` stable-key retention through the migration, and the after-bump mid-loop window (mutation-kill: remove the after-bump → re-cached → RED). Round-1 `/ce-code-review` (9 reviewers — correctness/adversarial on Opus; reliability/performance/testing/maintainability/project-standards/kieran-typescript/learnings on Sonnet; `ce-agent-native-reviewer` skipped per PEvO): clean except 2 P3 advisories, both architect-dismissed — (a) residual "case-3" window (a fetcher that registers AND resolves entirely inside the 1–2-round sub-ms SCAN loop before the after-bump) is unreachable at PEvO single-instance/small-keyspace scale since `fn()` is a slow HAF query, and the comments correctly do NOT claim it closed (task acceptance explicitly permitted accept-and-document); (b) `toDelete` accumulates all matched keys before the first DEL — cosmetic at small keyspace, streaming delete-per-batch would be strictly better. Cross-task verified: the before→before+after change does NOT break the single-flight per-tier suppression invariants (`clearVolatile` still bumps only `volatileEpoch`, so STABLE in-flight writes survive the 3s tick). Below-gate maintainability nit (`suffix`→`keySubPrefix` rename) suppressed. The stale `single-flight-coalescing-amplifies-cache-invalidation-race-2026-05-20.md` doc refreshed at archive to fold in the per-tier counters AND the multi-round before/after bracket. Cross-refs: `project_single_instance_only`, `single-flight-coalescing-amplifies-cache-invalidation-race-2026-05-20.md`, sibling `backend-cache-single-flight-coalescing`.

---

## BACKEND-CACHE-SINGLE-FLIGHT-COALESCING (archived 2026-05-26) — round-4 clean ✓

P2 performance-under-degraded-HAF: coalesce concurrent same-key fetches in `QueryCache.getOrSet` so N concurrent cache-misses for the same `(author, permlink)` during a degraded-HAF window share ONE in-flight fetcher instead of each spawning a walker call and amplifying pool contention by N. Round-1 (`623bee26`) added a `private inflight = new Map<string, Promise<unknown>>()`; `getOrSet` probes/awaits the existing promise, writes on non-null, clears the slot in `finally` across all terminal states; 4 unit specs + an integration canary on `/api/papers/:author/:permlink/enrichment` (mutation-kill: remove the map → 3× SQL volume). Round-2 (`d6e23014`) closed an invalidation-during-flight race adversarial surfaced: a single-flight fetcher resolving AFTER an `invalidate()` re-cached its pre-invalidation snapshot, amplified from per-fetcher to per-key-wave. Fix: an epoch counter captured at fetcher start, gating the success-path `this.set` on epoch-match; plus TOCTOU docblock honesty (single-flight eliminates duplication within an event-loop tick, reduces it under the Redis-probe window) and a clause-(c) citation fix. Round-3 (`cbefb6ba`) split the single `epoch` into per-tier `volatileEpoch`/`stableEpoch` after review found `clearVolatile()` (every 3s block tick) bumped the shared epoch and suppressed in-flight STABLE writes, defeating the `stable: true` contract: `clearVolatile` now bumps only `volatileEpoch`; `clear`/`invalidate`/`invalidatePrefix` bump both; the `getOrSet` gate keys a stable entry on `stableEpoch` only, a non-stable entry on both. Round-4 (`87162078`, test-only) pinned the two converse per-tier suppression directions that were only transitively covered: `invalidate()` during an in-flight STABLE fetcher suppresses the write (mutation-kill: drop `stableEpoch++` from `invalidate()` → RED), and `clearVolatile()` during an in-flight NON-stable fetcher suppresses the write (mutation-kill: drop the `volatileEpoch` conjunct → RED). Round-4 `/ce-code-review` (6 reviewers — correctness on Opus; testing/project-standards/kieran-typescript/maintainability on Sonnet; `ce-agent-native-reviewer` skipped per PEvO): 0 findings — both mutation-kills confirmed genuine and isolating, all four tier×operation combinations now pinned, clause-(c) companion citation verified. Follow-ups: `backend-cache-single-flight-coalescing-swr-cold-path` (blocked — extends epoch-gating to `getOrSetSWR`/`revalidate`/`registerPeriodicRefresh.reload`); `backend-cache-keys-scan-and-invalidateprefix-race` (the SCAN/race split-out, archived same pass). `single-flight-coalescing-amplifies-cache-invalidation-race-2026-05-20.md` refreshed at archive for the per-tier shape. Cross-refs: `project_single_instance_only`, `feedback_dismiss_preemptive_test_hardening`.

---

## BACKEND-IPFS-PIN-INSIDE-DB-TRANSACTION (archived 2026-05-26) — clean ✓

P0: make the `pending_ipfs_uploads` INSERT load-bearing for `/api/ipfs/upload` success so a pin can never go live without a tracking row (the undetectable-orphan class). On insert failure the handler compensates by unpinning via `unpinFromIpfs(cid, backend)` against the same backend that pinned (the `PinResult.backend` discriminator), and refuses the pin with 503 before calling `pinToIpfs` when `getAppPool()` is null. Round-3 landed 6 held items + a bundled emdash fix: an over-unpin guard (a row-absence re-check `SELECT 1 … WHERE cid` before compensating, so a concurrent or committed-but-unacked row's live pin survives; if the re-check itself throws, skip the unpin — bias toward a tolerated orphan over guaranteed data loss); a real-path `verifyHiveSignature` companion (`ipfs-upload-real-path-verifyhivesignature.test.ts`) after correcting a false carve-out clause-(b) claim that a sibling test exercised real crypto when it applied `MOCK_VERIFY_SIGNATURE`; `unpinFromPinata` benign-absence tolerance mirroring Kubo; Pinata compensation-dispatch + double-failure tests; `dbErr` log-serialization normalization (pino serializes only the `err` key); and the user-facing "IPFS not configured" emdash. Final pass held 2 minor items, both landed: the `rowCount !== null && === 0` guard at the re-check (matches the file's sibling rowCount checks; behaviour-preserving since `null === 0` was already false) and a `unpinFromPinata` benign-absence unit (`ipfs-shared-unpin.test.ts`) pinning the content-based not-pinned tolerance the pre-fix code lacked. Combined-cluster `/ce-code-review` re-review (correctness/security/adversarial on Opus): clean — all three over-unpin exit paths and the double-failure path traced, the real-middleware companion confirmed to run genuine signature recovery (only the chain-read `getAccounts` mocked). Accepted residual carried forward: the Pinata not-pinned match is content-based, not status-gated, because the live already-unpinned body is undocumented (docs.pinata.cloud documents only the 200) — current behaviour was deliberately pinned with a test rather than over-fit to a verbatim string. Cross-refs: `test-mock-carve-out-clause-c-2026-05-04.md`, `re-review-cluster-path-restricted-union-diff-not-per-commit-2026-05-26.md`, `pg-cross-join-lateral-where-guard-fires-after-srf-2026-05-16.md`.

---

## BACKEND-IPFS-SHARED-MODULE-EXTRACTION (archived 2026-05-26) — clean ✓

P3 maintainability: de-duplicate the byte-identical `unpinFromKubo` and the broadcaster-`image`-array SRF CASE-WHEN guard fragment into `backend/src/lib/ipfs-shared.ts` (exporting `PinBackend`, the guard, `unpinFromKubo`/`unpinFromPinata`/`unpinFromIpfs`), imported by both `routes/ipfs.ts` and `ipfs-cleanup.ts`; the SRF test consumes the shared symbol so the test fragment cannot drift from production. First pass held 1 item: the guard hardwired the relation alias `c` as an invisible, unenforced contract (a third call site aliasing the comment relation differently would interpolate successfully then fail at runtime with a column-not-found error). Resolved by converting the `IMAGE_SRF_GUARD_EXPR` constant into an `imageSrfGuardExpr(alias)` builder that validates `alias` as a bare SQL identifier (`/^[A-Za-z_][A-Za-z0-9_]*$/`, throws otherwise — interpolated not bound because SRF-argument SQL takes no placeholder, and call sites pass compile-time literals never user input) and substitutes it into both the `jsonb_typeof` guard and the `THEN` branch; both sites pass `'c'`. The canary now asserts the guard shape, alias substitution (a different alias appears verbatim and the default `c` does not leak), the non-identifier-alias throw, and that every `jsonb_array_elements_text(` SRF argument at both call sites interpolates the builder — so a gutted builder, a dropped alias argument, or an inlined SRF argument reds. Combined-cluster `/ce-code-review` re-review: clean (correctness confirmed every reference resolves with no dangling symbol; maintainability confirmed both symbol groups were genuinely duplicated pre-extraction; the builder enforces rather than merely signals the alias contract). The cluster's third task `backend-ipfs-cleanup-backend-dispatch` was held this pass for a `pin_backend` domain-enforcement finding (free-text TEXT column + unchecked `as PinBackend` cast + non-exhaustive `unpinFromIpfs` else). Cross-refs: `pg-cross-join-lateral-where-guard-fires-after-srf-2026-05-16.md`, `re-review-cluster-path-restricted-union-diff-not-per-commit-2026-05-26.md`, `req-query-as-string-cast-silent-coerce-2026-05-16.md`.

---

## ARCHITECT-RECOVER-EMAIL-API-CONTRACT-UPDATE (archived 2026-05-26)

Architect-self doc-sync: brought `agents/docs/api-contracts/auth.md` and `ARCHITECTURE.md` §§ 6.3/6.4 in line with the already-landed two-phase memo-key recovery (`backend-recover-email-verification-and-notify`, archived 2026-05-26 round-3 clean). No implementer code diff — the recover trio in `backend/src/routes/recover.ts` was the source of truth and was reviewed under the backend task, so `/ce-code-review` did not apply here; the edits were verified line-by-line against `recover.ts` + migration `012_pending_recovery.sql`. Changes: (1) `auth.md` `POST /api/auth/recover` rewritten for the **path-dependent** response discriminator — memo-key path returns `{ recovery: 'pending_verification', message }` at 200 with no JWT (phase 1 stages only), ORCID path still returns the `{ token, expires_at, custody, username }` JWT envelope immediately; (2) new `auth.md` sections for `POST /api/auth/recover/verify` (phase-2 apply, 24h verify token, `INVALID_TOKEN` collapses not-found/consumed/disputed/expired/account-gone/upgraded into one generic message to avoid an account-state oracle, 409 DUPLICATE on email-claimed-since-phase-1) and `POST /api/auth/recover/dispute` (48h dispute window, `COALESCE(disputed_at, NOW())` idempotency, dispute-after-consume recorded for forensics but not rolled back); (3) documented the shared `auth-recover` 10/IP/hr limiter spanning all three endpoints; (4) `ARCHITECTURE.md` § 6.4 Recover row split into seed-phrase (two-phase, requires memo key **AND** email-control sub-proof) and ORCID (immediate, severed at state D) rows; (5) § 6.3 recovery-transition note that `recover(seed_phrase, …)` lands at phase 2, keeping the state machine truthful with invariant #4; (6) dispute-mail PII convention (domain-only via `emailDomain()`, CNPD-defensible) and the forensic-trail-does-not-survive-`DELETE /api/settings/email` note both captured in `auth.md`. § 6.5 invariant #3 left unchanged (email-control is an additional proof on top of the seed-phrase factor, not a change to factor-matching). auth.md additions kept emdash-free per the api-contracts convention. Non-goal deferred: a `details.reason` discriminator on the collapsed `INVALID_TOKEN` 400 (SPA can string-match today).

