## UI-ORCID-STUB-REAL-ROUNDTRIP-UNFIXME (archived 2026-06-10) — drive the ORCID E2E specs through the real backend round-trip; round-3 clean ✓

Un-fixme'd the real-backend ORCID blocks against the orcid-stub compose sidecar (commit 66a46ec1): settings-orcid-factor.spec.js gained the live acceptance test (real /orcid/start -> in-page authorize fulfil -> real /orcid/callback proof mint -> real /api/settings/set-password 200, plus DB password_hash, cached-proof shape, post-reload UI, and password-login-200 assertions), orcid-link.spec.js's 403 cross-user state-hijack test runs for real (the stub sets ORCID_CLIENT_ID/SECRET so its skip guard no longer fires), and orcid-no-password.spec.js's set-password fixme was consolidated to a pointer; its signup/recover fixmes stay (token-only stub serves no pub.orcid.org works endpoint; follow-up ui-orcid-signup-recover-real-roundtrip filed). Non-obvious redirect-host bridge: the SPA's ORCID_REDIRECT_HOSTS allowlist guard would throw on the stub host, so the test passes /orcid/start through to the real backend and rewrites only the redirect_url host to orcid.org before intercepting **/oauth/authorize*. Round-1 hold (3 items, commit 9a1c95b6): waitForResponse arm on the post-reload email fetch, routeOrcidStubBridge(page, baseURL, code) dedup of the byte-identical bridge at both call sites, orcid-no-password.spec.js header rewritten off the stale SEC-004-BE framing. Round-2 hold (1 item, commit f921f9a8): the waitForResponse arm was itself non-discriminating — the set-password section's x-if gate (!emailLoading && emailStatus && hasPassword === false) keeps it hidden through the whole email-fetch window, so toBeHidden() soft-PASSED on the loading frame; replaced with a positive post-loading-surface gate (await the "Change email" button visible — it renders only once !emailLoading && emailStatus && hasEmail && verified) and the comment rewritten to the real mechanism, anchored on the binding expression and state names. Round-3 /ce-code-review (correctness on the session model; testing/maintainability/project-standards/julik-frontend-races/learnings on Sonnet; ce-agent-native skipped per PEvO): clean — correctness and testing independently verified the gate is discriminating (a hasPassword:false regression renders the section in the same Alpine flush and fails toBeHidden(); a failed fetch falls back to hasEmail:false and times out the gate loudly), and the comment anchors are convention-clean. One P2 dismissed at user triage as preemptive hardening: the role+name "Change email" locator vs the file's getByTestId house style (an en.json rename of settings.emailChange breaks it loudly via locator timeout, not silently; the fix would add a production testid purely for a test locator; suite-level English-string precedent exists in settings.spec.js).

## BACKEND-ACCREDITATION-STATE-READ-TIEBREAKER-SWEEP (archived 2026-06-09) — same-block `id` tie-breaker on the 4 remaining inline accreditation-state reads; clean ✓

The semantic-sibling sweep the prior `window-cte-deterministic-tiebreaker` syntactic grep missed: four INLINE "latest accredit/revoke wins" reads (commit 77571323) gained the `ORDER BY cj.block_num DESC, cj.id DESC` same-block deterministic tie-breaker — `findAccreditedAccountWithOrcid`'s first read + binding-live re-check and `getExistingAccreditation` (routes/orcid.ts), and `getAccreditationFromHaf` (routes/profile.ts) — each with a convention-anchored comment (cites the custom-json hive-primitive design-rules convention by name, not a dated filename). `/ce-code-review` (correctness + security on Opus; testing/maintainability/project-standards/learnings on Sonnet; ce-agent-native skipped per PEvO): clean on all four sites. Security: pure determinism hardening that CLOSES the non-determinism window (a same-block revoke-after-accredit now correctly reports revoked, not a coin-flip stale accredit), the `required_posting_auths ?| accreditationAuthorities` SEC-AUTH-BYPASS authority gate is byte-preserved at every site, and a self-broadcast can't sort after a legitimate authority op (the gate excludes it from the candidate set before ordering). Correctness confirmed the four are the COMPLETE in-scope set (re-grep of `block_num DESC`: the notification window-feed and the genesis `MIN()` aggregate are not of-shape; the accepted residuals wot `update_params` / reputation `update_weights` / `lib/idempotency.ts` / papers `batchResolveVotes` are out of family), `cj.id` is the monotonic global op id (DESC + LIMIT 1 = latest), and no projection change was needed (profile.ts already projects `cj.id AS event_id`; the orcid reads use it in ORDER BY only). Sub-goal 2 (single-source the ranking so the shape canary reaches these inline reads) was deferred to the architect and, at user triage, accepted-as-correct + archived; the residual canary-reachability gap (a future dropped tie-breaker at an inline read passes the suite because the live HAF corpus can't seed a same-block collision) is now tracked by the new follow-up `backend-accred-state-read-tiebreaker-eslint-guard` (a structural ESLint guard mirroring `pevo/no-custom-id-block-num-floor`). Family now complete per `accreditation-state-read-latest-action-wins`.

## BACKEND-BUILDWITH-ADOPTION-PROFILE-ACCREDITATION (archived 2026-06-09) — convert manual single-CTE WITH spellings to buildWith; remaining sites documented; re-review clean ✓

Converted the manual `WITH ${body.sql}` template spellings at the profile.ts + accreditation.ts CTE sites to `buildWith` (byte-equivalent emission, verified round-1). Round-1 hold (1 item): acceptance #2's global `grep -rnE 'WITH \$\{' backend/src` was not clean — remaining sites were neither converted nor documented. Fixed at commit 7a2a24fc (comment-only): enumerated every remaining `WITH ${` match and documented the three genuinely-inconvertible ones inline with their structural reason — `lib/ipfs-shared.ts` (conditional collapse-to-empty: buildWith always emits `WITH …` and can't express the empty branch), `notification-queries.ts` (a builder CTE hand-joined with inline-literal CTEs, which buildWith — composing CTE builders, not literals — can't express), and `routes/papers.ts` detail query (the CTE params bind AFTER the outer author/permlink/bridge params and `$4` is reused as appTag + parent_permlink, so a buildWith-first-params adoption would renumber every `$N` on this hot query); the fourth grep match is the buildWith implementation itself. Re-review `/ce-code-review` (correctness on Opus; testing/maintainability/project-standards/learnings on Sonnet; ce-agent-native skipped per PEvO): clean — all three rationales verified accurate to the code at HEAD, anchors convention-clean, and the architect confirmed the grep is exhaustive (a broader `WITH (RECURSIVE )?` quote-or-backtick sweep finds no non-`${` manual WITH compositions). One P3 maintainability nit dismissed at triage: the papers.ts comment's `$4`-reuse clause partially restates the param-layout block ~20 lines above it (harmless redundancy; the novel "would renumber every $N" consequence + the "Not buildWith:" label is what earns the comment). Pre-existing `routes/papers.ts` `Per task hold-block item 4d` anchor-rot (not introduced here) noted against the anchor-rot sweep effort.

## BACKEND-PROFILE-REVIEWS-ACCRED-GATE-CANARY-STALE-PARAM-SLOTS (archived 2026-06-09) — restore the tautological param-slot canary against the 7-CTE-param shape; clean ✓

The `/api/profile/:username/reviews` param-position canary in `profile-reviews-accred-gate.test.ts` had gone tautological: it pinned `params[3]`=username / `params[5]`=anonAccount with `toBe('string')`, but when the reviews CTE grew from 2 to 7 params (the buildWith composition adding `authorshipClaimsCteBody{claimer}`) those fixed slots moved onto the CTE's own appTag/bridge string params, so the assertion passed unconditionally even on a mis-bind. Restored at commit 55a971ff (test-only): `USERNAME_SLOT`/`ANON_SLOT` are now DERIVED from the live builder param count (`buildWith(1, activeAccreditationsCteBody, authorshipClaimsCteBody{claimer}).params.length`) and VALUE-pinned (`=== requested username` / `=== config.hiveAnonAccount || ''`), and the old `round-3 hold #1` rot-anchor was removed and the header re-anchored on the builder composition. `/ce-code-review` (correctness on Opus; testing/maintainability/project-standards/learnings on Sonnet; ce-agent-native skipped per PEvO): clean. Correctness EMPIRICALLY mutation-killed it (swapped username↔appTag binds → the value-pin failed red) and confirmed the handler bind order `[...accredCte.params(7), username(7), appTag(8), anonAccount(9), ...]`, so the derived slots land exactly. One P3 dismissed at triage: maintainability + learnings flagged the `+2` fixed offset / derive-from-shared-builder as a staleness edge and suggested an independent literal slot pin — adjudicated as preemptive hardening, because the value-pin already fails RED on a wrong offset and so is NOT the `dedup-shared-constant-defeats-test-value-pin` tautology (which bites only when the EXPECTED VALUE, not merely the slot index, is derived from the shared constant; here the expected value is the literal username). Pre-existing `round-N hold` anchors in the test header (lines 1-56, untouched) noted against the anchor-rot sweep effort.

## UI-ORCID-FACTOR-NEGATIVE-PATH-E2E (archived 2026-06-09) — registered-factor-mismatch 403 pinned to the §6.5 invariant #2 guard message; re-review clean ✓

Negative-path E2E (isolated mismatch describe in `settings-orcid-factor.spec.js`) that actually exercises §6.5 invariant #2 (registered-factor equality): because the orcid-stub reflects the submitted `code` straight back as the token `orcid`, a happy-path run where `code == seeded iD` passes whether or not the backend checks the equality — only a mismatch (seed iD A, drive `code = B != A`, both valid ORCID-iD format so B clears `ORCID_RE` and reaches the equality check rather than the 400 format path) forces `handleFreshAuth`'s `accountOrcid !== orcidId -> 403`. Isolated account from the happy path (which the real set_password mutates to State B); B = `0000-0002-1825-0097` asserted `!== A`. Round-1 hold (1 item): the test asserted only `status===403` + `error.code==='FORBIDDEN'`, but `/api/orcid/callback` has TWO FORBIDDEN exits — the caller-mismatch guard ("Callback caller does not match initiator", reachable because fresh_auth is in AUTHENTICATED_MODES) and the registered-factor guard ("The ORCID you authenticated with is not linked to this account.") — so the assertion could not prove which fired; a regression decoupling the two username derivations would 403 via the caller guard and leave invariant #2 silently unexercised. Fixed (commit 9a1c95b6): added `expect(cbBody.error.message).toBe('The ORCID you authenticated with is not linked to this account.')`, anchored on the message text (not a backend line number), so a caller-guard 403 now fails the test instead of passing it. Re-review `/ce-code-review` (correctness + adversarial on Opus; testing/maintainability/project-standards/julik-frontend-races/learnings on Sonnet; ce-agent-native skipped per PEvO): clean — the asserted string matches the registered-factor `sendError` site verbatim, fresh_auth's membership in AUTHENTICATED_MODES makes the caller guard genuinely reachable (so the disambiguation is load-bearing), and the fix directly follows the `assert-discriminating-error-message-when-code-shared` convention written for this exact case. Coordinated in the same commit with the round-trip task's `routeOrcidStubBridge` dedup so the two edits to the mismatch test did not collide.

## BACKEND-NOTIFICATIONS-ROUTE-NEWEST-FIRST-WHOLE-BLOCK (archived 2026-06-09) — newest-first fetch + whole-block delivery; 3 rounds, edge-aligned-keep refinement; clean re-review ✓

Route-consumer half of the settled fetch-ordering contract (the digest half is tracked separately). `fetchNotificationsFromHaf` gained a `direction` param + a monotonic HAF op-id (`<view>.id`) same-block tie-breaker + a partial-cap-boundary-block drop on cap-hit; the route's `applySinceBlockFilter` was rewritten for newest-first whole-block delivery and the SPA rewind dropped (original commit 3591fb46). Three hold rounds, all resolved: round-1 (4 items) — `capHit` switched from `>= cap` over `LIMIT cap` to a `cap + 1` probe with `> cap` so an exactly-cap fully-contained window drops nothing, the `MOCK_VERIFY_SIGNATURE` carve-out acknowledgment moved to clause (a), the digest `direction='asc'` arg pinned, and a single-block-exceeds-cap empty-batch test added; round-2 confirmed those; round-3 (2 items, commit 8a0d44b8) — cap-edge over-drop of a COMPLETE boundary block fixed by gating the boundary-drop on `probeBlock === lastKeptBlock` (drop the truncated-end block only when the cut fell INSIDE a block; an edge-aligned cut keeps the complete block, because a forward 'desc' cursor and the inherited 'asc' digest never recover a falsely-dropped complete block), plus hold-item ordinals removed from test source. Final re-review (`/ce-code-review`, correctness + adversarial on Opus; testing/maintainability/project-standards/kieran-typescript/learnings on Sonnet; ce-agent-native skipped per PEvO): both round-3 items verified correct — the gate is direction-symmetric and `has_more = capHit` holds in both the drop and keep sub-cases; the op_id tie-breaker, cap+1 probe, and cross-account isolation were re-probed and not regressed. The asc-firing-gate partial-cut coverage two route reviewers flagged as missing in the route test file is resolved at HEAD by the sibling digest task's `fetch-notifications-asc-whole-block.test.ts` canary (which fires the gate with `probeBlock === lastKeptBlock` and mutation-kills an `events[0]` asc-branch swap); one maintainability dual-array-indexing comment nit (raw `result.rows` vs sorted `events`) dismissed at triage as optional polish on already-correct code. 24/24 tests green across the three notification/digest test files; typecheck + lint clean. The pre-staged `/ce-compound` fired at archive: folded the `cap + 1` probe (`> cap`) and the `probeBlock === lastKeptBlock` edge-aligned-keep refinement into `forward-cursor-feed-newest-first-and-rewind-masks-cap-edge` (the doc had been teaching the naive "drop the truncated-end block on cap-hit" form, which over-drops a complete edge-aligned block). Accepted residual: a single Hive block alone holding more than `cap` events is a permanent drop in either direction (near-unconstructible at single-instance accredited scale). Deploy backend-first or atomically, never frontend-first; the paired UI task `ui-notifications-drop-rewind-and-block-cap` is already archived.

## BACKEND-AUTHOR-INDEX-CAST-HARDENING (archived 2026-06-09) — {1,9} overflow rationale anchored in-source; re-review clean ✓

P0 fix: 7 broadcaster-controlled JSONB `::int` casts (2 `author_index` in hafsql.ts/reputation.ts + 5 revote `weight` across reputation.ts and routes/papers.ts) were wrapped in regex-guarded `CASE WHEN <regex> THEN ...::int END` so a forged custom_json fails closed (the row drops, the query completes) instead of aborting the daily reputation cycle and every read-surface query touching the targeted paper. The weight bound is `^-?[0-9]{1,9}$` (overflow-safe: the spec's unbounded `+` admits a 14-digit value that overflows int4 and re-aborts the very query the fix protects; max Hive vote weight is 10000); `author_index` is `^[0-9]{1,9}$`. Round-1 hold (1 item, comment-only) = anchor that `{1,9}` rationale in-source so a future editor cannot "simplify" the bound back to `+`. Landed at commit 415a616e: one-line SQL comments at each weight-guard site (the three reputation.ts revote arms, the papers.ts detail + voters enrichment revote queries) and the author_index guard (hafsql.ts), each behaviorally anchored. Re-review /ce-code-review (correctness on Opus; project-standards + maintainability on Sonnet; ce-agent-native skipped per PEvO): clean — verified comment-only (no SQL/regex/cast/placeholder changed), technically accurate (999,999,999 < int4 max 2,147,483,647; the unbounded form overflows and aborts), anchors clean (no slug/round/line/SHA/section marker). Dismissed: the five identical weight-comments duplicate (acceptable — the revote weight cast is inline in separate template literals across three files with no shared SQL builder).

## BACKEND-CLAIMER-SELF-REVIEW-DISPLAY-CALLSITE-EXCLUSION (archived 2026-06-09) — credited claimer's self-review/self-vote dropped from display surfaces; ClaimsRow type re-review clean ✓

Closed the display-side residual of co-author-claim-zero-score: a credited ORCID-matched or name-only-slot claimer (absent from `authors[].hive`) could self-review/self-vote their own paper and inflate the displayed `avg_rating`/`review_count`/`net_votes` (no computed-score effect — the reputation cycle already gates claimers). New `excludeClaimedSelfWhere` helper (the `accepted_claims NOT EXISTS` gate) composed at every review surface (listing rev_agg LATERAL + detail review list in papers.ts, profile reviews-list + stats, search, single-review fetch) and every vote surface (`batchResolveVotes` claimedSet for listings, `fetchEnrichmentFromHaf` acceptedClaimers for detail). Round-1 hold (3 items, commit 7802fcf1): the P1 revote-channel `net_votes` bypass closed (acceptedClaimers skip in BOTH the native-vote and revote merge loops, completing the native/revote × listing/detail 2x2), exact source-count test pins + a behavioral net_votes canary that mutation-kills both channels, and a named `drVoteAccreditedIdx` const. Round-2 hold (1 item, P3, commit 0bfef6c6): typed the per-paper claims query `pool.query<ClaimsRow>` so the accepted-claimer exclusion reads `status`/`claimer` as declared columns, not `any`. Re-review /ce-code-review (correctness + security on Opus; kieran-typescript/testing/maintainability on Sonnet; ce-agent-native skipped): clean — ClaimsRow matches the SELECT projection 1:1, type-only, a net improvement. Triaged at archive: correctness + security converged that `pool.query<T>` is a compile-time-only assertion (it catches a TS-read-side drift but NOT a SQL-side SELECT-column drop) — accepted as-is per the dismiss-preemptive-test-hardening stance (the SQL projects all six columns today; a real-path SQL-drift test is deferred), and the batchResolveVotes listing-path `Record<string,unknown>` asymmetry dismissed (explicit casts, already accepted in round-2). Architect-handled at archive: `api-contracts/papers.md` (review_count + the enrichment net_votes/voters notes gained the accepted-claimer exclusion) and `api-contracts/reviews.md` (`GET /api/reviews/:author/:permlink` now 404s a credited-claimer self-review, previously 200). The vote-resolution 2x2-parity /ce-compound lesson is already captured in `vote-resolution-native-revote-2x2-gate-2026-06-09.md`.

## BACKEND-ADMIN-BROADCAST-HELPER-EXTRACTION (archived 2026-06-09) — docblock reword landed; re-review clean ✓

Extracted `broadcastAdminCustomJson` + `AdminKeyNotConfiguredError` into `hive.ts`, adopted at 3 of 5 enumerated sites (`wot.ts` `broadcastWotAccreditation` + `cascadeRevocation`, `claims.ts` admin revoke; `wot.ts` no longer imports dhive). The two `orcid.ts` sites stayed inline by architect decision (their pre-broadcast `PrivateKey.fromString` outside the inner `try` preserves the SEC-002-TOCTOU-LOCK 504+lock-release boundary that folding the async helper would break). Follow-ups were filed: `backend-admin-broadcast-envelope-sweep-remaining-sites` (papers/accreditation/signup-verify + the orcid key-validate shim) and `backend-pin-shared-broadcast-values` (real-envelope test gap). Round-1 `/ce-code-review` held 1 item — the `AdminKeyNotConfiguredError` docblock overclaimed a per-caller mapping no current path exercises. Fixed in commit `eb2eef9e`: reworded to a forward-looking-safety-net description (every current adopter pre-checks `config.pevoAdminPostingKey` and short-circuits before the helper, so the throw is unreachable today; a future non-pre-guarded adopter surfaces it). Re-review `/ce-code-review` (correctness/security on Opus; testing/maintainability/project-standards/learnings on Sonnet; ce-agent-native skipped per PEvO): clean. The architect independently verified the load-bearing claim against all call sites — `broadcastWotAccreditation`, `cascadeRevocation`, `revokeVoucheeIfBelowThreshold`, and `claims.ts` admin revoke each pre-guard `config.pevoAdminPostingKey` and short-circuit, so `AdminKeyNotConfiguredError` is genuinely unreachable on every present path (the guard is pinned by the sibling pin task's real-helper test). One P3 cosmetic nit dismissed at triage: the parenthetical says "the WoT paths return a `skipped` result," but `cascadeRevocation` returns `[]` (2 of 3 paths return `skipped`); "skipped result" reasonably generalizes an empty-list skip and the load-bearing unreachability claim is correct.

## BACKEND-REPUTATION-CLAIMS-CTE-DEDUP (archived 2026-06-09) — single-source the claim-resolution CTE; round-2 clean ✓

Extracted the ~100 lines of `claim_events`+`accepted_claims` resolution that `reputation.ts` inlined as a verbatim copy of `authorshipClaimsCteBody`, so the daily reputation cycle and the read surfaces now compose ONE shared builder (`AuthorshipClaimsScope` gained a `{claimers: string[]}` variant; the cycle projects `accepted_claims` as `status='accepted'`). A real-Postgres byte-identical equivalence test (frozen pre-refactor inline copy vs new builder) pins that the accepted `(claimer, paper_author, paper_permlink)` set is preserved. Round-1 `/ce-code-review` held 3 items, all landed round-2 (commit 9a3b2bd4): (1) equivalence scenario S6 (a stranger, not paper_author/bridge/admin/claimer, forges a `revoke_authorship` against an accepted hive-match claim; the revoke SIGNER gate rejects it so the claim stays accepted, differentially exercising the one gate the byte-identical proof exists to protect); (2) the three `authorshipClaimsCteBody` comment blocks reworded from "Mirrors reputation.ts accepted_claims" to single-source-of-truth language (the dedup deleted that inline copy); (3) the task-slug parenthetical dropped from the equivalence test header. Re-review `/ce-code-review` (11 personas; correctness/security/adversarial on Opus): clean. The architect independently verified S6 isolates the signer gate (mallory's revoke passes every condition except `rv.approver IN (...)`, block 205 > claim block 105) and that the revoke/approve signer gates and the list-final gate are byte-unchanged (only comments moved); the claimer-filter push into the `claim_events` scan is a net perf win. The vote-resolution 2x2-parity learning surfaced in the sibling display review is genuinely new and deferred to that task's clean archive via /ce-compound.

## BACKEND-NOTIFICATION-ARM-CLAIM-REVIEW-PARITY (archived 2026-06-09) — new_review intentionally fires for a credited-claimer self-review; architect-ratified, clean ✓

Cross-surface parity follow-up from the display-exclusion task: the display review aggregates (`excludeClaimedSelfWhere`) and the reputation score (the cycle's `accepted_claims NOT EXISTS` gate) exclude a credited authorship claimer's self-review, but the `new_review` notification arm was never examined. DECIDED and architect-RATIFIED: leave the arm UNGATED for the credited-claimer case and document the rationale in-code (commit 57914b8f, comment-only). Reasoning (anchored on behavioral semantics): a notification confers no credit and no display weight, so the self-dealing-inflation risk the display/score exclusions close does not transfer; the arm reports raw review-shaped activity on the recipient's paper, and gating would compose a recipient-scoped accepted-claims set into the multi-CTE notification query for a rare case with zero integrity benefit. `/ce-code-review` (11 personas; security on Opus): clean. Security confirmed NO new abuse/leak vector (a credited claimer is a strict subset of the accredited accounts that can already fire `new_review`; `DISTINCT ON` earliest-wins dedups edit-storms; `co.author != $1` drops the recipient's own self-review; the payload exposes only the claimer's already-public on-chain review op). The `new_review`-vs-display divergence is now recorded in `api-contracts/notifications.md` Notes so integrators don't assume notification↔display parity.

## UI-WOT-RETRACT-OUTCOME-MESSAGING (archived 2026-06-09) — branch retract UI on revocation_outcome instead of always showing success; 3 rounds, clean ✓

`handleRetract` in `vouch-section.js` read only `res.data.revocations` and showed the generic retract-success copy even when the dependent-accreditation cascade fail-closed, so a non-action or failure read as SUCCESS. Now branches on the `revocation_outcome` discriminator (the retract `custom_json` already succeeded above; the outcome describes the cascade): `timeout`/`chain_error` → `step='error'` (red) `wot.retractRevocationDegraded`; `unverified` → `step='success'` (green, benign HAF-ingestion lag) `wot.retractUnverified`; `revoked`/`skipped`/absent → existing success copy with revoked accounts when present (`revocations || []` guards the non-revoked arms). `profile.js` styles `message` binary (error=red, else green). Round-1 (commit cee02ffa) surfaced the discriminator + 3 new `wot.*` keys (en.json + 15 locale stubs, STUBS.md sweep) + 4 handler tests; architect held one P2 — the `unverified` copy contradicted the contract's fail-closed semantics (it told the user a dependent revocation had happened and was merely lagging, the opposite of fail-closed). Round-2 (commit 7b845a98) reworded `retractUnverified` across en.json + the 15 stubs to attribute the on-chain lag to the unconfirmed RETRACTION itself, state dependents were not re-evaluated, and stop asserting the retract done ("Your vouch retraction was submitted but is not yet confirmed on-chain, so dependent accreditations were not re-evaluated. Re-check shortly."), staying green. A coordination hold then landed because the backend collapsed verify+re-evaluation into one HAF read and removed the `query_error` variant (contract now the 5-arm enum `revoked`/`skipped`/`unverified`/`timeout`/`chain_error`, `accreditation.md` updated, commit 607124d8); round-3 (commit 1f6c9315) removed the dead `query_error` handler branch, the orphaned `wot.retractQueryError` key across all 16 locales + its 15 STUBS lines, and the fabricated-outcome unit test, with the read-failure cause now folding into the `unverified` arm. Re-review `/ce-code-review` over commits 7b845a98 + 1f6c9315 (6 personas: correctness on Opus; testing/maintainability/project-standards/api-contract/learnings on Sonnet; ce-agent-native skipped per PEvO): branch verified exhaustive over the 5-arm enum + the absent/pre-discriminator backward-compat case, no arm mis-routed after the removal, reworded copy contract-faithful, `query_error`/`retractQueryError` fully purged from src/locales/STUBS/tests, i18n parity holds across all 16 locales, copy emdash-free, comment anchors clean. One P2 (project-standards): the re-stubbed `retractUnverified` got no STUBS.md `### Updated` companion — DISMISSED at triage: the key was born as a stub this sprint and never translated, so the `Updated` rationale (warn of misleading prior translation memory) is moot, and adding it would double-list the key (30 lines / 15 locales) and break the per-key single-source-of-truth grep invariant; `### Added` re-stub-in-place is the correct classification for a never-translated, still-pending key. Approach matched three documented conventions: `test-fabricated-error-shape-masks-dead-branch` (removing the fabricated-outcome test is the prescribed remediation, not a coverage regression), `contract-field-removal-sweep-consumers-and-fixtures`, and the safe textual-`fs` locale edit per the Edit/Write unicode-escape-corruption hazard. Origin: surfaced by the wot-retract-cascaderevocation re-review (api-contract finding); contract authority `api-contracts/accreditation.md`.

## ARCHITECT-E2E-STUB-ORCID-OAUTH-PROVIDER (archived 2026-06-09) — stub ORCID OAuth token endpoint as an E2E compose sidecar; review clean ✓

Stood up `orcid-stub` (node:20-alpine, ~20-line inline POST /oauth/token server) in `docker-compose.test.override.yml` so the REAL backend `/api/orcid/callback` completes a genuine OAuth token exchange in E2E without reaching live ORCID. User-confirmed decisions: scope = fresh_auth/set_password only; hand-written Node stub (no off-the-shelf image); backend-only-real fidelity (Playwright fulfils the browser authorize hop in-page). The stub reflects the submitted form `code` back as the response `orcid` field so a spec controls the per-run bound iD (seeds accounts.orcid to match, forced unique to survive retries); no host port published (`expose` only). Backend handoff resolved with NO code change — `config.orcidBaseUrl`/`orcidClientId`/`orcidClientSecret` already env-overridable; the hardcoded pub.orcid.org works URL is reached only by signup/accredit (out of scope). `/ce-code-review` (8 personas: correctness/security/adversarial on Opus; testing/maintainability/project-standards/reliability/learnings on Sonnet; ce-agent-native skipped per PEvO): clean on security/correctness/standards. Testing's "P1: reflected code must be ORCID_RE-format" was verified DOWN to advisory — the consumer contract already lived in `ui-orcid-stub-real-roundtrip-unfixme` and the seeded TEST_ORCID is already 4-4-4-4 format. Two P3 in-place fixes landed on the override: a `req.on('error')` no-op (a client RST mid-body can't crash the stub into a restart loop) and an inline comment pinning the ORCID-iD-format code contract. Negative-path coverage for §6.5 invariant #2 (seed orcid=A, drive code=B, assert 403) filed as `ui-orcid-factor-negative-path-e2e`; spec un-fixme work tracked in `ui-orcid-stub-real-roundtrip-unfixme`. Design validated by the `e2e-external-provider-stub-backend-only-real-fidelity` learning.

## BACKEND-WOT-RETRACT-POLL-RECOUNT-SINGLE-READ (archived 2026-06-09) — collapse retract verify + recount into one VouchStatus snapshot; core clean ✓ (test/type polish → follow-up)

Closed the narrow sticky-revoke race where POST /api/wot/retract verified the retraction (pollForRetraction) and recounted the vouchee's accredited vouchers in two separate HAF reads — a fresh vouch landing between them could straddle HAF's ~3s ingestion lag and revoke an at-threshold account. getVouchStatus now carries the account's own `accreditation_method` via one combined read (json_agg of accredited vouchers + a self_method scalar subquery shaped as a single-group aggregate so the method survives the zero-vouchers case); `shouldRevokeOnRetract(status) = method==='wot' && !eligible`; `revokeVoucheeIfBelowThreshold(status)` decides from that one snapshot instead of issuing a second discovery query. The `query_error` VoucheeRevocationOutcome variant was removed — a re-evaluation read failure now fails closed to the existing `unverified` arm (commit 0c2e624c). `/ce-code-review` (11 personas; Opus correctness/security/adversarial): core verified CORRECT — single MVCC snapshot, griefing vector not reopened, fail-closed sound, net HAF-read win; `active_accreditations` is one-row-per-account (ROW_NUMBER rn=1) so the self_method scalar subquery cannot throw, and the INNER-join json_agg cannot inflate vouches.length. query_error→unverified collapse RATIFIED by the architect; contract rewritten (commit 607124d8); frontend dead-branch + the now-overloaded `unverified` messaging routed to `ui-wot-retract-outcome-messaging`. Test/type polish (the no-second-read test was vacuous because it used an at-threshold status; no real-pool FROM-redirect test pins the combined SELECT; `accreditation_method` should be a literal union) routed to follow-up `backend-wot-retract-test-and-type-polish`. Coupled with the cascade task below (same batch).

## BACKEND-WOT-RETRACT-CASCADEREVOCATION-WRONG-ACCOUNT (archived 2026-06-09) — re-evaluate the vouchee not the voucher on /retract; round-4 docblock fixed, clean ✓

P0 retract-semantics fix: /api/wot/retract called cascadeRevocation(voucher), which re-evaluated the voucher's still-active vouchees and never the one account that lost a vouch (and could cascade unrelated vouchees). Replaced with a vouchee-targeted check (commit 6e1b61b3). Four review rounds hardened it: round-3 closed an authz/griefing hole (an accredited voucher could revoke a victim by POSTing a retraction they never broadcast) via pollForRetraction on-chain verification + a 403 accreditation gate + a 422 self-target guard + a per-account wotWriteLimiter + an exhaustive assertNever switch + typed queries + a shared buildRevocationPayload (commit 36e9f977); the recount now excludes nobody because the poll pre-verifies the retracting edge is gone. Round-4 (final) hold was a single docblock-accuracy item — `cascadeDiscoverySelect`'s docblock no longer claims it "mirrors" the retract recount; it names the shared LEFT-join/NULL-skip structure AND the one-clause FILTER divergence, anchored on stable symbols (commit 54d503ab). `/ce-code-review` re-review: clean. Dismissed at triage: re-vouch-between-poll-and-recount TOCTOU (was routed to the now-also-archived poll-recount task), signer dropped from the revoke log (logging-minimal stance), two preemptive test-hardening gaps.

## UI-ORCID-OPEN-REDIRECT-GUARD-SWEEP (archived 2026-06-09) — open-redirect host guard at the 4 remaining ORCID start sites; clean ✓

Applied the shared `ORCID_REDIRECT_HOSTS` host-allowlist guard (`new URL(data.redirect_url).hostname` checked before `window.location.href`) at the four ORCID start handlers that navigated to the backend-supplied authorize URL with no check — `login.js handleOrcidLogin`, `recover.js handleOrcidVerify`, `signup.js handleOrcidVerify` + `handleOrcidSignup` (commit `f669035d`) — mirroring the existing `settings.js handleOrcidLink` inline shape (throw `'Invalid ORCID redirect URL'` routed through each handler's pre-existing sanitized catch, no new copy), and `Object.freeze`'d the exported allowlist with its docblock corrected to a now-true uniform-coverage claim (8 redirect sites, 8 host checks). Completes the semantic-sibling sweep that `ui-orcid-redirect-host-allowlist-sweep` spawned (the literal-scoped sweep missed these four). `/ce-code-review` (7 personas: correctness/security/adversarial on Opus; testing/maintainability/project-standards/learnings on Sonnet; ce-agent-native skipped per PEvO): zero code findings — security + adversarial empirically refuted userinfo (`orcid.org@evil.com`), subdomain (`evil.orcid.org`), trailing-dot, mixed-case, and `javascript:`/`data:` bypasses (all reject), and confirmed `Object.freeze` is honest consumer-mutation defense, not an XSS boundary. Per-handler rejection unit tests added. One cross-corroborated item dismissed (preemptive hardening per the dismiss-preemptive-test-hardening stance): no deceptive-host test fixtures pin the exact-`.hostname`-match invariant against a future `endsWith`/substring refactor — the guard is correct today and the failure mode needs a future bad refactor. Pre-existing/out-of-scope (not introduced): `recover.js handleOrcidVerify` lacks a `_mounted` teardown guard; two pre-existing `FE-ERR-...`/`UI-TEARDOWN-...` slug comments in sibling test files.

## UI-REAUTH-MODAL-GLOBAL-UPLOAD-GATE (archived 2026-06-09) — cross-surface upload reauth prompt serialization; round-2 clean ✓

Serialized the light-account upload re-auth password prompt across ALL upload surfaces so a concurrent editor inline-image upload and a publish/edit supplementary batch can no longer collide on the singleton `reauthModal` (the loser previously hit refuse-while-open → `UPLOAD_CANCELLED` and silently dropped its upload). Chose option 2 (a shared upload-layer gate, `reauthModal` contract untouched): a module-level `promptChain` + `withPromptLock(requestPrompt)` in `lib/ipfs-upload.js` queues each caller behind the in-flight prompt and advances on settle (resolve OR reject) so a cancelled/failed prompt never wedges the next waiter; `ensureCredential` wraps its `reauthModal.request()`, so every surface routing through it is covered with no edits to editor/publish/edit. Round-1 (commit `8367faed`) held 3 items; round-2 (commit `a2542dd3`) landed all: (1) a per-session `disposed` flag checked inside the queued lambda so a session abandoned WHILE queued resolves to null instead of opening the modal and routing a typed password into a discarded session (`uploadOnce` clears it so dispose-then-reuse still prompts); (2) an exported test-only `resetPromptChain()` isolation seam called in `beforeEach`; (3) four guard tests (cancelled-advances, reject-advances, 3-concurrent-serialize, disposed-while-queued). Re-review `/ce-code-review` (correctness/security/adversarial on Opus; testing/maintainability/project-standards/learnings on Sonnet): zero findings — adversarial mutation-tested the disposed guard (removal fails the test) and confirmed no production unhandled-rejection leak; security confirmed the synchronous `disposed ? null : request()` check closes the password-into-dead-session vector with no TOCTOU window. Residuals all P3 and unreachable today (callers dispose only in `finally` after the awaited upload settles): the open-prompt-then-dispose sub-case is unhandled (a one-line post-await `if (disposed) throw UPLOAD_CANCELLED` would close it if a future caller disposes mid-open-prompt); `resetPromptChain` export hardening; `disposed`-vs-revivable naming. Origin: the `ui-ipfs-upload-token-roundtrip` cross-surface follow-up.

## UI-SETTINGS-ORCID-FACTOR-E2E (archived 2026-06-09) — ORCID-factor set_password E2E (frontend dispatch + cache); re-review clean ✓

Added `settings-orcid-factor.spec.js` driving the ORCID-factor settings critical-action round-trip end-to-end against the test-mode stack: a passwordless State-C account's `set_password` fires `POST /api/orcid/start {mode:'fresh_auth',action:'set_password'}`, and the REAL `orcid-callback.js _handleFreshAuth` dispatch caches the proof under the exact `(set_password, <username>, '')` triple which the re-submit consumes — the seam the unit tests mock. The real backend `/orcid/callback` + `/set-password` are network-stubbed (no stub ORCID OAuth provider in the harness; the real-backend proof round-trip is a documented `test.fixme` filed as `architect-e2e-stub-orcid-oauth-provider`, which also unblocks the identical fixmes in the `orcid-link`/`orcid-no-password` specs). Round-1 held 3 items; the re-review (commit `9bb701f9`) landed all: (1) P2 — `seedStateCAccount` omitted `orcid`, seeding `(password_hash NULL, orcid NULL)` which matches NO ARCHITECTURE.md §6.1 state; it now sets a per-run-unique synthetic 16-digit ORCID iD (derived from `(Date.now, retry)`, collision-safe against the partial UNIQUE(orcid) index) making the fixture a genuine State C, so the un-fixme'd companion will not 403 `ORCID_REQUIRED` before the proof gate; (2)+(3) the header task-slug and `STUB_EXPIRES_AT` "parent task pins" anchors re-anchored on stable production symbols (`withSettingsFreshAuth`, `cacheConsentOpProof`/`getCachedConsentOpProof`). Re-review `/ce-code-review` (correctness on Opus; testing/maintainability/project-standards/learnings on Sonnet): zero findings — correctness verified against the real migrations that `upgraded_at` has no DB default (→ NULL) and the partial UNIQUE(orcid) index exists, and that the orcid addition does not regress the 2 real tests (`hasPassword` derives from `password_hash` only). The fix is exactly what the `account-state-fixture-must-satisfy-all-dimensions-2026-06-09` convention (written from this incident) prescribes. Clause-(c) frontend seam satisfied; the real-backend proof lives on the filed infra task.

## BACKEND-CITATION-EXPORT-FORMAT-ESCAPE (archived 2026-06-09) — BibTeX/RIS/APA file-format-injection escaping; round-5 clean ✓

Escaped BibTeX/RIS/APA format metacharacters in the paper-citation export path (`routes/papers.ts` `bibtexEscape`/`risEscape`/`singleLine`; tests in `papers-cite-escape.test.ts`), defeating `@article{` brace-breakout, RIS record-forgery, and one-line-APA fracture. Across five rounds: the BibTeX brace-escape neutralizes the `@`-breakout; a shared `lib/line-terminators.ts` `LINE_TERMINATORS` constant (consumed by both the cite-export and email-digest paths so they cannot drift) flattens a 10-code-point separator class — CR, LF, VT, FF, FS, GS, RS, NEL, LS, PS — with the C0 separators FS/GS/RS added for `splitlines()`-class importers; escape helpers widened to `(s: unknown): string` with non-string coercion; the DOI/co-author keying bug was split to `backend-cite-export-pevo-metadata-key-mismatch`. Round-5 (commit `dfd8b5ae`) landed the final 2 cleanup items: the RIS C0-forgery test now asserts `not.toMatch(ANY_SEP)` (the prior cardinality-only checks held on revert and proved nothing; `ANY_SEP` is built from `String.fromCharCode` literals so it goes RED when FS/GS/RS leave the constant), and the `papers.ts` consumer docblock enumeration was corrected 7-char to 10-char. Architect round-5 `/ce-code-review` (correctness on Opus; testing/project-standards/maintainability/learnings on Sonnet) came back clean — RED-on-revert empirically confirmed, enumeration accurate with no `\s`-membership claim or rot anchor. Non-blocking residuals routed separately: the `line-terminator-flattening-...-2026-06-08` solutions-doc Rule 1 ("only NEL is non-`\s`") is now incomplete for the 10-char set (FS/GS/RS are also non-`\s` in V8) — architect to `/ce-compound-refresh`; the pre-existing FF/LS RIS test shares the just-fixed false-confidence shape (the parametrized per-separator rows already cover revert).

## BACKEND-ANCHOR-ROT-SWEEP-BRIDGE-HEADER (archived 2026-06-09) — cross-file anchor-rot sweep in bridge test docblocks; clean ✓

Swept the 3 remaining cross-file anchor-rot sites in `bridge-haf-lag-locks.test.ts` (commit `81f3950b`): the `orcid.test.ts` line-number anchors and the `(SEC-002-TOCTOU-LOCK)` slug parenthetical in the header "Real-path companion" docblock, plus the `tasks-archive.md` redirect in the `waitForLockAcquired` docblock. All re-anchored on stable describe-block names and behavioral descriptions ("the same-tick SETNX-lock contention `describe` block in `orcid.test.ts`", "its stale-lock-expiry spec"); the setTimeout-stagger rationale was already inline, so the redirect dropped cleanly. Architect `/ce-code-review` (project-standards lead + maintainability + correctness, comment-only scope) came back clean: the widened in-file grep (BACKEND-/BE-/UI-/SEC-/round-/`.ts:N`/`§ N.M`/project_/see-the-task/tasks-archive) returns zero hits, audit-own-replacement is clean, and behavior parity was mechanically confirmed (every changed line is comment or docblock). Out of scope (acknowledged): `orcid.test.ts`'s own `describe` title still carries the `SEC-002-TOCTOU-LOCK` slug — adjacent rot in that file, not this one.

## BACKEND-NOTIFICATIONS-CACHE-KEY-SINCE-BLOCK-MISS (archived 2026-06-09) — poll cache-sharing; round-2 resolved, parked residual now owned by route redesign ✓

Dropped `since_block` from the notifications cache key (`notifications:${account}:${limit}`) so every SPA poll within the TTL shares one HAF computation instead of missing on each advanced cursor (block-watcher `clearVolatile` also flushed ~3s); the batch is computed against a fixed window floor (`chain head - 100k`, genesis-clamped, falling back to the genesis floor pre-first-tick), cached `stable: true` (survives `clearVolatile`) at a 60s TTL, with the poll's `since_block` re-applied in-app via `applySinceBlockFilter`. Round-2 `/ce-code-review` had held 3 items — window-batch starvation (decoupled the internal fetch cap `NOTIFICATION_WINDOW_FETCH_CAP=1000` from the response `limit`), `has_more` recomputation over the capped batch (`filtered.length > events.length || batch.has_more`, no longer forced false on cursor removal), and cache-behavior pins (a call-count canary + a `clearVolatile`-survival canary in the new `notifications-window-cursor.test.ts`) — all landed (commit `bed8734a`) and verified. PARKED rather than archived pending the cursor-ordering redesign; the route-side >CAP window-starvation residual it flagged is now owned and fixed by `backend-notifications-route-newest-first-whole-block` (newest-first fetch + whole-block delivery on the shared `fetchNotificationsFromHaf`), so the residual's tracking is sufficient and this archives as round-2-resolved. Accepted (not bugs): clients offline past the ~100k-block (~3.5-day) window lose the gap on the bell feed (digest covers it); accounts with no in-window events park `latest_block = since_block`. The minor PARKED nits (carve-out clause placement, the `>= 500` loose cap assertion, the `as number` param-index cast, negative-caching a HAF timeout) were folded into the route-newest-first task's hold cycle.

## BACKEND-NOTIFICATIONS-ROUTE-COMMENT-STALE-REWIND (archived 2026-06-09) — superseded by the cursor redesign ✓

P3 stale-docblock task: `applySinceBlockFilter`'s docblock described the old cursor-advance rule before the SPA boundary-rewind fix, then was updated (commit `577bb263`) to describe the `has_more`-driven rewind to `latest_block - 1`. Superseded before archive by the notifications cursor redesign (architect decision `architect-notifications-block-granular-cursor-stall`, unified scope): the rewind is removed entirely — the route now fetches newest-first with whole-block delivery and the client always advances to `latest_block` (no rewind). `backend-notifications-route-newest-first-whole-block` rewrote the docblock to the newest-first / no-rewind contract (landed in commit `e3336dff`, verified at re-review against the live `applySinceBlockFilter`), and `api-contracts/notifications.md` was updated 2026-06-09 to match. The P3 comment-consistency concern is fully absorbed by the route task's docblock rewrite; the interim working-tree edit it made was correct for the pre-redesign code and is now overwritten. No separate change remains. Archived as superseded (not on the now-false "comment matches the contract doc" criterion).

## BACKEND-BROADCAST-OPTS-DERIVE-FROM-ROUTEFLAVOR (archived 2026-06-09) — derive 3 fields from routeFlavor; review clean ✓

Removed `evidenceSuffix` / `routeLabel` / `recoveryHint` from `BroadcastAccreditationOpts` and derived all three from the `routeFlavor: 'confirm' | 'link'` discriminator via a module-level `ROUTE_FLAVOR_DERIVATION` lookup read once inside `broadcastAccreditationAndSeed` (`routes/signup-verify.ts`), so a mismatched (routeFlavor, suffix) pair (which would silently produce a wrong on-chain `evidence_hash`) is unrepresentable; mirrors the `postBroadcastSuccessCopy` derive-from-routeFlavor precedent, no flag-arg added. `/ce-code-review` (commit `00ca9510`, 9 personas; correctness/security/adversarial on Opus) confirmed all 6 map values byte-identical (sha256-diffed) to the removed inline literals incl. the non-obvious `confirm → 'signup'` mapping; unrepresentability holds (module-private, both call sites pass hardcoded literals, `tsc` clean); no live defect. One finding — the per-flavor `evidence_hash` value is unpinned (every broadcast test `mockReject`s before the hash branch, so a future suffix edit stays green) — folded into follow-up `backend-pin-shared-broadcast-values`. Pre-existing 502 `recoveryHint` body-text pin gap noted, non-blocking.

## UI-IPFS-UPLOAD-TOKEN-ROUNDTRIP (archived 2026-06-09) — SPA two-step upload; 3 review rounds; clean ✓

Adopted the two-step IPFS upload (`POST /api/ipfs/upload-token` pre-flight then `POST /api/ipfs/upload` with `X-Upload-Token`) in the SPA after the backend bound uploads to a single-use, fresh-auth-gated token. New per-file HTTP (`uploadFileToIpfs` / `mintIpfsUploadProof` in `api.js`), batch orchestration (`lib/ipfs-upload.js` `createUploadSession` / `uploadFile` / `describeUploadError`), and a promise-based password reauth modal (`components/reauth-modal.js`); publish/edit wrap the upload region in one session (`dispose()` in `finally`), editor inline-image uses single-shot upload with a sequential `_imageUploadQueue` / `_drainImageUploadQueue` re-entrancy guard. Keychain self-custody signs the upload-token descriptor (binds the SHA-256, one prompt/file, JWT on the upload step); light+password mints a per-file `ipfs_upload`-targeted fresh-auth proof via `POST /api/custody/fresh-auth`, one password prompt/batch held in the session closure (`repromptUsed` bounds re-prompt to once/session); passwordless State C is blocked client-side with a "set a password" prompt (a `File` cannot survive the ORCID full-page redirect) — an approved SPA carve-out recorded in ARCHITECTURE.md §6.4 + `api-contracts/ipfs.md`. Round-1 `/ce-code-review` held 5 items (edit-path page coverage + dead `uploadToIpfs` mock; multi-image upload cancelling all-but-first on the shared reauth modal; `repromptUsed` bound on the mint re-prompt; `fetchEmailStatus` failure hard-blocking a valid password-holder; a coordination-path docblock). Round-2 verified all 5 and held 3 missing behavior tests (editor queue serialization, `repromptUsed` bound, the `ensureCredential` transient-status fallthrough) — each a genuine mutation-killer built on the real `ApiRequestError` shape. Round-3 held one P3 (a `Fix 4:` ordinal prefix on a test comment) → dropped at commit `e859beaf`, behavioral anchor retained; re-review clean (5 personas, zero findings). §6.5 invariant #1 holds throughout (the JWT upload path always carries the `ipfs_upload` proof; no JWT-only reach to the token). Cross-surface reauth-modal contention (an editor inline-image drain racing a publish/edit batch) is pre-existing and filed as `ui-reauth-modal-global-upload-gate`. E2E not required for archive (unit suite + edit-path page coverage sufficient).

## BACKEND-REVOKE-AUTHORSHIP-SIGNER-GATE (archived 2026-06-09) — P0 closed; round-1 comment-only hold landed; re-review clean ✓

Closed a P0 targeted reputation-denial vector: the revoke arm of `claim_events` / `accepted_claims` (`reputation.ts`) and `authorshipClaimsCteBody` (`hafsql.ts`) filtered only on `custom_id` + `action`, so any Hive account could broadcast a stranger-signed `revoke_authorship` and the `NOT EXISTS` clause treated it as authoritative — silently stripping an accepted claim from both the read surface and cycle credit. Fix (commit `5190927e`) added `rv.approver IN (rv.paper_author, $bridge, $admin, rv.claimer)` at both sites (the four authorities per `hive-schemas.md` § 2.11), `approver = required_posting_auths ->> 0` being the chain-verified signer; the gate fails closed on NULL/malformed payloads. First-review `/ce-code-review` verified the gate CORRECT and COMPLETE at both surfaces (correctness/security/adversarial on Opus; forged-revoke / false-positive-suppression / cross-surface-divergence / `->> 0`-element attacks all refuted; no second ungated revoke-resolution path — the notification arm carries its own § 2.11 gate). Round-1 held one P3 comment-only item: the cycle SQL-shape canary header overclaimed coverage (implied pinning the literal `$17`/`$21` text catches a param-position insertion ahead of `$17`; it does not — an earlier bind shifts the binding while the SQL text still reads `$17, $21`). Re-review on fix commit `8bfb0acb` (4-persona: correctness on Opus; testing + maintainability + project-standards on Sonnet) — ZERO findings; correctness independently confirmed against `reputation.ts` that the placeholders are hand-written static text and the param array binds bridge@17 / admin@21, so the reworded header is accurate. Regex assertion unchanged (not weakened); the deferral of a structural param-position test is described behaviorally (no slug). The cycle/read-surface positional-test gap is deferred to the planned merge of the two mirrored claim-resolution surfaces. Sibling cluster: weights-signer-gate, claim-approve-revoke-correlation, and the still-open claimer-binding gap on the credit-op fresh-auth gate (held `backend-authorship-credit-ops-fresh-auth` item 1 — the freshness gate, unlike this signer gate, does not yet bind which `claimer` a permitted signer revokes).

## BACKEND-BROADCASTWOTACCREDITATION-VOUCH-STATUS-BUST-AND-POLL (archived 2026-06-09) — P3 cleanup re-review clean ✓

Five held cleanup items on the `/api/wot/vouch` bust-and-poll fix landed (commit `7bdea822`) and re-reviewed clean: a shared `vouchStatusCacheKey(vouchee)` builder now spells the cache key once for the `getVouchStatus` read and both bust sites (vouch-poll loop + retract handler), so a divergent literal can no longer silently no-op the invalidation, with an INDEPENDENT literal value-pin kept in the test so the shared builder cannot defeat the value check (per the dedup-shared-constant convention); `wot-vouch-poll.test.ts` pins bust-strictly-before-read per iteration via `mock.invocationCallOrder` (correctness empirically confirmed a read-then-bust swap fails red); `VOUCH_STATUS_FIXTURE` made self-consistent; `broadcastWotAccreditationMock` typed; the `pollForVouch` timeout-path docblock documents the `clearVolatile` block-tick mitigation and the `capMs`-bounds-sleeps semantics. `/ce-code-review` (correctness on Opus; security/testing/maintainability/project-standards/kieran-typescript/learnings on Sonnet; ce-agent-native skipped per PEvO): all clean. One sub-threshold P3 dismissed (the `vi.fn` generic omits the param type so `mock.calls` args type as `never`; latent, no test reads call args). Learnings flagged the `invocationCallOrder` per-iteration ordering assertion as a novel, undocumented vitest pattern.

## BACKEND-WOT-CASCADE-SINGLE-DISCOVERY-QUERY (archived 2026-06-09) — P1 perf + correctness; final round clean ✓

Collapsed `cascadeRevocation`'s 1+2N per-level HAF round-trips into one discovery query, then (round-2) fixed INNER→LEFT-JOIN cascade-terminal parity, then (round-3, commit `a18373ba`) removed the dead non-budget nested-error branch and its vacuous test. The round-2 hold had asked to pin non-budget nested-error accounting; round-3 re-review established the PREMISE was wrong: a recursive `cascadeRevocation` can throw ONLY `PartialCascadeError` (every other error is normalized to `return []` by its own outer catch; the sole pre-`try` call, `getPool()`, is a memoized cache hit in recursive frames and `new Pool()` does not throw synchronously on valid config). Backend correctly took the REMOVE path: a type-narrow guard `if (!(nestedErr instanceof PartialCascadeError)) throw nestedErr;` (which also does the TS narrowing for the fold) plus a behavioral comment, and deleted the vacuous test (it passed via the recursive swallow, never the branch it claimed to cover, and was green against pre-fix code). Also added a CTE FROM-redirect no-op guard (`expect(redirectedCte).not.toContain(T.customJson)`) so a future alias/whitespace drift fails fast instead of running discovery SQL against live HAF. `/ce-code-review` (correctness + adversarial on Opus; testing/reliability/maintainability/project-standards/kieran-typescript/learnings on Sonnet): all clean; the invariant independently re-proven (adversarial empirically probed `new Pool()` with bad config / `max:0` — no synchronous throw). Residual fragility (a future pre-`try` throw would propagate via the guard and reintroduce a double-count) is documented inline and contingent on a future invariant violation, not a present bug.

## UI-SETTINGS-ACTION-FRESH-AUTH-PROOF-CHALLENGE (archived 2026-06-09) — P1 light-account lockout closed; 3 review rounds; clean ✓

Built one reusable settings-action fresh-auth proof-challenge flow (`lib/settings-fresh-auth.js` `withSettingsFreshAuth`) and wired all three JWT-path critical actions (change-email / set-password / delete-account), closing a confirmed-live light-account lockout (every JWT-path action 401'd `FRESH_AUTH_REQUIRED` because the SPA minted no body proof). Mints via the account's registered factor per ARCHITECTURE.md §6.4 (password → `POST /custody/fresh-auth`; ORCID → `beginSettingsActionOrcidFreshAuth` full-page round-trip), per-action target-bound so a proof for one action cannot replay on another; Keychain path unchanged. Round-1 `/ce-code-review` held 5 items (commit `41509a53`): the dead `err.status===401` retry gate (`ApiRequestError` carries no `status`) → `REMINTABLE_REASONS.includes(err.details?.reason)` with the test de-fabricated to the real error shape; `ORCID_REDIRECT_HOSTS` adopted in the new ORCID entry point; the second wrong-password mint wrapped (`MINT_FAILED`→`freshAuthFailed`); a direct ORCID unit test added; action coverage extended (passwordless `delete_account` ORCID factor, 401/403 on non-`change_email` actions); `usesPasswordFactor` centralizes factor selection so the mint and retry gate cannot drift. Round-2 verified all 5 and held one comment fix; round-3 (commit `27f55b26`) corrected the false clause-c E2E claim in the settings-orcid unit-test header (states honestly the ORCID-factor settings path has no E2E companion yet; no slug cited; `ui-settings-orcid-factor-e2e` tracks the real E2E). §6.5 invariant #1 holds throughout (no critical action reachable JWT-only without a proof). Architect-side: `api-contracts/orcid.md` + `custody.md` drift (`delete_account` enum + stale `change_email` "not live" note) corrected in round-1. Flagged for a separate look (pre-existing, off-diff): `orcid-callback.js` `destroy()` clears `pevo_orcid_return_to` while the fresh_auth flow writes `pevo_fresh_auth_return_to`.

## UI-ORCID-REDIRECT-HOST-ALLOWLIST-SWEEP (archived 2026-06-09) — P3 dedup clean ✓; spawned open-redirect follow-up

`export`ed `ORCID_REDIRECT_HOSTS` (`['orcid.org','sandbox.orcid.org']`) from `lib/fresh-auth.js` and replaced the two remaining inline two-host open-redirect literals — `pages/settings.js` `handleOrcidLink` and `pages/accreditation.js` — with the shared constant (commit `997716db`), closing the `convention-sweep-syntactic-form-misses-semantic-siblings` follow-up from the settings-action re-review. `/ce-code-review` (6 personas): correctness / testing / project-standards clean, behavior identical, both deduped sites already carry non-allowlisted-host rejection tests. The review surfaced a PRE-EXISTING gap beyond this task's scope: four OTHER ORCID flows (`login.js` `handleOrcidLogin`; `recover.js` + `signup.js`×2 `handleOrcidVerify` / `handleOrcidSignup`) navigate to the backend-supplied `data.redirect_url` with NO host guard, and the new docblock overstates ("every redirect-host check uses one allowlist"). Routed to new task `ui-orcid-open-redirect-guard-sweep` (P2) rather than held — the sweep's own acceptance (the two literal sites) was met. Folded into the follow-up: `Object.freeze` the exported allowlist array.

## BACKEND-REPUTATION-WEIGHTS-SIGNER-GATE (archived 2026-06-09) — P0 closed; round-1 hygiene hold landed; re-review clean ✓

Closed a P0 reputation-algorithm takeover: `loadReputationWeights` (`reputation.ts`) selected the latest `update_weights` custom_json gated ONLY on `custom_id` + `action`, with no signer filter, so any Hive account could broadcast `{action:update_weights,weights:{...}}` with `custom_id=<appTag>` and control every reputation weight from the next cache refresh. Fix (commit `09329790`) added `AND cj.required_posting_auths ? $2` bound to `config.hiveAdminAccount` (singular) on BOTH the existence probe and the latest-op read — mirroring the accreditation/WoT/consent gate shape, unforgeable because Hive consensus admits an op only if every named posting-auth actually signed — plus `sanitizeReputationWeights` (exported, pure) accepting only finite numbers and clamping `decay_floor`/`self_citation_discount` to `[0,1]`, `decay_rate`/`decay_grace_months` to `>=0`. The P0 was independently verified CLOSED by correctness+security+adversarial. Round-1 hold (2 P3 hygiene items) re-reviewed on fix commit `8b8cfb44` via 4-persona `/ce-code-review` (correctness on Opus; testing + kieran-typescript + project-standards on Sonnet) — ZERO blocking findings. Item 1: `sanitizeReputationWeights` now guards `|| Array.isArray(raw)` so an array short-circuits to `{}` before the `as Record` cast, with a matching unit. Item 2: the `decayMultiplierSql` docblock reflow is comment-only (SQL byte-identical), anchored on stable symbols. Noted non-blocking: the array-guard unit is structurally non-discriminating (no array index aliases a weight key, so pre/post behavior is identical) — accepted per the dismiss-preemptive-hardening stance. Architect companion actions completed at archive: `reputation-algorithm.md` records the read-side signer-gate invariant, and `/ce-compound` captured the convention as a read-side corollary to Rule 5 in `hive-primitive-aware-design-rules-for-pevo-custom-json-ops` (a custom_json read gated on custom_id+action alone is an auth bypass; sibling cluster: active-vouches-signer-gate, revoke-authorship-signer-gate).

## BACKEND-NOTIFICATIONS-CLAIM-APPROVE-REVOKE-CORRELATION (archived 2026-06-09) — round-1 hold items landed; re-review clean ✓

Closed the residual claim-correlation gap on notification arms 8/9 (`claim_approved`/`claim_revoked`): the signer gate alone (signer = real native post author) left the recipient (`$1` = claimer) unbound, so any accredited owner could self-sign an approve/revoke of their OWN real paper naming an arbitrary victim as `claimer` and spam the victim with a notification + digest email. Fix (commit `5dc36c0a`) added a correlated EXISTS requiring a real `claim_authorship` op by `$1` at the same `(paper_author, paper_permlink)`, plus per-pair `DISTINCT ON` earliest-wins dedup; the `hafsql.ts authorshipClaimsCteBody` cross-surface instance was audited CONFIRMED-SAFE (its credit subject is bound to a real `claims_base` row the list-final EXISTS validates). Round-1 hold (2 items) re-reviewed on fix commit `951c3096` via 3-persona `/ce-code-review` (correctness on Opus; testing + project-standards on Sonnet) — ZERO findings. Item 1: the `§2.11` section-number citation on the revoke signer-gate comment is converted to a schema-NAME anchor with no new rot class (convention-enforcing-fix self-audit passed). Item 2: the new arm-9 revoke victim canary is non-vacuous — the signer is the real post author (bob), so the signer gate's author-EXISTS branch passes for BOTH the non-claiming victim (eve→0) and the real claimer (alice→1), isolating the correlation EXISTS as the sole discriminator; it goes RED on a `= $1`→`!= $1` inversion the prior source-shape string pin could not catch. Plus an arm-9 dedup canary. 22/22 green against real Postgres. `api-contracts/notifications.md` firing-condition + dedup docs were updated architect-side alongside the round-1 hold. Recorded accepted tradeoff (do not "fix"): the per-pair dedup is per-poll-snapshot not lifetime — the same window-floor/cursor-ordering family the in-flight cursor redesign reshapes.

## BACKEND-CO-AUTHOR-CLAIM-ZERO-SCORE (archived 2026-06-09) — round-3 hygiene landed; re-review clean ✓

P0 fix for claimed papers scoring zero (the `user_papers` CTE keyed on the claimer while every downstream CTE keyed on the chain author, so votes/reviews never matched and `paper_scores` collapsed to 0). Round-1 landed the chain-identity rekey (a deduped `chain_papers` CTE; `paper_vote_signals` / `paper_resolved_votes` / `paper_reviews` / `paper_scores` rekeyed to the on-chain post identity while credit attribution stays on the claimer via `up.author`) — verified correct, with the floor-sweep param-renumber entanglement (it landed inside commit `1ab97151`) confirmed non-corrupting. Round-2 (commit `28dfffaa`) closed the two self-dealing holes the rekey opened: the list-final approval slot gate (`author_index IS NOT NULL` + `jsonb_typeof(authors[idx]) = 'object'`, mirrored across `reputation.ts` `accepted_claims` and `hafsql.ts` `authorshipClaimsCteBody`) and the claimer self-vote / self-review exclusion (`accepted_claims NOT EXISTS` in both `paper_resolved_votes` and `paper_reviews`, catching ORCID-/name-only-slot claimers who are absent from `authors[].hive`) — both verified correct. Round-3 (commit `ddf0bffe`) was comment/docstring/test-pin hygiene only (H1–H5: behavioral test labels replacing hold-item anchors; corrected the frank/grace/ivan mutation-kill narrative; refreshed the `excludeSelfReviewWhere` docblock; removed a stale "genesis floor 0" harness docstring; tightened the self-vote/self-review source pins to the full correlated predicate keyed on `plv.voter` / `c.author`). The round-3 `/ce-code-review` (correctness on Opus; testing/maintainability/project-standards/learnings on Sonnet; ce-agent-native skipped per PEvO) returned ZERO findings: every comment claim verified factually accurate against the SQL, no round-N/slug/SHA anchor reintroduced, and H5's tighter pins genuinely catch a column-alias swap the old shared substring missed. Follow-ups tracked separately: the display-callsite self-review exclusion (`backend-claimer-self-review-display-callsite-exclusion`) and pre-existing round-1-hold anchor rot in the `excludeSelfReviewWhere` docblock tail (`backend-haf-query-comment-anchor-sweep`).

## BACKEND-WINDOW-CTE-DETERMINISTIC-TIEBREAKER (archived 2026-06-09) — held items landed; missed siblings + canary gap routed to follow-up ✓

`/ce-code-review` (correctness/security/adversarial on Opus; performance/testing/maintainability/project-standards/learnings on Sonnet; ce-agent-native skipped per PEvO) on the round-3 fix commit `bee6083f`. All four held items VERIFIED: `getAccreditedSet` (accreditation.ts) now resolves a same-block accredit/revoke IDENTICALLY to `activeAccreditationsCteBody` — correctness + security confirmed the full predicate matches (custom_id, action set, AND the `required_posting_auths ?| accreditationAuthorities` authority gate), not merely the ORDER BY tiebreaker, so the gate and the reputation/display CTE can no longer disagree; the two accreditations-route sites carry `, cj.id DESC`; the shape canary now also asserts `activeVouchesCteBody`; the three reputation union-CTE comments document the GLOBAL `haf_operations`-PK invariant (cross-arm `op_id DESC` is genuine latest-wins; do not namespace op_id per arm). No injection; no plan regression (the secondary key only orders within already-tiny per-partition sets; HAF indexes are fixed external infra regardless). Two non-blocking findings were routed to the new `backend-accreditation-state-read-tiebreaker-sweep` follow-up rather than held: (1) the semantic `block_num DESC` sweep found four MORE accreditation-state reads of the identical `action IN ('accredit','revoke') ORDER BY block_num DESC LIMIT 1` shape still missing the tiebreaker — `orcid.ts` findAccreditedAccountWithOrcid (x2) + getExistingAccreditation, and `profile.ts`'s profile-metadata read (architect-verified; documented as a family in `accreditation-state-read-latest-action-wins`); (2) the inlined sites this commit patched are not shape-canary-reachable, and the in-test coverage note both omits them and overstates the reputation-CTE lifecycle coverage (the lifecycle suite asserts run-to-run idempotency on the live corpus but never seeds a same-block native-vote/revote collision). Dismissed: canary exact-string brittleness (preemptive hardening). Already-accepted residuals (wot.ts update_params, reputation.ts update_weights, idempotency.ts sha256-key, papers.ts batchResolveVotes display-surface) not re-litigated. The convention doc's Rule 2 `trx_in_block`→`id` correction was already landed (last_refreshed 2026-06-09), so no architect doc edit was outstanding.

## BACKEND-COMMENTS-TREE-CACHE-CANONICAL-PER-PAPER (archived 2026-06-09) — round-2 held items landed (premise-corrected); re-review clean ✓

`/ce-code-review` (correctness on Opus; kieran-typescript/testing/reliability/maintainability/project-standards/learnings on Sonnet; ce-agent-native skipped per PEvO) on commits `12c5a25b` + `33b12cb1`. Both round-2 held items landed. Item 2 (RawCommentRow.created type-soundness) involved a PREMISE CORRECTION the implementer correctly made: the architect hold wrongly claimed node-pg returns `timestamptz` as a JS Date and prescribed `.toISOString()`, which threw at runtime (HafSQL surfaces `comments.created` as an ISO-8601 STRING) and 500'd the endpoint — caught by the real-HAF identical-sets spec, then corrected to `created: string | Date` + `instanceof Date ? toISOString() : r.created`. Correctness + reliability confirmed the throw path is fully closed and the net behavior is byte-identical to baseline for the string input; the null branch is type-unsound on paper but unreachable (Hive comment `created` is NOT NULL on-chain), dismissed per no-defensive-coding-for-impossible-values. Item 1 (self-validating ordering spec) landed: the silent `<2 rows continue` became a hard `>=2` assertion and a `sawPrimaryKeyTieGroup` guard keeps the `desc===asc reversed` tiebreaker check from going vacuous. Triage: the `sawPrimaryKeyTieGroup` live-corpus coupling (votes-variant 0-vote ties) accepted as a by-design loud-failure tradeoff (the implementer flagged the dependency); the ISO-format wire-pin and a named-normalizer extraction were optional, not taken; the two `backend-papers-filter-accreditation lane 2 canary` task-slug citations a reviewer flagged are PRE-EXISTING (outside this commit's diff), noted for a separate comment-anchor cleanup, not blocking this archive. Learning ties to `hold-block-must-not-contradict-convention-docs` (verify a HAF-row type empirically before prescribing it) and `pg-bigint-default-stringification`.

## UI-NOTIFICATIONS-DROP-REWIND-AND-BLOCK-CAP (archived 2026-06-09) — round-1 hold fixes landed; re-review clean ✓

Completes the notifications cursor-stall fix paired with `backend-notifications-route-newest-first-whole-block`. Dropped the SPA's `latest_block - 1` cursor rewind (the poll loop now advances to `latest_block` unconditionally, safe under the new whole-block-delivery contract) and reconciled the `MAX_EVENTS = 200` cap with whole-block delivery. Round-1 (commit `088890aa`) landed spec items 1-4; the architect held 3 fixes. Round-2 (commit `7aacd479`) landed them: (1) the feed sorts newest-first by `block_num` descending UNCONDITIONALLY (the over-cap-only ternary is gone), so the cap is a pure tail-trim and an oversized whole-block poll can no longer flip the list asc↔desc against the next under-cap poll (also fixes the pre-existing newest-renders-at-the-bottom oddity); (2) the `markAllRead`/`seenBlock` comment now states the honest bounded-feed trade-off (the cap sheds the OLDEST events beyond `MAX_EVENTS`, which leave memory and stop counting toward `unreadCount` regardless of `markAllRead`) and the merge/sort comment stops claiming the single-oversized-block tie case is handled (events sharing one `block_num` are not separable by the `b.block_num - a.block_num` comparator — deferred as a single-instance non-concern per `forward-cursor-feed-newest-first-and-rewind-masks-cap-edge`); (3) tests added a newest-first order assertion to the over-cap test, a new under-cap order test, a `toHaveBeenCalledTimes(1)` tight-loop fence, and restored cross-poll merge+dedup coverage. 5-persona `/ce-code-review` (correctness on Opus; testing/maintainability/project-standards/learnings on Sonnet; ce-agent-native skipped per PEvO) returned clean. The single P2 raised (cross-poll asc↔desc flip untested) was verified a FALSE POSITIVE: the new under-cap test feeds deliberately-scrambled `[10, 30, 20]` and asserts `[30, 20, 10]`, so reverting the unconditional sort reddens it — the mutation IS killed (the reviewer's own per-question analysis agreed). All 3 changed test files green on HEAD. Deploy ordering preserved: ship after/atomically with `backend-notifications-route-newest-first-whole-block` (still pending), never frontend-first; the SPA bundle ships from `backend/public`, so atomic co-deploy is the safety mechanism.

## BACKEND-ACTIVE-VOUCHES-SIGNER-GATE (archived 2026-06-09) — round-1 hold items landed; re-review clean ✓

Re-review of the 2 test-only hold items (commit `033c7a34`) on the P0 `vouch_ranked` / `activeVouchesCteBody` signer gate (`required_posting_auths ? voucher`), which round-1 (commit `42785f7b`) already verified CLOSED and which this commit leaves untouched. 6-persona `/ce-code-review` (correctness/security on Opus; testing/maintainability/project-standards/learnings on Sonnet; ce-agent-native skipped per PEvO) returned ZERO findings. Item 1 (clause-(c) carve-out reword) verified factually accurate against the codebase per the coverage-claim-downgrade-requires-codebase-search convention: `getVouchStatus` is consumed by `GET /api/wot/:username` in `tests/routes/wot.test.ts` against LIVE HAF (no mocked pool), and `broadcastWotAccreditation` is mocked-only (`wot-broadcast-timeout.test.ts`) — the prior overclaim is removed. Item 2 (alias-free FROM redirect `replace(T.customJson, 'synthetic_cj')` + `expect(redirected).not.toContain(T.customJson)` guard) matches the canonical test-haf-sql-selection-redirect-cte-from-synthetic-values convention form; `T.customJson` occurs exactly once in the CTE so the redirect is complete (`FROM synthetic_cj cj`, alias preserved) and the guard fails fast on drift. The regression defense still goes red two ways if the gate is dropped (the `.toEqual([...])` behavioral set + the unchanged SQL-shape canary). Sibling `backend-revoke-authorship-signer-gate` (same review batch) held back to pending for a P3 canary-header reword; its P0 gate verified closed.

## BACKEND-SESSION-INVALIDATION-FAIL-CLOSED (archived 2026-06-09) — held items landed; re-review clean ✓

Re-review of the held-item fix commit `91c4299b` layered on the already-confirmed fail-closed catch→503 (`0d264ff3`). Both architect-held items landed correctly: (1) `details: { retriable: true }` added to the session-invalidation 503 `sendError` so the SPA's `isRetriable503()` (api.js, gates on `details.retriable === true`) auto-retries a transient Postgres outage instead of wedging the user behind a manual reload; the failclosed test asserts `error.details.retriable === true` and still runs the real `verifyHiveSignature` + real crypto (mutation-kill confirmed). (2) Both pre-existing user-facing emdash strings de-emdashed to period-separated form ("Signature already used. Replay rejected.", "Invalid signature. Does not match account posting key."); no test pinned the old text and no new emdash was introduced. 7-persona `/ce-code-review` (correctness/security on Opus; testing/maintainability/project-standards/reliability/learnings on Sonnet; ce-agent-native skipped per PEvO) returned ZERO findings — fail-closed semantics unchanged, comment anchors stable on the `isRetriable503()` symbol, carve-out header already documented.

## BACKEND-VERIFYHIVESIGNATURE-PREEXISTING-REPLAY-REVOCATION-HARDENING (archived 2026-06-09) — 3 edge cases fixed; correct, no blocking defects ✓

First review of commit `9ac96ba2` (architect-resolved to fix all three pre-existing `verifyHiveSignature.ts` edge cases in one pass, built on the already-landed fail-closed catch). 10-persona `/ce-code-review` (correctness/security/adversarial on Opus; testing/reliability/api-contract/maintainability/project-standards/kieran-typescript/learnings on Sonnet; ce-agent-native skipped per PEvO) confirmed all three correct: (1) replay TOCTOU — `isReplaySignature` claims the signature synchronously (check-then-add) on the Redis-down/SETNX-throw fallback before the `await getAccounts`, with the timestamp + structural `Signature.fromString` parse moved ahead so only valid in-window signatures enter the in-memory map; concurrent identical replays collapse to one survivor (mutation-kill verified). (2) same-second revocation — the legitimately-reissued token is spared by `reissuedAt` epoch-ms identity (`recover.ts` /recover + /recover/verify embed the exact `sessions_invalidated_at` ms; middleware revokes `iat <= invalidatedSec && reissuedAt !== invalidatedMs`); round-trip fidelity holds (TIMESTAMPTZ + Node Date is ms-exact, no custom pg type parser) and the full truth table was traced. (3) iat-absent — `typeof payload.iat !== 'number'` → 401 closes the unrevocable-token hole at the single `jwt.verify` site. Zero P0/P1; no auth bypass or revocation hole. Dispositions: the item-2 test mocks the pool (the recover→pg→middleware round-trip is unpinned, clause-(c) companion absent) → follow-up `backend-reissuedat-roundtrip-realpath-test` in `pending/`; the same-second `/reset`→`/login` self-healing relogin edge (the new `<=` revokes a same-integer-second relogin that lacks `reissuedAt`) → accepted + documented in ARCHITECTURE § 6.7 plus a one-line code comment folded into the follow-up. Dismissed: `reissuedAt` lacks a runtime `typeof` guard (safe-direction — a non-number revokes, never admits) and the in-memory replay fallback records pre-crypto signatures (bounded by the TTL sweep, single-instance, correct-direction). Architect documented the `reissuedAt`/iat-required invariants + the round-trip "do not switch to NOW()" note in ARCHITECTURE § 6.7 and added `SESSION_INVALIDATED` to api-contracts/common.md.

## BACKEND-SIGNUP-VERIFY-ACTIVATION-SCAFFOLD-EXTRACTION (archived 2026-06-09) — withSignupActivationLock + broadcastAccreditationAndSeed extracted; byte-parity clean ✓

Pure refactor (commit 5f874215) de-duplicating ~110 lines of post-lock scaffold across the `/confirm` and `/link` signup-finalization handlers into two shared symbols: `withSignupActivationLock(opts, fn)` in `lib/signup-activation-lock.ts` (owns acquire / 409-LOCK_HELD / try-catch(onError)-finally-release; the route body is an `fn(releaseLock)` callback that releases mid-execution at the single-fire critical-section boundary, wrapper's finally re-releases idempotently) and `broadcastAccreditationAndSeed(opts): 'ok'|'handled'` in `routes/signup-verify.ts` (the HAF-probe → admin-signed accredit custom_json → PostBroadcastWriteError cascade → reputation seed, discriminated by `routeFlavor: 'confirm'|'link'`). 10-persona `/ce-code-review` (correctness/security/adversarial on Opus; testing/maintainability/reliability/api-contract/kieran-typescript/project-standards/learnings on Sonnet; ce-agent-native skipped per PEvO) confirmed BYTE-PARITY: probe gate preserved (`/confirm isResume = resumeStuck||resumeChainExists`, `/link isResume: resumeStuck` reproduces old `if(resumeStuck)`), evidence-hash material identical, 409/500/502/504/200 contract byte-identical, no-dangling-JWT short-circuit preserved (`return 'handled'` + caller `if(cascade==='handled') return`), and `release()` verified throw-safe (releaseRedis try/catches evalScript, memoryRelease is sync) so the finally cannot ERR_HTTP_HEADERS_SENT. Zero P0/P1. Dismissed P2/P3 (code correct at both call sites): the `'ok'|'handled'` union lacks compile-time exhaustiveness; the `account` inline structural type drifts from SignupRow/LinkRow; `onError` must-respond/must-not-throw is prose-only (Express-5 + headersSent backstop absorbs a throw). PRE-EXISTING (not introduced): `${account.email}` null→`'null'` in the evidence hash for ORCID-only signups (changing it invalidates on-chain evidence). The derivable-params finding (evidenceSuffix/routeLabel/recoveryHint derivable from routeFlavor → mismatched-triple silent-wrong-hash risk; 2-reviewer corroborated) split to follow-up `backend-broadcast-opts-derive-from-routeflavor` in `pending/`.

---

## BACKEND-SIGNUP-ACTIVATION-UPDATE-FAILURE-INJECTION-COVERAGE (archived 2026-06-09) — end-to-end createClaimedAccount-then-finalize-fail crash transition; production-safe seam; clean ✓

Adds the end-to-end test (commit 9b65bf07) that drives the ACTUAL crash transition the activation-recovery suite previously only pre-seeded: request 1 `/confirm` where createClaimedAccount succeeds then the finalize UPDATE fails (handler 500, row left verify_token-set + posting_key_enc-NULL + chain account materialized), request 2 retry hits `resumeChainExists` and finalizes without re-broadcasting (single claim-token burn pinned by `createClaimedAccount toHaveBeenCalledTimes(1)` across both requests). Failure injected via a new production-side `__test_seams.failNextFinalizeUpdate(authToken)` + `maybeThrowInjectedFinalizeFailure`, placed AFTER createClaimedAccount and BEFORE the finalize UPDATE on the fresh path — gated `!process.env.VITEST && NODE_ENV!=='test'` (De Morgan inverse of drainArgon2Queue's guard) and forbidden from `src/` import by an extended eslint no-restricted-imports rule (mirrors the anonymousReview seam precedent). 9-persona `/ce-code-review` (correctness/security/adversarial on Opus): production-SAFE — security empirically confirmed the eslint rule fires and the Dockerfile pins `NODE_ENV=production`, so the module-level Set is never armed in prod and even an env misconfig fires no throw (fail-closed: a 500, never a more-privileged state); the throw propagates through withSignupActivationLock's catch → onError 500 with the lock released by finally (no leak); one-shot `Set.delete` consumption holds under the activation lock's serialization; all three documented mutation-kills verified valid; carve-out clauses (a)/(b)/(c) compliant (pg pool NOT mocked; real-path companion `signup-verify-concurrent-activation.test.ts` exists). Zero P0/P1/P2. Dismissed P3 advisories: mutation-kill bullet (a) imprecisely calls the shared primary `WHERE verify_token=$1` lookup a dedicated resume re-find (the revert still reddens); module-level Set has no afterEach reset (benign — unique hardcoded token, one-shot consume); env-gate idiom duplicated at 2 sites (extract `isTestProcess()` at a 3rd copy); the seam adds a consumer layer vs the anonymousReview/reputation seam shape (justified — the throw must fire at the real finalize site).

---

## BACKEND-ANCHOR-ROT-SWEEP-SIGNUP-VERIFY (archived 2026-06-09) — round-2 SEC-004-BE title + carve-out clause-(c) holds landed; clean re-review ✓

Comment/label-only sweep of pre-existing anchor rot in signup-verify.ts + 4 test files (round-1 commit 08498d76). The architect round-2 hold (2 items) landed at commit f869e9b6 and was verified clean on re-review (project-standards persona + an independent widened in-file grep): (1) the two `SEC-004-BE:`-prefixed describe titles in signup-verify.test.ts reworded to behavioral titles ("ORCID signup + confirm without password (email+ORCID path, password_hash = NULL)" / "ORCID signup + confirm WITH password (ORCID-match then password-confirm path)") — the slug family is gone, `password_hash` is a real column (stable symbol), and the replacement introduces no new rot anchor; (2) carve-out clause (c) reworded to scope the real-verifyHiveSignature signed-request claim to `/link` only (route registration confirmed: `/link` carries the middleware at its `router.post('/link', …, verifyHiveSignature, …)` registration, `/confirm` does not — its resume-path auth is the in-handler posting-key check), so `/confirm`'s real-auth coverage in this file is the argon2 + pg path of clause (b). Audit-own-replacement clean; the widened-pattern grep (`BACKEND-/BE-/UI-/SEC-/round-N/.ts:NN/§ N.M/project_*/tasks-archive`) returns zero across the touched signup-verify test files. The sibling `backend-anchor-rot-sweep-bridge-header` (bridge-haf-lag-locks.test.ts) stays in `review/` — disjoint file, not part of this code-path group.

---

## BACKEND-CASCADE-PENDING-VOUCHEES-INCLUDE-SLICE (archived 2026-06-09) — pending dedup at both PartialCascadeError throw sites + typed discovery generic; clean re-review ✓

`cascadeRevocation`'s operator-follow-up `pending` array could carry duplicate account names: in a diamond-shaped voucher graph a vouchee reaches `pending` via both `nestedErr.pending` and the parent level's `slice(i+1)` fold, and a broadcast-timeout at index j followed by a nested budget blow at k>j double-adds the rows between. Round-2 fix applied order-preserving `[...new Set(pending)]` immediately before BOTH `PartialCascadeError` throw sites and typed the discovery query `pool.query<{ vouchee: string }>` (dropping three `as string` casts). Re-review (correctness + adversarial on Opus, plus testing/kieran-typescript/maintainability/reliability/project-standards/learnings on Sonnet; ce-agent-native-reviewer skipped per PEvO) found ZERO defects: dedup at both sites is order-preserving (Set iteration = insertion order); `completed` (tx-ids) and `pending` (bare account names) are disjoint namespaces so the intra-`pending` dedup cannot cross-contaminate; `completed ∪ pending` still covers each identified vouchee with no drops; the generic matches the `SELECT vouchee` column and all three casts are gone. One non-blocking P3 nicety left for discretion: the deadline-check throw site's dedup has no independent multiplicity-sensitive test (the diamond case exercises it only as a no-op). Pre-existing comment-anchor rot (`BACKEND-REPUTATION-SSOT round-N` citations in `wot.ts` and the test header) is out of scope here and folded into the standing anchor-rot sweep.

---

## BACKEND-NOTIFICATIONS-CITATION-ARMS-PAPER-EXISTS-GATE (archived 2026-06-09) — citation arms 6a/6b INNER-JOIN paper-existence gate; round-3 test blind spot closed; clean re-review ✓

Citation notification arms 6a/6b matched on broadcaster-controlled `cited_ref.author = $1` with `cited_paper` only LEFT JOINed for title, so an accredited user could broadcast a "paper" stuffing thousands of fake `{author: victim, permlink: 'fake-N'}` citation refs and spam unlimited citation notifications + digest emails. Fix promoted `cited_paper` to INNER JOIN gated by `validPevoPaperWhere(source:'all')` in both arms (6a native: belt-and-suspenders against the fake-permlink vector; 6b bridge: alongside the `user_bridge_papers` ownership JOIN), added `DISTINCT ON` per-(citing-post, cited-paper) earliest-wins dedup so a repeated/edited citations array yields one notification, and restored `COALESCE(cited_paper.title,'')` parity with sibling arms 1a/1b. Round-1 (commits `c345053d`/`5a43c51c`) held three items: residual duplicate-real-permlink spam (closed by DISTINCT ON), missing arm-6b bridge behavioral test, and a dropped-COALESCE split-brain. Round-3 (commit `1c692bf8`, test-only) closed the last hold: the arm-6b bridge test's spam case cited `pevo.bridge/fake` (absent from BOTH `bp` and `cited_paper`), so the `cited_paper` INNER JOIN backstopped the `hit_count=0` assertion and a dropped `bp` ownership JOIN would not have reddened it; the fix seeds a valid bridge paper registered by a DIFFERENT user (present in `cited_paper`, absent from alice's `bp`) and asserts `hit_count=0`, making the `bp` `registered_by=$1` gate load-bearing. Final re-review (7-persona `/ce-code-review` on `1c692bf8`: correctness + security on Opus, testing/maintainability/project-standards/kieran-typescript/learnings on Sonnet): correctness + security + testing all traced that the new assertion reddens IFF the `bp` JOIN is dropped, the reconstruction faithfully mirrors production arm-6b topology, and the dedup-test `DISTINCT ON ... 1` projection cleanup preserves semantics. Only finding was a P3 cosmetic naming nit (`otherUser` fixture variable), dismissed as below-gate. The test reconstructs arm SQL inline as a standalone string; the `notifications-arm-sql-shape` source-shape canary covers production-string drift.

---

## BACKEND-DIGEST-SHARE-LINE-TERMINATORS (archived 2026-06-09) — digest.ts singleLine reuses the shared LINE_TERMINATORS; clean re-review ✓

`digest.ts`'s `singleLine` carried its own line-terminator regex omitting VT/FF, diverging from the cite-export alphabet — the exact drift the shared constant exists to prevent. Fix extracted `LINE_TERMINATORS` to `backend/src/lib/line-terminators.ts`, consumed by both `routes/papers.ts` and `digest.ts`. Round-1 hold (commit 428ef752) corrected a wrong `\s`-membership docblock claim and widened the class to the C0 information separators FS/GS/RS (U+001C–U+001E) after adversarial reproduced an RIS record-forgery through `splitlines()`-class importers. Round-2 re-review (commit 1577347b, 7-persona `/ce-code-review`): the widened class is exactly the 10 code points CR/LF/VT/FF/FS/GS/RS/NEL/LS/PS (correctness scanned the BMP; security cross-checked Python `str.splitlines()`, confirming US/NUL are correctly excluded), the forgery is closed, and the corrected `\s`-membership prose is finally right (verified Node 20: only NEL + FS/GS/RS are non-`\s`; VT/FF/LS/PS ARE `\s` members caught by a downstream `\s+` collapse). All three digest-task files (`lib/line-terminators.ts`, `digest.ts`, `digest-title-strip.test.ts`) reviewed clean. The two residual cleanup findings — a false-confidence RIS C0-forgery test and a stale 7-char consumer docblock — both live in the sibling `backend-citation-export-format-escape` task's files and were held there, not here. SEP-table duplication across the two suites is the intended independent value-pin (`dedup-shared-constant-defeats-test-value-pin`), not a dedup target. This task is the resolution event for `line-terminator-flattening-alphabet-and-regex-s-membership-2026-06-08`.

---

## ARCHITECT-NOTIFICATIONS-BLOCK-GRANULAR-CURSOR-STALL (archived 2026-06-09) — unified fetch-ordering redesign; newest-first + whole-block delivery; decision recorded ✓

Architect-owned design decision for the shared `fetchNotificationsFromHaf` fetch-ordering / `has_more` contract (consumers: SPA route, digest, and the cache-key task's >CAP residual). Adversarial design verification (workflow `wf_182b09cb-09d`) killed the narrow "drop the SPA rewind + whole-block-at-the-response-limit-edge" plan as strictly-worse-than-status-quo: the `latest_block - 1` rewind is load-bearing at the *fetch-cap* edge (1000), not the response-limit edge (50) — the batch is `ORDER BY block_num ASC LIMIT 1000` (oldest-first, no tie-breaker) so the cap cuts through a block at the newest tail and dropping the rewind advances past it → permanent silent skip; the real trigger is "total in-window events > 1000" (normal for an active author), not "one block > 1000 events". Root cause = oldest-first fetch (a "what's new" feed must fetch newest-first). User chose unified scope + newest-first route. Settled contract: shared `fetchNotificationsFromHaf` gains an `id` same-block tie-breaker (the `backend-window-cte-deterministic-tiebreaker` convention applied to the one query it excluded) + a `direction` param + drops the partial boundary block on cap-hit (neither consumer ever sees a cap-truncated block). Route → newest-first (`'desc'`) + whole-block delivery, `has_more = filtered > delivered`, client always advances to `latest_block` (rewind removed); eliminates the single-block stall AND the >CAP starvation. Digest → oldest-first (`'asc'`), advances to the highest delivered block each non-empty run (partial-block drop kills the re-send cascade). Integer cursor preserved; composite cursor rejected (breaks API/localStorage/digest BIGINT + needs intra-block paging; trigger unreachable at beta scale; raising the cap is the cheap interim knob). Deploy backend-first, never frontend-first. Deliverables: `notifications.md` contract updated; `backend-notifications-route-newest-first-whole-block` + `ui-notifications-drop-rewind-and-block-cap` filed in `pending/`; `backend-notifications-digest-window-cursor` block re-characterized (architect cause resolved, gated on the route task's shared-function change, kept in `blocked/`); `backend-notifications-route-comment-stale-rewind` flagged superseded.

---

## BACKEND-REPUTATION-DECAY-MULTIPLIER-HELPER (archived 2026-06-09) — decayMultiplierSql adoption pin; clean re-review ✓

Extracted `decayMultiplierSql` (commit 5c9f0a9d) to deduplicate the 3× inlined decay formula across the paper/review/citation `*_scores` CTEs and drop the redundant outer `GREATEST(decay_floor, …)` (numeric-identical). Re-review hold (commit e1ee60f4) added an adoption pin in `hafsql.test.ts`: the inline token `GREATEST(w.decay_floor` must appear 0× in `reputation.ts` source and `decayMultiplierSql(` must be invoked exactly 3×. A one-site re-inline that reproduces the helper output keeps the snapshot green but now trips source-absence; the call-count assertion backstops the only evasion (a whitespace-reformatted re-inline must also fabricate a 3rd call). Verified clean by correctness + testing; anchored on SQL/call tokens, not line numbers.

---

## BACKEND-REPUTATION-GETHEAD-STATEMENT-TIMEOUT (archived 2026-06-09) — per-query statement_timeout helper; 4 holds landed; clean re-review ✓

`queryWithStatementTimeout(pool, timeoutMs, sql, params)` (commit a741399c) bounds the batch head read (5s), main WITH-CTE query (25s), and `getHeadBlock` (5s) via connect + BEGIN + `SET LOCAL`, so a hung HAF replica fails the cycle fast instead of stranding it on the pool-level 30s default. Re-review (commit 5fe126c4) landed 4 holds, all verified clean (correctness / kieran-ts / reliability): row-type generic `<R extends pg.QueryResultRow>` threading `client.query<R>`; `HEAD_QUERY_TIMEOUT_MS` exported and de-duplicated (removed `GETHEAD_TIMEOUT_MS`); residual-bound docblock (BEGIN/COMMIT/ROLLBACK run under the pool-level 30s, not `timeoutMs`); mirror-claim reword. A 57014 propagates through the re-throw to the outer catch without advancing `cycle:last`.

---

## BACKEND-REPUTATION-PIPELINE-EXEC-ERROR-CHECK (archived 2026-06-09) — staging exec() per-command-error guard; 2 holds landed; clean re-review ✓

Guard (commit d1dc69f8) inspects `pipeline.exec()` per-command error tuples after the staging SETs and `break`s before the sentinel SET + atomic CYCLE_SWAP Lua if any failed — closing the ioredis "exec() doesn't throw on per-command error" gap that would otherwise let a partial staging set reach the RENAME-and-advance swap and leave prod keys mixed across cycles. `break` leaves `cycle:last` unadvanced so the next run's `clearStagingKeys` drops the partial set. Re-review (commit 9bc8aaef) landed 2 holds clean (correctness / testing / reliability): the bail `logger.error` now carries the first failed command's message (`results?.find(([e]) => e !== null)?.[0]?.message`, same single line, zero volume cost) to distinguish OOM / WRONGTYPE / eviction; the three Arm-3 test comments were rewritten — they no longer falsely claim "no staging key is written" (the spy runs the real `exec()` unconditionally; only the first returned tuple is flipped; the written key is cleaned in `afterEach`).

---

## BACKEND-REPUTATION-ACTIVE-AUTHORS-ACCREDITED-FILTER (archived 2026-06-09) — paper-arm accredited filter; EXPLAIN + comment holds landed; clean re-review ✓

Added `AND c.author = ANY($2::text[])` (accreditedArr) to the `active_authors` paper arm (commit 5193af77) so it stops materializing non-accredited PEvO authors that the sole consumer (`voter_weights`, joining on the same accredited set) discards — semantically equivalent; the bridge/anon accounts are deliberately not OR'd in (never voters). Re-review (commit 66d8f7cc) landed 2 holds clean (performance / maintainability): the paper-arm comment dropped positional params ($2/$17/$18) for stable names (accreditedArr / hiveBridgeAccount / hiveAnonAccount); the `EXPLAIN (ANALYZE, BUFFERS)` acceptance was discharged honestly against the live HAF replica — the `(parent_author, parent_permlink)` index already bounds the scan (the original "full corpus scan" framing was pessimistic), the predicate is an additional in-memory filter retained as structural defense, and both variants return identical rows.

---

## BACKEND-NOTIFICATIONS-CLAIM-VOUCH-ARMS-SIGNER-GATE (archived 2026-06-08) — arms 8/9 EXISTS existence proof closes self-asserted-paper_author forgery; claim-correlation residual split to follow-up

Round-2 (commit 4edd2390) added an EXISTS proof against `hafsql.comments` + `validPevoPaperWhere(source:'native')` to arms 8 (`claim_approved`) / 9 (`claim_revoked`), binding the signer (`required_posting_auths ->> 0`) to the REAL native post author instead of the JSON-self-asserted `paper_author` — closing the stranger/fake-paper forgery (negative canaries pin both arms: a stranger self-naming as paper_author of a fake or non-owned paper is dropped). Arm-4 vouch comment re-anchored on the schema NAME (Vouch / Web of Trust). Verified by a 9-reviewer `/ce-code-review` (correctness/security/adversarial on Opus). Invariants: bridge/admin branches stay param-bound (no JSON trust); arm-9 claimer-self branch safe because `json ->> 'claimer'` is pinned to `$1`; boolean precedence correct (EXISTS-OR group fully parenthesized inside the action/claimer/block_num ANDs); NULL `paper_permlink` → EXISTS no-match. RESIDUAL split to `backend-notifications-claim-approve-revoke-correlation`: the proof binds signer→paper-author but NOT paper→`claimer`($1), so any accredited paper-owner can still fire a spurious approve/revoke notification at an arbitrary victim by naming their OWN real paper; the same self-referential pattern also lives in `authorshipClaimsCteBody`; arms 8/9 also lack DISTINCT ON dedup. P2 (notification/email nuisance, no state/credit impact).

---

## BACKEND-NOTIFICATIONS-EDIT-REVOTE-DEDUP (archived 2026-06-08) — per-arm DISTINCT ON dedup; clean re-review ✓

Round-1 (commit 3007f498) wrapped all eight raw-operation-view arms in DISTINCT ON dedup (comment arms 1a/1b/5 earliest-wins `block_num ASC`; vote arms 2a/2b/2c latest-wins `block_num DESC` with `vote_weight != 0` hoisted to the OUTER select so vote-then-retract suppresses; citation arms 6a/6b earliest-wins on the (citing post, cited paper) 4-tuple), so edits/revotes no longer re-fire duplicate notifications. Round-2 (commit 95c85069) landed 5 text/canary hold items, verified clean by a 3-reviewer `/ce-code-review` (correctness confirmed the SQL is byte-identical / comment-only): dropped the `BACKEND-SELF-REVIEW-EXCLUSION` slug/round citation from arm 1b's comment (P1 acceptance violation) and re-anchored on behavior; extended the arm-sql-shape header inventory (entries 6-9) + mutation-kill paragraph; trimmed the arm-1a INNER-JOIN canary title to join-form-only; ADDED the citation `ORDER BY citing.block_num ASC` direction pin (count==2, catches a DESC flip that would resurrect per-edit citation re-fire); expanded arm-5's dedup docblock.

---

## BACKEND-NOTIFICATIONS-VOTE-ARM-TEST-COVERAGE (archived 2026-06-08) — target_type pinned against production SQL; clean re-review ✓

P3 test-quality follow-up to the vote-arm content-filter task. Round-2 (commit f8d939a7, test-only) landed 2 hold items, verified clean by a 3-reviewer `/ce-code-review`: (1) target_type now pinned against PRODUCTION — the vote-arm source-shape test slices each arm by its tag boundary (`region()` helper) and asserts the projection literal inside each (`'paper'::text` for 2a/2b, `'review'::text` for 2c), so a production literal swap fails red (the old `toContain("'review'")` only proved 'review' appeared SOMEWHERE in the 3-arm span); the 2b synthetic canary dropped the condemned `MIN('paper'::text)` for the fixture-derived `MIN(bp.target_type)`. (2) the arm-2b slice asserts INNER JOIN form (JOIN present, LEFT absent) + the `user_bridge_papers` CTE `registered_by=$1` predicate, so an INNER→LEFT flip or dropped predicate in production fails. All 6 `region()` markers verified unique + correctly ordered.

---

## UI-NOTIFICATIONS-BLOCK-CURSOR-BOUNDARY-REWIND (archived 2026-06-08) — poll() rewinds cursor to latest_block-1 on has_more, clean review ✓

`frontend/src/notifications.js` poll loop now sets the next cursor to `latest_block - 1` when the route reports `has_more === true` (the server's LIMIT cut the batch after the in-app since_block filter), so the boundary block is re-fetched instead of silently dropped; `has_more === false` advances to `latest_block` as before. The existing SPA dedup key (`block_num`/`type`/`actor`/`permlink`) collapses the re-rendered overlap. Landed at commit 032581a1 with three unit tests (rewind / advance / two-poll re-fetch+dedup). Unblocked by the backend `applySinceBlockFilter` rework (route-layer `has_more` recomputed as `filtered.length > events.length || batch.has_more`, no longer forced false when the in-app cursor filter drops events).

7-persona `/ce-code-review` (correctness on Opus): implementation clean, design validated against `cursor-agnostic-cache-must-dominate-result-set-2026-06-05`. Verified: off-by-one correct (strict `> latest_block-1` re-includes exactly the boundary block); `nextCursor >= cursor` always (the `latest_block > cursor` guard means the cursor never regresses); `has_more` undefined falls to the safe advance path; dedup collapses the overlap to one render. Dismissed findings (all verified non-issues): the `refresh()`/generation cursor-regression race is unreachable (`refresh()` has zero call sites and the scheduled-timer chain is strictly sequential via `_scheduleNext`/`clearTimeout`); the `latest_block=1 → nextCursor=0` loop is unreachable (Hive block numbers are ~94M); Alpine render churn is a non-issue (merge-then-single-assign is one reactive write); plus two optional tests and an unnamed rewind constant below the action floor.

One real residual carved out as a follow-up: a single block whose event count for one account exceeds the response `limit` stalls the feed (`has_more` pins true, `latest_block` never advances, cursor freezes at `latest_block-1`, newer blocks blocked too). Reachable only at a scale the accredited beta won't hit; the clean fix is cursor granularity (whole-block delivery or composite cursor), tracked in `architect-notifications-block-granular-cursor-stall`. Contract doc `api-contracts/notifications.md` `has_more` bullet updated to document the rewind; the stale `applySinceBlockFilter` docblock comment flagged to backend in `backend-notifications-route-comment-stale-rewind`.

---
