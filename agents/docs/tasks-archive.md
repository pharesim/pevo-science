## BACKEND-COMMENT-ANCHOR-SWEEP-SUPERSESSION-CLUSTER (archived 2026-05-21) — round-4 clean ✓

Four-round comment-anchor-sweep across the supersession-cluster files: drop slug/round-N/line-number citations and stale docblock claims from `backend/src/types/domain.ts`, `backend/src/lib/author-supersession.ts`, `backend/src/helpers.ts`, `backend/src/routes/profile.ts`, `backend/tests/helpers.test.ts`, and `backend/tests/routes/continuation-author-gate.test.ts`. Round-1 (commit `b3918007`/`9fdf48b0`) fixed the originally-cited sites plus widened `applyAuthorSupersession`'s non-object branch to return `{orcid_verified: null, orcid_discrepancy: false}` (vs prior `{}`), aligning JS-side projection with the SQL-side `jsonb_build_object` emit-both-keys invariant. Round-2 hold (4 items) addressed mismatched-anchor comments and the inaccurate "case-1 of the supersession lattice" terminology in `types/domain.ts`. Round-3 hold (`12d2ae1c`) added a section-banner rewrite at the cumulative-union display canaries block + stripped 11 `Acceptance #N:` prefixes that cited the archived `backend-multi-author-cumulative-union.md` task; whole-file audit surfaced 6 additional relative-positional anchors (`above`, `below`, "next test") fixed in the same commit. Round-4 (`beccde39`) fixed two banner imprecisions the round-3 expansion introduced — "third-party claim" → "non-self claim" (matches `buildCumulativeAuthorsForChain`'s "absent a self-claim, the most-recent claim across the chain") and "ORCID server-overridden for accredited hives" → "...whose claim differs from the attestation" (preserves the matching-claim canary's no-mismatch-event invariant). One residual P3/50 banner imprecision dismissed at round-4 triage: the "differs" qualifier is narrower than the code's full override semantics (covers cases (b) mismatch + (a) match-pass-through, but misses (c) no-claim-prefill and (d) claim-but-no-attestation-suppress). Harm is reader-confusion only; further iteration on test-banner prose has diminishing returns. Load-bearing learning surfaced + filed via `/ce-compound`: comment-sweep expansions must audit added-clause behavioral accuracy against code, not just rot-pattern absence (the round-3→round-4 hold was itself triggered by added prose drifting from code despite passing the broadened rot grep). Cross-refs: `agents/docs/solutions/conventions/task-slug-citations-in-comments-go-stale-on-archive-2026-05-15.md`, `agents/docs/solutions/conventions/docblock-anchor-stable-symbols-not-line-numbers-2026-05-15.md`, `agents/docs/solutions/conventions/convention-enforcing-fix-must-audit-its-own-new-code-2026-05-17.md` (broadened across the four rounds to enumerate relative positional anchors, line-number anchors, SHA refs, date anchors, partial-strip stubs, orphan single-letter prefixes, dangling determiners, bare possessives, dangling prepositions).

## ARCHITECT-CUSTODY-MD-FRESH-AUTH-CHANGE-EMAIL-ACTION-DOC (archived 2026-05-21) — clean ✓

Architect-self-task fixing a pre-existing doc-vs-code drift in `agents/docs/api-contracts/custody.md` `/fresh-auth` surfaced by `/ce-code-review` api-contract AC-3 during `backend-custody-limiter-cpu-amplification-mitigation` round-1. The handler at `backend/src/routes/custody.ts` accepted `change_email` as a third `action` value (alongside `author_accept` / `author_resign`), with `root_author` / `root_permlink` NOT required for `change_email` (target synthesized from authenticated username via `changeEmailFreshAuthTarget(username)`). The doc enumerated only `author_accept | author_resign` and listed all four body fields as flat REQUIRED. Update split body-field requirements into action-category prose (consent-op actions vs non-broadcast action) mirroring the existing pattern in `agents/docs/api-contracts/orcid.md`, widened the action enum in the body example + VALIDATION_ERROR error message, documented that the `change_email` proof is consumed at the JWT path of `POST /api/settings/email` (NOT `/api/custody/broadcast`), and clarified that `set_password` is NOT minted via this route (structurally inapplicable: passwordless ORCID-only accounts have no password). Doc-only change; no code touched. Stale-note observation flagged for triage at archive: `agents/docs/api-contracts/orcid.md` and `settings.md` both carry "until then change_email returns 400 VALIDATION_ERROR" / "once those mint paths are live" notes that pre-date the now-live runtime widening, out of this task's scope and naturally addressed during the in-flight `backend-change-email-mint-path-and-followups` review-cycle archive. Cross-refs: `backend/src/routes/custody.ts` `validateFreshAuthBodyShape` middleware + `/fresh-auth` handler; `backend/src/lib/fresh-auth.ts` `FreshAuthTargetAction` union + `changeEmailFreshAuthTarget`; `agents/docs/api-contracts/orcid.md` (parallel ORCID-mechanism mint path); `agents/docs/api-contracts/settings.md` (`POST /api/settings/email` consumer); originating finding: `/ce-code-review` api-contract AC-3 during `backend-custody-limiter-cpu-amplification-mitigation` round-1.


## BACKEND-CUSTODY-UPGRADE-SEED-PHRASE-REAUTH (archived 2026-05-21) — round-7 clean ✓

Seven-round task replacing the password-based re-auth on `POST /api/custody/upgrade` with a seed-phrase-derived-pubkey proof. Closes the state-C (passwordless ORCID-only — see ARCHITECTURE.md § 6.1) upgrade gap, aligns with § 6.4 critical-action re-auth contract, and structurally cannot be solved with password (which state C accounts don't have). Round-1 (`1f1be4e`) chose pubkey-match + signed-challenge (mandatory) rather than pubkey-match-alone: body shape `{ derived_pubkey, signed_proof, signed_at }`, canonical challenge `${appTag}-custody-upgrade|v1|${username}|${signed_at}`, 60s freshness window, signature recovery via `Signature.fromString().recover()` + timing-safe match against both the declared `derived_pubkey` AND any on-chain authority slot (posting/active/owner) from `getAccounts`. All failure modes collapse to uniform 401 with structured-warn discriminators (`proof_malformed`, `pubkey_binding_mismatch`, `chain_key_mismatch`, `hive_account_missing`) so the route is not a chain-state oracle; Hive RPC failure → 503. 13 real-path integration specs covering states A/B/C happy paths + state-D 409 + 6 rejection paths. Deleted `custody-upgrade-null-hash.test.ts` and `custody-upgrade-argon-error-translation.test.ts` (branches gone after password-removal). Round-2 (`9210dd2`, 6 items): added 503-throw-path test; introduced `skipFailedRequests` option on the `rateLimit` primitive and opted `upgradeLimiter` in (transient Hive RPC failures no longer burn the 1/hr slot); one-sided past-only timestamp window (closes 120s replay window from the symmetric form); STM-prefix fixed-length-invariant docblock on `timingSafePubkeyEqual`; `rows.length === 0` 401 canary; stolen-JWT VALIDATION_ERROR no longer burns the slot via `skipFailedRequests`. Round-3 (`c9c7c5f`, 5 items): combined fix replacing GET → next() → deferred-INCR with atomic Lua `RATE_LIMIT_CHECK_AND_CONSUME` in `redis-scripts.ts` (closes permanent-lockout TTL bug AND TOCTOU overshoot in one round-trip); clock-skew Option A 5s forward-skew tolerance; task-file path citation dropped from `upgradeLimiter` inline comment; verbatim grep over all `rateLimit(` call sites (per `wrapping-primitive-exhaustive-call-site-audit-2026-04-22.md`) confirming no credential-probing-route misuse; JSDoc misuse-direction warning on `skipFailedRequests`. Round-4 (`62066cb`, 6 items): `res.on('close')` + `'finish'` with once-guard (closes TCP-abort slot-leak); `ScriptReturn` structural typing on `evalScript` (closes unsafe `as [number, number]` cast); rolling-window JSDoc clarification; stale-redis-binding fix; `rateLimit-in-memory.test.ts` for the splice path; skew-reject diagnostic `proof_future_skew` warn log. Round-5 (`2493383`, 2 items): refund-gate widened from `statusCode < 400` to `statusCode < 400 && writableEnded` (covers pre-status-set TCP-abort during pending awaits — pinned by new real-Redis `http.request` + `req.destroy()` regression test); `ScriptReturn ↔ SharedScriptName` exhaustiveness guard via `_NoMissingReturn`/`_NoExtraReturn` type-level pattern (after architect's `satisfies Record<...> satisfies ScriptReturn` snippet failed to compile — `satisfies` is value-level only). Round-6 (`91beb3a`, 3 items): in-memory abort-path regression test parallel to the Redis one (closes one-sided revert path through CI); canonical `satisfies-Record-then-typeof` exhaustiveness pattern via `_SCRIPT_RETURN_SHAPE` value (per `satisfies-record-for-mapped-type-and-set-completeness-2026-05-17.md` convention); refund-closure cross-reference comment. Round-7 (`a74347c9`, 2 items, comment-only): dropped `Round-5 → round-6 regression mirror:` round-number framing from test comment block (replaced with companion `it`-block-title anchor); replaced `rateLimit.ts:204-215` line-number anchor with "the in-memory refund closure in `rateLimit.ts`" symbol descriptor; audit-own-replacement sweep caught a sibling `/slow`-handler comment with the same round-N rot and rewrote it to anchor on the gate predicate (`statusCode < 400 && writableEnded`). Round-7 `/ce-code-review` (6 reviewers — correctness on Opus; testing/maintainability/project-standards/kieran-typescript/learnings-researcher on Sonnet; ce-agent-native-reviewer skipped per PEvO CLAUDE.md; security/adversarial/reliability/data-migrations skipped — comment-only diff): zero in-diff findings; one P3 project-standards nit on the round-7 signal heading itself omitting the `, commit <SHA>` qualifier per agents/backend/CLAUDE.md spec format — dismissed at archive as a documentation-format nit not worth holding round-7 reopen. Spawned follow-up: `backend-custody-limiter-cpu-amplification-mitigation` filed round-3 when `skipFailedRequests` adoption surfaced the layered-defense-pattern need (currently in `tasks/pending/` after round-1 architect hold). Architect-zone follow-up landed in-session at the round-3 archive boundary: `agents/docs/api-contracts/custody.md` `/upgrade` section rewritten for the new body shape + 503 SERVICE_UNAVAILABLE entry + clock-skew Option A documentation. Cross-refs: `backend/src/routes/custody.ts` (`POST /api/custody/upgrade` handler + `validateUpgradeBodyShape` middleware + `timingSafePubkeyEqual` + `buildCustodyUpgradeChallenge` exported for cross-source byte-equality test); `backend/src/middleware/rateLimit.ts` (`skipFailedRequests` option + atomic-Lua path + refund-gate); `backend/src/lib/redis-scripts.ts` (`RATE_LIMIT_CHECK_AND_CONSUME_LUA` + `ScriptReturn` mapped type + `_SCRIPT_RETURN_SHAPE` exhaustiveness guard); `backend/tests/routes/custody-upgrade.test.ts` (14 specs); `backend/tests/middleware/rateLimit.test.ts` + `rateLimit-in-memory.test.ts` (skip-failed primitive + abort-path regression); `agents/docs/api-contracts/custody.md`; `agents/docs/ARCHITECTURE.md` § 6 (account-state machine + critical-action re-auth contract); `wrapping-primitive-exhaustive-call-site-audit-2026-04-22.md`; `strict-superset-wrapper-inherits-escape-hatches-2026-05-12.md`; `satisfies-record-for-mapped-type-and-set-completeness-2026-05-17.md`; `docblock-anchor-stable-symbols-not-line-numbers-2026-05-15.md`; `task-slug-citations-in-comments-go-stale-on-archive-2026-05-15.md`; `convention-enforcing-fix-must-audit-its-own-new-code-2026-05-17.md`.


## BACKEND-HAF-OUTAGE-TRANSLATION-AUDIT-ACROSS-ROUTES (archived 2026-05-20) — round-4 clean ✓

Four-round cluster-wide audit extending the `HafQueryError` translation + 503-retriable response pattern to all HAF-touching routes. Round-1 (commit `94bf2949`) catalogued every HAF-touching site across `backend/src/routes/` + `backend/src/lib/` + `backend/src/accreditation.ts`, translated 4 missing sites (`profile.ts:fetchUserPapersFromHaf`, `profile.ts` enrichment Promise.all, `profile.ts:fetchUserReviewsFromHaf`, `reviews.ts:fetchReviewFromHaf`, `comments.ts:paperExistsInHaf`), and rolled back one initially-translated site (`notification-queries.ts` — broad multi-CTE notifications query legitimately hits `statement_timeout` on wide `sinceBlock` ranges, mis-classifying expensive query as outage). Round-2 (`d6a1eff`) closed 6 hold items: `fetchCommentsFromHaf` listing translation (preflight/listing composition asymmetry); `HafQueryError` docstring rationale anchor; cause-discriminated `isRetriableHafError` helper (`08*` + `57014` only initially, gating on `instanceof HafQueryError && isRetriableHafError(err)`); SQL-fragment discriminators on every canary; `profile.ts` enrichment Promise.all wrap canary; 5 intentional-swallow rationale comments. Round-3 (`44f7c0b1`) closed 5 hold items: extended `isRetriableHafError` with `57P03` + `53300`; retyped helper parameter `(err: HafQueryError)` (drops unchecked cast); `details.retriable: absent` assertion on the 42601 deterministic-pg canary; dropped `as ErrorOptions` cast from `HafQueryError` constructor; removed dead `sendOk(res, [], …)` else-branch in `comments.ts`. Round-4 (`c80e2a3c`) dropped a single `round-3 hold extension of` round-number anchor from the 57P03 canary's it-block comment, per the round-4 hold prescription. Round-4 `/ce-code-review` (6 reviewers — correctness on Opus; testing, maintainability, project-standards, kieran-typescript, learnings-researcher on Sonnet; ce-agent-native-reviewer skipped per PEvO CLAUDE.md): one finding (project-standards anchor 75 cross-corroborated by learnings-researcher) on the preserved `the 42601 canary above` positional anchor — architect dismissed per prior round-3→round-4 hold-block call that the sibling-canary-in-same-describe-block anchor is durable. Cross-task with `backend-fetch-paper-detail-haf-error-vs-not-found` round-2 review (`33ceef04`): cross-corroborated reliability finding (anchor 75) on missing `57P01` (admin_shutdown) — filed as new follow-up `backend-isretriable-haf-add-57p01-and-53300-coverage` rather than bundled. Architect followups landed in-session at commit `66b213ac`: `papers.md` 503 SERVICE_UNAVAILABLE enumeration for `/comments` + 4 paper-detail-class routes; `common.md` 503-retriable note refresh. Convention doc per `[TODO Architect]` round-1 carry-forward (`haf-error-translation-pattern.md` via `/ce-compound`) still deferred post-archive. Cross-refs: `agents/docs/api-contracts/papers.md`, `profiles.md`, `reviews.md`, `common.md`; `backend/src/db.ts` `HafQueryError` + `isRetriableHafError`; `backend/tests/routes/haf-outage-translation-canaries.test.ts` (10 specs, mirror-shape canaries); `convention-enforcing-fix-must-audit-its-own-new-code-2026-05-17.md`; `task-slug-citations-in-comments-go-stale-on-archive-2026-05-15.md`; `comment-sweep-expansion-must-audit-added-clause-behavioral-accuracy-2026-05-20.md`.


## BACKEND-FETCH-PAPER-DETAIL-HAF-ERROR-VS-NOT-FOUND (archived 2026-05-20) — round-3 clean ✓

Three-round task distinguishing HAF errors from data-not-found at the paper-detail-class route layer (primary detail, enrichment, retract, cite). Round-1 (`b427a700`) introduced the `HafQueryError` class in `backend/src/db.ts` and tagged HAF-pool query failures at `fetchPaperDetailFromHaf` + `fetchEnrichmentFromHaf` catch arms; the 4 route handlers branch on `instanceof HafQueryError` → 503 SERVICE_UNAVAILABLE with `details.retriable: true`. Implementer chose Option B (`HafQueryError` tagging) over Option A (discriminated return type) for narrower-scope reasons. Helper docstrings on `getAccreditedOrcidsByAccount` and `getAllEverAccreditedOrcidsWithStatus` rewritten to drop aspirational "loud-fail parity" framing and replace with the now-realized "throws on HAF unavailable; route layer translates to 503" contract. 8 mocked-pool specs covering 503-vs-404 at all 4 routes. Round-2 hold (7 items, `33ceef04`): `papers.ts` catch arms skipped `isRetriableHafError` cause-discrimination (P1 cross-corroborated by 6 reviewers — deterministic pg failures emitted 503 retriable, inducing SPA retry storm on dead queries); 4 deterministic-pg canaries (`code: '42601'`) added; `isRetriableHafError` added to `dbStubFactory`; 2 task-slug citations dropped from `papers.ts` catch comments; `details.retriable === true` assertions added to 3 walker-abort 503 specs; dead `operation` field removed from `HafQueryError`; per-route 503 message strings on `/retract` ("Retraction temporarily unavailable") and `/cite` ("Citation export temporarily unavailable"); stale mutation-kill comment in `papers-canonical-orcid-resolution.test.ts` updated to post-fix throw-path semantics. Round-3 hold (2 items, `5b2f71a2`): dropped uppercase task-slug citation from test-file header (replaced with route-path + behavioral anchor); replaced `cache.ts:73` line-number anchor in `fetchPaperDetailFromHaf`'s cache-poisoning defense comment with `hafCache.getOrSet` stable-symbol anchor (the original line citation was actually misanchored on a `logger.debug` line; `hafCache.getOrSet` is the public method that enforces the null-skip rule). Round-3 `/ce-code-review` (6 reviewers): zero findings — behavioral accuracy of the `hafCache.getOrSet` claim verified (`cache.ts:92` + null-skip guard at line 107); test header anchor verified non-rotting. Architect followup landed in-session at `66b213ac` (papers.md 503 enumeration). Cross-task `57P01` (admin_shutdown) reliability finding from this task's round-2 review filed as `backend-isretriable-haf-add-57p01-and-53300-coverage` rather than bundled. Cross-refs: `backend/src/db.ts` `HafQueryError` + `isRetriableHafError` (the helper introduced here, extended in `backend-haf-outage-translation-audit-across-routes`); `backend/tests/routes/papers-haf-error-vs-not-found.test.ts` (mocked-pool 503-vs-404 canaries + 4 deterministic-pg canaries); `backend/src/routes/papers.ts` (`fetchPaperDetailFromHaf` cache-poisoning defense + 4 catch arms); `agents/docs/api-contracts/papers.md` (503 SERVICE_UNAVAILABLE entries on 4 routes); `task-slug-citations-in-comments-go-stale-on-archive-2026-05-15.md`; `docblock-anchor-stable-symbols-not-line-numbers-2026-05-15.md`.


## BACKEND-RETRACT-RATE-LIMIT-HAF-503-BURN (archived 2026-05-20) — round-2 clean ✓

Two-round task closing the slot-burn cascade on `/api/papers/:author/:permlink/retract` introduced by the new retriable-503 envelope. Pre-fix: SPA auto-retries on `details.retriable: true` would burn the per-account 5/hour budget during a single HAF outage event, locking the legitimate user out for the rolling window. Round-1 (`a5589588`) added `skipFailedRequests: true` to `retractLimiter` declaration. Implementer chose Option A over Option B (slot refund API) and Option C (Retry-After header) per JSDoc-verified middleware semantics: `skipFailedRequests` refunds the slot on any `res.statusCode >= 400` (4xx and 5xx) via `res.on('finish')` + `res.on('close')` once-guard. Threat-model analysis verified 4xx-refund safety on `/retract`: verified-signature gate ensures attacker can only probe their own paper set; 422/404/403 paths are deterministic-from-state, not abuse-driveable; 502/504 carry `verify_before_retry` envelopes so SPA does not auto-retry; 503/500 paths covered by skipFailedRequests. Cross-route audit catalogued every PEvO rate limiter; identified `retractLimiter` as the only `byAccount` long-window limiter with a route-side `retriable: true` emit not already using `skipFailedRequests`. `registerLimiter` (byIp NAT-shared) flagged as quasi-followup → architect filed as new `backend-register-rate-limit-byip-skipfailed` task during round-1 review. Two canaries pinning slot-burn = 1 per outage event + abuse-cap-preserved. Round-1 hold (1 item, `ae570f71`): clause-(c) citation in test header claimed `retract.test.ts` exercises real `verifyHiveSignature`; verified false (that file mocks the middleware via `MOCK_VERIFY_SIGNATURE`); rewrote to cite corpus-level real-path coverage via `sign-request.ts` consumers (`auth.test.ts`, `claims.test.ts`, `bridge-haf-lag-locks.test.ts`) and `tests/middleware/rateLimit.test.ts` for the rate-limiter middleware itself. Round-2 `/ce-code-review` (6 reviewers): zero blocking findings — all 4 cited companion test files verified to exist and use real `verifyHiveSignature`; one informational correctness note (rateLimit.test.ts internally stubs `verifyHiveSignature` rather than exercising it, but the comment correctly scopes its claim to the rate-limiter risk class). Architect followups deferred per the implementer's `[TODO Architect]` (api-contracts/papers.md + common.md 503-retriable note refresh) — landed during the cluster's later `/ce-code-review` session at commit `66b213ac`. Cross-refs: `backend/src/routes/papers.ts:720` (`retractLimiter` declaration); `backend/src/middleware/rateLimit.ts` (`skipFailedRequests` + `writableEnded` gate at line 188 per `deferred-refund-gate-must-check-writableEnded-not-just-statusCode-2026-05-17.md`); sibling task `backend-register-rate-limit-byip-skipfailed` (the byIp parity-sweep extension filed during this task's round-1 review); `test-mock-carve-out-clause-c-2026-05-04.md` (the carve-out framework the round-1 hold cited).


## BACKEND-ISRETRIABLE-HAF-ADD-57P01-AND-53300-COVERAGE (archived 2026-05-20) — round-1 clean ✓

Single-round symmetric-coverage task closing two asymmetries in `isRetriableHafError`'s retriable set surfaced by cluster review of `backend-fetch-paper-detail-haf-error-vs-not-found` round-2 + `backend-haf-outage-translation-audit-across-routes` round-3: (1) `57P01` (admin_shutdown — graceful shutdown half of the Postgres restart cycle) missing; previous extension added 57P03 (startup/PITR/standby-promotion) but not its operational mirror; (2) `53300` (too_many_connections) was added to the discriminator without a paired canary, leaving a mutation-kill gap on that positive-class extension; (3) 3 test-local mock copies of `isRetriableHafError` still embedded the round-2 retriable set (missing both 57P01 and 53300). Round-1 (`a43613ff`) added `57P01` to the production discriminator with operational rationale in the docstring ("Postgres graceful shutdown windows; realistic during HAF maintenance" — paired with 57P03 as the two halves of the restart cycle); added two new canaries (57P01 + 53300) in `haf-outage-translation-canaries.test.ts` keyed on the same SQL discriminator as the existing 57P03 canary (`c.body, c.json_metadata` + `paper_title` for `fetchReviewFromHaf`); swept the 3 mock-copy parity sites — `papers-haf-error-vs-not-found.test.ts`, `retract-rate-limit-skip-failed.test.ts`, `tests/support/argon2-error-mocks.ts:dbStubFactory` — all now read `code.startsWith('08') || code === '57014' || code === '57P01' || code === '57P03' || code === '53300'`. Default-to-retriable preserved across all copies. Round-1 `/ce-code-review` (7 reviewers — correctness on Opus; reliability, testing, maintainability, project-standards, kieran-typescript, learnings-researcher on Sonnet; ce-agent-native-reviewer skipped per PEvO CLAUDE.md): one finding (project-standards anchor 75 cross-corroborated by correctness anchor 40) on positional anchor `the 57P03 canary above` in the new 57P01 canary comment — architect dismissed per the same rationale as `backend-haf-outage-translation-audit-across-routes` round-4: sibling-canary-in-same-describe-block anchor is durable enough. Operational-realism caveat (the addition is gap-from-symmetry reasoning, not observed-occurrence data; Mahdi's HAF restart pattern may or may not emit 57P01 before connection-drop depending on smart-stop vs fast-stop) documented in the task body; low risk to land (set widens only; no false-positive risk because 57P01 IS structurally a transient shutdown signal). Cross-refs: `backend/src/db.ts:isRetriableHafError` discriminator + docstring; `backend/tests/routes/haf-outage-translation-canaries.test.ts` (10 canaries including 57P01, 57P03, 53300, 42601 negative-class); 3 mock-copy parity sites; PostgreSQL SQLSTATE Appendix A (errcode reference); originating task `backend-haf-outage-translation-audit-across-routes` round-3 + `backend-fetch-paper-detail-haf-error-vs-not-found` round-2 cluster reviews.


## UI-FRONTEND-RETRY-TIMER-GUARD-SWEEP (archived 2026-05-20) — round-3 clean ✓

Convention-consistency sweep adopting the in-tree timer-guard primitive (`frontend/src/lib/timer-guard.js`) across 4 naked-setTimeout retry-backoff sites: `bridge.js` LOCK_HELD backoff + 3 sites in `paper-detail.js` (`loadPaper` SERVICE_UNAVAILABLE + `loadPaper` NOT_FOUND + `handleCitationExport` SERVICE_UNAVAILABLE). Added `_sleep(ms)` primitive that registers its setTimeout ID with the existing teardown tracking set so `_teardownTimers()` resolves pending sleeps on destroy. `paper-detail.js` adopted `createTimerGuard` / `_mounted` for the first time as part of the migration. Round-1 hold (3 items, commit `c192833c`): `handleCitationExport` destroy-mid-503-backoff test missing (mirrored the `loadPaper` shape with load-bearing call-count assertion); vacuous 503 destroy-test assertions (switched to string sentinels seeded AFTER `loadPaper()` call so the synchronous prologue's reset doesn't overwrite them, and distinct from the regression-write value `errorIs503 = true`); `destroy()` method-list comment replaced with invariant-anchored phrasing per root CLAUDE.md "Comment anchors". Round-2 hold (1 item, commit `5dae8458`): sentinel-seeding comment header opened with "Seed sentinel values BEFORE calling loadPaper" while the code seeded AFTER on the next two lines — internal contradiction that would have lured a future maintainer into re-introducing pre-call seeding the entry-prologue immediately overwrites. Round-3 fix rewrote the comment header to "Seed sentinel values AFTER calling `loadPaper()`" with the synchronous-prologue rationale preserved in the body. Round-3 review `/ce-code-review` (5 reviewers — correctness on Opus; testing, maintainability, project-standards on Sonnet; ce-agent-native-reviewer skipped per PEvO CLAUDE.md; ce-learnings-researcher confirmed alignment with three same-day conventions): zero findings. Cross-refs: `vacuous-state-unchanged-assertion-sentinel-pattern-2026-05-20.md` (codifies the post-prologue seeding pattern), `comment-sweep-expansion-must-audit-added-clause-behavioral-accuracy-2026-05-20.md`, `alpine-persistent-instance-unconditional-ui-flag-reset-2026-05-20.md` (sibling learning explaining the asymmetry: `handleCitationExport` destroy test cannot use sentinels because of unconditional finally-reset, hence leans on call-count instead), `docblock-anchor-stable-symbols-not-line-numbers-2026-05-15.md`, `convention-enforcing-fix-must-audit-its-own-new-code-2026-05-17.md`.


## UI-BRIDGE-REGISTER-MID-BROADCAST-LOOKUP-GATE (archived 2026-05-20) — round-1 clean ✓

Pre-existing race surfaced by `/ce-code-review` adversarial reviewer during `ui-bridge-register-lock-held-ux` round-2: a mid-broadcast Lookup unmasked `step === 'registering'` (because `handleLookup`'s entry reset overwrites `step` to `'idle'` per the round-1 hold fix on the sibling task) and let the user land on a different paper than the one broadcast. Broadcast snapshot was always correct (the payload identifier is captured synchronously at `handleRegister` entry before any await), but the visible UI diverged from the on-chain outcome — the user saw paper B's preview, then redirected to paper A. Round-2 of the sibling task increased the visibility window from "step==='registering' visible at the byline area" to "state masked the moment the user clicks Lookup." Fix (commit `fbe0088b`) applies BOTH defense layers: shape (a) template `:disabled` gate on the identifier input + Lookup button now also disables when `step === 'registering'` (mirrors the existing `lookingUp` binding); shape (b) handler-side bail in `handleLookup` at entry — required for the vitest pin which invokes `handleLookup()` directly and bypasses the template gate. New vitest case `bails without firing lookup while a register broadcast is in flight` drives `handleRegister` to the in-flight state via a never-resolving `registerBridgePaper` mock (step transitions synchronously before the first await per `synchronous-flag-before-await-idempotency-guard-2026-05-16.md`), changes the identifier, awaits `handleLookup`, asserts the two fetch mocks were not called and `step` still reads `'registering'`. Round-1 review `/ce-code-review` (6 reviewers — correctness on Opus; testing, maintainability, project-standards, julik-frontend-races, learnings-researcher on Sonnet; ce-agent-native-reviewer skipped): one cross-reviewer-promoted finding (anchor 75) — template `:disabled` clause for `step === 'registering'` has no test-layer pin (handler bail is fully tested via vitest; template clause asserted to "mirror lookingUp" but a regression that drops the clause would leave a clickable Lookup button mid-broadcast while the handler-bail still keeps data integrity). Architect-triaged dismiss per `feedback_dismiss_preemptive_test_hardening`: failure mode is theoretical (template clause hasn't been edited away; a regression would require a deliberate template edit), handler bail is the load-bearing correctness backstop, template gate is UX nicety. Cross-refs: parent task `ui-bridge-register-lock-held-ux` (round-3 archived same day), `synchronous-flag-before-await-idempotency-guard-2026-05-16.md`, `defense-in-depth-canary-must-pin-each-layer-2026-05-07.md`.


## UI-BRIDGE-REGISTER-LOCK-HELD-UX (archived 2026-05-20) — round-3 clean ✓

SPA affordance for the new 409 LOCK_HELD vs DUPLICATE error code split on `/api/bridge/register` (added by `backend-bridge-write-haf-lag-and-retry-amplification` round-2). Round-1 (commit `3062f827`): branched `handleRegister`'s 409 catch on `err.code` — LOCK_HELD → localized "Registration in progress; please retry in a moment" + auto-retry with bounded backoff (1-3s, 2-3 attempts) using `_sleep` primitive with post-await `_mounted` guard; DUPLICATE → existing existing_author/existing_permlink rendering; new i18n key for lock-held + 15-locale stub sweep. Round-1 hold (2 items): `handleLookup` didn't clear `duplicateExisting` + `step` + `errorMessage` at entry (extracted `resetRegisterError()` method shared with the Try-again handler); two coverage gaps (destroy-during-LOCK_HELD-backoff + DUPLICATE with partial `err.details`). Round-2 hold (1 item): the `handleLookup` three-field reset bug had no direct behavior test — only manual diff inspection guarded against a regression that re-inlined the Try-again handler without the `duplicateExisting` clear or trimmed the entry reset from three fields to two. Round-3 fix (commit `b4e7c93f`): added vitest case `clears duplicateExisting, step, and errorMessage when looking up a new identifier after a prior DUPLICATE error` seeding the post-DUPLICATE state and asserting all three fields cleared. The seeded-then-cleared shape is the textbook load-bearing mutation-kill assertion (NOT vacuous per `vacuous-state-unchanged-assertion-sentinel-pattern-2026-05-20.md` — the seeded sentinels are domain-distinct from the cleared defaults across all three fields). Round-3 review `/ce-code-review` (5 reviewers): zero blocking findings. Below-gate observation (maintainability, conf 45): commit message + signal block claim "added at the end of the handleLookup describe block" but test was inserted mid-block with another `handleLookup` sibling still below — coordination-artifact accuracy nit, not a code defect. Cross-refs: contract docs `agents/docs/api-contracts/bridge.md` + `common.md`, sibling task `ui-bridge-register-mid-broadcast-lookup-gate` (archived same day, filed during round-2 review of this task), parent `backend-bridge-write-haf-lag-and-retry-amplification` (archived 2026-05-20), `tests-must-fail-on-mutation-of-code-under-test-2026-04-22.md`, `mutation-kill-claims-must-match-assertion-and-corpus-2026-05-15.md`.


## UI-PAPER-DETAIL-RETRIABLE-503-HANDLING (archived 2026-05-20) — round-3 clean ✓

Sibling-to-`ui-haf-outage-503-retry-affordance` task adding `SERVICE_UNAVAILABLE` + `details.retriable: true` branching to all four paper-detail SPA call sites (`loadPaper`, `loadEnrichment`, `handleRetract`, `handleCitationExport`) with per-site policy: auto-retry for read paths, manual-only for retract (positive-confirmation UX on destructive write op), bounded auto-retry for citation export. Round-1 hold (6 items, commit `6b05c157`): citation dropdown `:disabled` gate during the ~4s retry window (synchronous flag at handler entry); `handleRetract` comment cited stale slot-burn rationale (rewritten to anchor on stable destructive-write-op invariant per root CLAUDE.md "Comment anchors"); `handleCitationExport` retry leaked paper identity on tab-switch (added paper-identity guard + captured `author`/`permlink` closures for `fetchCitationExport` + download filename); `enrichmentRetriable` hint rendered alongside stale-but-valid reviews data (gated on `!enrichmentLoaded`); `loadEnrichment` `console.warn` was unconditional on recognized retriable-503 (aligned with `loadPaper`'s carve-out per PEvO logging-minimal policy); enrichment retry banner missing `:disabled` guard (added `enrichmentRetrying` flag + `retryEnrichment` wrapper with synchronous entry-flag + re-entrant bail). Round-2 hold (1 item, commit `6433ce2d`): `citeLoading` state-leak on paper-to-paper navigation — round-2's identity-guarded `citeLoading = false` finally caused permanent-disabled-state leak because page-mount.js re-renders Alpine only on route NAME change, not param change, so the same `paperDetailPage` instance persists across `/paper/A → /paper/B`; reverted to unconditional reset (captured `author`/`permlink` closures on `fetchCitationExport` and the download filename already prevent the corrupt-download race the round-1 identity-guarded `finally` was originally guarding) + positive-path vitest pin asserting `citeLoading === false` after a tab-switch divergence. Round-3 review `/ce-code-review` (6 reviewers — correctness on Opus; testing, maintainability, project-standards, julik-frontend-races, learnings-researcher on Sonnet; ce-agent-native-reviewer skipped): zero blocking findings; one informational residual (julik, conf 50) on the post-success 3s cosmetic-Lookup window dismissed per `feedback_dismiss_preemptive_test_hardening`. Learnings researcher cited full alignment with same-day `alpine-persistent-instance-unconditional-ui-flag-reset-2026-05-20.md` (this task is the canonical production-side operationalization of that learning). Cross-refs: backend dep `backend-fetch-paper-detail-haf-error-vs-not-found`, `backend-canonical-root-walker-cumulative-aware`, sibling task `ui-haf-outage-503-retry-affordance` (archived 2026-05-20), `alpine-persistent-instance-unconditional-ui-flag-reset-2026-05-20.md`, `synchronous-flag-before-await-idempotency-guard-2026-05-16.md`.


## UI-PAPER-DETAIL-ORCID-DISCREPANCY-INDICATOR (archived 2026-05-20) — round-3 clean ✓

UI-side rendering of the typed-vs-verified ORCID discrepancy on paper-detail + paper-list + profile-inline-listing surfaces. Canonical-display rule `orcid_verified ?? orcid` codified in `frontend/src/lib/authors.js:canonicalOrcid()` and consumed by 3 render sites (paper-detail byline, paper-card, profile-inline). Discrepancy indicator shows on `shouldShowDiscrepancyIndicator(author)` (added round-2): combines wire-level `orcid_discrepancy === true` with a `orcid !== orcid_verified` values-differ check to suppress the indicator on the case-b server-override path where the backend overrides chain ORCID with `orcid_verified` before returning but still emits `orcid_discrepancy: true` reflecting the pre-override comparison. Round-1 hold (7 items, commit `7a55cca7`): case-b tooltip rendered identical claimed/verified values (added `shouldShowDiscrepancyIndicator` helper); `canonicalOrcid` / `hasOrcidDiscrepancy` had zero unit-test coverage (added `lib-authors.test.js` with 20 cases); paper-card / paper-feed / profile-inline listings had no E2E coverage (added `paper-list-orcid-discrepancy.spec.js`); case-b E2E regression pin added on paper-detail spec; ORCID format validation via `validOrcidOrNull` against `ORCID_RE` to prevent broadcaster trust-spoof (junk strings rendering as green orcid.org links); JSDoc-vs-code semantics alignment (picked truthy-`||` over `??`); collapsed duplicate `paperCard.orcid*` / `paperDetail.orcid*` i18n key sets into a shared `orcid.*` namespace + 15-locale stub re-sweep. Round-2 hold (1 item, commit `b728e00e`): profile-inline E2E xpath `xpath=ancestor::div[contains(@class, "card") or contains(@class, "bg-")][1]` couldn't match the actual `<article class="card">` wrapper profile.js renders — the `ancestor::div` axis filters on element name `div` and skipped the `<article>` row, leaving both discrepant and matching row locators resolving to the outer profile-header card scope; discrepant-row `toHaveCount(1)` would fail in CI, matching-row `toHaveCount(0)` would pass vacuously, so the claimed profile-inline surface coverage was not actually pinned. Round-3 fix replaced with `page.locator('article.card').filter({ hasText: ... })` matching the paper-feed half's pattern, with `await expect(row).toBeVisible()` on both rows providing implicit population wait + positive control against vacuous pass. Round-3 review `/ce-code-review` (5 reviewers): zero blocking findings. Below-gate observations (testing, conf 65 + maintainability, conf 40): full Playwright E2E run deferred per docker-stack policy; same-file positional "matches the paper-feed half above" reference is a soft anchor but no quotable convention clause forbids same-file directional references. Cross-refs: parent spec `architect-orcid-typed-vs-accredited-supersession-spec` (archived 2026-05-16), backend dep `backend-papers-canonical-orcid-resolution`, sibling `backend-profile-papers-supersession-parity`, `tests-must-fail-on-mutation-of-code-under-test-2026-04-22.md` (this fix is the canonical wrong-scope-locator wedge), `structural-mirror-test-silent-staleness-on-wrapper-adoption-2026-05-20.md` (E2E spec authored against a DOM shape production never used).


## BACKEND-COMMENT-ANCHOR-ROT-SWEEP-ACCREDITATION-TS (archived 2026-05-20) — round-4 clean ✓

Four-round task sweeping pre-existing comment-anchor rot per root `CLAUDE.md` "Comment anchors" from `backend/src/routes/accreditation.ts`, `backend/tests/routes/accreditation.test.ts`, and `backend/tests/routes/accreditation-idempotency.test.ts`. Round-1 ran the per-task enumerated sweep + the implementer's own broadened-grep audit; round-2 hold (7 items) caught partial-strip residuals (orphan single-letter prefixes `b:`/`c:`, dangling `(per )` prepositions, bare possessive `'s`) the round-1 grep didn't catch + 5 case-insensitive `Round-2's` / `F[0-9]+:` sites + 1 dangling forward reference; round-2 landed all 7 + 3 additional case-insensitive `BACKEND-*` slug citations the broadened grep surfaced. Round-3 hold (4 items) caught additional partial-strip residuals (orphan `c`/`b` letters, dangling `Mirrors the staging` determiner, unnamed `the discriminator`) the round-2 plus-line audit missed because it didn't re-read the WHOLE comment surface; round-3 holistic re-read landed the 4 + 4 additional sites (Greek-letter `α-disposition` label, dead `item 4` pointer, unnamed-discriminator at L1087).

Round-4 hold (2 items, commit `f7131e39`): both architect-cited `above` relative anchors were prescribed verbatim by the round-2 hold-block — a `hold-block-must-not-contradict-convention-docs` incident per the broadened `convention-enforcing-fix-must-audit-its-own-new-code-2026-05-17.md` self-audit clause that explicitly enumerates `above`/`below`/`next test`/`previous test`/relative positional anchors. Round-4 landed both architect-cited + 21 holistic positional + 2 F-label residuals across all three named files in one commit. `/ce-code-review` on `f7131e39` (5 reviewers — correctness on Opus; testing, maintainability, project-standards on Sonnet; `ce-agent-native-reviewer` skipped per PEvO CLAUDE.md; `ce-learnings-researcher` failed with 529 overload, not gating): zero findings. Broadened acceptance grep returns 5 durable substring matches of `be-{before,after,then}-INCR` (behavioral phrases the architect explicitly accepts) + zero real rot. Bonus correctness improvement at `accreditation-idempotency.test.ts:299` — the round-3 sweep corrected a misnamed event citation (`existing_accreditation_lookup_failed`, a fictional event with no production emit site, → real event `existing_accreditation_gate_unavailable` emitted at the gate-HAF-outage branch). Cross-refs: `task-slug-citations-in-comments-go-stale-on-archive-2026-05-15.md`, `docblock-anchor-stable-symbols-not-line-numbers-2026-05-15.md`, `convention-enforcing-fix-must-audit-its-own-new-code-2026-05-17.md`, `hold-block-must-not-contradict-convention-docs-2026-04-22.md`, sibling-cluster `backend-comment-anchor-sweep-bridge-ts` (round-1 archived same day), `backend-comment-anchor-sweep-supersession-cluster` (round-3 → round-4 hold same day).


## BACKEND-COMMENT-ANCHOR-SWEEP-BRIDGE-TS (archived 2026-05-20) — round-1 clean ✓

Single-round sibling sweep on `backend/src/routes/bridge.ts` per root `CLAUDE.md` "Comment anchors" — follow-up to `backend-bridge-write-haf-lag-and-retry-amplification` round-5 archive, which left coordination-state anchors elsewhere in `bridge.ts` after the narrow round-5 fix. Architect enumerated 10 sites; the implementer's whole-file audit (acceptance criterion #4) surfaced 6 additional sites for 16 total: file-header banner slug citation + `BRIDGE_LOCK_TTL_SECONDS` rationale (`above` as magnitude comparison rephrased to `exceeds` defensively to avoid grep substring match) + `releaseBridgeLock` Lua CAS no-op + `callerLabel` literal-union docblock + HAF-catch fail-closed signal slug + `/check` cache-skip + `/check` `assertNever` exhaustiveness guard + `lookupPreprint` hoist + `/register` lock-acquisition slug + LOCK_HELD vs DUPLICATE structured anchor (`below` relative anchor replaced with `BridgeCheckResult.exists === true` discriminator) + `/register` sibling `assertNever` + `logBroadcastAttempt` factory slug + `getRequiredBridgePostingKey` use site (both `Round-3 hold #6` round-citation AND `BACKEND-BRIDGE-KEY-STARTUP-VALIDATION-AND-PINO-REDACT` all-caps task-slug purged; relative anchor `assertBridgeKeyConfigured above` replaced with `assertBridgeKeyConfigured already returned 503 earlier in the handler`). All anchored on stable behavioral / symbol references. `/ce-code-review` on `7f33ea2e` (5 reviewers — correctness on Opus; testing, maintainability, project-standards on Sonnet; `ce-agent-native-reviewer` skipped per PEvO CLAUDE.md; `ce-learnings-researcher` failed with 529 overload, not gating): clean. Three soft findings surfaced and triaged at archive: (1) P3 conf-35 `earlier in the handler` borderline positional anchor at the `getRequiredBridgePostingKey` site — dismissed per `feedback_dismiss_preemptive_test_hardening` (`earlier` not in broadened-grep enumeration; named symbol carries the anchor); (2) pre-existing `above` relative anchors at L657/L661 introduced by sibling commit `dd3090af` AFTER this sweep's baseline scan — out-of-scope, candidate for a future sweep if/when the area is touched; (3) pre-existing PubMed timeout magnitude inaccuracy at L391 (15s in comment vs 10s in code) — pre-existing in `7f33ea2e^`, out-of-scope per task NON-GOALS. Behavioral verification: 33/33 scoped vitest pass identically before/after (pure comment edits). Cross-refs: `task-slug-citations-in-comments-go-stale-on-archive-2026-05-15.md`, `docblock-anchor-stable-symbols-not-line-numbers-2026-05-15.md`, `convention-enforcing-fix-must-audit-its-own-new-code-2026-05-17.md`, sibling-cluster `backend-comment-anchor-rot-sweep-accreditation-ts` (round-4 archived same day), `backend-comment-anchor-sweep-supersession-cluster` (round-3 → round-4 hold same day), parent task `backend-bridge-write-haf-lag-and-retry-amplification` (archived 2026-05-20).


## UI-ISRETRIABLE503-HELPER-ADOPTION-SWEEP (archived 2026-05-20)

Extracted `isRetriable503(err)` predicate helper to `frontend/src/api.js`, co-located with the `ApiRequestError` envelope definition, and migrated 9 inline `err?.code === 'SERVICE_UNAVAILABLE' && err?.details?.retriable === true` predicates across `paper-detail.js` (6 sites), `profile.js` (2), and `threaded-comments.js` (1) to call the helper. Pure refactor with no semantic change; existing inline-comment retry-policy rationale at each migration site preserved. The three component/page test files inline-redefine the predicate in their `vi.mock` factories rather than importing the real one (accepted tradeoff per implementer comment — divergence-detection lives in `api.test.js`).

Reviewed via `/ce-code-review` on commit `f6095cc8` with 6 reviewers (correctness, testing, maintainability, project-standards, learnings, reliability; `ce-agent-native-reviewer` skipped per root CLAUDE.md PEvO carve-out; `ce-adversarial-reviewer` skipped — 89-line refactor below the 50-line non-test threshold for that persona's high-risk-domain trigger). Clean pass — zero actionable findings. Truth-table unit tests in `frontend/tests/unit/api.test.js` cover all 7 spec-required cases plus 3 bonus shapes (real `ApiRequestError` instance, truthy non-boolean rejection, `details` present but `retriable` missing). Cross-reviewer convergent residual risk on the inline-mock divergence pattern — accepted per PEvO preemptive-hardening-dismissal posture.

**Notes on task-spec accuracy.** The task file claimed "17 inline predicates" and referenced `frontend/src/lib/api.js`. The actual count was 9 and the file is `frontend/src/api.js`. Implementer correctly used the actual count and path; the spec's numbers were write-time estimates that weren't grep-verified. The wrapping-primitive-exhaustive-call-site-audit convention was satisfied — post-migration `grep -rnE "err\??\.code === 'SERVICE_UNAVAILABLE'" frontend/src/` returns zero matches outside `api.js` itself.

## BACKEND-BRIDGE-TEST-FENCE-REPLACE-SETTIMEOUT (archived 2026-05-20) — dedup of work shipped 2026-05-11

Duplicate filing of work that already shipped on 2026-05-11 in `backend(bridge-test-fence): replace setTimeout fences with explicit barriers in concurrent /register spec`. Re-filed at architect archive-intake of `backend-bridge-write-haf-lag-and-retry-amplification` as a carry-forward of the round-2 "at archive" prescription, per `multi-round-task-at-archive-followup-blindness-2026-05-20`. Backend re-review signal verified the existing implementation against all five acceptance criteria: two polling-based barriers `waitForLockAcquired` (polls `fakeRedis.store.has(lockKey)`) and `waitForSetnxBlocked` (polls a per-key `setnxBlockedCount` counter on FakeRedis) replace the prior `setTimeout(r, 5)` + `setTimeout(r, 20)` stagger fences in the concurrent-`/register` spec; both barriers yield via `setImmediate` with a 200ms timeout. Mutation-kill verified by bypassing `acquireBridgeLock` and observing the spec fail red with `lock ... not acquired within 200ms`. No production-code edits; pure test-infrastructure refactor. Architect dedup-archive only — no `/ce-code-review` fan-out because the diff for this round is empty (the implementer commit was reviewed when it originally shipped). Cross-refs: `multi-round-task-at-archive-followup-blindness-2026-05-20.md`.


## BACKEND-BROADCAST-ATTEMPT-HELPER-EXTRACTION (archived 2026-05-20) — dedup of work shipped 2026-05-11

Duplicate filing of work that already shipped on 2026-05-11 in `backend(broadcast-helper): extract makeLogBroadcastAttempt factory` with hold-fix rounds through the round-5 follow-up. Re-filed at architect archive-intake of `backend-bridge-write-haf-lag-and-retry-amplification` as a carry-forward of the round-2 "at archive" prescription, per `multi-round-task-at-archive-followup-blindness-2026-05-20`. Backend re-review signal verified the helper site: `backend/src/lib/broadcast-error.ts` exports `makeLogBroadcastAttempt(eventLabel, baseContext, loggerInstance?)` returning a per-attempt closure; `backend/src/routes/bridge.ts` and `backend/src/routes/custody.ts` both import and consume the factory at factory / success-attempt / outcome-attempt call sites. Wrapping-primitive call-site audit (acceptance step 4): `backend/src/routes/anonymousReview.ts` and `backend/src/routes/account-creation.ts` are the other `broadcastSendOperationsWithTimeout` direct callers but emit no sibling `logBroadcastAttempt` warn pattern today — not a drift risk under the `wrapping-primitive-exhaustive-call-site-audit-2026-04-22` convention's reading (the "every direct caller of the pattern is a structural drift risk" framing applies to call sites that already emit the pattern; forcing adoption at greenfield sites would be adding logs, dismissed per PEvO logging-minimal memory). No code changes this round. Architect dedup-archive only — no `/ce-code-review` fan-out because the diff for this round is empty (the implementer commits were reviewed when they originally shipped). Cross-refs: `multi-round-task-at-archive-followup-blindness-2026-05-20.md`, `wrapping-primitive-exhaustive-call-site-audit-2026-04-22.md`.


## BACKEND-BROADCAST-ATTEMPTS-KEY-IMPORT-SWEEP-REST-OF-TEST-SUITE (archived 2026-05-20) — round-1 clean ✓

Single-round maintainability sweep replacing 13 inline copies of the broadcast-attempts Redis keyspace literal (`${config.appTag}:pending_accred_broadcast_attempts:${token}`) in `backend/tests/routes/accreditation.test.ts` (7 sites including the `broadcastAttemptCount` helper and 4 local-variable / cleanup / pre-seed sites, plus one narrative-comment update) and `backend/tests/routes/accreditation-idempotency.test.ts` (6 sites including the `readBroadcastAttemptsCounter` helper, 2 pre-seed-at-cap sites, and 3 afterEach glob-cleanup sites) with calls to the exported `broadcastAttemptsKey(token)` helper from `backend/src/routes/accreditation.ts`. Continuation of the `backend-verify-cap-redis-flap-recovery` round-3 helper-extraction sweep that already migrated `pending-decrement-queue.test.ts` and `admin.test.ts`. One narrative comment in `accreditation.test.ts` deliberately retains the inline literal because it documents what pino-redaction sees on the wire when ioredis attaches `command.args` to a `ReplyError` rejection (same principle as the `logger-redact.test.ts` carve-out — pino-redaction-anchored literals stay because the comment's subject IS the serialized string shape). Glob suffixes (`accred-idem-*`, `accred-grace-*`) pass through the helper unchanged because `broadcastAttemptsKey(token: string)` performs no token-format validation, making it polymorphic for both literal tokens and glob patterns at `redis.keys()` cleanup sites. The `config` import is retained in both files because many other `config.*` usages (`config.verifyBroadcastAttemptsCap`, `config.smtpHost`, `config.pevoAdminPostingKey`, idempotency-cache keys, accred-verify rate-limit keys) remain. Round-1 `/ce-code-review` (3 personas — correctness on Opus; testing, maintainability on Sonnet; project-standards folded into testing; security/reliability/adversarial/learnings skipped at architect scope on the 71/19 LOC pure-string-substitution diff; `ce-agent-native-reviewer` skipped per PEvO CLAUDE.md): all three returned clean with two out-of-scope observations from maintainability — production source narrative comment at `accreditation.ts` lines 80-83 still describes the keyspace via inline literal (candidate for a follow-up production-comment pass), and the helper parameter name `token` is technically loose for glob-suffix call sites (a `token` / `pattern` overload split would clarify intent but is premature abstraction). Pre-existing 502-vs-503 flake in `accreditation.test.ts` (`pre-INCR redis.eval rejection`) carried forward from sibling tasks' signal blocks unchanged. Cross-refs: `docblock-anchor-stable-symbols-not-line-numbers-2026-05-15.md`.

---

## BACKEND-VERIFY-POST-SUCCESS-RETRY-IDEMPOTENCY (archived 2026-05-20) — round-4 clean ✓

Four-round task making `POST /api/accreditation/verify` idempotent across the AbortError-after-success window so the SPA's 30s fetch-timeout retry on a confirmed-broadcast doesn't surface as a `400 BAD_REQUEST "Invalid or expired token"` that pushes the user through "Request New" → fresh email-link flow despite the accreditation having actually succeeded on chain. Round-1 (`b5f58dc1`) added a `accreditation-completed:<sha256(token)>` Redis record with 24h TTL written atomically with `deleteToken` via `redis.multi()`, plus a token-not-found code path that checks the grace-period record before emitting 400; envelope is byte-identical to the fresh-success 200 so SPA needs no branching. Round-1 hold (9 items, `591fd669`) addressed `hashTokenForLogs` consistency across new log emissions, an `seedAccreditationBonus`-throw catch arm covering the post-broadcast write window, completion-record-write-before-broadcast race coverage, in-memory fallback for the Redis-unavailable case, and stable-symbol-anchored docblocks. Round-2 (`cd10cd03` then re-stamped `980b0764`) addressed those 9 items. Round-2 hold (3 items, `5c3c9541`) covered `recordAccreditationCompletion` pipeline-rejection failure path (test stub shape + JSDoc), an `isRedisAvailable` flap precondition on the retry leg of a healthy-Redis-after-pipeline-rejection scenario, and JSDoc cleanups at `deleteTokenBestEffort` + `recordAccreditationCompletionBestEffort`.

Round-3 (`ccfc6147`) addressed those 3 items. Round-3 hold (2 items, `6f31dba4`): typed-`ChainableCommander` self-referential pipeline stub replacing a nested-object cast that erased the type checker (`as unknown as ReturnType<typeof redis.multi>` at the call site widened to the full interface, so future pipeline-step additions like `.expire(...)` would silently pass against incomplete stub coverage); and a JSDoc backstop-sentence rewrite from a single inaccurate "24h TTL backstop" claim into a per-class breakdown (in-memory fallback record for pipeline-rejection class with flap-resilience within process lifetime; HAF gate + per-token idempotency check for healthy-Redis retry; 24h Redis TTL applies only when pipeline succeeded). Round-4 (`3f19a62e`, 106/9 LOC across `accreditation.ts` JSDoc + `accreditation-idempotency.test.ts` typed stub) landed both items exactly per architect prescription, with additive return-type annotations (`set/del(..._args: unknown[]): ChainableCommander`) and a 5-line stub-rationale comment as strict improvements. Round-4 `/ce-code-review` (3 personas — correctness on Opus; kieran-typescript, project-standards on Sonnet; testing/maintainability/security/reliability/adversarial/learnings skipped at architect scope on the narrow JSDoc + test-stub-shape diff; `ce-agent-native-reviewer` skipped per PEvO CLAUDE.md): all three returned clean with one kieran-typescript residual surfaced for awareness — the realized type-safety invariant is narrower than the round-3 hold block's wording (un-stubbed `ChainableCommander` methods surface at runtime via `TypeError: fakePipeline.<method> is not a function`, only typo'd method names surface at compile-time because `as unknown as ChainableCommander` widens the literal to the full interface) but the round-4 stub is still a strict improvement over round-3's call-site cast and runtime-surfacing is sufficient for a CI-run spec. Architect-zone parallel work landed in commit `66b213ac` (architect-prescribed [TODO Architect] from round 1): `accreditation.md` documents the 24h grace-period idempotency sentence + lossy 3-field replay shape + POST_BROADCAST_OPERATOR_REQUIRED-then-400 retry clarification. Cross-refs: `helper-extraction-express5-response-ordering-2026-04-28.md`, `task-slug-citations-in-comments-go-stale-on-archive-2026-05-15.md`, `docblock-anchor-stable-symbols-not-line-numbers-2026-05-15.md`, `synchronous-flag-before-await-idempotency-guard-2026-05-16.md`.

---

## BACKEND-BRIDGE-WRITE-HAF-LAG-AND-RETRY-AMPLIFICATION (archived 2026-05-20) — round-5 clean ✓

Five-round task closing two concurrent-request race classes on bridge writes (HAF-lag duplicate-check race on `/register`, version-counter race on `/update`) at the application layer rather than relying on chain-side dedup, plus a fail-closed gate on HAF query errors during writes. Round-1 (worker-fanout, integrated as commit `2616cc1` lineage with cherry-pick onto `23bdae9`'s boot-cache pattern) landed the per-permlink Redis SETNX lock on `/register` and per-(author, permlink) SETNX lock on `/update`, both mirroring `withOrcidBindingLock`'s per-acquisition nonce + Lua CAS release pattern; a `BridgeCheckResult` discriminated union for fail-closed-on-HAF-error on the write path while keeping `/check` (read-only probe) fail-open; 3 specs in `bridge-haf-lag-locks.test.ts` (two concurrent same-identifier `/register` → exactly one broadcast + second 409 retriable; two concurrent same-paper `/update` → same; HAF throw → 503 retriable). Round-2 (commit `8f81492`, 9 hold items) split the 409 contract into `LOCK_HELD` (concurrent, retriable) vs. `DUPLICATE` (already-exists, non-retriable) so SPA/integrator code can branch on `err.code` instead of message text, skipped caching of `haf_unavailable` so `/check` never serves a 30-min stale fail-open after HAF recovers in 1-2s, added `assertNever` exhaustiveness guards on both `BridgeCheckResult` switch sites, parameterized `checkExistingBridge` on a `callerLabel` literal-union so `/check` HAF blips no longer false-alert on the `bridge.register` operator dashboard filter, hoisted `lookupPreprint` (15-25s external HTTP) OUT of the lock critical section so in-lock wall-clock stays comfortably under the 35s TTL, inspected the Lua CAS return value and emitted a structured warn on the 0-return TTL-exceeded cascade signal, added `/check` fail-open HAF-throw spec, and added lock-release `finally` correctness assertions on the concurrency specs. Round-3 (commit `7690efd`, 4 hold items) added `fakeRedis.store.has(...)` cache-key-absence assertion on the `/check` fail-open spec (mutation-kills the round-2 cache-skip invariant directly), rewrote a stale `logBroadcastAttempt` docblock that referenced an archived companion task, dropped the `callerLabel` default value to force compile-time enforcement of explicit labeling at every call site, and trimmed the HAF-failure warn message to a disposition-neutral form now that both `/register` (fail-closed → 503) and `/check` (fail-open → 200) call it. Round-4 (commit `fda790f`, 1 hold item, ~1 LOC) trimmed the warn-log message string further: structured `event` and `route` fields carry the fail-open vs. fail-closed disposition; meta-explanation of the data model lives in the inline comment block, not in operator-facing log text.

Round-5 (commit `549d99f0`, 1 hold item, ~1 LOC) dropped a `Round-3 hold item #4: ` prefix from the inline comment block above the same HAF-failure `logger.warn`; behavioral framing preserved verbatim. Convention-enforcing-fix per the task-slug-citations and convention-enforcing-fix-must-audit-its-own-new-code conventions — round-4 was itself a convention-enforcing fix (trimming a log message) and should have swept the adjacent comment in the same edit. Round-5 `/ce-code-review` (3 personas — correctness on Opus; maintainability, project-standards on Sonnet; testing/security/adversarial/api-contract/reliability/performance/data-migrations skipped at architect scope on the ~1 LOC comment-only diff; `ce-agent-native-reviewer` skipped per PEvO CLAUDE.md): clean on the prescribed item; correctness + maintainability + project-standards cross-corroborated one P3 pre-existing observation (9-10 surviving `Round-N hold item` citations elsewhere in `bridge.ts` at lines 96, 204, 209, 213, 301, 324, 388, 420, 458, 512-513; the last embeds task-slug `BACKEND-BRIDGE-KEY-STARTUP-VALIDATION-AND-PINO-REDACT`) — out-of-scope for round-5's narrow prescription. Filed at archive as `backend-comment-anchor-sweep-bridge-ts` (P3, tasks/pending/) alongside the existing in-flight sibling sweep tasks. Architect-zone parallel work landed in commits `938af54` (LOCK_HELD row in `common.md` standard error codes table + `/check` errors section corrected for fail-open envelope) and `66b213ac` (papers.md doc updates landed alongside the walker sibling task). **Known residual (per round-2 architect prescription): the Redis-unavailable degrade path.** `acquireBridgeLock` returns `'unavailable'` and the route falls through unlocked during a Redis outage; this is intentional — it trades the rare-race window for not-503ing every `/register` during a Redis flap (adversarial ADV-002 dismissal). The code comment at `bridge.ts` documents this; the round-2 lock-extension on `BroadcastTimeoutError` (analogous to orcid's A.1 protection) is not present on bridge and would land as a follow-up if duplicate-broadcast-after-timeout becomes a measured problem. Convention docs filed during the task: `read-then-write-races-on-haf-backed-routes-2026-05-15.md` + `redis-advisory-lock-with-lua-cas-nonce-2026-05-15.md`. Followup tasks filed at archive (commit `2822e1f9`, all in tasks/pending/): `backend-bridge-outer-catch-event-discriminators` (P3, outer-catch event taxonomy refinement), `backend-broadcast-attempt-helper-extraction` (P2, extract duplicated `logBroadcastAttempt` from custody.ts + bridge.ts), `ui-bridge-register-lock-held-ux` (P2, SPA affordance for the new LOCK_HELD 409 code), `backend-bridge-test-fence-replace-setTimeout` (P3, replace `setTimeout` stagger with explicit barrier per adversarial ADV-004), `backend-comment-anchor-sweep-bridge-ts` (P3, sibling-rot sweep per round-5 review). EVALSHA migration deferred to existing `backend-redis-script-evalsha-optimization` task in `tasks/review/` per round-2 architect note (scope includes bridge.ts when reviewed). Cross-refs: `chain-write-timeout-ambiguous-outcome-2026-04-22.md`, `read-then-write-races-on-haf-backed-routes-2026-05-15.md`, `redis-advisory-lock-with-lua-cas-nonce-2026-05-15.md`, `task-slug-citations-in-comments-go-stale-on-archive-2026-05-15.md`, `convention-enforcing-fix-must-audit-its-own-new-code-2026-05-17.md`.

---

## BACKEND-HAF-WALKER-WALL-CLOCK-BUDGET (archived 2026-05-20) — round-4 clean ✓

Four-round task adding `AbortController`-bounded wall-clock budgets to the canonical-walker code paths in `backend/src/routes/papers.ts` so degraded-HAF + attacker-controlled continuation chains can't saturate worker threads. Round-1 (commits `1d01a21` + `79078d7` + `741a3e9`) landed the `hafWalkerWallClockMs` config knob (default 3000ms, derived from 50-200ms × 10-15 expected query depth), threaded `signal?: AbortSignal` through 5 walker functions (`findCanonicalRoot`, `resolveContinuationChain`, `fetchHeadAuthorizedAuthors`, `reconstructVersionsFromHaf`, `fetchPaperDetailFromHaf`), wrapped the primary `GET /:author/:permlink` handler in `AbortController` + `setTimeout` + `try/finally clearTimeout`, added the depth-cap arithmetic docblocks (10-hop × 2-query × 30s = 10min backward; 50-hop × ≥1-query × 30s = 25min forward), and bundled an `IS NOT NULL` predicate on the loop-continuation SQL probe so SQL is SSoT for "has continues pointer". Two wall-clock canaries per walker (orthogonal mutation-kill: depth-cap fires before wall-clock under generous budget; wall-clock fires before depth-cap under tight budget) plus an `IS NOT NULL` canary on the loop probe. Round-2 (commit `2494725`, 8 hold items) closed a P1 SECURITY defect — backward walker iter-0 wall-clock abort fail-CLOSED to URL coords instead of the attacker-controlled `pevo.continues` target; the gate previously fail-OPENED to attacker coords, surfacing alice's content under the attacker's URL — plus a P1 DoS escape hatch wrapping `/cite`, `/retract`, `/enrichment` sibling routes so the amplifier closes on all four routes not just the primary GET, a pg v8.x AbortSignal-unsupported docblock correction (real worst-case = `budget + statement_timeout`, ~33s — pg never shipped signal support; manual abort check between queries is the right fallback), a 503 SERVICE_UNAVAILABLE envelope on walker abort (distinguishes degraded HAF from missing content in HTTP-status-only monitoring), a NaN guard on `parseInt(env || '3000', 10)` (`disabled` / `3000ms` env values are truthy, bypassing `||` fallback, producing `setTimeout(fn, NaN)` → immediate-fire on every request), `budgetMs` payload on the 4 wall-clock event emit sites, cache-bypass on abort (`fetchPaperDetailFromHaf` + `fetchEnrichmentFromHaf` return null on aborted signal so the 30-min `hafCache` TTL never poisons with partial-chain data), and a BRITTLENESS WARNING comment on the test-helper `isInitialBackwardProbe` SELECT-clause regex discriminator.

Round-3 (commit `94bf294`, 3 hold items) added `details.retriable: true` to all 5 SERVICE_UNAVAILABLE emit sites (matching the established `bridge.ts`/`accreditation.ts` retriable-503 contract documented in `common.md`), factored `parseHafWalkerBudget(env)` out of `config.ts` (exported for testability) with a 7-case unit-test matrix (unset, empty, `'disabled'`, `'0'`, `'-1'`, `'5000'`, `'3000ms'`), and refined the env-parse helper to use `Number(env)` + `Number.isFinite && > 0` so `'1e3'` and `'1.5'` no longer silently truncate to a 1ms budget. Round-4 (commit `88d2c2ff`, 1 hold item, ~13 LOC test addition) pinned the parseInt → Number migration directly: two new `it()` cases (`'1e3' → 1000`, `'1.5' → 1.5`) that fail RED on a regression reverting `Number(env)` back to `parseInt(env, 10)` — the round-3 7-case matrix passed under either parse formula because `parseInt('5000') === Number('5000')`, leaving the migration motivation with zero mutation-kill until round-4. Round-4 `/ce-code-review` (5 personas — correctness on Opus; testing, maintainability, project-standards, kieran-typescript on Sonnet; `ce-agent-native-reviewer` skipped per PEvO CLAUDE.md): all five returned clean. Architect-zone parallel work landed in commit `66b213ac`: `papers.md` + `common.md` doc updates covering the new 503 behavior on the 4 walker-touching routes. Pre-existing comment-anchor residuals at `config.ts:132` (task-slug citation) and `papers.ts:2138-2139` (triple-rot comment) noted by maintainability for a future sweep task if/when the area is touched; not bundled. Cross-refs: `verify-resource-knob-math-before-load-bearing-security-margins-2026-04-22.md`, `pg-abortcontroller-budget-bounded-by-statement-timeout-2026-05-16.md`, `defense-gated-walker-early-exit-return-discipline-2026-05-16.md`, `task-slug-citations-in-comments-go-stale-on-archive-2026-05-15.md`.

---

## UI-HAF-OUTAGE-503-RETRY-AFFORDANCE (archived 2026-05-20) — round-2 clean ✓

Two-round task wiring the SPA to surface the backend's `503 SERVICE_UNAVAILABLE` retry signal (`error.details.retriable === true`) at the four HAF-translated outage routes from `backend-haf-outage-translation-audit-across-routes`. Round-1 (`18aa311d`) replaced blanket `.catch(() => empty-state)` swallows with discriminating catches on the three with-callers routes (`/api/profile/:username/papers`, `/api/profile/:username/reviews`, `/api/papers/:author/:permlink/comments`), added retry-card affordances + flag state, and documented the contract on the fourth helper (`fetchReview`, no SPA consumers yet); 10 component-tier tests, 3 i18n keys with 15-locale stubs. Round-1 hold (3 items, `cf13513f`): `loadProfile` stale-call guard via local-capture in `fetchProfilePapers().catch()` then assignment after the post-`Promise.all` username guard (the catch was synchronously leaking a prior profile's 503 onto a navigated-to profile's reused Alpine x-data instance); retry-card template tightening (`papersRetriable && userPapers.length === 0` + reviews equivalent) so retry-card and populated-list cannot co-render, plus success-path explicit flag-clear replacing the removed top-of-function reset; synchronous-in-flight guards (`reviewsLoading` in `loadReviews`, separate `_loadInFlight` in `loadComments` since the user-visible `loading` is initialized true for the skeleton render) before any `await`, reset in `finally`, per `synchronous-flag-before-await-idempotency-guard-2026-05-16.md`. Round-2 (`9186f491`, 104+/8- across the 4 files) landed all 3 items plus 4 tests pinning the stale-call guard, in-flight drop on `loadReviews`, success-path clear, and in-flight drop on `loadComments`. Round-2 `/ce-code-review` (5 personas — correctness on Opus; testing, maintainability, project-standards, julik-frontend-races on Sonnet; `ce-agent-native-reviewer` skipped per PEvO CLAUDE.md; `ce-learnings-researcher` skipped; adversarial skipped at 37 prod-code lines below the 50-line threshold; kieran-typescript not applicable to `.js`): 3 findings surfaced, all dismissed during triage. TEST-01 (P2 conf 75, testing): "clears reviewsRetriable explicitly on the success path" test doesn't pin the moved-reset because pre-fix code's top-of-function reset would satisfy the same assertion on a sequential call; the regression class is already covered by the concurrent in-flight-guard test, making TEST-01 redundant documentation rather than missing coverage — dismissed per `feedback_dismiss_preemptive_test_hardening`. JFR-2 (P3 conf 85, julik-frontend-races): `_loadInFlight = true` is set before the `try {` opens, leaving three pre-try property assignments (`this.loading = true`, `this.error = null`, `this.errorRetriable = false`) technically outside `finally` coverage; plain property writes on an Alpine data object cannot throw, runtime risk zero, preemptive hardening against a theoretical future-throw insertion. correctness-2 (P3 conf 75, pre-existing): `loadProfile` itself lacks the synchronous in-flight guard added to `loadReviews`/`loadComments`; reachable via the papers retry-button, but worst case is a duplicate fetch with idempotent writes — the round-1 local-capture handles the flag-staleness class, and the nested `loadReviews` is independently guarded. 4 of 5 personas concurred the round-1 hold items landed correctly; project-standards returned clean. Cross-refs: `synchronous-flag-before-await-idempotency-guard-2026-05-16.md`, sibling `backend-haf-outage-translation-audit-across-routes` (held — provides the 503 contract this task consumes).

---

## BACKEND-COMMENT-ANCHOR-ROT-SWEEP-FRESH-AUTH-AND-REVIEW-TESTS (archived 2026-05-20) — round-2 clean ✓

Two-round task sweeping pre-existing comment-anchor rot per root `CLAUDE.md` "Comment anchors" from three backend test files: `backend/tests/lib/fresh-auth.test.ts`, `backend/tests/routes/review-parity-invariant.test.ts`, and `backend/tests/routes/settings-email-fresh-auth.test.ts`. Round-1 landed the architect-enumerated scope: ~19 sites in `fresh-auth.test.ts` (round-N describe-label suffixes, slug-named describe blocks, inline round-N comments, file-header docblock attributions, section-band slug comment), 3 sites in `settings-email-fresh-auth.test.ts` (file-header slug prefix, "Acceptance criterion #5" prefix, slug-named block-comment band), 3 sites in `review-parity-invariant.test.ts` (header "Acceptance criterion #4", deeper-paragraph slug + round-N, inline slug + round-N prefix). Round-1 hold (3 items): prepend "Hoisted to module scope:" to the `redisAvailable` opening line to preserve the design-intent signal that was lost when "Round-5 hold #8: hoist..." was stripped to "Module-scope ..."; drop `(2026-05-16)` calendar-date qualifier from the `Concurrent dual-consume race` section heading; broaden grep to include `\w+\.ts:[0-9]+` (file:line anchors — the parallel rot class flagged at the sibling accreditation task) and rewrite 4 file:line citations to stable-symbol anchors (`papers.ts:2201-2216` → `fetchEnrichmentFromHaf`; `reputation.ts:562-585` → `paper_reviews` CTE; `orcid.test.ts:207-209` → `config.orcidClientId`/`orcidClientSecret`; `orcid.test.ts:251-277` → `installOrcidFetchStub` helper). Round-2 (`bc0f799`, 49+/11- across the 3 files) landed all 3 items. Round-2 `/ce-code-review` (5 personas — correctness on Opus; testing, maintainability, project-standards, learnings-researcher on Sonnet; `ce-agent-native-reviewer` skipped per PEvO CLAUDE.md; adversarial/security/api-contract/performance/reliability/kieran-typescript skipped per comment-only diff): **0 findings, 0 residual risks** across all reviewers. Symbol anchors verified against the real exported names (`fetchEnrichmentFromHaf` at `backend/src/routes/papers.ts`, `paper_reviews` CTE at `backend/src/reputation.ts`, `installOrcidFetchStub` helper at `backend/tests/routes/orcid.test.ts`, `config.orcidClientId`/`orcidClientSecret` are real config module-level assignments). Self-audit clause holds: no new round-N markers, slug citations, line-number anchors, SHA references, date anchors, or partial-strip stubs in the replacement prose. Sibling task `backend-comment-anchor-rot-sweep-accreditation-ts` held round-2 for 4 partial-strip residuals in `accreditation.test.ts` (same orphan-letter / dangling-determiner class as that task's round-1 hold items 1-5) — independent finding scope from this task. Cross-refs: `task-slug-citations-in-comments-go-stale-on-archive-2026-05-15.md`, `docblock-anchor-stable-symbols-not-line-numbers-2026-05-15.md`, `convention-enforcing-fix-must-audit-its-own-new-code-2026-05-17.md`, sibling `backend-comment-anchor-rot-sweep-accreditation-ts` (held round-2).

---

## BACKEND-MULTI-AUTHOR-CUMULATIVE-UNION (archived 2026-05-19) — round-3 clean, architect doc edits deferred to follow-up ✓

Three-round task replacing the round-3 no-shrink rule on the head-meta override block (formerly at `papers.ts:626-690`) with a cumulative-union construction at the paper-detail surface — displays `detail.authors[]` as the union of `pevo.authors[].hive` across all chain posts, with sub-fields resolved by self-claim-wins-else-most-recent-fallback, and ORCID server-override for accredited targets emitting `orcid_claim_mismatch` audit events on divergence. The structural shift (drops forbidden by construction rather than rejected by check) was articulated as the user's equal-rights trust policy: any author in `pevo.authors[]` can broadcast continuations regardless of when they were added; trust is dynamic; cost falls on the introducer via accreditation revocation cascading through introduced accounts. Round-1 backend signal landed the cumulative-union helper + per-hop cumulative admit-set in `resolveContinuationChain` + `buildCumulativeAuthorsForChain` + accreditation-set + accreditation-orcid-by-account dual lookups in the parallel-fetch block + accredited_authors rebuild from `detail.authors` (closes the round-3 finding-#1 leak by construction). Round-2 (`b22ce5d`+`3b6d781`+`b248761`) closed 5 hold items: ORCID server-override misses "accredited but no on-chain ORCID" spoof case + empty-map cache poisoning in `getAccreditedOrcidsByAccount` when pool is null + 5 task-slug citations replaced with ARCHITECTURE.md § 2 references + 6 round-N citations replaced with permanent behavioral statements + stale `// satisfies no-shrink` comment deletion. Round-3 (`7b109a9` plus production fix in sibling `c2f9c9b`) closed 4 hold items: case-d audit canary asserting `accreditationStatus: 'active'` discriminator + slug citation at `accreditation.ts:173` introduced by sibling commit `0e648b6` replaced with ARCHITECTURE.md § 2 reference + 2 behavioral-statement rewrites of round-N citations introduced by round-2 fix commits + pre-existing file header round-3 reference rewrite. Cluster review 2026-05-19 (5-reviewer fan-out at the architect-context level, low-stakes review per the small comment-cleanup scope) surfaced 1 actionable finding plus 4-7 pre-existing rot anchors in the same test file — ALL bundled into `backend-comment-anchor-sweep-supersession-cluster` per user triage. [TODO Architect] doc edits (ARCHITECTURE.md § 2 "Multi-Author Trust Model" rewrite spanning lines 172-288 + `papers.md` PaperDetail Notes update + 2 convention-doc paragraph updates) deferred to `architect-cumulative-union-doc-edits` (filed in `tasks/blocked/` with `[BLOCKED by Backend]` gate on `backend-cumulative-union-listing-surfaces-parity` archive) so the § 2 rewrite covers cumulative-union (detail) AND listing-surfaces parity in one pass rather than churning the doc twice. Filed at architect-self deferral 2026-05-19; the doc-edit task captures verbatim the 4 [TODO Architect] items plus the round-2 hold's addition of "per-request scope of the drops-forbidden-by-construction invariant." Sibling backend agent landed `backend-cumulative-union-listing-surfaces-parity` Option 4 at commit `e0a82d1`+`6c05266` (architect ratification block landed 2026-05-19 commit `b5a0f92` from a sibling architect session); when that task archives the architect-cumulative-union-doc-edits unblocks. Out of scope: Phase 2 multi-author trust model consent ops (`backend-coauthor-trust-model` rebases on cumulative-union once this archives), bridge-paper update flow retirement (per user call, separate task), N-deep chain progressive author additions (accepted risk under broadcaster-attribution + accreditation cascade). Architect re-review notes carry-forward: backward `findCanonicalRoot` walker NOT modified (its per-hop check is strictly stricter than cumulative-union's per-hop check, fail-CLOSED — no new attack surface; UX edge case filed as `backend-canonical-root-walker-cumulative-aware`). Cross-refs: `pevo-object-identity-is-author-vouching-not-metadata-claim-2026-04-28.md` (the structural convention this gate enforces), `pevo-paper-version-chain-and-edit-semantics-2026-04-30.md` rule #4 (Authors list is monotonic — now reinforced by cumulative-union), `caching-wrapper-discriminated-union-poisoning-2026-05-11.md` (governs round-2 item 2), `accredited-orcid-is-optional-not-edge-case-2026-05-16.md` (four-branch verification lattice), `task-slug-citations-in-comments-go-stale-on-archive-2026-05-15.md`, `convention-enforcing-fix-must-audit-its-own-new-code-2026-05-17.md`, sibling-cluster archives `backend-papers-canonical-orcid-resolution` + `backend-profile-papers-supersession-parity`, sibling-task `architect-cumulative-union-doc-edits` (blocked), `backend-orcid-claim-mismatch-post-revocation-audit` (archived 2026-05-18, source of the emitOrcidClaimMismatchAudit helper extraction round-3 item 1 pinned).

---

## BACKEND-PAPERS-CANONICAL-ORCID-RESOLUTION (archived 2026-05-19) — round-3 clean, cluster follow-ups filed ✓

Three-round task adding read-time supersession projection (`orcid_verified`, `orcid_discrepancy` per `authors[i]`) on `GET /api/papers` (list) and `GET /api/papers/:author/:permlink` (detail) per the four-branch supersession lattice (`accredited-orcid-is-optional-not-edge-case-2026-05-16.md`). Round-1 (`469a571` + `2c40f3a`) landed SQL projection in `authorsWithSupersessionSelect` + JS-side `computeSupersession`/`applyAuthorSupersession` helpers + pre-override chain-orcid capture in `buildCumulativeAuthorsForChain` (so discrepancy correctly fires on chain papers even when server-override unifies `out.orcid` with the attestation) + fallback-path coverage at `?version=N` and `metadata_restored` branches. Round-2 (`d41da25`) closed 9 hold items: hive-key normalization parity via extracted `canonicalHiveKey` helper adopted at SQL JOIN + JS computeSupersession + buildCumulativeAuthorsForChain; SQL/JS empty-string chain-orcid parity via `NULLIF(BTRIM, '')`; `includeAffiliation` parameterization on `authorsWithSupersessionSelect` (PaperSummary omits, PaperDetail retains); fallback-branch supersession canaries. Round-3 (`ed7dfa9`) closed 9 more: SQL TRIM (U+0020-only) vs JS `.trim()` (full Unicode) asymmetry on hive via reject-at-boundary (option a per `sql-trim-vs-js-trim-whitespace-character-set-asymmetry-2026-05-19.md`) — new `trimAsciiSpace` helper + `[a-z0-9.-]+` charset regex guard on BOTH sides; `normalizeHiveAccount` adopted at 2 sibling raw-lookup sites in `papers.ts`; comment-anchor cleanup in tests; whitespace-only chain-orcid bypass closed via BTRIM in SQL CASE + `chainOrcid.trim()` in JS; deleted redundant item-3 behavior canary; rename `canonicalHiveKey → normalizeHiveAccount` across lib + papers.ts + tests; JSDoc fix; mechanical test cleanups. Cluster review 2026-05-19 (10-reviewer fan-out, cross-corroborated with the supersession-cluster sibling reviews) surfaced 4 actionable items, ALL filed as separate follow-up tasks per user triage to avoid expanding hold rounds: `backend-orcid-trim-parity` (P2, BTRIM vs `.trim()` asymmetry on chain orcid — same convention class round-3 closed for hive, left open on orcid; display inconsistency not spoof), `backend-normalize-hive-account-adoption-sweep` (P2, 4 sibling wrapping-primitive call-site misses including 2 P2 privilege-escalation surfaces — accreditedVoteCount predicate + anonymous-review self-block — and 2 P3 UX bugs), `backend-comment-anchor-sweep-supersession-cluster` (P2, 2 self-violation round-N citations introduced by round-3 itself per `convention-enforcing-fix-must-audit-its-own-new-code-2026-05-17.md` + other cluster-wide rot), and `backend-haf-outage-translation-audit-across-routes` (filed from sibling task 4 review). False-positive dismissed at triage: testing T1 claimed `\$` in JS regex literal acts as end-anchor, verified — `\$` is the ECMAScript escape for literal `$`, the SQL-shape canary works correctly. Pre-existing (no action): api-contract AC-1 `accredited_authors: []` on `?version=N` + `metadata_restored` branches. Architect-side contract-doc deferrals settled at round-3 by verbatim emit shape (no boundary rewrite on emitted `authors[i].hive`/`orcid` — only lookup is normalized; consumers compare case-insensitively per the contract). [TODO Architect] items belong to `architect-cumulative-union-doc-edits` (blocked on `backend-cumulative-union-listing-surfaces-parity` archive so § 2 rewrite covers both surfaces in one pass). Cross-refs: `sql-trim-vs-js-trim-whitespace-character-set-asymmetry-2026-05-19.md`, `wrapping-primitive-exhaustive-call-site-audit-2026-04-22.md`, `accredited-orcid-is-optional-not-edge-case-2026-05-16.md`, `task-slug-citations-in-comments-go-stale-on-archive-2026-05-15.md`, `convention-enforcing-fix-must-audit-its-own-new-code-2026-05-17.md`.

---

## BACKEND-PROFILE-PAPERS-SUPERSESSION-PARITY (archived 2026-05-19) — round-2 clean, HAF-outage extension filed ✓

Two-round task extending the supersession projection (`orcid_verified`, `orcid_discrepancy`) to `GET /api/profile/:username/papers` after round-1 of `backend-papers-canonical-orcid-resolution` scoped only to `/api/papers` (list + detail). The profile-papers route assembles rows via `fetchUserPapersFromHaf` → `toPaperSummary` and bypasses the SQL-side `authorsWithSupersessionSelect` projection. Round-1 (`d41da25`) extracted `canonicalHiveKey` + `computeSupersession` + `applyAuthorSupersession` from `routes/papers.ts` to new `backend/src/lib/author-supersession.ts`, extended `toPaperSummary` with optional `orcidMap` parameter, wired `getAccreditedOrcidsByAccount` into the profile route, post-strip affiliation from PaperSummary at `toPaperSummary` (option b — matches the SQL-side `includeAffiliation:false` default), 8 new tests covering the 4-case matrix + mixed-case hive parity + affiliation strip + empty accreditation map. Round-2 (`a419b1d`) closed 7 hold items + 1 drift finding closed during the round: comment-anchor cleanup in `profile-papers-supersession.test.ts` (dropped task slugs + "Round-1 of" qualifier + line-number references); `PaperAuthor` type extended with `orcid_verified?: string | null` + `orcid_discrepancy?: boolean` (option a kept `affiliation?` on the base type — acceptable type-vs-runtime drift); `as unknown as PaperSummary['authors']` double-cast removed at toPaperSummary boundary; HAF-outage on /api/profile/:username/papers now returns 503 retriable instead of 500 (wrapped `getAccreditedOrcidsByAccount()` in try/catch translating to `HafQueryError`; route handler catches and returns 503 with `retriable:true`); mislabeled "degraded HAF / no pool" test rewritten to actually exercise the throw path; `applyAuthorSupersession` spread replaced with explicit projection `{name, hive, orcid, affiliation, ...supersession}` (closes broadcaster-controlled extra-fields leak that SQL path didn't have); `toPaperSummary.orcidMap` made required (dropped dead conditional branch; 4 legacy callers in helpers.test.ts updated to pass `new Map()`); cache-TTL comment in `profile.ts` corrected (was wrongly "30 min", actually 30s default + 10-min underlying accreditation cache); `rows: [] as any[]` → `unknown[]`; plus a discovered drift adoption of `normalizeHiveAccount` at `routes/profile.ts:349-351` (third site closed by the wrapping-primitive call-site audit). Cluster review 2026-05-19 (10-reviewer fan-out cross-corroborated with sibling task 3 review) surfaced 4 findings, ALL filed as separate follow-ups per user triage: `backend-haf-outage-translation-audit-across-routes` (P2, item 3 wrapped one of three HAF-touching sites on the route — `fetchUserPapersFromHaf` still swallows to 200-empty and `getAllAccreditedAccounts` in enrichment Promise.all still throws raw pg to 500; correctness + adversarial + reliability cross-corroborated, broadened scope to all PEvO HAF-touching routes); stale `PaperAuthor.orcid_verified` docblock and other cluster-wide rot bundled into `backend-comment-anchor-sweep-supersession-cluster`; BTRIM/.trim() orcid asymmetry shared with sibling task 3 → `backend-orcid-trim-parity`. Architect-side contract-doc updates landed at round-1: `agents/docs/api-contracts/profiles.md` added authors[i].orcid_verified/orcid_discrepancy field notes with the ~10-min cache-staleness window for this surface (10-min orcidMap cache dominates the 30s response-cache wrapper). Cross-refs: `cross-surface-parity-audit-at-sibling-composition-sites-2026-05-14.md`, `wrapping-primitive-exhaustive-call-site-audit-2026-04-22.md`, sibling `backend-papers-canonical-orcid-resolution` (archived same day round-3 clean, source of the helpers + 4 of the cluster-review findings).

---

## BACKEND-PASSWORD-HASH-NULL-TYPING-AUDIT (archived 2026-05-19) — option-(a) archive, round-4 held item moot ✓

Four-round task widening `accounts.password_hash` from `string` → `string | null` across 5 SQL call sites + canonical hoist + custody-upgrade null-guard with `burnSentinel` timing-equalization + cross-ref polish + `beforeEach` reset + bounded-poll `fetchSettledAuditRows`. Rounds 1-3 (`aa32eca`, `99c6e72`, `009b4a2`) landed the production scope correctly. Round-4 hold reduced to one P3 item (stale `retry: 1` comment value at `backend/tests/routes/custody-upgrade-null-hash.test.ts:117` — should read `retry: 3`). The cited file was deleted by sibling commit `1f1be4e` (`backend(custody-upgrade-seed-phrase-reauth)`) which replaced password re-auth with seed-phrase-derived pubkey + signed-challenge proof, removing the password-hash null-guard branch the test was fencing. Backend correctly parked in `blocked/` rather than guess. Architect-archived directly per disposition option (a): the production scope landed across rounds 1-3 and survived the seed-phrase-reauth migration on the parts that weren't password-specific; round-4's only actionable item is moot. Option (b)'s natural inheritor `BACKEND-RECOVER-TEST-RETRY-SELF-POISONING` archived 2026-05-19 with `recover.test.ts:77` correctly citing `retry: 3` from the start, so the analogous fix is already in place; no further action required. Cross-refs: `ts-closure-denarrowing-nullable-property-hoist-2026-05-04.md`, `timing-equalization-sub-branch-oracles-2026-04-21.md`, `vitest-retry-fire-and-forget-side-effect-poisoning-2026-05-04.md` (the inheritor's primary convention), `auth-structured-log-shape-2026-04-29.md` (fire-and-forget endorsement preserved through this task), sibling `backend-custody-upgrade-seed-phrase-reauth` (the migration that mooted round-4).

---

## BACKEND-SEC-002-HARDENING (archived 2026-05-19) — round-5 clean ✓

P2 post-review hardening of `/api/orcid` follow-up to SEC-002 (auth-gate on `/callback`). Five rounds. Round-1 (commit `0e4241b`) landed the original 6 items: state-consume inside the try/catch around token-exchange dispatch (closes the Redis-throw escape), `NO_ACCOUNT` envelope re-shape (`orcid_id` moved under `error.details` via a generic `ApiError.error.details` widening + `sendError` `details` param), state-not-consumed-on-403 code contract (auth-gate fires before DEL), two-browser-contexts 403 e2e at `frontend/tests/e2e/orcid-link.spec.js:107-176`, HAF-lag TOCTOU mitigation via `${appTag}:orcid_binding:${orcid_id}` cache 120s EX + Redis-first short-circuit in `findAccreditedAccountWithOrcid` (same-tick race split out as SEC-002-TOCTOU-LOCK follow-up — landed separately), `checkOrcidProcessSafety()` startup warn 5s post-listen under `NODE_ENV=production` when Redis not ready. Round-2 hold (commit `ab2baaf`): widened outer try/catch to encompass the upstream `redis.get(stateKey)` + `authenticateRequest` dispatch so infrastructure throws on the read or auth path also map to 500 INTERNAL_ERROR with state preserved (symmetric with the 403 state-not-consumed contract); 4 P3 dismissals (revoke-side cache invalidation, frontend never read top-level `orcid_id`, fixed `setTimeout(5000)` robustness, `@ts-expect-error` cosmetic). Round-3 hold (commit `20dd3ac`): `sendError` `headersSent` guard at `backend/src/response.ts` (defense-in-depth against future SEC-AUTH-BYPASS / SEC-002-TOCTOU-LOCK try/catch widenings around middleware), key-targeted `redis.get` mock swap (`mockImplementationOnce` → `mockImplementation((key) => key === stateKey ? throw : origGet(key))`) so a future upstream `redis.get` can't silently intercept the wrong call, hoisted `verifyHiveSignatureFailureToken` `vi.mock` wrapper defaulting to real-middleware delegation for the `authenticateRequest`-throw → 500 + state-not-consumed spec (architect-authorized targeted mock preserving the SEC-002-BE real-`verifyHiveSignature` invariant), `expect(delSpy.mock.calls.map((c) => c[0])).not.toContain(stateKey)` assertion on the existing 403 spec pinning the state-not-consumed-on-403 contract. Round-4 hold (commit `2eb2efb`): direct `sendError` `headersSent` test coverage in `backend/tests/response.test.ts` (Spec A + Spec B — mutation-deletion of the guard turns Spec A red), enriched warn payload from `{}` to `{reqId: getRequestId(), method, url, attemptedStatus, attemptedCode}` so operators can grep the double-response signal back to the source route, file-level `beforeEach` reset of `verifyHiveSignatureFailureToken.value = false` closing the leak class structurally even if a future spec author copies the round-3 try/finally pattern incorrectly, task-slug parentheticals stripped from 3 sites + 1 Error string per the comment-anchor convention (describe-block landmark `(SEC-002-HARDENING)` left as the family-anchor used by sibling spec comments). Backend ran the architect's mutation-verify recommendation (`inner-catch-shadows-outer-catch-in-route-tests-2026-04-28.md`): stashed the round-2 widening and confirmed the auth-throw spec turns red, ruling out an inner-catch shadowing path. Round-5 hold (commit `b467df5`): one-line addition of `reqId: 'no-request'` to the Spec A `objectContaining({ ... })` matcher so a deletion mutation of `reqId: getRequestId()` at `backend/src/response.ts:27` is caught — round-4 pinned 4 of 5 enriched fields; this pins all 5. Backend ran the targeted mutation-verify: deleting `reqId: getRequestId()` from the production line turns the spec red with the expected missing-property diff. `/ce-code-review` on `b467df5` (5 reviewers — correctness on Opus; testing, maintainability, project-standards, learnings-researcher on Sonnet; `ce-agent-native-reviewer` skipped per PEvO CLAUDE.md): zero findings. Learnings-researcher observation per `mutation-kill-claims-must-match-assertion-and-corpus-2026-05-15.md` (the new assertion kills the key-deletion mutation but does NOT kill a helper-replacement mutation hardcoding `'no-request'` — the test corpus runs outside any request scope so `getRequestId()` returns that literal, the expected and mutated values coincide) dismissed at triage as preemptive-test-hardening: round-5 hold was explicit about the deletion-mutation class and the prescribed fix matches; the helper-replacement mutation is theoretical-only. Deferred `agents/docs/api-contracts/orcid.md` doc updates landed at archive: state consumption semantics block under POST /api/orcid/callback documenting the 403 state-not-consumed contract (legitimate initiator retries `/callback` with a valid bearer without re-running OAuth) and the state-not-consumed-on-infra-error contract (state-read and auth-dispatch throws preserve state via the widened try/catch); NO_ACCOUNT envelope shape already documented inline at `orcid.md:155` per the round-2 superseding decision (no `error.details` payload — the caller already knows the ORCID they submitted); optional `common.md` `error.details` channel note skipped as redundant with the existing `details.*` table usage.

---

## BACKEND-VERIFY-CAP-REDIS-FLAP-RECOVERY (archived 2026-05-19) — round-3 clean ✓

P2 reliability task adding auto-recovery + manual-reset for the `/api/accreditation/verify` broadcast-attempts cap when a Redis flap leaves the counter inflated until 24h TTL. Architect picked option (a) in-process pending-decrement queue over the per-token reconciliation + Lua self-healing alternatives — bounded blast radius, fail-open on restart absorbs into the 24h TTL, single-Node-process per backend container makes the cross-process limitation irrelevant. Round-1 landed `backend/src/lib/pending-decrement-queue.ts` (`Map<attemptId, {token, attemptId, queuedAt, key}>` with `enqueueDecrement`/`drainQueue`/`startDecrementQueueDrainer`/`stopDecrementQueueDrainer`, default 30s drain interval via `VERIFY_DECREMENT_QUEUE_DRAIN_MS`, 1000-entry cap with single overflow warn), `POST /api/admin/accreditation/reset-broadcast-counter` (admin Hive signature against `config.hiveAdminAccount`, body `{ token }`, response `{ token_hash, prior_value }`, structured audit log on success/forbidden/Redis-unavailable), 8 new log discriminators, and 17 new specs. Round-2 hold (7 items): `broadcastAttemptsKey` duplicated verbatim across `admin.ts` and `accreditation.ts` (P1 drift hazard), flat-underscore vs dotted-path event-name inconsistency across all 8 new events (P1 dashboard-filter mismatch), task-slug citations + one round-N reference at 6+ production/test sites (P1 anchor rot), 500-branch spec missing in `admin.test.ts`, MOCK_VERIFY_SIGNATURE carve-out clauses (a)+(c) shortfall, non-atomic `redis.get(key)` + `redis.del(key)` → `redis.getdel(key)`, missing `Retry-After: 30` on admin reset 503. Round-2 commit `a678463` landed all 7 in one bundled commit. Round-3 hold (2 items): comment cross-references in `accreditation.ts` still cited 3 old flat-underscore event names (missed by self-audit per `convention-enforcing-fix-must-audit-its-own-new-code-2026-05-17.md`), local `counterKey` helper duplication in `pending-decrement-queue.test.ts` not migrated to imported `broadcastAttemptsKey` (item 1's deduplication intent only partially achieved). Round-3 commit `12e12f7` landed both: 3 comment cross-references renamed to dotted-path form (`accreditation.verify.broadcast_decrement_*`), test imports `broadcastAttemptsKey` from `accreditation.ts` at 12 call sites, unused `config` import removed. Architect note on the "L243" claim from the round-3 hold: backend correctly identified that the cited `+`-line was a `Round-3 hold #6` round-number comment introduced by `a678463`'s `'enqueued_for_drain'` block, not a flat-underscore cross-ref — that comment's anchor-rot cleanup belongs to the separate `backend-comment-anchor-rot-sweep-accreditation-ts` task, not here. `/ce-code-review` on `12e12f7` (6 reviewers — correctness on Opus; testing, maintainability, project-standards, kieran-typescript, learnings on Sonnet; `ce-agent-native-reviewer` skipped per PEvO CLAUDE.md): zero round-3 findings. One P3 pre-existing maintainability surfacing (5 + 6 duplicate `broadcastAttemptsKey` template strings in `accreditation.test.ts` and `accreditation-idempotency.test.ts`) filed as separate pending task `backend-broadcast-attempts-key-import-sweep-rest-of-test-suite` rather than expanding this task's round-N scope. Pre-existing residual risks accepted as architectural trade-offs: in-memory queue is fail-open on process restart (24h TTL absorbs), `isRedisAvailable` not enumerated in the carve-out's permitted mock-target list (intent covers it; would only bite if list tightens mechanically), module-level `setInterval` cleanup timer runs in test workers (Vitest module isolation limits blast radius).

---

## BACKEND-FRESH-AUTH-CONSUME-REDIS-MEMSTORE-RACE (archived 2026-05-19) — round-3 clean ✓

P1 backend task closing the concurrent dual-consume race on `consumeFreshAuthToken` / `consumeSessionFreshAuthToken`'s memStore fallback path (surfaced by adversarial adv-1 P1 conf 75 on `backend-custody-broadcast-orcid-fresh-auth` round-2). Option B (in-process lock) chosen per architect — a module-scoped `Set<string>` of in-flight tokens with synchronous `has` → `add` critical section before any awaits, shared across both consume helpers because tokens are uniquely bound to one kind at issuance. Round-1 landed the lock + 7 concurrent-consume tests + lock-cleanup wire-shape pin. Round-2 hold (3 items): slug citations in production+test, lock-cleanup test mutation-blind (wire-code collapse between lock-held and consumed-token), shared-lock-domain claim untested. Round-2 landed structural `_getInFlightConsumesSizeForTests` test-only export + cross-helper wire-shape test. Round-3 hold (3 items): residual rot anchors (`Acceptance per task §4`, `(See Option B docblock in lib/fresh-auth.ts.)`), cross-helper test's mutation-kill claim did NOT actually hold under microtask FIFO (architect traced the gap — `catch → memStore.get → memStore.delete` runs synchronously with no intervening `await`, so the second helper sees an empty memStore and returns `expired` whether the lock domain is shared or split), `_setMemStoreEntryForTests` `unknown` parameter surface. Round-3 commit `259498a` landed the Set-identity anchor via new `_getInFlightConsumesSetReferenceForTests(): ReadonlySet<string>` export + a spy-on-`.has` test that runs unconditionally (no `skipIf`, closing the secondary Redis-absent CI gap), plus `StoredEntry | object` parameter widening with JSDoc on the deliberate-misuse contract. `/ce-code-review` on `259498a` (6 reviewers — correctness on Opus; testing, maintainability, project-standards, kieran-typescript, learnings on Sonnet; `ce-agent-native-reviewer` skipped per PEvO CLAUDE.md): zero findings. Mutation-kill verified end-to-end — split-mutation routes session-helper's `.has` to a sibling Set; `sessionHelperCalls = 0`; `expect(sessionHelperCalls).toBeGreaterThan(0)` fails. Kieran noted `ReadonlySet<string>` is a structural overlay (caller can cast back to `Set<string>`); accepted as convention-level for a single-instance Node process. Pre-existing rot in `tests/lib/fresh-auth.test.ts:6` (line-number anchor in header docblock) and `:501` (`BACKEND-CUSTODY-BROADCAST-ORCID-FRESH-AUTH` slug in a describe block) noted out-of-scope; folded into the cluster's downstream sweep task. Pre-existing reliability finding REL-1 (no `commandTimeout` on ioredis client) acknowledged at round-2 — not filed as follow-up; speculative under sustained Redis-hang + retry per `feedback_dismiss_preemptive_test_hardening`.

---

## BACKEND-VERIFYHIVE-AUTHMETHOD-DISCRIMINATOR (archived 2026-05-19) — round-3 clean ✓

P3 backend task adding a `req.hiveAuthMethod: 'jwt' | 'signature'` discriminator to the unified `verifyHiveSignature` middleware so route handlers can distinguish replayable-JWT auth from per-request Hive-signature auth without re-parsing `req.headers['authorization']`. Round-1 landed the field + global `Express.Request` namespace augmentation + `routes/settings.ts` POST `/email` migration to consume `req.hiveAuthMethod === 'jwt'` via a local `isJwtPath` alias + real-path integration test exercising both branches with genuine signed requests + downstream-consumer extension at the fixture-driven settings-email test. Round-2 hold (3 items): 4 sites with task-slug citations + 3 "Acceptance criterion #N" references in test source (the canonical `convention-enforcing-fix-must-audit-its-own-new-code-2026-05-17.md` self-violation — removing a stale line-number anchor and substituting a slug citation), stale clause-(a) mock-carve-out docblock in `settings-email-fresh-auth.test.ts` that claimed the route still reads `req.headers[authorization]` directly and that `req.hiveAuthMethod` would be extracted "once introduced" (internally contradictory with the same diff that introduced it), `MOCK_VERIFY_SIGNATURE` fixture parameter typed `Record<string, unknown>` such that `req.hiveAuthMethod = ...` writes into an `unknown`-valued slot (kieran-typescript: the `'jwt' | 'signature'` union from the global augmentation was not enforced at the write site). Round-3 commit `5fc0cae` landed all three: behavioral anchors at every citation site (`req.hiveAuthMethod`, `isJwtPath`, `MOCK_VERIFY_SIGNATURE`, `verifyHiveSignature`, no-row-before-JWT invariant), clause-(a) prose rewritten to describe the post-migration state with named clause-(c) companion path (`backend/tests/middleware/verifyHiveSignature-authmethod.test.ts`), fixture parameter narrowed to `Pick<Request, 'hiveUsername' | 'hiveCustody' | 'hiveAuthMethod' | 'headers'>` with `import type { Request } from 'express'` added. `/ce-code-review` on `5fc0cae` (6 reviewers — correctness on Opus; testing, maintainability, project-standards, kieran-typescript, learnings on Sonnet): zero findings. Kieran verified `Pick<Request, ...>` actually enforces the `'jwt' | 'signature'` union via isolated `tsc` probe with `@ts-expect-error` assertions on `'bearer'` and `'admin'`; both fire correctly. Test tsconfig at `backend/tests/tsconfig.json` includes `../src`, so the augmentation is in scope during `typecheck:tests`. Self-violation audit clean: no new task slugs, round-N markers, line numbers, or SHAs in the replacement prose. Pre-existing rot in `tests/routes/settings-email-fresh-auth.test.ts` (slug citations at lines 2 + 609, "Acceptance criterion #5" at line 501-502) and `tests/routes/review-parity-invariant.test.ts:5` ("Acceptance criterion #4") noted out-of-scope; folded into the cluster's downstream sweep task. Source: backend-surfaced from `backend-settings-email-reauth-fresh-auth.md` round-1 implementation; round-1 took the inline `authHeader.startsWith('Bearer ')` re-check path to avoid colliding with a sibling in-flight worker editing `verifyHiveSignature.ts`.

---

## BACKEND-VALIDATE-MIDDLEWARE-TYPED-RETURN (archived 2026-05-19) — round-1 clean ✓

P3 backend task teaching `validate<T extends z.ZodTypeAny>(schema: T)` to return a typed `RequestHandler<Record<string, string>, unknown, z.infer<T>>` so route handlers can read `req.body` with the inferred zod type instead of `any`. Surfaced by cluster-B `/ce-code-review` on `backend-verify-cap-redis-flap-recovery` round-1 (kieran-typescript KT-1, 2 `req.body.token as string` casts in `admin.ts`). Commit `6c4c604` lifts `validate` to a generic factory with JSDoc documenting the typed-return contract and the handler-side annotation pattern, plus 5 route handlers annotate `Request<Record<string, string>, unknown, z.infer<typeof schema>>` to inherit the typed body (admin, accreditation × 2 for `/request` + `/verify`, anonymousReview, contact, profile's PUT `/:username/notification-preferences`). Both `req.body.token as string` casts at admin.ts are removed; `routes/papers.ts:2833` `req.body.reason as string` intentionally NOT migrated — its handler has no `validate(...)` middleware on its chain and adding a zod schema is out of scope. `/ce-code-review` on `6c4c604` (6 reviewers — correctness on Opus; testing, maintainability, project-standards, kieran-typescript, learnings on Sonnet): zero findings. Correctness verified each of the 5 schema↔Request-generic pairs aligns (`accreditationRequestSchema`, `accreditationVerifySchema` × 2, `anonymousReviewSchema`, `contactSchema`, `notificationPrefsSchema`); `req.body.token` resolves to inferred `string` from `accreditationVerifySchema`'s `z.string().min(1).max(128)`; strict `tsc --noEmit` passes on the post-commit tree; no other post-validate `req.body.X as Y` casts remain under `backend/src/`. Kieran verified `unknown` for Express's `ResBody` slot doesn't break downstream `sendOk`/`sendError` callers (both accept unparameterized `Response`); `<T extends z.ZodTypeAny>` is the conventional zod constraint; `import type { z }` is correctly type-only in the 4 new handler files; `profile.ts` correctly retains a value-import since it defines `notificationPrefsSchema` inline. Acceptance #2 said "at least three" casts but only 2 existed in scope — honestly disclosed in commit message. Learnings researcher flagged: this is a shared-helper contract change per `helper-contract-flip-untouched-adopter-audit-2026-05-16.md` — correctness confirmed via grep that every `validate(`-using route file with a body-read was either annotated (the 5 above) or has no `req.body.X as` cast remaining. Doc-precision residual: JSDoc says `Record<string, string>` "mirrors Express's `ParamsDictionary`" — technically `ParamsDictionary` is `{ [k: string]: string | string[] }`, so `Record<string, string>` is narrower; functionally correct for PEvO's route params (all single strings). Below actionable bar per kieran. Pre-existing test gap: `routes/contact.ts` has no test file at all — not introduced by this diff; not filed as follow-up.

---

## BACKEND-FLUSH-AND-EXIT-AUTH-CONVERGE (archived 2026-05-19) — round-3 clean ✓

P3 backend task migrating the inline boot-fatal flush + watchdog pattern at `routes/auth.ts:175-193` (the original site that `flushAndExit` was extracted from) to import the canonical helper from `backend/src/lib/flush-and-exit.ts`. Round-1 commit `084d9ee` replaced the inline `setTimeout`/`logger.flush` block with a single `flushAndExit()` call (`-19/+7` LOC, behavior-parity verified — the `typeof logger.flush === 'function'` guard + bare-exit fallback dropped in favor of the helper's 2s watchdog). Round-2 hold (1 item from `/ce-code-review` testing reviewer): stale companion-comment in `tests/lib/flush-and-exit.test.ts` header docblock cited the deleted `routes/auth.ts:175-193` line range. Round-2 commit `0399efa` rewrote the docblock to anchor on `flushAndExit()` and the two mock-based tests below. Round-3 hold (1 item, maintainability): round-number qualifiers (`round-4 hold #1`) and task-slug citation (`BACKEND-BRIDGE-KEY-STARTUP-VALIDATION-AND-PINO-REDACT`) in the describe label + header docblock — rot anchors that appear in every vitest runner output indefinitely and rot when the originating task archives. Round-3 commit `5705a99` landed: describe label stripped of `(round-4 hold #1)` parenthetical; header docblock first paragraph rewritten against stable symbols (`flushAndExit` helper name, `validateConfig()` chain in `src/index.ts` including the `uncaughtException` / `unhandledRejection` handlers, async `.catch` on `SENTINEL_ARGON2_HASH_PROMISE` in `src/routes/auth.ts`). `/ce-code-review` on `5705a99` (5 reviewers — correctness on Opus; testing, maintainability, project-standards, learnings on Sonnet; `ce-agent-native-reviewer` skipped per PEvO CLAUDE.md): zero findings. Correctness verified all replacement-prose anchors against the worktree (`src/index.ts` `validateConfig()` try/catch + handler calls; `src/routes/auth.ts` `SENTINEL_ARGON2_HASH_PROMISE.catch` calling `flushAndExit()`); helper schedules `setTimeout(..., 2000)` then `logger.flush(cb)`, whichever fires first triggers `process.exit(1)`. Mocking-justification paragraph citing `test-mock-carve-out-clause-c-2026-05-04.md` genuinely unchanged. Residual surfaced: pre-existing `Round-4 hold #1 (BACKEND-BRIDGE-KEY-STARTUP-VALIDATION-AND-PINO-REDACT)` citation in `src/lib/flush-and-exit.ts` docblock (sibling production file) noted out-of-scope — same rot class but not flagged in this hold; left for a separate sweep. Source: round-4 architect re-review of `backend-bridge-key-startup-validation-and-pino-redact.md` finding #4 (reliability RR-1 closure).

---

## BACKEND-VERIFY-GRACE-PERIOD-SIBLING-BRANCH-COVERAGE (archived 2026-05-19) — round-1 clean ✓

P3 backend task extending the 24h grace-period idempotency record from the fresh-broadcast path to the two sibling 200-emitting branches of `POST /api/accreditation/verify` (existing-accreditation gate-hit and per-token idempotency-hit). Surfaced by cluster-B `/ce-code-review` on `backend-verify-post-success-retry-idempotency` round-1 (adversarial + correctness residual). Implementation at commit `1e2609a` replaces `deleteTokenBestEffort` with `recordAccreditationCompletionBestEffort` on both branches, using the real HAF `existingForUser.tx_id` / `existing.tx_id` rather than the acceptance-allowed sentinel string — `findExistingAccreditation` filters for accredit custom_json ops where `included_trx_id` is non-null in practice. Two new specs in `accreditation-idempotency.test.ts` assert retry returns 200 with the cached envelope and `broadcastJsonMock` is NOT re-invoked; two existing cleanup-failure specs updated to pin the unified `completion_record_failed_post_success` warn shape. `/ce-code-review` on `1e2609a` (11 reviewers, `ce-agent-native-reviewer` skipped per PEvO CLAUDE.md): zero correctness/security/adversarial findings; one P1 api-contract finding (AC-1: contract guaranteed 4-field envelope on short-circuit branches, retry returns 3-field) triaged to architect-zone and landed in cluster commit `a285ebc` as the lossy-replay paragraph + `POST_BROADCAST_OPERATOR_REQUIRED` retry-400 clarification + new admin endpoint contract row + `admin.ts` log-prefix-table entry + manual-reset runbook + queue-drainer auto-recovery section in the chain-write-timeout convention. **Residual risk** retained at archive: HAF could theoretically return `included_trx_id: null` for legacy/virtual ops; if it did, the record would write `tx_id: null`, the round-2-introduced shape guard would reject on retry, the retry would fall through to 400 BAD_REQUEST. Runtime risk is low — `findExistingAccreditation` filters for proper accredit custom_json ops where the column is non-null in practice; the failure mode (400 = pre-grace-period UX baseline) is benign if it ever occurs. Adversarial reviewer's deeper dive concurred. The task's stale `recordAccreditationCompletionBestEffort` docblock (claims usage is "broadcast-success path AFTER seedAccreditationBonus succeeds" but the helper is now also called from gate-hit + idem-hit branches) is folded into the round-3 hold on the parent task `backend-verify-post-success-retry-idempotency` rather than a separate fix here. Source: round-1 first review; clean.

---

## UI-SETTINGS-EMAIL-SMTP-FAIL-COPY-SOFT-HINT (archived 2026-05-19) — round-1 clean ✓

P2 UI copy-only task softening the en.json `settings.emailVerificationSent` toast for the Option-A SMTP-failure 200 path on `POST /api/settings/email` (per `agents/docs/solutions/conventions/timing-equalization-smtp-failure-mode-oracle-2026-04-22.md`). New English value: `Verification email sent. Check your inbox; if nothing arrives in a few minutes, try again.` — semicolon connector, no emdash. 15 stub locales (ar/cs/da/de/es/fa/fr/he/it/nl/pl/pt/sv/tr/zh) reset to the new English verbatim because the prior translations only covered the first sentence; STUBS.md gained an `### Updated 2026-05-19 (UI-SETTINGS-EMAIL-SMTP-FAIL-COPY-SOFT-HINT)` block listing the 15 locales for translator follow-up. No JS logic change; `frontend/src/pages/settings.js` already references the i18n key, and `frontend/tests/unit/pages-settings.test.js:225` asserts the key (`settings.emailVerificationSent`) not the rendered string, so the existing test continues to pass. `/ce-code-review` on commit `f2c599b` with 5 personas (correctness on Opus; testing, maintainability, project-standards, learnings on Sonnet; `ce-agent-native-reviewer` skipped per PEvO root CLAUDE.md): one P2 finding — maintainability noted that STUBS.md's `## Format` spec documents only the `### Added` heading variant while `### Updated` is now used 3× in the file (2026-05-15, 2026-05-17, 2026-05-19) — dismissed at triage as preemptive hardening (working examples already present in three prior entries; no production failure mode; per `feedback_dismiss_preemptive_test_hardening`). Correctness verified all 16 locale JSONs parse cleanly; canonical English appears exactly once in each; STUBS.md locale list matches disk exactly; the `try again` framing is a generic UX delivery hedge applied uniformly — does not reintroduce a status/UI oracle on success-vs-SMTP-fail. Project-standards verified the no-emdash rule, the bare `ui(settings):` subject prefix, the Co-Authored-By trailer, full zone-audit compliance (all paths within UI zone), and that the task-slug citation is confined to STUBS.md (translator-coordination doc, not source) so the comment-anchor rule isn't engaged. Source: cluster-B `/ce-code-review` finding AC-02 on `backend-change-email-mint-path-and-followups` round-2. Latent follow-up (not filed): the canonical Option-A convention doc could optionally gain a one-sentence note that "UI copy should give users a retry path without naming the failure condition" — architect-owned, separable from this archive.

---

## BACKEND-JSONB-ARRAY-ELEMENTS-LATERAL-GUARD-SWEEP (archived 2026-05-19) — round-3 clean ✓

Three-round P2 task migrating 4 production sites with WHERE-clause jsonb_typeof guards (placebo because Postgres evaluates CROSS JOIN LATERAL BEFORE WHERE) to canonical CASE-WHEN-at-SRF-argument-position form, with per-shape behavioral canaries. Round-1 hold (3 docblock-quality items: line-number rot in canary file headers, duplicated 9-site audit table, round-N + sibling-slug citations) landed cleanly; round-2 hold (1 item: 2 audit-table dispositions stale — `citing_papers` CTE and `authorsWithSupersessionSelect` were marked OUT OF SCOPE but had already been fixed via the now-archived cycle-cascade sibling task) landed verbatim in a 7-line docblock-only re-sync. Round-3 `/ce-code-review` on commit `84a87cf` with 5 personas (correctness on Opus; testing, maintainability, project-standards, learnings on Sonnet; `kieran-typescript` skipped — no TS changes; `ce-agent-native-reviewer` skipped per PEvO root CLAUDE.md): zero findings. Correctness verified both production sites still use canonical CASE-WHEN-at-SRF-arg form (`reputation.ts citing_papers` CTE `CROSS JOIN LATERAL jsonb_array_elements(CASE WHEN jsonb_typeof(citing.json_metadata -> $3 -> 'citations') = 'array' THEN ... ELSE '[]'::jsonb END)` and `hafsql.ts authorsWithSupersessionSelect` `jsonb_array_elements(CASE WHEN jsonb_typeof(...) = 'array' THEN ... ELSE '[]'::jsonb END) WITH ORDINALITY`). Maintainability self-violation audit clean: no new line numbers, task slugs, round-N qualifiers, or SHAs in the replacement text. The two re-synced entries use the short `already correct (CASE-WHEN at SRF arg).` form, matching the `paper_resolved_votes NOT EXISTS` form; the `excludeSelfReviewWhere → already correct, reference implementation (...)` longer form intentionally marks the canonical reference. Sibling cross-reference at `notification-queries-lateral-guard-canary.test.ts` preserved as one-line pointer. Verification gates clean (`tsc --noEmit`, lint). Learnings researcher flagged a possible `/ce-compound` candidate: audit-tables in canonical-home docblocks are a variant of the `enumerated-exemption-lists-are-drift-vectors-2026-04-28` family — a narrow companion convention ("audit-tables enumerating per-site dispositions must be re-synced when cited sibling tasks archive") would close the structural drift loop; not filed at archive but worth tracking if the recurrence pattern shows up again. Original 243-line task body covered: 4-site migration (profile.ts citations CTE, stats.ts total_citations subquery, notification-queries.ts arm 6a + arm 6b), 2 new behavioral canary test files (citations-lateral-guard-canary.test.ts + notification-queries-lateral-guard-canary.test.ts) with per-shape coverage (jsonb null, string, integer, object, plus well-formed positive controls and mixed-batch regression guards), full `jsonb_array_elements` audit across `backend/src/` (9 sites enumerated with dispositions: 4 migrated, 4 already correct, 2 IPFS exempt with structural rationale). Source: `/ce-code-review` of `backend-self-review-exclusion-everywhere` round-4 commit `ba95a4a` (reliability R-1, adversarial #1 + #2, learnings researcher LATERAL evaluation order trap). Cross-references: `pg-jsonb-null-vs-sql-null-use-jsonb-typeof-2026-05-12.md` (canonical convention), `defense-in-depth-canary-must-pin-each-layer-2026-05-07.md` (per-layer canary requirement). The audit-table truth-claim drift between round-2 and the cycle-cascade sibling's archive is the round-3 reason.

---

## BACKEND-TESTS-TYPECHECK-COVERAGE (archived 2026-05-19) — round-3 clean ✓

Three-round P1 task extending `tsc --noEmit` over `backend/tests/` and closing the LogContext typo-bypass class that surfaced when kieran-typescript and maintainability reviewers (cross-corroborated, conf 100) found that `backend/tests/lib/broadcast-error.test.ts` was passing invented field names like `{ user: 'alice' }`, `{ action: 'test' }`, `{ case: '…' }`, `{ run: '…' }` through the `LogContext` interface. The interface existed specifically to catch typo'd structured-log fields at compile time, but `backend/tsconfig.json` had `include: ['src']` so test sites were outside the tsc perimeter and esbuild stripped types without enforcement.

Round-1 (`c90d890`, 2026-05-16) added `backend/tests/tsconfig.json` extending the root tsconfig with `tests/` included, added `typecheck:tests` to `package.json`, and fixed 12 LogContext typo-bypass sites in `broadcast-error.test.ts`. Round-1 `/ce-code-review` (6 personas) raised 3 items: P1 #1 — `typecheck:tests` was added as a standalone script rather than ALONGSIDE the existing `typecheck` script (Acceptance #4 unmet), with `npm run typecheck:tests` exiting non-zero on 249 pre-existing errors across 56 other test files, meaning the gate could not function as a regression net today. P1 #2 — the introduced `TestLogContext = LogContext & Record<string, unknown>` cast widened the contract at 5 sites, reopening the typo-bypass at every cast. P2 #3 — 4 `Parameters<typeof handleBroadcastError>[2]` cast sites used a fragile structural-index query when the exported named type `HandleBroadcastErrorOpts` was already available.

Round-2 (`2c34341`, 2026-05-16) chose path-(b) defer-and-document for item 1: filed `backend-tests-typecheck-residual-drift.md` as a follow-up with per-category triage approach (A real drift / B type-import / C vitest-globals tsconfig / D wot-broadcast TS1309 / E fixture-mock-shape / F per-file exclude), kept `typecheck:tests` standalone, updated Acceptance #4 in the task body with a round-1 hold amendment cross-referencing the follow-up. Chose path-(a) for item 2: promoted `run?: string` to a declared optional field on `LogContext` itself (after the round-5 hold #2 specs proved `run` is a real structured-log marker operators see, not test-only scaffolding), removed `TestLogContext` entirely. For item 3: replaced 5 `Parameters<typeof handleBroadcastError>[2]` casts (one more than the hold cited — inspection found a fifth at HEAD) with `as unknown as HandleBroadcastErrorOpts` named-type form. Tests: 42/42 pass; tsc tests-config delta 0 (the 249-error backlog moved to the follow-up). Round-2 `/ce-code-review` (7 personas) raised 3 comment-anchor-hygiene items: P2 task-slug citation in production `LogContext.run` docblock; P2 task-slug citation in test file header; P3 cosmetic comment-vs-code drift at two inline cast-description sites still saying `Parameters<typeof>[2]` after the casts had migrated.

Round-3 (`8b44450`, 2026-05-18) landed all 3 round-2 hold items in a single header/comment-only commit: rewrote `LogContext.run` docblock dropping the `Backend-tests-typecheck-coverage round-1 hold item 2 (path (a))` paragraph and the "See the task file for paths (b) and (c) that were dismissed" redirect, kept behavioral semantics; rewrote test file header dropping the slug and round-N qualifiers; updated 2 inline comments to describe casts as `as unknown as HandleBroadcastErrorOpts`. The "line ~614" anchor and a `BACKEND-ORCID-BROADCAST-OUTCOME-DISCRIMINATION round-4 hold #1` heading in surrounding unedited comments were explicitly left out of scope per the implementer's tight-scope discipline (architect's cluster-D carry-forward).

Round-3 `/ce-code-review` (6 personas — correctness on Opus; testing/maintainability/project-standards/kieran-typescript/learnings-researcher on Sonnet; `ce-agent-native-reviewer` skipped per PEvO CLAUDE.md; adversarial/reliability/security skipped — comment-only edits): zero in-diff findings. Pre-existing rot at the deliberately scoped-out sites acknowledged; one cross-cutting observation surfaced — 12 `it()` description strings in `broadcast-error.test.ts` embed `round-N hold` qualifiers that appear in vitest output and CI logs (pre-existing across many commits, not flagged as actionable). Cross-refs: `agents/docs/solutions/conventions/task-slug-citations-in-comments-go-stale-on-archive-2026-05-15.md`, `docblock-anchor-stable-symbols-not-line-numbers-2026-05-15.md`, `convention-enforcing-fix-must-audit-its-own-new-code-2026-05-17.md`, `discipline-interface-tsc-perimeter-omission-2026-05-11.md`, `pino-spy-serializer-ordering-trap-2026-05-06.md`, `broadcast-per-attempt-vs-error-event-roles-2026-05-13.md`. Backend: `backend/src/lib/broadcast-error.ts` (`LogContext.run` field declaration), `backend/tests/lib/broadcast-error.test.ts`, `backend/tests/tsconfig.json`, `backend/package.json` (`typecheck:tests` script). Follow-up tracking: `backend-tests-typecheck-residual-drift.md` (archived 2026-05-18 cleared the 249-error backlog), `backend-makelogbroadcastattempt-cast-narrow.md` (cluster-D follow-up, archived same day).


## BACKEND-MAKELOGBROADCASTATTEMPT-CAST-NARROW (archived 2026-05-19) — round-1 clean ✓

P3 cluster-D follow-up from `backend-tests-typecheck-coverage` round-2 review. The round-2 fix for parent item P2 #3 migrated 5 `as unknown as Parameters<typeof handleBroadcastError>[2]` casts in `backend/tests/lib/broadcast-error.test.ts` to the named-type form `HandleBroadcastErrorOpts`. The kieran-typescript residual flagged that the same structural-index fragility class survived at 9 sites in the same test file for the sibling helper `makeLogBroadcastAttempt`. These were intentionally out-of-scoped from the parent task (which targeted only `handleBroadcastError[2]`) and tracked as a carry-forward.

Round-1 (`a076b92`, 2026-05-18) added `export type MakeLogBroadcastAttemptOpts = { info: typeof logger.info; warn: typeof logger.warn }` to `backend/src/lib/broadcast-error.ts` co-located with `makeLogBroadcastAttempt`, updated the function signature to consume the named type (`loggerInstance: MakeLogBroadcastAttemptOpts = logger`) so the type is single-source-of-truth, added the type to the test-file import block, and replaced all 9 `as unknown as Parameters<typeof makeLogBroadcastAttempt>[2]` occurrences with `as unknown as MakeLogBroadcastAttemptOpts`. The two-step `as unknown as <T>` form was preserved (the cast objects inject `{ info: vi.fn(), warn: vi.fn() }` which don't carry every pino-method type; the intermediate `unknown` is load-bearing). Production behavior unchanged — the named type is structurally identical to the inline shape it replaced, and call sites in `backend/src/` (custody.ts, bridge.ts) use the 2-arg form with default `loggerInstance = logger`.

Round-1 `/ce-code-review` (6 personas — correctness on Opus; testing/maintainability/project-standards/kieran-typescript/learnings-researcher on Sonnet; `ce-agent-native-reviewer` skipped per CLAUDE.md; adversarial/reliability/security skipped — small, well-scoped diff): zero findings. `-Opts` naming on a logger-instance argument flagged sub-threshold (the implementer's signal block addressed this — sibling `HandleBroadcastErrorOpts` is the established convention in the file and grep-alignment wins over literal accuracy; `broadcast-per-attempt-vs-error-event-roles-2026-05-13.md` further establishes `makeLogBroadcastAttempt` as a factory whose third parameter is "options to the factory", anchoring the naming decision). Pre-existing rot at `backend/src/lib/broadcast-error.ts:672` (`Round-2 hold #4 of` task-slug citation in unedited section block) noted as residual; out of this commit's scope. Cross-refs: `docblock-anchor-stable-symbols-not-line-numbers-2026-05-15.md` (Parameters<typeof>[N] structural-index is the type-citation equivalent of a line-number anchor), `pino-spy-serializer-ordering-trap-2026-05-06.md` (the documented "Parameters<> collapses overloads" rationale that justifies the migration), `discipline-interface-tsc-perimeter-omission-2026-05-11.md` (named-type protection only fires inside the tsc perimeter; the perimeter extension landed by parent task `backend-tests-typecheck-coverage` is what makes this protection real, not cosmetic). Backend: `backend/src/lib/broadcast-error.ts`, `backend/tests/lib/broadcast-error.test.ts`. tsc clean (root + tests configs), lint clean, vitest deferred to parent's serialized post-fan-out run.


## BACKEND-RECOVER-TEST-RETRY-SELF-POISONING (archived 2026-05-19) — round-1 clean ✓

P3 cluster-D follow-up from `backend-password-hash-null-typing-audit` round-4 review, instantiating the convention `vitest-retry-fire-and-forget-side-effect-poisoning-2026-05-04.md` which had named `backend/tests/routes/recover.test.ts:602-633` as a latent unrepaired sibling. The three-condition trigger: fire-and-forget audit-log write in production (`logCustodyBroadcast(...).catch(() => {})`) + strict-equality count assertion (`expect(rows.length).toBe(1)`) + `vitest.config.ts` `retry: 3` + class-level `beforeAll`/`afterAll` cleanup not `beforeEach`. Attempt #1's audit row remains visible to attempt #2's count assertion, poisoning the mutation-kill ground truth regardless of whether the actual mutation-fence claim is correct.

Round-1 (`96dab9d`, 2026-05-18) added a `beforeEach` reset in the `POST /api/auth/recover — with DB` describe block (`DELETE FROM custody_audit_log WHERE username = ANY($1::text[])` scoped to `TEST_USER` only) and a module-local `fetchSettledAuditRows(pool, username, operationType)` bounded-poll helper (1.5s poll @ 25ms intervals for `>= 1` row, then 100ms settle, then re-SELECT and return). The 100ms settle window catches an in-flight retry-poisoned second INSERT before the count assertion runs — mutation-kill on the over-log production change class. The task body's citation `recover.test.ts:602-633` was stale at HEAD: those lines now test `/resend-verification` null-hash timing and have no audit-log assertion. The implementer found the real three-condition matches at lines 170 (`rejects wrong memo key`, `recovery_failure` op) and 243 (`succeeds with correct memo key`, `account_recovery` op) and fixed both. Production code in `backend/src/routes/recover.ts` UNCHANGED — fire-and-forget audit-log writes preserved per `auth-structured-log-shape-2026-04-29.md` (the convention's "Rejected fix shapes" section explicitly names "await the fire-and-forget call in production" as a rejected remedy). Reference implementation `custody-upgrade-null-hash.test.ts` had been deleted by commit `1f1be4e`; helper signature pulled from `git show 009b4a2:backend/tests/routes/custody-upgrade-null-hash.test.ts`. Helper kept local per task's out-of-scope guidance (sibling test file `custody-consent-ops.test.ts` was concurrently edited in a parallel worktree).

Round-1 `/ce-code-review` (7 personas — correctness on Opus; testing/maintainability/project-standards/kieran-typescript/reliability/learnings-researcher on Sonnet; `ce-agent-native-reviewer` skipped per CLAUDE.md; adversarial/security/api-contract/performance/data-migrations skipped — test-only diff): zero actionable in-diff findings. Two sub-threshold residual items dismissed at triage — `beforeEach` DELETE `.catch(() => {})` swallow-errors pattern (matches existing file convention at `cleanup()` line 70; failure surfaces as noisy mismatch via downstream `toBe(1)` rather than silent pass; preemptive-hardening dismissal). One cross-cutting observation handed to a separate architect action: 5 inline poll-loop sites in `backend/tests/routes/custody-consent-ops.test.ts` (anchor on surrounding `it` block names) use the same fire-and-forget audit pattern WITHOUT the 100ms settle window — coverage-asymmetry rather than active bug (consent-ops has its own `beforeEach` reset neutralizing the three-condition trigger). Filed as follow-up `backend-consent-ops-audit-poll-settle-sweep.md` per the convergence-sweep rule in `worker-fanout-helper-name-divergence-2026-05-15.md`. Cross-refs: `vitest-retry-fire-and-forget-side-effect-poisoning-2026-05-04.md` (canonical convention; this task's repair empties the named-sibling catalog), `auth-structured-log-shape-2026-04-29.md` (fire-and-forget endorsement), `worker-fanout-helper-name-divergence-2026-05-15.md` (convergence-sweep timing), `test-helper-closure-capture-over-arg-threading-2026-05-04.md` (extraction triggers and shared-helper convention). Backend: `backend/tests/routes/recover.test.ts`. tsc clean, lint clean.

