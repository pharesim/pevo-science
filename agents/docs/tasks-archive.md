## UI-CREDIT-OP-PROOF-CACHE-SLOT-KEY — key the credit-op fresh-auth proof cache per slot (archived 2026-06-14) — 2 hold rounds, re-review #2 clean ✓

The SPA fresh-auth proof cache keyed credit ops (`claim`/`approve`/`revoke_authorship`) by paper alone, so two proofs for different slots/subjects on the same paper collided (a slot-2 proof overwriting slot-3, then `POST /api/custody/broadcast` returning 403 `FRESH_AUTH_REQUIRED` `target_mismatch`). Fixed: `cacheConsentOpProof`/`getCachedConsentOpProof` (`frontend/src/lib/fresh-auth.js`) now take optional `authorIndex`/`claimer`, normalize absent values to `null`, and strict-match the full target; the key derives from the BACKEND-ECHOED `author_index`/`claimer` (threaded from `/orcid/callback` `_handleFreshAuth` and the password factor), not a client reconstruction; the credit-op broadcast consumer is `withAuthorshipFreshAuth` (paper-detail claim/approve/revoke). Delivered alongside `ui-multi-author-consent-affordances` (its broadcast-wiring consumer) after the backend echo (item 7 of the archived `backend-authorship-credit-ops-fresh-auth`) unblocked it. Two architect hold rounds, both closed clean: round 1 — the `_handleFreshAuth` echo guard validated only the `(action, root_author, root_permlink)` triple, not the credit fields the cache now keys on (a dropped credit-field echo would cache `undefined → null` while the lookup keys on the real index/subject, permanently missing and re-OAuth-looping with no error surface); fixed to require `author_index` a non-negative integer (slot 0 valid via `Number.isInteger`) for claim/approve and `claimer` a non-empty string for approve/revoke, surfacing `status='error'`/`orcid.verificationFailed` on a malformed echo BEFORE any cache write; plus a slot-0 positive-hit test and a test-header anchor cleanup. Round 2 (re-review #2) — closed the two under-tested guard branches (revoke with missing/empty `claimer`; approve with valid `claimer` but missing/non-integer `author_index`), each asserting the error status/key AND the ABSENCE of the sessionStorage proof write (parsed). Re-review #2 landed at `b1838940` and verified clean (`/ce-code-review`: correctness on the session model, testing + project-standards on Sonnet; ce-agent-native skipped per PEvO — 0 findings); teeth confirmed by mutation (4 tests flip red); pages-orcid-callback 76 green, full unit suite 1525.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## ORCID CALLBACK ERROR MESSAGES: DE-HARDCODE COUNT, FIX MIS-MAPPED + GENERIC ERRORS (archived 2026-06-14) — held items landed, re-review clean ✓

The ORCID callback page (`frontend/src/pages/orcid-callback.js` `_verify` error-branching) mapped backend errors to fixed i18n copy by error code only, producing a hardcoded works count, a mis-mapped VALIDATION_ERROR, and a generic timeout/outage fallback that offered an unsafe one-tap retry. All five acceptance criteria met in the implementation: (1) de-hardcoded the works count — `signup.orcidInsufficientWorks` parameterized to `{min}`/`{have}` fed from the backend's `details:{required,have}` 422; (2) fixed the VALIDATION_ERROR mis-map (accredit-mode "already accredited" 422 no longer borrows the works copy) by discriminating on the works-count `details` shape; (3) de-genericized the 504 `ORCID_PROVIDER_TIMEOUT` and 500 `INTERNAL_ERROR` cases with dedicated copy and a no-one-tap-retry `'timeout'` affordance (avoids the auth-code double-spend the backend's `verify_before_retry` hint warns against); (4) pre-flight note on the signup page covering the self-asserted-vs-external and visibility failure modes; (5) regression coverage asserting the resolved string + interpolated `{min}`. Architect held two polish items, both landed at `338c1286` and verified clean at re-review (`/ce-code-review`: correctness on the session model, testing + project-standards on Sonnet; ce-agent-native skipped per PEvO — 0 findings): (a) `orcid.serviceUnavailable` reworded to the no-button "start over" framing (matching its `'timeout'` affordance and sibling `orcid.providerTimeout`), re-stubbed across all 16 locale files and recorded in STUBS.md under `### Updated`; (b) the `INTERNAL_ERROR` branch comment qualified per `api-contracts/orcid.md` "State consumption semantics" — state is consumed only when the throw is downstream of the consume DEL; a pre-consume throw preserves state; withholding the one-tap retry is correct regardless because the client cannot distinguish the two (comment accuracy only, behavior unchanged). Direct verification confirmed all 16 locales consistent, no emdash in the user-facing copy, the comment matches the contract verbatim, and the suite is green (pages-orcid-callback 76; the task note's "72" was a stale count). Advisory/dismissed: no unit test pins the resolved copy text (pre-existing, theoretical-only).

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## SETTINGS FRESH-AUTH USERNAME_MISMATCH TEARDOWN (archived 2026-06-14) — held items landed, re-review clean ✓

`withSettingsFreshAuth` (`frontend/src/lib/settings-fresh-auth.js`) now tears the corrupted session down on `username_mismatch` (JWT subject vs proof subject diverge) instead of returning a generic `freshAuthFailed`, matching the authorship (`withAuthorshipFreshAuth`) and session-kind (`broadcastWithFreshAuth`) siblings. The shared teardown was extracted to `handleSessionInconsistency()` in `lib/fresh-auth.js` (reason-only gate matching the no-`status` `ApiRequestError` shape). Original implementation reviewed clean; the architect held two items, both landed at `32f0917d` and verified clean at re-review (`/ce-code-review`: correctness on the session model, testing + project-standards on Sonnet; ce-agent-native skipped per PEvO — 0 findings): (1) caller-level `sessionInconsistent` coverage in `pages-settings.test.js` — `handleSetPassword` asserts the held plaintext (`newPasswordInput`/`newPasswordConfirmInput`) is zeroed for XSS-read hygiene with no second toast and no generic error (teeth: removing the `sessionInconsistent` arm falls through to the success path's `toast.show`, failing the assertion), plus a `handleEmailDelete` clean-abort case (no disconnect/navigate/toast); (2) the stale "Shared by both orchestrators" docblock on `passwordPromptMessage` reworded to name the settings + authorship password-factor surfaces (`broadcastWithFreshAuth` has no password prompt). Direct verification confirmed `handleSetPassword` zeroes both inputs before the `sessionInconsistent` early return, `handleEmailDelete` returns before any side effect, and `passwordPromptMessage` has exactly the two claimed callers. Suites green (pages-settings 89, lib-settings-fresh-auth, lib-authorship-consent, fresh-auth-401-retry; 125 total).

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## SIGNUP DUPLICATE-ORCID 409 BURNS THE ORCID NONCE, BLOCKING RETRY (archived 2026-06-14) — Option 2 (terminal 409); architect deliverables landed ✓

Follow-up filed from the `/ce-code-review` of `backend-signup-orcid-duplicate-409`: a duplicate-ORCID `/signup` returns the clean `409 ORCID_ALREADY_LINKED`, but the single-use ORCID verification nonce is consumed (del-on-read) BEFORE the failing INSERT, so a same-`orcid_token` resubmit falls through to the missing/invalid-token `422` rather than reproducing the 409 — a confusing dead-end (pre-existing; the 409 commit only changed the error code from 500). The user chose Option 2 (terminal 409) over Option 1 (re-seed the nonce for a deterministic retry-409): a duplicate-ORCID 409 means the ORCID is already bound to ANOTHER account, so a same-ORCID resubmit can never succeed regardless of nonce state — the real recovery is logging into the existing account, not retrying signup; re-seeding would only reproduce a permanent rejection (cosmetic). No backend code change (the 23505 to `accounts_orcid_unique` to 409 mapping already lands; the nonce-del-before-INSERT ordering is left as-is by design, the SPA owns recovery). Resolved at `fb4f53dc`. Two architect-owned deliverables landed at archive: (1) extended the `/signup` `ORCID_ALREADY_LINKED` entry in `api-contracts/auth.md` with the terminal-recovery semantics (resubmit cannot succeed, nonce consumed so a resubmit yields 422, restart OAuth or log into the existing account); (2) spawned `ui-signup-orcid-already-linked-terminal-409` so the SPA treats the 409 as terminal (a clear "log in instead" affordance, no blind resubmit), mirroring the existing `/orcid/callback` durable-binding 409 handling. No `/ce-code-review` fan-out: the resolution carried no code diff (decision plus task-file only).

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## SignupBodySchema REJECTS password:null, 400s PASSWORDLESS ORCID SIGNUP (archived 2026-06-14) — widen to .optional().nullable(); round-2 (test-isolation + comment hygiene) clean ✓

`POST /api/auth/signup` carrying `password: null` (the passwordless-ORCID signup contract the SPA sends) was 400'd at schema validation because `SignupBodySchema.password` was `z.string().optional()` (accepts `string | undefined`, not null). Widened to `z.string().optional().nullable()` (no `.min(1)`, so a previously-accepted empty string is unaffected); no handler change — `hasPassword` already treats null/empty as no-password, and a null-password account lands `accounts.password_hash IS NULL`. Real-path coverage in `auth.test.ts` (null then 200 + password_hash NULL; omitted then 200; non-null string then 200 + password_hash NOT NULL). Landed at `460cd989`; downstream `ui-orcid-signup-recover-real-roundtrip` unblocked (`220fb9cc`). Round-1 review (`/ce-code-review`, 5 reviewers: correctness + security on the session model; testing/project-standards/api-contract on Sonnet; ce-agent-native skipped per PEvO) clean on the critical axis: a `password_hash IS NULL` account CANNOT be password-authenticated — login + resend-verification guard the null/empty hash BEFORE the argon compare, and ARCHITECTURE.md § 6.1 State C (passwordless ORCID-only) enumerates the state; api-contract clean (`auth.md` already documented `password: null`). Round-1 held two backend-owned items, both landed at `3cf4a609` and verified clean at re-review: (P2) the new test block's `afterAll` cleanup, which deleted by a `0000-0003-${suffix}-%` wildcard over an ORCID band shared with sibling suites under `maxWorkers:2` (a coinciding timestamp last-4 could clobber a concurrent sibling's row mid-test), now scopes the orcid arm to `orcid = ANY([exact 3 seeded orcids])` so it cannot touch a foreign row; (P3) the `SEC-004` tracking-id removed from the source comment (re-anchored on the durable § 6.1 State C reference) and the test describe title, with pre-existing `SEC-004-BE` cites in unrelated files left untouched per commit scope. Re-review confirmed: no `SEC-004` remains in the touched files, the comment anchors on § 6.1 State C, exact-match cleanup, 3 null-password specs green. Dismissed/residual (unchanged): the pre-existing `/api/auth/reset` mailbox-gated password-add path; the documented 403-vs-401 passwordless disclosure (by design).

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## BOOT ASSERTION THAT THE accounts_orcid_unique INDEX PHYSICALLY EXISTS (archived 2026-06-14) — to_regclass boot probe for the sole-guard ORCID index; round-2 clean (round-1 hold was a false positive) ✓

`verifyAppDbMigrationsWith` (`backend/src/app-db.ts`) now runs, after the existing missing-migrations check, a read-only `to_regclass('public.accounts_orcid_unique') IS NOT NULL` probe and throws the same `BootFatalError` (which boot maps to `flushAndExit`) when the sole-guard partial unique index is absent — so a decoupled state (007's `schema_migrations` row present but the index dropped via a hand-run `DROP INDEX`, a rows-only `pg_dump`/restore, or out-of-order hand-apply) fails fast at boot instead of at the first silent duplicate-ORCID write. Gated on `expected.includes('007_accounts_orcid_unique.sql')` for fork-safety; scoped to this one index (not a verify-every-index sweep); read-only, so it respects the "migrations authoritative, no DDL on startup" contract. Real-DB coverage in `app-db-fresh-migrations.test.ts`: cold-apply, `DROP INDEX`, assert the throw names the index, with a guard that 007's row survives so the throw is the index check (not the missing-migration path); mutation-kills (delete the throw body, the spec goes red). Landed at `8ee3ea79`. Round-1 review (`/ce-code-review`, 4 reviewers: correctness on the session model; reliability/testing/project-standards on Sonnet; ce-agent-native skipped per PEvO) clean on substance: `to_regclass` verified correct for a partial unique index against the live DB, fail-fast/boot-ordering correct (probe awaited before `listen()`), standards clean. Round-2 fix `23d54216` re-anchored the docblock off a `updateAccountOrcid` symbol the correctness reviewer claimed did not exist.

RECORD CORRECTION (architect, at archive): that round-1 hold was a FALSE POSITIVE. `updateAccountOrcid` DOES exist at `routes/orcid.ts:2039` — an `UPDATE accounts SET orcid = $1 WHERE username = $2` writer (the accredit/link post-broadcast cascade) with no application-level uniqueness check, exercised via `__test_seams.updateAccountOrcid`. The original comment ("/signup and updateAccountOrcid carry no app-level uniqueness check") was accurate and precise; naming the exact exported symbol was in fact more convention-compliant than the vaguer "the ORCID-write handlers in routes/orcid.ts" that replaced it. The replacement is still accurate (harmless, comment-only), so the code stands as committed and the task is archived clean — but the hold should not have been filed, and the re-review note's "no updateAccountOrcid function exists anywhere" is wrong. Process miss: the architect propagated the reviewer's "symbol does not exist" claim without grep-verifying it before holding; such non-existence claims must be grep-confirmed first. Dismissed (unchanged from round 1): index-name to migration-filename string coupling (intentional fork-safety), missing `statement_timeout` on the boot verify queries (pre-existing, all four).

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## SIGNUP DUPLICATE-ORCID SURFACES AS 500 INSTEAD OF 409 (archived 2026-06-14) — map 23505 on accounts_orcid_unique to 409 ORCID_ALREADY_LINKED; first review clean ✓

A `/signup` that binds an already-used ORCID to a new account row trips the `accounts_orcid_unique` partial index (007) and Postgres raises 23505, which fell through to a generic `500 INTERNAL_ERROR "Registration failed"`. The `/signup` catch (`routes/auth.ts`) now detects `err.code === '23505'` AND `err.constraint === 'accounts_orcid_unique'` and returns the clean `409 ORCID_ALREADY_LINKED` ("This ORCID is already linked to another account") the `/orcid/callback` accredit/link paths already use, gated on the constraint name so an unrelated future unique violation still 500s. The email-duplicate path (409 DUPLICATE before the INSERT) is untouched. Real-path coverage in `auth.test.ts` under the `dbReachable` guard: a prior row holds the ORCID, the second `/signup` (ORCID-only and ORCID+email branches) hits the real index, raises 23505, and asserts 409 + ORCID_ALREADY_LINKED + terminal wire shape. Landed at `edd35067`. First review (`/ce-code-review`, 5 reviewers: correctness + security + adversarial on the session model; api-contract/testing on Sonnet; ce-agent-native skipped per PEvO) clean: correctness empirically confirmed against live pg that a partial-index violation populates `err.constraint = 'accounts_orcid_unique'` (so the branch reliably fires); security confirmed it is NOT an enumeration oracle (reaching the 409 requires a valid single-use ORCID OAuth nonce, so the caller already owns the ORCID) and is a pure error-code remap (the index still rejects the duplicate, no row persists, ARCHITECTURE.md 6.1/6.4/6.5 upheld); api-contract confirmed the 409 byte-matches the existing `/orcid/callback` 409s and is SPA-safe (the SPA branches on `err.code`; both the old 500 and new 409 land in the same generic handler). Architect documented the new 409 on `/signup` in `api-contracts/auth.md` at archive. Triage: the ORCID+email DO-UPDATE-to-23505 test-arc gap dismissed (same catch branch + same constraint gate as the covered plain-INSERT arc); the P2 pre-existing retry-dead-end (the ORCID nonce is burned before the INSERT, so a rejected user cannot retry without re-running OAuth — predates this diff, which only changed the error code) filed as a follow-up task (`backend-signup-orcid-dup-nonce-retry`).

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## ORCID WORKS-GATE: STRUCTURED ERROR DETAILS + THRESHOLD DE-HARDCODE (archived 2026-06-14) — details:{required,have} on the works-gate 422; first review clean ✓

The ORCID works-count rejection (422 VALIDATION_ERROR) in `handleSignup` and `handleAccredit` (`routes/orcid.ts`) now passes a structured `details: { required: config.orcidMinWorks, have: externalWorksCount }` as the 5th arg to `sendError` (which already promotes it to `error.details`), so the SPA renders the real numbers instead of a hardcoded "3". Human message unchanged. New describe block in `orcid.test.ts` drives the count below the threshold (the shared `installOrcidFetchStub` previously only served `works = orcidMinWorks`, so the rejection branch was wholly unguarded): signup at `orcidMinWorks-1` (details deep-equals `{required, have}` with `have` distinct from `required` so a hardcode of either fails loudly) and accredit at 0 (asserts the gate precedes the admin broadcast, no broadcast fires). Landed at `10ac1d14`. First review (`/ce-code-review`, 4 reviewers: correctness on the session model; api-contract/security/testing on Sonnet; ce-agent-native skipped per PEvO) clean: numbers correct and consistent with the message, both branches edited (grep confirms exactly two works-gate sites), owner-only reachable (`have` is the authenticated owner's own ORCID public-works count), no info leak (threshold is public policy), purely additive envelope change, test mutation-kills (remove the 5th arg, both specs red). Architect completed task item 3 at archive: documented `details: { required, have }` on the insufficient-works 422 in `api-contracts/orcid.md`. Dismissed at triage: a test-comment nit (accredit spec comment misattributes the un-accredited state to `hafQueryMock`; behavior correct) and a pre-existing NaN-if-non-numeric-`ORCID_MIN_WORKS` residual (predates the diff).

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## BACKEND-ANCHOR-CANARY-LINE-NUMBER-CITE-EXTENSION (archived 2026-06-14) — extend the standing src/ comment-anchor canary to the line-number-cite rot class; first review clean ✓

Extended the standing canary `tests/eslint/no-stale-comment-anchors.test.ts` with a combined `LINE_CITE_RE` detecting the three documented line-number-cite rot forms in `src/` comments: `<file>.(ts|tsx|js|mjs|cjs|sql):<n>` path cites, `line <n>`/`lines <n>-<n>` references, and a terminal `~<n>` tilde approximation — wired in as a fourth scan class alongside round-N/Option-X.N/role-slug. Closes the detection gap that let the `idempotency.ts` `getPool()` file/line cite survive the big src/ sweep. Load-bearing design choice: the tilde arm fires ONLY when `~<n>` is terminal (followed by EOL or clause punctuation `) ] ; : }` or a `.`/`,` not opening a decimal/thousands group), sparing the ~80 legitimate `~<n>` approximations in src/ (latency, strides, decimals, `~2×`, `~95%`, bare counts) without a fragile unit denylist; documented accepted blind spot is a cite followed by a word (`~337 in foo`). 7 planted positives + 12 planted negatives added to the self-test (`no-bridge-paper-literal` precedent); fixtures live under tests/ (not scanned) so they cannot self-trip. Optional SEC-NNN/BE-NNN tracking-id check omitted by design (0 current instances; a durable id ref need not sit on a solutions/-path line, so DURABLE_PATH_RE could not reliably exempt it — net-negative). Landed at `7ad16d53`. First review (`/ce-code-review`, 6 reviewers: correctness + adversarial on the session model; testing/maintainability/project-standards + learnings on Sonnet; ce-agent-native skipped per PEvO) came back clean — project-standards and maintainability fully clean, acceptance criteria all met, all 19 planted assertions independently verified, live full-src scan 0 hits, new comment text self-rot-free. All findings P2/P3 advisory and latent (0 current src/ instances): a future-code false-positive surface from the all-lines scan (tilde arm matches bitwise-NOT `~0;`, path arm matches string-literal `"redis.ts:6379"`), a symmetric unitless-approx FP (`under ~5.`), a by-design detection leak (misses `L371`/bare `:371`/`@@`/unlisted exts), and a mutation-coverage gap (4/6 extensions tsx/mjs/cjs/sql and 4/5 tilde terminal-punct members ] ; : } droppable without a self-test flip). All dismissed at triage per the preemptive-test-hardening default (theoretical-only, corpus clean, acceptance met).

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## UI-MULTI-AUTHOR-CONSENT-AFFORDANCES (archived 2026-06-14) — unified credit-badge display + Route-2/3 consent affordances + pending-authorships discovery; re-review clean (held items fixed, suite green) ✓

Landed the frontend consent surface over the ratified consented-authorship model (`ARCHITECTURE.md` § 2): a single SPA-computed "credited" badge per paper-detail author (union of the hive-keyed `consented` flag (Routes 1/2) OR an `accepted` `authorship_claims[]` entry (Route 3) — decision β; the old `Confirmed`/`Pending` two-tier claim badge collapsed into it, pending → plain text + affordance), Route-2 accept/resign affordances (net-new), the existing Route-3 claim/approve/revoke ops re-routed through the per-slot-keyed `fresh_auth_proof` wiring, and a pending-authorships discovery surface off `GET /api/me/authorships/pending` (503 fail-closed → retry affordance, never a silent empty). Light-account ops sign via the custody fresh-auth orchestrator (`withAuthorshipFreshAuth`), self-custody via Keychain. Delivered with the sibling `ui-credit-op-proof-cache-slot-key` in one commit (shared `orcid-callback.js`). Round-1 `/ce-code-review` (10 reviewers) held: a BLOCKER red unit suite (`handleRejectClaim`→`handleRevokeClaim` test lag), synchronous double-submit guards on the five consent handlers, extraction of the duplicated password-factor machinery into a shared `mintViaPasswordFactor`/`REMINTABLE_REASONS`/sentinels in `lib/fresh-auth.js`, `username_mismatch` session teardown matching `broadcastWithFreshAuth`, an `authorships` store in-flight load short-circuit, `orcid-callback` `destroy()` return-path scrub, and comment-anchor cleanup; minor #5 (rethrow on second-mint transport error) dismissed as contradicting settings' pinned masking. Round-2 re-review (`3b1ea515`, 9 reviewers; ce-agent-native skipped per PEvO) came back CLEAN on all held items: BLOCKER resolved (197/197 green across the six touched files; full suite reported 1490), the shared `mintViaPasswordFactor` proven byte-equivalent to both prior inline copies (the dismissed second-mint masking is sound — identical pre-existing behavior on both surfaces, pinned by a settings test), the double-submit guards verified synchronous-first with `finally` reset, and `destroy()` clearReturnPath satisfies the documented storage-scope cleanup-parity convention. New non-blocking findings triaged at archive: the `withSettingsFreshAuth` `username_mismatch`-teardown asymmetry filed as `ui-settings-fresh-auth-username-mismatch-teardown` (P2, different surface); pre-existing frontend comment-anchor residuals filed as `ui-frontend-comment-anchor-residual-sweep` (P3); dismissed/accepted — the `authorships.load()` dropped-refresh edge (narrow, self-healing), the `withAuthorshipFreshAuth` `username_mismatch`-gate comment being factually wrong about the error shape (behavior correct — error from `signer.js#broadcastOps` does carry `status`; gate fires on `reason` regardless), the echo-guard stray-field hardening (backend-divergence-gated), minor unit-test gaps, and the now-teardown-only Alpine import. The sibling `ui-credit-op-proof-cache-slot-key` was held round-2 for two echo-guard negative-coverage tests (revoke/missing-claimer, approve/missing-author_index) and is not yet archived.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## BACKEND-ANCHOR-ROT-RESIDUAL-IDEMPOTENCY-CLAIMS-SEC-ID (archived 2026-06-14) — 2 residual comment-anchor cites the src/ canary structurally misses; first review clean (record corrected) ✓

Cleared the two pre-existing comment-anchor residuals the standing `src/` canary cannot catch (both comment-only, `f50000e8`). (1) `lib/idempotency.ts`: a `(see ... src/db.ts)` file-path cite re-anchored on the exported symbol `getPool()` (verified exported in `db.ts` with `max:3` / `connectionTimeoutMillis:5000`, ~57 call sites, so the anchor is durable). (2) `routes/claims.ts`: the revoke-gate authorization comment dropped a `SEC-003-BE` id and re-anchored on behavior ("Including it would mean every authenticated user could revoke any claim on any bridge paper"), WHY preserved. First review (`/ce-code-review`, 6 reviewers incl. correctness + security on the session model; ce-agent-native skipped per PEvO) clean: comment-only confirmed by diff, the revoke gate is a pure `isPostAuthor || isClaimer || isAdmin` OR with `verifyHiveSignature` intact, the security WHY is accurate, no new rot class introduced. RECORD CORRECTION (architect, at triage): the implementer's "SEC-003-BE resolves nowhere / dead pointer" rationale was factually wrong — `SEC-003-BE` resolves to the durable `solutions/conventions/test-config-mock-distinct-role-accounts-2026-04-21.md` (which documents it as the P0 bridge-account-equality gate incident, finding B5) and is the organizing `describe()` id of `claims.test.ts`. The behavioral re-anchor is nonetheless convention-compliant (behavior is the preferred anchor and the WHY is preserved), so the code stands as committed; only the rationale was inaccurate. Follow-up filed to extend the canary to the line-number cross-reference class that let the `idempotency.ts` site slip through (the canary covers round/Option/role-slug classes, not line-number cites).

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## BACKEND-HAF-QUERY-COMMENT-ANCHOR-SWEEP (archived 2026-06-14) — full backend/src comment-anchor sweep + citationsArrayGuardSql extraction + standing canary; round-3 (planted-positive) clean ✓

Full `backend/src/` comment-anchor-rot sweep (user-scoped to `src/`, broadened from the architect's 3-file unblock after a fresh re-enumeration found rot in ~34 files). Removed round/hold ordinals, `F-finding`/`item #N`/`Option A.N` labels, orphan task slugs (uppercase `BACKEND-`/`BE-`/`SEC-` and lowercase `<role>-<kebab>` plus dead `.md`/`see task` redirects), and line-number/SHA cross-refs — all re-anchored on stable symbols/behavior; load-bearing WHY docblocks preserved, durable `solutions/` + `api-contracts/` refs deliberately kept. Extracted `citationsArrayGuardSql(citingAlias, appTagParam)` in `notification-queries.ts` (mirrors `imageSrfGuardExpr`; alias identifier-validated, appTag a bound `$N`) replacing the verbatim arm 6a/6b SRF guard with byte-equivalent SQL, preserving the LATERAL-before-WHERE invariant. Landed across `324ca283` (sweep + extraction), `4c172629` (round-2: 6 `orcid.ts` `A.1` orphans re-anchored, 8 lowercase dead slugs cleared, new standing canary `tests/eslint/no-stale-comment-anchors.test.ts` walking every `src/*.ts` for round/Option/role-slug rot with a durable-path carve-out), and `2dfb9acb` (round-3: planted-positive/negative self-test so a corrupted detection regex cannot silently no-op). Reviews: round-1 sweep clean (extraction equivalence proven); round-2 held then cleared the A.1 orphans, lowercase slugs, and canary-tightening; round-3 held one test-quality item (the canary asserted only `violations==[]`, which a broken regex satisfies vacuously), closed by a third `it()` block asserting each regex fires on known-bad anchors and spares legitimate/durable tokens (both halves of the carve-out). Final re-review (5 reviewers) clean; all 11 assertions independently verified against the regex semantics and the canary runs 3/3 green. Left for a future `tests/` sweep: test-header rot in `profile-reviews-accred-gate` / `reputation-batch-internals`.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## BACKEND-SIGNUP-ACTIVATION-FAILURE-RECOVERY-AND-POOL-HOLD (archived 2026-06-14) — crash-recoverable activation + Redis fail-closed per-token lock; round-3 (test-effectiveness) clean ✓

Reworked `/api/auth/confirm` + `/api/auth/link` activation so the irreversible `createClaimedAccount` broadcast is deduplicated under concurrency, pins NO pool connection across the ~30s broadcast (Facet 2), and fails recoverably (Facet 1). Mechanism: a per-`auth_token` Redis activation lock (`lib/signup-activation-lock.ts`) replacing the cross-broadcast pg advisory lock; encrypt-before-broadcast (key-config error → 500 before any claim-token burn); a `resumeChainExists` crash-resume that finalizes WITHOUT re-broadcasting; `409 LOCK_HELD retriable` instead of a spurious 55P03→500. Re-implemented onto refactored main (the `225d3ebe` WIP branch was infeasible against main's independent `withSignupActivationLock` refactor — branch obsolete, do not merge). Round-1 review held 7 items; round-2 (`89d0a067`+`1b6a5eff`) landed them — two P1 security items: item 1 a `STUCK_RECOVERY_WINDOW` (1h) recency guard on both stuck-recovery lookups + migration `016_accounts_updated_at.sql` (with the architect-required `COALESCE(created_at, NOW()-1day)` back-fill before SET NOT NULL, so no ~1h post-deploy bypass), item 2 fail-closed-on-Redis-down (in-memory fallback removed → retriable 503). Round-2 re-review (10 reviewers) found the 7 items code-correct but held 5 test-effectiveness/docstring items; round-3 (`f01e0aa2`) closed them and came back CLEAN, mutation-verified: deleting the `updated_at` clause flips BOTH stale-row tests red, the `/confirm` test now stubs getAccounts to pass the ownership probe (isolating the guard), the `/link` mirror drives REAL `verifyHiveSignature` (400 not 401 proves the signature verified), and the `refundStatusCodes:[409]` unit tests pin both 409-refunds AND 400-still-consumes (brute-force protection). Lock/rateLimit round-3 changes were comment-only (zero executable-token diff). ARCHITECTURE §6.1 now enumerates `updated_at` as a recency-marker overlay (architect-landed). Decision A (accept migration), Decision B (best-effort-JWT contract for /confirm) recorded.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## BACKEND-CONSENTED-ROUTE1-BRIDGE-PREDICATE-HELPER (archived 2026-06-14) — route consented_authors Route-1 bridge arm through validPevoPaperWhere; clean (proven equivalence) ✓

Centralized the `consented_authors` Route-1 bridge-account predicate (architect follow-up filed from the bridge-exclusion review). The Route-1 arm hand-rolled `(type = 'bridge_paper' AND author = $bridge)` inline; routed it through `validPevoPaperWhere({source:'bridge'})` so the bridge-identity predicate has a single source of truth (the Route-2 ORCID guard already uses the helper) — if its discriminant ever gains a third condition, both gates move together. Landed at `dbacf066` (1-line change). First review (correctness + testing + project-standards) clean: correctness PROVED the emitted SQL is semantically identical (AND-commutativity; the helper's parens around the type expr are precedence-neutral since `->`/`->>` bind tighter than `=`; no param re-allocation) and ran typecheck + consented-authors real-postgres 13/13 green; testing confirmed the Route-1 bridge self-consent pins still hold corpus-wide and per-paper; project-standards confirmed it restores `pevo-object-identity-is-author-vouching-not-metadata-claim`.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## BACKEND-EXTRACT-CHAIN-CUMULATIVE-HELPER-TO-LIB (archived 2026-06-14) — option-(b) lib extraction of cumulative helpers + chain walkers; first review clean (faithful move) ✓

Architect-directed option-(b) extraction: moved the cumulative-authors helper AND its chain-walker dependency closure out of the 3500-line `routes/papers.ts` into neutral lib modules, for clean routes→lib layering with zero lib→route edges. `safePevoMeta` → `helpers.ts`; cumulative helpers (`resolveChainCumulativeAuthors` [signature unchanged incl. `prebuiltChainPosts`], `computeChainCumulativeFromHaf`, `buildChainCumulativeFromPosts`, `buildCumulativeAuthorsForChain`, `emitOrcidClaimMismatchAudit`, `hivelessCompositeKey`, the new shared `enrichRowsWithChainAuthors` consolidating the listing + profile per-row loops with the affiliation-strip + `length>0` takeover gate folded in, + types) → `lib/chain-cumulative.ts`; the three chain walkers (`resolveContinuationChain`, `findCanonicalRoot`, `reconstructVersionsFromHaf`) + support + `CHAIN_CUMULATIVE_AUTHORS_TTL_MS` → shared `lib/chain-walkers.ts` (called by papers.ts on non-cumulative detail/version paths). Landed at `022a7d0b` (the redo on the correct post-sibling-archival base; the first fan-out attempt on a 53-commit-stale papers.ts was discarded). First review (`/ce-code-review`, 7 reviewers: correctness + adversarial on the session model; performance/testing/maintainability/project-standards + learnings on Sonnet; security/data-migration/reliability/ce-agent-native skipped — pure internal move) came back CLEAN: correctness diffed every relocated body against `022a7d0b^` and found them byte-identical modulo the `export` keyword (cache key `chain-authors:<a>:<p>` / TTL `1_800_000` / `getOrSet` epoch-guard preserved; both surfaces' fallback/strip/takeover behavior preserved in the merged helper), the hard invariant holds (zero `./papers.js` imports for moved symbols, zero lib→route edges, verified by grep + maintainability), project-standards found no anchor rot (only stable `solutions/` refs), testing confirmed both test re-points correct (the profile fallback `vi.mock` targets `enrichRowsWithChainAuthors` — the handler's real call boundary — not the inner symbol, so it is not vacuous; 19/19 + 2/2 green), and `022a7d0b` confirmed on-main (`git merge-base --is-ancestor`). typecheck + the cumulative/parity/error-isolation suites green. One P3 dismissed at triage (preemptive hardening): the profile fallback test now mocks the whole `enrichRowsWithChainAuthors` (post-gate `new Map()`), so the helper's `length>0` empty-cumulative gate is exercised by `papers-cumulative-route-error-isolation-mocked.test.ts` (real helper, both surfaces) rather than a dedicated named assertion — code verified byte-identical and transitively covered.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## BACKEND-BRIDGE-EXCLUDE-ROUTE2-ORCID-CONSENT (archived 2026-06-14) — bridge NOT EXISTS guard on the Route-2 ORCID consent anchor; first review clean ✓

Excluded bridge papers from the Route-2 ORCID eligibility arm of `consentedAuthorsCteBody` (`hafsql.ts`). A bridge slot's ORCID is external preprint metadata (self-asserted at arXiv/Crossref), so before this change an attested account whose ORCID matched a bridge slot could `author_accept` into reputation/citation credit by ORCID-equality alone, bypassing the deferred verified bridge-claim flow. Fix (f2202a12): a `NOT EXISTS` subquery on the `claimed_orcid_slots` arm of `consent_signer_eligibility`, composed from `validPevoPaperWhere({ source: 'bridge' })` (no new param) — bridge papers stay single-consented (the bridge account via the Route-1 arm) until the verified claim flow lands. The guard sits inside the single builder, so it applies identically to the reputation cycle (signers-scoped) and every display surface, preserving cycle-vs-display credited-set parity. Defensive: no live exploit (no current bridge slot ORCID matches an attested account). A real-postgres regression (`consented-authors-bridge-orcid-exclusion-real-postgres.test.ts`) pins the inverse boundary the sibling corpus omits — attested ORCID matches a bridge slot → NOT consented — with a native-paper no-regression control and a signers-scope parity spec. First review (`/ce-code-review`, 8 reviewers: correctness + security + adversarial on the session model; performance/testing/maintainability/project-standards + learnings on Sonnet; ce-agent-native skipped per PEvO) came back clean: the `NOT EXISTS` correlates on the slot's own canonical-chain root `(author, permlink)` (unique Hive key, no wrong-row/over-exclusion), correctness mutation-probed the guard (removing it leaks claimer into the bridge set, native control stays green), adversarial grepped all callers and confirmed parity, security traced `bridge.ts` keeping `hive: null` on bridge slots (so the unguarded hive-anchor arm is structurally unreachable for bridge slots), and learnings confirmed alignment with `pevo-object-identity-is-author-vouching-not-metadata-claim`. Two non-blocking triaged notes: (1) the `consented_authors` Route-1 arm still hand-rolls the bridge predicate the new guard centralizes — filed as `backend-consented-route1-bridge-predicate-helper` (pre-existing, P3); (2) a P3 sub-gate advisory that the docblock frames the hive-arm's no-guard as structural rather than importer-policy-enforced — dismissed at triage (intentional defensive posture, `hive: null` enforced write-side, no live exploit). A security false-positive ("lint clean unmet" from `'bridge_paper'` literals in the test) was verified and dismissed: `npm run lint` is `eslint src/` (exits 0) and 20 sibling test files use the identical literal.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## BACKEND-PAPERS-LISTING-VOTES-AGGREGATE-CTE (dropped 2026-06-14) — sort=votes correlated subquery is already index-optimal; the aggregate CTE is a ~3300x regression ✗

Architect decision (option 1, drop): the parent papers-listing task's "third structural arm" — replace the per-row `accreditedVoteCount` correlated subquery on `sort=votes` with a votes-aggregate CTE — does NOT transfer from citations to votes. Backend implemented `accreditedVoteCountsCteBody` exactly as specified and ran EXPLAIN on the live Mahdi HAF over the same page: the correlated form costs ~206,927 (each paper's votes resolve via an `Index Scan using hafsql_author_permlink_idx` — a cheap paper-scoped indexed point-lookup), while the aggregate CTE costs ~691,396,430 (~3300x) because the `GroupAggregate` groups across ALL papers, cannot use the author/permlink index, and falls to a `Parallel Seq Scan on operations` over the ~5e8-row vote corpus plus a ~1.2e9-row DISTINCT-ON sort (the real-path `papers.test.ts` hung and was killed under this plan). The citation arm was a CTE win precisely because reverse-citation ("who cites X") has no index; votes are the opposite case. The candidate-paper-scoped re-scope (option 2) was judged unlikely to beat the already-indexed correlated form materially and not worth an EXPLAIN bake-off. Nothing landed (the investigation code was reverted). Durable lesson: collapsing a per-row correlated subquery into a corpus-wide aggregate CTE only wins when the correlated form lacks a selective index for its per-row probe; it is a regression when the probe is already an indexed point-lookup over a bounded page set. Dropping this also clears the last papers.ts-sibling gate on `backend-extract-chain-cumulative-helper-to-lib`.

## BACKEND-ACCRED-STATE-READ-TIEBREAKER-ESLINT-GUARD (archived 2026-06-14) — pevo/no-accred-state-read-missing-id-tiebreaker rule + RuleTester suite; round-1 5-item hold clean ✓

ESLint rule flagging HAF custom_json accreditation-state reads (action IN accredit/revoke; latest-wins LIMIT-1 or OVER/ROW_NUMBER) whose `ORDER BY block_num DESC` lacks the `id`/`op_id` secondary key — structural enforcement for a drift class that recurred twice via syntactic sweeps. Round-1 hold (5 items, d8afb592): added the missing RuleTester suite (17 cases pinning every fire/skip path + a meta-mutation pair that lints the real site literals with the tiebreaker stripped); widened the gate to the bare ranked-CTE alias form (`ar.action`, excluding `transaction`/`my_action` via `\b(?:\w+\.)?action`) to match the docstring; added `classifyLatestWinsTiebreaker` returning missing/extracted/null so a `${...}` fragment extraction reports a distinct `extractedFragment` diagnostic instead of silently disarming; documented the DISTINCT ON evasion gap (pinned by a valid test) and suppression discipline. Re-review (correctness + testing + maintainability) verified all 5 landed: bare-alias regex traced sound, three-state classifier correct on both OVER-body and LIMIT-1 arms, zero genuine suppressions, no shared-`foldStringExpr` fork. Residual (dismissed): the meta-mutation case reproduces the non-exported inline literals rather than importing them (importing would defeat the rule's purpose — it exists because these are inline) and there is no automated `eslint src/` CI gate (pre-existing project infrastructure; the rule fires on real code under the standard `npm run lint`).

## BACKEND-CONSENTED-SET-DISPLAY-SELF-DEALING-EXCLUSION (archived 2026-06-14) — display aggregates exclude self-dealing by the full credited set (claims ∪ consented); round-2 clean ✓

Extended the display self-dealing exclusion from accepted-claims-only to the full cycle credited set (accepted claims ∪ consented authors, Routes 1/2/3 minus demotions) via `excludeConsentedSelfWhere` + `consentSeedCteBody` + `consented_authors AS MATERIALIZED`, ANDed at all seven display surfaces, with EXPLAIN evidence on the listing path. Round-1 (3 items): enrichment routed through the badge's shared volatile cache entry; new_review docblock enumeration updated to the credited-set pair; degrade describe reworded. Round-2 (1 item, d64f5b4d, comment-only): the shared-entry comments no longer overclaim — cross-surface cache reuse holds only when both surfaces resolve the same canonical (author, permlink) pair (root-URL requests); the badge canonicalizes via findCanonicalRoot, the enrichment route keys on its caller-supplied pair, and the cache key always matches the loader args so a miss costs one extra query, never a wrong-paper result. Re-review confirmed comment-only and factually accurate against both handlers.

## BACKEND-NOTIFICATION-VOTE-ARMS-ID-TIEBREAKER (archived 2026-06-14) — same-block id tiebreaker on the DISTINCT ON vote arms; first review clean ✓

The notification vote arms (2a/2b/2c) deduped via `DISTINCT ON (author, permlink, voter) ORDER BY ... block_num DESC` with no secondary key, leaving the kept variant of a same-block vote/revote pair planner-dependent. Fix (5d7f4ab9): added `v.id DESC` (the monotonic op-id, projected `v.id AS op_id`, mirroring reputation.ts `paper_latest_votes`) to all three vote arms' ORDER BY — order-only, the DISTINCT ON prefix is unchanged so the result set is identical; only which same-block variant is deterministically kept changes. The earliest-wins arms (reviews, reply, citations, claims) were deliberately left key-free with a tie-insensitivity rationale (every emitted field derives from the dedup key + block_num). Re-review (correctness + testing): verified order-only, v.id after block_num in all three arms, the op-id column exists, and canary #7 now pins the full tie-broken ORDER BY so a dropped key fails red.

## BACKEND-NOTIFICATIONS-DIGEST-WINDOW-CURSOR (archived 2026-06-14) — wide-floor digest consumption + advance-to-highest-delivered; final cadence-prose item clean ✓

The digest re-fired edits of pre-cursor content and dropped boundary-block overflow; fixed by consuming `fetchNotificationsFromHaf` the same way the SPA route does (wide window floor + in-app `filterEventsAfter` cursor) and advancing `last_digest_block` to the highest delivered block on every non-empty run (the shared partial-block drop handles single-block overflow). Final held item (3039ef37, comment-only): the multi-block-truncation recovery prose asserted recovery unconditionally, but for WEEKLY cadence recovery is impossible — the ~201,600-block weekly stride is over 2x the 100,000-block (~3.5-day) window, so a dropped block is below the floor by the next run (and a 3.5-day window with a 7-day cadence structurally misses ~half of each week). The reword (both `fetchNotificationsFromHaf` docblock and `runDigest` comment) frames floor-slide recovery as typical at daily cadence and states the weekly-cadence and sustained-dense cases as bounded accepted residuals. Re-review verified the cadence arithmetic and comment-only scope.

## BACKEND-CUSTOM-ID-BLOCK-NUM-FLOOR-SWEEP (archived 2026-06-14) — inert block_num>=genesis floor dropped at 16 BitmapAnd-toxic sites; round-2 docblock item clean ✓

Dropped the by-construction-inert `block_num >= $genesis` floor + its bound param at all 16 eslint-suppressed BitmapAnd-toxic sites (the floor never excludes a row since pre-genesis PEvO custom_jsons cannot exist), removing all `eslint-disable` directives so `pevo/no-custom-id-block-num-floor` stands as a pure new-code guard with zero live suppressions; per-site real-HAF row-count-unchanged verification; the delicate param-renumber (incl. the 20-bind computeReputationBatch CTE) traced slot-by-slot in the prior rounds. Final held item (3931c51b, comment-only): the `findCustodyBroadcastByIdempotencyKey` docblock's retained "(matching the rest of the HAF queries)" parenthetical still overgeneralized post-sweep (no broader floor-carrying family remains), so it was dropped — the comment-view arm's genesis floor now stands on its own rationale (skips pre-appTag scans; the sole getCachedGenesisBlock consumer), the custom_json arm's no-floor rationale unchanged. Re-review confirmed no codebase-wide prevalence claim remains.

## BACKEND-WOT-RETRACT-TEST-AND-TYPE-POLISH (archived 2026-06-14) — non-vacuous no-second-read pin + real-pool getVouchStatus SELECT test + single-sourced AccreditationMethod; clean ✓

Three quality items on the verified-correct wot-retract single-read collapse: the no-second-read regression pin now drives the revoke branch (below-threshold status) so it fails red if a second read is reintroduced; a real-pool FROM-redirect test pins the combined `vouchStatusSelect` (zero-vouchers one-row invariant, scalar-subquery+bare-aggregate legality, no-op redirect guard); `accreditation_method` narrowed to the `AccreditationMethod` literal union. Final held item (4312f89f, type-only): `AccreditationMethod` was declared twice (wot.ts + types/domain.ts) — single-sourced by importing the canonical domain declaration and re-exporting it from wot.ts (the import+re-export pair is required because a bare re-export has no local binding for the in-file `=== 'wot'` discriminant, the VouchStatus field, the SQL-read cast, and the test import). Re-review verified both statements present, no second declaration, members unchanged, all import paths valid.

## BACKEND-CALLSITE-CANARY-HEADER-NUMERAL-DEROT (archived 2026-06-14) — drop duplicated count numerals so CALLSITES is the single inventory; first review clean ✓

Comment-only (a7adb03d): the `excludeSelfReviewWhere-callsite-canaries.test.ts` header restated counts (total SQL sites, file count, per-file "2+2+1+1+1+4=11" sums) the CALLSITES data table already encodes — four prior review rounds on the parent papers-listing task were spent reconciling those numerals after they drifted. The fix replaces the numerals with table references ("a revert at any callsite listed in CALLSITES") and removes coordination-state narration ("the architect's hold-block fix recipe asked for…" reworded to "Runtime SQL inspection via mock-pool was considered and ruled out:"), keeping the symbol-level explanations (which file contributes which sites and why). Re-review + grep confirmed no count numeral or coordination-state reference remains; assertions/CALLSITES/minOccurrences unchanged; the retained `defense-in-depth-canary-must-pin-each-layer` citation is a durable solutions-file slug, not a rotting task slug.

## BACKEND-REPUTATION-BATCH-SEAM-ESLINT-GUARD (archived 2026-06-14) — add the missing __test_seams no-restricted-imports guard; first review clean ✓

Config-only (5544ec7a): `reputation-batch.ts` exported a `__test_seams` namespace without the `no-restricted-imports` guard its three sibling seams carry. Added the entry scoped to `src/**/*.ts`, `importNames: ['__test_seams']`, `group: ['**/reputation-batch', '**/reputation-batch.js']`, message anchored on the seam contents; the seam-enumeration docblock gained a matching bullet. The pre-existing `**/reputation` glob does not match `reputation-batch` (exact segment), so the entry is distinct; test imports stay legal (src-scoped). Re-review (project-standards) clean; probe confirmed a src/ import fails lint while tests/ imports pass.

## BACKEND-CONSENT-STACK-COMPOSER-POLISH (archived 2026-06-14) — name ConsentSeedScope + complete the composer excluded-sites docblock; clean ✓

The two P3 maintainability nits split from the consent-stack-dedup first review, implemented in 5a85312a: extracted the seed scope union to a named exported `ConsentSeedScope` type used at both `consentSeedCteBody` and `consentStackCteBody` signatures (members unchanged — behavior-preserving type rename, byte-equivalence pins stay green), and completed the `consentStackCteBody` docblock's excluded-sites list to name both the custom-seed sites AND the seed-less per-paper badge/detail sites (`fetchConsentedAccountsForPaper` + per-paper detail chain). Re-review confirmed no orphaned inline union remains (the distinct `{signers: string[]}` chain scope is correctly untouched) and anchors are clean. Filed and cleared within the same review pass.

## BACKEND-CONSENT-STACK-TEST-INFRA-DEDUP (archived 2026-06-14) — consentStackCteBody composer + shared redirectHafViews test helper; first review clean ✓

`consentStackCteBody(startIdx, scope?)` (hafsql.ts) collapses the consent-stack 3-tuple (`consentSeedCteBody → consentChainCteBody({rootsFromCte:'consent_seed'}) → consentedAuthorsCteBody`) into one builder, byte-equivalent to the prior sequential composition; seven production sites migrated (papers listing + vote batch, profile x2, reviews, search, stats). A `redirectHafViews(stmt, mapping)` test helper promotes the me-pending local redirect() with a per-mapping-key drift guard; four test files migrated. `/ce-code-review` fan-out (correctness + security + adversarial on the session model; testing/maintainability/project-standards/performance + learnings on Sonnet; ce-agent-native skipped per PEvO) returned ZERO findings from all five high-stakes/perf reviewers: byte-equivalence mechanically verified across all three scope shapes ({signer} narrows resolution to {signers:[signer]}, {papers} seeds page-bounded with unscoped resolution incl. the empty-list FALSE backstop, omitted fully unscoped) AND non-1 starting indices; the forward adoption grep confirms zero consentSeedCteBody route callers remain outside hafsql.ts; all values bind positionally (no injection); the helper preserves each file's redirect/drift guards incl. the stricter whole-schema ones. Two P3 maintainability nits split to `backend-consent-stack-composer-polish` (pending): the inline scope-union type is duplicated across consentSeedCteBody + consentStackCteBody (extract a named ConsentSeedScope), and the composer docblock's excluded-sites list omits the seed-less per-paper badge/detail sites (a future mis-migration risk the learnings researcher independently flagged). The project-standards "Co-Authored-By names a non-existent model" finding was a false positive (Claude Fable 5 is the live model).

## BACKEND-CONSENTED-SET-READ-SURFACES (archived 2026-06-14) — consented badge on paper-detail + GET /api/me/authorships/pending; round-2 re-review clean ✓

Both read surfaces consume the SQL twin (the exact consent stack the reputation cycle composes) for zero cycle-vs-read drift, fail-closed (503, never a degraded root-only badge), at-most-one-block-stale via the volatile cache tier. Round-1 (3 items): bounded the naming_posts seed with NAMING_POSTS_SEED_CAP=500 (spam defense; truncated-but-served, never fail-closed); reworded a stale papers.ts comment; pinned the ?version=N branch's fail-closed guard. Round-2 (2 items, verified this pass): the seed cap is now injectable (namingPostsSeedCap test-seam param; production uses the default) with real-Postgres truncation coverage pinning the LIMIT's existence, its newest-first ORDER BY direction, AND the default-500 composition shape — all three mutation probes (LIMIT-deletion, DESC→ASC, 500→5) confirmed structurally mutation-killing; the papers.ts comment reworded to the in-file-verifiable form. The LIMIT operand is number-typed with controlled callers (residual, safe).

## BACKEND-DISPLAY-CLAIMS-CTE-UNSCOPED-COST (archived 2026-06-14) — AS MATERIALIZED fence + {papers} scope + vote-batch degrade; round-2 re-review clean ✓

`authorship_claims AS MATERIALIZED` fences the claims status resolution once per query (no per-row LATERAL re-eval; EXPLAIN evidence recorded); a {papers} page-bounded scope variant added; the vote-batch accepted-claims query decoupled from listing availability (degrade-and-serve un-excluded vs reject the whole Promise.all — the display-parity refinement's authoritative enforcement is the cycle). Round-1 (2 items): degrade-path pin + four comment edits. Round-2 (1 item, verified this pass): two stale absolute param-index quotes ($23/$25) in the revoke-gate cycle-shape canary's header + it() title corrected to $22/$24 (matching the test's own green expect() regex), header reworded to startIdx-relative phrasing; the same class found and fixed in the approve-gate sibling ($23→$22). Titles confirmed to match the actual assertions; hafsql.test.ts genuinely pins relative offsets. Three P3 maintainability nits dismissed at triage (the implementer chose the hold-block-sanctioned retitle option over dropping indices; the remaining absolute-index illustrations sit adjacent to the assertions that pin them and go red on a real allocation shift; one pure line-length style nit). The degrade-vs-fail-closed boundary is documented in `fail-closed-vs-degrade-accepted-haf-error-boundary`.

## BACKEND-ORCID-TEST-HEADER-DBJS-ENUMERATION (archived 2026-06-14) — db.js mock-inventory clause completed; first review clean ✓

Comment-only: the orcid.test.ts carve-out clause-(a) mock-inventory sentence named only db.js getPool while the factory stubs three exports; it now names getPool / isHafConfigured→true / no-op closeHafPool, and the positional "see the note above its factory" pointer became the name anchor "see the verifyHiveSignature.js vi.mock factory's note". Set-equality re-derived and verified this pass against all five vi.mock factories (db.js x3; app-db.js getAppPool; hive.js hiveClient broadcast.json + getAccounts + broadcastJsonWithTimeout + broadcastAdminCustomJson + BroadcastTimeoutError + DEFAULT_BROADCAST_TIMEOUT_MS; accreditation.js getAccreditedSet; verifyHiveSignature.js wrapper). Closes the fourth and final under-enumeration layer on this sentence; the incident chain is the canonical instance behind universal-mock-inventory-must-be-re-derived-not-incrementally-patched.

## BACKEND-REISSUEDAT-ROUNDTRIP-REALPATH-TEST (archived 2026-06-14) — real-DB reissuedAt↔sessions_invalidated_at round-trip pin; round-3 re-review clean ✓

A real-app-Postgres test drives a genuine /recover/verify reissue, reads back sessions_invalidated_at, and asserts the reissued JWT's reissuedAt === stored.getTime(), then presents it to the REAL verifyHiveSignature (survives 200) alongside same-second controls (the WITH-reissuedAt control survives, the WITHOUT is revoked 401) — discrimination pinned to the reissuedAt identity, not the nondeterministic sign-time iat. Round-1: deterministic control pair + reachable account-state fixture. Round-2: ms-strictness pin in the mocked hardening sibling + attested revert probes. Round-3 (1 item, verified this pass): the overstated co-listed "NOW()/seconds-rounding" detection claim split in both roundtrip files — seconds-rounding is caught deterministically, a NOW()-switch only probabilistically (a just-captured Node Date and SQL NOW() usually share the ms bucket) so the suite must not be relied on as the NOW() detector; the preReset control renamed controlWithoutReissue across both files (rename complete, assertions unchanged). The F2 doc comment + ARCHITECTURE §6.7 cover the same-second self-healing edge.

## BACKEND-PAPERS-LISTING-CORRELATED-SUBQUERIES (archived 2026-06-12) — up-to-80 correlated listing subqueries collapsed to one rev_agg LATERAL + once-per-request citation scan; round-5 re-review clean ✓

The listing's per-row scalar subqueries (reviewCount + avgRating twice-scanning the same accredited-review row set, plus the per-row jsonb citation containment over the whole corpus) collapsed to ONE rev_agg LATERAL Aggregate per page row (8c58a7c3; EXPLAIN on real HAF: a single indexed parent-pair scan computing both aggregates in one node) and a once-per-request `paper_citation_counts` corpus scan (landed via the sibling citation-count task; Subquery Scan loops=1, healthy with the CTE inlined, no MATERIALIZED pin needed). The sort=votes aggregate-CTE arm split to `backend-papers-listing-votes-aggregate-cte` (now blocked). Rounds 2-5 were successive holds on the `excludeSelfReviewWhere-callsite-canaries` header: minOccurrences 3-to-2 + envelope-field pins + canary hardening (real helper imports, same-reviewer and malformed-rating corpus rows), then a round-ordinal de-rot, then the "three"-to-"four" reputation.ts narration fix, then four stale counts (8-to-11 sites, 8-to-6 files, routes-to-modules reword, expected-to-minOccurrences) — four consecutive rounds of prose-numeral rot against the CALLSITES table. Round-5 re-review clean: all counts reconcile with the array, the real-code grep matches per-file (2+2+1+1+1+4), no fifth stale count found. The structural fix that ends the class — dropping the duplicated numerals from the header prose so the CALLSITES table is the single inventory — is filed as `backend-callsite-canary-header-numeral-derot`.

## BACKEND-REDIS-KEYS-SCAN-REPLACEMENT (archived 2026-06-12) — blocking KEYS replaced by SCAN + members-set index; 3 hold rounds; round-4 re-review clean ✓

Replaced the three blocking `redis.keys()` sites (f90d7088): the cleanup helpers SCAN iteratively; `getBatchReputationMap` reads a `batch_members` Redis Set maintained by SADD inside the CYCLE_SWAP Lua (atomic; the key deliberately sits OUTSIDE `BATCH_KEY_PREFIX` so an account literally named `members` cannot String-vs-Set collide), with a wholly-empty-index SCAN backfill. Round-1 hold (4 items, 199372b5): memory-filename docblock cite replaced with the plain single-instance invariant; the seed/revoke writers joined the index (SADD on NX-OK, SREM after DEL, `pipeline.exec()` tuple inspection); deterministic backfill pins via the gated `setBatchMembersKey` seam + its eslint `no-restricted-imports` guard; CYCLE_SWAP arity guard. Round-3 hold (3 items, 2570ef8f): `as const` on both `__test_seams`; the NX-OK SADD gate DROPPED — the conditional index write reopened an orphan window (crash between SET and SADD, then a retry's NX no-op skips the SADD on a false "already a member" assumption, and a single orphan in a populated index is invisible until the next cycle swap because the backfill heals only a wholly-empty index) — SADD is now unconditional; write-path `sismember` pins added, the orphan-window case mutation-killing the gated form. Round-4 re-review clean: adversarial's interleaving/growth/seam/isolation attacks all fail — the one new interleaving downgrades the failure class from "accredited user invisible until cycle swap" to one harmless MGET-null-skipped set member. Residuals filed separately at archive: the pre-existing `reputation-batch.ts` seam missing its `no-restricted-imports` entry; the test-header round-ordinals appended to the anchor-sweep task. The general lesson (an index maintained conditionally or by some-but-not-all writers drifts) is documented in `agents/docs/solutions/` as the index writer-completeness rule.

## BACKEND-ADMIN-BROADCAST-ORCID-SITES (archived 2026-06-12) — orcid handleAccredit/handleLink on broadcastAdminCustomJson, TOCTOU-lock-shape preserved ✓

Migrated the two orcid admin custom_json sites (handleAccredit, handleLink) to broadcastAdminCustomJson via the pre-validate-outside-the-inner-try option (commit d29f9708): a validation `PrivateKey.fromString` parse stays inside withOrcidBindingLock's fn but outside the inner try, preserving the security-tested 504-ambiguous-outcome + lock-release escape for a malformed key, while the helper call sits inside the try; the pre-lock unset-key 500-guard keeps AdminKeyNotConfiguredError unreachable. Round-1 review verified ZERO behavioral change: byte-identical envelopes across the key-state x lock-state matrix, BroadcastTimeoutError class identity across the helper boundary (lock-TTL-extend + skipRelease untouched), the validation parse mutation-killed by the SEC-002 specs, envelope literal confined to hive.ts both grep directions. Rounds 2-4 were successive holds on ONE header sentence — the carve-out clause (a) mock inventory — which proved the incremental-patch rot class live: round 2 added the broadcastAdminCustomJson seam (338eb188) but left accreditation.js getAccreditedSet and the getAccounts read stub unnamed; round 3 re-derived the full five-factory enumeration (0d102616) with an attested factory-by-factory audit, verified at round-4 re-review as accurate for four factories with anchors clean — but still naming only getPool of the db.js factory's three exports (isHafConfigured -> true, no-op closeHafPool), judged not absorbed by the "database pools" grouping given the sentence names even the hive.js constant stand-ins. That residual plus a positional "above its factory" pointer split at triage to backend-orcid-test-header-dbjs-enumeration (pending) so this task could archive. The incident chain is the canonical instance behind universal-mock-inventory-must-be-re-derived-not-incrementally-patched.

