## BACKEND-JSONB-ARRAY-ELEMENTS-LATERAL-GUARD-SWEEP (archived 2026-05-19) — round-3 clean ✓

Three-round P2 task migrating 4 production sites with WHERE-clause jsonb_typeof guards (placebo because Postgres evaluates CROSS JOIN LATERAL BEFORE WHERE) to canonical CASE-WHEN-at-SRF-argument-position form, with per-shape behavioral canaries. Round-1 hold (3 docblock-quality items: line-number rot in canary file headers, duplicated 9-site audit table, round-N + sibling-slug citations) landed cleanly; round-2 hold (1 item: 2 audit-table dispositions stale — `citing_papers` CTE and `authorsWithSupersessionSelect` were marked OUT OF SCOPE but had already been fixed via the now-archived cycle-cascade sibling task) landed verbatim in a 7-line docblock-only re-sync. Round-3 `/ce-code-review` on commit `84a87cf` with 5 personas (correctness on Opus; testing, maintainability, project-standards, learnings on Sonnet; `kieran-typescript` skipped — no TS changes; `ce-agent-native-reviewer` skipped per PEvO root CLAUDE.md): zero findings. Correctness verified both production sites still use canonical CASE-WHEN-at-SRF-arg form (`reputation.ts citing_papers` CTE `CROSS JOIN LATERAL jsonb_array_elements(CASE WHEN jsonb_typeof(citing.json_metadata -> $3 -> 'citations') = 'array' THEN ... ELSE '[]'::jsonb END)` and `hafsql.ts authorsWithSupersessionSelect` `jsonb_array_elements(CASE WHEN jsonb_typeof(...) = 'array' THEN ... ELSE '[]'::jsonb END) WITH ORDINALITY`). Maintainability self-violation audit clean: no new line numbers, task slugs, round-N qualifiers, or SHAs in the replacement text. The two re-synced entries use the short `already correct (CASE-WHEN at SRF arg).` form, matching the `paper_resolved_votes NOT EXISTS` form; the `excludeSelfReviewWhere → already correct, reference implementation (...)` longer form intentionally marks the canonical reference. Sibling cross-reference at `notification-queries-lateral-guard-canary.test.ts` preserved as one-line pointer. Verification gates clean (`tsc --noEmit`, lint). Learnings researcher flagged a possible `/ce-compound` candidate: audit-tables in canonical-home docblocks are a variant of the `enumerated-exemption-lists-are-drift-vectors-2026-04-28` family — a narrow companion convention ("audit-tables enumerating per-site dispositions must be re-synced when cited sibling tasks archive") would close the structural drift loop; not filed at archive but worth tracking if the recurrence pattern shows up again. Original 243-line task body covered: 4-site migration (profile.ts citations CTE, stats.ts total_citations subquery, notification-queries.ts arm 6a + arm 6b), 2 new behavioral canary test files (citations-lateral-guard-canary.test.ts + notification-queries-lateral-guard-canary.test.ts) with per-shape coverage (jsonb null, string, integer, object, plus well-formed positive controls and mixed-batch regression guards), full `jsonb_array_elements` audit across `backend/src/` (9 sites enumerated with dispositions: 4 migrated, 4 already correct, 2 IPFS exempt with structural rationale). Source: `/ce-code-review` of `backend-self-review-exclusion-everywhere` round-4 commit `ba95a4a` (reliability R-1, adversarial #1 + #2, learnings researcher LATERAL evaluation order trap). Cross-references: `pg-jsonb-null-vs-sql-null-use-jsonb-typeof-2026-05-12.md` (canonical convention), `defense-in-depth-canary-must-pin-each-layer-2026-05-07.md` (per-layer canary requirement). The audit-table truth-claim drift between round-2 and the cycle-cascade sibling's archive is the round-3 reason.

---

## BACKEND-TESTS-TYPECHECK-COVERAGE (archived 2026-05-19) — round-3 clean ✓

Three-round P1 task extending `tsc --noEmit` over `backend/tests/` and closing the LogContext typo-bypass class that surfaced when kieran-typescript and maintainability reviewers (cross-corroborated, conf 100) found that `backend/tests/lib/broadcast-error.test.ts` was passing invented field names like `{ user: 'alice' }`, `{ action: 'test' }`, `{ case: '…' }`, `{ run: '…' }` through the `LogContext` interface. The interface existed specifically to catch typo'd structured-log fields at compile time, but `backend/tsconfig.json` had `include: ['src']` so test sites were outside the tsc perimeter and esbuild stripped types without enforcement.

Round-1 (`c90d890`, 2026-05-16) added `backend/tests/tsconfig.json` extending the root tsconfig with `tests/` included, added `typecheck:tests` to `package.json`, and fixed 12 LogContext typo-bypass sites in `broadcast-error.test.ts`. Round-1 `/ce-code-review` (6 personas) raised 3 items: P1 #1 — `typecheck:tests` was added as a standalone script rather than ALONGSIDE the existing `typecheck` script (Acceptance #4 unmet), with `npm run typecheck:tests` exiting non-zero on 249 pre-existing errors across 56 other test files, meaning the gate could not function as a regression net today. P1 #2 — the introduced `TestLogContext = LogContext & Record<string, unknown>` cast widened the contract at 5 sites, reopening the typo-bypass at every cast. P2 #3 — 4 `Parameters<typeof handleBroadcastError>[2]` cast sites used a fragile structural-index query when the exported named type `HandleBroadcastErrorOpts` was already available.

Round-2 (`2c34341`, 2026-05-16) chose path-(b) defer-and-document for item 1: filed `backend-tests-typecheck-residual-drift.md` as a follow-up with per-category triage approach (A real drift / B type-import / C vitest-globals tsconfig / D wot-broadcast TS1309 / E fixture-mock-shape / F per-file exclude), kept `typecheck:tests` standalone, updated Acceptance #4 in the task body with a round-1 hold amendment cross-referencing the follow-up. Chose path-(a) for item 2: promoted `run?: string` to a declared optional field on `LogContext` itself (after the round-5 hold #2 specs proved `run` is a real structured-log marker operators see, not test-only scaffolding), removed `TestLogContext` entirely. For item 3: replaced 5 `Parameters<typeof handleBroadcastError>[2]` casts (one more than the hold cited — inspection found a fifth at HEAD) with `as unknown as HandleBroadcastErrorOpts` named-type form. Tests: 42/42 pass; tsc tests-config delta 0 (the 249-error backlog moved to the follow-up). Round-2 `/ce-code-review` (7 personas) raised 3 comment-anchor-hygiene items: P2 task-slug citation in production `LogContext.run` docblock; P2 task-slug citation in test file header; P3 cosmetic comment-vs-code drift at two inline cast-description sites still saying `Parameters<typeof>[2]` after the casts had migrated.

Round-3 (`8b44450`, 2026-05-18) landed all 3 round-2 hold items in a single header/comment-only commit: rewrote `LogContext.run` docblock dropping the `Backend-tests-typecheck-coverage round-1 hold item 2 (path (a))` paragraph and the "See the task file for paths (b) and (c) that were dismissed" redirect, kept behavioral semantics; rewrote test file header dropping the slug and round-N qualifiers; updated 2 inline comments to describe casts as `as unknown as HandleBroadcastErrorOpts`. The "line ~614" anchor and a `BACKEND-ORCID-BROADCAST-OUTCOME-DISCRIMINATION round-4 hold #1` heading in surrounding unedited comments were explicitly left out of scope per the implementer's tight-scope discipline (architect's cluster-D carry-forward).

Round-3 `/ce-code-review` (6 personas — correctness on Opus; testing/maintainability/project-standards/kieran-typescript/learnings-researcher on Sonnet; `ce-agent-native-reviewer` skipped per PEvO CLAUDE.md; adversarial/reliability/security skipped — comment-only edits): zero in-diff findings. Pre-existing rot at the deliberately scoped-out sites acknowledged; one cross-cutting observation surfaced — 12 `it()` description strings in `broadcast-error.test.ts` embed `round-N hold` qualifiers that appear in vitest output and CI logs (pre-existing across many commits, not flagged as actionable). Cross-refs: `agents/docs/solutions/conventions/task-slug-citations-in-comments-go-stale-on-archive-2026-05-15.md`, `docblock-anchor-stable-symbols-not-line-numbers-2026-05-15.md`, `convention-enforcing-fix-must-audit-its-own-new-code-2026-05-17.md`, `discipline-interface-tsc-perimeter-omission-2026-05-11.md`, `pino-spy-serializer-ordering-trap-2026-05-06.md`, `broadcast-per-attempt-vs-error-event-roles-2026-05-13.md`. Backend: `backend/src/lib/broadcast-error.ts` (`LogContext.run` field declaration), `backend/tests/lib/broadcast-error.test.ts`, `backend/tests/tsconfig.json`, `backend/package.json` (`typecheck:tests` script). Follow-up tracking: `backend-tests-typecheck-residual-drift.md` (archived 2026-05-18 cleared the 249-error backlog), `backend-makelogbroadcastattempt-cast-narrow.md` (cluster-D follow-up, archived same day).


## BACKEND-MAKELOGBROADCASTATTEMPT-CAST-NARROW (archived 2026-05-19) — round-1 clean ✓

P3 cluster-D follow-up from `backend-tests-typecheck-coverage` round-2 review. The round-2 fix for parent item P2 #3 migrated 5 `as unknown as Parameters<typeof handleBroadcastError>[2]` casts in `backend/tests/lib/broadcast-error.test.ts` to the named-type form `HandleBroadcastErrorOpts`. The kieran-typescript residual flagged that the same structural-index fragility class survived at 9 sites in the same test file for the sibling helper `makeLogBroadcastAttempt`. These were intentionally out-of-scoped from the parent task (which targeted only `handleBroadcastError[2]`) and tracked as a carry-forward.

Round-1 (`a076b92`, 2026-05-18) added `export type MakeLogBroadcastAttemptOpts = { info: typeof logger.info; warn: typeof logger.warn }` to `backend/src/lib/broadcast-error.ts` co-located with `makeLogBroadcastAttempt`, updated the function signature to consume the named type (`loggerInstance: MakeLogBroadcastAttemptOpts = logger`) so the type is single-source-of-truth, added the type to the test-file import block, and replaced all 9 `as unknown as Parameters<typeof makeLogBroadcastAttempt>[2]` occurrences with `as unknown as MakeLogBroadcastAttemptOpts`. The two-step `as unknown as <T>` form was preserved (the cast objects inject `{ info: vi.fn(), warn: vi.fn() }` which don't carry every pino-method type; the intermediate `unknown` is load-bearing). Production behavior unchanged — the named type is structurally identical to the inline shape it replaced, and call sites in `backend/src/` (custody.ts, bridge.ts) use the 2-arg form with default `loggerInstance = logger`.

Round-1 `/ce-code-review` (6 personas — correctness on Opus; testing/maintainability/project-standards/kieran-typescript/learnings-researcher on Sonnet; `ce-agent-native-reviewer` skipped per CLAUDE.md; adversarial/reliability/security skipped — small, well-scoped diff): zero findings. `-Opts` naming on a logger-instance argument flagged sub-threshold (the implementer's signal block addressed this — sibling `HandleBroadcastErrorOpts` is the established convention in the file and grep-alignment wins over literal accuracy; `broadcast-per-attempt-vs-error-event-roles-2026-05-13.md` further establishes `makeLogBroadcastAttempt` as a factory whose third parameter is "options to the factory", anchoring the naming decision). Pre-existing rot at `backend/src/lib/broadcast-error.ts:672` (`Round-2 hold #4 of` task-slug citation in unedited section block) noted as residual; out of this commit's scope. Cross-refs: `docblock-anchor-stable-symbols-not-line-numbers-2026-05-15.md` (Parameters<typeof>[N] structural-index is the type-citation equivalent of a line-number anchor), `pino-spy-serializer-ordering-trap-2026-05-06.md` (the documented "Parameters<> collapses overloads" rationale that justifies the migration), `discipline-interface-tsc-perimeter-omission-2026-05-11.md` (named-type protection only fires inside the tsc perimeter; the perimeter extension landed by parent task `backend-tests-typecheck-coverage` is what makes this protection real, not cosmetic). Backend: `backend/src/lib/broadcast-error.ts`, `backend/tests/lib/broadcast-error.test.ts`. tsc clean (root + tests configs), lint clean, vitest deferred to parent's serialized post-fan-out run.


## BACKEND-RECOVER-TEST-RETRY-SELF-POISONING (archived 2026-05-19) — round-1 clean ✓

P3 cluster-D follow-up from `backend-password-hash-null-typing-audit` round-4 review, instantiating the convention `vitest-retry-fire-and-forget-side-effect-poisoning-2026-05-04.md` which had named `backend/tests/routes/recover.test.ts:602-633` as a latent unrepaired sibling. The three-condition trigger: fire-and-forget audit-log write in production (`logCustodyBroadcast(...).catch(() => {})`) + strict-equality count assertion (`expect(rows.length).toBe(1)`) + `vitest.config.ts` `retry: 3` + class-level `beforeAll`/`afterAll` cleanup not `beforeEach`. Attempt #1's audit row remains visible to attempt #2's count assertion, poisoning the mutation-kill ground truth regardless of whether the actual mutation-fence claim is correct.

Round-1 (`96dab9d`, 2026-05-18) added a `beforeEach` reset in the `POST /api/auth/recover — with DB` describe block (`DELETE FROM custody_audit_log WHERE username = ANY($1::text[])` scoped to `TEST_USER` only) and a module-local `fetchSettledAuditRows(pool, username, operationType)` bounded-poll helper (1.5s poll @ 25ms intervals for `>= 1` row, then 100ms settle, then re-SELECT and return). The 100ms settle window catches an in-flight retry-poisoned second INSERT before the count assertion runs — mutation-kill on the over-log production change class. The task body's citation `recover.test.ts:602-633` was stale at HEAD: those lines now test `/resend-verification` null-hash timing and have no audit-log assertion. The implementer found the real three-condition matches at lines 170 (`rejects wrong memo key`, `recovery_failure` op) and 243 (`succeeds with correct memo key`, `account_recovery` op) and fixed both. Production code in `backend/src/routes/recover.ts` UNCHANGED — fire-and-forget audit-log writes preserved per `auth-structured-log-shape-2026-04-29.md` (the convention's "Rejected fix shapes" section explicitly names "await the fire-and-forget call in production" as a rejected remedy). Reference implementation `custody-upgrade-null-hash.test.ts` had been deleted by commit `1f1be4e`; helper signature pulled from `git show 009b4a2:backend/tests/routes/custody-upgrade-null-hash.test.ts`. Helper kept local per task's out-of-scope guidance (sibling test file `custody-consent-ops.test.ts` was concurrently edited in a parallel worktree).

Round-1 `/ce-code-review` (7 personas — correctness on Opus; testing/maintainability/project-standards/kieran-typescript/reliability/learnings-researcher on Sonnet; `ce-agent-native-reviewer` skipped per CLAUDE.md; adversarial/security/api-contract/performance/data-migrations skipped — test-only diff): zero actionable in-diff findings. Two sub-threshold residual items dismissed at triage — `beforeEach` DELETE `.catch(() => {})` swallow-errors pattern (matches existing file convention at `cleanup()` line 70; failure surfaces as noisy mismatch via downstream `toBe(1)` rather than silent pass; preemptive-hardening dismissal). One cross-cutting observation handed to a separate architect action: 5 inline poll-loop sites in `backend/tests/routes/custody-consent-ops.test.ts` (anchor on surrounding `it` block names) use the same fire-and-forget audit pattern WITHOUT the 100ms settle window — coverage-asymmetry rather than active bug (consent-ops has its own `beforeEach` reset neutralizing the three-condition trigger). Filed as follow-up `backend-consent-ops-audit-poll-settle-sweep.md` per the convergence-sweep rule in `worker-fanout-helper-name-divergence-2026-05-15.md`. Cross-refs: `vitest-retry-fire-and-forget-side-effect-poisoning-2026-05-04.md` (canonical convention; this task's repair empties the named-sibling catalog), `auth-structured-log-shape-2026-04-29.md` (fire-and-forget endorsement), `worker-fanout-helper-name-divergence-2026-05-15.md` (convergence-sweep timing), `test-helper-closure-capture-over-arg-threading-2026-05-04.md` (extraction triggers and shared-helper convention). Backend: `backend/tests/routes/recover.test.ts`. tsc clean, lint clean.


## BACKEND-CUSTODY-AUDIT-RETENTION-SWEEP (archived 2026-05-18) — round-5 clean ✓

Five-round P1 pre-launch-blocker task implementing the 24-month time-based retention enforcement for `custody_audit_log` rows. Migration 006 had documented the retention period as a `COMMENT ON COLUMN` but no periodic cleanup job existed; per GDPR Art. 5(1)(e), a retention policy that lives only in documentation is legally equivalent to indefinite retention under CNPD enforcement, and the compliance clock starts on first-row-insert.

Round-1 (`backend(custody-audit-retention-sweep)`, 2026-05-15) landed `startRetentionSweepTicker(pool, intervalMs, ttlMonths)` as a `setInterval`-based ticker that periodically deletes rows older than the retention TTL, plus `validateRetentionSweepConfig` reading env (`CUSTODY_AUDIT_RETENTION_MONTHS`, `CUSTODY_AUDIT_SWEEP_INTERVAL_MS`) and parsing the retention SOT from the column COMMENT. A `BootFatalError` subclass propagates config-validation failures out of `validateConfig()` into the boot-fatal catch hierarchy.

Rounds 2-5 cycled through comment-anchor and test-harness self-violations, each surfaced by `/ce-code-review`'s persona cluster: round-2 added a `mockRestore` in `finally` for a `vi.spyOn(global, 'setInterval')` transparent probe per `vi-spyon-mockimplementation-bypasses-function-under-test-2026-05-12.md`; round-3 landed 4 items including the `BootFatalError` rewrap discrimination test, slug-citation strip, and line-number anchor replacement on the mechanism docblock; round-4 added the carve-out clause-(a) bullet acknowledging the `setInterval` spy as a "transparent negative-assertion probe" (no real path to exercise "did not happen" contracts); round-5 (`187e788`) landed two textual items — the carve-out bullet and a `BootFatalError` docblock correction attributing recursive `.cause` walking to PEvO's custom `redactErrSerializer` in `logger.ts` (stock `pino-std-serializers` does not walk `.cause`).

Round-5 `/ce-code-review` (7 personas — correctness on Opus; testing/maintainability/reliability/project-standards/kieran-typescript/learnings-researcher on Sonnet; `ce-agent-native-reviewer` skipped per PEvO CLAUDE.md): zero in-diff findings. Cross-refs: `boot-fatal-call-stack-unwind-and-rethrow-trap-2026-05-11.md`, `boot-fatal-flush-watchdog-pattern-2026-05-11.md`, `defensive-recursive-serializer-and-pino-err-redact-policy-2026-05-11.md`, `test-mock-carve-out-clause-c-2026-05-04.md`, `convention-enforcing-fix-must-audit-its-own-new-code-2026-05-17.md`, `vi-spyon-mockimplementation-bypasses-function-under-test-2026-05-12.md`. Backend: `startup-checks.ts` (BootFatalError + validateRetentionSweepConfig), `jobs/custody-audit-retention-sweep.ts`, `index.ts` (startup wiring). Tests: 14/14 passing against real Postgres + Redis. Migration 006 column COMMENT remains the authoritative retention-policy SOT; this task added mechanical enforcement.


## BE-PASSWORD-POLICY-HARMONIZE (archived 2026-05-18) — round-3 clean ✓

Three-round P3 cross-stack drift-gate task hardening the backend password-policy helper against silent divergence from the frontend's identical helper. After FE-PASSWORD-POLICY-DRY and BE-PASSWORD-POLICY-DRY extracted shared helpers in each stack independently, both encoded the same `length >= 10 && /[a-z]/ && /[A-Z]/ && /[0-9]/` rule but would drift without a structural gate.

Round-1 (`6c4e963`, 2026-04-21) landed `password-policy-drift.test.ts` — a differential test running both helpers against a corpus of canonical-form passwords and coercion-attack vectors (objects with `toString`, `valueOf`, `Symbol.toPrimitive` returning a valid password). The test asserts that any BE-side disagreement with FE-side on any input causes a disagreement-accumulator failure naming both vectors. Round-2 (`591f815`) replaced a dead `valueOf`-only vector with `Symbol.toPrimitive` — the previous shape was structurally insufficient because `OrdinaryToPrimitive(obj, hint='string')` consults `toString` first (returns `'[object Object]'`), reaching `valueOf` only after that path is exhausted; the `Symbol.toPrimitive` vector is consulted first by `ToPrimitive` regardless of hint.

Round-3 (`b000cd1`, 2026-05-04) was a comment-accuracy fix dropping the "number hint (`+pw`)" over-claim from Vector 2's kills-list. Empirically, `+{[Symbol.toPrimitive]: () => 'Abcdef1234'}` returns `'Abcdef1234'` then `Number(...)` is `NaN`, which fails class checks for an UNRELATED reason (NaN-stringification fails length/regex) rather than via the disagreement-accumulator surfacing a BE/FE mismatch. The comment was tightened to name "string hint" + "default hint" as accurate kills; closing claim narrowed from "covers all forms uniformly" to "covers the string and default hint forms uniformly".

Round-3 `/ce-code-review` (6 personas — correctness on Opus; testing/maintainability/project-standards/kieran-typescript/learnings-researcher on Sonnet; `ce-agent-native-reviewer` skipped per CLAUDE.md; adversarial/reliability/security skipped — comment-only edit in test file): zero in-diff findings. Cross-refs: `js-coercion-mutation-kill-vector-2026-05-04.md` (canonical convention for this test class; Rule 4 prose summary tightening candidate flagged during the cluster-B review pass), `tests-must-fail-on-mutation-of-code-under-test-2026-04-22.md`, `mutation-kill-claims-must-match-assertion-and-corpus-2026-05-15.md`. Paired sibling: `ui-password-policy-harmonize.md` (separate task).


## UI-PAPERS-ORCID-NULL-FALLBACK-VERIFICATION (archived 2026-05-18) — round-5 clean via in-place fix ✓

Five-round P1 SPA-audit task verifying that every site reading `authors[].orcid` on a `PaperSummary` / `PaperDetail` response null-guards the value. The origin was the backend's vouch-coauthor spoof-suppression branch in `buildCumulativeAuthorsForChain` (`backend-multi-author-cumulative-union` round-2 hold fix at commit `3b6d781`), which started emitting `authors[].orcid = null` for accredited authors on continuation-chain papers when a co-author's chain post claimed an ORCID value the accredited user had never asserted. The API contract at `agents/docs/api-contracts/papers.md` widened `authors[].orcid` to `string | null`; the task surface was the SPA's reaction to the new null path.

Round-1 (`6bf50d0`, 2026-05-17): UI agent's independent cross-grep of `\.orcid` across `frontend/src/` confirmed `_prefillForm` in `pages/edit.js` is the only SPA consumer of paper-author orcid in a non-template context (primary author → `authorOrcid` string; existing co-authors retained as raw objects whose orcid is read by `:value="ca.orcid || ''"` in the template). All other surfaces use Alpine `x-text` or `:value` bindings, both null-safe via JSDOM-verified coercion. Added a new `_prefillForm null-orcid regression` describe block in `frontend/tests/unit/pages-edit.test.js` pinning the data-side null-preservation contract.

Rounds 2-4 cycled through architect hold blocks on the regression spec's comment surface. The arc surfaced three successive architect-prescription self-contradictions, each caught by `/ce-code-review`'s persona cluster: round-2 found a tautological assertion loop (`null || ''` evaluated in the test harness rather than the template binding) and prescribed shape (a) — drop the loop, rely on the `.toBeNull()` data-side pins; landed at `26aff60`. Round-3 caught a `, commit 6bf50d0` SHA reference in the test comment as a rot class and prescribed substituting `(see task ui-papers-orcid-null-fallback-verification)`; landed at `dd6d2ad`. Round-4 caught that the round-3 prescription itself violated `task-slug-citations-in-comments-go-stale-on-archive-2026-05-15.md` (which had landed three days before the round-3 hold was written) — the slug citation rots the moment the task archives. Round-4 prescribed pure deletion of the parenthetical, landed at `c19173e`.

Round-4's own `/ce-code-review` then surfaced the same self-contradiction shape one level deeper: the round-4 hold-block had explicitly endorsed `frontend/src/pages/edit.js:183` as "the durable navigable code-pointer," but raw line-number anchors are themselves a documented rot class per `docblock-anchor-stable-symbols-not-line-numbers-2026-05-15.md` — the same convention round-4 cited to justify removing the slug. Cross-reviewer P1 finding (maintainability + project-standards + learnings-researcher, anchor 100). User authorized a one-off UI in-place fix at `453593c` to skip a round-5 hold cycle: replaced both `edit.js:183` occurrences in the block comment (lines 1029, 1032) with a binding-expression anchor — `:value="ca.orcid || ''"` is grep-stable and survives any insertion above line 183. File-path-only references (`frontend/src/pages/edit.js`) remain — file paths are stable anchors; only the `:NNN` suffix is the rot class. The pre-existing `papers.ts: 417-434` raw-line range on untouched lines 1016-1017 of the same block was logged as a residual_risk by maintainability and left in place per tight-scope discipline; candidate cleanup on the next edit of that block. 45/45 tests pass on `pages-edit.test.js` through every round; the three pre-existing `_mountEditors` unhandled rejections from the async editor-mount path are not introduced by any of these changes.

The structural learning — three successive architect-prescription self-contradictions caught by reviewers, each documented by a conventions doc that landed before or alongside the round that caught it — was the motivating force for `architect-comment-anchor-conventions-load-bearing-surface` (archived same day), which lifted the comment-anchor convention cluster from `agents/docs/solutions/conventions/` into root `CLAUDE.md` § "Comment anchors" so every implementer (and the architect's own hold-block writing) loads the rules at startup rather than discovering them reactively via `ce-learnings-researcher`.

Cross-refs: `agents/docs/solutions/conventions/task-slug-citations-in-comments-go-stale-on-archive-2026-05-15.md`, `docblock-anchor-stable-symbols-not-line-numbers-2026-05-15.md`, `convention-enforcing-fix-must-audit-its-own-new-code-2026-05-17.md`, `hold-block-must-not-contradict-convention-docs-2026-04-22.md`; root `CLAUDE.md` § "Comment anchors" (the lifted surface); `agents/docs/api-contracts/papers.md` (`authors[].orcid: string | null` contract); `backend/src/routes/papers.ts` `buildCumulativeAuthorsForChain` (the suppress-to-null branch); `backend/tests/routes/continuation-author-gate.test.ts` (backend canary for the suppress path); `frontend/src/pages/edit.js` `:value="ca.orcid || ''"` binding (the grep-stable anchor the round-5 fix targets); `frontend/tests/unit/pages-edit.test.js` `_prefillForm null-orcid regression` describe block (3 specs, all passing).


## BACKEND-CUSTODY-AUDIT-PII-ANNOTATION (archived 2026-05-18) — round-5 clean ✓

Five-round P2 task annotating the `user_agent` column on `custody_audit_log` as PII per GDPR / CNPD jurisdictional posture (Portugal). The original surfacing in 2026-05-05 was a data-migrations finding from `/ce-code-review` on `backend-coauthor-trust-model`: migration `005_custody_audit_consent_ops.sql` had added a `user_agent` column that captures raw HTTP `User-Agent` headers (OS / browser version, occasionally device or username substrings in mobile-app UAs) without documenting its PII status, retention policy, or deletion path. The DELETE-on-account-deletion sweep at `backend/src/routes/settings.ts` already covered GDPR right-to-erasure; the documentation gap was the actionable item.

Round-1 (`fdca2eb`, 2026-05-06): backend agent landed migration `006_custody_audit_pii_annotation.sql` with `COMMENT ON COLUMN custody_audit_log.user_agent` annotating PII status, legal basis (Art. 6(1)(f) legitimate interest in security audit), retention (24 months from insert — security-audit industry standard, balanced against Art. 5(1)(c)/(e) data-minimization), and the deletion path. `COMMENT ON COLUMN` is unconditional, so the migration is idempotent. Two `[TODO Architect]` markers requested `agents/docs/api-contracts/custody.md` and `agents/docs/ARCHITECTURE.md` cross-references to close the contract → ARCH documentation chain; both landed in subsequent architect commits. A `backend-custody-audit-retention-sweep` follow-up task was filed for the periodic cleanup job (now archived separately).

Rounds 2-5 (`62fd447`, `afe0ad4`, `500b86d`, `3e5734f`, 2026-05-15→2026-05-16) cycled through architect hold blocks addressing comment-anchor and clarity gaps in the migration text. Key decisions: round-2 swapped the SQL-header line-number citations (`settings.ts:312`, `custody.ts:282-289`) for symbolic anchors, anticipating the docblock-anchor-stable-symbols-not-line-numbers convention codified three days later; round-3 widened the `Insert path:` paragraph from broadcast-only to universal-fresh-auth scope per the convention-enforcing-fix-must-audit-its-own-new-code spirit; round-4 narrowed the row-class enumeration with the `e.g.,`-prefix to signal illustration-not-exhaustion (anticipating future op-type adds without forcing migration updates); round-5 (architect's Option B) added a "Scope on the wider table:" paragraph at BOTH occurrences (SQL header comment block + `COMMENT ON COLUMN` body) naming the non-broadcast row classes that write `user_agent` NULL (login_failure, password_reset, account_recovery, recovery_failure, email_deleted, upgrade, upgrade_failure) and labelling the two NULL paths (a)/(b) so a CNPD inspector reading `\d+ custody_audit_log` lands on unambiguous interpretation.

Round-5 `/ce-code-review` (6 reviewers — correctness on Opus; testing/maintainability/project-standards/data-migrations on Sonnet; learnings-researcher unstructured; `ce-agent-native-reviewer` skipped per PEvO CLAUDE.md; security/reliability/adversarial skipped — comment-only edit, no new attack surface): zero in-diff findings. Architect noted at triage that the implementer's chosen form (appended "Scope on the wider table:" paragraph) deviated from Option B's opening-sentence rewrite suggestion, but substance was preserved: two NULL paths labelled (a)/(b), `e.g.,`-prefixed row-class enumeration matched code call sites (login_failure at `auth.ts`, password_reset/recovery_failure/account_recovery at `auth.ts`, email_deleted at `settings.ts`, upgrade/upgrade_failure at `custody.ts`), retention-sweep boot-fatal regex `/Retention\s*:\s*(\d+)\s+months/i` parse unaffected (existing "Retention: 24 months" substring preserved verbatim), idempotency invariant intact (`COMMENT ON COLUMN` remains unconditional). Reviewer-surfaced residual risks (pre-existing emdashes in `--` SQL header comments per the code-comment exemption; two-location paragraph duplication accepted as Option B's deliberate design — header is per-file docstring, body is what `\d+` surfaces; op-type enumeration may go stale, mitigated by `e.g.,`) all below the actionable threshold.

Cross-refs: `agents/docs/solutions/conventions/load-bearing-greps-at-signal-block-write-time-2026-05-06.md` (the regex-parse-of-COMMENT pattern startup-checks.ts relies on); `agents/docs/solutions/conventions/docblock-anchor-stable-symbols-not-line-numbers-2026-05-15.md` (the convention the round-2 symbolic-anchor swap anticipated); `agents/docs/api-contracts/custody.md` "Audit log retention" subsection; `agents/docs/ARCHITECTURE.md` light-account consent-op cross-ref; `backend/migrations/006_custody_audit_pii_annotation.sql` (the canonical retention source); `backend/src/jobs/custody-audit-retention-sweep.ts` (consumer of the regex parse).


## ARCHITECT-COMMENT-ANCHOR-CONVENTIONS-LOAD-BEARING-SURFACE (archived 2026-05-18) — architect-self-implemented ✓

Single-round architect-self-implemented task lifting the comment-anchor convention cluster from `agents/docs/solutions/conventions/` into a load-bearing CLAUDE.md surface that implementer agents load at startup. Closes the structural gap documented in `conventions-in-solutions-dont-reach-implementer-context-2026-05-18.md`: implementer agents (UI, backend, pinner) do not load `solutions/` at startup, so comment-rot rules only surfaced reactively via `/ce-code-review`'s `ce-learnings-researcher` persona. The architect's own hold-block writing had the same blind spot, most recently demonstrated on `ui-papers-orcid-null-fallback-verification` round 3, where the prescribed `(see task <slug>)` fix shape contradicted the 2026-05-15 `task-slug-citations-in-comments-go-stale-on-archive` convention that had landed three days earlier.

Implementation (`00d67b9`): shape (b) per the task pitch — lifted three rules into a new root `CLAUDE.md` `## Comment anchors` section between "Commits and Pushes" and "Worktree Cleanup". Rules: (1) task-slug citations rot on archive (250-line trim of `tasks-archive.md` eventually drops the slug entirely); (2) line-number and SHA anchors drift through edits, rebases, and fork re-clones; (3) convention-enforcing fixes must audit their own replacement, with hold-block prescriptions explicitly in scope. Backend's pre-existing in-zone section (commit `1d3e4a1`, same-day architect work) collapsed to a 2-line pointer; matching pointers added to `agents/ui/CLAUDE.md` and `agents/pinner/CLAUDE.md` (the latter scoped to `.go` / `_test.go` / SQL). Single canonical surface, no three-way drift, every agent reads it on every startup. Self-audit on the new prose: no real task slugs, round numbers, SHAs, or load-bearing line-numbers — the positional examples in the section are generic placeholders illustrating violations, not durable anchors the section relies on. Cross-refs: `task-slug-citations-in-comments-go-stale-on-archive-2026-05-15.md`, `docblock-anchor-stable-symbols-not-line-numbers-2026-05-15.md`, `convention-enforcing-fix-must-audit-its-own-new-code-2026-05-17.md`, `hold-block-must-not-contradict-convention-docs-2026-04-22.md`, `conventions-in-solutions-dont-reach-implementer-context-2026-05-18.md`.


## UI-AUTH-DISCONNECT-CLEAR-ORCID-RETURN-TO (archived 2026-05-18) — round-1 clean ✓

Single-round P3 task adding parity sessionStorage scrub of `pevo_orcid_return_to` alongside the existing `pevo_orcid_mode` scrub in `frontend/src/auth.js::disconnect()`. Closes the julik-frontend-races RR-1 gap originally surfaced in the `/ce-code-review` on the sister fix `ui-orcid-callback-destroy-clear-return-to` (commit `2cb0051`, archived 2026-05-17): a concurrent-tab logout StorageEvent during a `/recover` ORCID flow could leak a stale `pevo_orcid_return_to='recover'` pointer into a later orcid-callback resolution on the same tab, mis-routing the user to `/recover` instead of `/signup` after a successful ORCID link.

Round-1 (`4cbac4e`): single `sessionStorage.removeItem('pevo_orcid_return_to')` line added inside the existing try/catch in `disconnect()` (no new defensive wrapping needed — the try/catch already wrapped the `pevo_orcid_mode` scrub for sessionStorage-unavailable private-mode environments). Two new unit specs in `frontend/tests/unit/auth.test.js` pin both removals via `vi.stubGlobal('sessionStorage', ...)` — the convention-canonical pattern per `storage-prototype-vi-spyon-jsdom-2026-05-17.md`. The new `pevo_orcid_mode` positive case fills a coverage gap that pre-existed the diff. Round-1 `/ce-code-review` (6 personas — correctness on Opus; testing/maintainability/project-standards/julik-frontend-races/learnings on Sonnet; `ce-agent-native-reviewer` skipped per CLAUDE.md; adversarial / security / api-contract / migrations / reliability / kieran-typescript skipped — 1-line production change, plain JS not TS, no API or auth boundary): clean. The julik-frontend-races emission was a confirmation packaged as a finding (`suggested_fix: "No change needed"`) — suppressed per false-positive catalog. Learnings-researcher confirmed alignment with `storage-scope-localstorage-vs-sessionstorage-for-spa-flow-state-2026-05-17.md` § 3 "Cleanup parity" (the convention that anticipated this exact gap). Cross-refs: `storage-scope-localstorage-vs-sessionstorage-for-spa-flow-state-2026-05-17.md`, `storage-prototype-vi-spyon-jsdom-2026-05-17.md`.


## BACKEND-TESTS-TYPECHECK-RESIDUAL-DRIFT (archived 2026-05-18) — round-1 clean ✓

Single-round task clearing the 249-error pre-existing baseline in `npm run typecheck:tests` and chaining `typecheck:tests` into `npm run typecheck` so the canonical pre-merge command catches `LogContext` typos and other test-file drift on commit. The follow-up task itself was filed by the backend agent at the end of `backend-tests-typecheck-coverage` round-1 hold path-(b) defer-and-document: the parent task delivered the structural foundation (`backend/tests/tsconfig.json` + standalone `npm run typecheck:tests` script + LogContext typo-bypass fix in `broadcast-error.test.ts`), and this task closed the 249-error backlog separately so the parent commit stayed reviewable.

Round-1 (`9a6edf1`): 249 errors → 0 across `tests/tsconfig.json`. `module: ESNext` + `moduleResolution: Bundler` set in tests-tsconfig (TS1309 top-level-await dropped 143 errors); `vitest/globals` added to `types:[]` so describe/it/expect/vi/beforeEach are visible without per-file imports; 12 mocked-pool `hafQueryMock` factories widened from `vi.fn(async () => ({rows: []}))` to `vi.fn(async (..._args: any[]) => ({rows: [] as any[]}))` so per-test `mockImplementation`s type-check across various (sql/params) signatures and row shapes; bridge.test.ts BridgeLookupResult import path corrected + `buildBridgeMetadata` call updated 6→10 args + `BridgePevo` extraction type added; 5 `body: unknown` widened to `Record<string, unknown>` for supertest `.send()` compatibility; 9 implicit-`any` callback params annotated; search.test.ts `let res` typed as `SupertestResponse`; auth-smtp-transporter.test.ts cast on `spy.mock.calls[0][0]`; orcid.test.ts two `redis.get` mockImplementations cast `as never` for ioredis overload; logger-redact.test.ts cast `new Error(...)` to `Error & { cause?: Error }` for TS lib Error.cause unknown-vs-Error; argon2-error-mocks 6 `mockFn()` sites got dummy `async () => undefined`, plus 3 missing exports (`getArgon2AbortCount`, `startArgon2AbortReporter`, `stopArgon2AbortReporter`) added to the semaphore mock factory, plus `runWithArgon2Slot: mockFn` cast to preserve generic. Chained `npm run typecheck` runs `typecheck:src` + `typecheck:tests`; spot-check verified — injected `{run:'msgfn-throws', usrname:'alice'}` at broadcast-error.test.ts:458 produced TS2561 "Did you mean 'username'?" at exit code 2; reverted. Documented the convention in `agents/backend/CLAUDE.md` under a new "Typecheck" section.

Round-1 `/ce-code-review` (6 personas — correctness on Opus; testing/maintainability/project-standards/kieran-typescript/learnings on Sonnet; `ce-agent-native-reviewer` skipped per CLAUDE.md; `ce-adversarial-reviewer` skipped — test-infrastructure-only diff with no auth/payments/mutations). The substantive findings on this commit: (a) the `agents/backend/CLAUDE.md` edit was architect-owned per `.githooks/commit-msg:138` (`^agents/[a-z]+/CLAUDE\.md$` in the architect zone) — the backend agent's commit violated the zone-audit hook, which must not have been installed in the authoring clone since the commit landed without the `[skip-zone-audit]` marker. The substantive content is correct; the architect re-authored the same file in a follow-up commit adding a `## Comment anchors` section to close cluster-D's convention-recurrence carry-forward, putting an architect-attributed write on the file. (b) Two `as never` casts in `orcid.test.ts:703, :735` for ioredis overload — dismissed below confidence gate (same risk class as `any` in test mocks the CLAUDE.md Typecheck section already endorses). (c) Signal block omission on the task file when moved pending → review — convention disagreement (project-standards reviewer reads first-review move as exempt; learnings reads always-required); dismissed for this commit, captured for future `/ce-compound` clarification. Cross-refs: `discipline-interface-tsc-perimeter-omission-2026-05-11.md`, `conventions-in-solutions-dont-reach-implementer-context-2026-05-18.md`, `vitest-tdz-factory-closure-vs-import-graph-eager-eval-2026-05-16.md`, `typescript-resolution-mode-esm-import-from-cjs-2026-05-16.md`.


## BACKEND-SEARCH-QUERY-PARAM-TYPEOF-NARROW-SWEEP (archived 2026-05-18) — round-4 clean ✓

Four-round task extending the `?type=` typeof-narrow + helper-extract pattern (originally landed in `backend-search-reviews-contract-reconcile`) to the three sibling `/api/search` query params `?language=`, `?source=`, `?sort=`. The failure mode each one closed: `req.query.x as string` silently coerces a `string[]` from repeated params into a comma-joined string, which then either silently mismatches downstream SQL binds (`?language=en&language=fr` → `'en,fr'` → zero results), silently shifts ternary semantics (`?source=native&source=bridge` → falls through to `'all'`), or silently falls through a default (`?sort=date&sort=relevance` → `'relevance'` default). Round-1 (`e7a495f`) extracted `isSearchType`/`isSearchSource`/`isSearchSort`/`parseLanguageFilter` into `backend/src/types/search-filters.ts`, tightened `searchFromHaf` signature to `type: SearchType`, added 4 repeated-param tests + `?language=` length-cap follow-up task. Round-1 hold (4 items, kieran-typescript+maintainability anchor 100): `source` variable narrow asymmetric (widens back to string after `isSearchSource`); `searchFromHaf` + `searchPapersFromHaf` internal signatures had `sort: string` / `source: string | undefined` instead of the literal-union types; 4 new helper exports lacked direct unit tests; route-level happy-path tests missing for `?source=native`/`?source=bridge`. Round-2 (`06a9ac4`) threaded `SearchSource` through route + both internal signatures, added 4 helper-direct describes (11 specs) + 4 route-level specs. Round-2 hold (2 items): `searchReviewsFromHaf` `sort` param not tightened (third sibling missed); no `?sort=relevance` happy-path spec. Round-3 (`9335aef`) tightened the third signature + added the spec. Round-3 hold (1 item): `searchReviewsFromHaf` accepts narrowed `sort: SearchSort` but body ignores it (hardcoded `ORDER BY 'c.created DESC'`); architect-decision was to document the intentional discard rather than remove the param (round-2 explicitly chose narrow-and-keep for symmetry with future relevance-ranking on reviews).

Round-4 (`f60420d`, comment-only): 4-line WHY comment added immediately before `const orderBy = 'c.created DESC';` inside `searchReviewsFromHaf` per verbatim architect prescription — explains that `sort` is accepted for signature symmetry with `searchPapersFromHaf`, relevance-ranking for reviews is not yet implemented, and points to the wire-through site + the companion `?type=review&sort=relevance` happy-path spec for when relevance-ranking lands. Round-4 `/ce-code-review` (6 personas — correctness on Opus; testing/maintainability/project-standards/kieran-typescript/learnings on Sonnet; `ce-agent-native-reviewer` skipped per CLAUDE.md; `ce-adversarial-reviewer` skipped — 1-line production change well below the 50-line threshold): no findings survive the confidence gate. KT-1 (URL-pattern pointer durability) and KT-2 (no `_sort` underscore prefix) below the 75 anchor. Sibling architect commit `5978a41` (`papers.md`) pinned the `/api/search` `?sort=`/`?source=`/`?language=` contract docs separately during round-1 triage. Architect carry-forward (papers.ts:477,479,517-519 cast sweep per `req-query-as-string-cast-silent-coerce-2026-05-16` learning) is still on the architect backlog. Cross-refs: `req-query-as-string-cast-silent-coerce-2026-05-16.md`, `route-level-error-class-coverage-after-helper-extraction-2026-04-29.md`.


## BACKEND-DECRERR-NARROWING-SYMMETRIC (archived 2026-05-18) — round-2 clean ✓

Two-round task applying the canonical `xxxErr instanceof Error ? xxxErr : new Error(String(xxxErr))` narrowing pattern to every `catch (xxxErr) { logger.{warn,error}({err: xxxErr, ...}) }` site in `backend/src/routes/accreditation.ts` so pino's `err` serializer always receives an `Error` instance — closing the "plain-object projection loss" failure mode where a non-`Error` rejection (mock, future helper, edge case) would land as `type='Object'` per `pino-err-slot-plain-object-projection-loss-2026-05-15.md`. Round-1 (`5883be8`) narrowed `incrErr` (pre-INCR cap counter catch on `/verify`), `decrErr` (broadcast-timeout decrement catch), and `deleteErr` (signup-verify-link parent cleanup catch). Round-1 hold (2 items, correctness+maintainability cross-reviewer anchor 100): the `mailErr` binding in the `/request` SMTP catch (around line 353) and the anonymous `err` binding in the `setInterval`-wrapped `cleanupExpiredTokens().catch()` callback (around line 919) were missed; both flowed raw `unknown` into pino's `err` slot.

Round-2 (`f80cab9`): both items landed with the canonical ternary; the cleanup catch's inline shorthand was reflowed onto multiple lines so the narrowed `err:` line carries the ternary cleanly. Optional-sibling-sweep enumeration of 6 unnarrowed sites in 5 sibling route files recorded as surface record only — `anonymousReview.ts:222` (attestErr), `auth.ts:530` (mailErr in /signup SMTP), `orcid.ts:1358` (expireErr in binding-lock TTL extension), `settings.ts:164` (mailErr in settings email-post SMTP), `signup-verify.ts:450,677` (probeErr in /confirm and /link HAF probes); not gated on this task. Round-2 `/ce-code-review` (7 personas — correctness on Opus; testing/maintainability/project-standards/kieran-typescript/reliability/learnings on Sonnet; `ce-agent-native-reviewer` skipped per CLAUDE.md; `ce-adversarial-reviewer` skipped — 8-line production change below the 50-line threshold): clean. Helper-extraction (`narrowToError`) remained correctly dismissed at the 5-of-6-site count, below the 6+ inline-vs-extract threshold the architect set in round-1. Cross-refs: `pino-err-slot-plain-object-projection-loss-2026-05-15.md`, `pino-err-slot-sibling-bypass-redact-policy-2026-05-06.md`, `auth-structured-log-shape-2026-04-29.md`.


## BACKEND-ACCREDITATION-LIMITER-SKIP-FAILED (archived 2026-05-18) — round-4 clean ✓

Four-round task adding `skipFailedRequests: true` to `accreditationRequestLimiter` (account-keyed, 3/24h) to close the SMTP-transient slot-burn cascade: a single transient SMTP failure burned one of the user's 3 daily slots; three failures in 24h locked them out with no recourse. Round-1 (`a6367ab`) landed the limiter config change plus a 5xx-refund canary in `accreditation.test.ts` mirroring the sibling `custody-upgrade.test.ts` slot-refund pattern. Round-2 hold (6 items, cross-corroborated correctness × security × testing on the comment-vs-reality contradiction at anchor 100): the preamble comment falsely claimed "4xx still consumes a slot" but `rateLimit.ts` refunds on ANY `statusCode >= 400`; added a symmetric 4xx-refund canary; adopted `deleteTokenBestEffort` at both /request SMTP-failure deleteToken sites; fixed inline test comment mechanics misdescription; corrected carve-out clause-(c) line citation. Round-3 hold (3 items, project-standards × maintainability): line-number anchors stripped from production comment but not from test comments; wrong-as-written `rateLimit.ts:100-101` citation (actual refund gate is `:156`) in test inline comment; round-1 Item-6 half-fix at test prose still citing `custody-upgrade.test.ts:498`.

Round-4 (`ac9e93d`, comment-only): test header prose and clause-(c) sweep now anchor on the sibling test's full title `Hive getAccounts throws then recovers: 503 refunds limiter slot so the retry succeeds`; `rateLimit.ts:100-101` citation replaced with `RateLimitConfig.skipFailedRequests` JSDoc symbol; 4xx-canary preamble anchors on `accreditationRequestLimiter` identifier; `deleteTokenBestEffort` JSDoc dropped `per round-2 F8` task-cycle marker. Round-4 `/ce-code-review` (5 personas — correctness on Opus; testing/maintainability/project-standards/learnings-researcher on Sonnet; `ce-agent-native-reviewer` skipped per CLAUDE.md): one P3 pre-existing finding (bare slug `// backend-accreditation-limiter-skip-failed.` at `accreditation.test.ts:1659-1660`, introduced round-1, not touched in rounds 2-3) dismissed as pre-existing per `feedback_dismiss_preemptive_test_hardening`. Cross-refs: `docblock-anchor-stable-symbols-not-line-numbers-2026-05-15.md`, `task-slug-citations-in-comments-go-stale-on-archive-2026-05-15.md`, `convention-enforcing-fix-must-audit-its-own-new-code-2026-05-17.md`.


## BACKEND-ACCREDITATION-VERIFY-LIMITER-SKIP-FAILED (archived 2026-05-18) — round-2 clean ✓

Two-round task adding `skipFailedRequests: true` to `accreditationVerifyLimiter` (IP-keyed, 5/60s) and emitting `Retry-After: 30` on both retriable 503 branches (`ACCREDITATION_GATE_UNAVAILABLE` HAF-outage gate and `SERVICE_UNAVAILABLE` pre-INCR-Redis-failure). The cascade: HAF outage triggers 503 with `details.retriable: true`; without `skipFailedRequests`, each 503 burns one of the 5/60s slots and the legitimate user trips 429 RATE_LIMITED before HAF recovers. Round-1 (`15856a5`) landed the limiter config change plus `Retry-After: 30` on the ACCREDITATION_GATE_UNAVAILABLE path plus a slot-refund canary mirroring the upgrade-limiter sibling. Round-1 hold (3 items, cross-corroborated reliability × adversarial × api-contract anchor 100): the sibling SERVICE_UNAVAILABLE 503 branch also emitted `details.retriable: true` but no `Retry-After`, so the SPA's `_startCooldown(null) → initial = 0` enabled tight click-loop refund-amplification on that branch; preamble line-number anchors stripped (4 sites, 2 already stale at write time); slug prefixes dropped from 2 test-block headers in `accreditation-idempotency.test.ts`.

Round-2 (`c48ceeb`, via worker-branch merge `6537aee`): `res.set('Retry-After', '30')` added immediately before `sendError(res, 503, 'SERVICE_UNAVAILABLE', ...)` so both retriable 503 branches share the 30s server-driven backoff floor; preamble rewrite anchors on symbolic error codes + `RateLimitConfig.skipFailedRequests` JSDoc identifier; slug prefixes + `accreditation.ts:36` and `custody-upgrade.test.ts:518` line citations replaced with `accreditationVerifyLimiter` identifier + the sibling test's full title. Round-2 `/ce-code-review` (6 personas — correctness on Opus; testing/reliability/maintainability/project-standards/learnings-researcher on Sonnet; `ce-agent-native-reviewer` skipped per CLAUDE.md): 0 net-new findings. One pre-existing 502-vs-expected-503 spec failure (`pre-INCR redis.eval rejection`) makes the new `expect(res.headers['retry-after']).toBe('30')` pin dormant; behavior verified by inspection; sibling ACCREDITATION_GATE_UNAVAILABLE Retry-After pin at `accreditation-idempotency.test.ts:545` is green and exercises the identical pattern, providing regression-floor coverage for the shape. Archive-time follow-ups landed: `accreditation.md` documents `Retry-After: 30` on both retriable 503 rows; `common.md` cross-cutting note updated to reflect the combined `details.retriable: true` + `Retry-After` signal shape on non-argon2 503 paths; `skip-failed-requests-jwt-required-credential-verify-carve-out-2026-05-17.md` audit grid extended with `accreditationVerifyLimiter` (IP-keyed one-shot ceremony, 256-bit token entropy as the brute-force bound). Cross-refs: `skip-failed-requests-jwt-required-credential-verify-carve-out-2026-05-17.md`, `cross-surface-parity-audit-at-sibling-composition-sites-2026-05-14.md`.


## BE-ACCREDITATIONS-LIKEGUARD (archived 2026-05-18) — round-4 clean ✓

Four-round task porting the BE-SEARCH-Q-LIKEGUARD-AND-LENGTH-CAP defenses to `/api/accreditations` `?field=` and `?institution=` filters, surfaced by the security + adversarial reviewers' scope-completeness check on the parent `?q=` fix (commit `869fea4`). The endpoint is unauthenticated read and shares the same general-purpose rate limit as `/api/search`; without escape + length-cap + `ESCAPE '\\'`, a `_%_%_…` enumeration injected unbounded ILIKE wildcards against every active-accreditation row. Round-1 (`fc0aa29`) added a new `validateOptionalLikeFilter()` sibling helper in `search-filters.ts` (differs from `validateSearchQuery` on optional-vs-required absent semantics), wired both filter sites through the helper, added `ESCAPE '\\'` to both ILIKE bind sites in `fetchAccreditationsFromHaf`, and 13 real-path specs in `accreditations-likeguard.test.ts`. Round-1 review hit Anthropic rate-limit mid-fan-out (5 reviewers truncated); round-2 (`848ff00`) landed slug-citation cleanup at 3 sites + maintainability-driven route-entry comment trim. Round-2 deferred-persona re-review surfaced 2 assertion-strength gaps: the task's mocked-pool SQL-contract spec (acceptance §4) hadn't landed, and the repeated-param specs only asserted 200 + array-shape (no result-set proxy).

Round-3 (`152e164`) landed a new sibling `accreditations-likeguard-mocked.test.ts` with 5 specs pinning bound-param LIKE-escape + `ESCAPE '\\'` clause emission at both ILIKE sites (carve-out clauses a/b/c documented inline) plus strengthened repeated-param specs with `meta.total === baseline` proxy. Round-3 hold (3 citation-hygiene items, cross-corroborated maintainability × project-standards anchor 100): 5 raw line-number citations + task-slug citations + cross-file line range introduced in the new mocked test file. Round-4 (`9f6e1d2`, comment-only): line-number anchors swept; task slugs (`BE-ACCREDITATIONS-LIKEGUARD`, `BE-SEARCH-Q-LIKEGUARD-AND-LENGTH-CAP`) dropped; cross-file `disciplines-canon-mocked.test.ts:817-903` replaced with the describe-block title `GET /api/search — ?q= LIKE-escape SQL contract`; SQL-shape framing corrected ("single window-function data query in `fetchAccreditationsFromHaf`" — there are NOT separate count and data branches). Round-4 `/ce-code-review` (5 personas — correctness on Opus; testing/maintainability/project-standards/learnings-researcher on Sonnet; `ce-agent-native-reviewer` skipped per CLAUDE.md): one P2 pre-existing finding (signal block cites SHA `71b3099` from worker-branch fanout but actual main SHA is `9f6e1d2`; content landed clean) dismissed as pre-existing convention drift. Archive-time follow-up landed: `accreditation.md` GET /api/accreditations section documents the 200-char cap, per-param "too long" 400 messages, LIKE-metacharacter literal-treatment, repeated-param silent-unfilter, and prefix-match semantics. Cross-refs: `task-slug-citations-in-comments-go-stale-on-archive-2026-05-15.md`, `docblock-anchor-stable-symbols-not-line-numbers-2026-05-15.md`, `convention-enforcing-fix-must-audit-its-own-new-code-2026-05-17.md`, `worktree-fanout-orphan-detection-2026-04-29.md`.


## UI-ACCREDITATION-VERIFY-NETWORK-ERROR-RETRIABLE (archived 2026-05-18) — round-2 clean ✓

Two-round task extending the SPA's accreditation-verify error classification so network-layer errors (TypeError from fetch failure, AbortError from the 30s fetch timeout in `api.js`) route to the Retry CTA instead of falling through to the generic `'error'` state with the "Request New" CTA that burns a `/accreditation/request` slot. Surfaced by `/ce-code-review` finding #5 on the sibling `ui-accreditation-verify-retriable-handling` round-1 commit `b66a370`: that task closed the backend-emitted retriable-envelope cascade (`err.code === 'ACCREDITATION_GATE_UNAVAILABLE'` or `err.details?.retriable === true`), but network-layer errors don't reach `ApiRequestError` and so don't carry `.code` / `.details` — the same cascade, one error class up. Round-1 (`a6fc5d4`) added a separate `_isNetworkError(err)` predicate keyed on `err?.name === 'TypeError' || err?.name === 'AbortError'`, routed network errors to the existing `retriable_error` state (Retry CTA), differentiated cooldown by name (AbortError → 5s, TypeError → 0s — immediate retry on connectivity restoration), added the `verify.networkUnavailable` translation key across en.json + 15 stub locales, and 3 specs in `pages-accreditation-verify.test.js`. Round-1 hold (1 item, cross-corroborated maintainability × project-standards anchor 100): new `describe('network-layer error handling')` block header opened with `// UI-ACCREDITATION-VERIFY-NETWORK-ERROR-RETRIABLE:` slug prefix.

Round-2 (`b1f36dd`, 1-line comment edit): slug prefix dropped; behavioral framing preserved verbatim. Round-2 `/ce-code-review` (5 personas — correctness on Opus; testing/maintainability/project-standards/learnings-researcher on Sonnet; `ce-agent-native-reviewer` skipped per CLAUDE.md): one P2 pre-existing finding (3 surviving sibling-task slug citations at `pages-accreditation-verify.test.js:98, 252, 303` — the sibling task `ui-accreditation-verify-retriable-handling` archived 2026-05-17 so the slugs are dead pointers) dismissed as pre-existing; the round-1 sweep correctly bounded its own scope. A separate UX-cascade follow-up was filed at round-1 architect triage: AbortError-after-server-success (broadcast confirms on chain, response lost at the 30s timeout, user clicks Retry on the now-deleted token, backend 400, SPA routes to Request New → burns slot despite the accreditation having succeeded). That task is the N+1 error class and requires backend coordination; tracked separately. Cross-refs: `task-slug-citations-in-comments-go-stale-on-archive-2026-05-15.md`, `convention-enforcing-fix-must-audit-its-own-new-code-2026-05-17.md`, `reviewer-discovery-error-class-stack-2026-05-18.md`.


## BACKEND-SEARCH-PARTIAL-DEGRADATION-ALLSETTLED (archived 2026-05-18) — round-3 clean ✓

Three-round task surfaced from round-1 reliability review (REL-03 P2/75) of sibling `backend-papers-filter-accreditation` lane-3. Goal: surface partial results on `?type=all` search when one HAF branch transiently fails, with a structured log event signaling the partial failure, instead of the prior `Promise.all`-swallow behavior that collapsed both branches into an empty 200. Round-1 (`31b02fb`) replaced `Promise.all` with `Promise.allSettled` on the `?type=all` branch at `backend/src/routes/search.ts:286-308`, emitting `event: 'search.type_all.partial_degradation'` on either branch rejection with `branch` (papers|reviews) and `queryParams` discriminators; canaries pinned both-branch-throw and single-branch-throw behavior via a sentinel-prefixed mock-discriminator (necessary because both branches share the underlying `pool.query` and the test-side mock has to distinguish them on the SQL shape). Round-2 (`882f6ed`) addressed 5 cluster items including a both-branches-throw canary (`canary 5`: two warn events fire, one per branch), `instanceof Error` narrowing for cross-realm safety on the `errClass` extraction, route-level Redis-fail-closed coverage, and `queryParams` payload pin extension to cover `source` and `includeRetracted` (added alongside the round-2 accreditation-gate lane-3).

Round-3 (`bba29c0`, comment-only, 12-line diff): canary 4 lead-in comment at `search-partial-degradation.test.ts:242-247` rewritten to scope the pin accurately. The prior "every field of the route's `queryParams` object" claim overstated coverage (production payload has 8 fields; canary asserts 6 user-filter fields). New comment explicitly carves out pagination internals (`limit`, `offset`) as out-of-scope, naming the actual regression class the canary catches ("new filter added but not threaded into the warn payload"). Architect's intentional decision NOT to extend assertions to `limit`/`offset` is documented in the comment itself — pagination drift is a different regression axis (pagination internals, not user filters), and extending the canary would be preemptive hardening for the wrong axis. Round-3 `/ce-code-review` (5 personas — correctness on Opus; testing/maintainability/project-standards/ce-learnings-researcher on Sonnet; ce-agent-native-reviewer skipped per CLAUDE.md, ce-adversarial-reviewer skipped for comment-only diff well below 50-line threshold): 1 P3 advisory finding (cross-reviewer correctness+testing anchor 100) — commit message body of `bba29c0` undercounts the round-2 assertion set (says "asserts 4", actual is 6 since round-2 added `source` and `includeRetracted` alongside the four original user filters). Dismissed during triage: commit message is git-immutable and the in-tree comment is accurate. Cross-ref: `mutation-kill-claims-must-match-assertion-and-corpus-2026-05-15.md` (commit aligns verbatim with this convention's prescribed "name what IS covered + name what is excluded + state why exclusion is deliberate" form).


## BACKEND-ORCID-CLAIM-MISMATCH-POST-REVOCATION-AUDIT (archived 2026-05-18) — round-4 clean ✓

Four-round task closing the post-revocation audit blind spot surfaced by `backend-multi-author-cumulative-union` round-1 adversarial review (adv-005 P2/80): once a bad actor's accreditation was revoked they dropped out of `getAccreditedOrcidsByAccount`, so subsequent forged-ORCID broadcasts no longer triggered `orcid_claim_mismatch` — exactly when operator triage needed the signal. Round-1 picked Alternative 2 (extend accreditation lookup to include revoked-but-once-accredited hives via a new `accreditationStatusCteBody()` deriving from `accred_ranked`), added `getAllEverAccreditedOrcidsWithStatus()` to `accreditation.ts`, gated server-override on `status === 'active'` (revoked accounts no longer have authoritative ORCID standing), and emitted `accreditationStatus: 'active'|'revoked'` in the audit payload so operators can distinguish active-spoof from post-revocation residual. Round-2 hold (5 items, `b2e9ef5`): test-mock matcher discrimination for the canonical-root walker SQL (shares SELECT shape with `headAuthorsLookupSql` → walker matcher MUST be listed first or the walker probe gets swallowed and `cont_columns_invalid` bail trips), case-d additive-field + dedup canary, helper extraction for cohesive emission, rename grep-sweep for the AllEver helper, doubled-docstring fix on `getAllAccreditedAccounts`. Round-3 (`/ce-code-review` 9 personas; reliability/kieran-typescript/performance skipped per mechanical-hold-fix scope) held 2 items: `AccreditationStatus` type stranded between docstring and function body, and round-N hold-item-marker comments in production + test code (violating `task-slug-citations-in-comments-go-stale-on-archive-2026-05-15.md` AND `convention-enforcing-fix-must-audit-its-own-new-code-2026-05-17.md`).

Round-4 (`72ff646`, mechanical cleanup, 4-line prod diff + 4 test-comment rewrites): type relocated to module-level type-export zone at `accreditation.ts:7`; case-d production comment at `papers.ts:476` rewritten to behavioral MUST (`accreditationStatus: 'active'` + `auditedKeys` dedup; failure mode: dashboards filtering by status silently miss case-d spoofs); 4 test-file sites at `papers-cumulative-orcid-audit.test.ts` (lines 131, 196, 400, 439) anchored on behavioral invariants. Round-4 `/ce-code-review` (5 personas — correctness on Opus; testing/maintainability/project-standards/learnings-researcher on Sonnet; reliability/kieran-typescript/security/adversarial/api-contract skipped per mechanical-cleanup scope; `ce-agent-native-reviewer` skipped per CLAUDE.md): 0 findings. Self-violation audit per `convention-enforcing-fix-must-audit-its-own-new-code-2026-05-17.md`: test file is fully clean of round-N markers (grep returns empty); remaining markers in `papers.ts:727,971,1824,1897,1936` and `accreditation.ts:33,155` belong to sibling tasks (BACKEND-REPUTATION-SSOT, BACKEND-MULTI-AUTHOR-CUMULATIVE-UNION forward-walker) and are out-of-scope for this hold per round-3 architect note. Cross-refs: `task-slug-citations-in-comments-go-stale-on-archive-2026-05-15.md`, `convention-enforcing-fix-must-audit-its-own-new-code-2026-05-17.md`.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>

## BACKEND-CUSTODY-SESSION-AUTH-PASSWORD-MINT (archived 2026-05-17) — round-3 clean ✓

Three-round task adding `POST /api/custody/session-auth` for password-mechanism session-kind fresh-auth proofs (sibling to ORCID-mechanism session_auth, unblocking the non-consent broadcast surface for self-custody password-mechanism accounts per ARCHITECTURE.md §6.5 #1). Round-1 landed the route, `freshAuthLimiter` adoption, `burnSentinel` await-discipline at null-hash sites, argon-error canaries. Round-2 (`6dad488`) addressed 6 items including `skipFailedRequests` adoption on both `freshAuthLimiter` and `sessionAuthLimiter` (with Lua atomic primitive), spy-assertion mutation kill on burnSentinel, definite-assignment narrowing, slug prefix cleanup. Round-2 architect re-review caught a P3 documentation defect: the security-argument comment for `skipFailedRequests` adoption made two false claims (`loginLimiter` "per-account JWT issuance gate" — actually IP-keyed via `byIp`; argon2 semaphore "bounds per-account attempts" — actually server-wide ~80 verifies/sec aggregate).

Round-3 (`5fa6663`, comment-only, 35-line diff): both `freshAuthLimiter` AND `sessionAuthLimiter` comments rewritten to honest deliberate-tradeoff framing — closing legitimate-user-lockout DoS (stolen JWT + 10 wrong passwords = 60s lockout) over rate-bounding per-account brute-force; JWT theft is PEvO's accepted upstream prerequisite per `project_single_instance_only`; argon2 server-wide semaphore caps aggregate verifies/sec but does NOT bound per-account; `loginLimiter` is IP-keyed so does NOT bound per-account brute-force from a JWT-holding attacker rotating IPs; unbounded per-account brute-force is the accepted residual. Round-3 `/ce-code-review` (6 personas — correctness/security on Opus; maintainability/project-standards/previous-comments/learnings-researcher on Sonnet; adversarial/reliability/kieran-typescript skipped for comment-only diff; `ce-agent-native-reviewer` skipped per CLAUDE.md): 0 primary findings. Cross-refs: `skip-failed-requests-jwt-required-credential-verify-carve-out-2026-05-17.md` (primary motivating convention), `verify-resource-knob-math-before-load-bearing-security-margins-2026-04-22.md` (argon2 semaphore scope origin), `verify-library-claims-before-load-bearing-security-margins-2026-04-22.md` (rule against repeated unverified claims).


## BACKEND-CUSTODY-BROADCAST-ORCID-FRESH-AUTH (archived 2026-05-17) — round-4 clean ✓

Four-round task wiring `fresh_auth_proof` (mode='session_auth' ORCID round-trip) into the non-consent broadcast path, closing ARCHITECTURE.md §6.5 invariant #1 on that surface. Round-1 landed `handleSessionAuth` + the custody.ts non-consent verification block. Round-2 (`1437e41`) addressed 6 items: KEY_PREFIX, `assertNever` on `storedMode`, `isOrcidMode` runtime guard, Redis try/catch, dual-write removal, dead-arm cleanup. Round-3 (`a1834ce`) landed 4 items including slug-citation cleanup at 3 named sites and `Set<OrcidMode> satisfies ReadonlySet<OrcidMode>` retyping for VALID_MODES. Round-3 architect re-review caught the convention-sweep miss: 3 more slug citations at sites NOT named in round-2 (orcid.ts:84 docblock, orcid.ts:1181 banner, custody.ts:350 label), plus VALID_MODES docstring overclaimed compile-time union-completeness — the `Set+satisfies` pattern only verifies per-element type; `assertNever` in `/callback` over `storedMode` is the actual exhaustiveness backstop.

Round-4 (`f29028f`, comment-only, 41+/18-): 3 sites rewritten with symbol-anchored behavioral descriptions, VALID_MODES docstring corrected to accurately describe per-element vs union-completeness + point at the dispatch-side `assertNever` + name 400 BAD_REQUEST runtime fallback, `Round-3 hold KT-1:` prefix stripped. Round-4 `/ce-code-review` (6 personas — correctness on Opus, kieran-typescript/maintainability/project-standards/previous-comments/learnings-researcher on Sonnet; security/reliability/adversarial skipped for comment-only diff; `ce-agent-native-reviewer` skipped per CLAUDE.md): 0 primary findings. Convention-enforcement self-audit (per `convention-enforcing-fix-must-audit-its-own-new-code-2026-05-17.md`) confirmed clean; `grep -rn 'BACKEND-CUSTODY-BROADCAST-ORCID-FRESH-AUTH' backend/src/` returns zero hits. Cross-refs: ARCHITECTURE.md §6.5 invariant #1, `task-slug-citations-in-code-comments-go-stale-on-archive-2026-05-15.md`, `discipline-interface-tsc-perimeter-omission-2026-05-11.md` (adjacent — TS-construct overclaim class).


## BACKEND-ORCID-CUSTODY-DEFAULT-INVARIANT (archived 2026-05-17) — round-5 clean ✓

Five-round task closing the JWT-vs-DB drift on the `custody` claim: orcid.ts was minting `{custody: account.custody || 'light'}` for ORCID-only accounts, coercing null DB-state to a JWT claim the DB never stored, which let `/custody/upgrade` pass the gate into the password-verify branch where `account.password_hash` is NULL. Round-1 (`36b3f49`, Option A) dropped both `||` defaults — `verifyHiveSignature`'s `|| 'self'` middleware fallback now closes the drift correctly. Round-2 hold corrected the original "ORCID-only custody=NULL" framing to state C (`custody='light' + password_hash=NULL`) per ARCHITECTURE.md §6.1. Round-3 (`3a9f7af`) landed 3 of 5 items; 2 became moot when sibling commit `1f1be4e` (seed-phrase re-auth) replaced the password-verify branch entirely. Round-4 (`ea54f91`) honest-typed orcid.ts comments against §6.1, added a `typeof payload.sub === 'string' && length > 0` runtime guard with regression test in `verifyHiveSignature`, and cleaned slug citations at 3 out-of-scope sites. Round-5 (`659a0f7`) fixed the round-4 typo where slug-cleanup comments cited `/signup-verify` (nonexistent) instead of `/resume-signup` (the actual route in `signup-verify.ts:133`).

Round-5 `/ce-code-review` (correctness, testing, maintainability, project-standards, learnings-researcher; `ce-agent-native-reviewer` skipped per CLAUDE.md): 0 primary findings; one info/50 maintainability residual on residual `/resume-signup` rot risk dismissed below the 75 floor (route renames cascade loudly through callers, tests, and app.ts so incidental surfacing covers it). Cross-refs: ARCHITECTURE.md §6, `task-slug-citations-in-code-comments-go-stale-on-archive-2026-05-15.md`, `ts-closure-denarrowing-nullable-property-hoist-2026-05-04.md`, sibling `backend-custody-upgrade-seed-phrase-reauth`.


## BACKEND-BUILD-CUSTODY-UPGRADE-CHALLENGE-EXPORT (archived 2026-05-17) — clean ✓

Three-line structural change at `backend/src/routes/custody.ts:993`: added `export` to `buildCustodyUpgradeChallenge` plus a 3-line comment marking the export test-support-only (not a stable contract). Unblocks the follow-up frontend byte-equality canary surfaced as adv-9 P2 carry-forward in round-3 of `ui-custody-upgrade-seed-phrase-derive-flow`. Pattern mirrors the `buildCanonicalAuthMessage` precedent (`backend/src/lib/authMessage.ts`) which `frontend/tests/unit/sec-001-equivalence.test.js` imports cross-source via Vitest. The `__test_seams` namespace pattern (`test-seams-export-shape-as-const-2026-05-04.md`) is a different convention — for in-module seams to reach private route helpers — and does not apply here. Implementer commit `ca4f909`.

Architect `/ce-code-review` round-1 (correctness, testing, maintainability, project-standards, learnings-researcher, kieran-typescript; `ce-agent-native-reviewer` skipped per PEvO CLAUDE.md): 0 primary findings. One P2/conf-55 maintainability residual ("export from route file diverges from lib/ precedent") suppressed below the 75 floor; the route-vs-lib judgment was made explicitly in the task brief and the rationale (keep helper co-located with consumers, mirror the sec-001 pattern) is sound. Follow-up UI task `ui-custody-upgrade-challenge-byte-equality-test` will land once this archives.


## UI-ACCREDITATION-VERIFY-RETRIABLE-HANDLING (archived 2026-05-17) — round-3 clean ✓

Three-round task surfaced from cross-reviewer cluster pass on `backend-accreditation-existing-accreditation-gate` (julik-frontend-races + reliability + adversarial). Goal: branch the SPA's `POST /api/accreditation/verify` failure UI on the backend's new `ACCREDITATION_GATE_UNAVAILABLE` 503 / `details.retriable: true` discriminator so a HAF outage doesn't cascade into a 24h email-path lockout. Pre-fix, the user clicking the email link saw a generic "Failed" page with only "Request New" as the forward affordance, burning their 3/24h `/api/accreditation/request` slots when a retry against the same token would have succeeded once HAF recovered.

Round-1 (commit `b66a370`) landed the core flow: distinct `retriable_error` state branch, Retry CTA re-invoking `verifyAccreditation(token)` against the same token (no rotation), `Retry-After` header support via `err.retryAfterSeconds` with countdown UI, non-retriable errors continuing to surface "Request New" CTA, 3 new i18n keys (`serviceTemporarilyUnavailable`, `retry`, `retryAvailableIn`) + 15 locale stubs, 7 specs (retriable vs non-retriable routing, `Retry-After` countdown, token-preservation invariant). Architect held 3 race-related items.

Round-2 (commit `cce7b41`) landed the held items: (1) `_verifyGeneration` counter bumped synchronously at top of `_verify()` and captured into `.then`/`.catch` closures to bail stale flights before state-write, mirroring `agents/docs/solutions/conventions/synchronous-flag-before-await-idempotency-guard-2026-05-16.md`; (2) `if (this.state === 'loading') return;` first-guard in `retryVerification()` to close the double-submit window before cooldown check; (3) 2 new race specs (concurrent flights — late success bails on gen mismatch; rapid double-tap — exactly-once retry under loading-state guard). 13 specs pass. Architect held 1 P3 item: the concurrent-flights spec's preamble comment was inverted ("success state must survive" was backwards — flight B's `retriable_error` is the latest, flight A's stale success must bail).

Round-3 (commit `ae8a137`) pure 3-line prose fix: replaced the inverted clause with "flight B's retriable_error state must not be overwritten by flight A's stale success resolution; the generation guard makes flight A's late `.then` bail." Verbatim the architect's suggested wording. No assertions or test logic touched. Round-3 architect re-review via `/ce-code-review` with 5 personas (correctness, testing, maintainability, project-standards, learnings-researcher; julik-frontend-races + reliability skipped as no production code changed; `ce-agent-native-reviewer` skipped per PEvO CLAUDE.md). All clean. One project-standards subagent finding (PS-001 P1, conf 100) was a false positive: the reviewer flagged "re-review signal in pending/" but didn't see commit `d59271a` which mv'd the file to `review/` (same two-commit Edit + mv split as task `ui-papers-orcid-null-fallback-verification`, ruled compliant in the cluster).

## UI-ORCID-CALLBACK-DESTROY-CLEAR-RETURN-TO (archived 2026-05-17) — first-review clean ✓

Single-round task. Commit `2cb0051` adds `sessionStorage.removeItem('pevo_orcid_return_to')` to `frontend/src/pages/orcid-callback.js::destroy()` so an abandoned mid-flight `/recover` callback cannot leak the return-path pointer into a later non-recover ORCID flow on the same tab. Mirror of `pevo_orcid_mode`'s scrub-on-logout pattern (parity intent stated in commit comment). Test replaces 2 inverse-contract assertions (which pinned the old "key survives teardown" contract) with a positive assertion that primes the key, kicks off `_verify` with a never-resolving `completeOrcid` mock, calls `destroy()`, and asserts `sessionStorage.removeItem` was called and the key is undefined.

`/ce-code-review` with 6 personas (correctness, testing, maintainability, project-standards, learnings-researcher, julik-frontend-races; `ce-agent-native-reviewer` skipped per PEvO CLAUDE.md). Four clean. Maintainability M1 (P2/conf 72, `'pevo_orcid_return_to'` string-literal duplication across 3 sites) dismissed as preemptive refactor per `feedback_dismiss_preemptive_test_hardening`. Julik RR-1 (P3/conf 35, `auth.js::disconnect()` doesn't scrub `pevo_orcid_return_to` — commit's claimed parity with `pevo_orcid_mode`'s scrub-on-logout is one-sided) filed as follow-up `ui-auth-disconnect-clear-orcid-return-to.md` in pending/. Learnings-researcher confirmed the destroy-time scrub is documented as known-good in `agents/docs/solutions/conventions/storage-scope-localstorage-vs-sessionstorage-for-spa-flow-state-2026-05-17.md` (cleanup-parity rule).

## UI-ROUTER-TRIM-REGISTER-NAV-GUARD-DOC-COMMENT (archived 2026-05-17)

# UI-ROUTER-TRIM-REGISTER-NAV-GUARD-DOC-COMMENT — trim verbose docblock on registerNavigationGuard

**Owner:** UI
**Created:** 2026-05-17 (architect, surfaced by `/ce-code-review` on `ui-mid-broadcast-spa-navigation-guard` — maintainability M-2, P2/conf 75)
**Priority:** P3

## Problem

`frontend/src/router.js:123-141` carries an 8-line docblock that mixes obvious-from-code restatement (signature, return-value semantics, the multi-guard AND-rule, "returns an unregister function for convenience") with the actually load-bearing WHY (popstate exclusion + the PEvO-specific `@click="navigate(...)"` rationale that justifies the scope decision). The signal is buried in the noise.

Root CLAUDE.md: *"Default to writing no comments. Don't explain WHAT the code does, since well-named identifiers already do that. Only add one when the WHY is non-obvious."*

## Goal

Trim to the popstate + `@click` scope rationale only. Drop the signature / return-value / multi-guard restatement.

## Acceptance

1. `frontend/src/router.js` — replace the docblock with a 3-4 line block containing only the popstate-out-of-scope explanation + the PEvO-specific `@click="navigate(...)"` rationale.
2. No code changes — purely the comment trim.
3. Tests untouched (no behavior change).

## Outcome

Landed at commit `4283a81`. Reviewed under `/ce-code-review` (correctness + testing + maintainability + project-standards + learnings-researcher) on 2026-05-17 — 100% clean across all 5 reviewers. The trim correctly identifies signal vs noise: the four dropped blocks (signature, return-value, multi-guard AND-rule, "for convenience") were all derivable from the function name and the 3-line implementation body; nothing load-bearing was lost. Popstate exclusion + `@click="navigate(...)"` rationale verified against the actual router code and a cross-repo grep of in-page navigation patterns.

