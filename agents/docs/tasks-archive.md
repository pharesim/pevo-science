## BACKEND-SIGNUP-FINALIZE-TIMEOUT-EXTENDED-LOCK-409 — signup-finalize held binding lock returns ambiguous 504, not terminal 409 (archived 2026-06-15) — clean ✓

**Owner:** backend

On the signup-finalize accreditation path (`broadcastAccreditationAndSeed`, `signup-verify.ts`), a held ORCID binding lock surfaced the terminal `409 ORCID_ALREADY_LINKED` even though a held lock there is typically this account's OWN prior timed-out attempt (the broadcast-timeout branch extends the shared lock to the 120s HAF-indexing-lag ceiling and skipReleases). The signup `auth_token` is not consumed on a held lock (recovery is the username-keyed resume branch), so terminal-409 misled a legitimate same-window retry. Architect wire-shape decision (c): add a `heldShape: 'terminal-409' | 'ambiguous'` discriminator to `withOrcidBindingLock` (default `'terminal-409'`, so `/orcid/callback` callers stay byte-identical -- their OAuth state token IS consumed pre-lock, terminal is correct); the signup caller passes `'ambiguous'`, routing `'held'` through `handleBroadcastErrorAmbiguous` (504 BROADCAST_TIMEOUT, outcome:'uncertain', verify_before_retry:true, no timeout_ms). Lock-nonce/Lua-CAS primitive untouched. Backend landed code (`407aaa3b`); architect landed the contract edits at archive: `auth.md` `/confirm` + `/link` gained the 504 BROADCAST_TIMEOUT entry (three triggers: genuine timeout, Redis-unavailable forced-ambiguous, self-held lock), `orcid.md` gained a shared-lock coherence line (callback held stays terminal 409).

**Architect review (2026-06-15) — ARCHIVED clean.** `/ce-code-review` on `407aaa3b` (9 personas; correctness/security/adversarial on Opus; ce-agent-native skipped per PEvO). Zero code defects: no double-send (held branch returns 'handled', route short-circuits before sendOk), no auth bypass (held branch is downstream of the /confirm posting-key proof / /link fresh-signature gates per §6.4/§6.5), the new real-path test exercises the genuine extend-on-timeout→retry→504 path (the timeout_ms-absent assertion pins the held branch, not a second timeout), and the TOCTOU/duplicate-bind guard is intact (a genuine cross-account take is still caught by the pre-lock durable-binding 409). Advisories dismissed: error-tier log on the benign self-held retry (documented one-line-volume tradeoff + minimal-logging policy), held-branch comment verbosity (the verbose behavioral framing is what the decision prescribed).

## BACKEND-SIGNUP-CONFIRM-ORCID-BINDING-GUARD — ORCID-uniqueness guard on the signup-finalize accreditation broadcast (archived 2026-06-15) — 1 hold round (2 items), re-review clean ✓

**Owner:** backend

`broadcastAccreditationAndSeed` (`signup-verify.ts`, shared by `/api/auth/confirm` + `/api/auth/link`) broadcast an `accredit` op for `account.orcid` with only an account-keyed dedup (`getAccreditedSet([username])`, gated on `isResume`) and never the ORCID-binding check `handleAccredit`/`handleLink` run — so one ORCID could land two on-chain accredit ops under different accounts (the chain-layer one-ORCID-one-account invariant; `accounts_orcid_unique` constrains only the denormalized column, not the chain). Fix (`63ebde27`): extracted the binding-lock + binding-check closure from `orcid.ts` into `lib/orcid-binding.ts` (byte-equivalent move — `findAccreditedAccountWithOrcid`, `withOrcidBindingLock`, cache/lock helpers); added a pre-lock `findAccreditedAccountWithOrcid` check on the finalize path → `409 ORCID_ALREADY_LINKED` on a different-account binding, then broadcast + cache under `withOrcidBindingLock` (closes the TOCTOU). Applies to `/confirm`, `/link`, and the resume path; email-only (orcid null) keeps the prior lock-free behavior. Architect decisions: A1 (409 + finalized-but-unaccredited, account stays recoverable), A2 (no new §6.1 row state — accreditation is an on-chain dimension orthogonal to the accounts-table state machine), B (chain-only check sufficient, the DB index is the row-level backstop, do not widen), lock (both functions factored, check+broadcast under the lock for callback parity). Contract: `api-contracts/auth.md` gained the chain-binding `409 ORCID_ALREADY_LINKED` cause on `/confirm` + `/link` (`aa6de121`).

**Architect review + re-review (2026-06-15) — ARCHIVED.** First pass `/ce-code-review` on `63ebde27` (10 personas) found the production substance sound: byte-equivalent extraction, TOCTOU lock correctly closed, Redis-down fails CLOSED on the HAF check, guarded state transition aligns with §6. Held 2 test-only items: comment-anchor rot (the test cited "decision A1" — ARCHITECTURE.md has no "A1" label, a coordination label that rots on archive) and coverage gaps (resume-flavor conflict + `/link` same-account allowance). Re-review on `e33384af` (test-only; 7 personas — correctness + security + adversarial on Opus; testing/maintainability/project-standards/learnings on Sonnet; ce-agent-native skipped per PEvO) clean: item 1 verified (no A1/coordination citations remain, replacement anchors on ARCHITECTURE.md §6.1, no new rot — diff-gated grep clean); item 2 verified (the stuck-recovery resume test genuinely reaches the guard with `isResume=true` and `createClaimedAccount` not called; the `/link` same-account allowance proves a REAL cache hit via the pre-assertion `get(key)===username`, not an unbound passthrough — `findAccreditedAccountWithOrcid` is cache-first). Substance unchanged (test-only diff). Dismissed: same-account tests rely on the cross-account sibling test for guard-deletion discrimination (adversarial confirmed NO whole-suite escape exists; preemptive hardening, user-triaged dismiss). Spun off: a pre-existing soft anchor-rot comment ("from the task" + repro labels at the `/confirm` 409 describe block, pre-existing from `63ebde27`, not introduced by the hold fix) → `backend-signup-guard-test-comment-anchor-reanchor`; the broadcast-timeout extended-lock terminal-409 interaction → `backend-signup-finalize-timeout-extended-lock-409` (blocked on an architect wire-shape decision).

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## BACKEND-REPUTATION-CALC-VERSION-AUTO-RECOMPUTE — auto-recompute finalized cycles on a scoring/weights change (calc-version stamp beside cycle:last) (archived 2026-06-15) — 1 hold round (3 items), re-review clean ✓

**Owner:** backend

`runBatchComputation` is forward-only (computes only cycles newer than `reputation:cycle:last`), so a corrected scoring calc left finalized cycles frozen until an operator ran `DEL cycle:last` + restart (the frozen-cycle footgun, `reputation-scores-frozen-after-calc-deploy-reset-cycle-last-2026-06-15.md`). Fix (`f30f46a7`): a calc-version fingerprint `computeCalcVersion(weights)` = an explicit `CALC_VERSION` constant (manual lever for code/SQL scoring changes) joined with a key-sorted sha256 of the sanitized weights (so an on-chain `update_weights` auto-triggers a backfill), persisted at `${appTag}:reputation:calc:version` beside the cursor. On mismatch the batch loop forces an IN-MEMORY `effectiveLastCycle=-1` (replay from cycle 0) and persists the new version ONLY after the replay durably reaches the latest fully-elapsed cycle (`reachedFullyElapsed`), so a crash/time-capped partial run keeps retrying rather than re-freezing at a half-applied version. The reset never writes `cycle:last`, so the must-not-advance-on-failure invariant holds. The run's single weights snapshot is threaded into `computeReputationBatch` (new optional `weightsArg`) so the persisted fingerprint always matches the weights actually applied even if the WEIGHTS_TTL refresh swaps the cache mid-run. Three sibling batch test files pin the new shared `calc:version` READ (return the matching fingerprint, delegate other keys to the real client, never write the key) to stay immune to the cross-file singleton.

**Architect review + re-review (2026-06-15) — ARCHIVED.** First pass `/ce-code-review` on `f30f46a7` (8 personas; correctness + adversarial on Opus; reliability/performance/testing/maintainability/project-standards/learnings on Sonnet; security/api-contract/data-migration skipped as no-surface; agent-native per policy) found the shipped code correct and well-tested: batch-loop invariants verified (in-memory reset never advances `cycle:last`; the cursor rewind during replay is safe under the held lock; persist-only-on-`reachedFullyElapsed` gates convergence), conforms to all three cited convention docs, full AC coverage. Held 3 cheap risk-tied items (`30c5118f`): a CALC_VERSION bump-reminder comment at the scoring SQL (silent-stale-scores footgun), a locale-independent codepoint comparator in `computeCalcVersion` (fingerprint determinism), and a weightsArg-threading test assertion. Re-review on `30c5118f` (4 always-on personas) clean: correctness empirically verified the comparator swap is byte-identical for the real 11-key set (0 of 110 pairwise disagreements) and deterministic; bump-reminder accurate; the assertion catches the thread-drop regression. Dismissed: the replay-from-0 non-resumable convergence stall (real but far from single-instance scale; the eager-atomic-reset fix is named inline; user-triaged no follow-up task) and the test polish (time-cap test, as-never casts, spy-block DRY, options-bag); one P3 test-comment-precision nuance non-blocking. The cross-file singleton-Redis-key test-isolation pattern is compounded separately.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## BACKEND-REVOTE-DISPLAY-COUNT-PARITY — display vote counts fold post-payout revote custom_json (review/comment/profile/paper-detail) (archived 2026-06-15) — 1 hold round, re-review clean ✓

**Owner:** backend

Four display-side accredited-vote-count surfaces (single-review `net_votes`, comment-tree `accredited_votes`, profile votes-sort `net_votes`, paper-detail embedded `reviews[].net_votes`) counted native Hive votes only and ignored post-payout `revote` `custom_json`, so a count diverged from the reputation the same data produced whenever an accredited voter flipped or retracted via revote. Fix (`96fa47e1`): `accreditedVoteCount` (`hafsql.ts`) gains an optional `appTagParam`; supplied, it UNION-ALLs the accredited `revote` arm (`action='revote'`, `{1,9}`-digit-guarded weight cast) and resolves latest-signal-per-voter across both arms by `block_num DESC, op_id DESC` (native `v.id` and revote `cj.id` share one monotonic HAF op-id sequence, NOT namespaced per arm), matching the `*_vote_signals` CTEs in `reputation.ts`. The native-only form (no `appTagParam`) is unchanged and retained for the papers-list placeholder that `batchResolveVotes` overwrites. Threaded through `routes/reviews.ts`, `routes/comments.ts`, `routes/profile.ts` (collapsing the inline twin onto the helper), and `routes/papers.ts` `fetchEnrichmentFromHaf`. Per-row correlated revote scan documented inline (O(namespace x rows), index-backed only on `custom_id`); follow-up `backend-revote-count-batched-scan-on-namespace-growth` filed for the batched collapse if the custom_json namespace grows. Architect contract-doc updates landed (`7b6516a0`, emdash-free): `api-contracts/reviews.md` net_votes note, `common.md` Accredited-Only Data Policy sentence, `papers.md` comments net_votes + new paper-detail embedded `reviews[].net_votes` note.

**Architect review + re-review (2026-06-15) — ARCHIVED.** First pass `/ce-code-review` on `96fa47e1` (10 personas; correctness + security + adversarial on Opus; testing/maintainability/project-standards/performance/api-contract/reliability/learnings on Sonnet; agent-native skipped per policy) found the SQL correct and complete: adversarial live-probed HAF (confirmed `operation_vote_view.id` and `operation_custom_json_view.id` are disjoint, so cross-arm latest-wins is deterministic), security clean (the `{1,9}` guard closes the int4-overflow/abort vector and no user value reaches SQL text), reputation.ts parity verified at the fragment level, callsite completeness re-enumerated from code (no 5th surface missed). Held 2 backend items (`8a817e9e`): a stale param-shape comment in `fetchUserReviewsFromHaf` listing the removed `accreditedAccounts` bind, and a redirect-integrity guard in `runVoteCountWithRevotes` so a future `accreditedVoteCount` table-alias rename fails loudly instead of false-greening the synthetic sets into the helper's `WHERE false` fallback. Re-review on `8a817e9e` (4 always-on personas) clean: comment accurate, guard logic sound. Dismissed: the optional-`appTagParam` "silent native-only" misuse note (deliberate, documented) and the pre-existing `cj.json::jsonb` cast-throw (shared verbatim with deployed reputation.ts; Hive validates custom_json). `/ce-compound` skipped: conforms to the existing op-id, cross-surface-parity, and 2x2-vote-resolution conventions. (Testing reviewer noted sibling synthetic-CTE helpers `runVoteCount`/`runAccredCte` lack the same redirect guard, pre-existing and out of scope.)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## BACKEND-EDITABLE-ACCREDITATION-METADATA — self-service metadata edit + first-accreditation tenure anchor (archived 2026-06-15) — 4 hold rounds, re-review clean ✓

**Owner:** backend

Accreditation metadata (`name`/`institution`/`field`) made editable post-accreditation, plus a first-accreditation tenure anchor so "accredited since" derives from the EARLIEST accredit op (all-history, across sanction gaps) rather than the latest. New `PATCH /api/accreditation/metadata` (`routes/accreditation-metadata.ts`, split out of `accreditation.ts`): re-broadcasts a MERGED admin-signed `accredit` op preserving `method`/`orcid`/`evidence_hash`/`issued_by` from the prior op (a WoT `issued_by:"wot"` op stays "wot", not flipped to the admin account); only supplied metadata fields change. Critical-action fresh-auth gate (target `edit_accreditation_metadata` bound to `(action, username, '')`, mintable via `POST /api/custody/fresh-auth` and `POST /api/orcid/start mode=fresh_auth`, modeled on `ipfs_upload`); JWT-alone rejected per §6.5 invariant #1. Eligibility checked in fixed order BEFORE the proof is consumed: op-read (also the upstream HAF-reachability gate, throw→503 / null→403) → currently-accredited (`getAccreditedSet`) → non-cached fail-closed `hasUnliftedSanction` (→403 `ACCREDITATION_SANCTIONED`, closes the `accredited_accounts_all` 10-min cache staleness window so a freshly-sanctioned account cannot self-lift). `/verify` stays idempotent (no metadata update); the old "metadata one-shot" docblock removed. Tenure anchor: `firstAccreditedAnchorCteBody` (`hafsql.ts`) MIN(block_num)→`haf_blocks.timestamp`, additive + BitmapAnd-safe; `accredited_since` exposed additively on accreditations STATUS/LIST (LIST still sorts by latest-op `timestamp`) and profile; frontend pre-wired `getAccreditedSince(acc) = acc.accredited_since || acc.timestamp`. `accounts` DB columns synced best-effort (chain SSoT). Hold rounds closed: P1 sanction-self-lift via the membership-cache window (round 1); proof-burn-before-op-read ordering, HAF-down vs not-accredited 503/403 distinction, `issued_by` preservation, JWT+proof and op-read-failure tests (rounds 1-2); the HAF-reachable-inference comment corrected across BOTH the op-read and the sibling membership-check sites (rounds 2-3, the `convention-enforcing-fix-must-audit-its-own-new-code` pattern). Reusable-`requireFreshAuth`/file-split cleanup tracked under a separate task. Architect doc follow-ups landed at archive: `api-contracts/accreditation.md` PATCH /metadata section, additive `accredited_since` on the accreditation + profile contracts, ARCHITECTURE.md §6.4 trim-wording fix (the reused schema does not trim).

## BACKEND-ADMIN-ROSTER-AND-AUTHORITY-ATTRIBUTION — chain-derived admin roster, tier gating, issued_by attribution (archived 2026-06-15) — 2 hold rounds (6 → 3 items), re-review clean ✓

**Owner:** backend

A chain-derived admin roster (the human-authorization layer in front of the single on-chain `pevo.admin` signer — the signer and `config.accreditationAuthorities` stay singular/un-widened), tier gating (`admin` < `super_admin` < `root`), and `issued_by` attribution on every authority op payload. Roster is read LIVE from `admin_grant`/`admin_revoke` ops (`activeAdminsCteBody`, singular `?` signer gate, latest-op-per-account wins) behind a Redis TTL cache — no persistent table. New `/api/admin/*` endpoints (`routes/admin.ts`), each `verifyHiveSignature -> requireAdminLevel(tier) -> validate -> requireFreshAdminAuth`: roster GET, roster grant/revoke (tier matrix + lockout guards: root un-demotable, no self-demote, super_admin-only-by-root), accreditation grant, paper retract, authorship revoke (pevo.admin-signed) / approve (bridge-key, bridged papers only). Fresh-auth machinery in `lib/fresh-auth.ts` (six `admin_*` target actions, single-use GETDEL proofs target-bound to `(action, username)`); JWT-alone rejected on every mutation (§6.5 invariant #1). `issued_by` server-set from `req.hiveUsername` (no spoofing); WoT auto-grants stamp the `"wot"` system marker. Self-service accredit paths (email/ORCID/signup) left ungated and stamp `issued_by = hiveAdminAccount` (scientist-triggered, no admin in the loop). Architect doc follow-ups (ARCHITECTURE.md §6.4 rows, `api-contracts/*` for `/api/admin/*`) tracked separately.

Two hold rounds. Round 1 (`f51e9820`+`8d3b0fd7`+`a026f63f`+`cfa2bfbe`+`a0df4799`, 10 personas) held 6: grant-handler peer-super_admin demote bypass (P1), dead `level` on the revoke schema (TOCTOU attractor), missing timeout cache-bust on retract/authorship, hand-copied valid-action strings (×3), WoT `issued_by:'wot'` assertion gap, and the missing root-demotes-super_admin positive test. Round 2 (`fad7ca56`) verified 5/6 and re-held 3 test-only items: the claimed round-1 root-demotes-super_admin test was misplaced in the grant describe block (the /roster/revoke positive branch stayed uncovered), the timeout cache-bust was untested, and the Zod-strip guarantee was unpinned.

**Architect re-review (2026-06-15) — ARCHIVED.** `/ce-code-review` on `dc9b8799` (round-2 fix, test-only: testing + correctness + adversarial + project-standards + maintainability; ce-agent-native skipped per policy). All 3 round-2 items verified FIXED: the root-demotes-super_admin positive is correctly in the `/roster/revoke` describe block (omits `level`, asserts 200 + one broadcast + the `admin_revoke` payload with roster-derived level + `issued_by: ROOT`); the three timeout specs inject a real `BroadcastTimeoutError` and assert the 504 envelope + the correct per-handler `hafCache.invalidate` key (`retracted-papers`, `claims:paperauthor:some-paper`, `claims:<bridge>:bridged-paper`); the Zod-strip spec posts the removed `level` key and asserts 200. Correctness confirmed the specs faithfully model production (bridge-key gate + `broadcastJsonWithTimeout` path for approve, real `BroadcastTimeoutError` for `instanceof` discrimination); adversarial found no false-assurance (carve-out documented, real-path companion present, config mutation restored in `finally`, `mockRejectedValueOnce` no bleed). Residual maintainability nits DISMISSED (user-triaged 2026-06-15): timeout-spec scaffolding extraction, describe-scoped config frame, and a cast comment are advisory test-style suggestions on a passing suite.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## BACKEND-REVOKE-SANCTION-WOT-MEMBERSHIP — revoke reclassified as authority sanction; live-threshold WoT membership; ever-sanctioned guard (archived 2026-06-15) — 3 hold rounds (8 → 3 → 3 items), re-review clean ✓

**Owner:** backend

`revoke` now means **sanction only** (`type:"sanction"` discriminator + `issued_by` on `RevokeAction`); WoT membership is **live-threshold-gated** and self-heals on a vouch drop with NO `revoke` broadcast (all cascade machinery — `cascadeRevocation`, `revokeVoucheeIfBelowThreshold`, `buildRevocationPayload`, `PartialCascadeError`, `CASCADE_BUDGET_MS` — removed from `wot.ts`); legacy revokes (reason "WoT threshold no longer met") are reclassified as non-sanctions (membership reverts to live threshold). The `activeAccreditationsCteBody` rewrite embeds a private vouch graph + on-chain `update_params` threshold behind the same `AS MATERIALIZED` planner fence as `loadWotThreshold` (validated ~13-20ms on live HAF, no BitmapAnd). New `hasUnliftedSanction` (non-cached, fail-closed) guards the ever-sanctioned WoT auto-accredit path AND all three self-service accredit paths (email `/verify`, ORCID callback, signup-verify) so a sanctioned account cannot self-lift by re-verifying (architect decision: only a deliberate admin `accredit` lifts). New `POST /api/admin/accreditation/sanction` (admin tier + `requireFreshAdminAuth('admin_sanction')`, self/root/tier-≥-actor guards). Reputation is cleared on sanction via `invalidateOnRevocation`; the per-cycle `pruneDeAccreditedMembers` backstop SREM+DELs members absent from the live set (op-less WoT drops).

Three hold rounds. Round 1 (`11039c47`, 10 personas) held 8: self-service self-lift (P1), orphaned `invalidateOnRevocation`/stale reputation (P2), sanction tier guard, cache-staleness comment, `aa_vouch_ranked` MATERIALIZED fence, sanction re-auth, `'sanctioned'` reason collapse, doc/tiebreaker cleanups. Round 2 (`81be5c49`) verified 7/8 and re-held item 2: the prune compared mismatched Redis key-spaces (full-key index members set-diffed against bare usernames → churned the whole index every cycle; `del(batchKey(m))` double-prefixed into a no-op → de-accredited score keys leaked) plus its coverage gap. Round 3 (`358f70c2`) held the now-live prune DEL's `exec()` per-command error handling (SREM-ok/DEL-fail silently orphaned a score key still served by `getReputationScore`).

**Architect re-review (2026-06-15) — ARCHIVED.** `/ce-code-review` on `358f70c2` (round-3 fix: 7 personas — correctness + adversarial on Opus; reliability/testing/maintainability/project-standards/learnings on Sonnet; ce-agent-native skipped per policy). All 3 round-3 items verified FIXED: `pruneDeAccreditedMembers` now inspects `prunePipe.exec()` per-command `[err,res]` tuples and warns on partial apply (correctness confirmed the null-guard + `pruned`-field warn discriminator; reliability confirmed warn-only is the correct posture — DEL-fail near-impossible on real Redis, sanction path invalidates independently); the fresh-seed-window docstring + eslint `__test_seams` catalog landed; the fake-client partial-failure test genuinely fails pre-fix. Clean across all lenses. Residual P2/P3 nits DISMISSED (user-triaged 2026-06-15): warn-only-no-repair re-opens the deliberate observability-as-floor decision; the `results?.find` null-guard asymmetry with the staging pipeline is functionally moot (null unreachable for a non-atomic `redis.pipeline()`); the `__test_seams` in-file docblock enumeration, the test message-vs-`pruned` discriminator, and the fresh-seed-recovery docstring phrasing are cosmetic. The key-space-mismatch failure shape (round-2) is compounded as `sidecar-index-member-ops-single-key-space-2026-06-15.md` (third shape in the sidecar-index family); the round-3 per-command-`exec()` inspection mirrors the already-documented `reputation-cycle-last-must-not-advance-on-failure` convention, so no new learning was filed for it.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## Accreditation Metadata-Edit Cleanup (perf + maintainability) (archived 2026-06-15) — /ce-code-review clean (10 personas); behavior-preserving refactor ✓

**Owner:** backend
**Created:** 2026-06-15

Non-blocking cleanups split out of the editable-accreditation-metadata review (2026-06-15) so they stay off that task's critical fix pass. None change behavior. Three items, all landed in commit `ad831073`:

- **Anchor CTE per-account predicate.** `firstAccreditedAnchorCteBody` (`hafsql.ts`) gains an optional `account?` param: a `cj.json::jsonb ->> 'account' = $N` equality narrows the `MIN(block_num)` GROUP BY to one account for the single-account STATUS (`fetchAccreditationStatusFromHaf`, `routes/accreditations.ts`) and profile (`getAccreditationFromHaf`, `routes/profile.ts`) reads via a `(idx) => firstAccreditedAnchorCteBody(idx, username)` closure; LIST stays on the all-accounts form. Pure equality on the same `custom_id = $appTag` index scan, NO `block_num >=` floor, so BitmapAnd-avoidance holds.
- **Reusable fresh-auth proof gate.** `consumeFreshAuthProof(req, targetFn)` + `requireFreshAuth(targetFn, message)` extracted into `lib/fresh-auth.ts`; the reason->status (403/401) mapping now lives once in `freshAuthFailureStatus`. ipfs `/upload-token` uses the middleware; PATCH `/metadata` calls `consumeFreshAuthProof` INLINE so its eligibility checks (op-read / accredited / not-sanctioned) run BEFORE the single-use proof is consumed (preserving the round-1 P2 fix). The 3rd copy (`requireFreshAdminAuth`, `admin-roster.ts`) was left for the admin-roster task.
- **Split `routes/accreditation.ts`.** The PATCH `/metadata` handler + its per-account `accred-edit` limiter moved to a new `routes/accreditation-metadata.ts` (own Router), mounted via `router.use(metadataRouter)` at the same `/api/accreditation` prefix; unused imports trimmed from `accreditation.ts`.

**Architect review (2026-06-15) — ARCHIVED.** `/ce-code-review` on `ad831073` (10 personas: correctness + security + adversarial on Opus; reliability / api-contract / performance / testing / maintainability / project-standards / learnings on Sonnet; agent-native skipped per policy). Clean across correctness (move is byte-identical, typecheck 0 errors, CTE param arithmetic $1-$6 consistent), security (proof-gate equivalence for both consumers, §6.5 no-JWT-only preserved, eligibility-before-proof ordering + sanction guard intact), api-contract (PATCH `/api/accreditation/metadata` path + response shapes/codes unchanged, no prefix doubling), performance (per-account filter is a pure equality predicate, no `block_num` floor, no planner flip), reliability, project-standards (no emdash, appTag-prefixed limiter key, account-state defense consistent). Four minor nits DISMISSED: (1) `fresh-auth.ts` docblock still says "primitive" after gaining the `requireFreshAuth` middleware (cosmetic); (2) the reason->status ternary's 3rd copy in `admin-roster.ts` left deliberately (out of scope; owned by `backend-admin-roster-and-authority-attribution`; the shared `freshAuthFailureStatus` now exists for it to adopt); (3) the single-account anchor-CTE param lacks a dedicated real-Postgres test (arithmetic verified correct; a param-count mismatch is a hard PG error at runtime); (4) a double blank line. None block archive. `/ce-compound` skipped: clean refactor, no novel learning.

## Bootstrap a repo-wide CONCEPTS.md (shared domain vocabulary) (archived 2026-06-15) — /ce-code-review clean after in-place fixes ✓

Seeded `CONCEPTS.md` at repo root with PEvO's core domain vocabulary (82 entries across 7 clusters: Platform & Identity, Content & Evaluation, Authorship, Accreditation & Trust, Reputation, Accounts & Authentication, Admin Authority, plus a Flagged-ambiguities tail), derived from the declared domain model (ARCHITECTURE.md, hive-schemas.md, reputation-algorithm.md, root CLAUDE.md) via the ce-compound-refresh repo-wide bootstrap path. Entries are definition-only with optional lifecycle paragraphs; no file/class/table names, no config thresholds, no metadata, no emdashes. Ownership + discoverability wired: CONCEPTS.md declared architect-owned (commit-msg `allowed_for_agent()` architect zone + architect "Files You Own" + the zone-audit convention-doc narrative summary; +2 hook tests T11b/T11c, suite 47/47) and surfaced under a new root-CLAUDE.md "Shared Domain Vocabulary" section mirroring agents/docs/solutions/. Architect /ce-code-review (correctness on Opus + project-standards + maintainability + testing + learnings; agent-native skipped per policy) confirmed the glossary substantively accurate against the source docs and convention-clean (emdash-free, anchor-rot-free, discoverable); eight findings were fixed in place: convention-doc zone-summary sync (the one missed bootstrap step), duplicate `revoke op` Avoid-alias dedup, platform-authority-as-backstop added to Approve Authorship, plus Rating / Operation-Attribution / Hive-native / Bridge-Paper / Vouch wording cleanups. /ce-compound skipped: the missed step was already documented in the zone-audit convention's Maintenance section, so no new learning.

## UI-ADMIN-CONSOLE — gated admin console (roster management + tier-gated authority actions) (archived 2026-06-15) — 2 hold rounds (10 items), re-review clean ✓

A tier-gated (`admin` < `super_admin` < `root`) console at `/admin` (`frontend/src/pages/admin.js` + `api.js` admin clients): chain-derived roster list with lockout-safe promote/demote (root never demotable, no self-demote, no self-promote, manageable-levels gate) and authority-action forms (grant accreditation, retract paper, revoke/approve authorship) routed through a shared confirm panel that surfaces the server-set `issued_by` attribution before commit. Every mutation is gated by per-action § 6.4 fresh re-auth via the existing `withSettingsFreshAuth` orchestrator: self-custody admins dispatch through `signRequest` (X-Hive-Signature, so the backend sees `hiveAuthMethod==='signature'` and `requireFreshAdminAuth` is satisfied), light admins use JWT + a single-use body `fresh_auth_proof`, and `fetchAdminRoster` stays JWT (read, not critical). The Sanction action is stubbed pending the revoke-sanction op contract. Built ahead of the backend; integration-verify was intentionally NOT performed and no live-verify task was filed — per the user, PEvO does not live-test chain-interaction functions, so a broken broadcast path surfaces as a normal bug report rather than a gated verification step.

Initial build-ahead review (`ea48d8e0`, 10 personas; `ce-agent-native` skipped per PEvO) held 9 items (round 1): `method:'admin'` enum-break → `'manual'`; blanket-toast → structured backend-rejection surfacing in `runConfirmed`/`loadRoster`; confirm-staging gate (`_stageConfirm` early-return on `submitting||pendingConfirm` + `:disabled` on all surfaces); post-success reload-failure routed to `loadError`; self-promote lockout + overwrite warning; dead `root` guard dropped from `canDemoteRow`; and (addendum) self-custody fresh-auth via `signRequest`. Re-review #2 (`81cf39ac`, 10 personas) verified 1-9 genuinely fixed and held one new item (round 2): `_errorMessage` leaked `api.js`'s synthetic `INTERNAL_ERROR` "Request failed with status N" transport string instead of the localized fallback (reachable while a stale image 404s the roster route). Re-review #3 (`b960bfa8`, 5 personas) confirmed item 10 fixed (`err.code !== 'INTERNAL_ERROR'` exclusion, mirrors `bridge.js handleLookup`; genuine codes still surface) plus a case-insensitive self-promote guard, all pinned by 3 regression tests, zero findings. The surfaced code-vs-convention tension (`_errorMessage`'s genuine-backend `err.message` passthrough vs `frontend-error-sanitization`) was settled by a narrow documented exemption in that convention: operator-only surface + the-message-is-the-feature + a load-bearing backend obligation that `/api/admin/*` messages stay static developer copy; synthetic `INTERNAL_ERROR` excluded; all sinks `x-text`-escaped. Security cleared the fresh-auth gate throughout (a stolen JWT alone cannot broadcast an authority op). Related: the `admin-account-locked-by-hive-create-claimed-account-semantics` convention was re-scoped this cycle — the structural `create_claimed_account` lock still dismisses a JWT-path finding on `config.hiveAdminAccount`-equality routes, but roster-gated routes are defended by `requireFreshAdminAuth` instead.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## BACKEND-WOT-THRESHOLD-QUERY-STATEMENT-TIMEOUT — loadWotThreshold 18s timeout silently degraded the live WoT threshold to the default (archived 2026-06-14) — 1 hold round (3 fixes), re-review clean ✓

A production-journal `DatabaseError: canceling statement due to statement timeout` fired every 30-min `loadWotThreshold()` refresh in `backend/src/wot.ts`. Investigation against live HAF: the `custom_id = $appTag` predicate already hits a good index (~12ms), but `ORDER BY block_num DESC LIMIT 1` over the sparse `update_params` match forced the planner to walk the 107M-row blocks index backward in a nested loop (block_num is a function over the op id, not a stored column) — an ~18s scan tripping the 5s cap and falling back to `DEFAULT_WOT_THRESHOLD` (3). Behavioral impact today is NONE-by-coincidence (no `update_params` override exists for `pevotest`, so the default IS the correct value), but it is a latent correctness bug: the first on-chain threshold override would be silently ignored. Fix (`ca18ee81`): wrap the row match in a `WITH candidates AS MATERIALIZED (...)` CTE so the planner resolves the small custom_id-indexed set first then sorts the tiny result — ~15ms, blocks-index scan `(never executed)`, match-independent. Semantics unchanged (precise `action='update_params'` check, `required_posting_auths ?|` signer gate preserved inside the fence, positive-integer validation); no `block_num >=` floor (would flip to BitmapAnd per the no-custom-id-block-num-floor rule); no new logging; a SQL-shape canary pins the fence. Compounded as `haf-custom-json-latest-op-materialized-fence-2026-06-14.md`.

Architect hold (`/ce-code-review` on `ca18ee81`, 9 personas) held 3 items, all landed in `31480f3f`: (1) the latest-op-wins read lacked the Rule 2 `(block_num, id)` same-block tiebreaker — the CTE now projects `id` and sorts `ORDER BY block_num DESC, id DESC` (operation_custom_json_view omits `trx_in_block`; the `id` projection does not weaken the fence since the CTE has no inner ORDER BY/LIMIT); (2) the planner-fence canary extended to also assert `ORDER BY block_num DESC, id DESC` so a dropped tiebreaker fails red; (3) cosmetic `pevo/` prefix on the lint-rule comment. Re-review of `31480f3f` (correctness on the session model, performance + testing on Sonnet; ce-agent-native skipped per PEvO — 0 findings) confirmed `id` is a real column on the view (sibling accreditation/papers reads use it against live HAF), the fence and signer gate are intact, and the canary robustly pins the tiebreaker. Out-of-scope follow-up left to the architect's call: other `ORDER BY block_num DESC` reads (`lib/idempotency.ts`, `routes/profile.ts`, `reputation.ts`) are account-filtered / window-function shapes not exposed to the empty-match worst case, so no broader sweep was filed.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## UI-EDITABLE-ACCREDITATION-METADATA — Settings editor for accreditation metadata + earliest-op tenure anchor (archived 2026-06-14) — 1 hold round (7 fixes), re-review clean ✓

Accredited users can edit name/institution/field from Settings; the broadcast is admin-signed (user only re-auths per § 6.4, never signs the op), and the displayed "accredited since" reads the earliest-accredit anchor so a metadata re-broadcast does not reset tenure. Built against the ARCHITECTURE.md § 2 contract while the backend half (`backend-editable-accreditation-metadata`, the admin-signed `PATCH /api/accreditation/metadata` endpoint + the `accredited_since` anchor field) remained unlanded — integration verification deferred until it lands. Initial review (`65a9f938`, 10 personas; ce-agent-native skipped per PEvO) verified the fresh-auth wiring (byte-for-byte the `handleEmailSubmit` orchestrator, distinct action `edit_accreditation_metadata`, proof threaded, no JWT-only bypass), validation (trim-before-empty, `<=` boundary, UTF-16 consistent with the Zod bounds), the synchronous double-submit guard, x-text no-XSS, and i18n (12 keys × 16 locales, no emdashes). Finding #1 was an UNRATIFIED cross-agent contract (the UI hard-coded `accredited_since`/endpoint/action/body that the backend task left as "e.g." and ARCHITECTURE.md did not pin); the architect RATIFIED it (`737a4d59`): pinned `accredited_since` (§ 2), added the self-service `PATCH /api/accreditation/metadata` § 6.4 row (owner-authorized, admin-key-signed, NOT roster-gated), and corrected a contradiction where § "Authorization enforcement" wrongly listed the endpoint among roster-gated admin actions. Independent re-enumeration also caught a third tenure surface the commit's "no other surface" audit missed (`researchers.js` directory card rendering latest-op `timestamp`). Held 7 fixes.

Re-review of the fix commit (`8294bea4`, 6 personas) verified all 7 landed and correct, no regressions: (1) researchers.js repointed; (2) shared `lib/accreditation-tenure.js` `getAccreditedSince(acc)` (= `accredited_since || timestamp || null`) now the SOLE tenure read across all three surfaces (independent re-grep confirmed no fourth `.timestamp` read remains) with factory exposure; (3) the poll-clobber race fixed via `auth.applyAccreditationMetadata` (merge-BEFORE-bump of `_pollingGeneration` so an in-flight `_checkAccreditation` drops at its generation guard; no re-fetch since HAF lags) — julik + correctness confirmed the merge/bump ordering and that no new same-tab race is introduced; (4) success toast gated on the apply returning true; (5) tests (new `lib-accreditation-tenure.test.js` + `auth.test.js` + tenure-revert guards on all 3 page tests + settings boundary/outcome/null-no-toast cases); (6) `:maxlength` bound to a `metadataMax` getter; (7) api.js comment de-rotted (no new rot introduced — self-audit clean). Dismissed P2/P3 nits: a disputed "first pass missed" test-comment phrasing (project-standards ruled below the anchor-rot threshold, gate-clean), the `||`-vs-`??` empty-string anchor fallthrough (chain block times are never empty), and an orphaned no-op poll interval (polling has already self-stopped for an accredited user). DOCUMENTED RESIDUALS carried forward (not blocking; held work is correct): (a) a narrow cross-tab edge — a storage-event login in a second tab during the HAF-lag window can re-poll stale data and revert the optimistic edit (display-only, self-healing on next HAF catch-up; the per-instance generation guard structurally cannot coordinate cross-instance; arguably a property of any optimistic-write-during-HAF-lag, beyond the held scope); (b) the `auth.test.js` generation-bump test asserts the counter increments but not that a stale in-flight `_checkAccreditation` actually drops (the drop logic itself is covered by existing cross-user re-login tests). Integration verification stays deferred until the backend half lands.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## ARCHITECT-COMMENT-ANCHOR-CI-GATE — mechanize the comment-anchor convention so sweeps stop missing files (archived 2026-06-14) — review clean after 2 self-review fixes ✓

A repo-local `.githooks/pre-commit` DIFF gate now fails a commit when a newly-ADDED staged line under `frontend/{src,tests}` or `backend/{src,tests}` (source extensions only) introduces comment-anchor rot — task-slug citations, round/hold ordinals, `Option X.N` labels, source line-number / `Lnn` cites, or `tasks-archive`/`see task`/`AC #N` redirects. Three decisions resolved the "why wasn't this already automated" gap: (1) detection is PREFIX-scoped to the known task-slug families (`backend|ui|architect` 2+ kebab segments; `BE-`/`JFR-`/`SEC-<n>-<alpha>` shout forms), sidestepping the legit-hyphenated-token allowlist minefield (`SHA-256`, `AES-GCM`, the base58 `HJ-NP` class never trip it) — verified zero false positives over the tree (601 detections, all real rot); (2) DIFF gate, not whole-tree-clean, since `backend/tests`+`frontend/**` carry ~600 accepted pre-existing anchors (whole-tree deferred until those trees are swept); (3) complements, not duplicates, the standing `backend/tests/eslint/no-stale-comment-anchors.test.ts` vitest canary (wider dirs + slug families, new-rot-only), intentionally overlapping on `backend/src`. Ships with `.githooks/tests/test-pre-commit.sh` (26 cases, all green), a root `CLAUDE.md` "Comment anchors" → "Mechanical gate" pointer, and the `/ce-compound` rationale doc `comment-anchor-rot-precommit-diff-gate-2026-06-14.md`. Landed `bfe288e5` (gate+test+CLAUDE.md) and `806878eb` (solution doc).

Architect `/ce-code-review` (6 personas; `ce-agent-native-reviewer` skipped per CLAUDE.md): no blockers. Two fixes applied in `95dba976`: (#1) the `done < <(staged_added)` process substitution discarded the scanner's exit status, so a git-diff failure would fail OPEN — now captured to a temp file with an explicit fail-CLOSED `exit 1` (verified: hook run outside a repo exits 1); (#2) `.vue` was in the awk scan-list but absent from the line-cite arm's extension list (PEvO is Alpine, no .vue files), dropped so the two lists match. Remaining findings were P3 advisories DISMISSED: a rare hyphenated `command-line 2` line-cite false positive (zero current occurrences, `anchor-allow` escape hatch exists), bash-3.2 / BSD-grep single-platform portability notes (Linux/WSL2 env, opt-in hook), test-harness `set -e` and coverage-gap nits (the 26 cases cover every arm), a debatable canary-update doc step (in tension with the intentional complement-not-duplicate decision), and the `project_<slug>` memory-filename form (not a source-comment rot class). Harness 26/26 post-fix.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## UI-ORCID-SIGNUP-RECOVER-REAL-ROUNDTRIP — drive the ORCID signup/recover E2E specs through a real backend round-trip (archived 2026-06-14) — prior hold (2 P3 doc items) resolved, re-review clean; 2 P3 cosmetic nits dismissed ✓

`frontend/tests/e2e/orcid-no-password.spec.js` gained two real-backend ORCID null-password round-trips (signup + recover) that drive every HTTP hop for real — `/api/orcid/start` → `/orcid/callback` → `/api/auth/signup`|`/recover` → `/api/auth/login` — and assert the real DB end-state (`password_hash IS NULL`, 403 `NO_PASSWORD_SET`, ORCID login reaching `/papers`), replacing prior simulated-only coverage; `fixtures/orcid.js` was extracted behavior-preserving for the sibling `settings-orcid-factor.spec.js`. The prior architect review confirmed the round-trips genuinely real (only network-topology bridges + two non-HTTP shims substituted) with teeth (a password-persistence regression fails the real-DB `toBeNull()` checks), and held 2 P3 doc-only items, both landed in `c24e0f54`: (1) the username-finalization test-DB shim comment now names all three activation columns it sets (`username`, `custody='light'`, `verify_token=NULL`) and cites the mirrored real write in `signup-verify.ts`; (2) a clause-(a) "Mocking justification" header block now names both shims (the read-only `/api/accreditations/<username>` `page.route` stub + the direct `UPDATE accounts` test-DB seed), what each replaces, why per-test-impractical, and asserts every HTTP hop stays real.

Final re-review scoped to the post-hold diff (`43cc8bdc..HEAD`, doc-only: the `bbe6607c` cross-cutting anchor sweep removing `SEC-004`/`SEC-002` refs + `c24e0f54`'s two fixes) across 3 personas (project-standards, maintainability, testing): both held items verified satisfied, the `UPDATE ... SET` three-column comment verified accurate against the actual SQL, and no test logic changed. Two new P3 cosmetic findings DISMISSED: (a) the new header retains a "the SEC-004 recover test" within-file pointer inconsistent with the re-anchored call-site phrasing — gate-spared (bare `SEC-<n>` is a deferred anchor class), a within-file pointer resolving to in-file `SEC-004 Tester` fixtures, and the SAME reference the prior hold already reviewed-and-dismissed (project-standards confirms it is not rot); (b) the Mocking-justification block does not enumerate `routeOrcidStubBridge`'s `/api/orcid/start` passthrough + `/oauth/authorize` compose-network shim — accurate-but-incomplete (neither substitutes a real PEvO auth hop), advisory. `playwright test --list` → 7 tests; the comment/header-only change preserves the prior 7/7 real-stack pass.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## deploy.sh restart: collapse the multi-minute 502 to a few-second swap (archived 2026-06-14) — review clean ✓

Tier 1 (deploy.sh-only, user-scoped 2026-06-14) near-zero-downtime restart. Root cause: `cmd_restart` ran `$COMPOSE down` first, so host nginx got ECONNREFUSED -> 502 for the whole down -> build -> migrate -> boot sequence (minutes, dominated by the image build). Fix (landed `f0228525`): build the new backend image FIRST while the old backend keeps serving `127.0.0.1:3001`; ensure infra up idempotently (postgres/redis/ipfs never torn down); migration-safety pre-flight greps the UNAPPLIED set (disk files not in `schema_migrations`, not all `*.sql` — which would always match the applied 004 DROP TABLE) for destructive DDL, clean -> migrate live (safe via `verifyAppDbMigrations`' fail-closed-only-when-behind / tolerant-when-ahead asymmetry), destructive -> brief-stop carve-out; swap only the backend last via `up -d --no-deps backend` (no `--remove-orphans`, so a live `test-up` session's E2E sidecars survive) with a HOST-side `/api/health` probe. `docker-compose.yml`: `stop_grace_period: 35s` (> the 30s graceful drain). The singleton-job double-broadcast hazard is genuinely avoided: `shutdown()` calls `stopAccountClaimer()`/`stopBridgeWorker()` at SIGTERM receipt, so jobs stop before the new backend reaches `listen()` (verified — the 35s window does not widen any overlap).

Architect `/ce-code-review` (7 personas; `ce-agent-native` skipped per PEvO; correctness/security/adversarial on the session model). Three failure-edge fixes applied + verified, then archived (`06692ab5`): (1) `swap_backend` reported a failed handover as success (warn + fall-off returned 0) — now `return 1` and `cmd_restart` exits non-zero on a dead-backend swap; (2) the destructive carve-out could leave the host port with no listener if `cmd_migrate` failed after `stop backend` — now restarts the old backend before aborting, which required `migrate_db` to propagate failure explicitly (bash suppresses `set -e` inside an `if !`-called function, so a bare failing psql let the loop continue and the function still returned 0); (3) the line-based destructive grep let a multi-line `ADD COLUMN ... NOT NULL` / `ALTER COLUMN ... TYPE` evade the carve-out and matched commented-out DDL — now normalizes each file (strip `--` comments, collapse newlines so `[^;]*` bridges span a statement; semicolons still bound them), unreadable -> carve-out. Dismissed (verified): singleton-overlap (refuted by `shutdown()`), `ALTER ... SET NOT NULL` / non-CONCURRENTLY index lock (documented availability-only foot-gun, beta-negligible), 60s probe budget (worst case ~10-15s). Tier 2 (true zero-downtime: internal proxy or host-nginx upstream flip + `RUN_BACKGROUND_JOBS`/`SIGUSR2` promotion + durable cadence guards) remains deferred.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## UI-ACCREDITATION-ORCID-REDIRECT-GUARD — wire the bfcache guard into the 5th ORCID-redirect page (archived 2026-06-14) — review clean ✓

Follow-up split from the `ui-orcid-redirect-stuck-button` review: `accreditation.js` was the 5th ORCID-redirect page carrying the identical bfcache stuck-button bug, missed by that task's "all four pages" scope. Fixed at `219d0e71` by reusing the already-reviewed `createOrcidRedirectGuard('orcidLoading')` mixin — import + spread (after the existing `createTimerGuard()`), a NEW `init()` calling `_installOrcidRedirectGuard()` (the page had none, mirroring login.js), and `_teardownOrcidRedirectGuard()` added to the existing `destroy()` (after `_teardownTimers()`). The `handleOrcidVerify` handler is unchanged (still sets `orcidLoading=true`, resets only in `catch`), so the pageshow guard is the actual fix. Reviewed clean (`/ce-code-review`: correctness on the session model, testing + project-standards on Sonnet; ce-agent-native skipped per PEvO — 0 findings): wiring mirrors login.js byte-for-byte with no regression from the added init(), and the new bfcache test (`pages-accreditation.test.js`) has teeth (2 mutation probes confirmed it catches both install-removal and persisted-check-removal) and is strictly stronger than the siblings (adds a `persisted:false` no-op assertion). Closes the 5th-page completeness gap; all five ORCID-redirect pages now bfcache-guarded.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## SPA TREATS /signup ORCID_ALREADY_LINKED 409 AS TERMINAL (archived 2026-06-14) — review clean; AC3 live-verify folded to existing follow-up ✓

`POST /api/auth/signup` returns 409 `ORCID_ALREADY_LINKED` when the ORCID is already bound to another account (`accounts_orcid_unique`); the 409 is terminal (ORCID taken AND the single-use verification nonce already consumed, so a same-`orcid_token` resubmit only yields a confusing 422 — backend Option 2, SPA owns recovery). Fix in `frontend/src/pages/signup.js` (`fa7f75a0`): `handleSubmit`'s catch branches on `err.code === 'ORCID_ALREADY_LINKED'`, sets a reactive `orcidAlreadyLinked` flag + the terminal `signup.orcidAlreadyLinked` copy, and renders a recovery affordance (Sign in link to /login + "Verify with ORCID" restart button, reusing existing keys); `canSubmit` returns false while the flag is set (blocks blind resubmit — AC2); the flag clears on `handleOrcidVerify` (restart OAuth) and `clearOrcid` (revert to password branch). One new key in all 16 locales (English-stubbed, STUBS.md). Reviewed clean across 4 personas (`/ce-code-review`: correctness on the session model, testing + project-standards + maintainability on Sonnet; ce-agent-native skipped per PEvO — 0 findings): contract matches end-to-end (`auth.md` ↔ `auth.ts` 23505→409 ↔ `api.js` code propagation); NO lockout trap (three recovery paths incl. the always-visible ORCID-badge X → clearOrcid); error-display only (no auth logic / fictional account-state defense); the 4 unit tests have teeth (mutation-verified — removing the canSubmit gate fails the no-re-POST test, dropping the 409 branch fails the message tests); no emdash in user-facing copy. AC3 (live duplicate-ORCID 409 → terminal copy in a browser) deferred by the implementer: it needs an account pre-bound to the same ORCID via the orcid-stub stack, not a plain dev browser. Reasonable carve-out for a P3 UX task; recommendation recorded — fold a single duplicate-409 terminal-copy assertion into the existing `pending/ui-orcid-signup-recover-real-roundtrip` E2E (which already drives a real `/api/auth/signup` through the shared orcid-stub infra) rather than a standalone E2E. Advisory (not a finding): `handleOrcidSignup` does not clear the flag, harmless under the current full-page-redirect model (component re-instantiates) but a stale-flag risk if ever refactored to in-page OAuth.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## ORCID REDIRECT BUTTON STUCK ON "REDIRECTING TO ORCID" AFTER BROWSER BACK (BFCACHE) (archived 2026-06-14) — review clean; accreditation.js gap split to follow-up ✓

Confirmed-reproducible real incident: each ORCID-redirect handler set a reactive loading flag `true` then did `window.location.href = <orcid url>`, resetting the flag only in `catch`, so the success path navigated away with the flag still `true`; on browser Back the page is bfcache-restored (Alpine `init()` does not re-run, `destroy()` does not fire) and the button stayed `:disabled` on its "Redirecting to ORCID..." label until a hard reload. Fix: a shared `createOrcidRedirectGuard(flagName)` mixin (`frontend/src/lib/orcid-redirect-guard.js`, mirroring the existing `timer-guard.js` pattern) that registers a `pageshow` listener resetting the flag on bfcache restore (`event.persisted === true`) and clearing the `pevo_orcid_mode` marker — wired into the four pages the task enumerated (signup `orcidLoading`, settings `orcidLinking`, login [new `init()` added], recover [new `destroy()` added, also fixing a latent `_orcidCheckTimer` leak]). Landed at `f5297ac2`; reviewed clean across 6 personas (`/ce-code-review`: correctness + adversarial on the session model, julik-frontend-races + testing + maintainability + project-standards on Sonnet; ce-agent-native skipped per PEvO): listener lifecycle correct (method-reference field, deregister-before-reassign, SSR guards), reactivity through the Alpine proxy confirmed, the `pevo_orcid_mode` clear cannot strand an in-flight callback (separate document behind ORCID in history), and the per-page bfcache tests have teeth (verified by mutation; lib + 4 page tests, 185 pass). One P2 completeness finding (4/6 reviewers, confidence 100): `accreditation.js` is a FIFTH ORCID-redirect page with the byte-for-byte identical bug, missed by the "all four pages (verified)" scope table (live route — `orcid-callback.js` has a `backPath='/accreditation'` case). User triage: archive this task (its 4-page fix is correct) and split the 5th-page wiring to follow-up `ui-accreditation-orcid-redirect-guard` (pending), a trivial reuse of the new mixin. Advisory/dismissed: the deregister-before-reassign comment slightly overstates cross-instance efficacy but is harmless (page-mount destroyTree reliably fires teardown); minor untested defensive branches (theoretical-only).

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## UI-FRONTEND-COMMENT-ANCHOR-RESIDUAL-SWEEP — clear pre-existing comment-anchor rot in frontend/src + frontend/tests (archived 2026-06-14) — 1 hold round, re-review clean ✓

Durability sweep re-anchoring pre-existing comment-anchor rot (task slugs, round/item/acceptance ordinals, line-number/SHA cites, soft "see task" redirects) across `frontend/src/**` + `frontend/tests/**` onto stable symbols/behavior, keeping durable refs (solutions/*, api-contracts/*, ARCHITECTURE.md §). First pass (`bbe6607c`) was diff-clean (mechanically comment-only, every re-anchor on a stable symbol, no new rot, WHY preserved) but FAILED acceptance criterion #1 ("a FULL enumeration first"): its enumeration grep under-reached prefix families (caught `UI-`/`BE-`/`SEC-`/`BACKEND-` but missed `JFR-`, `TEST-`, and the soft `See task ITEM N` redirect), leaving in-scope rot in files outside its own diff — incl. two production-source sites whose TEST siblings WERE swept (`search.js` `JFR-001`, `accredited-directory.js` `See task ITEM N`), the documented `convention-sweep-syntactic-form-misses-semantic-siblings` failure mode. Architect held the 5 misses (the two above + `pages-settings.test.js` `AC #3`, `bridge-preview.spec.js` `bridge.js L66`, `keychain.js`/`global-setup.js` coordination-ID redirects), with scope clarifications: KEEP the self-describing E2E coverage-matrix header IDs (`E2E-AUTH-2` etc.) and `SEC-00N` security IDs (self-describing labels, not external redirects); DEFER `describe()`/`it()` test-name strings (re-labeling shifts CI output); `HJ-NP` is a false positive (base58 WIF charset). Fixed at `d1132857` (re-anchored all 5 onto behavior/durable symbols — `keychain.js` onto `sec-001-equivalence.test.js`; plus a bonus `publish.spec.js` "this task's contract" redirect) and verified clean at re-review (`/ce-code-review`: correctness + project-standards on the new diff; replacement-audit per the "convention-enforcing fix must audit its own new code" rule passed; widened completeness re-grep returns empty for all clear in-scope classes; comment-only, no behavior change). Systemic follow-up filed: `architect-comment-anchor-ci-gate` (mechanize the convention as a diff-gate so sweeps stop missing files — this is the 3rd+ documented recurrence).

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## PERSIST BACKEND LOGS FOR TWO WEEKS (SURVIVE RESTARTS/RECREATIONS) (archived 2026-06-14) — deployed + verified on toolshed ✓

A 2026-06-12 ORCID signup incident was undiagnosable two days later because backend logs were gone: `docker-compose.yml` set no `logging:` driver (default per-container `json-file`) and `./deploy.sh restart` does `down`/recreate, discarding the per-container logs on every restart. Fix routes backend + postgres stdout (already newline-delimited pino JSON, no backend-zone change) to the HOST systemd journal via a new prod-only `docker-compose.prod.override.yml` (journald driver + stable per-service `tag`), so entries survive recreation and image rebuilds; time-based 14-day pruning is native (`MaxRetentionSec=2week`). Portability-safe: `deploy.sh resolve_log_overlay` auto-applies the overlay ONLY when `/run/systemd/journal/socket` is present (`PEVO_LOG_DRIVER=auto|journald|json-file` overrides), because the journald driver hard-fails container start when the socket is absent — so dev/WSL/E2E stay on `json-file` automatically. `LOG_OVERLAY_FLAGS` is threaded into every container-CREATING path (`cmd_up`, `cmd_restart` infra `up`); `cmd_test_up` intentionally omits it. New `./deploy.sh logs-history <tag> <since>` wraps `journalctl CONTAINER_TAG=…` for cross-recreation retrieval (the >24h-after-restart path). redis/ipfs stay on `json-file` (chatty, low incident value, less journald rate-limit pressure). The `.githooks/commit-msg` architect zone was generalized from two exact `docker-compose` literals to `^docker-compose[^/]*\.yml$` (matching the convention doc's already-documented `docker-compose*.yml` glob), with 3 new harness cases (45/45 pass); `agents/architect/CLAUDE.md` "Files You Own" updated to match. Host retention is operator-configured via a self-contained `journald.conf.d` drop-in documented in the override file (Storage=persistent, MaxRetentionSec=2week, SystemMaxUse cap, RateLimitBurst against silent burst-drop).

`/ce-code-review` (6 reviewers; ce-agent-native skipped per PEvO): no P0/P1 defects, project-standards clean (zone-extension protocol followed). All 9 surfaced findings approved and applied (`4463f382`): host-persistence + Docker-Desktop-edge warning in `cmd_up`, self-contained host-setup block replacing a dangling "runbook" ref, inline permission remedy in `logs-history`, `cmd_test_up` seam comment, `COMPOSE_FILES`→`LOG_OVERLAY_FLAGS` rename (avoids Docker's `COMPOSE_FILE` collision), `none`-synonym drop, regex tightening + hook tests. Follow-up `f82667d3` corrected the embedded host-setup block after the operator hit it live: systemd config does not support trailing `#` comments on a value line, and the `<<'EOF'` heredoc terminator must be column-0 — switched to a values-only drop-in written via a paste-robust `printf` pipe. Verified on toolshed: `./deploy.sh restart` applied the overlay and `./deploy.sh logs-history pevo-backend` returned backend pino lines from the host journal (incl. a fresh post-restart container's startup sequence). Commits: 321c307e, 4463f382, f82667d3. Dismissed: a net-new deploy.sh shell-test harness (preemptive). Deferred to `/ce-compound-refresh`: a stale `git add docker-compose.yml` worked-example in the zone-audit convention doc.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## UI-CREDIT-OP-PROOF-CACHE-SLOT-KEY — key the credit-op fresh-auth proof cache per slot (archived 2026-06-14) — 2 hold rounds, re-review #2 clean ✓

The SPA fresh-auth proof cache keyed credit ops (`claim`/`approve`/`revoke_authorship`) by paper alone, so two proofs for different slots/subjects on the same paper collided (a slot-2 proof overwriting slot-3, then `POST /api/custody/broadcast` returning 403 `FRESH_AUTH_REQUIRED` `target_mismatch`). Fixed: `cacheConsentOpProof`/`getCachedConsentOpProof` (`frontend/src/lib/fresh-auth.js`) now take optional `authorIndex`/`claimer`, normalize absent values to `null`, and strict-match the full target; the key derives from the BACKEND-ECHOED `author_index`/`claimer` (threaded from `/orcid/callback` `_handleFreshAuth` and the password factor), not a client reconstruction; the credit-op broadcast consumer is `withAuthorshipFreshAuth` (paper-detail claim/approve/revoke). Delivered alongside `ui-multi-author-consent-affordances` (its broadcast-wiring consumer) after the backend echo (item 7 of the archived `backend-authorship-credit-ops-fresh-auth`) unblocked it. Two architect hold rounds, both closed clean: round 1 — the `_handleFreshAuth` echo guard validated only the `(action, root_author, root_permlink)` triple, not the credit fields the cache now keys on (a dropped credit-field echo would cache `undefined → null` while the lookup keys on the real index/subject, permanently missing and re-OAuth-looping with no error surface); fixed to require `author_index` a non-negative integer (slot 0 valid via `Number.isInteger`) for claim/approve and `claimer` a non-empty string for approve/revoke, surfacing `status='error'`/`orcid.verificationFailed` on a malformed echo BEFORE any cache write; plus a slot-0 positive-hit test and a test-header anchor cleanup. Round 2 (re-review #2) — closed the two under-tested guard branches (revoke with missing/empty `claimer`; approve with valid `claimer` but missing/non-integer `author_index`), each asserting the error status/key AND the ABSENCE of the sessionStorage proof write (parsed). Re-review #2 landed at `b1838940` and verified clean (`/ce-code-review`: correctness on the session model, testing + project-standards on Sonnet; ce-agent-native skipped per PEvO — 0 findings); teeth confirmed by mutation (4 tests flip red); pages-orcid-callback 76 green, full unit suite 1525.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## ORCID CALLBACK ERROR MESSAGES: DE-HARDCODE COUNT, FIX MIS-MAPPED + GENERIC ERRORS (archived 2026-06-14) — held items landed, re-review clean ✓

The ORCID callback page (`frontend/src/pages/orcid-callback.js` `_verify` error-branching) mapped backend errors to fixed i18n copy by error code only, producing a hardcoded works count, a mis-mapped VALIDATION_ERROR, and a generic timeout/outage fallback that offered an unsafe one-tap retry. All five acceptance criteria met in the implementation: (1) de-hardcoded the works count — `signup.orcidInsufficientWorks` parameterized to `{min}`/`{have}` fed from the backend's `details:{required,have}` 422; (2) fixed the VALIDATION_ERROR mis-map (accredit-mode "already accredited" 422 no longer borrows the works copy) by discriminating on the works-count `details` shape; (3) de-genericized the 504 `ORCID_PROVIDER_TIMEOUT` and 500 `INTERNAL_ERROR` cases with dedicated copy and a no-one-tap-retry `'timeout'` affordance (avoids the auth-code double-spend the backend's `verify_before_retry` hint warns against); (4) pre-flight note on the signup page covering the self-asserted-vs-external and visibility failure modes; (5) regression coverage asserting the resolved string + interpolated `{min}`. Architect held two polish items, both landed at `338c1286` and verified clean at re-review (`/ce-code-review`: correctness on the session model, testing + project-standards on Sonnet; ce-agent-native skipped per PEvO — 0 findings): (a) `orcid.serviceUnavailable` reworded to the no-button "start over" framing (matching its `'timeout'` affordance and sibling `orcid.providerTimeout`), re-stubbed across all 16 locale files and recorded in STUBS.md under `### Updated`; (b) the `INTERNAL_ERROR` branch comment qualified per `api-contracts/orcid.md` "State consumption semantics" — state is consumed only when the throw is downstream of the consume DEL; a pre-consume throw preserves state; withholding the one-tap retry is correct regardless because the client cannot distinguish the two (comment accuracy only, behavior unchanged). Direct verification confirmed all 16 locales consistent, no emdash in the user-facing copy, the comment matches the contract verbatim, and the suite is green (pages-orcid-callback 76; the task note's "72" was a stale count). Advisory/dismissed: no unit test pins the resolved copy text (pre-existing, theoretical-only).

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## SETTINGS FRESH-AUTH USERNAME_MISMATCH TEARDOWN (archived 2026-06-14) — held items landed, re-review clean ✓

`withSettingsFreshAuth` (`frontend/src/lib/settings-fresh-auth.js`) now tears the corrupted session down on `username_mismatch` (JWT subject vs proof subject diverge) instead of returning a generic `freshAuthFailed`, matching the authorship (`withAuthorshipFreshAuth`) and session-kind (`broadcastWithFreshAuth`) siblings. The shared teardown was extracted to `handleSessionInconsistency()` in `lib/fresh-auth.js` (reason-only gate matching the no-`status` `ApiRequestError` shape). Original implementation reviewed clean; the architect held two items, both landed at `32f0917d` and verified clean at re-review (`/ce-code-review`: correctness on the session model, testing + project-standards on Sonnet; ce-agent-native skipped per PEvO — 0 findings): (1) caller-level `sessionInconsistent` coverage in `pages-settings.test.js` — `handleSetPassword` asserts the held plaintext (`newPasswordInput`/`newPasswordConfirmInput`) is zeroed for XSS-read hygiene with no second toast and no generic error (teeth: removing the `sessionInconsistent` arm falls through to the success path's `toast.show`, failing the assertion), plus a `handleEmailDelete` clean-abort case (no disconnect/navigate/toast); (2) the stale "Shared by both orchestrators" docblock on `passwordPromptMessage` reworded to name the settings + authorship password-factor surfaces (`broadcastWithFreshAuth` has no password prompt). Direct verification confirmed `handleSetPassword` zeroes both inputs before the `sessionInconsistent` early return, `handleEmailDelete` returns before any side effect, and `passwordPromptMessage` has exactly the two claimed callers. Suites green (pages-settings 89, lib-settings-fresh-auth, lib-authorship-consent, fresh-auth-401-retry; 125 total).

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## SIGNUP DUPLICATE-ORCID 409 BURNS THE ORCID NONCE, BLOCKING RETRY (archived 2026-06-14) — Option 2 (terminal 409); architect deliverables landed ✓

Follow-up filed from the `/ce-code-review` of `backend-signup-orcid-duplicate-409`: a duplicate-ORCID `/signup` returns the clean `409 ORCID_ALREADY_LINKED`, but the single-use ORCID verification nonce is consumed (del-on-read) BEFORE the failing INSERT, so a same-`orcid_token` resubmit falls through to the missing/invalid-token `422` rather than reproducing the 409 — a confusing dead-end (pre-existing; the 409 commit only changed the error code from 500). The user chose Option 2 (terminal 409) over Option 1 (re-seed the nonce for a deterministic retry-409): a duplicate-ORCID 409 means the ORCID is already bound to ANOTHER account, so a same-ORCID resubmit can never succeed regardless of nonce state — the real recovery is logging into the existing account, not retrying signup; re-seeding would only reproduce a permanent rejection (cosmetic). No backend code change (the 23505 to `accounts_orcid_unique` to 409 mapping already lands; the nonce-del-before-INSERT ordering is left as-is by design, the SPA owns recovery). Resolved at `fb4f53dc`. Two architect-owned deliverables landed at archive: (1) extended the `/signup` `ORCID_ALREADY_LINKED` entry in `api-contracts/auth.md` with the terminal-recovery semantics (resubmit cannot succeed, nonce consumed so a resubmit yields 422, restart OAuth or log into the existing account); (2) spawned `ui-signup-orcid-already-linked-terminal-409` so the SPA treats the 409 as terminal (a clear "log in instead" affordance, no blind resubmit), mirroring the existing `/orcid/callback` durable-binding 409 handling. No `/ce-code-review` fan-out: the resolution carried no code diff (decision plus task-file only).

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## SignupBodySchema REJECTS password:null, 400s PASSWORDLESS ORCID SIGNUP (archived 2026-06-14) — widen to .optional().nullable(); round-2 (test-isolation + comment hygiene) clean ✓

`POST /api/auth/signup` carrying `password: null` (the passwordless-ORCID signup contract the SPA sends) was 400'd at schema validation because `SignupBodySchema.password` was `z.string().optional()` (accepts `string | undefined`, not null). Widened to `z.string().optional().nullable()` (no `.min(1)`, so a previously-accepted empty string is unaffected); no handler change — `hasPassword` already treats null/empty as no-password, and a null-password account lands `accounts.password_hash IS NULL`. Real-path coverage in `auth.test.ts` (null then 200 + password_hash NULL; omitted then 200; non-null string then 200 + password_hash NOT NULL). Landed at `460cd989`; downstream `ui-orcid-signup-recover-real-roundtrip` unblocked (`220fb9cc`). Round-1 review (`/ce-code-review`, 5 reviewers: correctness + security on the session model; testing/project-standards/api-contract on Sonnet; ce-agent-native skipped per PEvO) clean on the critical axis: a `password_hash IS NULL` account CANNOT be password-authenticated — login + resend-verification guard the null/empty hash BEFORE the argon compare, and ARCHITECTURE.md § 6.1 State C (passwordless ORCID-only) enumerates the state; api-contract clean (`auth.md` already documented `password: null`). Round-1 held two backend-owned items, both landed at `3cf4a609` and verified clean at re-review: (P2) the new test block's `afterAll` cleanup, which deleted by a `0000-0003-${suffix}-%` wildcard over an ORCID band shared with sibling suites under `maxWorkers:2` (a coinciding timestamp last-4 could clobber a concurrent sibling's row mid-test), now scopes the orcid arm to `orcid = ANY([exact 3 seeded orcids])` so it cannot touch a foreign row; (P3) the `SEC-004` tracking-id removed from the source comment (re-anchored on the durable § 6.1 State C reference) and the test describe title, with pre-existing `SEC-004-BE` cites in unrelated files left untouched per commit scope. Re-review confirmed: no `SEC-004` remains in the touched files, the comment anchors on § 6.1 State C, exact-match cleanup, 3 null-password specs green. Dismissed/residual (unchanged): the pre-existing `/api/auth/reset` mailbox-gated password-add path; the documented 403-vs-401 passwordless disclosure (by design).

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## BOOT ASSERTION THAT THE accounts_orcid_unique INDEX PHYSICALLY EXISTS (archived 2026-06-14) — to_regclass boot probe for the sole-guard ORCID index; round-2 clean (round-1 hold was a false positive) ✓

`verifyAppDbMigrationsWith` (`backend/src/app-db.ts`) now runs, after the existing missing-migrations check, a read-only `to_regclass('public.accounts_orcid_unique') IS NOT NULL` probe and throws the same `BootFatalError` (which boot maps to `flushAndExit`) when the sole-guard partial unique index is absent — so a decoupled state (007's `schema_migrations` row present but the index dropped via a hand-run `DROP INDEX`, a rows-only `pg_dump`/restore, or out-of-order hand-apply) fails fast at boot instead of at the first silent duplicate-ORCID write. Gated on `expected.includes('007_accounts_orcid_unique.sql')` for fork-safety; scoped to this one index (not a verify-every-index sweep); read-only, so it respects the "migrations authoritative, no DDL on startup" contract. Real-DB coverage in `app-db-fresh-migrations.test.ts`: cold-apply, `DROP INDEX`, assert the throw names the index, with a guard that 007's row survives so the throw is the index check (not the missing-migration path); mutation-kills (delete the throw body, the spec goes red). Landed at `8ee3ea79`. Round-1 review (`/ce-code-review`, 4 reviewers: correctness on the session model; reliability/testing/project-standards on Sonnet; ce-agent-native skipped per PEvO) clean on substance: `to_regclass` verified correct for a partial unique index against the live DB, fail-fast/boot-ordering correct (probe awaited before `listen()`), standards clean. Round-2 fix `23d54216` re-anchored the docblock off a `updateAccountOrcid` symbol the correctness reviewer claimed did not exist.

RECORD CORRECTION (architect, at archive): that round-1 hold was a FALSE POSITIVE. `updateAccountOrcid` DOES exist at `routes/orcid.ts:2039` — an `UPDATE accounts SET orcid = $1 WHERE username = $2` writer (the accredit/link post-broadcast cascade) with no application-level uniqueness check, exercised via `__test_seams.updateAccountOrcid`. The original comment ("/signup and updateAccountOrcid carry no app-level uniqueness check") was accurate and precise; naming the exact exported symbol was in fact more convention-compliant than the vaguer "the ORCID-write handlers in routes/orcid.ts" that replaced it. The replacement is still accurate (harmless, comment-only), so the code stands as committed and the task is archived clean — but the hold should not have been filed, and the re-review note's "no updateAccountOrcid function exists anywhere" is wrong. Process miss: the architect propagated the reviewer's "symbol does not exist" claim without grep-verifying it before holding; such non-existence claims must be grep-confirmed first. Dismissed (unchanged from round 1): index-name to migration-filename string coupling (intentional fork-safety), missing `statement_timeout` on the boot verify queries (pre-existing, all four).

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## SIGNUP DUPLICATE-ORCID SURFACES AS 500 INSTEAD OF 409 (archived 2026-06-14) — map 23505 on accounts_orcid_unique to 409 ORCID_ALREADY_LINKED; first review clean ✓

A `/signup` that binds an already-used ORCID to a new account row trips the `accounts_orcid_unique` partial index (007) and Postgres raises 23505, which fell through to a generic `500 INTERNAL_ERROR "Registration failed"`. The `/signup` catch (`routes/auth.ts`) now detects `err.code === '23505'` AND `err.constraint === 'accounts_orcid_unique'` and returns the clean `409 ORCID_ALREADY_LINKED` ("This ORCID is already linked to another account") the `/orcid/callback` accredit/link paths already use, gated on the constraint name so an unrelated future unique violation still 500s. The email-duplicate path (409 DUPLICATE before the INSERT) is untouched. Real-path coverage in `auth.test.ts` under the `dbReachable` guard: a prior row holds the ORCID, the second `/signup` (ORCID-only and ORCID+email branches) hits the real index, raises 23505, and asserts 409 + ORCID_ALREADY_LINKED + terminal wire shape. Landed at `edd35067`. First review (`/ce-code-review`, 5 reviewers: correctness + security + adversarial on the session model; api-contract/testing on Sonnet; ce-agent-native skipped per PEvO) clean: correctness empirically confirmed against live pg that a partial-index violation populates `err.constraint = 'accounts_orcid_unique'` (so the branch reliably fires); security confirmed it is NOT an enumeration oracle (reaching the 409 requires a valid single-use ORCID OAuth nonce, so the caller already owns the ORCID) and is a pure error-code remap (the index still rejects the duplicate, no row persists, ARCHITECTURE.md 6.1/6.4/6.5 upheld); api-contract confirmed the 409 byte-matches the existing `/orcid/callback` 409s and is SPA-safe (the SPA branches on `err.code`; both the old 500 and new 409 land in the same generic handler). Architect documented the new 409 on `/signup` in `api-contracts/auth.md` at archive. Triage: the ORCID+email DO-UPDATE-to-23505 test-arc gap dismissed (same catch branch + same constraint gate as the covered plain-INSERT arc); the P2 pre-existing retry-dead-end (the ORCID nonce is burned before the INSERT, so a rejected user cannot retry without re-running OAuth — predates this diff, which only changed the error code) filed as a follow-up task (`backend-signup-orcid-dup-nonce-retry`).

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## ORCID WORKS-GATE: STRUCTURED ERROR DETAILS + THRESHOLD DE-HARDCODE (archived 2026-06-14) — details:{required,have} on the works-gate 422; first review clean ✓

The ORCID works-count rejection (422 VALIDATION_ERROR) in `handleSignup` and `handleAccredit` (`routes/orcid.ts`) now passes a structured `details: { required: config.orcidMinWorks, have: externalWorksCount }` as the 5th arg to `sendError` (which already promotes it to `error.details`), so the SPA renders the real numbers instead of a hardcoded "3". Human message unchanged. New describe block in `orcid.test.ts` drives the count below the threshold (the shared `installOrcidFetchStub` previously only served `works = orcidMinWorks`, so the rejection branch was wholly unguarded): signup at `orcidMinWorks-1` (details deep-equals `{required, have}` with `have` distinct from `required` so a hardcode of either fails loudly) and accredit at 0 (asserts the gate precedes the admin broadcast, no broadcast fires). Landed at `10ac1d14`. First review (`/ce-code-review`, 4 reviewers: correctness on the session model; api-contract/security/testing on Sonnet; ce-agent-native skipped per PEvO) clean: numbers correct and consistent with the message, both branches edited (grep confirms exactly two works-gate sites), owner-only reachable (`have` is the authenticated owner's own ORCID public-works count), no info leak (threshold is public policy), purely additive envelope change, test mutation-kills (remove the 5th arg, both specs red). Architect completed task item 3 at archive: documented `details: { required, have }` on the insufficient-works 422 in `api-contracts/orcid.md`. Dismissed at triage: a test-comment nit (accredit spec comment misattributes the un-accredited state to `hafQueryMock`; behavior correct) and a pre-existing NaN-if-non-numeric-`ORCID_MIN_WORKS` residual (predates the diff).

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## BACKEND-ANCHOR-CANARY-LINE-NUMBER-CITE-EXTENSION (archived 2026-06-14) — extend the standing src/ comment-anchor canary to the line-number-cite rot class; first review clean ✓

Extended the standing canary `tests/eslint/no-stale-comment-anchors.test.ts` with a combined `LINE_CITE_RE` detecting the three documented line-number-cite rot forms in `src/` comments: `<file>.(ts|tsx|js|mjs|cjs|sql):<n>` path cites, `line <n>`/`lines <n>-<n>` references, and a terminal `~<n>` tilde approximation — wired in as a fourth scan class alongside round-N/Option-X.N/role-slug. Closes the detection gap that let the `idempotency.ts` `getPool()` file/line cite survive the big src/ sweep. Load-bearing design choice: the tilde arm fires ONLY when `~<n>` is terminal (followed by EOL or clause punctuation `) ] ; : }` or a `.`/`,` not opening a decimal/thousands group), sparing the ~80 legitimate `~<n>` approximations in src/ (latency, strides, decimals, `~2×`, `~95%`, bare counts) without a fragile unit denylist; documented accepted blind spot is a cite followed by a word (`~337 in foo`). 7 planted positives + 12 planted negatives added to the self-test (`no-bridge-paper-literal` precedent); fixtures live under tests/ (not scanned) so they cannot self-trip. Optional SEC-NNN/BE-NNN tracking-id check omitted by design (0 current instances; a durable id ref need not sit on a solutions/-path line, so DURABLE_PATH_RE could not reliably exempt it — net-negative). Landed at `7ad16d53`. First review (`/ce-code-review`, 6 reviewers: correctness + adversarial on the session model; testing/maintainability/project-standards + learnings on Sonnet; ce-agent-native skipped per PEvO) came back clean — project-standards and maintainability fully clean, acceptance criteria all met, all 19 planted assertions independently verified, live full-src scan 0 hits, new comment text self-rot-free. All findings P2/P3 advisory and latent (0 current src/ instances): a future-code false-positive surface from the all-lines scan (tilde arm matches bitwise-NOT `~0;`, path arm matches string-literal `"redis.ts:6379"`), a symmetric unitless-approx FP (`under ~5.`), a by-design detection leak (misses `L371`/bare `:371`/`@@`/unlisted exts), and a mutation-coverage gap (4/6 extensions tsx/mjs/cjs/sql and 4/5 tilde terminal-punct members ] ; : } droppable without a self-test flip). All dismissed at triage per the preemptive-test-hardening default (theoretical-only, corpus clean, acceptance met).

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## UI-MULTI-AUTHOR-CONSENT-AFFORDANCES (archived 2026-06-14) — unified credit-badge display + Route-2/3 consent affordances + pending-authorships discovery; re-review clean (held items fixed, suite green) ✓

Landed the frontend consent surface over the ratified consented-authorship model (`ARCHITECTURE.md` § 2): a single SPA-computed "credited" badge per paper-detail author (union of the hive-keyed `consented` flag (Routes 1/2) OR an `accepted` `authorship_claims[]` entry (Route 3) — decision β; the old `Confirmed`/`Pending` two-tier claim badge collapsed into it, pending → plain text + affordance), Route-2 accept/resign affordances (net-new), the existing Route-3 claim/approve/revoke ops re-routed through the per-slot-keyed `fresh_auth_proof` wiring, and a pending-authorships discovery surface off `GET /api/me/authorships/pending` (503 fail-closed → retry affordance, never a silent empty). Light-account ops sign via the custody fresh-auth orchestrator (`withAuthorshipFreshAuth`), self-custody via Keychain. Delivered with the sibling `ui-credit-op-proof-cache-slot-key` in one commit (shared `orcid-callback.js`). Round-1 `/ce-code-review` (10 reviewers) held: a BLOCKER red unit suite (`handleRejectClaim`→`handleRevokeClaim` test lag), synchronous double-submit guards on the five consent handlers, extraction of the duplicated password-factor machinery into a shared `mintViaPasswordFactor`/`REMINTABLE_REASONS`/sentinels in `lib/fresh-auth.js`, `username_mismatch` session teardown matching `broadcastWithFreshAuth`, an `authorships` store in-flight load short-circuit, `orcid-callback` `destroy()` return-path scrub, and comment-anchor cleanup; minor #5 (rethrow on second-mint transport error) dismissed as contradicting settings' pinned masking. Round-2 re-review (`3b1ea515`, 9 reviewers; ce-agent-native skipped per PEvO) came back CLEAN on all held items: BLOCKER resolved (197/197 green across the six touched files; full suite reported 1490), the shared `mintViaPasswordFactor` proven byte-equivalent to both prior inline copies (the dismissed second-mint masking is sound — identical pre-existing behavior on both surfaces, pinned by a settings test), the double-submit guards verified synchronous-first with `finally` reset, and `destroy()` clearReturnPath satisfies the documented storage-scope cleanup-parity convention. New non-blocking findings triaged at archive: the `withSettingsFreshAuth` `username_mismatch`-teardown asymmetry filed as `ui-settings-fresh-auth-username-mismatch-teardown` (P2, different surface); pre-existing frontend comment-anchor residuals filed as `ui-frontend-comment-anchor-residual-sweep` (P3); dismissed/accepted — the `authorships.load()` dropped-refresh edge (narrow, self-healing), the `withAuthorshipFreshAuth` `username_mismatch`-gate comment being factually wrong about the error shape (behavior correct — error from `signer.js#broadcastOps` does carry `status`; gate fires on `reason` regardless), the echo-guard stray-field hardening (backend-divergence-gated), minor unit-test gaps, and the now-teardown-only Alpine import. The sibling `ui-credit-op-proof-cache-slot-key` was held round-2 for two echo-guard negative-coverage tests (revoke/missing-claimer, approve/missing-author_index) and is not yet archived.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## BACKEND-ANCHOR-ROT-RESIDUAL-IDEMPOTENCY-CLAIMS-SEC-ID (archived 2026-06-14) — 2 residual comment-anchor cites the src/ canary structurally misses; first review clean (record corrected) ✓

Cleared the two pre-existing comment-anchor residuals the standing `src/` canary cannot catch (both comment-only, `f50000e8`). (1) `lib/idempotency.ts`: a `(see ... src/db.ts)` file-path cite re-anchored on the exported symbol `getPool()` (verified exported in `db.ts` with `max:3` / `connectionTimeoutMillis:5000`, ~57 call sites, so the anchor is durable). (2) `routes/claims.ts`: the revoke-gate authorization comment dropped a `SEC-003-BE` id and re-anchored on behavior ("Including it would mean every authenticated user could revoke any claim on any bridge paper"), WHY preserved. First review (`/ce-code-review`, 6 reviewers incl. correctness + security on the session model; ce-agent-native skipped per PEvO) clean: comment-only confirmed by diff, the revoke gate is a pure `isPostAuthor || isClaimer || isAdmin` OR with `verifyHiveSignature` intact, the security WHY is accurate, no new rot class introduced. RECORD CORRECTION (architect, at triage): the implementer's "SEC-003-BE resolves nowhere / dead pointer" rationale was factually wrong — `SEC-003-BE` resolves to the durable `solutions/conventions/test-config-mock-distinct-role-accounts-2026-04-21.md` (which documents it as the P0 bridge-account-equality gate incident, finding B5) and is the organizing `describe()` id of `claims.test.ts`. The behavioral re-anchor is nonetheless convention-compliant (behavior is the preferred anchor and the WHY is preserved), so the code stands as committed; only the rationale was inaccurate. Follow-up filed to extend the canary to the line-number cross-reference class that let the `idempotency.ts` site slip through (the canary covers round/Option/role-slug classes, not line-number cites).

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## BACKEND-HAF-QUERY-COMMENT-ANCHOR-SWEEP (archived 2026-06-14) — full backend/src comment-anchor sweep + citationsArrayGuardSql extraction + standing canary; round-3 (planted-positive) clean ✓

Full `backend/src/` comment-anchor-rot sweep (user-scoped to `src/`, broadened from the architect's 3-file unblock after a fresh re-enumeration found rot in ~34 files). Removed round/hold ordinals, `F-finding`/`item #N`/`Option A.N` labels, orphan task slugs (uppercase `BACKEND-`/`BE-`/`SEC-` and lowercase `<role>-<kebab>` plus dead `.md`/`see task` redirects), and line-number/SHA cross-refs — all re-anchored on stable symbols/behavior; load-bearing WHY docblocks preserved, durable `solutions/` + `api-contracts/` refs deliberately kept. Extracted `citationsArrayGuardSql(citingAlias, appTagParam)` in `notification-queries.ts` (mirrors `imageSrfGuardExpr`; alias identifier-validated, appTag a bound `$N`) replacing the verbatim arm 6a/6b SRF guard with byte-equivalent SQL, preserving the LATERAL-before-WHERE invariant. Landed across `324ca283` (sweep + extraction), `4c172629` (round-2: 6 `orcid.ts` `A.1` orphans re-anchored, 8 lowercase dead slugs cleared, new standing canary `tests/eslint/no-stale-comment-anchors.test.ts` walking every `src/*.ts` for round/Option/role-slug rot with a durable-path carve-out), and `2dfb9acb` (round-3: planted-positive/negative self-test so a corrupted detection regex cannot silently no-op). Reviews: round-1 sweep clean (extraction equivalence proven); round-2 held then cleared the A.1 orphans, lowercase slugs, and canary-tightening; round-3 held one test-quality item (the canary asserted only `violations==[]`, which a broken regex satisfies vacuously), closed by a third `it()` block asserting each regex fires on known-bad anchors and spares legitimate/durable tokens (both halves of the carve-out). Final re-review (5 reviewers) clean; all 11 assertions independently verified against the regex semantics and the canary runs 3/3 green. Left for a future `tests/` sweep: test-header rot in `profile-reviews-accred-gate` / `reputation-batch-internals`.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## BACKEND-SIGNUP-ACTIVATION-FAILURE-RECOVERY-AND-POOL-HOLD (archived 2026-06-14) — crash-recoverable activation + Redis fail-closed per-token lock; round-3 (test-effectiveness) clean ✓

Reworked `/api/auth/confirm` + `/api/auth/link` activation so the irreversible `createClaimedAccount` broadcast is deduplicated under concurrency, pins NO pool connection across the ~30s broadcast (Facet 2), and fails recoverably (Facet 1). Mechanism: a per-`auth_token` Redis activation lock (`lib/signup-activation-lock.ts`) replacing the cross-broadcast pg advisory lock; encrypt-before-broadcast (key-config error → 500 before any claim-token burn); a `resumeChainExists` crash-resume that finalizes WITHOUT re-broadcasting; `409 LOCK_HELD retriable` instead of a spurious 55P03→500. Re-implemented onto refactored main (the `225d3ebe` WIP branch was infeasible against main's independent `withSignupActivationLock` refactor — branch obsolete, do not merge). Round-1 review held 7 items; round-2 (`89d0a067`+`1b6a5eff`) landed them — two P1 security items: item 1 a `STUCK_RECOVERY_WINDOW` (1h) recency guard on both stuck-recovery lookups + migration `016_accounts_updated_at.sql` (with the architect-required `COALESCE(created_at, NOW()-1day)` back-fill before SET NOT NULL, so no ~1h post-deploy bypass), item 2 fail-closed-on-Redis-down (in-memory fallback removed → retriable 503). Round-2 re-review (10 reviewers) found the 7 items code-correct but held 5 test-effectiveness/docstring items; round-3 (`f01e0aa2`) closed them and came back CLEAN, mutation-verified: deleting the `updated_at` clause flips BOTH stale-row tests red, the `/confirm` test now stubs getAccounts to pass the ownership probe (isolating the guard), the `/link` mirror drives REAL `verifyHiveSignature` (400 not 401 proves the signature verified), and the `refundStatusCodes:[409]` unit tests pin both 409-refunds AND 400-still-consumes (brute-force protection). Lock/rateLimit round-3 changes were comment-only (zero executable-token diff). ARCHITECTURE §6.1 now enumerates `updated_at` as a recency-marker overlay (architect-landed). Decision A (accept migration), Decision B (best-effort-JWT contract for /confirm) recorded.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## BACKEND-CONSENTED-ROUTE1-BRIDGE-PREDICATE-HELPER (archived 2026-06-14) — route consented_authors Route-1 bridge arm through validPevoPaperWhere; clean (proven equivalence) ✓

Centralized the `consented_authors` Route-1 bridge-account predicate (architect follow-up filed from the bridge-exclusion review). The Route-1 arm hand-rolled `(type = 'bridge_paper' AND author = $bridge)` inline; routed it through `validPevoPaperWhere({source:'bridge'})` so the bridge-identity predicate has a single source of truth (the Route-2 ORCID guard already uses the helper) — if its discriminant ever gains a third condition, both gates move together. Landed at `dbacf066` (1-line change). First review (correctness + testing + project-standards) clean: correctness PROVED the emitted SQL is semantically identical (AND-commutativity; the helper's parens around the type expr are precedence-neutral since `->`/`->>` bind tighter than `=`; no param re-allocation) and ran typecheck + consented-authors real-postgres 13/13 green; testing confirmed the Route-1 bridge self-consent pins still hold corpus-wide and per-paper; project-standards confirmed it restores `pevo-object-identity-is-author-vouching-not-metadata-claim`.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
