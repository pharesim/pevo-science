## BACKEND-COAUTHOR-TRUST-MODEL (archived 2026-05-06) — round-5 clean ✓ + 5 architect contract-doc updates + 5 ARCH.md cluster items

P1 multi-round task delivering the consent-gated multi-author trust model: explicit `author_accept` / `author_resign` `custom_json` ops as the authority for vouched authorship, per-paper claimed-vs-vouched discrimination, and a per-op fresh-auth ceremony on the custody-broadcast path so consent ops are gated by a fresh authentication challenge bound to the specific `(action, root_author, root_permlink)` target.

5 rounds across ~10 days. Round-1 (`658332a`): `AuthorAcceptAction` / `AuthorResignAction` discriminated-union types in `hive.ts`; `consent-ops.ts` module with `fetchConsentOpsForPaper` (HAF-required, signer-binding SQL per `hive-primitive-aware-design-rules-for-pevo-custom-json-ops-2026-05-05.md` rule 5) + pure `computeVouchedAuthors`. Round-1 follow-up (architect-decided): discriminated-union return shape `{status: 'ok', ops} | {status: 'haf_unavailable', reason}` so the integration site can route HAF outages to 503 (per `ARCHITECT-HAF-UNAVAILABILITY-VOUCHED-SET-POLICY`, archived 2026-05-06). Round 2 (continuation-chain admit-gate integration + cache invalidation) was deferred behind `backend-multi-author-cumulative-union.md`. Round 3 (`a28a0d0`): custody endpoint extension — `/api/custody/broadcast` enforces `fresh_auth_proof` when the bundle contains a consent op; `/api/custody/fresh-auth` (password-path issuance) and `/api/orcid/start { mode: 'fresh_auth' }` (ORCID-path issuance) mint single-use proofs bound to the JWT subject. Round 4 (`b258b6e`): hold-block fixes including dual-tier (Redis + memStore) deletion convention, `LIMIT 1000` cap on consent-op SQL, `CustodyAuditExtras` correlated-options-discriminated-union shape, mutation-kill divergent-guards setup. Round 5 (`d820787`) closed the round-4 hold cluster's 9 items: symmetric dual-tier deletion (compensating-del on memStore-fallback success), SQL signer-filter via claimed-set IN clause (de-vouch-spam defense), per-op fresh-auth target binding via length-prefixed SHA-256 content hash + new `target_mismatch` rejection reason, ORCID fetch timeout discipline (AbortController + 10s default + new `ORCID_PROVIDER_TIMEOUT` 504), `CustodyAuditExtras` phantom-arm collapse, deletion of misnamed bridge-paper exclusion test, mutation-kill setup tightening, `it.skipIf` for Redis-availability tests with module-scope `redis.status === 'ready'` poll, stale citation cleanup.

**Architect round-5 review** invoked `/ce-code-review` on commit `d820787` with 11 personas (`ce-agent-native-reviewer` skipped per project CLAUDE.md). All 9 round-5 hold items verified correctly addressed. 12 new findings surfaced; 5 landed at archive (architect-owned doc updates; this entry), 7 dismissed in triage (operational unlikelihood, defensive-code rot risk bounded, untested defensive branches currently unreachable from normal flow, polish items not worth a round-6 cycle).

**5 architect-owned contract-doc updates landed at archive:**
- `agents/docs/api-contracts/custody.md` `FRESH_AUTH_REQUIRED` 403 — `target_mismatch` added to the closed enum on `details.reason`; consumer-branching guidance updated; the round-3 prose "missing, expired, malformed, or bound to a different user" extended with "or bound to a different consent target."
- `agents/docs/api-contracts/custody.md` `POST /api/custody/fresh-auth` body — three newly-required fields (`action`, `root_author`, `root_permlink`) documented; `VALIDATION_ERROR` (400) bullet expanded to enumerate the new validation predicates.
- `agents/docs/api-contracts/orcid.md` `POST /api/orcid/start` body — conditional fresh_auth target fields documented (REQUIRED on `mode === "fresh_auth"`, IGNORED on other modes); state map shape extended with the new `fresh_auth_target` field that `/callback` reads back to bind the issued proof.
- `agents/docs/api-contracts/orcid.md` Errors (all modes) — new `ORCID_PROVIDER_TIMEOUT` (504) row documented with full `details: {retriable: false, outcome: 'timeout', verify_before_retry: true}` shape; distinction from `BROADCAST_TIMEOUT` (Hive-broadcast leg) called out; "treat retriable: false on a 504 as restart the OAuth flow, not transient gateway error" guidance added.
- `agents/docs/api-contracts/orcid.md` `fresh_auth` Errors — new 400 `BAD_REQUEST` row for the defensive corrupt-state guard at `/callback` when `fresh_auth_target` is absent from the Redis state map.

**5 ARCH.md cluster updates landed at archive** (carried forward from rounds 3-5 hold blocks):
- `Author Accept (custom_json)` and `Author Resign (custom_json)` schema snippets — `type:` → `action:` to match the universal PEvO `custom_json` discriminator convention (every other op uses `action:`; broadcast emission and parsing key on `payload.action`).
- `Author Accept` validity prose — reworded to reflect the implicit signer-IS-accepter binding (the payload carries no `accepting_author_hive` field; signer identity = subject identity by construction). Same shape applied to `Author Resign`.
- Same-block tie-break primitive — `(block_num, trx_in_block)` → `(block_num, id)` when querying `hafsql.operation_custom_json_view`. The HAF view does NOT project `trx_in_block`; `id` is a bigint encoding `block_num + trx_in_block + op_in_trx`, so ordering by `id` within a fixed `block_num` is equivalent. Cache-invalidation prose at the same section also flipped from `type` to `action`.
- `Light-account signing of consent ops` section — fresh-auth issuance endpoints cross-referenced explicitly (`POST /api/custody/fresh-auth` for password path, `POST /api/orcid/start { mode: "fresh_auth" }` + `/callback` for ORCID path), with the per-op target-binding semantic and the cross-paper/cross-action substitution rejection rule.
- `Light-account signing of consent ops` section — operational note added on `custody_audit_log` columns: standard custody columns plus four consent-op-specific columns (`auth_mechanism`, `fresh_auth_outcome`, `session_id`, `user_agent`) populated only when fresh-auth was required; PII annotation on `user_agent` per GDPR/CNPD with the deletion-transaction discipline.

**7 reviewer findings dismissed during triage** (file:line at top of the dismissed list, reason after): orcid.ts:215-245 body-read-phase timeout gap (operational unlikelihood; ORCID load balancer dribbling bytes is not a realistic failure mode); orcid.ts:311-336 untested fresh-auth-target validation branches (TS narrowing + Zod parse close at compile time); orcid.test.ts:3021-3068 10s wall-clock per-test (~20s CI cost not worth a round-6 cycle); orcid.ts:234 `init.signal` overwrite vs docstring (latent, no current caller); fresh-auth.ts:36-46 module docstring drift vs new 3-arg signatures (function-level docs correct); custody.ts:96-154 `findConsentOpsInBundle` mislabeled-backstop comment (actual SQL exact-match backstop is correct today); orcid.ts:499-512 architect-defensive 400 untested (currently unreachable from any normal client flow).

**Round 2 status:** still gated on `backend-multi-author-cumulative-union.md` (currently in `tasks/blocked/`) per round-1's signal block. Round 4 (migration-day flag) is gated on Round 2.

**Cross-references:** `agents/docs/solutions/conventions/hive-primitive-aware-design-rules-for-pevo-custom-json-ops-2026-05-05.md` (rules 5/6 land on the consent-ops SQL); `agents/docs/solutions/conventions/hive-signature-request-binding-shape-2026-04-21.md` (precedent for the length-prefixed content-hash domain separator); `agents/docs/solutions/conventions/correlated-options-discriminated-union-2026-04-28.md` (CustodyAuditExtras shape preservation); `agents/docs/solutions/conventions/chain-write-timeout-ambiguous-outcome-2026-04-22.md` (ORCID 504 + `verify_before_retry: true`); `agents/docs/solutions/conventions/tests-must-fail-on-mutation-of-code-under-test-2026-04-22.md` (round-5 mutation-kill attestation in the round-5 signal block); `ARCHITECT-HAF-UNAVAILABILITY-VOUCHED-SET-POLICY` (archived 2026-05-06) for the fail-closed posture on the consent-ops admit gate.

Full history in git: round-1..round-5 commits `658332a`, `b258b6e`, `a28a0d0`, `d820787` plus all hold-block transitions. Round 5 hold block authored at `af88cc6`.

---

## ARCHITECT-HAF-UNAVAILABILITY-VOUCHED-SET-POLICY (archived 2026-05-06) — fail-closed with surgical minimization

P1 architect-design task. Decided fail-open vs fail-closed for the consent-ops admit gate when HAF is unavailable. Resolution: **fail-closed**, matching PEvO's existing security-critical HAF-required reads (`backend/src/routes/orcid.ts:1502-1506` "Fail closed when HAF is unavailable: returning null would silently bypass...", `backend/src/routes/claims.ts:35,75`) and the discriminated-union return shape from `backend/src/routes/bridge.ts:161-177`.

**Decision shape:**
- `fetchConsentOpsForPaper` returns `{status: 'ok', ops}` | `{status: 'haf_unavailable', reason: 'no_pool' | 'query_failed'}`. `getVouchedAuthors` propagates the same union upward. The pure `computeVouchedAuthors` is unchanged. Filed as a [TODO Backend] block in `backend-coauthor-trust-model.md` Round 1 follow-up section so the round-1 implementer lands the signature change before Round 2 picks up.
- Round 2 integration site in `papers.ts` short-circuits the HAF fetch when the consent flow is inert: single-author claimed-set (root broadcaster implicit-vouched per ARCH.md rule 1) and bridge papers (per ARCH.md "Bridge papers" subsection). Multi-author chains call `getVouchedAuthors`; on `'haf_unavailable'` the route returns 503 INTERNAL_ERROR via `sendError`. Same posture applies to `/api/me/authorships/pending` (sibling task `backend-notification-infra-for-consent-ops`).
- Operator signal: per-request structured pino log on every fail-closed firing (event name unchanged from Round 1's `consent_ops.fetch_failed` OR renamed to `consent_ops.haf_unavailable` — implementer's call, one stable name on both `no_pool` and `query_failed` branches). Sampling is the log pipeline's job, not the code's.

**Why fail-closed:** the alternative (fail-open / degrade to root-only) would silently demote legitimate co-author continuations BELOW the cumulative-union claimed-set baseline currently in production AND create an attacker-attractive bypass window during HAF flaps. The "chain is SSoT" stance settled in `feedback_dont_relitigate_settled_ssot` is binding here: if we can't read the chain, we don't answer the security question. Single-author papers (overwhelming majority in beta) and bridge papers don't hit the HAF fetch at all under the surgical-minimization integration shape, so the fail-closed surface is bounded to genuinely multi-author papers — small in the beta cohort.

**Files updated at archive:**
- `agents/docs/ARCHITECTURE.md` Section 2 "Vouched-set computation (Phase 2 constraints)" — appended a paragraph after the four constraint bullets stating the fail-closed posture, the integration-site short-circuit guards, the helper return-type constraint, and the operator signal. References `verifyOrcidBinding` and `resolveContinuationChain` chain-walk SQL as posture-matching precedents.
- `agents/docs/tasks/review/backend-coauthor-trust-model.md` Round 1 follow-up — new `### [TODO Backend]` block specifying the helper signature change with the discriminated-union shape, the bridge.ts precedent, and the test additions.
- `agents/docs/tasks/review/backend-coauthor-trust-model.md` Round 2 plan — appended a "HAF unavailability — fail closed" subsection enumerating the two short-circuit guards (single-author / bridge), the 503 path on `'haf_unavailable'`, and the test scenarios for fail-closed paths.
- `agents/docs/tasks/blocked/backend-multi-author-cumulative-union.md` Cross-references — pointed at the new ARCH.md policy. Cumulative-union is HAF-required by construction (the per-hop chain-walk SQL fails-closed implicitly when HAF throws); the consent-ops layer now matches that posture explicitly. No code change needed in cumulative-union itself.

**Coordination:** the `backend-coauthor-trust-model` file moved from `pending/` to `review/` mid-session (round-4 implementer commit `b258b6e`) while this architect task was in flight. The doc edits landed at the new `review/` path; the next architect re-review on round-4 will see the [TODO Backend] block as a new outstanding item not yet addressed by round-4's commits, and either fold it into a held-pending-fix block or accept it as a separate-round follow-up.

---

## BE-VERIFY-BROADCAST-ATTEMPTS-CAP (archived 2026-05-06) — round-4 clean ✓ + 9 in-place cleanups + 1 backend follow-up + 3 contract-doc additions

P1 reliability task: bound the broadcast-retry amplification on `/api/accreditation/verify` 504. 4 rounds across ~9 days. Round-1 landed the per-token Redis-counter cap with atomic Lua INCR + first-write EXPIRE; round-2 added a distinct error code `BROADCAST_ATTEMPT_LIMIT_EXCEEDED`, decrement-on-timeout, env-var cap (`VERIFY_BROADCAST_ATTEMPTS_CAP`), concurrent-retry test, structured event field, and tightened the Lua to a single round-trip. Round-3 hardened: token redaction in operator logs (`hashTokenForLogs`, sha256 → 12 hex chars), soft-block on cap-exceeded (do NOT delete token; the legitimate user can wait out the cap-counter TTL rather than burning a `/request` slot), loud Redis-required throws across all 7 cap specs, `__test_seams` export pattern (mirroring the `routes/orcid.ts` precedent), Reliability-R2 disclosure (in-memory fallback when Redis flaps mid-request, with a structured `accred_verify_broadcast_decrement_redis_unavailable` warn for operator visibility), and pre-INCR 503 envelope discipline (`{retriable:true}` on Redis failure before broadcast).

Round-4 added: wrapped pre-INCR 503 try/catch test coverage; Reliability-R2 production fix as a symmetric `accred_verify_broadcast_increment_redis_unavailable` warn so operators see BOTH increment and decrement signals during a flap window; `as const` on `__test_seams` exports in BOTH `accreditation.ts` AND the precedent `orcid.ts` (mutation-locks the seam shape against test-time accidental rewrites); Lua constant rename `INCR_AND_EXPIRE_IF_FIRST_LUA` → `INCR_AND_EXPIRE_ON_ZERO_TO_ONE_LUA` (the new name reflects the actual `count==0 → count==1` invariant rather than the corrected-away "first-write only" framing); `hashTokenForLogs` parity unit specs (deterministic, no-collision, lowercase-hex shape, no normalization, no substring leak); `token_hash` in cap-exceeded warn payload for operator-correlation handle without raw-token exposure.

**Architect round-4 review** invoked `/ce-code-review` on commit `bed4f1f` with 10 personas (`ce-agent-native-reviewer` skipped per project CLAUDE.md). The 10 round-4 hold items mechanically applied; substantive Reliability-R2 production fix verified sound.

The headline P1 question (whether the deferred pino-redact task's plan would actually flip δ's "intentional fail-red" tests to green when it lands) was investigated and dissolved: the deferred task `backend-bridge-key-startup-validation-and-pino-redact.md` already has a re-open block (2026-05-04) acknowledging that wave-1's pino `serializers.err` fires AT WRITE TIME and the spy intercepts BEFORE pino. The wave-2 plan is a `logger` wrapper layer that runs `redactErrSerializer` BEFORE delegating to pino, which makes `vi.spyOn(logger, 'warn').mock.calls` see scrubbed args. The fail-red strategy is structurally sound for that wave-2 plan.

8 P2/P3 in-place cleanups landed at archive (architect cleanup commit, `[skip-zone-audit]` for backend zone): F2 + F3 + F11 stripped task-coupled comment prefixes that round-4 expanded rather than scrubbed (`__test_seams` block, Reliability-R2 production warn, `hashTokenForLogs` describe block); F6 + F7 + F8 fixed test docstring drift across 3 specs (round-4 hold #3b name + comment, round-3 hold #5 prefix-claim, round-4 hold #3c short-circuit-vs-falls-through); F9 strengthened a `toHaveBeenCalled()` to `toHaveBeenCalledTimes(1)` per the inline comment claim; F12 reflowed a line-break artifact in the cap-scope WHY comment. F15 fixed a missing trailing colon on the round-4 backend signal-block header per `agents/backend/CLAUDE.md`'s prescribed form.

1 backend follow-up filed: `backend-decrerr-narrowing-symmetric.md` for the pre-existing `decrErr` outer-catch `unknown` narrowing asymmetry that became visible alongside item 8b's `incrErr` narrowing in the same file.

3 reviewer findings dismissed: F10 (`memoryBroadcastAttempts` test cleanup discipline — fix would widen test-only seam for zero functional benefit; module-private map + random tokens + vitest module reload between files = no practical impact), F13 (12-hex hash birthday-bound at 2^24 — already documented in the helper docstring with widen-when-needed guidance; premature for beta scale), F14 (cap-exceeded `attemptId` operator-correlation enhancement — `token_hash` already gives per-token correlation; per-request granularity is nice-to-have, not a defect; would require helper signature changes for full surface).

3 architect-owned contract-doc updates landed at archive: `BROADCAST_ATTEMPT_LIMIT_EXCEEDED` (502) row added to `agents/docs/api-contracts/accreditation.md` with explicit token-persists-on-soft-block + don't-burn-/request-slot guidance for SPA consumers; `SERVICE_UNAVAILABLE` (503) row added for the pre-INCR Redis-unavailable case; `agents/docs/api-contracts/common.md` third-bucket note extended to acknowledge that `details.retriable: true` may appear on non-argon2 503 envelopes (cross-endpoint convention: opaque retry hint independent of `details.reason`).

**Cross-references:** `agents/docs/solutions/conventions/chain-write-timeout-ambiguous-outcome-2026-04-22.md` Option A.2 (504 envelope used unchanged) + Option A.4 (idempotency_key durable structural fix deferred to `backend-broadcast-idempotency-cluster-followup.md`); `agents/docs/solutions/conventions/test-seams-export-shape-as-const-2026-05-04.md` (`__test_seams` shape lock — round-4 of this task is the canonical application); `agents/docs/solutions/conventions/test-mock-carve-out-clause-c-2026-05-04.md` (mock-target scope authority used to dispatch test scaffolding decisions). The `backend-bridge-key-startup-validation-and-pino-redact.md` task in `tasks/review/` carries the wave-2 work that flips δ's intentional fail-reds green; architect re-verifies the test transition at archive of that task per its TODO #2.

Full history in git: commits `f194a80` (queue + admin reset endpoint, BE-VERIFY-CAP-REDIS-FLAP-RECOVERY landing in parallel), `bed4f1f` (round-4 hold-fix landing), and the round-1/2/3 chain back through `19365d4` and earlier.

---

## BACKEND-SIGNUP-SMTP-STATUS-CODE-ORACLE (archived 2026-05-05) — accepted as residual ✓

P3 task — close the residual `/signup` 4-bin status-code differential under SMTP outage (409 duplicate / 500 sendMail throw / 200 success / 422 unaccredited domain). Filed 2026-05-04 as `[BLOCKED by Architect]` requiring two decisions: (1) close vs accept-as-residual; (2) if close, account-row semantics on sendMail throw (Shape A keep row + 200, B keep row + bg retry, C rollback + 200).

Architect decision (2026-05-05): **accept as residual**, mirroring the precedent of `backend-resend-verification-smtp-timing.md` (archived 2026-04-22). Disclosure value bounded by `signupLimiter` (10/hr/IP) plus multi-IP / multi-domain probe requirement is comparable to the resend-verification timing residual already accepted. Closing carries non-trivial semantic cost: the row-rollback question (Shape A vs B vs C) drags in design choices larger than the disclosure value justifies. If the calculus ever shifts, Shape A is the close-it shape — matches the other two routes, lets `/resend-verification` carry recovery — but today the unaccredited-domain case dominates `/signup` failures and keeping the current 500 ("retry later") is clearer than a 200-with-no-email outcome.

Convention doc `agents/docs/solutions/conventions/timing-equalization-smtp-failure-mode-oracle-2026-04-22.md` extended with an "Accepted Residuals" section enumerating the `/signup` decision and re-open conditions (signupLimiter weakened, new probe channel, account-row semantics shift) so future reviewers don't re-litigate.

Implementer = architect; archived directly without `/ce-code-review` — architect-only design decision plus convention-doc update, no implementer code diff to review.

Full history in git.

---

## ARCH-TEST-CARVE-OUT-CLAUSE-C-CLARIFY (archived 2026-05-05) — risk-class equivalence settled, mock-target scope expanded

Doc-only architect-self task filed during round-2 review of `backend-auth-smtp-status-code-oracle.md` to stop reviewers (human and persona) from relitigating the same finding on every audit. Settled two ambiguities in root `CLAUDE.md` "Running Tests" carve-out:

1. Clause (c) "the same assertion" reads as **risk-class equivalence**, not literal mirror. The real-path companion catches the same failure mode; it does NOT need to assert what the mocked test asserts. Helper-shape units and route-behavior integrations cover complementary mutation classes by design.
2. Mock-target scope under the carve-out expanded beyond `getPool()` / `getAppPool()` to cover shared pool/cache helpers (`getPool`, `getAppPool`, `getRedis`, `getHafPool`), third-party libraries non-trivial to run for real per-test (nodemailer, hive-API client, IPFS client), and observability surfaces (logger spies). Auth/permission middleware (`verifyHiveSignature` etc.) stays carved out per clause (b).

Decision and dismissed strict-mirror reading captured in `agents/docs/solutions/conventions/test-mock-carve-out-clause-c-2026-05-04.md` so future findings citing "the cited companion does not assert the same thing" are dismissable as relitigating settled convention; reviewers ask "do the two tests cover the same risk class?" instead.

Implementer = architect; archived directly without `/ce-code-review` (architect-self doc-only change, no implementer code diff to review).

Full history: see commit `ed92d82` and the task file body via git.

---

## BACKEND-BRIDGE-PAPER-AUTHOR-GATE (archived 2026-05-05) — round-4 clean ✓ (META-defense decommissioned upstream by `9bb8fec`; carry-forwards deferred to that archive)

P0 task — close the bridge_paper auth-bypass: any unaccredited Hive account could surface `APP_TAG`-tagged content on `/api/papers`, `/api/search`, `/api/disciplines`, `/api/stats`, etc., by setting `json_metadata.pevotest.type = 'bridge_paper'` — no author check. ARCHITECTURE.md already promised author-pinning to `config.hiveBridgeAccount`; the SQL did not enforce it.

Round-1 (`497795e`) tightened the gate SQL at the 3 explicitly-named sites (papers.ts:269, search.ts:87, stats.ts:55). Round-2 hold expanded scope after the adversarial reviewer's full grep audit surfaced 12 additional unguarded sites: introduced `validPevoPaperWhere(opts: { commentAlias?, appTagParam, bridgeAccountParam, source?: 'native' | 'bridge' | 'all' })` at `backend/src/hafsql.ts:223-238`, migrated all 15 sites to the helper (papers list/detail/source-routing, search papers/source-routing, stats CTE + 4 count subqueries, disciplines, comments paperExistsInHaf, bridge register both checks, reputation 3 sites, notification user_bridge_papers CTE, app sitemap), changed `isPevoBridgePaper(meta)` → `isPevoBridgePaper(meta, author)`, added the `npm run check:bridge-paper-discipline` bash lint guard with allowlist, and the canary file `bridge-paper-author-gate.test.ts` with the load-bearing `assertBridgeAuthorPin()` helper. Mutation sensitivity verified: dropping the bridge-author conjunct from `hafsql.ts:234` flipped 17/23 canary tests red across all migrated routes.

Round-3 hold (`e521a96`) hardened the META-defense: `assertBridgeAuthorPin` switched from `match()` to `matchAll()` with optional `expectedCount` to catch per-site mutations in `reputation.ts:374`/`:494` that the single-match would miss; added missing notification CTE canary; `toPaperSummary` got spoof-degradation coverage; bash guard's negative test landed; guard regex extended to single-/double-/backtick-quoted literals; two-pass regex for conjunct-order tolerance.

Round-4 hold (`b8bcc40`) addressed META-defense polish: positional regex groups → named capture groups (`(?<authorSlot>\$\d+)`) eliminating the comment-vs-code drift class for the canary helper; documented 6 runtime-equivalent bypass classes structurally (concatenation, template interpolation, Array.join, case-toggle, slice, fromCharCode); "CI guard" → "lint discipline tripwire" honest framing; missing 6th allowlist test case; `toPaperSummary` `source_type: 'native'` symmetry assertion; scratch path moved from `backend/src/__discipline_test_scratch__/` to `os.tmpdir()` with `BRIDGE_PAPER_DISCIPLINE_ROOT` env-var override (closes SIGKILL leakage); `.` escaped in allowlist regex; `spawnSync` `'utf8' as const` overload narrowing. Backend correctly deferred two `[TODO Architect]` items: convention doc enumeration + framing rename, and round-2 carry-forward `papers.md` API-contract prose.

Round-4 architect re-review (2026-05-05, 7-persona pass: correctness, testing, maintainability, project-standards, learnings, security, kieran-typescript; `ce-agent-native-reviewer` skipped per project CLAUDE.md, adversarial skipped because diff was test/scaffolding only with no new auth behavior): clean. 7 findings surfaced — 6 targeted the bash META-defense files which commit `9bb8fec` (the structural follow-up filed during round-4 as `backend-discipline-guard-pipeline-integration.md`) had already decommissioned in favor of an inline ESLint custom rule `pevo/no-bridge-paper-literal` walking ESTree nodes (Literal, TemplateLiteral, BinaryExpression, CallExpression) with constant-folding coverage for 3 of the 6 bypass classes. The 6 findings became moot at HEAD; dismissed as obsoleted by upstream replacement. Finding #7 (`assertBridgeAuthorPin` lacks direct unit test) dismissed: indirect coverage via 14 site canaries across 4 review rounds + layered defense from the new ESLint rule + no helper-internal regression in the production path.

Carry-forwards deferred to `backend-discipline-guard-pipeline-integration.md` archive (both stale once 9bb8fec's ESLint rule replaces the bash mechanism): (a) convention doc `pevo-object-identity-is-author-vouching-not-metadata-claim-2026-04-28.md` "CI guard" / "lint discipline tripwire" framing; (b) `agents/docs/api-contracts/papers.md` "Bridge paper identity guarantee" note's `npm run check:bridge-paper-discipline` reference. Both should land together in the architect commit that archives the AST-rule task.

Operational learning: re-review intake checks `git merge-base --is-ancestor <claimed-sha> main` for orphan SHA detection but does NOT explicitly check whether *subsequent* commits decommissioned the diff scope. A `git log <claimed-sha>..HEAD -- <diff-files>` probe before reviewer dispatch would have caught the upstream replacement in seconds. Worth a `/ce-compound` after the AST-rule task archives.

Full history: see commits `497795e`, `ad79602`, `3c2a2a1`, `9199312`, `e521a96`, `b8bcc40` and the task file body via git.

---

## BE-AUTH-SMTP-STATUS-CODE-ORACLE (archived 2026-05-04) — round-2 clean for original scope; cross-file audit + 2 follow-up tasks filed ✓

P1 task — close the SMTP failure-mode status-code disclosure on `/api/auth/reset-request` and `/api/auth/resend-verification` (sibling axis to SEC-LOGIN-UNKNOWN-USER-TIMING wall-time oracle). Architect chose Option C (2026-04-22): wrap `sendMail` in try/catch + always return 200 + log structured warn. Round-1 (`e6df0d7`) implemented Option C on the two named routes. Round-1 hold-fix (`76d90ed`) added (P2) symmetric `else { logger.warn }` on `/resend-verification` for SMTP-not-configured operator visibility, and (P3) `connectionTimeout: 5000 + socketTimeout: 10000` on both `createTransport` calls to bound the SMTP-handshake-stall pin. Round-2 hold (`bc4e389`) addressed the cross-reviewer finding that round-1 missed `/signup` (P1) and only added 2 of 4 timeout knobs (P2): extracted `createSmtpTransporter()` helper at `backend/src/routes/auth.ts:286-297` consolidating all 3 auth.ts call sites, added `greetingTimeout: 8000` and `dnsTimeout: 5000` to cover all sequential SMTP-failure stages, retired the round-1 inline comment that incorrectly attributed EHLO-stall bound to `socketTimeout`. Two test files: new `auth-smtp-transporter.test.ts` (4 unit tests pinning the canonical 5-field options shape via `vi.spyOn(nodemailer, 'createTransport')`) and `recover.test.ts` extension (2 integration-style tests asserting `event: 'auth.<endpoint>.smtp_not_configured'` + `route` + `emailKnown: 'known'` on the SMTP-not-configured else-branches).

Round-2 architect re-review (cluster-A walk, 2026-05-04, 9-persona pass: correctness, testing, maintainability, project-standards, learnings, security, reliability, adversarial, kieran-typescript; `ce-agent-native-reviewer` skipped per CLAUDE.md): round-2 hold items 1-5 verified landed correctly for the auth.ts scope. Cross-reviewer convergence surfaced 3 follow-up scopes that explicitly fall outside this task's original boundary, filed as new tasks rather than as round-3 hold items:

- **`backend-smtp-transporter-helper-promote-and-migrate.md` (P1, new)** — bundles the wrapping-primitive convention's repo-wide audit (`maintainability M1` conf 100 + `adversarial adv-1` conf 80 + `learnings L1`): 4 raw `nodemailer.createTransport` sites in non-auth files (`digest.ts:110`, `contact.ts:32`, `accreditation.ts:264`, `settings.ts:43`) lack the timeout knobs; `/api/contact` is unauthenticated → DoS amplifier into argon2-semaphore saturation degrading `/login`. Helper migrates from `routes/auth.ts` to `lib/smtp.ts` (Finding M2). Same task carries the docblock fixes (Finding M3+REL-01 28s vs ~10s correction, Finding REL-02 dns.lookup fallback honesty, Finding 5(c) parity-framing trim, Finding M4 task-history paragraph removal, Finding M5+KT-4 `: nodemailer.Transporter` return type), the test-shape fix (Finding T1 split bundled `toMatchObject` into 4 per-field `expect()` calls), the test header rewrite (Finding T2 clause-c risk-class framing), and the spec-fails-on-revert attestation (Finding L2 per `tests-must-fail-on-mutation-of-code-under-test-2026-04-22.md`).

- **`backend-signup-smtp-status-code-oracle.md` (P3, new, [BLOCKED by Architect])** — captures the residual `/signup` 4-bin status-code differential under SMTP outage (`adversarial adv-2` conf 75): 409 / 500 / 200 / 422 distinguishes registration state across institutional vs unaccredited domains. The original task (2026-04-22) explicitly carved `/signup` out; rate-limiter cap (10/hr/IP) plus multi-IP / multi-domain probe requirement bounds practical exploitability, comparable to the resend-verification timing residual archived 2026-04-22. Blocked on architect decision (close-vs-accept-as-residual; if close, account-row rollback semantics on sendMail throw).

- **`architect-test-carve-out-clause-c-clarify.md` (low, architect)** — clarify root `CLAUDE.md` "Running Tests" carve-out clause (c) wording (`testing T2` conf 90 + `project-standards residual` conf 45). The current "real-HAF variant of the same assertion exists" text is genuinely ambiguous: literal mirror vs risk-class equivalence; mock-target scope (pool helpers only, or extends to nodemailer / logger / IPFS / hive-API). Lean: lenient (risk-class) framing matches existing codebase practice; convention doc captures the decision via `/ce-compound`.

Confidence-gated drops at the 75 anchor: `security sec-1` (50, pre-existing round-1 nodemailer err logged at warn echoes username + server text), `adversarial adv-3` (50, per-stage timeout fingerprinting), `adversarial adv-4` (70, helper test mock shape brittleness if helper grows to call `verify()`/`close()`), `kieran-typescript KT-1/KT-2/KT-3` (50, double-cast `undefined as unknown as void` / missing eslint-disable rationale / non-narrowing non-null assertions). Cross-reviewer promotions: 3 findings to anchor 100 (M1+adv-1+L1, M3+REL-01, M5+KT-4).

---

## BE-CLAIMS-ERROR-POLISH (archived 2026-05-04) — round-4 clean ✓ (architect-applied contract-doc updates in same archive cycle)

P3 cluster-A task — surface bridge-account misconfiguration with a distinct 503 SERVICE_UNAVAILABLE rather than a misleading `INTERNAL_ERROR` fall-through. Round-1 (`1cec6df`) added pre-branch / post-authz guards on approve and revoke handlers. Round-2 hold (`67311b3`) backported `bridge.ts` register/update from 500 INTERNAL_ERROR to the same 503 + extracted `assertBridgeKeyConfigured(res): boolean` so all 4 call sites source from one constant. Round-3 hold-fix (`8240429`) reordered the revoke handler's helper guard INSIDE the admin-on-bridge branch so it fires only when the server actually needs the bridge key — a `isClaimer` self-revoke on a bridge paper with key unset previously got a misleading 503; now reaches the client-signed return path and gets 200 + operation. Net behavior matrix: claimer/post-author on bridge with key unset → 200 + client-signed; admin on bridge with key unset → 503; admin on bridge with key set → 200 + bridge-key broadcast (unchanged); admin on native → 200 + admin-key broadcast (unchanged). One new test pins the corrected `isClaimer` self-revoke cell.

Round-4 architect review (cluster-A walk, 2026-05-04, 5-persona pass: correctness, testing, maintainability, project-standards, security): clean. Behavior matrix walked across all 8 cells; new test mutation-sound (revert turns red). Architect-applied in-place fixes during archive (round-3 items #2 + #3 originally deferred to architect zone): `agents/docs/api-contracts/bridge.md` register (line 147) and update (line 189) error codes updated `INTERNAL_ERROR` → `SERVICE_UNAVAILABLE (503)`; `agents/docs/api-contracts/common.md` SERVICE_UNAVAILABLE row updated to acknowledge the bridge-key-not-configured sub-case alongside the existing argon2-capacity sub-case.

---

## BE-AUTH-RESUME-SIGNUP-TIMING-GUARD (archived 2026-05-04) — round-3 clean ✓

P2 cluster-A task — close the unknown-email timing oracle on `POST /api/auth/resume-signup` (sibling endpoint outside the SEC-LOGIN-UNKNOWN-USER-TIMING audit's original scope; ~1ms unknown-email vs ~50ms argon2.verify on confirmed-state known emails). Round-1 (`e627dcf`) applied the `await burnSentinel(password)` pattern. Round-2 hold (`9710713`) closed two architect findings: (P2) ORCID-only confirmed-state account triggered `argon2.verify(null, ...)` TypeError → ~0ms 500, a third oracle the original task missed — fixed via null-guard burn + widened `password_hash: string` → `string | null` SQL generic; (P3 elevated) parametrized the timing test across 3 scenarios (unknown-email, non-confirmed-state, ORCID-only-confirmed). The bulk of round-2's findings were sibling-site `password_hash` typing-rigor issues filed as the new audit task `backend-password-hash-null-typing-audit.md` (P1).

Round-3 hold-fix (`6988beb`) closed the round-2 P3 polish item: extracted `TIMING_ORACLE_FLOOR_MS = 35` (and `TIMING_ORACLE_CEILING_MS = 150`) from local consts in 3 test files into shared `backend/tests/support/timing-constants.ts`. Implementer found a third duplication site (`auth-concurrency.test.ts`) beyond the two listed in the hold block — instance of `wrapping-primitive-exhaustive-call-site-audit-2026-04-22.md` paying off. Round-3 architect review (cluster-A walk, 2026-05-04, 5-persona pass): clean. Pure DRY refactor; FLOOR/CEILING values byte-identical pre/post; mutation kill property preserved (a future argon2 retuning edits one file).

---

## BE-RESET-REQUEST-SHUTDOWN-ENUMERATION (archived 2026-05-04) — round-3 clean ✓

P1 cluster-A task — close the `/reset-request` email-enumeration oracle that opens during SIGTERM drain. Pre-fix during the drain window (up to 30s per rolling deploy), the unknown-email branch's `burnSentinel` threw `ShuttingDownError` from the drained argon2 semaphore → outer catch translated to 503, while the known-email branch (no argon2) returned 200. Status-code differential = direct email enumeration via paired probes during a predictable operator-known window. Architect chose Option A (catch ShuttingDownError on the unknown-email branch, fall through to `sendOk` 200) over Option B (pre-flight `isShuttingDown()` + symmetric 503). Round-1 (`c72cefe`) implemented Option A with `if (!(err instanceof ShuttingDownError)) throw err;` so saturation, abort, and generic Errors all propagate correctly.

Round-1 hold (`f77291b`) closed: (P2) extracted duplicated success-message literal to module-scoped `RESET_REQUEST_OK_MESSAGE` so wording-drift cannot silently re-open the oracle; (P3) added `logger.debug({ event: 'reset_request_drain_suppression', email_hash: hashEmailForLogs(normalizedEmail) }, ...)` for operator visibility (debug-level, default-off); (P3) cross-linked the catch comment to `agents/docs/solutions/conventions/timing-equalization-sub-branch-oracles-2026-04-21.md`. Round-2 hold (`e43cf1b`) closed three test-side items: (P2) `vi.spyOn(logger, 'debug')` assertion pinning the drain-suppression call shape — kills mutations that delete the log call, rename the discriminator, drop `email_hash`, or change the level; (P3) tests now import `RESET_REQUEST_OK_MESSAGE` so wording-drift between either production branch and either test fails loudly; (P3) extended catch-comment cross-reference to also cite `timing-equalization-smtp-failure-mode-oracle-2026-04-22.md`.

Round-3 architect review (cluster-A walk, 2026-05-04, 5-persona pass: correctness, testing, maintainability, project-standards, learnings): clean. All 3 round-2 hold items verified landed correctly. Note: the legacy event tag `reset_request_drain_suppression` was renamed to `auth.reset_request.drain_suppression` by the subsequent commit `153605c` (auth-structured-log convergence); test was updated to match in the same commit.

---

## BACKEND-ARGON2-TIMEOUT-CLASSIFIER-SUB-BRANCH-COVERAGE (archived 2026-05-04) — round-1 clean ✓

P3 follow-up filed 2026-05-04 from cluster A `/ce-code-review` of `backend-argon2-error-routes-test-coverage` round-4 (testing + correctness personas, conf 65-75). The round-4 self-test landed `outcome.kind` branch coverage cleanly, but the timeout classifier inside `assertArgon2AbortIsSilentImpl` has a 3-way OR (`code === 'ECONNABORTED'` || numeric `timeout` field || `/Timeout/i` on message) and the existing `timeoutRejection()` factory set all 3 simultaneously — a drop-2-arms mutation false-passed via the surviving arm. Round-1 (`52b8064`) added 3 sub-cases to `backend/tests/support/argon2-error-mocks.test.ts` each driving the timeout-with-mock-called-once happy path with exactly one OR-arm of the classifier active (`code: 'ECONNABORTED'` only with non-/Timeout/i message; numeric `timeout: 250` only with no code; bare `new Error('Operation Timeout exceeded')` with no code or numeric field). Per `agents/docs/solutions/conventions/tests-must-fail-on-mutation-of-code-under-test-2026-04-22.md`, each arm verified by reverting (commenting out) the corresponding arm in the helper and confirming the matching sub-case turned red — clean run = 7 passed; per-arm mutated run = 1 failed | 6 passed. Mutations reverted before commit; helper file unchanged from main.

Round-1 architect review (cluster A walk, 2026-05-04, 6-persona pass: correctness, testing, maintainability, project-standards, kieran-typescript, learnings): clean. Each sub-case verified arm-isolated via classifier algebra; each calls `mockFn()` once before the assertion (satisfying the round-3 invocation-guard requirement); fixtures avoid coupling to supertest's exact internal text. The new sub-cases extend the convention `agents/docs/solutions/conventions/timing-equalization-sub-branch-oracles-2026-04-21.md` from production code to a test helper's own internal classifier — first such application in the repo. Architect-tracked forward observation (not a finding): if a fourth helper grows a multi-arm classifier-OR, the same enumeration discipline applies — could become a `/ce-compound` candidate as "test-helper sub-branch enumeration" (extension of timing-equalization-sub-branch-oracles).

## BACKEND-ARGON2-ROUTE-MOCK-KIT-BIND-HELPERS (archived 2026-05-04) — round-3 clean ✓ (architect-applied micro-fix in same archive cycle)

Pre-bind the 4 (later 3) assertion helpers (`assertArgon2AbortIsSilent`, `assert503`, `assert503QueueFull`, `assert503Shutdown`) onto `Argon2RouteMockKit` so callers cannot drop the round-3 invocation guard by forgetting the second-arg threading. Round-1 (`d217720`) extended the kit with bound methods (each method captures the kit-local `mockFn` at build time so the silent-return invocation guard is structural, not per-site discipline); migrated all 7 caller test files (8 `assertArgon2AbortIsSilent(...)` call sites collapsed to single-arg form); renamed standalone `assertArgon2AbortIsSilent` → `assertArgon2AbortIsSilentImpl` (still exported for the round-4 self-test); made `assert503` module-internal as `assert503Impl`; removed `assert503QueueFull` / `assert503Shutdown` standalone exports. Round-2 hold-fix (`19a7d0c`) closed two architect items: (P2) merged the orphaned 35-line JSDoc rationale block above `assertArgon2AbortIsSilentImpl` into the consolidated "Internal implementation, public API is the kit-bound method..." paragraph so TS/TypeDoc/VSCode hover surfaces the full rationale (round-1 hold item 4 outcome-introspection + round-3 invocation-guard fix-in-helper + round-4 exact-count `toHaveBeenCalledTimes(1)` assumption); (P3) dropped the 3-arg `assert503` field from the `Argon2RouteMockKit` interface + builder return literal + file-header JSDoc list ("four assertion helpers" → "three"), with the speculative "future cross-branch identity check" framing replaced by the present-day constant-pairing-correctness payoff (closures forward to `assert503Impl` with the right retry-after constant + reason discriminator pre-pinned, so call sites cannot accidentally pair the queue-full retry window with the shutdown reason or vice versa).

Round-2 architect review (cluster A walk, 2026-05-04, 6-persona pass: correctness, testing, maintainability, project-standards, kieran-typescript, learnings): one P2/100 finding from the maintainability persona — `assert503Impl` naming asymmetry. The file establishes `*Impl` as the suffix for functions exported for direct self-test consumption (per the JSDoc on `assertArgon2AbortIsSilentImpl`), but `assert503Impl` carried the same suffix without being exported (commit 19a7d0c made it module-internal as part of dropping the dead 3-arg public surface). Architect applied the rename `assert503Impl` → `assert503` in-place during archive triage (the previously-deleted public name was gone; no collision); single-zone backend commit (`<architect-applied-rename-sha>`) updated the function declaration + 2 call sites in the wrappers + 1 JSDoc reference. `tsc --noEmit` clean; no other consumers in tree. The kit-bind closure-capture pattern was already captured as a convention at commit `4427166` (`agents/docs/solutions/conventions/test-helper-closure-capture-over-arg-threading-2026-05-04.md`); architect refresh applied during the same archive cycle to drop the dead `assert503: ...` line from the doc's two example blocks (lines 66, 148) and rename `assert503Impl` → `assert503` in the surviving wrapper closures, plus replaced stale `signup-verify.ts:145` line refs in `agents/docs/solutions/conventions/ts-closure-denarrowing-nullable-property-hoist-2026-05-04.md` (now drift-stale by 24 lines after the round-2 hoist comment expansion at task `backend-password-hash-null-typing-audit`).

## BE-BROADCAST-CLUSTER-P3-SWEEP (archived 2026-05-04) — round-2 clean ✓

Four P3 polish items across the round-3 broadcast-timeout fixes (filed 2026-04-28 from BE-ORCID-BROADCAST-ABORT-TIMEOUT round-3 archive review). Round-1 (`ad6f4fe`) applied: stale section-header comment in `claims.test.ts:387` refreshed to reference `agents/docs/api-contracts/common.md` + `TIMEOUT_DETAILS`, `retract.test.ts:881` matcher widened from `not.toHaveBeenCalledWith` to `not.toHaveBeenCalled()` to catch unrelated future cache-invalidation calls in the timeout catch path, claims.ts revoke timeout-message strings aligned across bridge + admin signers (operator gets the signer from the structured log field `signer:'bridge'|'admin'`), 504 envelope field-ordering canonicalized via the helper's `Record<string, unknown>` insertion-order build (`{retriable, outcome, verify_before_retry, timeout_ms, verify_location?}` — required-then-optional aesthetic, not a positional contract). Round-2 hold-fix (`0bc43ae`) reframed the `broadcast-error.ts` WHY comment to honestly describe the source-readability convention (no consumer-facing positional-read contract claim; `forceAmbiguousOutcome` branch exempted because it intentionally omits `timeout_ms`), collapsed the duplicated rationale in `orcid.test.ts:1632` to a one-line back-reference to the source comment (the second duplicated site at ~1432 was incidentally removed by sibling commits between the round-1 review point and current main), and trimmed the trailing forward-reference to the upcoming helper-extraction task from `claims.ts:330` (load-bearing rationale stands without it).

Round-2 architect review (cluster B walk, 2026-05-04, 5-persona pass: correctness, testing, maintainability, project-standards, learnings): clean. One P3 finding surfaced (back-reference anchor in `orcid.test.ts:1633` reads "see broadcast-error.ts timer-fire 504 envelope build" — informal English, not grep-stable if extracted into a named helper) — dismissed during architect triage as cosmetic; back-reference works today and the "extracted into helper" risk is hypothetical. Architect followup A1 (canonical 504 envelope key order undocumented in `common.md`; `orcid.md:197` field-list described as divergent) resolved at archive with the source-readability stance: no contract claim added to `common.md`; `orcid.md:197` parenthetical clarification added stating "the field set is the contract; consumers MUST access by key name, not by position. The serialization order is a source-readability convention, not a wire contract." Closes the cluster-of-three rationale-comment-drift class (broadcast-error.ts:273-282 reframe + orcid.test.ts:1632 back-reference + claims.ts:330 forward-reference trim) and aligns the API-contract docs with the source convention. No `/ce-compound` invocation — the source-readability vs wire-contract distinction was already captured by the convention `chain-write-timeout-ambiguous-outcome-2026-04-22.md`'s envelope-shape definition; this round's work is a reaffirmation, not a new pattern.

## BE-ARGON2-ERROR-ROUTES-TEST-COVERAGE (archived 2026-05-04) — round-4 clean ✓

Lock the security invariant "every argon2 semaphore error is correctly translated to its HTTP response by every route" with route-level integration tests across {auth/login, /signup, /resend-verification, /reset-request, /reset, /recover, signup-verify/resume-signup, custody/upgrade, settings/set-password} × {ArgonQueueFullError, ShuttingDownError, ArgonAbortError}. Round-1 (`6c4e963` initial + `e7a5602` round-1 hold-fix items 1-6) landed 39 distinct {route × branch × error-class} cells via 5 new test files using `vi.hoisted` + `vi.importActual` to bind production class hierarchies (closing the synthetic-mock-bypass concern), with a shared `tests/support/argon2-error-mocks.ts` kit-builder + assertion helpers. Round-2 hold-fix (`c4d988e`) added `assertArgon2AbortIsSilent` coverage on the `/reset-request` unknown-email branch + the `/signup` dup-burn paths (auth.ts:401, 416), bundled with the pre-existing-test-mocks migration. Round-3 hold-fix (`5cb0fbc` after orphan-replay `718b7ed`+`cfd5a73`+`002fec1`) widened the helper signature to accept `mockRunWithArgon2Slot` as a second arg and added an exact-count `toHaveBeenCalledTimes(1)` invocation guard, closing the false-pass class where a route hung at a different unmocked `await`. Round-4 hold-fix (`53daad0`) closed the meta-coverage gap by adding a 4-case helper self-test (`backend/tests/support/argon2-error-mocks.test.ts`) regression-fencing the helper's three `outcome.kind` branches plus the round-3 invocation-guard, and added `expect(value, msg)` diagnostic strings on both expects so CI failures distinguish "route wrote response with status N" from "route hung but argon2 path was never entered".

Round-4 architect review (cluster A walk, 2026-05-04, 6-persona pass: correctness, testing, maintainability, project-standards, kieran-typescript, learnings): clean per archive; round-4 hold items 1+2 verified landed; 3 P3 polish findings surfaced (test-1 redundant assertions, awkward `n/a`-parenthetical diagnostic, timeout-classifier OR-arms tested together) — first two dismissed during architect triage as cosmetic round-N-cycle cost vs. value, third spun off as `backend-argon2-timeout-classifier-sub-branch-coverage.md` (P3 follow-up under `tests/pending/`) per the same `tests-must-fail-on-mutation-of-code-under-test` convention that motivated the round-4 self-test.

The kit-builder shape introduced here became the substrate for `backend-argon2-route-mock-kit-bind-helpers.md` (kit-binds the assertion helpers onto `Argon2RouteMockKit` to eliminate the round-3 second-arg threading); that follow-up task is in flight separately.

## BACKEND-A1-EXTEND-LOCK-MISSING-EVENT-DISCRIMINATION (archived 2026-05-01) — round-2 clean ✓

Add operational discrimination to the `binding_lock_extend_lock_missing` operator anchor on the post-broadcast cascade path. Pre-fix the anchor collapsed three distinct ops causes (self-expire from HAF lag preamble exceeding the 35s lock window / Redis eviction / sibling DEL) into one signal. Round-1 (`1aa2382`) added a `redis.pttl(lockKey)` probe immediately before `redis.expire(lockKey, HAF_INDEXING_LAG_CEILING_SECONDS)`; when `expire` returns 0, the structured `logger.error` payload now carries a 3-way `cause:` discriminator (`'expired_or_evicted'` if pttlBefore === -2, `'released_during_extend'` if pttlBefore > 0, `'unknown'` defensive default) plus the raw `pttlBefore` value for dashboard correlation. Probe is best-effort by design — if `redis.pttl` itself throws, the existing outer catch fires `event:'binding_lock_extend_threw'`. 2 specs landed at round-1: strengthened lock-absent spec asserts `cause:'expired_or_evicted'` + `pttlBefore: -2` under `objectContaining`; new race-window spec uses `vi.spyOn(redis, 'pttl').mockResolvedValueOnce(30_000)` + `vi.spyOn(redis, 'expire').mockResolvedValueOnce(0)` to drive the rare sibling-DEL race deterministically. Round-1 architect review: 5-persona pass; 2 cross-reviewer findings held — P1 (testing T01 + reliability gap convergence) on uncovered pttl-throw path, P2 (testing T02 + project-standards residual convergence) on unupdated test-header carve-out documentation. Round-2 hold-fix (`16dbbdd`, test-only) added a 3-spec pttl-throw block (×2 modes via `describe.each([accredit,link])`) using `vi.spyOn(redis, 'pttl').mockRejectedValueOnce(...)`, asserting `event:'binding_lock_extend_threw'` via `objectContaining` + negative-shape assertion against `binding_lock_extend_lock_missing` + `expireSpy.not.toHaveBeenCalled()` (round-1 best-effort-probe-runs-first invariant); extended the test-file header docblock with a paragraph enumerating all three new specs with carve-out clauses (a)/(b)/(c). Round-2 architect review: 3-persona pass (testing, project-standards, correctness), all clean.

Architect refresh applied during cluster-A walk: task body's stale `a1_extend_*` literal references updated to `binding_lock_extend_*` per the [TODO Architect] handoff from BACKEND-ORCID-LOCK-TTL-EXTEND-ON-TIMEOUT round-3 hold (which renamed the literals).

Architect-owned residual (deferred): the round-1 [TODO Architect] convention-doc paragraph in `agents/docs/solutions/conventions/chain-write-timeout-ambiguous-outcome-2026-04-22.md` documenting the `cause:` discriminator + the eviction-vs-DEL classification caveat (reliability conf 60 from round-1 review). Defer to the broader event-anchor convention rollup landing across the broadcast-error file family.

## BACKEND-HANDLE-BROADCAST-ERROR-HELPER (archived 2026-05-01) — round-5 clean ✓

Extract the duplicated `BroadcastTimeoutError` 504/502 discrimination pattern from 7 HTTP-surface catch sites into a shared `handleBroadcastError(res, err, opts)` helper in `backend/src/lib/broadcast-error.ts`. Round-1 (`0c95115`) shipped the helper + migrated all 7 sites; round-1 hold (P1+P2) closed an `accreditation.ts /verify` `deleteToken` ordering regression and a "canonical envelope" doc-claim conflict. Round-2 hold (`14d3d2e`) wrapped the deleteToken in try/catch + cleanup-failure log. Round-3 hold (`2eb2efb`) tightened the integration test to mutation-pin the (b) cleanup-failure log and (c) no-`ERR_HTTP_HEADERS_SENT` guard with `vi.spyOn(logger,'error')` + key-targeted spy + 403 state-not-consumed assertion + verifyHiveSignature-throw spec via hoisted `verifyHiveSignatureFailureToken` flag. Round-4 hold added `BroadcastTimeoutError` constructor-time guard against NaN/±Infinity/0/negative `timeoutMs` (`4d7dcd5`); round-4 architect review surfaced 5-reviewer convergence (correctness, agent-native, reliability, adversarial, security) on a P1 — the constructor RangeError throws synchronously inside the `setTimeout(() => reject(new BroadcastTimeoutError(...)))` callback BEFORE `reject()` runs, escapes as Node `uncaughtException`, crashes the worker. Round-5 hold-fix (`5258102`) moved the bound check to the WRAPPER ENTRY via new `assertFinitePositiveTimeoutMs(timeoutMs, fnName)` helper called as the first executable statement in `broadcastJsonWithTimeout` and `broadcastSendOperationsWithTimeout`. RangeError now propagates as a normal async-function Promise rejection that reaches the route catch → `handleBroadcastError`'s non-timeout branch → 502 BROADCAST_FAILED with structured `event:'broadcast_failed'` operator log. Constructor guard kept as belt-and-suspenders for future direct callers (test fixtures); never fires in production. 12 new specs (6 per wrapper) cover the input matrix (NaN, +Infinity, -Infinity, 0, negative, finite-positive control) plus `expect(broadcastSpy).not.toHaveBeenCalled()` to pin BEFORE-dhive placement. Round-5 architect review: 6-persona pass, all clean. Mutation kill verified across all defense classes.

## BACKEND-ORCID-BROADCAST-OUTCOME-DISCRIMINATION (archived 2026-05-01) — round-5 clean ✓

Discriminate "broadcast succeeded, post-broadcast write threw" from "broadcast threw" so the operator-alert pipeline routes the former to DB on-call (not broadcast on-call). Round-1 (`d8b9b75`) added `PostBroadcastWriteError` class + `currentStep` tracking idiom + `instanceof` discrimination order (PostBroadcastWriteError check fires BEFORE BroadcastTimeoutError so a wrapped timeout cause still emits 502 POST_BROADCAST_FAILED with `details.outcome:'confirmed'` + `tx_id` + `failed_step`). Per-step `postBroadcastMsgFn` for ORCID-shaped user-facing messages. Round-1 hold cycled through 9 items including the `'unknown'` failedStep removal, msg-fn-throws guard, third return-value `'post_broadcast'`, native `Error.cause` forwarding, sanitized fallback. Round-2 hold (`6044ebc`) closed 5 P2/P3 items including switch+`assertNever` exhaustiveness on per-step messages, `Extract<>` narrowing on handleLink's `currentStep`, structured `event:'post_broadcast_msg_fn_threw'` field. Round-3 hold (`5acbdf1`) closed the round-2 holds; round-3 architect surfaced 4-reviewer convergence on a P2 — `event:` literal placed BEFORE `...opts.logContext` was caller-overrideable. Round-4 hold-fix (`4d7dcd5`) repositioned `event:` AFTER the spread at both anchor sites + 2 mutation-kill specs. Round-4 architect surfaced 3-reviewer convergence on a P2 spread-after-literal asymmetry — protection applied to `event:` only; `err`/`cause`/`txId`/`failedStep` still BEFORE the spread, same caller-override mechanic. Plus a P3 sibling-anchor convention gap: 3 sibling logger sites (`broadcast timed out`, `broadcast failed on ambiguous-outcome path`, `broadcast failed`) carried no structured `event:` discriminator. Round-5 hold-fix (`a811a12`) extended spread-after-literal to all 4 source-of-truth fields at both anchor sites + added `event:'broadcast_timeout'` / `'broadcast_ambiguous'` / `'broadcast_failed'` (placed AFTER spread per the same convention) + the file's docblock now enumerates all 5 anchors with both stable suffix and structured discriminator. 5 new test specs (3 sibling-anchor pin specs via `objectContaining` + 2 spread-kill specs covering 7 source-of-truth fields). Round-5 architect review: 6-persona pass, all clean.

Architect-owned residual (deferred to separate sweep): `agents/docs/api-contracts/common.md` already documents POST_BROADCAST_FAILED row with discriminator (added during round-1 review pass); ARCHITECTURE.md §5 Operator Signals catalog gains the 5 `event:'broadcast_*'` + `'post_broadcast_*'` literals alongside the broader event-anchor sweep landing across the broadcast-error file family.

## BACKEND-ORCID-LOCK-TTL-EXTEND-ON-TIMEOUT (archived 2026-05-01) — round-4 clean ✓

A.1 implementation: extend ORCID binding lock TTL to `HAF_INDEXING_LAG_CEILING_SECONDS` (120s) on `BroadcastTimeoutError` to structurally close the duplicate-bind race. Round-1 (`81795fd`) added the skipRelease return-signal pattern (caller signals "don't release on finally"), `extendBindingLockOnTimeoutOrLog` helper called BEFORE response-write so a malicious caller terminating mid-write cannot escape before extend lands. Round-1 hold (`ddfff93`) closed 9 items including operator-anchor logging on all 4 helper branches (success, lock-missing, redis-absent, throw), `HAF_INDEXING_LAG_CEILING_SECONDS` source-of-truth aliasing for `ORCID_BINDING_CACHE_TTL`, helper extraction. Round-2 hold (`5acbdf1`) closed 5 items: ordering spec rewritten to pin `redis.expire` invocation order (pinning the contract layer, not the helper extraction), structured `event:` field literals pinned via `objectContaining` across all 4 helper branches, derivation-chain comment block on the constant, `skipRelease` decorative on lock-missing branch, call-shape assertion on `redis.expire`. Round-3 hold (`7387435`) renamed the four `event:'a1_extend_*'` literals to domain-rooted `event:'binding_lock_extend_*'` (aligns with sibling anchors `nonce_drift`, `redis_outage`, `lock_contention_held`, `post_broadcast_write_failed`) + decoupled test assertions from the bare `120` literal via `__test_seams.HAF_INDEXING_LAG_CEILING_SECONDS` export. Round-3 architect review surfaced 3-reviewer convergence (project-standards + maintainability + testing) on a P1 — two sibling cross-reference comments (`backend/src/lib/broadcast-error.ts:185` + `backend/src/routes/accreditation.ts:305`) still cited the dead `a1_extend_*` literal, breaking the rename's "survive task slug staleness" intent. Round-4 hold-fix (`a811a12`, bundled with `BACKEND-ORCID-BROADCAST-OUTCOME-DISCRIMINATION` round-5) refreshed both cross-references. Round-4 architect review: 6-persona pass, all clean.

Architect-owned residual (deferred): the implementer's [TODO Architect] from round-3 — refresh `agents/docs/tasks/pending/backend-a1-extend-lock-missing-event-discrimination.md` body to reference `binding_lock_extend_*` literals — was actioned during the cluster-A walk on that task. The convention-doc paragraph documenting Option A.1's `cause:` discriminator (per `chain-write-timeout-ambiguous-outcome-2026-04-22.md`) still pending the broader event-anchor convention rollup.

## BACKEND-CASCADE-FNS-RETHROW-PERMANENT-ERRORS (archived 2026-05-01) — round-2 clean ✓

Make `PostBroadcastWriteError` discrimination reachable on real cascade failures. Pre-fix the discrimination machinery was dead-defensive: `cacheOrcidBinding` / `updateAccountOrcid` / `seedAccreditationBonus` all swallowed async errors internally, so the per-step 502 `POST_BROADCAST_FAILED` envelope (`details.outcome:'confirmed'`, `failed_step`) never fired in production. Architect chose option B-narrow: rewire `updateAccountOrcid` + `seedAccreditationBonus` to re-throw on permanent / operator-actionable errors; leave `cacheOrcidBinding` swallowing (lazy-repopulate is the right behavior on cache write). Round-1 (`09e01e3`) added private discriminators `isPermanentDbError` (SQLSTATE classes `23*` integrity / `42*` syntax+access — re-throw; connection / serialization / pool-exhaustion — swallow) and `isPermanentSeedError` (`TypeError|SyntaxError|RangeError` — re-throw; everything else — swallow); null-Redis branch on the seed kept as silent return per the contract that ephemeral Redis health must not surface 502 to the user-visible accredit envelope. Operator anchor on the 4th log-suffix gained `event:'post_broadcast_write_failed'`. 9 new specs (5 updateAccountOrcid + 4 seedAccreditationBonus) covering permanent SQLSTATE / TypeError / transient swallow / null-pool-throws-immediately / null-Redis-silent-return. Round-2 hold-fix (`545ba77`) tightened the vacuous null-Redis spec from `if (!redis) {} ` (zero assertions under documented topology) to `vi.spyOn(redisModule, 'getRedis').mockReturnValueOnce(null)` with concrete silent-return + zero-Redis-call + zero-log-line assertions; file-header docblock added a 30-line "Mock carve-out" paragraph documenting CLAUDE.md "Running Tests" clauses (a)/(b)/(c). Round-2 architect review: clean 3-persona pass (testing, project-standards, correctness). Mutation kill verified — dropping `if (!redis) return` from `seedAccreditationBonus` would make `redis.set` throw on null receiver → `isPermanentSeedError` matches TypeError → re-thrown → spec flips red on `resolves.toBeUndefined()`. Architect-owned deferred items remain trackable: the api-contracts/orcid.md POST_BROADCAST_FAILED entry already documents the envelope and per-step recovery semantics; the ARCHITECTURE.md "Operator Signals" section gains `event:'post_broadcast_write_failed'` alongside the broader event-anchor sweep landing across the broadcast-error file family.

## ARCHITECT-COMMIT-ZONE-AUDIT-HOOK (archived 2026-04-30) — round-3 clean ✓

Add `.githooks/commit-msg` zone-audit hook + per-agent staging guidance + convention doc to backstop cross-agent commit-scope drift (motivating case: `3c2a2a1` swept architect-driven `tasks/review→pending` moves into a backend worker commit). Round-1 (`a8ffb41`) shipped the initial hook (slug-prefix discrimination + per-agent CLAUDE.md path-scoped staging + convention doc + README setup); architect /ce-code-review surfaced 18 findings centered on a P0 slug-guard ordering bug and a P1 missing test artifact. Round-2 (`38540a7`) reworked the hook with architect bypass for any-slug task files + global rename-detection pass blocking non-architect review→pending moves + 32-case test script + zone-map SSOT pivot (hook is runtime-authoritative, doc + Files-You-Own derived) + bare-prefix style mandate (rejects `fix(role):` wrappers) + `--no-verify` documentation + 5 root-meta paths added to architect zone (`.gitignore`, `.dockerignore`, `.env.example`, `deploy.sh`, `LICENSE`). Round-2 review surfaced one remaining P1: `-M50` rename-detection bypassed when content change drops similarity below 50% (file shows as A+D not R). Round-3 (`41c111e`) added a complementary deletion-detection pass for non-architect agents, plus tests for the low-similarity reproducer, Unmerged (U) filter, `[skip-zone-audit]` precedence, and `tasks/blocked/` variants (38 tests passing). Root CLAUDE.md rule #2 cross-references the hook as runtime SSOT. Round-3 architect correctness re-review zero findings.

## BACKEND-ORCID-DROPLOCKCONTENTION-RETRIABLE (archived 2026-04-30) — round-2 clean ✓

Drop the unreachable `retriable: true` discriminator from the same-tick lock-contention 409 in `withOrcidBindingLock`. Round-1 (`b1aec7e`) stripped `retriable`, `retry_after_seconds`, and the `Retry-After` header from the `'held'` branch's `sendError` call; deleted the now-unused `ORCID_BINDING_LOCK_RETRY_AFTER_SECONDS` constant; updated test assertions to the new wire shape (409 + `ORCID_ALREADY_LINKED`, no `retriable`). Round-2 hold-fix (`4eb07ed`) added a structured `logger.warn` carrying `event:'lock_contention_held'`, `orcidId`, `routeLabel` so dashboards can pivot on the anchor; rewrote handleAccredit and handleLink durable-binding 409 comments to lead with the wire-shape invariant ("All ORCID_ALREADY_LINKED 409 paths share the same wire shape: status 409, code ORCID_ALREADY_LINKED, no `retriable`, no `Retry-After`, no `retry_after_seconds`") + cross-reference to `agents/docs/api-contracts/orcid.md:185`. Round-2 architect re-review zero findings.

Companion UI work: `UI-ORCID-CALLBACK-RETRIABLE-MACHINERY-REMOVE` (archived same day) strips the dead frontend auto-retry machinery that the dropped discriminator activated.

Convention reinforced: `chain-write-timeout-ambiguous-outcome-2026-04-22.md` "Sibling principle — `retriable: true` is meaningless when state is single-use" section is the canonical rationale; this commit is its first code-side implementation.

---

## UI-ORCID-CALLBACK-RETRIABLE-MACHINERY-REMOVE (archived 2026-04-30) — clean ✓

Strip the now-unused auto-retry machinery from `frontend/src/pages/orcid-callback.js` after the backend stopped emitting `retriable: true` on the same-tick lock-contention 409 (companion `BACKEND-ORCID-DROPLOCKCONTENTION-RETRIABLE`). Removed: `_retryCount`, `MAX_RETRIES`, `retryCountdown`, `errorKind === 'alreadyLinkedRetriable'`, `_retryVerify`, `_tickRetryCountdown`, `_lastVerifyArgs`, the `orcid.alreadyLinkedRetriable` locale key from all 16 languages + `STUBS.md` cleanup. Net `-357/+24`, 5 unit tests deleted (machinery-specific) + 2 added (durable-rendering + retriable-shaped-envelope-treated-as-durable for mutation-killing). Test count `37/37` pass.

Architect re-review zero findings. One adjacent gap surfaced (frontend has no `POST_BROADCAST_FAILED` 502 handler — activated by cluster 2 task 4's cascade-fns rethrow round-1) and was filed as `ui-orcid-callback-post-broadcast-failed-handler.md` in pending/; orthogonal to this cleanup.

Convention reinforced: `chain-primitive-proxy-prefer-deletion-2026-04-28.md` — when the authoritative layer (single-use OAuth state) makes a downstream defense unreachable, decommission the consumer rather than retain it as scaffolding.

---

## BE-CLAIM-ACCOUNT-CHAIN-RECONCILE (archived 2026-04-30) — round-2 clean, superseded by R4 ✓

Reconcile DB `account_creation_tokens` count with on-chain `pending_claimed_accounts` after `BroadcastTimeoutError` during `claim_account` batch broadcast. Round-1 (`ef56eab`) added `reconcileClaimTimeout` capturing pre/post counter delta and INSERT-reconcile path. Round-1 architect hold-block (`3312e2c`) flagged 4 items including log-severity tweaks, `reconcile_outcome` enum field on the seven reconcile log lines, and inline rationale comment for the `hiveOnboardAccount` choice. Round-2 hold-fix (`2622742`) landed all 4 items: 8/11 → 11/11 tests passing.

