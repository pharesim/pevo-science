## BACKEND-PAPERS-LISTING-CORRELATED-SUBQUERIES (archived 2026-06-12) — up-to-80 correlated listing subqueries collapsed to one rev_agg LATERAL + once-per-request citation scan; round-5 re-review clean ✓

The listing's per-row scalar subqueries (reviewCount + avgRating twice-scanning the same accredited-review row set, plus the per-row jsonb citation containment over the whole corpus) collapsed to ONE rev_agg LATERAL Aggregate per page row (8c58a7c3; EXPLAIN on real HAF: a single indexed parent-pair scan computing both aggregates in one node) and a once-per-request `paper_citation_counts` corpus scan (landed via the sibling citation-count task; Subquery Scan loops=1, healthy with the CTE inlined, no MATERIALIZED pin needed). The sort=votes aggregate-CTE arm split to `backend-papers-listing-votes-aggregate-cte` (now blocked). Rounds 2-5 were successive holds on the `excludeSelfReviewWhere-callsite-canaries` header: minOccurrences 3-to-2 + envelope-field pins + canary hardening (real helper imports, same-reviewer and malformed-rating corpus rows), then a round-ordinal de-rot, then the "three"-to-"four" reputation.ts narration fix, then four stale counts (8-to-11 sites, 8-to-6 files, routes-to-modules reword, expected-to-minOccurrences) — four consecutive rounds of prose-numeral rot against the CALLSITES table. Round-5 re-review clean: all counts reconcile with the array, the real-code grep matches per-file (2+2+1+1+1+4), no fifth stale count found. The structural fix that ends the class — dropping the duplicated numerals from the header prose so the CALLSITES table is the single inventory — is filed as `backend-callsite-canary-header-numeral-derot`.

## BACKEND-REDIS-KEYS-SCAN-REPLACEMENT (archived 2026-06-12) — blocking KEYS replaced by SCAN + members-set index; 3 hold rounds; round-4 re-review clean ✓

Replaced the three blocking `redis.keys()` sites (f90d7088): the cleanup helpers SCAN iteratively; `getBatchReputationMap` reads a `batch_members` Redis Set maintained by SADD inside the CYCLE_SWAP Lua (atomic; the key deliberately sits OUTSIDE `BATCH_KEY_PREFIX` so an account literally named `members` cannot String-vs-Set collide), with a wholly-empty-index SCAN backfill. Round-1 hold (4 items, 199372b5): memory-filename docblock cite replaced with the plain single-instance invariant; the seed/revoke writers joined the index (SADD on NX-OK, SREM after DEL, `pipeline.exec()` tuple inspection); deterministic backfill pins via the gated `setBatchMembersKey` seam + its eslint `no-restricted-imports` guard; CYCLE_SWAP arity guard. Round-3 hold (3 items, 2570ef8f): `as const` on both `__test_seams`; the NX-OK SADD gate DROPPED — the conditional index write reopened an orphan window (crash between SET and SADD, then a retry's NX no-op skips the SADD on a false "already a member" assumption, and a single orphan in a populated index is invisible until the next cycle swap because the backfill heals only a wholly-empty index) — SADD is now unconditional; write-path `sismember` pins added, the orphan-window case mutation-killing the gated form. Round-4 re-review clean: adversarial's interleaving/growth/seam/isolation attacks all fail — the one new interleaving downgrades the failure class from "accredited user invisible until cycle swap" to one harmless MGET-null-skipped set member. Residuals filed separately at archive: the pre-existing `reputation-batch.ts` seam missing its `no-restricted-imports` entry; the test-header round-ordinals appended to the anchor-sweep task. The general lesson (an index maintained conditionally or by some-but-not-all writers drifts) is documented in `agents/docs/solutions/` as the index writer-completeness rule.

## BACKEND-ADMIN-BROADCAST-ORCID-SITES (archived 2026-06-12) — orcid handleAccredit/handleLink on broadcastAdminCustomJson, TOCTOU-lock-shape preserved ✓

Migrated the two orcid admin custom_json sites (handleAccredit, handleLink) to broadcastAdminCustomJson via the pre-validate-outside-the-inner-try option (commit d29f9708): a validation `PrivateKey.fromString` parse stays inside withOrcidBindingLock's fn but outside the inner try, preserving the security-tested 504-ambiguous-outcome + lock-release escape for a malformed key, while the helper call sits inside the try; the pre-lock unset-key 500-guard keeps AdminKeyNotConfiguredError unreachable. Round-1 review verified ZERO behavioral change: byte-identical envelopes across the key-state x lock-state matrix, BroadcastTimeoutError class identity across the helper boundary (lock-TTL-extend + skipRelease untouched), the validation parse mutation-killed by the SEC-002 specs, envelope literal confined to hive.ts both grep directions. Rounds 2-4 were successive holds on ONE header sentence — the carve-out clause (a) mock inventory — which proved the incremental-patch rot class live: round 2 added the broadcastAdminCustomJson seam (338eb188) but left accreditation.js getAccreditedSet and the getAccounts read stub unnamed; round 3 re-derived the full five-factory enumeration (0d102616) with an attested factory-by-factory audit, verified at round-4 re-review as accurate for four factories with anchors clean — but still naming only getPool of the db.js factory's three exports (isHafConfigured -> true, no-op closeHafPool), judged not absorbed by the "database pools" grouping given the sentence names even the hive.js constant stand-ins. That residual plus a positional "above its factory" pointer split at triage to backend-orcid-test-header-dbjs-enumeration (pending) so this task could archive. The incident chain is the canonical instance behind universal-mock-inventory-must-be-re-derived-not-incrementally-patched.

## BACKEND-IMPLEMENT-CONSENTED-AUTHORSHIP-MODEL — wire the consent model; remove auto-accept; credit the consented-set via a shared chain-aware SQL CTE (archived 2026-06-12)


**Owner:** backend
**Created:** 2026-06-05 (from the `architect-reconcile-authorship-claim-vs-vouched-tracks` brainstorm)
**Ratified:** 2026-06-09 (architect + user — the open forks below are settled; supersedes the standalone deep-plan, now removed)
**Priority:** P2 (design coherence; docs are intentionally ahead of code)

## Problem

Reputation/citation credit must flow only to co-authors who have **consented** to a paper. Today the live cycle still uses **legacy auto-accept**: a bare ORCID-or-hive match in `authors[]` confers credit with no act of consent (the ORCID + hive arms of `authorshipClaimsCteBody`). Two mechanisms exist:
- `claim_authorship` / `approve_authorship` / `revoke_authorship` (LIVE) — credit via `accepted_claims` (`reputation.ts`) + `authorshipClaimsCteBody` (`hafsql.ts`), with ORCID/hive **auto-accept**.
- `author_accept` / `author_resign` (INERT) — `computeVouchedAuthors` / `fetchConsentOpsForPaper` in `consent-ops.ts`, keyed on `authors[].hive`, wired into no gate.

The docs (`ARCHITECTURE.md` § 2, `hive-schemas.md` § 2.9–2.11, `reputation-algorithm.md` "Co-author Credit") describe the unified model; the code does not yet implement it.

## Decided model (see `ARCHITECTURE.md` § 2 "Consented vs claimed authorship")

A claimed author (named in `authors[]` at the root post or via a continuation revision) becomes **consented** — credited + badged — via one of:
- **Route 1 — root broadcaster:** implicit consent via the post signature.
- **Route 2 — anchored slot:** the co-author broadcasts `author_accept`. Eligibility anchor = `slot.hive == signer` OR `slot.orcid ==` the signer's authority-attested ORCID.
- **Route 3 — name-only slot (no `hive`/`orcid`):** `claim_authorship` + the author/admin's `approve_authorship`.

There is **no metadata auto-accept**: an ORCID/hive match only gates *who may consent*, never credit. Demotion: `author_resign` (anchored self) / claimer self-`revoke_authorship` (name-only) + the author/admin `revoke` backstop (either route). **No data migration / no flag-day** — nothing live uses these ops yet.

## Ratified decisions (2026-06-09) — settled, do not re-litigate

1. **Credited-set lives in SQL (Option A).** One shared lean recursive CTE computes the credited *set* — which slots are credited per paper. BOTH the reputation cycle and the read surfaces consume it: single source of truth for *who is credited* (the no-cycle-vs-read-drift outcome `reputation-claims-cte-dedup` established for claims). Display polish — name resolution, the 5-branch ORCID server-override, audit emission — stays in JS on top (audit emission is side-effectful and cannot move to SQL). SQL owns membership; JS owns presentation.

2. **Full cumulative chain, not root-slot only.** A `WITH RECURSIVE` walk over the `continues` pointer chain assembles each root paper's cumulative `authors[]` union across ALL revisions, threading the cumulative-author admission gate down the recursion (mirroring the JS `resolveContinuationChain` rule: a continuation post is admitted only if its author is in the cumulative set built from prior hops). Per slot it emits `{hive, orcid, name, first-appearance block}`. Rationale: a co-author named in a later revision contributed to that version and must earn credit. **Today the cycle has ZERO chain-awareness** — `user_papers` filters `(continues) IS NULL` and every `authorshipClaimsCteBody` arm resolves against the root post only — so this CTE is foundational new infrastructure that all three credit routes sit on.

3. **Retroactivity invariant — consent gates MEMBERSHIP, never the vote window.** Once an author becomes consented, the next cycle credits them the paper's FULL vote/review history, including votes/reviews from BEFORE their accept. This is free under the full-recompute cycle (scores are recomputed from scratch each cycle; `prevScores` only weights voters, it is not a per-author accumulator). The only way to break it is to add a `vote.block_num > accept.block_num` window — DO NOT. Rule 6 (accept block must be > the slot's first-appearance block) gates accept *validity* (anti-name-squatting); it does NOT gate which votes count.

4. **Route-3 name-only claims resolve against the cumulative chain — NO op-schema change.** The `claim_authorship` op already carries root `paper_author`/`paper_permlink` + `author_index`, and `hive-schemas.md` § 2.10 already defines the author list as the cumulative union across the chain. The gap is code-only: `authorshipClaimsCteBody`'s list-final gate resolves `author_index` against the ROOT post only. Fix: resolve `author_index` against the cumulative-chain union (the CTE from #2). A name-only author who first appears in a continuation can then claim + approve + be credited. No on-chain format change.

## Credit composition (directional, not a spec)

```
credited(paper) =
    Route 1: root broadcaster
  ∪ Route 2: author_accept where signer == slot.hive OR signer's attested-ORCID == slot.orcid,
             Rule 6 (accept.block > slot first-appearance block), latest-op-wins (accept vs resign)
  ∪ Route 3: claim + approve(author|bridge) where author_index resolves to a NAME-ONLY slot
             (no hive/orcid anchor) in the CUMULATIVE chain union, latest-op-wins, revoke override
  \ demotions: author_resign / self-revoke / author-admin revoke (latest op per (author, paper))
```

Self-credit exclusion (consenter == post author) and the credited-claimer self-vote/self-review exclusion (from `co-author-claim-zero-score` Item 2) apply to the WHOLE credited set, not just Route-3 claimers. Credit is keyed to on-chain post identity (`chain_papers` dedup); credit = the same full paper score as the poster (shared, not divided).

## Implementation plan (ordered; U1 before U2 is mandatory)

### U1 — Shared consented-set resolution (rename vouched→consented; ORCID anchor; the recursive chain CTE)
- `consent-ops.ts`: rename `computeVouchedAuthors`→`computeConsentedAuthors`, `getVouchedAuthors`→`getConsentedAuthors`, and all sense-3 "vouched" symbols → "consented" (per `vouch-three-senses-consented-not-vouched`; leave the WoT-accreditation and object-vouching senses). Extend eligibility: a slot anchors on `hive == signer` OR `orcid == attested_orcid(signer)`, attested-ORCID resolved **latest-action-wins** over `active_accreditations` (NOT strict existence; a null attested ORCID against an orcid-anchored slot ⇒ NOT eligible / suppress). Source the consented identity `(name, orcid, hive)` atomically from one attestation, never per-field. Keep Rules 2 (`(block_num, id)` order), 5 (signer == `required_posting_auths[0]`), 6 (temporal lower bound).
- `hafsql.ts`: new shared CTE (e.g. `consentedAuthorsCteBody`) — the `WITH RECURSIVE` chain walk (decision #2) emitting per-paper cumulative slots with first-appearance block, then the Route-2 consent resolution over it.
- KTD-3 fail-closed: change the fetch's `[]`/root-only return on `!pool` to a discriminated union `{ status: 'ok', ops } | { status: 'haf_unavailable' }` (mirror the `read-then-write-races` `assertNever` shape). Never cache the failure sentinel.
- Characterization-first: pin current `computeVouchedAuthors` (hive-only) before extending so the ORCID anchor is provably additive.
- Audit the SQL `BTRIM` (U+0020 only) vs JS `.trim()` (full Unicode) asymmetry on any new anchor-equality predicate (`sql-trim-vs-js-trim`).
- **Tests:** hive-anchored accept → consented (characterized); orcid-anchored accept w/ matching *attested* ORCID → consented; broadcaster-claimed-only ORCID → suppress; null attested ORCID on orcid-anchored slot → not consented; attested-then-revoked → not consented; accept block ≤ first-appearance → rejected (Rule 6); accept→resign→accept latest-wins; HAF down → `haf_unavailable` (not empty set); a continuation-added anchored slot is resolvable.

### U2 — Remove the ORCID + hive auto-accept arms; make Route-3 chain-aware
- **Depends on U1** (Route-2 credit must exist before its old auto-accept path is removed — avoid a no-credit window).
- `hafsql.ts` `authorshipClaimsCteBody`: delete the ORCID arm and the hive arm from the status CASE; keep the revoked arm + the explicit-approval arm. Add the name-only constraint (the approval arm credits only when the slot carries NO `hive`/`orcid` anchor — anchored slots go through Route 2). **Resolve `author_index` against the cumulative-chain union, not root-only** (decision #4).
- The shared builder is already landed (`reputation-claims-cte-dedup` collapsed the cycle's inline copy onto `authorshipClaimsCteBody`), so the arms live in ONE place — remove there.
- **KTD-4 blast-radius audit** — removing the arms flips `accepted` vs `pending` for all ~13 `authorshipClaimsCteBody` consumers at once. Two land-mines:
  - **`isApprovedCoAuthor` authz** (`claims.ts`) gates bridge-key co-author broadcasts. An ORCID-auto-accepted co-author LOSES co-sign authority once the arm is removed — a *behavior change*, not just a credit change. Pin it with a test and surface it.
  - **JS ORCID-supersession mirror** (`author-supersession.ts`, shared `CHAIN_ORCID_BTRIM_CHARSET`) mirrors the SQL ORCID arm. Audit/retire it so the four-surface invariant isn't split.
- Invert the existing auto-accept tests (`reputation-orcid-auto-accept-*`) to assert the arms are GONE.
- **Tests:** ORCID/hive-matching slot w/o `author_accept` → NOT credited; name-only claim+approve → credited; anchored slot via Route 3 → NOT credited; `isApprovedCoAuthor` co-sign behavior change pinned; supersession display still agrees with the arm-less SQL on a padded-ORCID case.

### U3 — Wire the union credit-set into the reputation cycle
- **Depends on U1, U2.**
- `reputation.ts` `computeReputationBatch`: credit Route 1 ∪ 2 ∪ 3 − demotions, keyed to on-chain post identity. Compose the Route-2 consented CTE alongside the (now arm-less, chain-aware) `authorshipClaimsCteBody`. **Retroactivity invariant (decision #3): NO accept-block vote window.** Generalize the `co-author-claim-zero-score` Item 2 self-exclusion (`paper_resolved_votes` / `paper_reviews` `NOT EXISTS accepted_claims`) to ANY consented author, not just Route-3 claimers. Re-throw HAF errors (KTD-3) — never swallow to empty (a transient failure must not poison `prevScores` / advance `cycle:last`).
- Characterization-first on the cycle's current credit output for a seed → the union extension is provably additive.
- **Tests:** Route-2 anchored accept gets the paper score (0 without the accept); Route-3 still credited (no regression); consented author self-vote/review excluded; demotion zeroes next cycle, re-accept re-credits; native rows unregressed; **retroactivity — a vote cast before the accept still counts after**; `reputation-lifecycle` (real HAF) green.

### U6 — Regression + parity backstops
- A representative-seed regression proving the credited-set matches the model (approve / ORCID-accept / hive-accept / name-only / revoke / resign / suppress-null-ORCID / continuation-added). Confirm `npm run typecheck` + `npm run lint` clean and the real-HAF lifecycle green.

## [TODO Architect] doc reconciliation (architect-owned; backend files this note, does NOT edit these)
- `reputation-algorithm.md`: drop the "live cycle uses legacy auto-accept" status note; describe the union credit-set in "Co-author Credit"; **state the retroactivity invariant explicitly** — credit gates set-*membership*, never a per-author vote window: once consented, the next full-recompute cycle credits the paper's entire vote/review history (votes/reviews from before the accept included), because scores are recomputed from scratch each cycle and `prevScores` weights voters only. (Today the doc says co-authors get "the same score as the posting author" and that demotion is "going forward," which implies but never names this property; name it so a future editor cannot reintroduce a `vote.block_num > accept.block_num` window.) **FIX the stale param table** — "Canonical SQL Query → Parameters" still lists `$21 = admin`, but the `reputation-claims-cte-dedup` builder shifted the layout to `$1–$20` + builder `$21–$25` (admin re-bound at `$25`).
- `ARCHITECTURE.md` § 2: flip the "not yet wired" / "live cycle does NOT yet compute the consented-set" status statements.
- `hive-schemas.md` § 2.9: tighten the `author_index` resolution domain to "cumulative-chain author union" so root-only resolution isn't re-introduced (resolve the § 2.9 "fixed at posting" vs § 2.10 "cumulative union" ambiguity).
- `api-contracts/papers.md` (`consented` field) + the pending-endpoint contract are covered by `backend-consented-set-read-surfaces`.

## Sequencing & dependencies (verified 2026-06-09)
- **Prerequisite — SATISFIED:** `backend-co-author-claim-zero-score` archived 2026-06-09 (list-final arms + Item 2 self-dealing are live; generalize Item 2 to the consented set).
- **Shared builder — LANDED in code:** `backend-reputation-claims-cte-dedup` collapsed the cycle's inline claim resolution onto `authorshipClaimsCteBody`, so the auto-accept arms live in ONE place. That task is held in `pending/` on 3 test/comment hygiene items (forged-revoke equivalence seed, stale "Mirrors reputation.ts" comments, a test-header slug) — none block the builder's use, but **rebase U2's arm-removal on the final builder once those land** to avoid comment churn.
- **Phasing:** U1 before U2 is mandatory (else anchored co-authors lose all credit in the gap). Go-forward only (no production ops, no migration) — land U1→U2→U3 back-to-back or in one PR to close the window.
- **Read surfaces:** split to `backend-consented-set-read-surfaces` (badge + pending endpoint), which depends on U1's shared CTE.
- **Co-requisites / related:** `backend-authorship-credit-ops-fresh-auth` (pending — § 6.4 fresh-auth for consent ops; assumes it lands in parallel, not re-implemented here), `ui-multi-author-consent-affordances` (blocked — UI), `backend-bridge-paper-author-claim-flow` (blocked — bridge papers stay single-consented-author; consent short-circuits), `backend-extract-chain-cumulative-helper-to-lib` (blocked — the JS cumulative helper; reconcile with the SQL chain CTE). (The former `backend-notification-infra-for-consent-ops` task was superseded by this task + the read-surfaces task and removed 2026-06-09.)

## Acceptance
- Reputation/citation credit flows ONLY to consented authors (Routes 1/2/3); no auto-accept path credits anyone.
- An anchored ORCID slot credits its owner only after that owner — accredited with that ORCID — broadcasts `author_accept`; a name-only slot credits only after `claim` + `approve`.
- The author/admin `revoke` backstop and `author_resign` / self-`revoke` demote a consented co-author next cycle; a later re-consent re-credits.
- A co-author named via a continuation revision (anchored OR name-only) is creditable; credit binds only to named slots.
- **Retroactivity: a vote/review cast before an author consented still counts toward their credit once consented.**
- A consented author's self-vote/self-review is excluded (Item 2 generalized to any consented author).
- The `isApprovedCoAuthor` co-sign-authority behavior change is pinned by a test and surfaced.
- `[TODO Architect]` doc-handoff note filed before the task moves to `review/`.
- No sense-3 "vouched" symbol remains in `consent-ops.ts` or new code; comment anchors on stable symbols (no task slugs / line numbers / SHAs); `npm run typecheck` + `npm run lint` clean.

## Cross-references
- `agents/docs/ARCHITECTURE.md` § 2 "Consented vs claimed authorship", "Consented-set computation (Phase 2 constraints)", wire formats "Author Accept / Author Resign (custom_json)"; § 6.4 re-auth rows.
- `agents/docs/hive-schemas.md` § 2.9–2.11 (name-only route, `author_index`).
- `agents/docs/reputation-algorithm.md` "Co-author Credit" + "Canonical SQL Query".
- `backend/src/reputation.ts` (`computeReputationBatch`, `accepted_claims`), `backend/src/hafsql.ts` (`authorshipClaimsCteBody`, `activeAccreditationsCteBody`, `excludeClaimedSelfWhere`), `backend/src/consent-ops.ts` (`computeVouchedAuthors`, currently inert), `backend/src/routes/papers.ts` (`resolveContinuationChain`, `buildCumulativeAuthorsForChain` — the JS chain walk being ported to SQL), `backend/src/lib/author-supersession.ts`.

---

## Backend completion note (2026-06-09/10, commits `ebd7dea3` U1 + `319f0c3c` U2+U3)

All four units landed; the ratified model is live in code. U1 and U2+U3 are two back-to-back commits on main (no anchored-co-author no-credit window: the arms were removed in the same commit that wired Route-2 credit).

**U1 (`ebd7dea3`):** sense-3 rename (`computeConsentedAuthors` / `getConsentedAuthors`; no "vouched" symbol remains in `consent-ops.ts`); slot-aware eligibility (`ClaimedSlot` with normalized `hive`/`orcid` anchors + first-appearance block; ORCID anchors resolve ONLY through the authority-attested map, latest-action-wins, null never anchors); KTD-3 fail-closed discriminated union on the fetch (`{status:'ok',ops}|{status:'haf_unavailable'}`, assertNever in the orchestrator). New `hafsql.ts` infrastructure: `consentChainCteBody` — the `WITH RECURSIVE` port of the JS walker (cumulative admission gate threaded down the recursion; earliest-wins canonical-path selection via a second recursion over the materialized sibling-ranked tree; visited-array cycle guard; 50-hop cap; two-track display-ordered slot union = the `author_index` domain; append-only ops-union claimed slots with Rule-6 first blocks) — plus `consentedAuthorsCteBody` (Route 1/2 + resign/revoke demotions, latest-op-wins per `(block_num, id)`) and `buildRecursiveWith`. The design was validated empirically on a real planner before landing (synthetic-corpus harness; the production CTEs are regression-pinned by `tests/consented-authors-cte-real-postgres.test.ts` using the FROM-redirect technique).

**U2 (`319f0c3c`):** ORCID + hive auto-accept arms DELETED from `authorshipClaimsCteBody` (single 'accepted' path: explicit approval gated on a NAME-ONLY slot resolved against the cumulative-chain display union — decision #4 implemented, root-only resolution gone). The builder embeds a `claims_`-prefixed chain backbone seeded from `claims_base` (walk bounded by claim cardinality; no consumer signature change beyond `buildWith`→`buildRecursiveWith` at all 10 composition sites). `chainOrcidAutoAcceptMatchSql` deleted (no production consumer). KTD-4 audit: the JS supersession mirror keeps its display role (its comment retargeted); the `isApprovedCoAuthor` co-sign behavior change is pinned by the real-postgres exact-set assertion (anchored co-authors have NO accepted claim row → no bridge co-sign authority) and surfaced here: **co-sign authority now requires Route-3 claim+approve; an ORCID/hive match alone no longer grants it.**

**U3 (`319f0c3c`):** the cycle credits Route 1 ∪ 2 ∪ 3 − demotions. Native arm gains the Route-1 demotion guard (root's own resign / revoke naming them); new `consented_authors` user_papers arm (deduped against native AND claims arms — a dual-route account contributes exactly one row); both self-dealing NOT EXISTS gates generalized to the consented set; consent stack composed after `active_accreditations` with an op-derived `consent_seed` (walk bounded by batch consent activity, not corpus). HAF errors still re-throw (the existing catch re-throws; KTD-3 preserved). **Retroactivity pinned structurally**: `reputation-consented-credit-cycle-sql-shape.test.ts` asserts the vote/review aggregation references no consent block columns (a `vote.block_num > accept.block_num` window cannot be reintroduced without flipping it).

**U6:** `consented-authors-cte-real-postgres.test.ts` is the representative-seed regression — approve (name-only `davesacct`), ORCID-accept (carol), hive-accept (eve incl. accept→resign→re-accept), name-only anchored-slot rejection (`sneaky`), revoke backstop (bob), forged revoke/accept inertness, self-signed-attestation attacker (imposter), suppress-null-ORCID (consent-ops unit matrix), continuation-added slot (eve), edit-removed append-only eligibility (zara), fork orphaning (mallory), cycle termination, bridge papers, Route-1 self-resign (greta), signers scope. Real-HAF `reputation-lifecycle` green; typecheck (src+tests) + lint clean (1 pre-existing warning, untouched).

**Verification:** consent suites 45/45; signer gates + cycle shapes + inverted canaries green; papers/profile/search/stats real-HAF route suites green (the recursive chain CTEs execute on live HAF); `excludeSelfReviewWhere` callsite canaries 6/6; eslint suite 47/47. Full-suite gate (post-U3): 12 failures, triaged via baseline worktrees — 1 real regression (the `bridge-paper-author-gate` expectedCount pin, updated to the new 7-site shape in `1e959411`, now 14/14), 1 load-flake (`stats-profile-parity`, green in isolation), 10 pre-existing on clean main verified at `f84f275d`: `reviews.test.ts` gate specs ×2 and `accreditation.test.ts` cap specs ×2 (both previously documented), plus `lib/idempotency-real-haf.test.ts` ×2, `papers-enrichment-parity-gate.test.ts` ×1, `wot-broadcast-timeout.test.ts` ×3 (newly confirmed pre-existing this session; architect may want a separate triage task for these).

**Surfaced for architect attention (not fixed here):**
1. **Batch-scoped self-dealing exclusion gap (pre-existing class, now also applies to the consented set):** `accepted_claims` (and now `consented_authors`) are scoped to the batch's target users, so the self-vote/self-review exclusion only catches credited co-authors who are IN the current batch. A credited co-author outside the batch self-voting a shared paper still counts toward the OTHER credited users' scores for that paper. Pre-existing for claims since the {claimers} scoping; mirrored (not widened) for consent. Unscoping would make the exclusion batch-independent at the cost of corpus-wide resolution per batch.
2. **isApprovedCoAuthor behavior change** (above) — flagged per the KTD-4 land-mine instruction.
3. The `[TODO Architect]` doc-reconciliation section above stands as written (reputation-algorithm.md status note + retroactivity naming + stale `$21 = admin` param table; ARCHITECTURE.md § 2 status flips; hive-schemas § 2.9 `author_index` domain tightening).

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

---

## Architect re-review (2026-06-11) — HELD PENDING FIXES (4 items)

`/ce-code-review` fan-out on commits `ebd7dea3` + `319f0c3c` + `1e959411` (correctness + security + adversarial on the session model; testing/maintainability/project-standards/performance/reliability + learnings on Sonnet; ce-agent-native skipped per PEvO; validation wave on every surviving finding). **The core model is VERIFIED CORRECT and STAYS**: Rule-5 signer gates hold on all five op types (forged accept/resign/revoke inert; the revoke arm's `r_author` is pinned to the real root by the eligibility JOIN, not the payload field); self-signed attestations cannot anchor ORCID slots (authority-gated `active_accreditations`, latest-action-wins); Rule 6 is strictly `>` over the append-only MIN first-appearance block (edit-reset squatting defeated); the chain walk's cumulative-admission gate, visited-array cycle guard, 50-hop cap, and earliest-wins canonical-path selection held against every constructed attack (orphaned forks excluded); KTD-3 fail-closed holds on every path (cycle catch re-throws; `haf_unavailable` never degrades to empty; assertNever exhaustive); dual-route accounts dedup to exactly one user_papers row; the retroactivity wiring is correct; param arithmetic is consistent across all ten `buildRecursiveWith` migrations; the batch-scoped self-dealing mirror is faithful (not widened). Commit hygiene, zone discipline, and carve-out headers clean. Four items before archive (user-triaged):

1. (P2, testing, validated) **Behavioral cycle-credit tests for the two ratified invariants.** (a) Retroactivity is pinned only structurally: the `reputation-consented-credit-cycle-sql-shape` pin is a denylist of three names (`route2_latest`, `first_block`, `consent_seed`) over the aggregation region, so a vote-window reintroduced via a freshly-named alias passes green, and no test anywhere drives a pre-accept vote through the cycle and asserts it counts. (b) "Demotion zeroes next cycle, re-accept re-credits" (the U3 test bullet) is covered only at `consented_authors` set membership, never through `computeReputationBatch`'s scoring output. One synthetic-VALUES (FROM-redirect) test driving the real composed cycle SQL covers both: a vote cast before the accept earns the full paper score once consented; latest-op resign scores 0; re-accept re-credits.
2. (P2, maintainability, conf 100) **`reputation-coauthor-claim-credit.test.ts` "named-slot gate" describe pins deleted semantics.** It embeds a frozen pre-arm-removal SQL copy that still contains the deleted ORCID auto-accept arm, asserts its auto-accept outcome, and a comment references the deleted `chainOrcidAutoAcceptMatchSql` symbol. Rework against the production `authorshipClaimsCteBody` with the no-auto-accept expectations (or, minimally, retitle the describe as an explicit historical snapshot and drop the dead-symbol reference) so no test asserts the arms this task deleted.
3. (P2, maintainability, validated) **Missing JS-to-SQL twin cross-reference.** `buildCumulativeAuthorsForChain` (papers.ts) has no docblock pointer to `consentChainCteBody`; the SQL side points at the JS walker but not vice versa. Add a docblock paragraph naming the SQL twin and the invariants that must stay mirrored (cumulative-admission gate, canonical-path selection, 50-hop cap, cycle guard, display-slot ordering). Anchor on symbols only.
4. (P2, performance, validated) **`chain_node_created` triple-correlated subqueries collapse to one LATERAL.** The post-walk annotation CTE runs three correlated subqueries against `operation_comment_view` per chain node (MIN(block_num), then MIN(id) with a nested MIN(block_num) repeat). It is not the recursive member, so a `CROSS JOIN LATERAL (SELECT o.block_num, o.id FROM <commentOps> o WHERE o.author = t.author AND o.permlink = t.permlink ORDER BY o.block_num ASC, o.id ASC LIMIT 1)` is legal there and does the same work in one indexed scan per node. Rewrite; the consented-authors real-postgres suite pins behavior.

Spun off at triage (NOT held here): the display-surface self-dealing gap for Route-2 consented authors (the cycle excludes them; `excludeClaimedSelfWhere` / `batchResolveVotes` remain claims-only) is filed as `backend-consented-set-display-self-dealing-exclusion` in `tasks/pending/`.

Dismissed/no-action at triage: `consent_ops_raw` namespace scan (same accepted shape as `activeAccreditationsCteBody`; seed-bounded downstream); `fetchConsentOpsForPaper` bare pool.query (the HAF pool sets a 30s connection-level statement_timeout; bare pool.query is the codebase-dominant pattern); the dedup-equivalence test deletion (the `sneaky` Route-3 pin plus the full-corpus exact-set assertion cover the anchored-slot-no-accept scenario); author_index dense-rank mutability (covered by the `[TODO Architect]` hive-schemas § 2.9 tightening); the batch-scoped self-dealing exclusion (faithful mirror of the pre-existing claims-arm class, accepted as documented residual).

When the four items land, `git mv` this file back to `tasks/review/`; the move is the re-review signal, scoped to the fix commits. Do not edit this hold block — the commit diff is the evidence; the architect updates it at re-review.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>

---

## Backend re-review signal (2026-06-11, single fix commit on main)

All four hold items landed:

1. **Behavioral cycle-credit test** — new `tests/reputation-consented-credit-cycle-behavioral.test.ts`: captures the REAL cycle SQL `computeReputationBatch` emits (the capturing-pool harness from the cycle-shape pins), FROM-redirects all five HAF view literals at synthetic temp tables, and executes it verbatim on a real planner. Pins both ratified invariants behaviorally: the corpus's only vote lands at block 100 and eve's `author_accept` at block 200, and her `papers` component equals the root broadcaster's full score (retroactivity — a reintroduced vote-window under ANY alias zeroes her score and fails red, the behavioral complement of the structural name-denylist pin); a latest-op resign scores her 0 through the composed scoring output; a re-accept restores the full score; the root broadcaster is unaffected throughout.
2. **Named-slot-gate describe reworked** — the frozen pre-arm-removal SQL copy (deleted ORCID auto-accept arm + dead `chainOrcidAutoAcceptMatchSql` comment reference) in `tests/routes/reputation-coauthor-claim-credit.test.ts` is replaced by a production-builder FROM-redirect describe: it composes `activeAccreditationsCteBody` + `authorshipClaimsCteBody` exactly as the read surfaces do over a synthetic corpus, with the exact-set assertion accepting ONLY the name-only claim+approve row. Unlisted (omitted `author_index`), out-of-range, anchored-slot-with-approval, and attested-ORCID-match-without-approval all stay pending — the last two are direct inversions of the deleted auto-accept semantics, with the corpus's authority attestations composed and provably accepting nothing. The file-header bullet was updated to match.
3. **JS-to-SQL twin cross-reference** — `buildCumulativeAuthorsForChain`'s docblock now names `consentChainCteBody` as the SQL twin and enumerates the five invariants that must stay mirrored (cumulative-admission gate, earliest-created canonical-path selection, 50-hop cap, visited-set cycle guard, two-track first-occurrence display-slot ordering as the `author_index` domain). Symbol anchors only.
4. **`chain_node_created` LATERAL collapse** — the triple-correlated `operation_comment_view` subqueries replaced by one first-op probe per chain node. `LEFT JOIN LATERAL ... ON TRUE` rather than `CROSS JOIN LATERAL` so a node with no visible op row keeps its NULL annotation (`ranked_children` orders `NULLS LAST`) — exactly the prior correlated-subquery semantics in one indexed scan; the site comment records why LEFT.

Verification: consent-stack batch green — consented-authors-cte-real-postgres, me-pending-authorships-real-postgres, hafsql, both cycle-shape pins, coauthor-claim-credit (13), the new behavioral canary, papers-consented-badge, me-authorships-pending, consent-ops: 124 passed / 4 data-dependent skips. Real-HAF `papers.test.ts` (15 passed / 1 skipped) executes the rewritten chain walk live. `npm run typecheck` (src+tests) clean; `npm run lint` clean except the known pre-existing `author-supersession.ts` warning.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>

---

## Architect re-review (2026-06-12) — round 2: ARCHIVED

`/ce-code-review` fan-out on fix commit `64129eb2` (correctness + adversarial on the session model; testing/performance/maintainability/project-standards + learnings on Sonnet; ce-agent-native skipped per PEvO). All four items held on 2026-06-11 are FIXED: the behavioral cycle test pins retroactivity and resign/re-accept through the real captured SQL with a dynamically-derived expected score (the vote-window-under-any-alias and inverted-resign mutants both fail red; the root-non-zero guard kills vacuity); the named-slot describe composes the production builders with a genuinely exact assertion (a restored ORCID auto-accept arm flips the attested-ORCID corpus row red); the twin docblock's five invariants verified accurate against BOTH implementations; the chain_node_created LATERAL collapse proven semantically equivalent in every constructible case — and the implementer's LEFT JOIN LATERAL deviation from the hold's CROSS prescription verified CORRECT (CROSS would drop op-less nodes including the depth-0 canonical_chain seed; the hold prescription was wrong). One new P3 (no test pins the LEFT-vs-CROSS distinction; both corpora are op-complete) dismissed at user triage as preemptive hardening — the diff changed form, not behavior; the site comment is the durable defense. Test-infra dedup (shared FROM-redirect helper + consent-stack composer) split to `backend-consent-stack-test-infra-dedup`. The [TODO Architect] doc reconciliation landed (reputation-algorithm.md, ARCHITECTURE.md section 2, hive-schemas.md section 2.9).

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>

## BACKEND-REISSUEDAT-ROUNDTRIP-ORCID-SITE (archived 2026-06-12) — ORCID-recovery reissuedAt round-trip pin ✓

Added the ORCID-branch companion to the memo-key reissuedAt round-trip test (commit ca28def2): `verifyHiveSignature-reissuedat-orcid-roundtrip.test.ts` seeds a reachable ORCID-only light account, forces the in-memory `orcidVerified` fallback (a genuine production path: orcid.ts writes it Redis-down, recover.ts reads it Redis-down; the `sessions_invalidated_at` write + `reissuedAt` embed sit unconditionally downstream of the nonce-store branch merge, so the pin is not fallback-vacuous), drives a genuine POST /api/auth/recover ORCID reissue against real Postgres, asserts the reissued JWT's `reissuedAt === sessions_invalidated_at.getTime()`, and pins the same-second discrimination with the control-token pair shape its memo-key sibling settled on. First review (correctness + security + adversarial on the session model; testing/maintainability/project-standards/learnings on Sonnet): zero substantive findings — branch reachability, fallback non-vacuity, fixture reachability (enumerated recover transition), mutation coverage of the seconds-rounding / re-derived-Date / write-but-no-embed regressions, and carve-out clauses (a)/(b)/(c) all verified. Round-1 hold (revert-probe attestation per the mutation-soundness convention) landed in e7c45399: the seconds-rounding probe went red on every attempt, restored green, and is attested on the stable embed symbol. The same probing surfaced that a NOW()-switch of the invalidation write is caught only ~4% per attempt (Node `Date` and SQL `NOW()` usually share the millisecond bucket, and retries compound survival) — the overstated in-file "NOW()/seconds-rounding turns this red" claim exists in BOTH roundtrip files and its reword is held on the memo-key sibling task (`backend-reissuedat-roundtrip-realpath-test`), which owns the cross-file fix along with the optional `preReset` -> `controlWithoutReissue` rename. The verify-side ms-strictness pin for the shared exemption lives in the mocked hardening suite (landed with the sibling's round-2 fix).

## BACKEND-CONSENT-OP-FIELD-NORMALIZATION-PARITY (archived 2026-06-11) — one shared trim+cap reader for consent-op fields across all three hash sites ✓

Converged the consent-op (author_accept/author_resign) field reads on a shared extractConsentOpFields (trim + cap via requireStringField: root_author at CONSENT_OP_ACCOUNT_MAX_LEN 64, root_permlink at HIVE_PERMLINK_MAX_LEN) used by all three hash sites — the orcid /start consent branch (previously bare typeof/length checks, no trim/cap), the custody /fresh-auth issuance, and the broadcast consume scan consentOpTarget — mirroring the credit-op convergence (commit a6a278f1). Clean-value hashes byte-identical to the replaced inline literals (backward-compat pin against a hand-built literal); behavioral deltas all fail-closed: padded fields now mint/consume against trimmed bytes on every mechanism (killing the cross-mechanism self-inflicted target_mismatch), over-cap and whitespace-only fields 400 at the ORCID issuance and the consume scan, and a field-malformed payload at consume fails 400 BEFORE the single-use proof is consumed. Round-1 review (8-reviewer fleet) verified the implementation SOUND with one held testing item — the consent branch's extraction-failure arm had zero route-level coverage — landed test-only in 5c3735e9: a consent-ops describe in orcid.test.ts mirroring the credit-ops sibling (missing root_author → 400 VALIDATION_ERROR and missing root_permlink → 400 via author_accept; over-cap 65-char root_author via author_resign — the independent one-past-the-cap literal per the dedup-constant convention), all three pure-validation cases running unconditionally with the real auth chain. Round-2 re-review (correctness on the session model; testing/maintainability/project-standards/learnings on Sonnet): CLEAN, zero findings — specs traced through the real route (the consent-arm extractor-failure 400 fires before the Redis stash write; both action values pass the branch), and the extractor guard confirmed the sole reachable 400 VALIDATION_ERROR site for these inputs. Deploy-window residual accepted at round-1: pre-deploy ORCID-minted proofs against padded/over-cap raw values fail closed post-deploy (TTL-bounded 5 minutes). The api-contract consent-op trim/cap/400 notes landed with the parent credit-ops archive. Orcid suite 105/105 green vs real Postgres/Redis.

## BACKEND-ADMIN-BROADCAST-ENVELOPE-SWEEP-REMAINING-SITES (archived 2026-06-11) — migrate papers/accreditation/signup-verify to broadcastAdminCustomJson ✓

Finished the admin custom_json envelope dedup at the three straightforward sites (commit abaf65a5): papers.ts retraction, accreditation.ts verify, and signup-verify.ts broadcastAccreditationAndSeed now call broadcastAdminCustomJson; the two orcid sites were split to backend-admin-broadcast-orcid-sites per the task's own provision. Round-1 review verified the migration CORRECT at all three sites (envelope parity byte-exact vs the removed inline forms; pre-guards intact so AdminKeyNotConfiguredError stays unreachable; broadcast-attempts-cap timeout-decrement semantics preserved; the envelope literal exists only in hive.ts). Accepted deviation recorded at round-1 triage: the set-but-malformed admin-key edge in broadcastAccreditationAndSeed moved from a pre-inner-try synchronous 500 escape to an inner-catch 502 BROADCAST_FAILED — found at conf 100 by three reviewers and accepted as the better shape (account row already finalized, 502 matches the stuck-recovery design, edge reachable only via operator misconfig). Round-2 hold (3 comment-only items) landed in de8e4b99: the hive.ts AdminKeyNotConfiguredError docblock dropped the per-adopter enumeration (which had rotted twice) for a call-site contract anchored on stable symbols (pre-guard whenever the unset-key path needs a shape more specific than handleBroadcastError's generic 502, wot.ts pre-guards as the pattern); signup-verify.test.ts findAccreditOp comment now enumerates all three broadcastJsonMock-backed seams with the envelope-in-.json scan invariant; signup-verify-session-binding.test.ts header mock inventory matches the factory. Round-3 re-review (correctness on the session model; testing/maintainability/project-standards/learnings on Sonnet; agent-native skipped per PEvO): CLEAN, zero findings — the docblock contract was verified factually accurate against all seven adopter call sites (every site pre-guards; the 502 mapping confirmed at handleBroadcastError's default branch), and the three-seam envelope invariant was independently confirmed against the vi.mock factories by three reviewers (the broadcastAdminCustomJson shim synthesizes the envelope before recording, mirroring the real helper). Implementer-verified suites 23/23 green vs real Postgres/Redis; architect round-1 verification 142/142.

## BACKEND-AUTHORSHIP-CREDIT-OPS-FRESH-AUTH (archived 2026-06-10) — per-target fresh-auth for claim/approve/revoke_authorship on custody broadcast ✓

Closed the § 6.4 gap where the name-only-route credit ops were broadcastable JWT-only through POST /api/custody/broadcast (§ 6.5 invariant #1 violation). Four rounds: feature commit 92f4b618 (gate + CREDIT_OP_ACTIONS + author_index hash binding, 8 holds); 89263220 (claimer bound into approve/revoke targets, malformed-gated-op 400 BEFORE proof consume, ORCID issuance route tests, target echo from both issuance paths, 5 holds); f3044517 (as-const tuple derivation kills the unsound casts, exhaustiveness backstop in the shared extractCreditOpFields, single-source trim+cap field normalization at all three hash sites, kind_mismatch regression test, consent_*→gated_* log rename, revoke-malformed proof-survives + ORCID skipIf de-vacuuming, folded computeFreshAuthTargetHash docblock fix, 2 small holds); eef25504 (pre-limiter caps linked to exported CREDIT_OP_ACCOUNT_MAX_LEN, backstop throws assertNever-style). Round-4 re-review (8-reviewer fleet, correctness/security/adversarial on the session model): CLEAN, zero findings — cap swap byte-identical, throw unreachable through all typed callers (Express 5 forwards a hypothetical throw to errorHandler as a clean 500), anchors clean. Final per-op binding: claim → (action, paper_author, paper_permlink, author_index); approve → + claimer; revoke → (action, paper_author, paper_permlink, claimer), no author_index. Archive-time architect doc work landed with this entry: ARCHITECTURE.md § 6.4 stale "enforcement-pending" caveat removed (binding is enforced as documented); api-contracts/custody.md + orcid.md now document the issuance-response target echo (author_index/claimer when bound) and the malformed-gated-op 400-before-consume surface, plus the consent-op trim/cap parity notes from the sibling task. Companions: backend-anchor-rot-sweep-fresh-auth (archived), backend-consent-op-field-normalization-parity (in review), ui-credit-op-proof-cache-slot-key (blocked; SPA per-slot proof-cache half).

## BACKEND-PIN-SHARED-BROADCAST-VALUES (archived 2026-06-10) — pin per-flavor evidence_hash + the real broadcastAdminCustomJson envelope ✓

Landed both regression pins (commit c791654d): F1 drives /confirm and /link broadcasts to completion in signup-verify.test.ts, decodes the captured op json, and asserts evidence_hash === sha256(email:username:signup|link) plus hard-coded literal suffix pins (the production map is read as subject only, so the dedup cannot defeat the pin); F2 calls the REAL broadcastAdminCustomJson into a spied hiveClient.broadcast.json in hive-broadcast-timeout.test.ts, pinning id === appTag, required_auths === [], required_posting_auths === [hiveAdminAccount], and the AdminKeyNotConfiguredError throw on an unset key with the spy never called. Round-1 review verified both pins mutation-killing (each mutation applied live, confirmed RED); one hold item — the two F1 fixtures seeded an unreachable account state (email set, neither password_hash nor orcid: no identity anchor) — fixed in 7b6910c5 with a shared argon2-sentinel EVIDENCE_PASSWORD_HASH making each a reachable email-path signup-pending row (email set, password set, no ORCID, username/custody unset, verify_token 'confirmed:'), state named by dimension description. Round-2 scoped /ce-code-review (correctness on the session model; testing/project-standards/learnings on Sonnet): clean — all six state dimensions verified reachable, INSERT param mapping traced, sentinel byte-identical to the suite's existing timing-guard idiom, no pin-tautology from the shared const (seed-only, never asserted), comments rot-free, hold-block mechanics correct (single R+M commit). Architect-verified signup-verify 14/14 + hive-broadcast-timeout 26/26 green vs real Postgres/Redis. The F2 envelope pins are the compensating control behind the cluster triage dismissal of the fabricated-mock-envelope finding (the route suites' factories emit placeholder envelope fields; the real shape is pinned here).

## BACKEND-ANCHOR-ROT-SWEEP-FRESH-AUTH (archived 2026-06-10) — sweep Round-N/hold comment anchors out of lib/fresh-auth.ts ✓

Swept all Round-N / hold #M / round-N comment anchors from backend/src/lib/fresh-auth.ts (commit edc19a64), re-anchoring each on stable symbols / behavioral descriptions per the root CLAUDE.md comment-anchor convention; also removed line-number anchors and a task-slug citation the sweep surfaced, and corrected two now-stale substantive phrasings. Comment-only, no behavior change; acceptance grep (Round-[0-9]|round-[0-9]|hold #) returns nothing; typecheck + lint clean; fresh-auth.test.ts green (53). Landed in the same batch AFTER the credit-ops fix (89263220) so the sweep also covered the credit-op additions to fresh-auth.ts. Architect review (right-sized /ce-code-review: project-standards + maintainability — comment-only diff): clean on its own acceptance — no new rot-class anchor introduced (the one § 6.5 line is a two-line reflow of pre-existing text), diff verified comment-only, swept comments preserve their invariants; pre-existing § N.M doc-section cites out of scope per architect convention call. One finding — the computeFreshAuthTargetHash docblock header still introduced the bind as "the target triple" while the function binds up to five fields (incl. author_index + claimer) — was folded into backend-authorship-credit-ops-fresh-auth's round-2 hold (same docblock that feature changed) so one return trip closed both. That reword landed in f3044517 and was verified by the credit-ops round-3 /ce-code-review: header now describes the per-op fields, "two distinct triples" became "two distinct field sequences", the intentional "original triple form" backward-compat reference retained, no other comment in the touched files describes the pre-refactor shape. The credit-ops task's unrelated round-3 hold (pre-limiter cap constant; backstop throw shape) does not gate this sweep's acceptance, which is fully met.

## UI-ORCID-STUB-REAL-ROUNDTRIP-UNFIXME (archived 2026-06-10) — drive the ORCID E2E specs through the real backend round-trip; round-3 clean ✓

Un-fixme'd the real-backend ORCID blocks against the orcid-stub compose sidecar (commit 66a46ec1): settings-orcid-factor.spec.js gained the live acceptance test (real /orcid/start -> in-page authorize fulfil -> real /orcid/callback proof mint -> real /api/settings/set-password 200, plus DB password_hash, cached-proof shape, post-reload UI, and password-login-200 assertions), orcid-link.spec.js's 403 cross-user state-hijack test runs for real (the stub sets ORCID_CLIENT_ID/SECRET so its skip guard no longer fires), and orcid-no-password.spec.js's set-password fixme was consolidated to a pointer; its signup/recover fixmes stay (token-only stub serves no pub.orcid.org works endpoint; follow-up ui-orcid-signup-recover-real-roundtrip filed). Non-obvious redirect-host bridge: the SPA's ORCID_REDIRECT_HOSTS allowlist guard would throw on the stub host, so the test passes /orcid/start through to the real backend and rewrites only the redirect_url host to orcid.org before intercepting **/oauth/authorize*. Round-1 hold (3 items, commit 9a1c95b6): waitForResponse arm on the post-reload email fetch, routeOrcidStubBridge(page, baseURL, code) dedup of the byte-identical bridge at both call sites, orcid-no-password.spec.js header rewritten off the stale SEC-004-BE framing. Round-2 hold (1 item, commit f921f9a8): the waitForResponse arm was itself non-discriminating — the set-password section's x-if gate (!emailLoading && emailStatus && hasPassword === false) keeps it hidden through the whole email-fetch window, so toBeHidden() soft-PASSED on the loading frame; replaced with a positive post-loading-surface gate (await the "Change email" button visible — it renders only once !emailLoading && emailStatus && hasEmail && verified) and the comment rewritten to the real mechanism, anchored on the binding expression and state names. Round-3 /ce-code-review (correctness on the session model; testing/maintainability/project-standards/julik-frontend-races/learnings on Sonnet; ce-agent-native skipped per PEvO): clean — correctness and testing independently verified the gate is discriminating (a hasPassword:false regression renders the section in the same Alpine flush and fails toBeHidden(); a failed fetch falls back to hasEmail:false and times out the gate loudly), and the comment anchors are convention-clean. One P2 dismissed at user triage as preemptive hardening: the role+name "Change email" locator vs the file's getByTestId house style (an en.json rename of settings.emailChange breaks it loudly via locator timeout, not silently; the fix would add a production testid purely for a test locator; suite-level English-string precedent exists in settings.spec.js).

## BACKEND-ACCREDITATION-STATE-READ-TIEBREAKER-SWEEP (archived 2026-06-09) — same-block `id` tie-breaker on the 4 remaining inline accreditation-state reads; clean ✓

The semantic-sibling sweep the prior `window-cte-deterministic-tiebreaker` syntactic grep missed: four INLINE "latest accredit/revoke wins" reads (commit 77571323) gained the `ORDER BY cj.block_num DESC, cj.id DESC` same-block deterministic tie-breaker — `findAccreditedAccountWithOrcid`'s first read + binding-live re-check and `getExistingAccreditation` (routes/orcid.ts), and `getAccreditationFromHaf` (routes/profile.ts) — each with a convention-anchored comment (cites the custom-json hive-primitive design-rules convention by name, not a dated filename). `/ce-code-review` (correctness + security on Opus; testing/maintainability/project-standards/learnings on Sonnet; ce-agent-native skipped per PEvO): clean on all four sites. Security: pure determinism hardening that CLOSES the non-determinism window (a same-block revoke-after-accredit now correctly reports revoked, not a coin-flip stale accredit), the `required_posting_auths ?| accreditationAuthorities` SEC-AUTH-BYPASS authority gate is byte-preserved at every site, and a self-broadcast can't sort after a legitimate authority op (the gate excludes it from the candidate set before ordering). Correctness confirmed the four are the COMPLETE in-scope set (re-grep of `block_num DESC`: the notification window-feed and the genesis `MIN()` aggregate are not of-shape; the accepted residuals wot `update_params` / reputation `update_weights` / `lib/idempotency.ts` / papers `batchResolveVotes` are out of family), `cj.id` is the monotonic global op id (DESC + LIMIT 1 = latest), and no projection change was needed (profile.ts already projects `cj.id AS event_id`; the orcid reads use it in ORDER BY only). Sub-goal 2 (single-source the ranking so the shape canary reaches these inline reads) was deferred to the architect and, at user triage, accepted-as-correct + archived; the residual canary-reachability gap (a future dropped tie-breaker at an inline read passes the suite because the live HAF corpus can't seed a same-block collision) is now tracked by the new follow-up `backend-accred-state-read-tiebreaker-eslint-guard` (a structural ESLint guard mirroring `pevo/no-custom-id-block-num-floor`). Family now complete per `accreditation-state-read-latest-action-wins`.

## BACKEND-BUILDWITH-ADOPTION-PROFILE-ACCREDITATION (archived 2026-06-09) — convert manual single-CTE WITH spellings to buildWith; remaining sites documented; re-review clean ✓

Converted the manual `WITH ${body.sql}` template spellings at the profile.ts + accreditation.ts CTE sites to `buildWith` (byte-equivalent emission, verified round-1). Round-1 hold (1 item): acceptance #2's global `grep -rnE 'WITH \$\{' backend/src` was not clean — remaining sites were neither converted nor documented. Fixed at commit 7a2a24fc (comment-only): enumerated every remaining `WITH ${` match and documented the three genuinely-inconvertible ones inline with their structural reason — `lib/ipfs-shared.ts` (conditional collapse-to-empty: buildWith always emits `WITH …` and can't express the empty branch), `notification-queries.ts` (a builder CTE hand-joined with inline-literal CTEs, which buildWith — composing CTE builders, not literals — can't express), and `routes/papers.ts` detail query (the CTE params bind AFTER the outer author/permlink/bridge params and `$4` is reused as appTag + parent_permlink, so a buildWith-first-params adoption would renumber every `$N` on this hot query); the fourth grep match is the buildWith implementation itself. Re-review `/ce-code-review` (correctness on Opus; testing/maintainability/project-standards/learnings on Sonnet; ce-agent-native skipped per PEvO): clean — all three rationales verified accurate to the code at HEAD, anchors convention-clean, and the architect confirmed the grep is exhaustive (a broader `WITH (RECURSIVE )?` quote-or-backtick sweep finds no non-`${` manual WITH compositions). One P3 maintainability nit dismissed at triage: the papers.ts comment's `$4`-reuse clause partially restates the param-layout block ~20 lines above it (harmless redundancy; the novel "would renumber every $N" consequence + the "Not buildWith:" label is what earns the comment). Pre-existing `routes/papers.ts` `Per task hold-block item 4d` anchor-rot (not introduced here) noted against the anchor-rot sweep effort.

## BACKEND-PROFILE-REVIEWS-ACCRED-GATE-CANARY-STALE-PARAM-SLOTS (archived 2026-06-09) — restore the tautological param-slot canary against the 7-CTE-param shape; clean ✓

The `/api/profile/:username/reviews` param-position canary in `profile-reviews-accred-gate.test.ts` had gone tautological: it pinned `params[3]`=username / `params[5]`=anonAccount with `toBe('string')`, but when the reviews CTE grew from 2 to 7 params (the buildWith composition adding `authorshipClaimsCteBody{claimer}`) those fixed slots moved onto the CTE's own appTag/bridge string params, so the assertion passed unconditionally even on a mis-bind. Restored at commit 55a971ff (test-only): `USERNAME_SLOT`/`ANON_SLOT` are now DERIVED from the live builder param count (`buildWith(1, activeAccreditationsCteBody, authorshipClaimsCteBody{claimer}).params.length`) and VALUE-pinned (`=== requested username` / `=== config.hiveAnonAccount || ''`), and the old `round-3 hold #1` rot-anchor was removed and the header re-anchored on the builder composition. `/ce-code-review` (correctness on Opus; testing/maintainability/project-standards/learnings on Sonnet; ce-agent-native skipped per PEvO): clean. Correctness EMPIRICALLY mutation-killed it (swapped username↔appTag binds → the value-pin failed red) and confirmed the handler bind order `[...accredCte.params(7), username(7), appTag(8), anonAccount(9), ...]`, so the derived slots land exactly. One P3 dismissed at triage: maintainability + learnings flagged the `+2` fixed offset / derive-from-shared-builder as a staleness edge and suggested an independent literal slot pin — adjudicated as preemptive hardening, because the value-pin already fails RED on a wrong offset and so is NOT the `dedup-shared-constant-defeats-test-value-pin` tautology (which bites only when the EXPECTED VALUE, not merely the slot index, is derived from the shared constant; here the expected value is the literal username). Pre-existing `round-N hold` anchors in the test header (lines 1-56, untouched) noted against the anchor-rot sweep effort.

## UI-ORCID-FACTOR-NEGATIVE-PATH-E2E (archived 2026-06-09) — registered-factor-mismatch 403 pinned to the §6.5 invariant #2 guard message; re-review clean ✓

Negative-path E2E (isolated mismatch describe in `settings-orcid-factor.spec.js`) that actually exercises §6.5 invariant #2 (registered-factor equality): because the orcid-stub reflects the submitted `code` straight back as the token `orcid`, a happy-path run where `code == seeded iD` passes whether or not the backend checks the equality — only a mismatch (seed iD A, drive `code = B != A`, both valid ORCID-iD format so B clears `ORCID_RE` and reaches the equality check rather than the 400 format path) forces `handleFreshAuth`'s `accountOrcid !== orcidId -> 403`. Isolated account from the happy path (which the real set_password mutates to State B); B = `0000-0002-1825-0097` asserted `!== A`. Round-1 hold (1 item): the test asserted only `status===403` + `error.code==='FORBIDDEN'`, but `/api/orcid/callback` has TWO FORBIDDEN exits — the caller-mismatch guard ("Callback caller does not match initiator", reachable because fresh_auth is in AUTHENTICATED_MODES) and the registered-factor guard ("The ORCID you authenticated with is not linked to this account.") — so the assertion could not prove which fired; a regression decoupling the two username derivations would 403 via the caller guard and leave invariant #2 silently unexercised. Fixed (commit 9a1c95b6): added `expect(cbBody.error.message).toBe('The ORCID you authenticated with is not linked to this account.')`, anchored on the message text (not a backend line number), so a caller-guard 403 now fails the test instead of passing it. Re-review `/ce-code-review` (correctness + adversarial on Opus; testing/maintainability/project-standards/julik-frontend-races/learnings on Sonnet; ce-agent-native skipped per PEvO): clean — the asserted string matches the registered-factor `sendError` site verbatim, fresh_auth's membership in AUTHENTICATED_MODES makes the caller guard genuinely reachable (so the disambiguation is load-bearing), and the fix directly follows the `assert-discriminating-error-message-when-code-shared` convention written for this exact case. Coordinated in the same commit with the round-trip task's `routeOrcidStubBridge` dedup so the two edits to the mismatch test did not collide.

## BACKEND-NOTIFICATIONS-ROUTE-NEWEST-FIRST-WHOLE-BLOCK (archived 2026-06-09) — newest-first fetch + whole-block delivery; 3 rounds, edge-aligned-keep refinement; clean re-review ✓

Route-consumer half of the settled fetch-ordering contract (the digest half is tracked separately). `fetchNotificationsFromHaf` gained a `direction` param + a monotonic HAF op-id (`<view>.id`) same-block tie-breaker + a partial-cap-boundary-block drop on cap-hit; the route's `applySinceBlockFilter` was rewritten for newest-first whole-block delivery and the SPA rewind dropped (original commit 3591fb46). Three hold rounds, all resolved: round-1 (4 items) — `capHit` switched from `>= cap` over `LIMIT cap` to a `cap + 1` probe with `> cap` so an exactly-cap fully-contained window drops nothing, the `MOCK_VERIFY_SIGNATURE` carve-out acknowledgment moved to clause (a), the digest `direction='asc'` arg pinned, and a single-block-exceeds-cap empty-batch test added; round-2 confirmed those; round-3 (2 items, commit 8a0d44b8) — cap-edge over-drop of a COMPLETE boundary block fixed by gating the boundary-drop on `probeBlock === lastKeptBlock` (drop the truncated-end block only when the cut fell INSIDE a block; an edge-aligned cut keeps the complete block, because a forward 'desc' cursor and the inherited 'asc' digest never recover a falsely-dropped complete block), plus hold-item ordinals removed from test source. Final re-review (`/ce-code-review`, correctness + adversarial on Opus; testing/maintainability/project-standards/kieran-typescript/learnings on Sonnet; ce-agent-native skipped per PEvO): both round-3 items verified correct — the gate is direction-symmetric and `has_more = capHit` holds in both the drop and keep sub-cases; the op_id tie-breaker, cap+1 probe, and cross-account isolation were re-probed and not regressed. The asc-firing-gate partial-cut coverage two route reviewers flagged as missing in the route test file is resolved at HEAD by the sibling digest task's `fetch-notifications-asc-whole-block.test.ts` canary (which fires the gate with `probeBlock === lastKeptBlock` and mutation-kills an `events[0]` asc-branch swap); one maintainability dual-array-indexing comment nit (raw `result.rows` vs sorted `events`) dismissed at triage as optional polish on already-correct code. 24/24 tests green across the three notification/digest test files; typecheck + lint clean. The pre-staged `/ce-compound` fired at archive: folded the `cap + 1` probe (`> cap`) and the `probeBlock === lastKeptBlock` edge-aligned-keep refinement into `forward-cursor-feed-newest-first-and-rewind-masks-cap-edge` (the doc had been teaching the naive "drop the truncated-end block on cap-hit" form, which over-drops a complete edge-aligned block). Accepted residual: a single Hive block alone holding more than `cap` events is a permanent drop in either direction (near-unconstructible at single-instance accredited scale). Deploy backend-first or atomically, never frontend-first; the paired UI task `ui-notifications-drop-rewind-and-block-cap` is already archived.

## BACKEND-AUTHOR-INDEX-CAST-HARDENING (archived 2026-06-09) — {1,9} overflow rationale anchored in-source; re-review clean ✓

P0 fix: 7 broadcaster-controlled JSONB `::int` casts (2 `author_index` in hafsql.ts/reputation.ts + 5 revote `weight` across reputation.ts and routes/papers.ts) were wrapped in regex-guarded `CASE WHEN <regex> THEN ...::int END` so a forged custom_json fails closed (the row drops, the query completes) instead of aborting the daily reputation cycle and every read-surface query touching the targeted paper. The weight bound is `^-?[0-9]{1,9}$` (overflow-safe: the spec's unbounded `+` admits a 14-digit value that overflows int4 and re-aborts the very query the fix protects; max Hive vote weight is 10000); `author_index` is `^[0-9]{1,9}$`. Round-1 hold (1 item, comment-only) = anchor that `{1,9}` rationale in-source so a future editor cannot "simplify" the bound back to `+`. Landed at commit 415a616e: one-line SQL comments at each weight-guard site (the three reputation.ts revote arms, the papers.ts detail + voters enrichment revote queries) and the author_index guard (hafsql.ts), each behaviorally anchored. Re-review /ce-code-review (correctness on Opus; project-standards + maintainability on Sonnet; ce-agent-native skipped per PEvO): clean — verified comment-only (no SQL/regex/cast/placeholder changed), technically accurate (999,999,999 < int4 max 2,147,483,647; the unbounded form overflows and aborts), anchors clean (no slug/round/line/SHA/section marker). Dismissed: the five identical weight-comments duplicate (acceptable — the revote weight cast is inline in separate template literals across three files with no shared SQL builder).

## BACKEND-CLAIMER-SELF-REVIEW-DISPLAY-CALLSITE-EXCLUSION (archived 2026-06-09) — credited claimer's self-review/self-vote dropped from display surfaces; ClaimsRow type re-review clean ✓

Closed the display-side residual of co-author-claim-zero-score: a credited ORCID-matched or name-only-slot claimer (absent from `authors[].hive`) could self-review/self-vote their own paper and inflate the displayed `avg_rating`/`review_count`/`net_votes` (no computed-score effect — the reputation cycle already gates claimers). New `excludeClaimedSelfWhere` helper (the `accepted_claims NOT EXISTS` gate) composed at every review surface (listing rev_agg LATERAL + detail review list in papers.ts, profile reviews-list + stats, search, single-review fetch) and every vote surface (`batchResolveVotes` claimedSet for listings, `fetchEnrichmentFromHaf` acceptedClaimers for detail). Round-1 hold (3 items, commit 7802fcf1): the P1 revote-channel `net_votes` bypass closed (acceptedClaimers skip in BOTH the native-vote and revote merge loops, completing the native/revote × listing/detail 2x2), exact source-count test pins + a behavioral net_votes canary that mutation-kills both channels, and a named `drVoteAccreditedIdx` const. Round-2 hold (1 item, P3, commit 0bfef6c6): typed the per-paper claims query `pool.query<ClaimsRow>` so the accepted-claimer exclusion reads `status`/`claimer` as declared columns, not `any`. Re-review /ce-code-review (correctness + security on Opus; kieran-typescript/testing/maintainability on Sonnet; ce-agent-native skipped): clean — ClaimsRow matches the SELECT projection 1:1, type-only, a net improvement. Triaged at archive: correctness + security converged that `pool.query<T>` is a compile-time-only assertion (it catches a TS-read-side drift but NOT a SQL-side SELECT-column drop) — accepted as-is per the dismiss-preemptive-test-hardening stance (the SQL projects all six columns today; a real-path SQL-drift test is deferred), and the batchResolveVotes listing-path `Record<string,unknown>` asymmetry dismissed (explicit casts, already accepted in round-2). Architect-handled at archive: `api-contracts/papers.md` (review_count + the enrichment net_votes/voters notes gained the accepted-claimer exclusion) and `api-contracts/reviews.md` (`GET /api/reviews/:author/:permlink` now 404s a credited-claimer self-review, previously 200). The vote-resolution 2x2-parity /ce-compound lesson is already captured in `vote-resolution-native-revote-2x2-gate-2026-06-09.md`.

## BACKEND-ADMIN-BROADCAST-HELPER-EXTRACTION (archived 2026-06-09) — docblock reword landed; re-review clean ✓

Extracted `broadcastAdminCustomJson` + `AdminKeyNotConfiguredError` into `hive.ts`, adopted at 3 of 5 enumerated sites (`wot.ts` `broadcastWotAccreditation` + `cascadeRevocation`, `claims.ts` admin revoke; `wot.ts` no longer imports dhive). The two `orcid.ts` sites stayed inline by architect decision (their pre-broadcast `PrivateKey.fromString` outside the inner `try` preserves the SEC-002-TOCTOU-LOCK 504+lock-release boundary that folding the async helper would break). Follow-ups were filed: `backend-admin-broadcast-envelope-sweep-remaining-sites` (papers/accreditation/signup-verify + the orcid key-validate shim) and `backend-pin-shared-broadcast-values` (real-envelope test gap). Round-1 `/ce-code-review` held 1 item — the `AdminKeyNotConfiguredError` docblock overclaimed a per-caller mapping no current path exercises. Fixed in commit `eb2eef9e`: reworded to a forward-looking-safety-net description (every current adopter pre-checks `config.pevoAdminPostingKey` and short-circuits before the helper, so the throw is unreachable today; a future non-pre-guarded adopter surfaces it). Re-review `/ce-code-review` (correctness/security on Opus; testing/maintainability/project-standards/learnings on Sonnet; ce-agent-native skipped per PEvO): clean. The architect independently verified the load-bearing claim against all call sites — `broadcastWotAccreditation`, `cascadeRevocation`, `revokeVoucheeIfBelowThreshold`, and `claims.ts` admin revoke each pre-guard `config.pevoAdminPostingKey` and short-circuit, so `AdminKeyNotConfiguredError` is genuinely unreachable on every present path (the guard is pinned by the sibling pin task's real-helper test). One P3 cosmetic nit dismissed at triage: the parenthetical says "the WoT paths return a `skipped` result," but `cascadeRevocation` returns `[]` (2 of 3 paths return `skipped`); "skipped result" reasonably generalizes an empty-list skip and the load-bearing unreachability claim is correct.

## BACKEND-REPUTATION-CLAIMS-CTE-DEDUP (archived 2026-06-09) — single-source the claim-resolution CTE; round-2 clean ✓

Extracted the ~100 lines of `claim_events`+`accepted_claims` resolution that `reputation.ts` inlined as a verbatim copy of `authorshipClaimsCteBody`, so the daily reputation cycle and the read surfaces now compose ONE shared builder (`AuthorshipClaimsScope` gained a `{claimers: string[]}` variant; the cycle projects `accepted_claims` as `status='accepted'`). A real-Postgres byte-identical equivalence test (frozen pre-refactor inline copy vs new builder) pins that the accepted `(claimer, paper_author, paper_permlink)` set is preserved. Round-1 `/ce-code-review` held 3 items, all landed round-2 (commit 9a3b2bd4): (1) equivalence scenario S6 (a stranger, not paper_author/bridge/admin/claimer, forges a `revoke_authorship` against an accepted hive-match claim; the revoke SIGNER gate rejects it so the claim stays accepted, differentially exercising the one gate the byte-identical proof exists to protect); (2) the three `authorshipClaimsCteBody` comment blocks reworded from "Mirrors reputation.ts accepted_claims" to single-source-of-truth language (the dedup deleted that inline copy); (3) the task-slug parenthetical dropped from the equivalence test header. Re-review `/ce-code-review` (11 personas; correctness/security/adversarial on Opus): clean. The architect independently verified S6 isolates the signer gate (mallory's revoke passes every condition except `rv.approver IN (...)`, block 205 > claim block 105) and that the revoke/approve signer gates and the list-final gate are byte-unchanged (only comments moved); the claimer-filter push into the `claim_events` scan is a net perf win. The vote-resolution 2x2-parity learning surfaced in the sibling display review is genuinely new and deferred to that task's clean archive via /ce-compound.
