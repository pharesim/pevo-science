export function initAccreditationBadge() {
  // This is a purely template-driven component; no JS logic needed.
  // The badge markup is inline in templates using the .badge-accredited CSS class.
  // This file exists for consistency with the component registration pattern.
}
