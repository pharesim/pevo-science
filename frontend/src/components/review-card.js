import Alpine from 'alpinejs';

export function initReviewCard() {
  // Review card is template-driven, rendered inside paper-detail's review loop.
  // All data comes from the parent x-for context.
}
